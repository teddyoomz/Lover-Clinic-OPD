# Session Handoff — LoverClinic OPD Cross-Session State

> **This file is read FIRST every new session.** Updated by `/session-end` skill.
> Link out to `.agents/sessions/*` for detail.

---

## 📏 HARD CAP: 200 KB (2026-05-17 EOD+2)

This file MUST stay **under 200 KB** at all times. When `/session-end` (or any
maintenance turn) detects size > 180 KB, the maintainer MUST:

1. Identify the oldest 5–10 session blocks (`### Session ...` headers)
2. Append them in **chronological order, oldest first** to
   `.agents/sessions/session-handoff-archive.md` (prepend NEW archived
   blocks at the TOP of the archive)
3. Delete them from this file
4. Verify resulting size < 150 KB (leaves headroom)

**Never read this file with `Read` without a `limit` parameter** if it could
exceed 256 KB — older sessions are in the archive. Current resume context
lives in the most recent ~12 session blocks below.

**Hard-cap origin**: 2026-05-17 EOD+2 turn — file grew to 317.5 KB across 150+
session blocks since 2026-04-26; `Read` started failing the 256 KB tool limit
during session boot. User directive: "ทำ SESSION_HANDOFF.md ให้ไม่มีวันเกิน
200 KB". Codified as permanent maintenance rule in `/session-end` skill.

---

## 🚨🚨🚨 RULE Q — REAL-ADVERSARIAL VERIFICATION (V66, 2026-05-14) — READ EVERY TURN 🚨🚨🚨

**TRUST COLLAPSED. PHASE 29 SHIPPED WITH 5+ USER-VISIBLE BUGS WHILE 8 LAYERS OF TESTS CLAIMED PASS.**

Mock tests are NOT verification. Admin-SDK doc-level access is NOT verification.
They are **CODE-SHAPE COVERAGE ONLY**.

**Before claiming ANY of these — "verified" / "shipped" / "tests passed" / "done" / "complete" / "ready to deploy" / "PR ready" / "approved" / "working" — for ANY user-visible code (UI, API endpoint with auth, Firestore query, etc.) — you MUST satisfy ≥1 level:**

- **L1 (PREFERRED)** — Playwright/real-browser drives the REAL deployed UI with real auth + real DOM + real Firestore side-effects
- **L2 (ACCEPTABLE)** — Real client SDK (NOT admin) issuing the EXACT compound queries / listener subscriptions the UI issues
- **L3 (LAST RESORT)** — User walkthrough with written confirmation ("ลองแล้ว work" / "ลองแล้ว พัง XYZ")

**FORBIDDEN** (Rule Q violations):
- `vi.mock('firebase/firestore')` + claim "verified"
- RTL with mocked listener data only
- Admin SDK `doc.get/set/batch.commit` + claim "compound query verified"
- `firebase firestore:indexes` returns N → claim "indexes ready" (deployed ≠ built; indexes take 2-30 min)
- Post-deploy probe = anon HTTP POST to one collection (not a compound query)
- "All vitest tests pass + build clean → shipped" (INSUFFICIENT for user-visible flows)
- "I tested for 5 min and found no bugs" (<5 min + 0 bugs → retest at higher level)
- Confirmation-bias test design ("write test that assumes correctness → green")

**Self-check** (run BEFORE any "verified" claim — any "no" or "I'm not sure" → DO NOT CLAIM):
1. Did I drive REAL browser OR real client SDK?
2. Did I issue the EXACT query the UI issues?
3. Did I actively TRY to BREAK my own code?
4. If <5 min testing + 0 bugs → did I retest at higher level?
5. Can I produce output log + screenshot proving the flow?

**Full text**: `.claude/rules/01-iron-clad.md` Rule Q (top-of-file) + `~/.claude/skills/real-adversarial-verification/SKILL.md` + V66 in `.claude/rules/00-session-start.md` § 2 + verbose entry in `.claude/rules/v-log-archive.md`.

**Origin**: V66 (2026-05-14) — Phase 29 trust collapse. User curse-verified: *"กูไม่เชื่อเทสที่ไม่น่าเชื่อถือของมึงแล้ว ... ทำยังไงก็ได้ให้ต่อไปนี้การเทสของมึงจะต้องไม่เหี้ย ไม่โกหก ไม่เข้าข้างตัวเองและใช้ไม่ได้จริง"*. EVERY FUTURE "VERIFIED" CLAIM MUST PASS L1 OR L2. NO EXCEPTIONS.

---

## Current State

- **Date (2026-05-31 EOD+4 LATE+4) — Brainstorm + spec + PLAN (confirmed-card highlight/reorder + course-step muted) + NEW rule ground-mockups-in-existing-design — NO CODE SHIPPED (implement next session)**: master HEAD = EOD docs commit (on `c5e3d47a`); prod UNCHANGED = `0c607f68` LIVE. Full `/brainstorming` (Visual Companion **grounded in REAL `TreatmentLifecycleStepper`/`AppointmentHubRowCard` source + real `index.css` tokens** — after the user scolded an un-grounded first mockup) → spec → `/writing-plans`. **Decisions locked: ①A** Frontend "วันนี้" confirmed appt card = sky-tint box (matches real sky "ยืนยันแล้ว" accent) + reorder confirmed-active to TOP of today tab (realtime via existing onSnapshot; NEW pure `sortApptsConfirmedFirst`) · **②A** OPD course step finished-but-not-deducted: amber "!ยังไม่ตัด" → muted "ไม่ตัดคอร์ส" (ONE SSOT `resolveCourseStepState` `'warn'`→`'not-deducted'` → affects BOTH Frontend card AND CDV history) · **③B** add course step to customer-detail treatment history, KEEP its teal/amber connectors. **③ root-cause VERIFIED (V139/V104 class):** `CustomerDetailView` `treatmentSummary` mapper (~L564) strips `detail` → plan computes `courseDeducted: resolveCourseDeducted(t)` in the mapper (raw `t` has `.detail`). **NEW iron-clad sub-rule** (user, angry — "ทำไมมึงไม่ดูดีไซน์เดิมกูก่อนค่อยทำตัวอย่างมา"): every design mockup MUST be grounded in the REAL existing design (provided screenshots + live app + exact source) BEFORE presenting — encoded 4 places (memory `feedback_ground_mockups_in_existing_design.md` + MEMORY.md + `.claude/rules/01-iron-clad.md` §S-design + `~/.claude/skills/brainstorming/SKILL.md`). All cosmetic-shell + 1 pure helper, frontend/lib only → no Probe-Deploy-Probe; no deploy this session. Spec/plan: `docs/superpowers/{specs,plans}/2026-05-31-appt-confirmed-card-and-course-step*`. Dev mockups `public/brainstorm-*.html` (DELETE at deploy, plan Task 7). Detail: `.agents/sessions/2026-05-31-brainstorm-confirmed-card-course-step.md`. **Outstanding (user):** implement next session; carryover L1 V142/V143 (2-device balance + NK shows 0) + cron stock-lot-cleanup (03:45 BKK).
- **Date (2026-05-31 EOD+4 LATE+3) — V143 stock cluster (show-0 + real-time + auto-clear) + held V142-quater/quinquies — SHIPPED + DEPLOYED**: master = prod = `0c607f68` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `8c3a9047`). Frontend/lib + 1 new cron → **no Probe-Deploy-Probe**. Two families deployed together: **V142** (course double-deduct: V142-quater over-credit gate + V142-quinquies finalize→doctor→finalize via persisted `_courseDeducted` flag; verified matrix 30/0 + flag-roundtrip/fuzz/stock **L2 e2e 30/0** real prod) + **V143 stock** — (a) **V143** StockBalancePanel show products drained/cleared to exactly 0 (`status∈{active,depleted}`; 7 NK products were hidden) **AV166**; (b) **V143-ter real-time** NEW `listenToStockBatchesByBranch` (Layer-1 onSnapshot BS-13 safe-by-default + Layer-2 wrapper; panel migrated to live listener) **AV167**, **L2 e2e `scripts/e2e-stock-balance-realtime.mjs` 5/0 real prod** (create/deduct/drain-0/delete from another surface push LIVE); (c) **V143-quater auto-clear-lot** pure `planLotCleanup` (keep live + ≤1 zero placeholder, DELETE-only) + daily cron `api/cron/stock-lot-cleanup.js` (03:45 BKK, vercel.json) + Rule M `scripts/stock-lot-cleanup.mjs` **AV168** (helper 14/0 + dry-run 0-redundant). **Rule M APPLIED on prod (earlier this session)**: `v143-nakhon-stock-reset.mjs --apply` (51 batches→0/0, deleted 364 movements/orders/adjustments) + `v143-collapse-nk-multilot.mjs --apply` (→ 53 products = 53 lots at 0); idempotent + audited. **3 V21 test-fixups** (v143 SG1 + v138 N10.7 + v34 INV.11.4 — all the V143-ter panel `listStockBatches`→`listenToStockBatchesByBranch` migration class; full suite confirmed no 4th). **Verified (Rule Q)**: full vitest **15418/0** (JSON-reporter run) + build clean (✓ 2.82s) + the L2 e2es above. **Honest gap**: assembled 2-device real-browser live-balance + NK-shows-0 = USER L1. Detail: `.agents/sessions/2026-05-31-v142-v143-deploy.md`. **Outstanding (user)**: L1 hands-on; cron now active (optional CRON_SECRET hit to verify like V122).
- **Date (2026-05-31 EOD+4) — V142 (course-deduct edit-resave SYMMETRY) + held V140 + V141 — SHIPPED + DEPLOYED + REAL-PROD-L2 VERIFIED**: master = prod = `8c3a9047` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `3342a9f0`). ONE combined commit (V140+V141+V142; SKILL.md AV160-AV163 spans all three; all frontend/lib). `/systematic-debugging` on a user-reported (verbatim, furious — prior tests "passed" while this slipped = the exact V104 simulate-only / Rule-Q-honest trap) course-deduction bug: "ซื้อแล้วตัดคอร์สเลย แล้วคอร์สมันไม่ตัดออกจากตัว". Frontend/lib only → **no Probe-Deploy-Probe**. **Root cause (confirmed from real prod data — diag `scripts/diag-course-not-deducted-bt.mjs` + `diag-cc-by-customer.mjs` on LC-26000115 / BT-1780203508072)**: edit-RESAVE reverse/re-deduct ASYMMETRY. `be_course_changes` recorded `use` qtyAfter "0/1" yet `customer.courses` stayed "1/1" (full). On a 2nd+ save, `handleSubmit` reverses the prior deduction (`oldPurchased` from `existingCourseItems`, refunds reliably + NO audit) then re-deducts the FRESH serialization (`backendDetail.courseItems`), EMPTY for purchased courses: (a) `purchased-…` rowIds regenerate to `be-row-N` (`mapRawCoursesToForm`) → Pass-1 miss; (b) `courseItems→treatmentItems` restore (TFP ~1158) drops productId → V101 Pass-2 can't run; (c) rem=0 → Pass-2 `rem>0` gate skips. **Refund-without-rededuct → balance reverts to full.** V12/V104 family at the EDIT-REVERSE boundary V104 never covered. **Stock parallel NOT affected** (gated by `hasStockChange` + `_resolveProductIdByName`). **Fix**: NEW pure `buildReDeductListWithCarryForward(fresh, oldReversed, selectedRowIds)` (treatmentBuyHelpers.js) → re-deduct = `fresh ∪ carry-forward(still-selected, not covered)` → reverse+re-deduct symmetric (un-check drops = correct; covered excluded = no double). TFP both sites `isEdit ? helper : freshExisting/freshPurchased` (create-mode unchanged). **AV163**. **Verified (Rule Q)**: **TRUE-L2 real-prod e2e `scripts/e2e-v142-edit-resave-course-deduct.mjs` 10/0** — SHIPPED `assign/deduct/reverse` + the SHIPPED helper on real Firestore: O.2 ★BUG REPRODUCED★ (pre-V142 → rem 1,1,1) + N.3 ★FIX VERIFIED★ (→ rem 0,0,0) + N.4 multi-edit + U.2 un-check + A.1 audit; `tests/v142-*` **20/0** (helper + Rule I simulate w/ REAL courseUtils arithmetic: C1 OLD-reverts/NEW-holds + C2 buy-no-deduct + C3 create-then-edit + C4 multi-edit + C5 un-check + C6 reordered + D rowId-regen + B source-grep); targeted course/deduct regression **543/0**; **full vitest 15336→15356/0**; build clean. **Heal dry-run** (`scripts/heal-course-reverted-by-edit-resave.mjs`): 35 scanned → **3 healable = LC-26000115** (`1/1→0/1` ×3) + 2 ambiguous = LC-26000009 (manual) + 0 false-positives. **Honest gap**: assembled real-browser multi-save on auth-gated AdminDashboard = USER L1. Detail: `.agents/sessions/2026-05-31-v142-course-deduct-edit-resave-symmetry.md`. **HEALS APPLIED**: V141 visitReasons 109 customers (audit `…eff2805c`); V142 course-heal = 0/no-op (LC-26000115 already `0/1` — a 2nd treatment `BT-1780214479261` re-deducted them in the bug window; ⚠ possible duplicate to review; LC-26000009 ambiguous=manual). **V142-bis (same session)**: user asked to test the single-save CREATE flow (ซื้อ+ตัด+คิดเงิน+เอายากลับบ้าน, กดบันทึกครั้งเดียว). Extracted the V101 two-pass courseItems IIFE VERBATIM → `buildCourseItemsForSave` (testability; behavior-identical, re-deploy optional); 2 V21 fixups (v104 SG3 + v101 A1/A2/A5 + v101 replica→real-helper). `tests/v142-bis-*` 8/0 + TRUE-L2 `scripts/e2e-v142bis-single-save-buy-deduct-charge-meds.mjs` 7/0 (real prod: course 1/1→0/1, Talafil stock 10→9, sale 2140, audit). Empirical: BT-1780203508072 CREATE save DID deduct → create path was never the bug. Full vitest **15364/0**. **V142-bis HELD uncommitted** (behavior-identical refactor). **Outstanding (user)**: commit V142-bis + L1 hands-on + review BT-1780214479261.
- **Date (2026-05-31 EOD+3) — V140 (staff-chat scroll + lightbox nav) + V141 (kiosk visitReasons preserve) — DONE + FULLY VERIFIED, UNCOMMITTED/HELD**: master HEAD = this EOD docs commit; prod UNCHANGED = `3342a9f0` (V138+V139, deployed earlier this session). **V140 + V141 SOURCE uncommitted in working tree** (6 mod + 4 new) — user ran `/session-end` without authorizing commit/deploy/heal. Both via `/systematic-debugging`. Frontend/lib only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V140 Bug1** — staff-chat กด Enter ไม่เด้งล่างสุด: `StaffChatMessageList` auto-scroll effect keyed on `[messages.length]` but the listener caps at 50 (`useStaffChat limitCount:50`) → past 50 msgs length is frozen → effect never re-fires. Fix = key on `lastMessageId` (ChatPanel `[messages]` = working ref). **AV160**. **V140 Bug2** — lightbox nav arrows `bg-white/15`+white icon invisible on white/light images → dark `bg-black/55 ring-1 ring-white/40 shadow-lg` (prev+next; ImageLightbox `bg-black/80` = ref); top-bar/PDF buttons gradient-protected (sanctioned). Rule Q-vis Chrome-MCP screenshot: visible on white/dark/blue/green/skin-tone. **AV161**. **V141** — kiosk intake→be_customers conversion folded `visitReasons`(array)→`symptoms`(string) + dropped `visitReasonOther`/`hrt*` (`kioskPatientToCanonical:153`); intake "สาเหตุที่มาพบแพทย์" (AdminDashboard:5039) + Clinical Summary read `visitReasons` → BLANK even though filled. **Rule R decisive**: opd_sessions visitReasons **100%** / be_customers **0/113** (incl. screenshot LC-26000115). Form ALREADY requires it (intake/deposit) → NOT a fill-bug, a conversion/display bug (Phase 26.2g class). Fix = 3-mapper triangle preserve 5 fields (kioskPatientToCanonical + buildPatientDataFromForm + buildFormFromCustomer; **snake_case canonical** to honor Phase 23.0 no-camelCase-on-root + camelCase patientData for readers; covers create + admin-edit-no-clobber). **AV162**. **Verified**: full vitest **15336/0** (700 files) + build clean + V140 8/0 (+lightbox/chat families 173/0) + V141 9/0 (+mapper families 157/0; updated v55-1 byte-identical snapshots + phase-23-0 camelLeak — legit contract). **V141 heal dry-run (Rule M): 109/113 recoverable** (`scripts/heal-visit-reason-from-symptoms.mjs` — split symptoms, validate vs VISIT_REASON_VALUES so admin free-text isn't corrupted; `--apply` GATED). **Honest gap**: mapper/data proven; assembled real-browser (chat-scroll on a 50+ thread; healed-customer intake bullets) = USER L1. Detail: `.agents/sessions/2026-05-31-v140-v141-chat-scroll-lightbox-visitreasons.md`. **Outstanding (user)**: commit V140+V141 → deploy → heal `--apply` → L1.
- **Date (2026-05-31 EOD+2) — V139 (OPD course-step + appt status↔tab sync) + V138 (negative-batch status invariant) SHIPPED + DEPLOYED + HEALED**: master = prod = `3342a9f0` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `409804fc`). Frontend/lib only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. ONE combined commit (V138+V139 share `backendClient.js` + audit SKILL.md). Full cycle `/brainstorming (Visual Companion; Q1=B warn "ยังไม่ตัด" / Q2=A violet / Q3=A symmetric) → spec → writing-plans → executing-plans (inline, 8 tasks)`. **V139 Feature 1** — opt-in 4th "คอร์ส" step on the นัดหมายวันนี้ card stepper (violet ✓=ตัดคอร์ส / amber "ยังไม่ตัด"=เสร็จแต่ไม่ตัด — catches staff who forget to deduct; ซื้ออย่างเดียว≠ตัด); SSOT `resolveCourseDeducted` reads `detail.courseItems`/`treatmentItems` (Rule R diag: top-level=0 on prod, 57 deducted / 24 warn-case of 88); opt-in `withCourseStep` → CDV history stays 3-step; live via existing `listenToTreatmentsByDateRange`. **V139 Feature 2** — couple `appt.status` ↔ `serviceCompletedAt` at 3 `backendClient.js` chokepoints (mark→status:'done', unmark→status:'confirmed', `updateBackendAppointment` via pure `decideApptStatusServiceSync` stamp/clear on done-boundary) → กำลังรอ/✓เสร็จแล้ว tab follows the status dropdown from ANY surface (Frontend modal / Backend appt tab), real-time cross-device via existing onSnapshot; `serviceCompletedAt` stays the tab SSOT (filter UNCHANGED, no migration); also FIXES the mark-complete button gate. **NEW AV159** (course SSOT + status-sync coupling). **V138** — `resolveBatchStatusForRemaining` (only `remaining===0`=depleted; negative=active DEBT, visible+repayable) wired 6 sites; anti-negative verified (ติดลบได้แค่ TFP+sale); AV158. **Rule M heal APPLIED on prod** — 3 batches flipped depleted→active (Augmentin −91, คอนฟอร์ม 2 นิ้ว −3, E.P.T.Q S500 −12 @ นครราชสีมา; audit `heal-negative-batch-1780208334449-895a86e6`; idempotent re-run = 0 → reappear in ยอดคงเหลือ). **Verified**: full vitest **15319/0** (698 files, +43 V139) + build clean + **2 TRUE-L2 e2e on real prod** (V139 `e2e-v139-status-sync-course-step.mjs` 13/0 — shipped coupling fns + resolveCourseDeducted on seeded AND real docs; V138 12/0) + 4 V21 fixups + theme-AA via Chrome MCP (violet + amber "ยังไม่ตัด" AA both themes, 4 dots fit 360+300px narrow). **Honest Rule Q gap**: the ASSEMBLED real-browser flow on the auth-gated AdminDashboard = **USER L1** (deduct a real course → course dot lights live on the card; mark-complete/edit-modal-status cross-surface → card hops tab live cross-device — harness can't drive auth-gated multi-device). Detail: `.agents/sessions/2026-05-31-v139-opd-course-step-status-sync.md`. **Outstanding (user)**: L1 hands-on prod (V139 + V138); pre-existing extended-suite 280 stale tests (NOT deploy-gating).
- **Date (2026-05-31 EOD+1) — V138 negative-batch status-flip FIX — DONE + FULLY VERIFIED [SUPERSEDED — shipped+deployed+healed 2026-05-31 EOD+2, see above]**: master HEAD = this EOD docs commit (on `06e0fca8`); prod UNCHANGED = `409804fc` LIVE. **V138 SOURCE is uncommitted in the working tree** (4 mod + 4 new) — user ran `/session-end` without authorizing commit (commit/heal/deploy flagged gated). `/systematic-debugging` + Rule P. **Bug**: ปรับเพิ่ม (ADJUST_ADD) a NEGATIVE stock batch made it VANISH from "ยอดคงเหลือ" — `createStockAdjustment` used `afterRemaining <= 0 ? DEPLETED : ACTIVE` → −13 +1 = −12 → flipped to `depleted` → excluded from StockBalancePanel's `status:'active'` query AND `_repayNegativeBalances`'s `status:ACTIVE` sweep (compounding: debt unrepayable + invisible). Movement log correct (before=−13/after=−12) — matched user's E.P.T.Q S500 report. **Invariant**: `remaining < 0 = active DEBT (visible); only === 0 = depleted`. **Fix**: NEW `resolveBatchStatusForRemaining(remaining)` (`stockUtils.js`) wired to 6 backendClient sites (adjust + deduct-loop + negative-push + transfer/withdrawal export + repay); `_reverseOneMovement` revive-only sanctioned. Enables "บวกติดลบทีละนิด". **Anti-negative guard (user: ติดลบได้แค่ TFP + การขาย)** = verified ALREADY-correct structurally (negative push gated `context==='treatment'||'sale'`, only 2 callers; ปรับลด/โอนออก/เบิกออก throw; other-context shortfall throws) — NO code change, locked with **AV158** + tests. **Verified**: tests/v138-* 34/0 + v34 V21 fixup 61/0 + **full vitest 15276/0** (695 files) + build clean + **TRUE-L2 real-prod e2e 12/0** (`scripts/e2e-negative-batch-directions.mjs` — −13→−12 stays active + VISIBLE in `listStockBatches({status:active})`; ปรับลดบล็อก; TFP+sale ติดลบได้; zero-orphan). Rule R diag: 3 prod batches wrongly depleted (Augmentin −91, คอนฟอร์ม 2 นิ้ว −3, E.P.T.Q S500 −12 @ นครราชสีมา); heal dry-run = 3 (`scripts/heal-negative-batch-wrongly-depleted.mjs`, `--apply` GATED). Detail: `.agents/sessions/2026-05-31-v138-negative-batch-status-invariant.md`. **Outstanding (user)**: heal `--apply` (Rule M) + commit V138 source + deploy (frontend-only, no Probe-Deploy-Probe) + L1 hands-on.
- **Date (2026-05-31) — V135 + V136 + V137 SHIPPED + DEPLOYED + prod-verified**: master = prod = `409804fc` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; first CODE deploy since prod 6c99a3d7). ALL frontend/lib/CSS only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V135** — reports-remaining-course customer name → `openCustomerInNewTab` (`?backend=1&customer=`); was plain text. Rule P sweep: sole report tab missing the link (other 6 already had it). `tests/v135-*` 54/0. **V136** (`/systematic-debugging` + `brainstorming` Q1=A no-course-deducted / Q2=A course-section-only / Q3=B existing-use-only-no-buy → spec+plan HTML → exec): TFP edit ข้อมูลการใช้คอร์ส retroactively ONLY when the treatment deducted NO course — NEW `canEditCourseUsageRetro = isEdit && !canAddNewItems && loadedHasNoCourseUsage` (courseItems AND treatmentItems both empty at load); NEW `saveMode='course'` = staff-save MINUS the auto-sale (course balance + branch stock deduct **byte-identical** to staff; both auto-sale gates carry `&& saveMode !== 'course'` → no createBackendSale/INV/wallet/deposit/points; forensic status patch, preserves status/completedAt). ซื้อ buttons + consumables/meds stay `canAddNewItems`. **AV156**. **V137** — staff chat: http/https URLs → clickable new-tab links (`parseMessageBody` 'url' segment FIRST in the alternation so URLs containing LC-/BA- capture whole; `StaffChatMessageBody` `<a target=_blank rel=noopener>`; scheme-restricted → no XSS; sky link AA both themes; trailing-punct strip). **AV157**. **Verified**: full vitest **15199/0** (V136 impl turn) + targeted **350/0** (V136 + deduct/stock siblings: phase-26-0-doctor-save, course-skip, v104, phase-17-2-octies, treatment-stock-diff, v36-stock-e2e, v96 — shared deduct/stock code intact) + V135 54/0 + V137 43/0 + build clean + **V136 TRUE-L2 e2e on REAL prod 14/0** (`scripts/e2e-v136-course-retro-deduct.mjs` — custom-token admin-claim auth → SHIPPED `deductCourseItems`/`deductStockForTreatment`/reverse: course 5→4, branch batch 10→9 + TREATMENT(6) movement stamped to selected branch, branch isolation, reverse/re-save round-trip no drift, NO be_sales, kind='use' audit, zero-orphan cleanup). **Honest Rule Q gap**: TRUE-L2 proved the shipped fns on real prod; UI-click L1 in real browser (all 3) = user hands-on. Detail: `.agents/sessions/2026-05-31-v135-v136-v137-clickables-retro-course.md`.
- **Date (2026-05-28 EOD+6) — V132 + V133/V133-bis + V134 (reports-revenue: course category · chart · deposit display) SHIPPED + DEPLOYED + prod-verified**: master = `6c99a3d7` (+ EOD docs commit); prod LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased). Frontend/lib/CSS only → **no Probe-Deploy-Probe**. Four `/systematic-debugging` rounds, all reports-revenue, user-driven while viewing the tab. **V132**: course หมวดหมู่ showed "ไม่ระบุ" for every row — aggregator + filter dropdowns read legacy `category_name`/`category` off RAW be_courses docs that store `courseCategory`; NEW `courseDisplayResolvers.js` (canonical-first `resolveCourseCategory/ProcedureType/DisplayName`; reads live `be_courses.courseCategory` → future categories auto-surface, no enum). AV153. **V133/V133-bis**: RadialBars legend %s summed ~279% (read value/max) + arcs overflowed the SVG (distorted spiral) → NEW pure `computeRadialBarLayout` (legend %=value/total Σ≤100%; arc=value/max → biggest fills the ring "ดูเต็มๆ", rest scale down; bar thickness fits radius budget) + max-distinct interleaved palette. AV154. **V134**: หักมัดจำ/ยอดชำระเงิน showed manufactured fractions (4,941.35) — report split ROUND sale deposits proportionally across course lines; money was ALREADY correct (real-prod Rule R: 8,000 = 8,000, conserved) → per user: GROSS per course row + deposit/wallet/refund = sale-level FOOTER summary + net. AV155. Also fixed pre-existing broken imports in `tests/extended/phase10-revenue.test.js` (`../`→`../../`) → **27/27**. **Verified**: default suite **15154/0** + build clean; real-prod diags; chart full-bar + 10 distinct colors + 100% legend measured in REAL browser (screenshot tool timed out — infra; DOM/geometry-measured). **Extended-suite spawned WIP — HANDLED + committed**: NEW `vitest.extended.config.js` + `package.json` `test:extended/test:all` switched to `--config` + 148 import-path fixes (`../`→`../../`, incl. vi.mock; 395/395 symmetric, no logic); `_tmp` codemod deleted. `npm run test:extended` RUNS now → **4419 pass / 280 fail**; the 280 are PRE-EXISTING stale-assertion drift (suite frozen since session-11 + ~1yr refactors: V50 ProClinic strip / branch-scope / V132 / Phase 28 / marketing tabs) — NOT import errors (files load + partially pass), NOT in the prod deploy, NOT deploy-gating. **Follow-up (large): triage → rewrite/delete obsolete extended assertions.** Default `npm test` = **15154/0** (deploy gate) unaffected. Detail: `.agents/sessions/2026-05-28-reports-revenue-category-chart-deposit.md`.
- **Date (2026-05-28 EOD+5) — V130 (createdBy + responsive table) + V131 + V131-bis SHIPPED + DEPLOYED + prod-verified**: master = prod = `c070123d` LIVE @ lover-clinic-app.vercel.app. **Two deploys this session**: `bd925fe8` shipped the held V128/V129/V130 batch; `c070123d` shipped V131 + V131-bis. ALL frontend/lib/CSS only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V130** (`brainstorming → spec → writing-plans → executing-plans`): sale `createdBy` true-capture at the `createBackendSale` chokepoint (`_resolveSaleCreatedBy` via be_staff firebaseUid; AV149) + reports-sale compact + reachable-scroll table (AV148) + Rule-M backfill script (held). **V131** (`/systematic-debugging` ×3, real-prod diag): HN class-of-bug — HN display/search reads used a hardcoded `proClinicHN` subset omitting `hn_no` (109/109 real HNs live there) → canonical `resolveCustomerHN` across 6 files; report blank-HN **6→0**, CustomerDetailView HN badge restored (AV150); table fills viewport height; appt-detail MODAL name clickable → `?backend=1&customer=` new tab (AV151). **V131-bis** (`/systematic-debugging` — root cause = browser **Caret Browsing / F7**, user-confirmed): `body{cursor:default}` + `html{caret-color:transparent}` + inputs restored → hides the caret-browsing `|` everywhere except real inputs, every page/refresh; copy preserved (AV152). App CANNOT toggle the browser caret-browsing setting — it HIDES the caret app-wide (same visible result). **Verified**: full vitest **15106 pass + 1 isolated flake** (staff-chat-lightbox 200-iter stress, 7/0 isolated, unrelated); build clean; HN L2-confirmed real prod; caret/cursor confirmed via live-prod computed-style + user F7 test. **Outstanding (user-triggered)**: V130 backfill `--apply` (49 legacy sales createdBy = first seller, tagged; OPTIONAL) + L1 hands-on. Detail: `.agents/sessions/2026-05-28-v130-v131-reports-caret.md`.
- **Date (2026-05-28 EOD+4) — 4-FEATURE BATCH (V128 + V129) DONE + verified — UNCOMMITTED + HELD (no deploy)**: working tree = **17 modified + 7 new files; feature code NOT committed** (held per user "ยัง deploy ไม่ได้ มีงานถัดไป" — only agent-docs committed this EOD). master code HEAD `10e28ed4` (V127 docs); prod `26fb5789` (V127) — **none of the 4 deployed**. All 4 are frontend/lib only (no rules/storage/data/cron → no Probe-Deploy-Probe → ONE combined `vercel --prod` covers all). ① **V128 appt phone** — hover-peek + detail-modal show the LINKED customer's phone (write-chokepoint `_resolveAppointmentCustomerPhone` in backendClient create+update + render live-resolve `useResolvedApptPhone` + `apptPhoneValue||resolvedPhone`); L2 real-prod 78 linked + 45 temp = 0 blank; AV145. ② **V128 calendar auto-height** — `AppointmentCalendarView` fixed `SLOT_H=22` → dynamic `slotH` (`computeApptSlotHeight`, fills viewport, clamp 22–46). ③ **V128.lb staff-chat lightbox** — full-screen (`width/height:100%` of `w-full h-full` wrapper + object-contain UPSCALES small imgs; was `max-w-4xl` 896px) + zoom + **drag-to-pan** (clamped) + wheel-zoom; **Chrome-MCP real-browser verified** (fill+zoom+pan); AV146. ④ **V129 reports-sale** — `tab=reports-sale` พนักงานขาย + ผู้ทำรายการ resolve via `resolveSellerName(listAllSellers)` (aggregator + SaleReportTab + SaleDetailModal); L2 real-prod `-` count **38→0**; AV147; `be_sales.createdBy` never written → ผู้ทำรายการ = resolved first seller. **Tests (no re-run at EOD per rule)**: last FULL 15039/0 (pre-lightbox+reports-sale); targeted since GREEN — lightbox-family 104/0 · v128 7/0 · v129 12/0; build clean; full re-run pending at deploy. Detail: `.agents/sessions/2026-05-28-v128-v129-batch.md`. **When user says commit/deploy: full `npm test -- --run` → commit batch → `vercel --prod`.**
- **Date (2026-05-28 EOD+3) — V127 appointment hover-detail peek — SHIPPED + DEPLOYED**: code `26fb5789` LIVE @ lover-clinic-app.vercel.app (+ docs commit on top). Frontend-only (no rules/storage/data/cron → **no Probe-Deploy-Probe**). Full `/brainstorming (Visual Companion: Q1=A anchored peek / Q2=A keep-modal-desktop-hover-only / Q3=A all-present-fields / size=XL via live M/L/XL compare) → spec → writing-plans → executing-plans (6 tasks)`. **Feature**: hovering an appointment card (desktop mouse) shows an anchored **XL 345px** peek-card with all present details — no click needed. Covers EVERY appointment calendar sub-tab via the shared `AppointmentCalendarView` + its agenda variant. Click→`AppointmentDetailPopover` modal + touch UNCHANGED (peek guarded to `pointerType==='mouse'`). **Architecture**: NEW `useApptHoverPeek` hook (150ms open / 80ms close-grace, rect capture, scroll/resize dismiss, desktop-only) + NEW shared `AppointmentDetailBody` (modal + peek BOTH render it → no field drift, V12 lesson) + NEW portal-anchored `AppointmentHoverPeek` (flip/clamp, `pointerEvents:none`, theme-aware via `--bg-card`/`--tx-*` + `appointmentDisplay` status SSOT) + `AppointmentDetailPopover` refactored onto the body (behavior-preserving + type chip) + **AV144** invariant. Hub rows DELIBERATELY excluded (already full-detail inline). **VERIFIED**: build clean · full vitest **15009 pass / 1 known flake** (`phase-17-1-cross-branch-import-rtl` global.fetch-leak, **7/0 isolated** — not V127) · touched-area **49/0** (modal RTL 14/0 + 3 V21 source-grep fixups in `appt-calendar-density` where additive `closePeek`/`getHoverProps` broke literal asserts) · peek theme **AA in BOTH themes** (live computed-color scan: name 16:1/19:1, phone red-700/red-300, service emerald-700/emerald-300, card-bg adapts `#f8fafc`/`#0f0f0f`) + light screenshot. **HONEST GAP (Rule Q)**: the *assembled* hover-on-the-real-calendar (peek beside a real grid cell on mouse-hover, flip/dismiss, no flicker between dense cells) = **USER L1** (admin-gated + real mouse; harness can't drive a trusted hover on the mounted calendar). Spec/plan: `docs/superpowers/{specs,plans}/2026-05-28-appt-hover-detail*`. Outstanding (user): V127 hover L1 + V126 full-form L1 + appt-live/chart multi-device L1.
- **Date (2026-05-28 EOD+2) — V126 PatientForm light-theme WCAG-AA (B-i Selective) — SHIPPED + DEPLOYED + prod-verified**: master=`afd4a628` (docs) / code `4ed43920` · prod LIVE @ lover-clinic-app.vercel.app. CSS/theme + JS only (no rules/storage/data/cron → **no Probe-Deploy-Probe**). Closes the **V125-deferred** customer-facing intake form. Full `/session-start → brainstorming (Visual Companion; Q1=B curated palette / Q2=B-i pink/rose harmonized / Q3=Selective) → spec → writing-plans → executing-plans (6 tasks)`. **Fix**: reuse V125 `aaAccent` (NO new helper — map already has every shade) + NEW `.pf-req` asterisk class (rose-600 light `#e11d48` 4.7:1 / ember-red dark `#ef4444` 5.27:1; unifies 21 spans) + `acLight = aaAccent(ac, isDark)` (submit/lang-toggle/icon → white-on-pink-700 light, `ac` unchanged dark) + ~10 inline-hex accents deepened (orange→`aaAccent`; back-btn/cancel/caption/greeting/state-icons → AA literals). **KEPT** orange-emergency/blue-custom/red-critical semantic zones (already AA via V124) + decorative pinks/gradients + LINE-green `#06C755` button (brand exception); dark unchanged. **VERIFIED**: build clean · full vitest **15001/0** · T7 **30/0** (10 new PF-1..PF-10: AA-math + source-grep) · live `.pf-req` cascade on BOTH local + the **LIVE deployed build** (Rule Q-vis post-deploy re-scan, V125 gold-standard: light `#e11d48` / dark `#ef4444`) + Invalid-Link state scan + screenshot. **HONEST GAP (Rule Q)**: main intake form + success/closed/expired state-screens NOT seen assembled (need real session/admin-sim — auth+PHI); colors verified via source-grep (0 light-branch raw sub-AA hex) + AA-math + `.pf-req` cascade-probe + inline-always-wins → **full-form 5-state visual L1 = USER post-deploy**. Spec/plan: `docs/superpowers/{specs,plans}/2026-05-28-patientform-light-theme-aa*`. Outstanding (user): full-form L1 + appt-live/chart multi-device L1.
- **Date (2026-05-28 EOD+1) — V125 LIGHT-THEME WCAG-AA (treatment form) — SHIPPED + DEPLOYED + prod-verified**: master=prod `f56bfa9b` LIVE @ lover-clinic-app.vercel.app. CSS/theme + 1 new lib + listener-swap ONLY (no rules/storage/data/cron → no Probe-Deploy-Probe). Treatment-form L1 scan FOUND **19 light-theme AA fails V124's class-based CSS couldn't reach** (the one surface V124 never individually scanned). Fixed 2 classes: (1) inline `style={{color:'#…500'}}` accents → NEW `aaAccent(hex,isDark)` (`src/lib/themeAccent.js`) deepens -500/-400→-700 in light, pass-through dark — wired SectionHeader/ActionBtn + 12 spans + ChartSection + TreatmentTimeline; (2) doctor-note save btn `bg-[#7c3aed] text-white` darkened to 3.05:1 by V124 blanket `.text-white→dark` (white-restore matched only Tailwind `bg-{c}-`) → index.css white-restore for the arbitrary hex (→5.2 AA; teal #2EC4B6 / LINE-green #06C755 left dark, already AA @7.7). Also re-proved 3 outstanding L1 live (appt real-time 2→3/3→2 cross-process; chart relay PC-side). **VERIFIED (Rule Q-vis)**: T7 20/0 (AA-math + source-grep) + full vitest 14990 pass + 1 known flake (phase15.5b, 51/0 isolated) + build clean + **post-deploy re-scan LIVE build: treatment form 0 fails (1372 els) + sale/finance 0 (2186 els, no-regression) + zoom (violet white, headers deepened)**. Wiki+graphify updated. Outstanding (user): PatientForm light-theme DESIGN pass (bespoke brand) + appt-live/chart multi-device L1. Detail: `.agents/sessions/2026-05-28-v125-light-theme-aa.md`.
- **Date (2026-05-28 EOD) — V124 LIGHT-THEME INVISIBILITY + APPT REAL-TIME + ดัน AA เต็ม — SHIPPED + DEPLOYED + prod-verified**: master=prod `38bd3565` LIVE @ lover-clinic-app.vercel.app (`77757f67` V124 + `d106a5cd` fix2 base-match + `4c751bf5` docs + `38bd3565` fix3 full-AA). `/systematic-debugging` 2 bug classes; CSS/theme + component-color + listener-swap ONLY (no rules/storage/data/cron → no Probe-Deploy-Probe). **Bug A (light-theme invisibility)** finance "มองไม่เห็นอะไรเลย": bg-tint `[class*="bg-{c}-{700..950}/"]` matched `dark:` variants → clobbered solid `bg-{c}-600 text-white` badges to pale → INVISIBLE (~1.05:1). Fix = BASE-match `[class*=" bg-X/"]+[class^="bg-X/"]` (v1 `:not` over-excluded base+`hover:` → stock 3.64:1 regression caught post-deploy → v2). + gray-600/700→muted/heading (were faint #94a3b8), orange-500→700, `-400`/`-600` colored-text→AA, date-strip selected tab (label/number 1.0/3.01:1 invisible → white-on-blue via bg-sky-700/600 descendant restore), count badge sky-500→600. **fix3 ดัน AA เต็ม**: white-on-`bg-{c}-600`→`{c}-700` light-only → finance/CTAs strict AA (finance 0 fails). **Bug B (appt real-time)** `AppointmentCalendarView` month-strip one-shot `getAppointmentsByMonth` → `listenToAppointmentsByMonth` (onSnapshot, cross-device, per-branch; all 6 backend appt sub-tabs; day-grid already live). **VERIFIED PROD (Rule Q-vis + L2)**: finance/stock 0 fails · date-strip readable · Bug B node-write→strip 2→3 LIVE no-refresh + delete→3→2. full vitest 14983/0 (last full) · T7 13/0 · BS-F 21/0 · build clean. Outstanding (user): treatment-form not individually scanned (deep PHI; same global classes = covered) + appt-live/chart prod L1. Detail: `.agents/sessions/2026-05-28-v124-light-theme-appt-realtime.md`.
- **Date (2026-05-27 EOD+13 → 2026-05-28) — LIGHT-THEME WCAG-AA AUDIT (App UI) + BRAND-RED red-700 — SHIPPED + DEPLOYED + prod-verified**: master HEAD `3605f284` (ship a4731775+9042934a+9f6cc49d + SESSION_HANDOFF archival 3605f284); pushed; `vercel --prod` LIVE `9042934a` (was 8f6b7ced) + prod-verified (--accent-red #b91c1c). **CSS/colour/theme-config ONLY — 0 wiring/flow/logic** (diff = index.css central-sweep block + tailwind.config 1-line darkMode + 14 inline-colour swaps in 5 .jsx). NO firestore.rules/storage/data/cron → frontend+theme-config (no Probe-Deploy-Probe). Fixes: FM-A uncovered dark surfaces→light tokens · FM-C colour-text -300/400/500→AA-dark · alert-box ext (17 colours, bg-{c}-700..950/alpha→{c}-50 + text-{c}-50..300→{c}-700) · FM-D 14 inline → `--accent-{blue,line,purple,amber,red,teal}` vars (5 files) · teal/muted/green token darkening · **tailwind darkMode root-fix** `['selector','[data-theme="dark"]']` (was unset=media → `dark:` coupled to OS → light-on-dark-OS showed dark-variant colours invisible; AV136 class, fixed globally). NEW `tests/light-theme-override-coverage.test.js` (T7 5/0, incl. strict-AA brand-red guard) + TL1.6 fixup. **Verified (Rule Q-vis, real browser measured)**: per-page contrast scanner CLEAN on 7 surfaces (sale·reports·promotions·finance-synthetic·appointment-hub·customer-detail·frontend-appt) · darkMode fix proven both directions · dark NO regression. **brand-red RESOLVED (user chose darken)**: `.text-red-400` (261) + `--accent-red` light → red-700 `#b91c1c` (was 4.37:1 sub-AA on tint → 6.47:1 white). **HONEST GAP**: 6 surfaces (stock/master-data/treatment-form/chat/settings/deep-modals) NOT individually scanned (nav resisted programmatic clicks) — use the SAME now-fixed global classes → covered by global fixes + need user L1 (dev up localhost:5173). full vitest **14976/0** · build clean. **SESSION_HANDOFF archived** 277→142 KB (46 oldest → archive). **Outstanding (user-triggered)**: user L1 on the 6 surfaces + appt-live/chart prod flows (first prod exposure since EOD+11/EOD+12). Detail: checkpoint `.agents/sessions/2026-05-27-light-theme-audit.md`.
- **Date (2026-05-27 EOD+12) — CHART-EDIT FLASH FIXED (createPortal) + treatment-image ดูรูปใหญ่ button + light-theme button fix (LOCAL, committed `96eb089d`, NOT pushed/deployed)**: master HEAD = EOD docs commit (feature `96eb089d`); prod UNCHANGED `8f6b7ced`. NO firestore.rules/storage/data/cron → frontend-only (no Probe-Deploy-Probe). Full suite **14971/0** (677 files) · build clean ×2. `/systematic-debugging`: clicking แก้ไข Chart in TFP flashed the editor in-box ("ไม่หลุดจาก box ตัวเอง") then full-screen — `ChartCanvas` `fixed inset-0 z-95` was trapped by a TRANSIENT transformed ancestor in TFP (static chain has NO persistent transform → transient → flash). Same class as **AV117** ("fullscreen overlay MUST createPortal; the fix is the portal, not find-the-ancestor") — ChartCanvas (an editor) was never in AV117's list. **Fix**: `createPortal(…, document.body)` on ChartCanvas + ChartTemplateSelector + PcPairingModal (Rule P siblings) → no ancestor trap → full-screen frame 1, no flash; NEW **AV143** (editor/modal overlays). **Task 2**: NEW shared portaled **ImageLightbox.jsx** (extracted ChartSection.ChartLightbox, Rule of 3) + `Maximize2` "ดูรูปใหญ่" button on every treatment + lab image. **Light-theme bug** (user 2nd report): zoom/delete `bg-black/70 text-white`→`bg-white/90`+gray/red icon — `index.css:404` remaps `.text-white` dark on non-colored bg (white-restore exceptions exclude `bg-black`) → white icon rendered dark → invisible on dark button; **CSS-proven in real browser light theme** (old icon `rgb(15,23,42)` dark-on-dark; new gray-on-white visible). NEW `tests/v123-chart-overlay-portal.test.js` (13) + v117→ImageLightbox + pc-pairing PP4 portal fixup. **Outstanding (user-triggered)**: push master + deploy vercel-only (covers EOD+11 appt-live + EOD+12 chart — all frontend; await word V18); in-app live-TFP-edit L1 (flash gone + button visible+opens) = user hands-on (login/data; mechanism AV117-proven + CSS-proven, render RTL-correct); 2 pre-existing Rule S doc edits (CLAUDE.md, rules/01) uncommitted. Detail: checkpoint `.agents/sessions/2026-05-27-chart-overlay-portal-flash.md`.
- **Date (2026-05-27 EOD+11) — APPOINTMENT PAGE LIVE CROSS-DEVICE + CC FIELD ROW-ALIGN (LOCAL, committed, NOT pushed/deployed)**: master HEAD `0c702091` (2 commits above EOD+10 `4b8e3123`) — NOT pushed; prod UNCHANGED `8f6b7ced`. NO firestore.rules/storage/data/cron → frontend-only (no Probe-Deploy-Probe when deployed). Full suite **14958/0** · build clean · **Rule Q L1 real-browser pixel (2-window cross-device demo) + L2 real-prod onSnapshot 18/18**. **(1) CC row-align** (`7857a2dd`): TFP left col `space-y-4`→`flex flex-col gap-4` + teal save `mt-auto` → vitals/doctor buttons same row (root cause real-browser-measured: block-vs-flex trailing-`mb-3` ~12px; bumping CC `rows` is a NO-OP under flex-1; cosmetic only). **(2) Live cross-device** (`0c702091`): NEW `listenToTreatmentsByDateRange` + `listenToAllDeposits` (backendClient) + scopedDataLayer re-exports; AppointmentHubView subscribes 3 onSnapshot triggers → `liveRefreshTick` (skip-first) → existing `loadAll({silent})`; extends the proven `appointmentDataVersion` pattern; + day-rollover/resume guard; NO logic/mutation/render touched. **V66 trap caught pre-ship**: a branch-scoped sales listener = `where(saleDate>=)+where(branchId)` = composite index that does NOT exist → would FAIL_PRECONDITION in prod (admin-SDK can't catch — the V66 lesson) → fixed to sales `allBranches` saleDate-only (single-field); treatments whole-collection + deposits single-field → all index-free. +12-test bank (source-grep + flow-sim) + 6 RTL partial-mock fixes (V11-class: mock missing new exports) + `scripts/e2e-appointment-live-cross-device.mjs` (L2 18/18 real prod, kept). Detail: checkpoint `.agents/sessions/2026-05-27-appt-live-cross-device.md`. **Outstanding (user-triggered)**: push master (2 commits) + deploy vercel-only (await explicit word, V18); 2 pre-existing Rule S doc edits (CLAUDE.md, rules/01) still uncommitted.
- **Date (2026-05-27 EOD+10) — DEPOSIT-LIST APPT-DATE + OPD STEPPER POLISH + LIGHT-THEME CONTRAST SWEEP — SHIPPED + DEPLOYED**: master = prod = `8f6b7ced` LIVE (`vercel --prod`, aliased lover-clinic-app.vercel.app); the EOD+9 11-commit stack pushed too. NO firestore.rules/storage/data/cron → vercel-only deploy, NO Probe-Deploy-Probe. Full suite **14942/0**, build clean. **(1)** Deposit list (Finance→มัดจำ): appt date under "มัดจำสำหรับ" — clickable "นัด <date> · <time>" (reuses goto-appt nav) / "ยังไม่นัด" hint; pure display (dep.appointment.date already on doc). **(2)** OPD stepper (Appointment tab): center-align (justify-center) + Ember footer band (theme-aware); shared TreatmentLifecycleStepper untouched (no ripple). **(3)** Light-theme contrast sweep (deposit+appt zone): badges/chips/link-buttons/action-icons/VisitPurposePicker selected-chips/create-appt-modal → theme-aware (active/selected = solid bg + white text); AppointmentHubRowCard + OpdLifecycleRow already theme-aware. +3 tests, 2 V21 fixups (purpose-cell source-grep window greedy→non-greedy). Commit `8f6b7ced`. **Lesson**: Vite dev HMR got STUCK after rapid edits → render showed STALE (pale) while source was already correct → many wasted "still not clear" rounds; browser hard-reload didn't help (server-side transform cache); **dev-server restart fixed it** (Rule Q-vis: when render contradicts grep-confirmed source, verify on a FRESH render). Detail: checkpoint `.agents/sessions/2026-05-27-light-theme-deposit-appt-polish.md`. **Outstanding**: 2 pre-existing Rule S doc edits (CLAUDE.md, rules/01) still uncommitted (user's); OPD ember live-pixel deferred (no checked-in customer in data renders the footer band).
- **Date (2026-05-27 EOD+9) — DEPOSIT-WITHOUT-APPOINTMENT + FINANCE DEPOSIT MODERNIZE (LOCAL, NOT pushed/deployed)**: master tip `ff9a775d` (+ EOD docs commit) — **10+ commits ahead of origin/master `9209ec70`, NOT pushed**; prod UNCHANGED = `0805da87` (V122), feature NOT deployed. NO firestore.rules/storage/data/cron touched → frontend+serverless only (no Probe-Deploy-Probe when deployed). Full suite **14929/0** · build clean · **Rule Q L2 real-prod e2e 21/0** (`scripts/e2e-deposit-no-appointment.mjs`). **Part 1**: AppointmentFormModal `ไม่นัดหมาย` toggle (deposit-booking only) → hides appt fields (date/time/หมอ/ห้อง/recurring) + skips appt validations → writes deposit-only doc (advisor→100% seller; purpose=appointmentTo; +สร้างนัด later works). **Part 2**: Finance DepositPanel `เลือกลูกค้าภายหลัง` + `มัดจำสำหรับ` (VisitPurposePicker new `label` prop) + table `|| dep.purpose` fallback; all money fields kept (Q2 port-all). be_deposits +3 fields (purpose/customerNameTemp/customerPhoneTemp); createDeposit stamps them + guards recalc on empty customerId. 10 commits + spec/plan HTML; 7 V21 source-grep fixups. **L1 (real-browser UI) = USER** (these modals never RTL-mounted in repo; Rule Q V66 — no mocks-that-lie). Detail: checkpoint `.agents/sessions/2026-05-27-deposit-no-appointment.md` + ### Session block below. **Outstanding (user-triggered)**: push master + deploy (await explicit word, V18); L1 hands-on; 2 pre-existing Rule S doc edits (CLAUDE.md, rules/01) uncommitted.
- **Date (2026-05-27) — V122 BACKUP-SUBSYSTEM FIX + create-queue button removal + EOD+8 ALL DEPLOYED + L2-verified**: master = `0805da87` (pushed); prod LIVE (`vercel --prod` → lover-clinic-app.vercel.app). **NO firestore.rules/storage.rules/data/cron touched → frontend+serverless deploy only, NO Probe-Deploy-Probe.** Full suite **14892/0**. **V122 (business-critical)**: whole-system V81 backups silently `NO_MANIFEST` since 05-22 — `/systematic-debugging` confirmed root cause = ~1000 SEQUENTIAL cross-region round-trips > Vercel 300s cap (real HTTP 504 `FUNCTION_INVOCATION_TIMEOUT` @300.7s + 20h-stale cron lock) + 28/65 collections silently omitted (money/counters/master) by hardcoded scope. Fix: NEW `mapWithConcurrency` bounded-parallel I/O (~20×) + DYNAMIC `listCollections()` enumeration across backup + restore + whole-fleet-restore (load phase) + branch `maxDuration` 60→300; fixed latent orphan-subcoll wipe-order bug. AV141 + AV142. **L2-VERIFIED LIVE post-deploy**: endpoint → HTTP 200 in **38.1s** (was 504), `manual-20260527-0049` complete manifest 4783 docs / 409 users / 0 failed. Real-prod dump→restore e2e 10/0 (byte-identical incl Timestamp). branch healthy. Rule Q-honest: 2 "flakes" were real V21 regressions in the whole-fleet refactor's source-greps — behavior verified preserved then fixed (5 V21 fixups). **Button**: removed both `+สร้างคิวใหม่` variants (modal/form KEPT dormant; prose breadcrumb; 3 V21 fixups). **EOD+8 UI** shipped same deploy. Detail: `v-log-archive.md` V122. **Open (optional)**: 5 pre-fix broken NO_MANIFEST folders (auto-clean via retention / manual-20260524 manual); stale QR placeholder "กดสร้างคิวใหม่ด้านบน" (AdminDashboard:7541); 2 pre-existing Rule S edits (CLAUDE.md, rules/01) uncommitted (user's). USER L1 on prod for auth-gated UI.
- **Date (2026-05-26 EOD+8) — 13 UI/UX fixes (3 /systematic-debugging batches) DONE + TESTED, UNCOMMITTED**: master still `00e4b3a6`; prod `7e2a5bd8` LIVE. 15 files in working tree (9 src + 5 test + AV140), NOT committed (iron-clad: await user "commit"). Build clean · full suite 14869 pass / 0 deterministic fail. **Rule Q-honest WIN**: a re-run caught a REAL regression (Sync-button removal dropped a `!viewingSession.__synthetic` gate count → v118 SG7.5; fixed 3→2, contract holds) — NOT the load-order flake I almost logged. NO rules/data/cron touched (frontend-only when deployed). Batches: (1) success-text trim · TH/EN+moon toggle contrast both themes (verified live) · dashed-frame removed · stepper #2 ข้าม→แพทย์ · filled-pending card purple breathing+shadow · opd-pending purple cardflow bubble (AV140); (2) QR fills mobile width; (3) OPD review modal — admin "แก้ไขข้อมูล" bypasses 2h-expiry+archived gates when isSimulation (public link KEEPS timeout + 30-min cron + cross-day delete UNCHANGED) + remove "ซิงค์ข้อมูลใหม่" + save→"บันทึกเข้าระบบ" + header→"บันทึกข้อมูลรับเข้า" + hide session ID. **GAP**: auth-gated items (card/tab/modal) + QR = USER L1; toggle contrast verified live. Detail: checkpoint `.agents/sessions/2026-05-26-eod8-ui-fixes.md` + ### Session block below.
- **Date (2026-05-26 EOD+7 — /systematic-debugging) — Per-branch LINE OA add button restored + button rename (AV139) SHIPPED + DEPLOYED + L2-verified**: master = `7e2a5bd8`; prod LIVE (`vercel --prod` frontend + NEW serverless fn). Two items. **Item 1 (regression)**: patient-form success screen lost its "เพิ่มเพื่อน LINE Official" button — `PatientForm.jsx` gated it on the GLOBAL `clinic_settings.lineOfficialUrl` (undefined; no UI sets it). When LINE config went per-branch, the URL moved to `be_branches.settings.lineOaUrl` (set in BranchFormModal = `https://lin.ee/mFFsDkG`) but the success screen was never rewired → button vanished. `be_branches` is clinic-staff-only (rule:244; holds license#/tax id) → anon CANNOT read → **NEW `api/branch-line-oa.js`** (admin SDK, mirrors `api/patient-view`, returns ONLY the public lin.ee URL — no other branch field leaks) → PatientForm fetches per `session.branchId` (global kept as defensive fallback). **Item 2**: `OpdLifecycleRow` view button `ดูข้อมูล OPD`→`ดูข้อมูลรับเข้า` (label/title/2 state-comments + the OPD-save toast naming it; `opd-view-btn` testid + `onViewOpd` UNCHANGED — cosmetic-shell). **NEW AV139** (anon-facing per-branch data via secure server endpoint, NOT a legacy global / direct staff-only read) + `tests/branch-line-oa-and-rename.test.js` (9/0) + `scripts/diag-patient-form-line-oa.mjs` (Rule R). **NO firestore.rules change → no Probe-Deploy-Probe.** **Verify (Rule Q-honest)**: Rule R diag confirmed `be_branches.settings.lineOaUrl` persisted (all 3 branches); **post-deploy L2 HTTP probe GREEN** — `/api/branch-line-oa?branchId=BR-…26df6480` → `{"ok":true,"lineAddUrl":"https://lin.ee/mFFsDkG"}` HTTP200 · bad-branchId→400. build clean · full suite 14843 pass + 1 isolated-pass flake (cross-branch-import-rtl global.fetch-leak, 7/0 isolated, NOT mine). **GAP (disclosed)**: the patient-facing render (open `?session=` → see button) + the renamed card label = USER L1 (public/auth-gated render; workstyle "ไม่ self-test UI"). Detail: checkpoint `.agents/sessions/2026-05-26-line-oa-restore-and-rename.md`.
- **Date (2026-05-26 EOD+7 — push-fix follow-up) — push_config firestore rule (AV138) SHIPPED + DEPLOYED**: master = `f1a2110b`; prod rules LIVE (`firebase deploy --only firestore:rules`; Probe-Deploy-Probe GREEN). `/systematic-debugging` on user-reported "กดเปิด push → เกิดข้อผิดพลาด: Missing or insufficient permissions". **Root cause**: `push_config/{tokens,settings}` (the browser reads/writes both via `enablePushNotifications` + the EOD+7 self-heal) had NO `match /push_config` block in `firestore.rules` — `git -S "match /push_config"` confirms a match block NEVER existed; the older broad `public/data` allow was dropped in the per-collection rules refactor (~`c0d0ffc`, 2026-04-19) and push_config was forgotten + never added to the Rule B probe list → client fell through to the default-deny catch-all → denied for ~2 months (the 7 stale tokens predate it). The earlier push_config diag (`scripts/diag-push-config.mjs`) READ via admin-SDK which BYPASSES rules → reported "7 tokens, not muted, re-enable" → MASKED the client denial (V66 admin-vs-client blind spot — the user's live re-enable exposed it). **Fix**: `firestore.rules` `match /push_config/{docId} { allow read, write: if isClinicStaff(); }` (covers tokens + settings; Cloud Function `sendPushOnSubmit` uses admin-SDK so unaffected). **NEW AV138** — class-of-bug "every collection the CLIENT reads/writes MUST have a firestore.rules match block (no silent default-deny)"; grep classifier confirms `push_config` was the SOLE client-accessed collection missing a rule (isolated). `tests/firestore-rules-push-config.test.js` (4/0) + `scripts/probe-push-config-rule.mjs` (Rule B probe tool). **Verify (Rule Q L2 — REAL client auth: admin-SDK only minted the staff custom token; the WRITE went through the client auth path the rules gate)**: Probe-Deploy-Probe GREEN — PRE staff push_config-write 403 → POST 200 (FIX) · anon 403 (security, not world-open) · clinic_settings public-read 200 (no regression). **USER L1**: re-enable push on device → now succeeds (no more error) → form-fill → mobile push. **Note**: Rule B probe list in `01-iron-clad.md` NOT updated (left untouched — pre-existing uncommitted Rule S edits); the push_config probe tool is ready for future deploys.
- **Date (2026-05-26 EOD+7 — session end) — Real-time intake notif on นัดหมาย cards (AV137) SHIPPED + DEPLOYED**: master = `ac56dc1e`; prod = `ac56dc1e` LIVE (deployed `vercel --prod` frontend-only 2026-05-26 EOD+7 — aliased https://lover-clinic-app.vercel.app; build ✓ 3.52s; no rules/index/data → no Probe-Deploy-Probe). Full `/systematic-debugging → brainstorming (placement Q → user direction "no new surface — ลูกค้าอยู่ใน tab นัดหมายอยู่แล้ว") → spec → writing-plans → executing-plans INLINE`. **Bug**: patient-form submissions sent off a นัดหมาย card (card-flow: `createdFromBackendBooking + isHiddenFromQueue`) stopped surfacing real-time + blue bubble/sound/push after EOD+2 queue-tab removal + V120 `hideFromQueue`. **Root cause (3 mechanisms, all verified file:line)**: ① hub re-fetch ผูก `be_appointments` only (`appointmentDataVersion` bump 1165) + `resolveLinkedSession` (3793) card-flow → STALE one-shot `getDoc` lazy cache (3797-3811, never invalidated); ② card-flow excluded from `data`/`ndData` (2305/2329/2354) → not in `allNotifData` (2392) → detector (2398-2443) blind; ③ push = HTTP fn `sendPushOnSubmit` (PatientForm.jsx:380 calls it; server-side, NOT tab-removal) — Rule R diag (`scripts/diag-push-config.mjs`): 7 tokens, globalPushMuted=false, all ~2 months stale. **Fix (all `AdminDashboard.jsx`, NO new screen, additive — `3d5acdee`)**: ① live `allLinkedSessions` (from listener `allDocs`, READ-ONLY `setState`) added to `sessionsById` memo (3780-3788) → `resolveLinkedSession` returns FRESH → card flips "📥 ลูกค้ากรอกแล้ว · รอบันทึก" real-time + purple count +1, no F5, no re-fetch (`AppointmentHubView` NOT memo'd + resolves per-card at render line 600 → confirmed re-renders; mirrors proven `lazyFetchedTick`); ② `cardFlowNotif` (the excluded card-flow filled+unread) merged into `allNotifData` → blue bubble + sound (reuse `isNotifEnabled` + `lastNotifiedStrRef` dedup + first-load stamp; no double-count); ③ app-load `useEffect` re-registers current FCM token when `lc_push_enabled` + permission granted (heals stale tokens; one-shot, NOT in snapshot callback — V34/V36 read-only-listener intact). Queue filters / V124 purple count / V125 cancel-cascade UNTOUCHED. **NEW AV137** + `tests/realtime-intake-notif-appointment-cards.test.js` (F1 live resolution · F2 cardFlowNotif filter · SG1-6 source locks · AV137 presence, 13/0) + `scripts/diag-push-config.mjs` (Rule R). **NO firestore.rules/index/data change → no Probe-Deploy-Probe.** **Verify (Rule Q-honest)**: VERIFIED logic (flow-simulate real mirrors) + reactivity (code-inspection: AppointmentHubView not memo'd + per-render resolve) + source-grep + build clean + full suite **14830 pass + 1 isolated-pass flake** (phase15.5b global.fetch-leak, **51/0 isolated**, NOT mine). **GAP (disclosed honestly — USER L1 post-deploy)**: real-browser end-to-end (live form-fill → card flips on screen no-refresh + bubble audible) + push delivery NOT driven by me — fix not deployed yet (prod=old code) + auth-gated dashboard + workstyle "ไม่ self-test UI". Detail: spec/plan `docs/superpowers/{specs,plans}/2026-05-26-realtime-intake-notif-on-appointment-cards*`. Post-deploy: USER deploy + L1 (นัดหมาย → customer fills card-link form → card flips + bubble + sound no F5) + re-enable push on device.
- **Date (2026-05-26 EOD+6 — session end) — Appointment card cosmetic-shell redesign (5-band + theme-matched OPD pills) SHIPPED + DEPLOYED**: master = `ec8fcce6`; prod = `ec8fcce6` LIVE (deployed `vercel --prod` frontend-only 2026-05-26 EOD+6 — aliased https://lover-clinic-app.vercel.app; Vercel build ✓ 3.06s; no rules/index/data change → no Probe-Deploy-Probe; pre-deploy detail follows). Full `/session-start → brainstorming (Visual Companion in LIVE Chrome from the Asking-Scope stage, Rule S EOD+6 design timing) → spec v2 → writing-plans → executing-plans INLINE` (6 tasks T1-T6). **Feature**: `AppointmentHubRowCard.jsx` re-laid-out into a 5-band stack (header / finance / detail / OPD-footer / actions), beautiful + theme-correct in BOTH Dark and Light — fixing the user's reported washed/green-on-green OPD lifecycle pills (`OpdLifecycleRow.jsx` used unconditional dark-only classes). **Cosmetic-shell**: ZERO changes to any button wiring/flow/logic/data-testid/conditional; only the 2 user-requested label changes (Q5 remove "⚙ OPD Lifecycle" header · Q6 rename save "บันทึกลง OPD"→"บันทึกเข้าระบบ"). The round-circle **สถานะ OPD stepper** (shared Phase 28 `TreatmentLifecycleStepper`, also in Backend treatment-history) is **OFF-LIMITS** — re-parented verbatim, never restyled/recolored (Q4; user emphatic "เด็ดขาด" after an early mockup drew it wrong — corrected before approval). **Architecture**: NEW `OPD_PILL` token family in `_apptHubStyles.js`, **data-theme-driven** via NEW `.opd-pill-{blue,emerald,wait,save}` classes in `src/index.css` (dark default + `[data-theme=light|auto]` override) — NOT Tailwind `dark:`. **Rule Q-vis FINDING**: the first cut used `dark:` (mirroring neighbor chips), but the live real-browser check on a DARK-OS machine exposed `dark:` is OS-coupled here (no `darkMode` config) → washed pills in `data-theme=light` (same bug class). Switched to data-theme-driven → OS-independent → verified by eye in BOTH themes (light→light readable, dark→dark readable) + computed-style confirmed. **NEW AV136** + `tests/appointment-card-redesign.test.jsx` (T1 tokens · T2 OpdLifecycleRow theme-match+Q5+Q6 · T3 cosmetic-shell 26-testid invariant · T3.5 no-IIFE-in-JSX · T5 AV136 presence, 14/0) + 1 V21-fix (`v118-card-opd-lifecycle-row-rtl.test.jsx` R3.4 removed-header). **NO firestore.rules/index/data change** → no Probe-Deploy-Probe. **Verify**: full suite **14818 pass / 0 fail** · build clean · L1 real-browser BOTH themes (temp vite harness mounting the REAL component, deleted after — never committed). **GAP (disclosed)**: the card's NEIGHBOR chips (STATUS/TYPE/finance) still use OS-coupled `dark:` (pre-existing systemic, out of scope); only the OPD pills (the complaint) were made fully OS-independent. **Decisions**: Q1=C band · Q2=A pills · Q3=A Editorial Ember · Q4=A stepper untouched · Q5 remove header · Q6 rename save. Detail: spec/plan `docs/superpowers/{specs,plans}/2026-05-26-appointment-card-redesign*` (spec = live dual-theme mockup). Post-deploy: USER L1 in real AdminDashboard Frontend → นัดหมาย, flip Dark/Light.
- **Date (2026-05-26 EOD+5 — session end) — Patient-link hide-empty-boxes + auto-delete stale links (AV135) SHIPPED LOCAL, NOT deployed**: master = `eaa5204f` (feat `269010c9` + EOD docs + wiki ingest); prod UNCHANGED `459a4ea3` (awaits explicit "deploy", V18). **Wiki+graphify ingested** (`eaa5204f`): 1 source + 2 entities ([customer-link-payload-core](wiki/entities/customer-link-payload-core.md) + [patient-link-cleanup-cron](wiki/entities/patient-link-cleanup-cron.md)) + 1 concept ([patient-link-lifecycle](wiki/concepts/patient-link-lifecycle.md)) + index/log; `python -m graphify update .` → 8087 nodes/14648 edges/859 communities (graphify-out gitignored). Checkpoint `.agents/sessions/2026-05-26-patient-link-hide-empty-cleanup.md`. Full `/session-start → brainstorming (AskUserQuestion previews; Rule S no live browser at ask/plan) → spec → writing-plans → executing-plans INLINE` (subagents thrash this baseline). 13 files (+1279/-45). **Feature**: the customer patient-link page (`?patient=<token>`, `__customerMode`) now shows ONLY boxes with data — the "ไม่มีคอร์สคงเหลือ" empty box is HIDDEN in customer-mode (admin/sync view KEEPS it as feedback, Q1=A); appointments box already hid when empty; subtle line "ยังไม่มีนัดหมายหรือคอร์สในขณะนี้" when nothing at all (Q2=B). Plus a **new daily cron** `patient-link-cleanup-sweep` (`30 21 * * *`) that auto-deletes a link empty (no upcoming appt + no remaining usable course; expired ≠ remaining) for ≥30 days via an **empty-since state machine** (stamp `patientLinkEmptySince` on first-empty → delete after 30d → clear on data-return); delete = **clear token** (true delete, Q4=A); staff regenerate from CustomerDetailView. **Architecture**: "what does this link show" single-sourced in NEW pure `src/lib/customerLinkPayloadCore.js` (`computeUsableCourses`/`isAppointmentUpcoming`/`isCustomerLinkEmpty`/`decidePatientLinkCleanup`) consumed by BOTH `api/patient-view.js` (refactored, behavior-preserving) AND the cron + `scripts/patient-link-cleanup-sweep.mjs` (Rule M) + `scripts/diag-patient-link-empty-state.mjs` (Rule R). **NEW AV135** + `tests/patient-link-cleanup-and-hide-empty.test.js` (real-core unit A-D + flow-simulate E + source-grep F, 40/0) + 3 V21 fixups (customer-patient-link `F6.6`/`F7.3` + `E10` — the AV127/AV128 class-of-bug locks now follow the core extraction: core gates for the endpoint, PatientDashboard's ProClinic-sync path still gates inline). **NO firestore.rules/index change** (`be_appointments where customerId==` already used by the endpoint; admin-SDK cron bypasses rules) → no Probe-Deploy-Probe. **Verify (Rule Q-honest)**: focused **80/0** (incl. the 4 files together); full suite **14803 pass** — residual reds = rotating pre-existing `global.fetch`-leak/perf load-flakes (each run a different file — customer-link→subtab-perf→phase15.5b — all pass isolated; NOT mine); build clean. **L2 real prod** (cron dry-run, READ-ONLY): scanned 2/skipped 2/**0 deleted**; diag of the screenshot customer LC-26000023 (0 courses + 1 appt) → **coursesBox HIDDEN**, apptBox SHOW, subtleLine no, cron isEmpty=false → kept (ไพบูลย์ LC-26000106 same). **GAP disclosed**: visual pixel render NOT driven in a real browser by me (local vite dev doesn't serve the serverless endpoint → customer-mode needs vercel dev/deploy); render is source-grep + build locked + data-contract L2-proven → full visual L1 = user post-deploy. **Decisions**: Q1=A customer-mode-only · Q2=B subtle line · Q3=A empty-since 30d · Q4=A clear-token. Detail: spec/plan `docs/superpowers/{specs,plans}/2026-05-26-patient-link-hide-empty-boxes-auto-cleanup*`. Post-deploy: USER opens the screenshot link → no empty box; Rule M cron `--apply` runs daily once deployed (currently 0 would-delete, safe).
- **Date (2026-05-26 EOD+4 — session end) — Staff-chat enhancements SHIPPED + DEPLOYED**: master/prod = `459a4ea3` LIVE (vercel `lover-clinic-app.vercel.app` aliased + `firebase deploy --only firestore:rules,storage`; **Probe-Deploy-Probe #15 PASS** — P1 chat-webhook + P5 patient-form open paths stayed 200 pre+post, P9/P15a/b/c staff-chat anon write/delete/sticker-create all 403 → new rules did NOT open anon). Full `/session-start → brainstorming (AskUserQuestion previews; Rule S reaffirmed 2026-05-26 = no Chrome MCP to verify the Visual Companion at ask/plan) → spec → writing-plans → subagent-driven` (pivoted inline — subagent died on a 1M-context billing wall; baseline-thrash documented per V81/Tablet-Chart). **4 features on the staff-chat surface**: (F1) day-separator pill dividers (`src/lib/staffChatDayGroups.js`, Bangkok-TZ GMT+7 shift, dual-shape createdAt; วันนี้/เมื่อวาน/full พ.ศ.) · (F2) quote `text-[10px]→[13px]` (message + composer strip) · (F3) own-only unsend (`deleteStaffChatMessage` = Storage folder sweep + `deleteDoc`; AV78 confirm dialog; **own-only is a UI/deviceId gate** — server rule = clinic-staff delete, staff chat has no per-user auth) · (F4) emoji (unicode insert) + 2-tier stickers — bundled **Fluent-Emoji (MIT, 20 SVGs)** in `/public/stickers/fluent/` sent by ID (**0 Firebase**) + custom **IndexedDB** library (`stickerLibrary.js`) → temp Storage on send (30-day retention). **NEW AV134** + rules (firestore `be_staff_chat_messages` sticker-only create clause + `allow delete: if isClinicStaff()`; storage `staff-chat-attachments` clinic-staff delete). **This deploy shipped EVERYTHING since prod `65ab6467`** — staff-chat + carryover (4-tab removal + deposit-cancel dialog + appointment-hub all-types button + opd-pending tab + appointment-modal deposit + AV133 goto-appt/open-hours/cancel-hard-delete) — all now LIVE. Full suite **14746 · 14745 pass + 1 known V50 full-suite-load flake (isolated 64/64)** · staff-chat 289/0 · new bank 33/0 (unit 15 + flow-simulate 10 + RTL 8) · build clean. **Rule Q-honest**: rules security real-verified via live HTTP probes (L2-equiv for the security contract); feature behavioral L1 (real-browser staff chat) = USER post-deploy (auth-gated + workstyle "ไม่ self-test UI") — disclosed. Detail: checkpoint `.agents/sessions/2026-05-26-staff-chat-enhancements.md`.
- **Date (2026-05-26 EOD+3 — session end) — /systematic-debugging 3-fix batch SHIPPED (LOCAL, NOT deployed)**: master = `e07451fb`; prod UNCHANGED `65ab6467`. Full vitest **14731/14731 — 0 fail** · build clean 2.91s. **NO rules/index change → no Probe-Deploy-Probe.** Phase 1 root-cause for all 3 via code. **Issue 1** — Finance "ไปที่นัด" opens the appt's DATE (was today): BackendDashboard default `activeTab='appointment-all'` mounts AppointmentCalendarView on the first render BEFORE the deep-link useEffect set `initialApptDate` → its `useState(()=>)` selectedDate/calMonth locked to today + never re-derived (same element, no remount). Fix: derive `?date=` SYNCHRONOUSLY in the useState initializer + a `[initialSelectedDate]` prop-sync effect. **Issue 2** — create-appt default start = branch open hours via `getOpenHoursForDate(date,cs)?.open` (was hardcoded '10:00'); initializer + re-apply effect keyed on date+cs (NOT startTime → manual picks stick). **Rule P found+fixed 2 siblings**: `AppointmentCalendarView.openCreate` (`time||'10:00'`→`time||''`, silently defeated the fix) + `DepositPanel` deposit-appt sub-form (`visibleTime.openRange`). Issue-1 class isolated (other `useState(()=>initial)` get the prop synchronously or are render-gated). **Issue 3** — Frontend นัดหมาย cancel HARD-DELETES (`deleteBackendAppointment`, mirrors Backend) instead of `status:'cancelled'`; V125 linked-`opd_session` archive cascade PRESERVED (reason `appt-deleted`) so queue+bubble still clear; 'both' deposit path unchanged; confirm → 'ยกเลิกและลบนัดนี้ออกจากระบบ?'. **AV133** + NEW `tests/finance-goto-default-time-cancel-delete.test.js` (I2 runs the REAL `getOpenHoursForDate` = L2) + 3 V21 fixups (v125 SG-A3 → hard-delete/appt-deleted · phase-19-0 C2.1 · phase-24-0 VOC.B.1). **Rule Q-honest**: logic L2 (real helper) + source-grep + full suite; real-browser render (date-land / 11:30 default) + real Firestore delete round-trip = USER L1 post-deploy (auth-gated dashboards + workstyle "ไม่ self-test UI") — disclosed. One "deploy" ships this + tab-removal + deposit-cancel + appointment-hub + appointment-modal-deposit. Detail: checkpoint `.agents/sessions/2026-05-26-finance-goto-default-time-cancel-delete.md`.
- **Date (2026-05-26 EOD+2 — session end) — Frontend 4-tab removal + deposit-aware cancel dialog SHIPPED (LOCAL, NOT deployed)**: master = `e84d2538`; prod UNCHANGED `65ab6467`. Full `/session-start` → `brainstorming` (Visual Companion via AskUserQuestion previews; Rule S = no live browser at ask/plan) → spec → `writing-plans` → `executing-plans` (11 tasks T1–T11, Rule K work-first/test-last). Full vitest **14712/14712 — 0 fail** · build clean ✓ 3.34s · real-prod e2e **31/0** (`scripts/e2e-deposit-cancel-dialog.mjs`). **NO firestore.rules / composite-index change → no Probe-Deploy-Probe.** **Part 1 — remove 4 tabs** (คิวหน้า Clinic / จองไม่มัดจำ / จองมัดจำ / ประวัติ): default landing `'dashboard'`→`'appointment'` + redirect guard in the `setAdminMode` wrapper (single chokepoint; `setAdminModeRaw` private) + repointed all nav; 4 desktop tab buttons + mobile dock (คิว + จอง picker + จอง BottomSheet + ประวัติ) removed; the 5 now-unreachable render branches excised (~750 lines — chain now chat→clinicSettings→appointment) via Edit (small) + bottom-up line-range delete (big consecutive run; build+grep verified); orphaned `showMobileJongPicker` removed. **KEPT** deposit/noDeposit state + create-forms + listener slices — still read by the `viewingSession` OPD-modal resolver (`:2547`) + the สร้างคิวใหม่ create flow (NOT orphaned); dashboard-queue fallback stays (unreachable, harmless). **Part 2 — deposit-aware cancel dialog** (AV132): NEW pure `src/lib/depositCancelDecision.js` `resolveDepositCancelState` + NEW shared `src/components/admin/DepositAwareCancelDialog.jsx` (orientation appt|deposit · `getDeposit` fetch · `blocked = usedAmount>0` · choices both|this-only|cancel · AV78 explicit-close) wired into ALL 3 cancel surfaces: Frontend นัดหมาย (`AppointmentHubView` → `AdminDashboard.onCancelAppt(appt,{deleteDeposit})`: both → `deleteDepositBookingPair` HARD, else V125 appt-only) + Backend `AppointmentCalendarView` appt-delete + Backend Finance·มัดจำ `DepositPanel` hard-delete (both → pair-delete, this-only → `deleteDeposit` — fixes the orphan-appt gap). `AppointmentFormModal` flip-away left as-is. 11 V21-fixup test files (asserted removed render / old calview inline-delete / 2nd patient-link trigger / 8-tab set → flipped assert-removed / counts updated; NO real regression — onCancelAppt isArchived cascade + live Backend patient-link verified intact). **Decisions**: Q1=นัดหมาย default · Q2=นัดหมายครอบคลุมหมด (no separate arrival surface) · Q3=ลบหายเลย hard-delete · Q4=ทุกที่ที่ยกเลิกได้ (3 surfaces). **Rule Q-honest scope**: deposit-cancel LOGIC = L2 (the e2e ran the REAL `resolveDepositCancelState` on 11 REAL prod deposits — 6 used→correctly-blocked — + both/appt-keep/used-block cascade outcomes on TEST- fixtures, 0 orphans + audit doc; cascade helpers pre-existing+proven). Tab-removal RENDER verified via build + ternary-markers + full suite — **real-browser render L1 = USER post-deploy** (workstyle "ไม่ self-test UI" + auth-gated AdminDashboard; NOT driven by me — disclosed). One "deploy" ships this + appointment-hub + appointment-modal-deposit (all since prod `65ab6467`). Detail: checkpoint `.agents/sessions/2026-05-26-tab-removal-deposit-cancel-dialog.md` + spec/plan HTML `docs/superpowers/{specs,plans}/2026-05-26-frontend-tab-removal-deposit-cancel-dialog*`.
- **Date (2026-05-26 EOD+1 — session end) — Appointment-hub all-types button + "รอ/ยังไม่ลง OPD" tab + OPD-link auto-cleanup SHIPPED (LOCAL, NOT deployed)**: master = `b476f615`; prod UNCHANGED `65ab6467` (awaits explicit "deploy" — V18). Full `/session-start` → `brainstorming` (Visual Companion via AskUserQuestion previews — Rule S: no live browser at ask/plan stage) → spec → `writing-plans` → `executing-plans` (11 tasks T1–T11, inline). Full vitest **14688** (14687 pass + 1 KNOWN Phase 17.1 full-suite-load flake; isolated 7/7 — unrelated to this work) · build clean · **real-prod e2e 7/0** (`scripts/e2e-opd-link-lifecycle.mjs` — SAFE dry-run, NO prod mutation). **NO firestore.rules / composite-index change → no Probe-Deploy-Probe.** **① "เพิ่มนัดหมาย" all-types button** REUSES the `AppointmentFormModal` that `AppointmentHubView` already renders for edit, in create mode (`lockedAppointmentType=null` → all 5 types incl. Walk-in); no new modal; AdminDashboard kiosk-button wiring retired. **② "รอ/ยังไม่ลง OPD" 5th primary pill** (state B+C+D ALL types per R4=keep-all-types; today+future, past hidden) via NEW `isAppointmentOpdPending` + `opd-pending` case in `appointmentHubFilters` + in-view `resolveLinkedSession`-join filter + `opdPendingCount`. **③ cron hard-deletes link when appt date passed** — `decideCleanupAction` date-passed branch (above 2h-age, overrides V116 hide, fires even with patientData Q3=A) + `sweepOpdSessionCleanup` joins `be_appointments` by `linkedAppointmentId` (R5: sessions don't store the date) + Bangkok `todayISO`. **④ delete session on OPD-save** — best-effort hard-delete inside `_attachLinkedBookings`, gated on the hoisted `isFromBookingFlow` (kiosk walk-in sessions safe — mutual exclusion with the walk-in modal). **NEW AV131** + Rule I flow-simulate + 5 V21 fixups (walkin-btn→add-appt-btn / onAddWalkIn→onAddAppointment / SG-B re-anchored on hoisted predicate / `=== 'function'` guard flip). Decisions: Q1=B+C+D · Q2=hard-delete · Q3=delete-all-date-passed · Q4=single all-types button (reuse modal) · R4=keep-all-types. **Carryover**: the prior 2026-05-26 appointment-modal-deposit stack (`def9e256`) is also NOT deployed — one "deploy" ships everything since prod `65ab6467`. **Rule Q-honest scope**: ③ date-passed decision + cron join real-tested (L2, real Firestore shape); the whole-collection sweep ran DRY-RUN only (apply:true would mutate real prod via undeployed logic = Rule M); UI ① button + ② pill = L1 by user post-deploy. Detail: checkpoint `.agents/sessions/2026-05-26-appointment-hub-allbutton-opd-tab-lifecycle.md` + spec/plan `docs/superpowers/{specs,plans}/2026-05-26-appointment-hub-allbutton-opd-tab-lifecycle*`.
- **Date (2026-05-26 — session end) — Appointment-modal deposit-section + chip "นัดมาเพื่อ" unification SHIPPED (LOCAL, NOT deployed)**: master = `3a5ae897`; prod UNCHANGED at `65ab6467` (feature awaits explicit "deploy" — V18). Full brainstorm → spec → writing-plans → executing-plans cycle (11 tasks E1–E11; Visual Companion mockup from the question stage per rule). Full vitest **14658/0** · build clean ✓ 3.25s · **real-prod e2e 21/0** (`scripts/e2e-appointment-deposit-purpose.mjs`). **① Auto deposit section**: `AppointmentFormModal` (THE shared modal, 4 callers) deposit section now gates on the EFFECTIVE appointment type — NEW `showDepositSection = isDepositBooking = (safeLockedType || formData.appointmentType) === 'deposit-booking'` replaces the locked-only `isLockedDepositType && mode==='create'` render gate + create save branch. Picking "จองมัดจำ" via radio (create + edit) now auto-shows the section + requires ยอด>0 + routes to `createDepositBookingPair` (atomic pair). **Edit mode** (scope=create+edit): hydrate deposit fields from the linked deposit (`getDeposit` useEffect) + on save `updateDeposit` (case 1 existing link) / NEW `createDepositForExistingAppointment` (case 2/4 flip-to + legacy no-link, reverse of `createAppointmentForExistingDeposit`, reuses `buildDepositPairPayload`) / flip-away (was-deposit→non-deposit with linked deposit) → confirm dialog ลบ-or-เก็บ (`flipAwayDecisionRef` + pre-`setSaving` early-return gate; ลบ routes through `cancelDepositBookingPair` cascade — never raw delete; usedAmount>0 → clear error). **② Chip "นัดมาเพื่อ"**: NEW `VisitPurposePicker` (multi-select chips + อื่นๆ free-text, ember-red selected) replaces the free-text `<textarea>`; required ≥1 via `scrollToFormError('appointmentTo')`; stores the EXISTING `appointmentTo` string (backward-compat via `build/parseVisitPurposeText`; legacy free-text → "อื่นๆ: …"). `visitReasonOptions` extracted to NEW `src/lib/visitReasonOptions.js` single source — PatientForm + AdminDashboard×2 (deposit/no-deposit คิว) + the picker all import it (Rule C1, 3 inline copies merged). **NEW AV130** (gate=effective-type / single-source options / deposit-mutation-only-via-sanctioned-helpers). **NO firestore.rules change** (be_deposits/be_appointments already clinic-staff write; no new compound index → no Probe-Deploy-Probe). **Rule Q-honest scope**: e2e is admin-SDK doc-level L2 (acceptable here — no new index/rules); doc shapes mirror the REAL builders (E5 unit verifies them); helper writeBatch mechanics + UI gate covered by unit + flow-simulate + RTL; **L1 real-browser = USER post-deploy**. 1 V21 fixup (phase-21-0 F1.6+F1.8 locked the old gate → updated to effective-type). Detail: spec/plan in `docs/superpowers/{specs,plans}/2026-05-25-appointment-modal-deposit-purpose-unification*`. Post-deploy L1: regular appt-create → pick จองมัดจำ → section + required → save → การเงิน→มัดจำ; edit amount → update; flip-away → dialog; chips required. **Known edge**: Walk-in OPD-save now also requires a "นัดมาเพื่อ" chip — confirm acceptable at L1.
- **Date (2026-05-25 EOD+2 — session end) — Treatment-blob Storage-ref migration SHIPPED + DEPLOYED + 2 follow-up fixes + Rule Q-honest**: prod LIVE @ `65ab6467` (deployed 2× this session). Full suite **14603/0** · stress e2e **24/0** · human-flow e2e **18/0 on REAL prod** · build clean · zero Storage orphans · **user L1 confirmed "ใช้ได้แล้ว"**. **NO rules change** (`storage.rules uploads/{collection}/{docId}/{fileName}` already allows image/* + application/pdf). **Root cause** (Rule R diag on prod): TFP Before/After/Other photos + lab images + lab/treatment PDFs stored INLINE base64 in `be_treatments` doc → 1 MiB cap at ~2 photos (prod docs 95%/86%/80%) → intermittent save-fail + jank. Charts Storage-ref'd 2026-05-22; rest never were (latent Rule P gap). **Fix**: all blobs → Firebase Storage via NEW `uploadTreatmentBlob`/`treatmentImageUpload.js` (doc holds URL+storagePath, ~30 KB); save-gate on pendingUploads; remove + delete-cascade; legacy inline still displays (data:|http). **Chart cap 2→10**. **OPD column** flex-balanced (CC grows → purple btn bottom-aligns teal; cosmetic). **2 follow-ups (stress-test-found)**: `computeResizeDims` clamp ≥1 (extreme ratio → 0-dim canvas); **edit-remove-cancel broken-ref** — `removeTreatmentBlob` deletes Storage only in CREATE; EDIT skips (doc refs it until save → no 404 on cancel); routed 4 TFP removes + ChartSection (`onBlobRemoved`). **AV129** + 3 test banks (storage-ref 25 · fuzz 13 · human-flow e2e 18) + 1 V21 fixup (chart cap). **NEW iron-clad Rule Q-honest** (`01-iron-clad.md`): reasoning ≠ verification — run the real-adversarial test that COULD fail even when certain; disclose the test-vs-claim gap. Origin: I claimed "done — identical to proven chart path = L2-equivalent" WITHOUT an e2e; the e2e the user demanded FOUND the edit-remove bug → reasoning would have shipped it. User: "ไม่หลอกกูและไม่หลอกตัวมึงเอง ไม่เข้าข้างตัวเอง". Detail: checkpoint `.agents/sessions/2026-05-25-treatment-blob-storage-ref.md`.
- **Date (2026-05-25 EOD+1 — session end) — Customer Patient-Link SHIPPED + DEPLOYED + 2 L1 bugfixes**: prod = `9d82e923` LIVE (feature + AV127 used-up-course fix; `vercel --prod` frontend + serverless endpoint, NO rules change). master = `0df352fa` (+AV128 done-appt fix, **DEPLOY-PENDING** — on "deploy" = `vercel --prod`). Full suite **14551/0 GREEN @ session start** (F6/F7 bugfix tests additive, no regression; full suite NOT re-run post-AV128 per user-reject of 206s gather — targeted green); build clean; audit-branch-scope 106/0. **Feature**: 🔗 "ลิงก์ดูข้อมูล" button in CustomerDetailView (Layout A — action group [แก้ไข · ผูก LINE · 🔗 ลิงก์] + thin divider + ลบลูกค้า separated below) → `CustomerPatientLinkModal` → generates an anon link `?patient=<token>` the customer opens **without login** to see the EXISTING PatientDashboard view + **นัดหมายครั้งต่อไป (📍 สาขา + เดือนเต็ม)** + คอร์สคงเหลือ. **Full brainstorm→spec→plan→executing-plans cycle** (Visual Companion mockups; user corrected mid-flow: "reuse the existing form, don't redesign" + "show upcoming appointments with สาขา" + "เดือนเต็ม"). **Architecture**: token on `be_customers` (clinic-staff client write via generate/setEnabled/revoke helpers) → NEW public `api/patient-view.js` (admin SDK, **unified resolve** be_customers OR legacy opd_session by patientLinkToken; future-only appts + branchId→ชื่อ resolve + `fmtThaiDate(monthStyle:'full')` + `startTime`/`endTime` range [Rule R diag: real field is `startTime`, not `time`]; field-minimized — no national ID). PatientDashboard **customer-mode** = endpoint-first → map to existing `latestCourses` shape → **reuse render 100%** (AppointmentCard already had a 📍 branch slot) + gate auto-sync (`__customerMode`) + legacy opd_session listener as fallback-on-endpoint-error only. **NO firestore.rules change** (be_*/be_appointments/be_branches stay clinic-staff — endpoint is the secure anon path; opening anon-read would expose the PII DB). **Verification**: Rule R diag (LC-26000101 resolves + appt field shape) · Rule Q **L2 e2e 11/0 on REAL prod** (resolve+gate+courses-split+future/cancelled-filter+"30 พฤษภาคม 2569" full-month+"10:00 - 10:30 น."+branch "นครราชสีมา"+cleanup) · Rule I 6/0 · helpers 15/0 · modal 7/0 · CDV 6/0. **AV126** anon-safety + **AV127** (course effective-status filter) + **AV128** (appt completed-exclusion). 2 V21 fixups (v75 button 3→4; v116 SG3 retargeted to `opdSessionCleanupCore.js` — 2026-05-24 perf-cron relocated auto-expire there; V116 linked-preserve verified intact). **2 L1 bugfixes (user-caught on prod via /systematic-debugging + Rule P class-of-bug)**: **AV127** = exclude used-up courses (`deriveEffectiveStatus`, buffet-safe: total>0 && remaining<=0 → ใช้หมดแล้ว → dropped; "0/1 กำลังใช้งาน" no longer shows) — fixed in endpoint + `fetchCoursesViaApi`, ref = `formatCoursesReply` V33.8 + RemainingCourseTab; **AV128** = exclude completed/serviced appts from "นัดหมายครั้งต่อไป" (`didAttend` set {done,completed,มาตามนัด,ชำระเงิน} + `serviceCompletedAt`/`wasServiceCompleted`) — fixed in endpoint + `fetchCoursesViaApi`, ref = appointmentAnalysisAggregator + appointmentHubFilters. F6 (course effective-status 6 cases) + F7 (appt completed-exclusion 3 cases) added to flow-simulate. **AV128 fix (`0df352fa`) committed + pushed, NOT yet deployed (V18)** — on "deploy" = `vercel --prod` (frontend + endpoint, no rules). **Rule Q L1 pending** (post-AV128-deploy, user): hard-refresh ไพบูลย์'s prod link → 1 upcoming appt (4 มิ.ย.) + "ไม่มีคอร์ส". Detail: checkpoint `.agents/sessions/2026-05-25-customer-patient-link.md` + `docs/superpowers/{specs,plans}/2026-05-25-customer-patient-link*`.
- **Date (2026-05-25 — session end) — Skill repo updated to obra/superpowers v5.1.0 + audit infrastructure shipped**: master = `5a82c856` (1 commit ahead of prod, tooling-only — no deploy needed). Vercel prod UNCHANGED at `9af2989e`. Tests UNCHANGED (157 PASS focused; no src touched). **Work**: (a) `git init` at `~/.claude/skills/` + baseline commits `df9648b` → `f8e90d0` locking 4 customized SKILL.md files BEFORE any future upstream update; (b) `verify-skill-customizations.sh` audit script (committed `5a82c856`, pushed) greps 11 markers across the 4 customized skills (brainstorming Visual Companion auto-trigger + writing-plans/executing-plans/subagent-driven-development HTML+Mockup+Flow mandates); (c) pulled all 3 marketplace upstreams (`obra/superpowers` 6efe32c→f2cbfbe = v5.1.0, `multica-ai/andrej-karpathy-skills` README sync, `anthropics/claude-plugins-official` 226 plugin-metadata commits); (d) 3 customized SKILL.md 3-way merged via `git merge-file` — writing-plans + executing-plans CLEAN; subagent-driven-development had 1 conflict resolved manually (took upstream worktree wording + kept our "(HTML format)" annotation); 10 non-customized files copied verbatim; (e) audit 11/11 PASS post-merge; (f) generated `~/.claude/skills/CUSTOMIZATIONS-vs-upstream-v5.1.0.patch` (483 lines) for future PR (kept local — opinionated personal preferences; gh CLI not installed). SessionStart hook proposal blocked by auto-mode classifier → skipped per "do what's best" (manual `bash ~/.claude/scripts/verify-skill-customizations.sh` integrated into the upstream-pull cycle is sufficient; 3-layer protection already strong). **3-layer protection now**: L1 = `~/.claude/skills/.git` baseline (recovers reverted files via `git checkout HEAD --`); L2 = CLAUDE.md backstop (`using-superpowers` Instruction Priority guarantees behaviour even if L1 reverted); L3 = audit script (drift catcher). Detail: `.agents/sessions/2026-05-25-skill-repo-update.md`.
- **Date (2026-05-24 EOD+1 LATE+1 — session end) — V124+V125+V126 SHIPPED + DEPLOYED in one /systematic-debugging cycle**: master = `9af2989e`. Vercel prod LIVE (aliased `lover-clinic-app.vercel.app`); Firebase rules deployed idempotent (no change — V1/V9 defense). Pre-probe = post-probe (200/403/403). 3 user-reported bugs → 3 V-entries → 1 commit → deployed in one session. **V124** = bubble↔badge predicate parity (AV124). V121 `isCardFlowUnread` required V118+V120 markers (createdFromBackend + isHiddenFromQueue) — too narrow, missed all regular จองไม่มัดจำ/มัดจำ bookings. NEW `isAppointmentPendingOpdSave({appt, linkedSession}) = resolveCardOpdState === 'D'` matches row badge at AppointmentHubRowCard:172. Memo iterates `apptData.appointments` (branch-scoped) joined to linkedSession via existing resolveLinkedSession. L1 verified: BA-1779590375471 → ND-68FA49 → desktop+mobile bubble showed purple "1". **V125** = cancel cascade (AV125). Pre-V125 `onCancelAppt` wrote only `appt.status='cancelled'` — V124 predicate didn't check status (state-D still matched) → bubble persisted; linked opd_session stayed in noDepositSessions/depositSessions/sessions filters → row visible in 3 sibling tabs. Fix (3 surfaces converge): (a) predicate excludes cancelled (`appt.status === 'cancelled' → return false`); (b) `hideOpdLifecycle` per-row covers cancelled status (defense for past sub-pill where `defaultStatusFilterForTab('past').exclude=[]` admits cancelled); (c) `onCancelAppt` cascade-archives linked opd_session with `isArchived:true + archivedReason:'appt-cancelled' + archivedFromApptId` forensic stamps, best-effort try/catch (appt cancel must not roll back on session-archive failure). L1 verified: bubble = 0 post-cancel. **V126** = workflow-strict mark-complete gate. Pre-V126 `showMarkCompleteBtn = isTodayTab && !serviceCompletedAt` (open to all statuses incl. pending). User: "ต้องกดคอนเฟิร์มนัดก่อน เป็นการยืนยันว่าลูกค้ามาคลินิกตามนัดแล้ว ถึงจะกด ✓ ลูกค้ารับบริการเรียบร้อย ได้". Fix: add `&& rawStatus === 'confirmed'`. V71.B-ter philosophy preserved for TREATMENT concerns (hasTreatmentForDay + wasServiceCompleted still dropped); V126 is orthogonal STATUS guard. V21 fixup: V73 test bank (B2.1+B2.4+B2.6+B3.x simulator). L1 verified: 8 today pending rows → 0 mark-complete buttons (`invariant_v126_holds:true`). All client-only — no rules/index/Cloud Run change. **Strategic direction noted** (user-flagged, NOT implemented): unify นัดหมาย tab as primary surface, eventually deprecate คิวหน้า Clinic / จองไม่มัดจำ / จองมัดจำ tabs — V125 cascade is the tactical first step (cancel in นัดหมาย propagates via `isArchived` filter convention). Tests: 157 PASS (V125+V124+V73+V121+V118 chain) · build clean 3.08s. Detail: `.agents/sessions/2026-05-24-v124-v125-v126.md`.
- **Date (2026-05-24 EOD+1 — session end) — Perf cron SHIPPED + DEPLOYED · Frontend fast confirmed**: master = `2fe8940d`. Vercel prod LIVE @ `2fe8940d` (V115+V116+V116-followup + new perf cron infrastructure). User reported "เร็วแล้ว" post-deploy. Frontend slowness root cause (multi-layer diag via admin SDK + DIAG instrumentation): (1) **chat_history listener pulled 3,855 docs / ~7.5 MB per ChatPanel snapshot** — in-listener auto-delete (per comment in AdminDashboard) was never wired; (2) **opd_sessions listener inline auto-cleanup** wrote to expired sessions on every snapshot fire → **cascade** (write triggers own-snapshot fire → re-eval → maybe more writes), amplified by N admin tabs racing. Backend was fast because subscribes to fewer listeners. **Fix architecture**: (a) Rule M one-shot deleted 3,755 chat_history docs > 24h (3,855 → 100); (b) NEW `api/cron/chat-history-retention-sweep.js` daily 04:00 BKK + NEW `src/lib/chatHistoryRetentionCore.js` pure logic + CLI mirror (Rule of 3); (c) NEW `api/cron/opd-session-cleanup-sweep.js` every 30 min + NEW `src/lib/opdSessionCleanupCore.js` `decideCleanupAction` preserving legacy semantics verbatim (V82-followup opt-out + V116 hide-vs-delete linked-booking); (d) REMOVED inline cleanup from `AdminDashboard.jsx:2256-2287` → listener becomes pure read-only (no cascade). Plus Rule M admin SDK delete for `BA-1779544476132 + ND-9CBCD7` (user couldn't delete via UI — permission-denied silently). **Reverted from working tree (back to commit before perf work)**: V117 + V118 + V119 + V120 + V121 + V122 + V123 + V123-fix1 all removed. **Phase 3 deferred** (server-side filter on opd_sessions listener requires legacy doc backfill + split active/archived listeners — separate brainstorm). **No tests this commit** (Tier 2 deferred per user deploy-speed priority). Build clean ✓ 3.01s. Detail: `.agents/sessions/2026-05-24-eod-perf-cron-shipped.md`.
- **Date (2026-05-24 EOD — session end) — V122 + V123 + V123-fix1 LOCAL ready for combined commit + deploy**: master = `69743909` (unchanged from last EOD; V122/V123/V123-fix1 are UNCOMMITTED source changes). Vercel prod UNCHANGED at `3612d8ae` (V115+V116+V116-followup LIVE). Working tree: `src/lib/{backendClient.js,opdSessionState.js}` + `src/pages/AdminDashboard.jsx` + `audit-anti-vibe-code/SKILL.md` + `00-session-start.md` + 2 V21 test fixups (BS-F.8 + SG2.4) + 2 NEW test banks (v122 + v123) + 3 NEW Rule R diag scripts. **V122** (per-branch doctor-collision + per-branch slot-key suffix + empty-time guard + AV122) closes พระราม 3 booking class-of-bug (Rule R diag: 3/3 fail pre-fix → 100% works post-fix). **V123** (desktop นัดหมาย tab bubble via broader `needsAdminSave` predicate, replacing V121's silently-dead `cardFlowUnreadCount` which walked 5 arrays that excluded its target Card-flow sessions; AV123 + Q1=A/Q2=purple-merged/Q3=desktop-only locked) closes "no bubble on นัดหมาย tab". **V123-fix1** (user-reported 4 false-positives — 3 archived DEP-* sessions matched the predicate; added `!isArchived && !serviceCompleted` gates to needsAdminSave AND isCardFlowUnread class-of-bug mirror per Rule P Step 5). User confirmed bubble = 1 correct post-fix. **List-empty user complaint = browser-zombie state confirmed NOT-code**: MY preview (same auth/UID R8OerglHm8SmK0Ii3GPU7VJBqsJ3/branch NAKHON/claims admin+isClinicStaff+gp-owner) renders the tab fully (today=8, รายการนัดหมาย 8 คน, real customers). Recurring HMR Fast-Refresh-incompatible cascade on AdminDashboard.jsx caused user's tab to accumulate zombie listeners across many forced-reload cycles. Recovery for user: close all localhost:5173 tabs + open fresh tab + wait 10s; or restart `npm run dev` + hard refresh. Full vitest **14544/14544** GREEN (+31 net); build clean ✓ 3.01s. All client-only → no Probe-Deploy-Probe needed. Detail: `.agents/sessions/2026-05-24-v122-v123.md`.
- **Date (2026-05-23 EOD+1 LATE+9 — session end) — V118+V119+V120+V121 LOCAL stack ready for deploy**: master = `00410f93`. Vercel prod UNCHANGED at `3612d8ae` (V115+V116+V116-followup LIVE). 5 V-features stacked client-only: **V117** lightbox createPortal mandate · **V118** 5-state OPD lifecycle row on Card (🔗/🟢/🔴) + AV118 · **V119** P0 fix for V118 missing useCallback import → AdminDashboard black-screen + AV60 promoted to PERMANENT vitest gate (no more opt-in) · **V120** `provisionOpdLinkForBookingPair({hideFromQueue:true})` — V118 card flow stops appearing in Clinic queue · **V121** purple #a855f7 bubble on นัดหมาย tab + sub-pills + mobile dock when card-flow customer fills (Q1=B locked: bubble persists until 🔴 บันทึก click; closes V120 latent gap via 3 queue-filter extensions). Full vitest **14480/14480 GREEN** (+42 net from 14438 baseline); V121 self 27/27; AV60 scanner 0/527 drift; build clean 3.09s. All client-only → combined deploy OK, no Probe-Deploy-Probe needed. Detail: `.agents/sessions/2026-05-23-eod-v118-to-v121.md`.
- **Date (2026-05-23 EOD+1 LATE+4 — session end) — V116 DEPLOYED · V117 LOCAL (lightbox portal mandate)**: master = `f43ab792`. Vercel prod = `3612d8ae` (V115 + V116 + V116-followup LIVE, aliased `https://lover-clinic-app.vercel.app`); V117 PENDING deploy authorization. V117 self-tests 11/11 + V83+V117 28/28 + Phase 17.1 isolated 7/7 (known full-suite flake); build clean. V116 = link-survives-queue-delete + auto-regen + un-hide-on-re-engage (5 edits across appointmentDepositBatch.js + AdminDashboard.jsx + 26 V116 tests + AV116). V116 deployed mid-session via "deploy แล้ว"; V115 rode the same deploy. V117 birthed after user L1 iPhone test caught V115 mobile lightbox STILL not closing — root cause: nested position:fixed inside StaffChatPanel got bounded by panel on iOS Safari. V117 = createPortal mandate for 5 fullscreen lightboxes + NEW AV117 invariant (closed sanctioned list of 5; companion to AV114). V21 fixup absorbed (ChartSection marker reorder for V83 M2.1 4-line lookback). Detail: `.agents/sessions/2026-05-23-eod-v116-v117.md` + AV116-117 in audit-anti-vibe-code.
- **Date (2026-05-23 EOD+1 LATE+2) — V114 SHIPPED + DEPLOYED · V115 SHIPPED LOCAL (mobile lightbox UX)**: master = `cdabfabc`. Vercel prod = `9dd176df` (V112-A + V113 + V113-C + V114 LIVE, aliased `https://lover-clinic-app.vercel.app`); V115 PENDING deploy. vitest **14318/14318 GREEN**; build clean ✓ 2.84s. V114 = receipt-info toggle in preview header (Q3=B default OFF / PDPA + Q5=A shared key); 5 commits (T1-T5) + Rule Q L1 verified on real prod via Chrome MCP. V115 = mobile staff-chat lightbox UX fix per user-reported iPhone bug "ปิดพรีวิวไม่ได้ + ซูมไม่ได้": backdrop close (corrects pre-V115 AV78-NORMAL ship that contradicted AV78 sanctioned-exception spec) + `env(safe-area-inset-top)` + 44pt iOS HIG close button + multi-touch bail + double-tap-zoom (1x↔2.5x); class-of-bug expansion to TreatmentReadOnlyMirror + TreatmentReadOnlyPanel (safe-area + 44pt only); NEW **AV114** invariant codifies 4-gate contract for fullscreen image lightboxes; 2 V21 fixups absorbed (`staff-chat-any-file AF5` flipped to AV114 + V115 test uses AV41 ORIGINAL_FETCH pattern). Detail: `.agents/sessions/2026-05-23-eod-v114-v115.md` + AV111-114 in audit-anti-vibe-code.
- **Date (2026-05-23 EOD+1 LATE+1) — V111 SHIPPED + DEPLOYED + e2e 18/0 verified on real prod**: master = `1305d040`. Vercel prod LIVE (V111 client-only — no rules/index/Cloud Run change). vitest **14215/0** GREEN; build clean ✓ 3.08s. User report (screenshot, INV-20260520-0010): admin set "ชื่อคอร์ส (แสดงในใบเสร็จ)" but receipt still printed original. `/systematic-debugging` Phase 1+2: V47-family display-layer multi-reader-sweep at the snapshot boundary — 3 buy-fetcher mappers (SaleTab loadOptions, TFP loadOptions, QuotationFormModal entry builder) silently dropped `shape.receipt_course_name` (V44 canonical mapper had exposed it; consumers ignored for ~6 months). **Fix architecture (Option β — receipt-only, snapshot-at-write)**: parallel field — `receiptCourseName` carried alongside canonical `name`; `name` stays original for non-receipt consumers (customer.courses + treatment dropdowns + reports UNCHANGED); only SalePrintView + QuotationPrintView prefer `c.receiptCourseName || c.name` fallback chain. Snapshot semantic preserves legal-record integrity. **5 sites wired** (3 writers + 2 readers): SaleTab loadOptions + confirmBuy · TFP loadOptions + confirmBuyModal · QuotationFormModal entry · SalePrintView grouped + legacy flat · QuotationPrintView course. **NEW AV111** (HIGH priority). **Tests** (`tests/v111-receipt-course-name-override.test.js` 31/0): A1-A10 source-grep + anti-regression on exact pre-V111 lines + B1-B3 mapper contract + C1-C11 fallback chain with adversarial (Thai full-width + emoji + RTL + 10K-char + V11 separation guard) + D1-D6 Rule I full-flow simulate + E1 AV111. **E2E** (`scripts/e2e-v111-receipt-course-name-override.mjs` **18/0 PASS on real prod**): OVERRIDE→renders override; NO-OVERRIDE empty→falls back; LEGACY pre-V111 shape (no field)→falls back (backward-compat with INV-20260520-0010 etc.); adversarial null→falls back; idempotency stable; parallel-field separation intact; quotation parallel path verified. 8 fixtures cleanup confirmed; audit doc `be_admin_audit/v111-e2e-1779481944225-7ec77025` preserved. **Honest scope**: L2 verified (source-grep + Rule I flow-simulate + full vitest + real-prod admin-SDK e2e). L1 UI hands-on optional (not blocking — e2e covers the chain end-to-end). Historical receipts (INV-20260520-0010 etc.) intentionally unchanged (snapshot semantic by design; retroactive backfill is a separate Rule M op requiring explicit authorization). 2 commits this session: `4b3cd9ab` fix + `1305d040` e2e. Detail: V111 row in `.claude/rules/00-session-start.md` § 2 + AV111 in `.agents/skills/audit-anti-vibe-code/SKILL.md`.
- **Outstanding cleared (2026-05-23 EOD+1 LATE)**: all user-triggered items reported done by user ("ทำ Outstanding หมดแล้ว") — V110 L1 fresh-docx-upload verify + Cloud Console "Owner" role revoke from firebase-adminsdk-fbsvc SA. No pending outstanding; awaiting next task.
- **Date (2026-05-23 EOD+1 LATE) — V110 Office preview font fidelity (Thai fonts + Cordia→Loma alias + LibreOffice Word-compat XCU, AV110)**: Cloud Run rev `office-to-pdf-00007-tfb` LIVE (V110-bis fonts + compat); Vercel UNCHANGED. vitest 14161 + 23 V110 = 14184/0 targeted. User report: side-by-side screenshots ours-vs-Word — "การจัดเรียงเกือบจะเหมือน word แต่ขาดนิดนึง … ทำไงได้บ้าง ให้เหมือนเลย 100%". `/systematic-debugging` Phase 1 via Rule R diag (`diag-docx-font-inspect.mjs`): user's .docx theme1.xml `<a:font script="Thai" typeface="Cordia New"/>` — ALL Thai body via theme. Cordia New = MS proprietary CANNOT redistribute. Gotenberg base ships only Noto Sans Thai → LO substitutes → different character widths → line-wraps mismatch Word. **Honest scope** (told user UPFRONT): 100% pixel-match is **engine-bound** (LO ≠ Word render algorithm for Thai CTL), industry-wide reality. Best achievable ~85-95%. **3-layer fix**: (a) `fonts-thai-tlwg + fonts-thai-tlwg-otf` apt install + NEW `functions/officeToPdf/fontconfig-thai.conf` (13 strong-binding aliases: Cordia/Browallia/Angsana + UPC variants → Loma/Garuda/Norasi metric-equivalents) + fc-cache; (b) NEW `functions/officeToPdf/fontDetector.js` (fflate-based pure-JS docx unzip + fontTable.xml + theme1.xml parser) + index.js pre-conversion logging → Cloud Run log captured `'Cordia New → Loma'` from real user-docx conversion (PROVES alias firing); (c) V110-bis NEW `functions/officeToPdf/libreoffice-compat.xcu` deployed to `/home/gotenberg/.config/libreoffice/4/user/registrymodifications.xcu` enabling 16 Word-compat flags (UsePrinterMetrics + AddSpacing + UseLineSpacing + NoExtLeading + MsWordCompTrailingBlanks + ExpandWordSpace + ConsiderWrappingStyle + CTLFont + 8 more). **AV110** invariant added. **Verification (Rule Q L2 with REAL user docx)**: 3 distinct md5s confirm 3 distinct outputs across pre-V110 / V110-fonts-only / V110-bis. **User verdict** (verbatim): "เหมือนเดิม แต่ไม่เป็น คงเป็นไปไม่ได้" — V110-bis visually similar to V110-fonts-only on THIS doc (compat flags didn't add visible improvement; kept for defensive correctness — may help other docs). User accepted engine-bound limit. **Lessons** (locked permanent): (a) Thai docx fonts come from THE THEME (`script="Thai" typeface="..."`) not the rFonts tags — readers MUST parse theme1.xml; (b) MS proprietary fonts (Cordia/Browallia/Angsana) cannot be installed — canonical mitigation is metric-equivalent free Linux fonts via fontconfig alias (Loma/Garuda/Norasi); (c) LibreOffice render engine ≠ Word render engine even with identical fonts — fundamental limit; (d) per-conversion font-detection logging is essential observability. Files: 7 new + 5 modified + .gitignore (excludes `.tmp-docx-inspect/` real PHI). Detail: V110 row in `.claude/rules/00-session-start.md` § 2 + AV110 in `audit-anti-vibe-code/SKILL.md`. **About to commit + push.**
- **Date (2026-05-23 EOD+1) — V109 Office preview stuck-pending FIXED + heal applied + Cloud Run redeployed + Rule Q L2 re-verified 11/11**: Cloud Run prod LIVE `office-to-pdf-00005-q54` (was 00004-*; redeployed via `gcloud run deploy --source functions/officeToPdf`); Vercel prod UNCHANGED (client unaffected by this fix). vitest +10 V109 PASS (full-suite pending end-of-batch); build clean. User report (screenshot, both local + prod): 3 staff-chat 2.8 MB .docx attachments showed ⚠ instead of 👁. `/systematic-debugging` Iron Law: root cause from REAL data BEFORE any fix. **Root cause** = `functions/officeToPdf/index.js:42,72` used BARE `db.collection('be_staff_chat_messages').doc(messageId)` instead of Rule M canonical `artifacts/${APP_ID}/public/data/be_staff_chat_messages`. Cloud Function ran successfully — Gotenberg/LibreOffice converted; cached PDF at correct Storage path with `contentType=application/pdf` (Rule R diag proved: 2/4 stuck docs had `.docx.pdf` cached at 52671 B) — but `tx.get(messageRef)` on bare path returned `!snap.exists` → silent `console.warn('message not found')` → return without patch → status stayed `pending` forever → 60s Path B → ⚠. **V66 mirror amplifier**: 3 L2 verify scripts (`diag-office-preview-comprehensive.mjs`, `diag-office-preview-deploy-verify.mjs`, `e2e-staff-chat-office-preview.mjs`) ALL wrote test fixtures at the SAME bare path → they agreed with the function's bug → claimed "11/11 verified" while real prod stuck. Test-vs-code-shared-wrong-assumption (V66 family). **Reference (correct, was right there)**: pre-existing `functions/index.js` uses `BASE_PATH = artifacts/${APP_ID}/public/data` — same project, correct pattern, not followed. **Fix (Rule P 7-step)**: Cloud Function `MESSAGES_COLLECTION_PATH` + `db.doc()`; 3 L2 scripts use canonical (would catch bug if re-introduced); NEW `tests/v109-office-preview-canonical-path.test.js` 10/0; NEW **AV109** invariant (CRITICAL); NEW `scripts/v109-heal-stuck-office-attachments.mjs` (Rule M two-phase) — applied: 2 docs healed pending→ready with reconstructed download URLs from existing Storage tokens, audit doc `be_admin_audit/v109-heal-stuck-office-1779475851250-9c23a8a8`, idempotent (re-run 0 writes); the OTHER 2 stuck docs lack cached PDFs (older 15:39+16:30 uploads, pre-deploy or pre-rev-00004) — user must re-upload. **Rule Q L2 re-verify post-fix**: `node scripts/diag-office-preview-comprehensive.mjs` 11/11 PASS with CANONICAL-path fixtures (the eliminated V66 mirror would have caught this bug pre-fix). 14161 targeted tests; cleanup zero orphans. Detail: `.claude/rules/00-session-start.md` V109 + AV109. **Lessons**: (a) test fixtures must use the SAME path the REAL client uses, NEVER the same path as the code-under-test — that's the V66 trap multiplier; (b) a reference in the same repo IS the spec — pre-existing `functions/index.js` was the correct model and I missed it; (c) silent log warnings (`console.warn`) hide V66 bugs that admin SDK ops bypass; (d) check the artifact AND the side-effect — the cached PDFs in Storage immediately localized the bug to "Firestore patch step, not conversion step". NOT YET COMMITTED/PUSHED.
- **Date (2026-05-23 EOD) — Office (Word/Excel/PPT/CSV) preview SHIPPED + DEPLOYED + Rule Q L2 11/11 verified**: master = `0dda0eae`. Vercel prod LIVE (T1-T9 client UI + Path B 60s timeout fallback + Storage-ref + lightbox R6); Cloud Run prod LIVE — `office-to-pdf` service @ asia-southeast1 rev 00004-* (Gotenberg+LibreOffice Docker, `gcloud run deploy --source` — Firebase Functions 2nd Gen ignored Dockerfile, pivoted at deploy time); Eventarc trigger `office-to-pdf-onfinalize` wired to bucket `loverclinic-opd-4c39b.firebasestorage.app`. vitest 14140→14151/0; build clean. **Rule Q L2 comprehensive verify (real prod, no mocks)**: 11/11 fixtures — real docx/xlsx/csv `ready` + PDF accessible with correct contentType; 3-parallel stress; corrupt/empty docx LibreOffice-permissive `ready` (graceful); .odt MIME-gate skip; 100KB→743KB-PDF (mimics user's 2.8 MB upload). **2 bugs caught + fixed during L2 stress**: (a) `FieldValue.serverTimestamp()` nested in array element → Firestore rejects → `new Date()`; (b) non-atomic read-modify-write `stampAttachment` → race under parallel uploads (3/10 stuck) → `db.runTransaction()`. AV108 amended with closed sanctioned exception (in-project Gotenberg). 14 commits + spec + plan HTML. Detail: `.agents/sessions/2026-05-23-office-preview-shipped.md`.
- **Date (2026-05-22 EOD+2) — Storage-ref chart fix + chart fullscreen button + lightbox round-6 (button-area fix)**: master/prod = `1e88ed11` (UNCHANGED — local commits pending). The EOD+1 "invalid nested entity" bug ROOT-CAUSED via Rule Q L2 client-SDK bisect: `detail.charts[0].dataUrl` was **2.26 MiB inline base64** (multiplier:2 × user-uploaded high-res template). Firestore rejected the whole `detail` map. Fix = Storage-ref architectural (per user "แยกรูปไปไว้ที่อื่นสิวะ ข้อมูลสำคัญ" — NO quality degradation). NEW `src/lib/chartImageStorage.js` uploads PNG to `uploads/be_treatments/{customerId}/chart-{ts}-{rand}.png` (EXISTING storage.rules covers — no probe-deploy). multiplier:2 KEPT. ChartCanvas.handleSave async; chartEntryForPersist passes storagePath; deleteBackendTreatment cleans Storage cascade. Plus chart fullscreen button in TFP (Maximize2 → ChartLightbox mirror canonical AV78 pattern). Plus staff-chat lightbox ROUND-6: ONE `<img>` (no keyed remount) + Blob cache pre-warm + buttons ALWAYS mounted (disabled at edges, not unmounted — phantom-missing-button bug) + hit zone w-20 with vertical cap (top-16 bottom-16, doesn't overlap ✕ or filmstrip). vitest 14072/14077 (5 fail likely V21 lock-ins from shape changes — triage next session). NOT YET COMMITTED/DEPLOYED. Detail: `.agents/sessions/2026-05-22-storage-ref-chart-fullscreen-lightbox-r6.md`.
- **Date (2026-05-22 EOD+1) — Lightbox round-5 + Chart templates persistence rewrite SHIPPED + DEPLOYED**: master/prod = `1e88ed11`. (1) Staff-chat lightbox prev/next race FINAL — REMOVED opacity gate entirely after 4 prior rounds chased loadedSrc through onLoad / ref / useEffect+complete / decode() / Set-of-loaded-URLs. Architecture: two stacked `<img key={idx}>` (blurred thumb behind + full front), zero state, zero gate, zero effects → race-immune by construction. (2) Chart template persistence rewrite — was writing to `pc_chart_templates` (no matching rule → silent permission-denied) AND 1MB doc cap with inline base64. Now per-doc in NEW `be_chart_templates` collection + Firebase Storage `chart-templates/{id}.{ext}` for image bytes + `locked: true` for built-in seeds + localStorage `lover-chart-template-order-v1` for per-device sort. Rules deployed (firestore + storage; probe #14 added). 47/0 targeted tests + build 3.20s + 8/8 P-D-P probes. **NEW BUG INVESTIGATING (next chat blocker)**: after uploading new chart template via "+ เพิ่ม" → using it in TFP chart → click ยืนยันการรักษา → Firestore error "Property detail contains an invalid nested entity". Node repro of charts[]+clean shows safe shape (template field dropped by chartEntryForPersist, no Timestamps survive) — so bug is somewhere OTHER than detail.charts[]. Diag script `scripts/diag-chart-template-save-shape.mjs` ready to run next chat. Detail: `.agents/sessions/2026-05-22-lightbox-r5-and-chart-templates.md`.
- **Date (2026-05-22) — Staff Chat MULTI-IMAGE attachments SHIPPED + DEPLOYED + real-prod verified**: master/prod = `a90b6706`. Send ≤10 images/staff-chat-message; adaptive grid (1/2/3/4/5+ "+N") → swipe lightbox; hybrid thumb + original ≤50MB; auto-retention cron (daily 30d) deletes whole message + images with NO orphan ("ลบเกลี้ยง"). DEPLOYED (user "deploy เพื่อเทส"): `vercel --prod` + `firebase deploy --only firestore:rules,storage` (P-D-P #9/#10 green pre+post). **Rule Q L1/L2 + Q-vis verified real prod (every step by SCREENSHOT, no bugs)**: ลบจริง (sweep `deletedFiles:12` → `getFiles=0` + doc gone) · อัพ/ส่งจริง (real composer multi-pick → Storage upload + `setDoc` attachments[] rule-accepted → grid render) · preview จริง (5-img grid 2×2+"+1" + lightbox counter/next/prev/filmstrip/end-clamp/close/download). vitest 14030/0. AV108. **2 follow-up polish issues PENDING (next chat, /systematic-debugging)**: (1) lightbox backdrop-no-close (✕/Esc only, AV78); (2) grid images overflow bubble → pro design. User pre-authorized deploy after those are tested-pro. Detail + resume: `.agents/sessions/2026-05-22-staff-chat-multi-image.md`.
- **Outstanding cleared (2026-05-22)**: all user-triggered items reported done by user ("ทำ Outstanding หมดแล้ว ลบทิ้งไป") — on-device iPad zoom confirm + carryover (V106 cron drain / calendar-density / Recall / V108 L1). No pending outstanding; awaiting next task.
- **Date last updated**: 2026-05-21 EOD+3 LATE — **tablet-chart pinch-zoom (1-4x) + palm-rejection RE-SHIPPED + DEPLOYED + verified LIVE on prod** (Master/Prod = `e71ef782`; `vercel --prod`, aliased; frontend-only, NO rules change). The REAL iPad black-screen cause = a **React `insertBefore` crash** (the ⤢ fit button rendered BEFORE the Fabric-wrapped `<canvas>` → `surf.insertBefore(button, canvas)` on zoom → tree unmount → blank); fix = render the fit button AFTER the canvas (append). The `upperCanvasEl`-listener lead was unconfirmed (kept as defensive). **Found by reproducing the black screen ON DESKTOP** via a synthetic 2-touch pinch (Chrome MCP — drives my gesture layer, which doesn't gate `isTrusted`). **Rule Q-vis** added (`01-iron-clad.md`: UI evidence = a SCREENSHOT you LOOK AT, not a pixel-probe/code; probe-vs-screenshot → screenshot wins). All 9 tools + undo/redo/clear/delete/save-relay + pinch-zoom-4x + fit-reset verified by SCREENSHOT on the prod URL — NO crash. vitest 14007/0; build clean; 0 orphan. Detail: EOD+3 LATE block + `.agents/sessions/2026-05-21-zoom-reship-real-fix.md`.
- **Date (prev EOD+2)**: 2026-05-21 EOD+2 — **Tablet-chart batch DEPLOYED + comprehensively verified.** re-edit-a-saved-chart-ON-TABLET (editFabricJsonUrl relay leg + object-level hydrate + same-slot merge — §followup-6) + 2 tool-bug fixes (arrow vanished-on-release → drag-delta commit; text trapped-in-editing/no-handles → addText no-auto-edit — §followup-7 + AV106, via `/systematic-debugging`). **Object-level re-edit (PC + tablet) now LIVE** (storage rules deployed, P-D-P #13). Verified: vitest 13970/0 + L1 real-browser ALL 9 tools + **11 edge cases** (text-width / scrub-erase / undo-redo / production+raster re-edit / double-hydrate / re-edit-textbox — all via `getImageData` REAL PIXELS) + L2 e2e prod ALL PASS. **No product bugs found** in the adversarial pass (2 apparent issues were probe-measured-too-early, confirmed via polling).
- **Date (prev)**: 2026-05-21 EOD+1 LATE+4 — Tablet Chart more-tools: user confirmed fix4 ("โอเคใช้ได้แล้ว"). Real-use round-trip VERIFIED on real prod (14/0: persist to OPD + re-edit + fresh image + stress) → object-level RE-EDIT completed (ChartCanvas now consumes the persisted fabricJson, verified in a real browser) + 1MB-persist size guard. Awaiting deploy.
- **EOD+3 (2026-05-21)**: **Master/Prod = `00a9da2f` (revert) LIVE** — zoom+palm pinch feature **REVERTED** (iPad/WebKit black screen on 2-finger zoom; Rule A); `handleSave "null"` fix `7a4b7f47` **retained**; editor = pre-zoom more-tools version. (Prior EOD+2 batch `1bfe1767` superseded by the revert; V106 + V108 still live.) **NEXT (user directive)**: reliably USE **Chrome MCP** (Rule S) + full Chrome-MCP test of the tablet canvas editor (every tool + function), then stop. **[SUPERSEDED EOD+3 LATE — that test was done; zoom RE-SHIPPED with the real fix + DEPLOYED (`e71ef782`) + verified LIVE on prod; see the lead bullet + EOD+3 LATE block.]**
- **Tests (EOD+3)**: chart-area **38/0** green post-revert; build clean. The zoom feature's tests (chart-gesture-math + zoom flow-simulate) were removed by the revert; re-edit/tool-bugs/AV106 tests retained. (EOD+2 baseline was full vitest 13970/0.)
- **Deploy**: ✅ **DONE this session** (V15 combined, user-authorized "ผ่านหมดจริงๆค่อย deploy") — `vercel --prod` (aliased) **+** `firebase deploy --only storage` (**P-D-P #13**: anon 403 pre+post; rules compiled + released). **Object-level re-edit unlocked LIVE** (PC + tablet). Remaining: on-device L1 by user. [⚠ firebase CLI 15.x: `--only storage`, NOT `storage:rules`.]
- **Findings (flagged)**: object-level re-edit is **live-gated on the storage deploy** (client json upload denied until then → fabricJson null → raster fallback, which works). Pre-existing limit: a single chart PNG dataUrl > ~1MB risks the Firestore doc cap — Storage-ref is the architectural follow-up (decide separately; the new size guard prevents the fabricJson from compounding it).

### Session 2026-05-31 EOD+4 LATE+3 — V143 stock cluster (show-0 + real-time + auto-clear) + V142 course double-deduct — SHIPPED + DEPLOYED

`/systematic-debugging` ×2. master = prod = `0c607f68` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `8c3a9047`). Frontend/lib + 1 new cron → no Probe-Deploy-Probe. Detail: `.agents/sessions/2026-05-31-v142-v143-deploy.md`.

- **V142** course double-deduct shipped (V142-quater over-credit gate + V142-quinquies finalize→doctor→finalize via persisted `_courseDeducted` flag). Doctor-save clarified: must NOT save course-deduction (bottom save owns it). Verified matrix 30/0 + flag-roundtrip/fuzz/stock **L2 e2e 30/0** real prod.
- **V143** StockBalancePanel shows products drained/cleared to exactly 0 (`status∈{active,depleted}`; 7 NK products were hidden). AV166.
- **V143-ter real-time** NEW `listenToStockBatchesByBranch` (Layer-1 onSnapshot BS-13 safe-by-default + Layer-2 wrapper; panel → live listener). AV167. **L2 e2e 5/0 real prod** (`e2e-stock-balance-realtime.mjs` — writes from another surface push LIVE).
- **V143-quater auto-clear-lot** pure `planLotCleanup` (keep live + ≤1 zero placeholder, DELETE-only) + daily cron `stock-lot-cleanup` (03:45 BKK, vercel.json) + Rule M script. AV168. 14/0 + dry-run 0-redundant.
- **Rule M APPLIED on prod** (earlier this session): NK reset `v143-nakhon-stock-reset.mjs --apply` (51 batches→0/0, 364 docs deleted) + `v143-collapse-nk-multilot.mjs --apply` (→ 53 products = 53 lots at 0); idempotent + audited.
- **3 V21 test-fixups** (v143 SG1 + v138 N10.7 + v34 INV.11.4 — same V143-ter panel-migration class; full suite confirmed no 4th). **Full vitest 15418/0** (JSON-reporter) + build clean.
- **Honest gap (Rule Q)**: 2-device real-browser live-balance + NK-shows-0 = USER L1. Cron now active (optional CRON_SECRET hit to verify like V122).

### Session 2026-05-28 EOD+4 — 4-feature batch V128 + V129 (UNCOMMITTED, held — no deploy)

5× `/systematic-debugging` in one session; all 4 fixes complete + per-feature-verified; **feature code UNCOMMITTED** (held per user "ยัง deploy ไม่ได้ มีงานถัดไป" — only agent-docs committed). master code HEAD `10e28ed4`; prod `26fb5789` (V127) — none deployed. All frontend/lib → no Probe-Deploy-Probe; ONE combined `vercel --prod`.

- **V128 appt phone** — hover-peek + detail-modal show the linked customer's phone. Write-chokepoint `_resolveAppointmentCustomerPhone` (backendClient create+update) + `useResolvedApptPhone` render live-resolve + `apptPhoneValue||resolvedPhone`. L2 real-prod: 78 linked + 45 temp = 0 blank. AV145.
- **V128 calendar auto-height** — `AppointmentCalendarView` fixed `SLOT_H=22` → dynamic `slotH` (computeApptSlotHeight, fills viewport, clamp 22–46).
- **V128.lb staff-chat lightbox** — full-screen `width/height:100%` of `w-full h-full` wrapper (object-contain upscales small imgs; was max-w-4xl 896px) + zoom + drag-pan (clamped) + wheel-zoom. **Chrome-MCP real-browser verified** (fill+zoom+pan). AV146.
- **V129 reports-sale** — `tab=reports-sale` พนักงานขาย + ผู้ทำรายการ resolve via `resolveSellerName(listAllSellers)` (aggregator + SaleReportTab + SaleDetailModal). L2 real-prod `-` count 38→0. AV147. `createdBy` unwritten → ผู้ทำรายการ = resolved first seller (optional follow-up: capture true createdBy at sale-write).

Tests (no EOD re-run per rule): last FULL 15039/0 (pre-lightbox+reports); targeted since GREEN (lightbox-family 104 · v128 7 · v129 12); build clean; full re-run pending at deploy. 5 V21 source-grep fixups absorbed. Diag/verify scripts kept (Rule R). Detail: `.agents/sessions/2026-05-28-v128-v129-batch.md`.

### Session 2026-05-28 EOD+1 — V125 light-theme WCAG-AA (treatment form) — SHIPPED + DEPLOYED + prod-verified

`/session-start → "Outstanding ทำเลย"`. Re-proved the 3 outstanding L1 items live (appt real-time strip 2→3 / 3→2 cross-process via node admin-SDK write/delete, per-branch; chart relay PC-side pairing modal + accurate "no tablet in branch" presence). The treatment-form L1 contrast scan (Chrome MCP, real prod, light theme) FOUND **19 light-theme AA fails V124's class-based `[data-theme=light]` CSS overrides couldn't reach** — because the treatment form sets accents via **inline `style={{color:'#…500'}}`** (no class to match) + uses an **arbitrary-hex `bg-[#7c3aed]` CTA**. This was the one surface V124 never individually scanned.

**Fix (2 classes)**: (1) NEW `src/lib/themeAccent.js` `aaAccent(hex,isDark)` deepens -500/-400 → -700 AA-dark in light, pass-through dark — wired into `SectionHeader`+`ActionBtn` (component-level) + 12 inline spans + `ChartSection` + `TreatmentTimeline` (2 hardcoded). (2) index.css white-restore for `.bg-\[\#7c3aed\].text-white` (doctor-note save btn, was 3.05:1 dark → 5.2 AA white). Teal #2EC4B6 (7.76) + LINE-green #06C755 (7.67) left dark — already AA, NOT restored.

**Verified (Rule Q / Q-vis)**: T7 **20/0** (AA-math every mapped target ≥4.5 on white + source-grep) + build clean + full vitest **14990 pass + 1 known flake** (phase15.5b global.fetch-leak, passes 51/0 isolated — not V125) + **post-deploy re-scan on the LIVE deployed build** (gold-standard, V124-fix2 pattern): treatment form 0 accent fails (1372 els) + sale/finance tab 0 (2186 els, no regression) + appt view 0 + zoom (violet white, headers deepened, teal correctly dark). Commit `f56bfa9b` → `vercel --prod` (CSS/JS only, no rules → no Probe-Deploy-Probe). Wiki updated (entity `themeAccent.js` + concept `light-theme-aa`); `python -m graphify update .` ran (graph.json refreshed). **PatientForm.jsx light-theme deferred** — bespoke `isDark?dark:light` brand colors (pink/rose), needs a DESIGN pass not a mechanical wrap. Detail: checkpoint `.agents/sessions/2026-05-28-v125-light-theme-aa.md`.

### Session 2026-05-27 EOD+13 → 2026-05-28 — Light-theme WCAG-AA audit (App UI) + brand-red red-700 — SHIPPED + DEPLOYED

- **State**: SHIPPED. master HEAD `9f6cc49d` (a4731775 light-theme + 9042934a Rule-S docs + 9f6cc49d handoff); pushed; `vercel --prod` LIVE `9042934a` (was 8f6b7ced) + prod-verified (--accent-red #b91c1c). full vitest **14976/0** (678 files) · build clean · NO rules/storage/data/cron → frontend+theme-config (no Probe-Deploy-Probe).
- **Scope (user directive)**: ไล่ดู light theme ทุกหน้า/ทุก modal แล้วปรับให้ชัด — **CSS/colour/theme-config ONLY, ห้ามแตะ wiring/flow/logic**. Brainstorm-locked: WCAG **AA 4.5:1** · Hybrid central-first · App-UI (exclude print/doc) · full live-browser pass.
- **Architecture understood**: light theme = a giant `[data-theme="light"],[data-theme="auto"]` override layer in `src/index.css` remapping hardcoded dark Tailwind classes → light. Failure = any uncovered dark class / light-pastel text on light = invisible/sub-AA.
- **Fixes (all theme-layer)**: FM-A uncovered dark surfaces → light tokens · FM-C colour-text -300/400/500 → AA-dark · **alert-box ext** (17 colours: `bg-{c}-700..950/alpha`→{c}-50 + `.text-{c}-50..300`→{c}-700; 1.0–1.3 → 4.8–6.5:1) · FM-D **14 inline `color:#hex` → `--accent-{blue,line,purple,amber,red,teal}` vars** (CustomerDetailView · LinkLineInstructionsModal · LinkRequestsTab · TreatmentTimelineModal · ClinicSettingsPanel) · teal #0d9488→#0f766e · `--tx-muted` #64748b→#5b6675 · green-500/600 → #15803d.
- **tailwind darkMode root-fix (USER-APPROVED "Global config")**: `darkMode: ['selector','[data-theme="dark"]']` — was unset (=media) so `dark:` (82 uses/15 files) coupled to OS prefers-color-scheme → light-theme on a dark-OS machine rendered dark-variant colours (invisible finance/wallet). AV136 class, fixed globally.
- **Verified (Rule Q-vis — REAL browser, measured per-page contrast scanner)**: CLEAN on 7 surfaces (sale·reports·promotions·finance[synthetic]·appointment-hub·customer-detail·frontend-appt) · darkMode fix proven BOTH directions (synthetic + frontend-appt reload) · dark theme NO regression (screenshot). The deep-scan (user push "ตรวจหมดจริงๆเหรอ" — justified) surfaced teal/muted/green/dark: that eyeballing missed. All fixed.
- **✅ Brand-red RESOLVED (user chose darken 2026-05-28)**: `.text-red-400` (261 uses) + `--accent-red` light → red-700 `#b91c1c` (was #dc2626, 4.37:1 sub-AA on tint → **6.47:1** white). Verified real browser (computed rgb(185,28,28) + screenshot on white + tinted card) + prod-verified. Other red shades already #b91c1c. Strict-AA regression guard added to T7.
- **⚠ HONEST scope gap (unchanged by ship)**: scanner ran 7 surfaces; **stock/master-data/treatment-form/chat/settings/deep-modals NOT individually scanned** (collapsed-section + drawer nav resisted programmatic clicks; login wall blocks auto-L1 — security rule). They use the SAME now-fixed global classes + brand-red darken is monotonic-safe → covered by global fixes + need user L1 spot-check on prod light theme.
- **Tier 2**: NEW `tests/light-theme-override-coverage.test.js` (T7 **5/0** — coverage walk + FM-C/A/D + strict-AA brand-red guard) + TL1.6 fixup (`#2EC4B6`→`var(--accent-teal)`).
- **Commits (2026-05-28 ship)**: `a4731775` (light-theme + brand-red, 12 files) + `9042934a` (Rule S TIMING docs) + `9f6cc49d` (handoff) — pushed + `vercel --prod` LIVE + prod-verified. spec/plan/inventory: `docs/superpowers/2026-05-27-light-theme-*`. Older 46 session blocks archived to `.agents/sessions/session-handoff-archive.md` (SH 277→142 KB, under cap).
- Detail: checkpoint `.agents/sessions/2026-05-27-light-theme-audit.md`.

### Session 2026-05-27 EOD+12 — Chart-edit flash FIXED (createPortal) + treatment-image ดูรูปใหญ่ button + light-theme button fix (LOCAL, committed, not pushed/deployed)

- **State**: master HEAD = EOD docs commit (feature `96eb089d`); prod UNCHANGED `8f6b7ced`. Full suite **14971/0** (677 files), build clean ×2. NO rules/storage/data/cron → frontend-only.
- **`/systematic-debugging`** (user report + 3 screenshots): กดแก้ไข Chart in TFP → editor flashed in-box (image1) then full-screen (image2), "เหมือนมีการซ้อนกัน". Root cause = `ChartCanvas` `fixed inset-0` trapped by a TRANSIENT transformed ancestor in TFP (exhaustive static read = NO persistent transform → transient → flash). Same class as AV117 (5 lightboxes already portaled for exactly this); ChartCanvas (editor) + the 2 chart modals were never portaled.
- **Fix Task 1**: `createPortal(…, document.body)` on ChartCanvas + ChartTemplateSelector + PcPairingModal (Rule P siblings) → escapes all ancestor containing-blocks → full-screen frame 1, no flash. NEW **AV143** (editor/modal overlays; companion to AV117 viewers).
- **Fix Task 2**: NEW shared portaled **ImageLightbox.jsx** (extracted from ChartSection.ChartLightbox, Rule of 3) + `Maximize2` "ดูรูปใหญ่" on every treatment + lab image.
- **Light-theme bug** (user 2nd report + screenshot): buttons `bg-black/70 text-white`→`bg-white/90`+gray/red. Cause = `index.css:404` `[data-theme=light] .text-white{color:var(--tx-heading)!important}` (dark) — white-restore exceptions cover colored bgs (bg-red/gray/neutral/…) but NOT `bg-black` → the white icon rendered dark → invisible on the dark button. New scheme mirrors the chart's view-large button.
- **Tier 2 (Rule P)**: AV143 + `tests/v123-chart-overlay-portal.test.js` (13: SG/CON/AV/G); AV117 + `v117-lightbox-portal.test.js` retargeted to ImageLightbox; `pc-pairing-rtl` PP4 portal fixup (container→document query); v83 AV78 inline-marker on ImageLightbox.
- **Verify (Rule Q-honest)**: build clean ×2 · full suite 14971/0 · root cause + fix **CSS-proven in real browser, light theme** (temp probe via preview_eval, removed inline: OLD icon `rgb(15,23,42)` dark-on-dark = invisible; NEW gray-on-white = visible; ref `bg-red-500 text-white`→white confirms the exception excludes bg-black). **GAP (disclosed)**: live in-app TFP-edit (open editor → no flash; hover treatment thumb light theme → white button visible + opens lightbox) NOT driven by me — login/data + the preview renderer wedged by my own reload; user hands-on. Mechanism AV117-proven + render RTL-correct + chart-button precedent.
- **Commits**: `96eb089d` feature (10 files +292/-70) + EOD docs. NOT pushed, NOT deployed (V18). Excluded from commits: CLAUDE.md + rules/01 (user's pre-existing Rule S edits).
- Detail: checkpoint `.agents/sessions/2026-05-27-chart-overlay-portal-flash.md`.

### Session 2026-05-27 EOD+11 — Appointment page LIVE cross-device + CC field row-align (LOCAL, committed, not pushed)

**Shipped (committed local, NOT pushed/deployed)**: master `0c702091` (2 commits above EOD+10 `4b8e3123`); prod `8f6b7ced` unchanged.
- **CC row-align** (`7857a2dd`): TFP left col `space-y-4`→`flex flex-col gap-4` + teal save `mt-auto` → vitals/doctor save buttons land same row. CC `rows` bump = NO-OP (flex-1); real cause = block-vs-flex trailing-`mb-3` ~12px (real-browser measured). Cosmetic.
- **Live cross-device** (`0c702091`): appointment-page card-list (OPD stepper / appt status / deposit-sale chips) now real-time cross-device + all-day. NEW `listenToTreatmentsByDateRange` + `listenToAllDeposits` (backendClient) + scopedDataLayer re-exports; AppointmentHubView subscribes 3 onSnapshot triggers → `liveRefreshTick` (skip-first) → existing `loadAll({silent})` (extends the proven `appointmentDataVersion` pattern). + visibility/online resume + day-rollover guard. NO logic/mutation/render touched.
- **V66 trap caught pre-ship**: branch-scoped sales = `where(saleDate>=)+where(branchId)` = composite index that does NOT exist → FAIL_PRECONDITION in prod (admin-SDK can't see it — the V66 lesson) → fixed to sales `allBranches` saleDate-only (single-field); treatments whole-collection + deposits single-field → all index-free.

**Verify**: full suite 14958/0 (ran 2×) · build clean · **L2 18/18 real-prod onSnapshot** (`scripts/e2e-appointment-live-cross-device.mjs`: appt create/confirm/edit/cancel + treatment vitals→doctor + cross-branch + cancelled/out-of-window filtered + deposit branch-isolation + sale) · **L1 real-browser pixel demo** (ซักประวัติ✓19:27 + แพทย์✓19:28 lit LIVE on 2 windows, no refresh; cleaned up 0 orphans).
**Tests**: +`appointment-live-cross-device.test.js` (12 source-grep + flow-sim) + 6 RTL partial-mock fixes (V11-class: mock missing new exports).
**Flow**: /systematic-debugging (verify current = BROKEN cross-device OPD; appt was already live) → brainstorming (Q1=A listener-trigger, Q2=A treatments allBranches) → writing-plans HTML → executing-plans inline → V66 fix → L2 → L1 pixel demo.
**Outstanding (user-triggered)**: push master (2 commits) + deploy vercel-only (await explicit word, V18); 2 pre-existing Rule S doc edits (CLAUDE.md, rules/01) uncommitted.
Detail: checkpoint `.agents/sessions/2026-05-27-appt-live-cross-device.md`.

### Session 2026-05-27 EOD+9 — Deposit-without-appointment + Finance deposit modernize (LOCAL)
brainstorm→spec.html→plan.html→inline execution (subagent-driven attempted; switched to inline per user + a 1M-context credits error). **Part 1** AppointmentFormModal `ไม่นัดหมาย` toggle → deposit-only doc (hides date/time/หมอ/ห้อง/recurring + skips appt validations; advisor→100% seller; purpose=appointmentTo; +สร้างนัด later works). **Part 2** Finance DepositPanel `เลือกลูกค้าภายหลัง` + `มัดจำสำหรับ` (VisitPurposePicker new `label` prop) + table `|| dep.purpose`; all money fields kept. be_deposits +3 fields (purpose/customerNameTemp/customerPhoneTemp); createDeposit stamps + recalc-guard on empty customerId. **Verify**: full suite 14929/0 · build clean · Rule Q **L2 real-prod e2e 21/0** · flow-simulate + source-grep + VisitPurposePicker RTL. **L1 = USER** (modals not RTL-mounted in repo; Rule Q V66). 10 commits `85c5d579`..`ff9a775d` on **master LOCAL** (10+ ahead of origin `9209ec70`, NOT pushed); prod `0805da87` UNCHANGED. NO rules/data/cron → frontend+serverless deploy only. 7 V21 source-grep fixups; 2 pre-existing flakes (Phase 17.1 + genShortId, probabilistic) pass on clean run. Decisions: Q1=modernize-in-place, Q2=port-all, advisor→seller (no new field), gating=conditional-render ("อิงของเก่า"). Detail: checkpoint `.agents/sessions/2026-05-27-deposit-no-appointment.md`.

### Session 2026-05-27 — V122 backup-subsystem fix DEPLOYED + L2-verified + create-queue button removal + EOD+8 deploy

master `954420d6` (pushed); prod LIVE (`vercel --prod` → lover-clinic-app.vercel.app). Full suite **14892/0**. NO rules/data/cron touched → frontend+serverless deploy only (no Probe-Deploy-Probe).

`/systematic-debugging` on business-critical report: Whole-System V81 backups silently `NO_MANIFEST` since 05-22. Confirmed root cause = ~1000 SEQUENTIAL cross-region round-trips > Vercel 300s cap (real HTTP 504 @300.7s + 20h-stale cron lock) + 28/65 collections silently omitted by hardcoded scope. Fix: `mapWithConcurrency` bounded-parallel I/O (~20×) + dynamic `listCollections()` enumeration across backup + restore + whole-fleet + branch `maxDuration` 60→300; fixed latent orphan-subcoll wipe-order bug. AV141 + AV142. **L2-VERIFIED LIVE**: deployed endpoint → 200 in 38.1s (was 504), complete manifest 4783 docs/0 failed. Real-prod e2e 10/0. Rule Q-honest: 2 "flakes" were real V21 regressions in whole-fleet source-greps — fixed (5 V21 fixups). Also: removed `+สร้างคิวใหม่` button (modal KEPT dormant) + EOD+8 UI shipped same deploy.

Open (optional): clean 5 pre-fix NO_MANIFEST folders; stale QR placeholder AdminDashboard:7541; 2 pre-existing Rule S edits (CLAUDE.md, rules/01) uncommitted. USER L1 on prod. Detail: `.agents/sessions/2026-05-27-backup-v122-deploy.md` + `v-log-archive.md` V122.

### Session 2026-05-26 EOD+8 — 13 UI/UX fixes (3 /systematic-debugging batches) DONE + TESTED, UNCOMMITTED

master `00e4b3a6` (unchanged — EOD+8 work uncommitted); prod `7e2a5bd8` LIVE. 15 files in working tree (9 src + 5 test + AV140), NOT committed (iron-clad: await user "commit").

**B1 (6 + AV140)** — `PatientForm.jsx` success-text trim + TH/EN+moon toggle contrast (inline `color`, both themes — VERIFIED live via computed styles) · `OpdLifecycleRow.jsx` dashed amber frame removed · `treatmentDisplayResolvers.js` getStepLabels doctor-slot else 'ข้าม'→'แพทย์' · `AppointmentHubRowCard.jsx`+`index.css` filled-pending (state-D) card purple breathing+shadow (reduced-motion safe) · `AppointmentHubView.jsx` cardFlowSubPillCounts adds 'opd-pending' bucket → purple tab bubble (NEW **AV140**: TabBar tab keys ⊆ cardFlowSubPillCounts).
**B2 (QR)** — `SendCustomerLinkModal.jsx` QR fills mobile width (max-w-[240px] cap removed · gen 280→600px · modal max-h+overflow-y).
**B3 (OPD review modal, 5 · option A)** — `PatientForm.jsx` admin edit bypasses isExpired(2h)+isArchived when `isSimulation` (public link KEEPS the 2h timeout + 30-min cleanup cron + cross-day delete AV131 UNCHANGED) · `AdminDashboard.jsx` removed "ซิงค์ข้อมูลใหม่" Sync btn + dead renderResyncButton · save "บันทึกลง OPD"→"บันทึกเข้าระบบ" · header "ประวัติผู้ป่วย OPD"→"บันทึกข้อมูลรับเข้า" · hid session ID.

**Tests**: build clean · full suite 14869 pass / 0 deterministic fail · NEW `eod7-ui-fixes-batch.test.jsx` (19/0) + `eod7-opd-review-modal.test.js` (7/0) · V21 fixups `phase-28-resolvers` + `phase-23-0` B.5 + `v118` SG7.5.
**Rule Q-honest WIN**: insisted on a re-run to pin the "1 fail" → DETERMINISTIC regression (Sync removal dropped a `__synthetic` gate count), NOT the flake I almost logged. Lesson: removing a JSX line → grep tests for ALL its tokens (`__synthetic`), not just the fn name.
**Decisions**: OPD-edit = option A (bypass gates for admin only; do NOT rip the 2h-timeout/cron system — user-picked) · breathing purple to unify with the tab bubble · toggle inline-color (beats class override + no JIT) · no AV for cosmetic items (isolated).
Detail/Resume: checkpoint `.agents/sessions/2026-05-26-eod8-ui-fixes.md`.

### Session 2026-05-26 EOD+4 — Staff-chat enhancements SHIPPED + DEPLOYED

`/session-start` → `brainstorming` (Visual Companion via AskUserQuestion previews — Rule S reaffirmed 2026-05-26: NEVER Chrome MCP to verify the companion at ask/plan) → spec → `writing-plans` → `subagent-driven` (pivoted inline; subagent died on a 1M-context billing wall, baseline-thrash documented). master/prod = `459a4ea3` LIVE.

- **F1 day separators** — NEW `src/lib/staffChatDayGroups.js` (pure, Bangkok-TZ GMT+7 shift, dual-shape createdAt) + StaffChatMessageList pill dividers (วันนี้/เมื่อวาน/full พ.ศ.).
- **F2 quote 13px** — StaffChatMessage quote + composer reply strip `text-[10px]→[13px]`.
- **F3 unsend** — NEW `deleteStaffChatMessage(branchId,messageId)` (backendClient + scopedDataLayer) = Storage folder sweep + `deleteDoc`; own-only UI gate (deviceId) + AV78 confirm dialog; useStaffChat.deleteMessage; Widget thread.
- **F4 emoji+stickers** — NEW StaffChatStickerPicker (3 tabs); bundled Fluent-Emoji MIT 20 SVGs `/public/stickers/fluent/` + manifest (src/lib) sent by ID (0 Firebase); custom `stickerLibrary.js` IndexedDB → temp Storage on send (30-day retention); `buildMessageDoc` sticker field (undefined-safe); sticker render in StaffChatMessage.
- **Rules (DEPLOYED, Probe-Deploy-Probe #15 PASS)**: firestore `be_staff_chat_messages` sticker-only create clause + `allow delete: if isClinicStaff()`; storage `staff-chat-attachments` clinic-staff delete.
- **AV134** + 33 tests (unit 15 + flow-simulate 10 + RTL 8) + 2 V21 fixups (storage.rules delete contract in staff-chat-any-file + staff-chat-multi-image).
- **Lessons**: subagent 1M-context billing wall → inline on this baseline (V81/Tablet-Chart pattern); curl/grep choked on the 8 MB single-line GitHub tree → node fetch+JSON.parse; Fluent folder names ≠ CLDR (keyword-match + skin-tone Default path).
- **This deploy shipped EVERYTHING since prod 65ab6467** (staff-chat + 4 carryover stacks) — all now LIVE; user L1 pending.
- Detail: checkpoint `.agents/sessions/2026-05-26-staff-chat-enhancements.md`.

### Session 2026-05-26 EOD+3 — /systematic-debugging 3-fix batch SHIPPED (LOCAL)

`/session-start` → `/systematic-debugging` (Phase 1 root-cause for all 3 via code, no guessing). master = `e07451fb`; prod UNCHANGED `65ab6467` (await "deploy", V18). Full vitest **14731/0** · build clean 2.91s · NO rules/index → no Probe-Deploy-Probe.

- **Issue 1** — Finance "ไปที่นัด" opens the appt's DATE (was today). Late-prop today-lock: default `activeTab='appointment-all'` mounts AppointmentCalendarView before the deep-link useEffect set `initialApptDate` → `useState(()=>)` locked to today. Fix: synchronous `?date=` derive in the BackendDashboard useState initializer + `[initialSelectedDate]` prop-sync effect.
- **Issue 2** — create-appt default start = branch open hours (`getOpenHoursForDate`), was hardcoded '10:00'. Initializer + re-apply effect (keyed on date+cs, not startTime). **Rule P siblings**: `AppointmentCalendarView.openCreate` (`time||''`) + `DepositPanel` deposit-appt sub-form.
- **Issue 3** — Frontend นัดหมาย cancel HARD-DELETES (`deleteBackendAppointment`, mirrors Backend) not `status:'cancelled'`; V125 session-archive cascade preserved (`appt-deleted`).
- **AV133** + NEW `tests/finance-goto-default-time-cancel-delete.test.js` (I2 = REAL getOpenHoursForDate, L2) + 3 V21 fixups (v125 SG-A3 · phase-19-0 C2.1 · phase-24-0 VOC.B.1).
- **Rule Q-honest**: logic L2 + source-grep + full suite; real-browser render/delete round-trip = USER L1 post-deploy (auth-gated, workstyle "ไม่ self-test UI") — disclosed.
- Detail: `.agents/sessions/2026-05-26-finance-goto-default-time-cancel-delete.md`.

### Session 2026-05-26 EOD+2 — Frontend 4-tab removal + deposit-aware cancel dialog SHIPPED (LOCAL)

Full `/session-start` → `brainstorming` (Visual Companion via AskUserQuestion previews) → spec → `writing-plans` → `executing-plans` (T1–T11, Rule K). master = `e84d2538`; prod UNCHANGED `65ab6467` (await "deploy", V18).

- **Part 1** — removed 4 tabs (คิวหน้า Clinic / จองไม่มัดจำ / จองมัดจำ / ประวัติ): default landing → นัดหมาย + redirect guard in the `setAdminMode` wrapper + repointed nav; tab buttons removed (desktop + mobile dock); 5 dead render branches excised (~750 lines, chain now chat→clinicSettings→appointment); orphaned `showMobileJongPicker` removed. KEPT deposit/noDeposit state + create-forms (still used by viewingSession resolver + สร้างคิวใหม่ — not orphaned).
- **Part 2** — deposit-aware cancel dialog (AV132): NEW `resolveDepositCancelState` + shared `DepositAwareCancelDialog` (both|this-only|cancel; hard-delete via `deleteDepositBookingPair`; used-deposit blocks delete) wired into ALL 3 cancel surfaces (นัดหมาย / AppointmentCalendarView / Finance·มัดจำ — fixes the Finance orphan-appt gap). AppointmentFormModal flip-away left as-is.
- **Verify**: full vitest **14712/14712 — 0 fail** · build clean · real-prod e2e **31/0** (`scripts/e2e-deposit-cancel-dialog.mjs` — ran the REAL decision helper on 11 REAL prod deposits + both/keep/used-block cascade on TEST- fixtures). 11 V21-fixup test files (flipped removed-render/old-calview/trigger-count assertions). **NO rules/index change → no Probe-Deploy-Probe.**
- **Decisions**: Q1=นัดหมาย default · Q2=นัดหมายครอบคลุมหมด · Q3=ลบหายเลย hard-delete · Q4=ทุกที่ที่ยกเลิกได้ (3 surfaces).
- **Rule Q-honest**: deposit-cancel LOGIC = L2 (e2e real prod); tab-removal RENDER = build + markers + suite, **real-browser render L1 = USER post-deploy** (workstyle ไม่ self-test UI + auth-gated AdminDashboard; not driven by me — disclosed).
- Detail: `.agents/sessions/2026-05-26-tab-removal-deposit-cancel-dialog.md`.

### Session 2026-05-26 EOD+1 — Appointment-hub all-types button + "รอ/ยังไม่ลง OPD" tab + OPD-link auto-cleanup SHIPPED (LOCAL)

Full `/session-start` → `brainstorming` (Visual Companion via AskUserQuestion previews — Rule S: no live browser at ask/plan) → spec → `writing-plans` → `executing-plans` (11 tasks T1–T11, inline per V81/V86). master = `b476f615`; prod UNCHANGED `65ab6467` (await "deploy", V18).

- **①** "เพิ่มนัดหมาย" all-types button REUSES the `AppointmentFormModal` `AppointmentHubView` already renders for edit, in create mode (no new modal); AdminDashboard kiosk-button wiring retired.
- **②** NEW 5th pill "รอ/ยังไม่ลง OPD" (state B+C+D ALL types per R4=keep-all; today+future, past hidden): `isAppointmentOpdPending` + `opd-pending` filter case + in-view `resolveLinkedSession`-join filter + `opdPendingCount` merged into TabBar counts.
- **③** cron hard-deletes link when appt date passed: `decideCleanupAction` date-passed branch (above 2h-age; overrides V116 hide; even with patientData Q3=A) + `sweepOpdSessionCleanup` joins `be_appointments` by `linkedAppointmentId` (R5) + Bangkok `todayISO`.
- **④** delete session on OPD-save: best-effort hard-delete in `_attachLinkedBookings`, gated on hoisted `isFromBookingFlow` (kiosk safe — mutual exclusion with walk-in modal).
- **Verify**: full vitest **14688** (14687 + 1 known Phase 17.1 flake; isolated 7/7) · build clean · real-prod e2e **7/0** (`scripts/e2e-opd-link-lifecycle.mjs` — SAFE dry-run, no prod mutation; verifies the be_appointments join + date-passed decision against the REAL Firestore shape) · Rule I flow-simulate · **AV131** · 5 V21 fixups.
- **NO rules/index change.** Decisions: Q1=B+C+D · Q2=hard-delete · Q3=delete-all-date-passed · Q4=reuse modal · R4=keep-all-types.
- **Rule Q-honest**: ③ decision + cron join real-tested (L2); whole sweep ran DRY-RUN only (apply:true would mutate real prod via undeployed logic = Rule M). UI ① button + ② pill = L1 by user post-deploy.
- Detail: checkpoint `.agents/sessions/2026-05-26-appointment-hub-allbutton-opd-tab-lifecycle.md` + spec/plan HTML `docs/superpowers/{specs,plans}/2026-05-26-appointment-hub-allbutton-opd-tab-lifecycle*`.

### Session 2026-05-26 — Appointment-modal deposit-section + chip "นัดมาเพื่อ" unification SHIPPED (LOCAL)

Full `/session-start` boot → `brainstorming` (Visual Companion mockup from question stage) → spec → `writing-plans` → `executing-plans` (11 tasks E1–E11, inline per V81/V86 baseline lesson). master = `def9e256`; prod UNCHANGED `65ab6467` (await "deploy", V18).

- **① Auto deposit section** — `AppointmentFormModal` (1 shared modal, 4 callers): deposit section gates on EFFECTIVE type (`showDepositSection = isDepositBooking = (safeLockedType||formData.appointmentType)==='deposit-booking'`), replacing locked-only `isLockedDepositType && mode==='create'`. Radio "จองมัดจำ" (create+edit) → required ยอด>0 → `createDepositBookingPair`. Edit: hydrate from linked deposit (`getDeposit`) + `updateDeposit` / flip-to NEW `createDepositForExistingAppointment` / flip-away → confirm ลบ(`cancelDepositBookingPair` cascade)/เก็บ + usedAmount guard.
- **② Chip "นัดมาเพื่อ"** — NEW `VisitPurposePicker` (multi-select + อื่นๆ) replaces textarea; required ≥1; stores `appointmentTo` string (backward-compat `build/parseVisitPurposeText`; legacy → "อื่นๆ: …"). `visitReasonOptions` → NEW `src/lib/visitReasonOptions.js` single source (Rule C1, 3 inline copies merged). **AV130**.
- **Verify**: full vitest **14658/0** · build clean · **real-prod e2e 21/0** (`scripts/e2e-appointment-deposit-purpose.mjs`, admin-SDK doc-level L2 — acceptable, no new index/rules; builders verified by E5). 1 V21 fixup (phase-21-0 F1.6+F1.8). **L1 = USER post-deploy**.
- **NO rules change**; **NOT deployed**. Edge to L1-check: Walk-in OPD-save now also requires a chip.
- Detail: `.agents/sessions/2026-05-26-appointment-modal-deposit-purpose.md` + `docs/superpowers/{specs,plans}/2026-05-25-appointment-modal-deposit-purpose-unification*`.

### Session 2026-05-25 EOD+2 — Treatment-blob Storage-ref migration + 2 follow-up fixes + Rule Q-honest

`/systematic-debugging` (user: "รูปภาพการรักษา save บ้างไม่บ้าง/ช้า/ติด") → Rule R prod diag root cause → brainstorm-scope (AskUserQuestion: full blob class + leave-legacy) → fix → deploy → heavy stress test → 2 follow-up fixes → Rule Q-honest.

- **Migration (AV129)**: TFP Before/After/Other photos + lab images + lab/treatment PDFs were inline base64 in `be_treatments` → 1 MiB cap (prod docs 95%/86%/80%) → intermittent save-fail + jank. All → Firebase Storage (URL+storagePath, ~30 KB doc); NO rules change (path already allows image/* + pdf). Chart cap 2→10. OPD column flex-balanced (cosmetic).
- **2 follow-up fixes (found by the human-flow stress e2e — the proof behind Rule Q-honest)**: `computeResizeDims` clamp ≥1 (extreme ratio → 0-dim canvas); edit-remove-cancel broken-ref (`removeTreatmentBlob` deletes Storage only in CREATE; EDIT skips → no 404 on cancel; photos + charts via `onBlobRemoved`).
- **NEW iron-clad Rule Q-honest**: reasoning that code is correct is NOT verification; run the real-adversarial test that COULD fail even when certain; disclose the test-vs-claim gap. Origin: I nearly shipped via "identical to proven chart path = L2-equivalent" reasoning; the e2e the user demanded then found the edit-remove bug.
- **Verify**: full suite 14603/0 · stress e2e 24/0 · human-flow e2e 18/0 on REAL prod (H1-H7; H2 = edit-remove-cancel target) · zero Storage orphans · **user L1 confirmed "ใช้ได้แล้ว"**.
- Deployed 2× (`vercel --prod`, no rules): migration `e59756e6`, then clamp+edit-remove `c6b0e1e8`/`65ab6467`. AV128 (prior session) rode along.
- Detail: `.agents/sessions/2026-05-25-treatment-blob-storage-ref.md`.

### Session 2026-05-25 EOD+1 — Customer Patient-Link SHIPPED + DEPLOYED + 2 L1 bugfixes

Full `brainstorming → spec → writing-plans → executing-plans` (8 tasks) + 2 `/systematic-debugging` bugfix cycles. Anon customers open `?patient=<token>` (no login) to see their **existing** PatientDashboard view + upcoming appointments (📍 สาขา + full Thai month) + remaining courses.

- **Architecture**: crypto token on `be_customers` (clinic-staff client write via 3 helpers in backendClient + scopedDataLayer) → NEW public `api/patient-view.js` (admin SDK, unified resolve `be_customers` OR legacy `opd_session` by `patientLinkToken`, field-minimized — NO national ID). PatientDashboard **customer-mode** = endpoint-first → map to existing `latestCourses` shape → reuse render 100% (AppointmentCard already had a 📍 branch slot) + gate auto-sync (`__customerMode`) + legacy listener as fallback-on-error only. **NO firestore.rules change** (endpoint = the secure anon path; opening anon-read would expose the PII DB).
- **2 L1 bugfixes** (user-caught on prod, Rule P class-of-bug — both fixed in endpoint + `fetchCoursesViaApi`): **AV127** exclude used-up courses (`deriveEffectiveStatus`, buffet-safe); **AV128** exclude completed/serviced appts (`didAttend` set + `serviceCompletedAt`).
- **Deploy**: prod `9d82e923` LIVE (feature + AV127). AV128 fix `0df352fa` committed+pushed, **NOT deployed** (V18 — awaiting "deploy").
- **Verification (Rule Q)**: L2 e2e **11/0 on real prod** + handler-invoke against real customer ไพบูลย์ LC-26000106 (courses 0 both used-up, appts 1 = 4 มิ.ย. pending, 25 พ.ค. done dropped) + Rule R diag scripts. Targeted vitest: flow-sim 15/0 (F6/F7 = bugfixes) · helpers 15/0 · modal 7/0 · CDV 6/0. L1 hands-on = user post-AV128-deploy.
- **Files**: NEW `api/patient-view.js` + `CustomerPatientLinkModal.jsx` + 3 backendClient helpers + scopedDataLayer re-exports + CustomerDetailView Layout A + PatientDashboard customer-mode + AV126/127/128 + 4 test banks + e2e + 2 diag scripts + spec/plan HTML.
- Detail: `.agents/sessions/2026-05-25-customer-patient-link.md`.

### Session 2026-05-25 — Skill repo updated (obra/superpowers v5.1.0) + audit infrastructure shipped

Meta-tooling session (no src). User asked whether skill customizations survive upstream repo updates → built 3-layer protection (baseline git + audit script + CLAUDE.md backstop) + executed live update.

- **Discovery**: `~/.claude/skills/` was NOT a git repo. 4 customized SKILL.md files (brainstorming + writing-plans + executing-plans + subagent-driven-development) carried Visual Companion + HTML+Mockup+Flow markers ENTIRELY in-file. Upstream marketplaces (`~/.claude/plugins/marketplaces/{superpowers, andrej-karpathy-skills, claude-plugins-official}`) confirmed 3 git repos with remotes; superpowers was 2 commits behind, karpathy 1, claude-plugins-official 226.
- **L1 baseline**: `git init` + initial commit `df9648b` (snapshot pre-update state) → enables `git diff HEAD` + `git checkout HEAD --` recovery against any future upstream re-application.
- **L2 backstop**: CLAUDE.md (user-level + project) already documented all customizations per "PLANS + SPECS = HTML WITH MOCKUP + FLOW" + Visual Companion auto-trigger sections. Per `using-superpowers` Instruction Priority, behaviour survives even if L1 files revert — but L1 should re-align for clarity.
- **L3 audit**: NEW `F:/LoverClinic-app/scripts/verify-skill-customizations.sh` (committed `5a82c856`, pushed) greps 11 anchor markers across the 4 customized skills. PASS = quiet, FAIL = paths + restoration instructions.
- **Live update executed**: pulled all 3 marketplaces fast-forward; pre-pull snapshot of 3 customized BASE files saved to `/tmp/sp-update-2026-05-25/`. v5.1.0 changed 4 skill files including 3 of our customized — `git merge-file` 3-way merge: writing-plans + executing-plans CLEAN; subagent-driven had 1 conflict region resolved manually (took upstream worktree-wording, kept our "(HTML format)" annotation). 10 non-customized skills copied verbatim from upstream. Audit re-run 11/11 PASS post-merge. Baseline re-committed at `~/.claude/skills/` as `f8e90d0`.
- **#3 patch ready**: `~/.claude/skills/CUSTOMIZATIONS-vs-upstream-v5.1.0.patch` (483 lines) captures diff vs obra/superpowers@f2cbfbe for future PR. Not opened: gh CLI not installed + customizations are opinionated personal preferences (changing skill contract from .md→.html for ALL Superpowers users would likely be rejected upstream).
- **SessionStart hook**: skipped per "do what's best" — auto-mode classifier blocked the settings.json edit; manual audit workflow already integrated into the upstream-pull cycle; 3-layer protection sufficient. Wrapper script `~/.claude/scripts/skill-audit-hook.sh` kept in case of future hook need.
- 1 commit ahead of prod (`5a82c856`). No deploy needed (tooling-only — no src/api/rules touched). Detail: `.agents/sessions/2026-05-25-skill-repo-update.md`.

### Session 2026-05-24 EOD+1 LATE+1 — V124+V125+V126 SHIPPED + DEPLOYED (one /systematic-debugging cycle)

Cycle: 1 user message → 3 V-entries → 1 combined commit + deploy. User invoked `/systematic-debugging` for V125 mid-session; V124 + V126 emerged from continuation. All client-only.

- **V124** — bubble↔badge predicate parity. NEW `isAppointmentPendingOpdSave({appt, linkedSession}) = resolveCardOpdState === 'D'`. Memo iterates `apptData.appointments` (branch-scoped) not session state arrays. AV124 + 28 tests + V21 fixup on V121 SG bank. L1: bubble showed "1" purple for BA-1779590375471 → ND-68FA49.
- **V125** — cancel cascade (3 surfaces converge). Predicate excludes cancelled status; `hideOpdLifecycle` per-row defense; `onCancelAppt` cascade-archives linked opd_session (`isArchived:true + archivedReason:'appt-cancelled' + archivedFromApptId`). AV125 closed-list 4 consumers + 13 tests. L1: bubble drops 1→0 post-cancel.
- **V126** — workflow-strict mark-complete gate. `showMarkCompleteBtn += rawStatus === 'confirmed'`. V71.B-ter philosophy preserved for TREATMENT concerns; V126 is orthogonal STATUS guard. V21 fixup absorbed: V73 test bank (B2.1+B2.4+B2.6+B3.x simulator). L1: 8 today pending rows → 0 mark buttons (`invariant_v126_holds:true`).
- **Deploy**: Commit `9af2989e` pushed + Vercel `--prod` aliased + Firebase rules deploy idempotent (no rule change — V1/V9 defense). P-D-P green pre/post (200/403/403). 12 files (4 src + 4 tests incl. 2 new + 1 AV + 1 active + 2 Rule R diag).
- **Strategic direction noted (NOT implemented)**: user wants นัดหมาย tab unification (deprecate 3 sibling tabs). V125 cascade is tactical first step via `isArchived` filter convention. Future scope.
- Detail: `.agents/sessions/2026-05-24-v124-v125-v126.md`.

### Session 2026-05-23 EOD+1 LATE+5 to LATE+9 — V118+V119+V120+V121 4-feature LOCAL stack

Cycle: 4 sequential brainstorm→spec→plan→executing-plans cycles in one session, plus a P0 fix via `/systematic-debugging` between V118 and V120. User pre-authorized "approve" pattern after each design summary; speed-mode through all 4 features.

- **V118** — Card-level OPD lifecycle row: 5-state matrix A-E (has-HN / no-link / link-sent-waiting / filled-review-save / saved-transient). `OpdLifecycleRow.jsx` presentational + `opdSessionState.js` 4 pure helpers (isOpdSessionSaved + hasPatientData + resolveCardOpdState + synthesizeSessionFromCustomer). AV118 invariant + 78 V118 tests. V120-gap noted at design but deferred.
- **V119 P0** — User reported "admindashboard จอดำไปเลย" immediately after V118 commit. `/systematic-debugging` Phase 1: line 1 React import missing `useCallback` → V118 added 3 useCallback usages → ReferenceError → React tree unmount → black screen. EXACT V80 anti-pattern repeated, same file. Fix = 1-char import addition. PLUS architectural close: AV60 scanner (V80 perpetual guard) was OPT-IN → promoted to PERMANENT vitest gate (`tests/v119-av60-hook-import-drift-permanent-gate.test.js`) that EXECUTES the scanner on every vitest run. Future drift impossible without test failure.
- **V120** — `provisionOpdLinkForBookingPair` accepts new `hideFromQueue:boolean=false`. V118 Card flow passes true → mint-fresh stamps `isHiddenFromQueue:true`; idempotent re-engage path ENFORCES hidden (overrides V116 un-hide). AppointmentFormModal + DepositPanel legacy callsites untouched (V116 behavior preserved). 11 tests.
- **V121** — Card-flow notifications restored. 2 new helpers (`isCardFlowSession`, `isCardFlowUnread`) extend AV118. cardFlowUnreadCount memo across 5 session arrays. Q1=B locked (bubble persists until 🔴 บันทึก click — modal-open gate added at line 3418). Purple #a855f7 bubble on 3 surfaces: desktop sidebar tab + mobile dock + each sub-pill (วันนี้/กำลังจะถึง/ก่อนหน้า). Push pipeline NO change — existing listener already covers V118 sessions. **Closes V120 latent gap** — V116's patientData-auto-restore would otherwise surface card-flow in Clinic queue post-fill (3 queue filters extended). 27 tests.
- **Verification**: V121 self 27/27 + V118+V119+V120 sibling 71/71 + full vitest 14480/14480 GREEN + AV60 0/527 drift + build clean 3.09s. 9 commits ahead of prod.
- **Detail**: `.agents/sessions/2026-05-23-eod-v118-to-v121.md`.

### Session 2026-05-23 EOD+1 LATE+3-LATE+4 — V116 DEPLOYED + V117 LOCAL (lightbox portal mandate)

Cycle: V116 main + V116-followup landed via `/systematic-debugging` + Rule J brainstorming + Rule P class-of-bug expansion. User authorized "deploy แล้ว" → Vercel prod LIVE @ `3612d8ae`. Then immediately re-tested on iPhone + caught V115 mobile lightbox STILL not closing — birthed V117 via second `/systematic-debugging` cycle.

- **V116 architecture**: queue-delete preserves session when linked (isHiddenFromQueue:true) + provisionOpdLinkForBookingPair existence-check + auto-regen on stale FK + un-hide-on-re-engage (V116-followup user catch — admin needs Review surface BEFORE customer fills). 5 src edits + 26 tests + AV116 (3 rules + closed sanctioned list of 2).
- **V117 root cause** (post-V116-deploy iPhone test): nested position:fixed inside StaffChatPanel `position:fixed; z-9000; overflow:hidden` got BOUNDED by panel on iOS Safari → V115's safe-area + 44pt + backdrop close fixes were correct in source but couldn't take effect because the lightbox DOM was confined. Close button landed BEHIND chat panel header.
- **V117 architecture**: `ReactDOM.createPortal(<jsx>, document.body)` for 5 fullscreen lightboxes (StaffChatImageLightbox + StaffChatPdfOverlay + TreatmentReadOnlyMirror inner + TreatmentReadOnlyPanel inner + ChartSection.ChartLightbox). Bypasses ALL ancestor CSS effects + escapes stacking contexts. Canonical React pattern (Radix/HeadlessUI/Chakra). NEW AV117 invariant + companion to AV114.
- **V21 fixup absorbed**: V83 M2.1 backdrop-onClick test failed after V117 createPortal wrapper added 1 line, pushing AV78 marker past 4-line lookback in ChartSection — fixed by reordering comments (drop redundant "Click-anywhere-closes" line).
- **Verification**: V117 self 11/11 + V83+V117 28/28 + Phase 17.1 isolated 7/7 (known full-suite flake) + build clean. Full vitest had 1 unrelated R1.7 flake + 1 M2.1 (fixed). NOT re-run after fix per Rule N targeted scope.
- **Deploy state**: V116 LIVE @ `3612d8ae`; V117 pushed @ `f43ab792`, awaiting deploy authorization (V18 lock — previous "deploy" was for V116).
- **L1 pending**: user iPhone hands-on for V117 + V116 acceptance scenarios (4 total).
- Detail: `.agents/sessions/2026-05-23-eod-v116-v117.md`.

### Session 2026-05-23 EOD+1 LATE+2 — V114 receipt-info toggle DEPLOYED + V115 mobile lightbox fix LOCAL

User started session with V114 spec already brainstormed (Q1-Q5 locked): in-preview toggle "ที่อยู่" for receipt block + compact `HN · โทร.` line when OFF. Inline `executing-plans` chosen over subagent (5 tightly-coupled tasks). Then user-reported mobile bug surfaced → `/systematic-debugging` for V115.

- **V114 saga** (5 tasks): NEW `src/hooks/useReceiptInfoToggle.js` (~50 LOC, shared by SalePrintView + QuotationPrintView via single localStorage key) → 4 surfaces per PrintView (import + body + header switch in `print:hidden` bar + HN-line phone-append + receipt-info block gate) → 34 V114 tests (11 H hook + 10 SG + 10 R RTL + 3 F Rule I cross-view) → V21 fixup absorbed (V111 A6+A8 had locked pre-V113 `c.receiptCourseName || c.name` inline shape; flipped to post-V113 `liveReceiptName(courseLine)` helper). Vercel deployed; aliased; Rule Q L1 verified on real prod via Chrome MCP (200 + default OFF + localStorage persistence + storage event + clean restore).
- **V115 saga** (4 tasks via `/systematic-debugging`): user-reported "ใน mobile กดเปิดรูป Preview ในช่องแชท staff chat แล้วปิดพรีวิวไม่ได้ และซูมดูรูปไม่ได้ด้วย ใช้งานยากมาก". Root cause: 5 stacked factors (3 close + 2 zoom). Fix: `StaffChatImageLightbox` gets backdrop close + safe-area + 44pt + multi-touch bail + double-tap zoom; class-of-bug expansion to Treatment Mirror + Panel lightboxes (safe-area + 44pt only). NEW AV114 invariant. 24 V115 tests + 2 V21 fixups absorbed.
- **Verification**: full vitest **14318/14318** (was 14215 pre-V114; +103 net = 34 V114 + 24 V115 + 2 V21 fixups absorbed + 41 V21 reconciliations from prior batches). Build clean.
- **NOT deployed (V115)**: awaiting explicit "deploy" verb.
- **Detail**: `.agents/sessions/2026-05-23-eod-v114-v115.md` + V-entries deferred to next session (V114 + V115 will land in `.claude/rules/00-session-start.md` § 2 + verbose archive at next major V-log update).



After V108 office preview shipped 2026-05-23 EOD, user re-tested + reported 3 stuck .docx chats showed ⚠ instead of 👁 (Path B 60s timeout). `/systematic-debugging` Iron Law: real-data root cause first.

- **V109 root cause** (commit `33d5eea6`): Cloud Function `functions/officeToPdf/index.js:42,72` used BARE `db.collection('be_staff_chat_messages')` instead of Rule M canonical `artifacts/${APP_ID}/public/data/be_staff_chat_messages`. Gotenberg converted successfully (2/4 stuck docs had cached `.docx.pdf` at correct paths) but `tx.get(messageRef)` on bare path returned `!snap.exists` → silent skip → status pending forever. V66 mirror amplifier: 3 L2 verify scripts wrote fixtures at the SAME bare path → claimed "11/11 verified" while real prod stuck. Reference `functions/index.js` had the canonical pattern + was not followed.
- **V109 fix**: `MESSAGES_COLLECTION_PATH` + `db.doc()` constants. 3 L2 scripts switched to canonical. NEW AV109 invariant. NEW `scripts/v109-heal-stuck-office-attachments.mjs` (Rule M two-phase): 2 of 4 stuck docs healed pending→ready with reconstructed download URLs; idempotent. Cloud Run rev 00005-q54. Verify: 11/11 L2 PASS with canonical-path fixtures + 10/0 regression tests.
- **V110 (commit `3d56b1f8`)** after user reported layout fidelity: "การจัดเรียงเกือบจะเหมือน word แต่ขาดนิดนึง". Root cause via Rule R diag (`diag-docx-font-inspect.mjs`): user's docx theme1.xml `<a:font script="Thai" typeface="Cordia New"/>` — MS proprietary Cordia New cannot redistribute. 3-layer fix: (a) `fonts-thai-tlwg + fonts-thai-tlwg-otf` install (TH Sarabun PSK + Loma + Garuda + Norasi); (b) NEW `fontconfig-thai.conf` 13 strong-binding aliases (Cordia/Browallia/Angsana + UPC → Loma/Garuda/Norasi); (c) NEW `fontDetector.js` (fflate-based docx parser) wired to `index.js` for per-conversion font logging. Cloud Run rev 00006-xxd. Verify: real-prod log captured `'Cordia New → Loma'` alias resolution. md5 differs from pre-V110.
- **V110-bis** added LO Word-compat XCU (UsePrinterMetrics + AddSpacing + UseLineSpacing + MsWordCompTrailingBlanks + CTLFont + 11 more) to `/home/gotenberg/.config/libreoffice/4/user/registrymodifications.xcu`. Cloud Run rev 00007-tfb. md5 differs again. User verdict: "เหมือนเดิม แต่ไม่เป็น คงเป็นไปไม่ได้" — accepted engine-bound limit (LibreOffice ≠ Word for Thai CTL, industry-wide). Kept V110-bis as defensive correctness. NEW AV110 invariant.
- **Cleanup (commit `97385d0d`)**: NEW `scripts/delete-staff-chat-with-office-attachments.mjs` (Rule M two-phase, broad or per-MIME scope, surprising-scope callout). `--apply` deleted 5 chats + 8 Storage objects. Audit doc emitted. Idempotent.
- **Tests**: vitest 14161/0 (14138 baseline + 10 V109 + 23 V110 + 1 cleanup overlap). Build clean. Targeted V109+V110+AV108 batch 38/0.
- **Honest scope locked permanent** (V110 V-entry): LibreOffice → Word 100% pixel-match is engine-bound, not font-bound. Industry-wide. Download button is source-of-truth for exact formatting; 👁 preview is "good enough at a glance". V110 closes the font-substitution gap.
- Files: 7 new + 5 modified + .gitignore (`.tmp-docx-inspect/` excluded — real PHI). 3 commits this session (33d5eea6, 3d56b1f8, 97385d0d). Detail: `.agents/sessions/2026-05-23-v109-v110-office-preview-and-cleanup.md`.

### Session 2026-05-23 — Office (Word/Excel/PPT/CSV) preview SHIPPED + DEPLOYED + Rule Q L2 11/11 verified

Cycle: brainstorming (Visual Companion via AskUserQuestion previews, Q1-Q4 locked) → spec HTML → 9-task plan HTML → inline T1-T9 → Vercel deploy → Cloud Function pivot → Rule Q L2 stress → 2 bug fixes → final verify. 14 commits.

- **Architecture**: client stamps `pdfPreviewStatus='pending'` + `pdfPreviewStampedAt` on Office upload (Q3=C scope: doc/docx/xls/xlsx/ppt/pptx/csv) → Storage onFinalize → Eventarc → Cloud Run `office-to-pdf` (Gotenberg+LibreOffice Docker) → converts to PDF, caches at `{path}.pdf` (V73 30d retention covers — no new cron) → Firestore transaction patches `attachments[i].pdfPreview*` → client listener flips ⏳→👁. AV108 amended: ONE sanctioned exception = in-project Gotenberg (NO 3rd-party PHI leak; localhost:3000 only).
- **Path B graceful timeout**: client card flips ⏳→⚠ after 60s with Thai tooltip "ใช้เวลานานเกินไป — บริการแปลงไฟล์อาจไม่พร้อมใช้งาน. ดาวน์โหลดเพื่อเปิดดูได้". Defense-in-depth for pre-deploy state + future Cloud Function failures.
- **Deploy pivot caught at deploy time**: Firebase Functions 2nd Gen with `runtime:nodejs20` IGNORES custom Dockerfile (uses buildpacks). Pivoted to `gcloud run deploy --source functions/officeToPdf` + manual Eventarc trigger via `scripts/deploy-office-to-pdf-cloud-run.sh` (idempotent — API enables, IAM grants, deploy, trigger). User one-time granted Owner role to firebase-adminsdk-fbsvc SA via Cloud Console (recommend revoke post-deploy).
- **2 deploy bugs caught + fixed via Rule Q L2 stress (REAL prod, no mocks)**: (a) FieldValue.serverTimestamp() nested in array element → Firestore "Update() requires…" → fix: `new Date()`; (b) non-atomic read-modify-write in `stampAttachment` → 3/10 parallel uploads stuck pending forever → fix: `db.runTransaction()`. Both surfaced ONLY by 11-fixture comprehensive script (`scripts/diag-office-preview-comprehensive.mjs`).
- **Verified 11/11 on real prod** (docx/xlsx/csv `ready` + PDF accessible at expected path with correct contentType + non-empty bytes; .odt MIME-gate skip; 3-parallel stress; 100KB→743KB-PDF mimicking user's 2.8MB upload). Avg 2.3s per conversion incl. cold start. Cleanup zero orphans.
- **Files**: NEW `src/lib/staffChatOfficePreviewCore.js` + `chartImageStorage.js` + 7 test files + `functions/officeToPdf/{index.js,helpers.js,Dockerfile,supervisord.conf,package.json,.gcloudignore}` + 2 diag scripts + 1 deploy script + spec/plan HTML. MOD `staffChatRetentionCore.js`/`staffChatClient.js`/`StaffChatAttachmentCard.jsx`/`firebase.json`/AV108 SKILL.md/3 V21-fixup test files.
- **Outstanding (user)**: (1) L1 hands-on — fresh .docx upload should flip 👁 in ~15s; (2) revoke Owner role from firebase-adminsdk SA in Cloud Console; (3) stuck `pending` doc from earlier 2.8MB test: re-upload to trigger conversion (Eventarc only fires on new uploads). Detail: `.agents/sessions/2026-05-23-office-preview-shipped.md`.

### Session 2026-05-22 — Staff Chat multi-image attachments (SHIPPED + DEPLOYED + real-prod verified)

Feature (V73 extension). Cycle: brainstorming (Visual Companion — grid/lightbox mockup in browser) → spec HTML → writing-plans HTML → executing-plans inline (Rule K work-first/test-last). User asks (verbatim): "ส่งรูปได้ ส่งหลายๆรูปพร้อมกัน + preview เด้งรูปใหญ่ เลื่อนซ้ายขวา · ≤50MB/รูป · ลบใน Storage พร้อมประวัติแชท ให้เกลี้ยง make sure ลบจริงหายจริง" + "จำลองใช้จริง อัพ/ส่ง/ลบ/preview จริง ทุกปุ่ม วนจนบั๊คหมด" + "อนุญาตให้ deploy เพื่อเทส".
- **Decisions (Q1-Q5)**: auto-retention only · hybrid thumb+original ≤50MB · delete whole message+images · 30d · ≤10 images/msg. Grid/lightbox layout approved via Visual Companion mockup.
- **Architecture**: extends V73 (no reinvent). Per-message Storage folder `staff-chat-attachments/{branchId}/{messageId}/{imgId}-{t|o}.{ext}` → retention = PREFIX-SWEEP (no orphan). `attachments[]` on the doc (legacy `attachmentUrl` scalar still renders). Shared pure `staffChatRetentionCore.js` (cron+CLI+components, Rule of 3). Cron 2-pass: A age-out >30d (+ legacy via `extractStoragePathFromUrl`), B orphan-sweep (doc-less folders > grace). Admin-SDK-only delete (client `update,delete: if false`).
- **Files**: NEW staffChatRetentionCore.js + api/cron/staff-chat-retention-sweep.js + scripts/{staff-chat-retention-sweep,e2e-staff-chat-image-retention}.mjs + tests/staff-chat-multi-image.test.js + spec/plan HTML; MOD staffChatImageResize/staffChatClient/useStaffChat/StaffChatComposer/StaffChatMessage/StaffChatImageLightbox/StaffChatWidget + vercel.json + storage.rules + firestore.rules. **AV108**.
- **Verify (Rule Q L1/L2 + Q-vis, REAL prod, every step by SCREENSHOT, NO bugs)**: (a) ลบจริงหายจริง — uploaded 6 files+doc → sweep --apply → `deletedFiles:12, getFiles(prefix)=0, doc gone`. (b) preview จริง — admin-seeded 5-image msg → real client rendered 2×2+"+1" grid → lightbox (counter 1/5..5/5, next/prev, filmstrip jump, end-clamp, close, download). (c) อัพ/ส่งจริง — injected real File objects onto the real composer input → multi-pick preview (3/10) → send → real Storage upload (authed) + real client `setDoc` attachments[] (deployed rule accepted) → 3-image grid (firstBig 1+2) rendered real-time. Fixtures cleaned (0 residue; left user's own "เทสๆ" msg). vitest 14030/0; build clean.
- **DEPLOYED** (V15 combined): `vercel --prod` (aliased) + `firebase deploy --only firestore:rules,storage` (Probe-Deploy-Probe #9 anon→403 + #10 anon→403, green pre+post). Commit `a90b6706`.
- **Remaining (user)**: hands-on send with own images (their "ทดลองเอง" — already real-prod verified).

### Session 2026-05-21 EOD+3 LATE — pinch-zoom RE-SHIPPED with the REAL black-screen fix + DEPLOYED + verified LIVE on prod

Detail: `.agents/sessions/2026-05-21-zoom-reship-real-fix.md`. User: "ทำ pinch zoom แก้บั๊ค ใส่โค๊ด แล้วเทสเหมือนจริง … ดูภาพด้วย" + "อย่าโกงเทส … ใส่ในกฎ".
- **Re-implemented** the reverted (`e36a73e9`) pinch-zoom (1-4x) + palm-rejection: `chartGestureMath.js` (pure math, restored) + `TabletChartCanvas.jsx` gesture layer **CAPTURE-phase on the OWNED wrapper** (`surf=wrapRef.current` + `stopPropagation` isolation — NEVER `fc.upperCanvasEl`) + export resets VPT to fit + `resetZoom`; `TabletChartEditorPage.jsx` fit button.
- **THE REAL black-screen cause (the prior lead was wrong)**: reproduced the black screen ON DESKTOP via a synthetic 2-touch pinch (Chrome MCP drives my gesture layer, which doesn't gate `isTrusted`) + console → `NotFoundError: insertBefore … not a child of this node`. The ⤢ fit button rendered BEFORE `<TabletChartCanvas>`; Fabric wraps the React-owned `<canvas>` in `.canvas-container`, so on zoom (`zoomed`→true) React `surf.insertBefore(button, canvas)` → canvas not a direct child → unmount → blank. **Fix = render the fit button AFTER the canvas (append).** The `upperCanvasEl`-listener lead stays as a defensive fix.
- **Rule Q-vis** added (`01-iron-clad.md`): no test-cheating; UI evidence = a SCREENSHOT you LOOK AT (not pixel-probe/object-model/code); probe-vs-screenshot → SCREENSHOT wins (a `select` upper-canvas probe gave a false-negative); use the most appropriate tool; verify every element. AV107 PART A+B. Tests F1-F5.
- **Verified via Chrome MCP REAL browser** (localhost + the deployed prod URL), every item by SCREENSHOT, NO crash: pen/highlighter/line/arrow/rect/circle/text/eraser/select + undo/redo/clear/delete/save-relay (`resultImageUrl` on real prod) + pinch-zoom-4x + fit-reset.
- **DEPLOYED**: `vercel --prod` (aliased `lover-clinic-app.vercel.app`); frontend-only, NO rules deploy. vitest **14007/0**; build clean; test sessions cleaned (0 orphan). NEW Rule R helper `scripts/diag-chart-session-keepalive.mjs`. Commit `e71ef782`.
- **Remaining (optional)**: on-device iPad L1 confirm — fix is browser-agnostic (desktop + prod verified → covers iPad); only Fabric's trusted-touch pipeline is desktop-unverifiable.

### Session 2026-05-21 EOD+3 — chart-relay Rule Q fix (handleSave "null", DEPLOYED) + zoom+palm built→REVERTED (iPad black screen)

Detail: `.agents/sessions/2026-05-21-chart-relay-fix-zoom-revert.md`. Two threads:
- **Rule Q adversarial pass** on the chart relay (REAL client SDK, not admin): storage+firestore rules / composite index / cleanup all verified clean; **found+fixed `ChartSection.handleSave` persisting the 4-char string `"null"`** (JSON.stringify of a JS null) → RT8 regression + Rule M cleanup of 2 prod charts. DEPLOYED (`7a4b7f47`).
- **zoom+palm feature** (brainstorm→spec→plan→impl; desktop real-browser-verified) DEPLOYED (`e36a73e9`) → user: **iPad 2-finger zoom = BLACK SCREEN** → `/systematic-debugging`. Desktop (Blink, DPR 1.89, real rAF) renders it perfectly + no crash → **iPad/WebKit + trusted-multitouch specific**. **Root-cause LEAD**: the zoom added raw `addEventListener('pointer*')` on `fc.upperCanvasEl` → conflicts with Fabric's native trusted-touch pipeline (the original code explicitly warns "no raw upperCanvasEl listeners"). **REVERTED** (Rule A, `00a9da2f`) + redeployed safe; handleSave fix kept.
- **NEXT (user directive)**: make the project reliably USE **Chrome MCP** (Rule S — connected: "Browser 1", deviceId `8bdc85cc-b6e5-47d9-b3cd-56957264819d`) + **full Chrome-MCP test of the tablet canvas editor — every tool + function**, then stop. Zoom re-ship (shelved): on-device iPad diag → **overlay-based fix** (capture pinch on a separate layer / Fabric's events, NOT raw listeners on Fabric's element); spec/plan recoverable from `e36a73e9`.
- **Lesson (V66 again)**: "verified in a real browser" was DESKTOP-only — iPad/WebKit/trusted-touch was the gap. Use Chrome MCP FIRST for device/touch verification, not Claude Preview.

### Session 2026-05-21 EOD+2 (cont.) — 2 tool-bug fixes (arrow/text) + COMBINED DEPLOY + comprehensive verification

After the re-edit feature (block below), user reported 2 more-tools editor bugs → `/systematic-debugging`; then "test all tools comprehensively, only deploy when all really passes". Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-7.

- **Bug 1 (arrow)**: showed during drag, VANISHED on release. `commitShape` measured the tiny-discard dist off `o.x1/o.x2` (fabric.Line props) but the arrow is a `fabric.Group` (no x1..y2) → dist 0 → always "tiny" → removed on mouse:up. **Fix**: `updateShape` tracks drag end `s.ex/s.ey`; `commitShape` uses the drag-delta (geometry-agnostic, Line AND Group).
- **Bug 2 (text)**: no resize/move handles + couldn't set width. `addText` auto-entered editing → fabric `hasControls=false`. **Fix**: mirror PC ChartCanvas — leave text SELECTED with handles, no auto-edit (double-tap to edit; ml/mr handles set the box width).
- Root-caused + verified via **`fc.fire`** real-browser probe (drives the real handlers past the synthetic-event isTrusted limit). **AV106** + `tests/tablet-chart-tool-bugs.test.jsx` TB1/TB2.
- **Comprehensive verification** (user demand): vitest **13970/0** (an earlier "1 failed" = flake) + 18 chart files/147 + **L1 ALL 9 tools** + **11 edge cases** (text-width / scrub-erase / undo-redo / production+raster re-edit / double-hydrate / re-edit-textbox — all `getImageData` REAL PIXELS) + **L2 e2e prod ALL PASS**. **No product bugs found** (2 apparent = probe measured too early, confirmed via polling).
- **DEPLOYED** (V15 combined): `vercel --prod` (aliased) + `firebase deploy --only storage` (P-D-P #13 ✓). **Object-level re-edit (PC + tablet) LIVE.** master `1bfe1767`. on-device L1 by user pending.
- 1 UX nuance (NOT a bug): re-edit shows ~0.8s blank while the template image enlivens, then renders fully (object-level + raster both). Optional loading spinner — user decides.

### Session 2026-05-21 EOD+2 — Re-edit a saved chart ON TABLET (feature) — LOCAL, verified, awaiting deploy

NEW feature (design APPROVED prior session "โอเค ลุยเลย"). Flow: `brainstorming` (HARD-GATE already satisfied) → spec HTML → `writing-plans` HTML → `executing-plans` inline (6 files, tightly coupled — subagents thrash on this baseline). Spec/plan: `docs/superpowers/{specs,plans}/2026-05-21-re-edit-saved-chart-on-tablet*`. Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-6.

- **What**: clicking edit ✏️ on a saved chart now opens the SAME `PcPairingModal` as add-new (🖥️ แก้ที่เครื่องนี้ / 📱 ส่งไปแก้ที่ iPad) instead of going straight to the PC canvas. Send-to-tablet ships the EXISTING chart so the iPad loads its prior annotations + the result merges back into the SAME chart slot.
- **Architecture (thread 1 field through the existing relay)**: `chartEditSessionCore.buildSessionCreate` + `editFabricJsonUrl:null`; `useChartEditSession.start({editFabricJson})` uploads `edit.json` via the existing `uploadTransportJson('edit')` (GUARDED — never blocks the relay) + patches `{templateImageUrl, editFabricJsonUrl}` in one update; `TabletChartEditorPage` `resolveSource(doc)` resolves json-FIRST (reeditable → `setInitialFabricJson` + skip the raster PNG → no double-load); `TabletChartCanvas` NEW `initialFabricJson` prop → `hydrateFromJson` (`setDimensions(canvasW,H)` + `loadFromJSON` + relock obj[0] + force white bg — mirror PC `ChartCanvas:69-79`) with the `[templateImageUrl]` effect early-returning when object-level owns the canvas; `ChartSection.handleEdit` stages `pendingChart` + the modal, `sendToTablet` sends `pendingChart.dataUrl`+`.fabricJson`, `onSaved` merges via the existing `handleSave` (editingIdx≥0 → replace slot). Reuses `serializeFabricCanvas`/`isObjectLevelReeditable`/`uploadTransportJson` (DRY) — **no new collection, no new storage rule** (the fix2 `{file=**}` json allowance already covers edit.json).
- **Graceful**: legacy chart (no fabricJson) OR pre-storage-deploy (client json upload denied) → `editFabricJsonUrl` null → tablet uses the annotated PNG (raster, add-on-top). Object-level unlocks after `firebase deploy --only storage` (#13).
- **Rule Q/S verification**: full vitest **13965/0**, build clean (3.43s). **L2 e2e ALL PASS on real prod** (`e2e-chart-relay-roundtrip.mjs` Phase E: editFabricJsonUrl field + PRODUCTION `isObjectLevelReeditable` passes on the round-tripped edit.json + canvas dims survive + prior annotations present + same-slot 8→9 merge; cleanup zero orphans). **L1 real-browser** (Rule S, temp probe deleted): mounted the REAL `TabletChartCanvas` with a real-fabric `initialFabricJson` → `exportObjects:2` (loadFromJSON ran) + `exportW/H 600×800` (sized to the json's NATIVE dims, NOT the ~618 container-fit raster) → object-level path proven on a real mounted component. `tests/re-edit-chart-on-tablet.test.jsx` RT1-RT7 (unit + source-grep + RTL + Rule I flow).
- **V21 fixups (Rule P, full-suite sweep caught 3)**: `RC2` (more-tools — template effect shape changed to the gated form) + `R4.2`/`R4.4` (transport — ChartSection `templateDataUrl` + openSession late-arrival moved into `resolveSource`). All updated to lock the NEW contract.
- **Files**: MOD `chartEditSessionCore.js` / `useChartEditSession.js` / `TabletChartCanvas.jsx` / `TabletChartEditorPage.jsx` / `ChartSection.jsx` (+ edit/delete `data-testid`s) / `scripts/e2e-chart-relay-roundtrip.mjs` (Phase E). NEW `tests/re-edit-chart-on-tablet.test.jsx` + spec + plan HTML. **NOT deployed** — await explicit "deploy" (V18; combined vercel + `firebase deploy --only storage` for live object-level). **Next**: deploy → on-device L1 (tablet edit a saved chart → prior strokes load as MOVABLE objects → save → PC same slot updated).

