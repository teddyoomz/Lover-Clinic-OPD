# Session Handoff — LoverClinic OPD Cross-Session State

> **This file is read FIRST every new session.** Updated by `/session-end` skill.
> Link out to `.agents/sessions/*` for detail.

---

## 📏 HARD CAP: 200 KB (2026-05-17 EOD+2)

This file MUST stay **under 200 KB** at all times. When `/session-end` (or any
maintenance turn) detects size > 180 KB, the maintainer MUST:

1. Identify the oldest 5–10 session blocks (`### Session ...` headers)
2. Append them in **chronological order, oldest first** to
   `.agents/sessions/session-handoff-archive.md` (prepend NEW archived
   blocks at the TOP of the archive)
3. Delete them from this file
4. Verify resulting size < 150 KB (leaves headroom)

**Never read this file with `Read` without a `limit` parameter** if it could
exceed 256 KB — older sessions are in the archive. Current resume context
lives in the most recent ~12 session blocks below.

**Hard-cap origin**: 2026-05-17 EOD+2 turn — file grew to 317.5 KB across 150+
session blocks since 2026-04-26; `Read` started failing the 256 KB tool limit
during session boot. User directive: "ทำ SESSION_HANDOFF.md ให้ไม่มีวันเกิน
200 KB". Codified as permanent maintenance rule in `/session-end` skill.

---

## 🚨🚨🚨 RULE Q — REAL-ADVERSARIAL VERIFICATION (V66, 2026-05-14) — READ EVERY TURN 🚨🚨🚨

**TRUST COLLAPSED. PHASE 29 SHIPPED WITH 5+ USER-VISIBLE BUGS WHILE 8 LAYERS OF TESTS CLAIMED PASS.**

Mock tests are NOT verification. Admin-SDK doc-level access is NOT verification.
They are **CODE-SHAPE COVERAGE ONLY**.

**Before claiming ANY of these — "verified" / "shipped" / "tests passed" / "done" / "complete" / "ready to deploy" / "PR ready" / "approved" / "working" — for ANY user-visible code (UI, API endpoint with auth, Firestore query, etc.) — you MUST satisfy ≥1 level:**

- **L1 (PREFERRED)** — Playwright/real-browser drives the REAL deployed UI with real auth + real DOM + real Firestore side-effects
- **L2 (ACCEPTABLE)** — Real client SDK (NOT admin) issuing the EXACT compound queries / listener subscriptions the UI issues
- **L3 (LAST RESORT)** — User walkthrough with written confirmation ("ลองแล้ว work" / "ลองแล้ว พัง XYZ")

**FORBIDDEN** (Rule Q violations):
- `vi.mock('firebase/firestore')` + claim "verified"
- RTL with mocked listener data only
- Admin SDK `doc.get/set/batch.commit` + claim "compound query verified"
- `firebase firestore:indexes` returns N → claim "indexes ready" (deployed ≠ built; indexes take 2-30 min)
- Post-deploy probe = anon HTTP POST to one collection (not a compound query)
- "All vitest tests pass + build clean → shipped" (INSUFFICIENT for user-visible flows)
- "I tested for 5 min and found no bugs" (<5 min + 0 bugs → retest at higher level)
- Confirmation-bias test design ("write test that assumes correctness → green")

**Self-check** (run BEFORE any "verified" claim — any "no" or "I'm not sure" → DO NOT CLAIM):
1. Did I drive REAL browser OR real client SDK?
2. Did I issue the EXACT query the UI issues?
3. Did I actively TRY to BREAK my own code?
4. If <5 min testing + 0 bugs → did I retest at higher level?
5. Can I produce output log + screenshot proving the flow?

**Full text**: `.claude/rules/01-iron-clad.md` Rule Q (top-of-file) + `~/.claude/skills/real-adversarial-verification/SKILL.md` + V66 in `.claude/rules/00-session-start.md` § 2 + verbose entry in `.claude/rules/v-log-archive.md`.

**Origin**: V66 (2026-05-14) — Phase 29 trust collapse. User curse-verified: *"กูไม่เชื่อเทสที่ไม่น่าเชื่อถือของมึงแล้ว ... ทำยังไงก็ได้ให้ต่อไปนี้การเทสของมึงจะต้องไม่เหี้ย ไม่โกหก ไม่เข้าข้างตัวเองและใช้ไม่ได้จริง"*. EVERY FUTURE "VERIFIED" CLAIM MUST PASS L1 OR L2. NO EXCEPTIONS.

---

## Current State

- **NEW (2026-06-16) — ED follow-up v2 (confirm card / latest-link-only / round dates) + intake-date preserve (B) + 🔴 province fix — SHIPPED local, backfill APPLIED, NOT deployed**: master HEAD `9f2870f9` (=origin; 10 commits); prod still **`019df953` (PRE-v2)** — frontend-only, no firestore.rules → vercel-only when authorized, no Probe-Deploy-Probe. `/brainstorming`→spec→`/writing-plans`→`/executing-plans` (8 tasks) + B. **R1** read-only confirm card on the follow-up link (ชื่อ-สกุล+อายุ+เบอร์ปิดกลาง, passive, neutral; `confirmInfo` snapshotted into the opd_session — anon `get`s it, can't read be_customers, NO rule change; editable fallback for legacy links). **R2** session pill removed all modes. **R3** `supersedePendingFollowups` — new link deletes prior PENDING session+round per customer+branch (latest-link-only). **R4** TFP note + CDV ED box both show each round date (dd/mm/yyyy พ.ศ.) + "วันนี้" badge. **🔴 province bug** (live on prod): the unconditional `if(!province)` BLOCKED follow-up submit ("กรุณาเลือกจังหวัด") — only L1 real-browser submit caught it (L2/admin-SDK bypassed client validation) → gated to `isIntake`. **B (เอาแม่นๆ)**: `patientData.assessmentDate` dropped by the kiosk→customer projection (0/154) → preserved (kioskPatientToCanonical snake `assessment_date`→camel; addCustomer stamps `thaiTodayISO()` once at CREATE; buildFormFromCustomer round-trips) — AV194/V141 class; **Rule-M backfill APPLIED** (audit `backfill-assessmentdate-1781546629477-…`): 154 customers (29 from EXACT intake-session date, 125 from createdAt), idempotent, LC-26000082→2026-05-20. Proven the date IS findable (LC-82 intake session `BL-1779253531712` assessmentDate "2026-05-20"). **Verified**: full vitest **16542/0** + build clean + **Rule Q L1** (anon link: confirm card renders [name/age/masked-phone, no inputs, no pill] + province-submit success "ส่งข้อมูลสำเร็จ") + **Rule Q L2 13/0** real prod (supersede selectivity + confirmInfo + materialize-dated-today) + Rule I flow-sim 6/0 + 11 V21/contract fixups caught (camelCase-leak, snapshots, import-locks, CRLF-fragile v136/H-quater). **Honest gap (Rule Q)**: backend-authed pixel render (CDV box + TFP "วันนี้") = USER hands-on after deploy (no staff creds for an automated backend L1). Checkpoint `.agents/sessions/2026-06-16-ed-followup-v2.md`.
- **NEW (2026-06-15 EOD+2) — ED Score box + follow-up assessment SHIPPED + DEPLOYED (zero-migration; brainstorm→plan→14-task impl)**: master HEAD `3cf4b01b` (=origin; probe #17 diag, non-bundled); **prod = frontend `019df953` (vercel, lover-clinic-app.vercel.app HTTP 200) + firestore.rules +be_assessments DEPLOYED (Probe-Deploy-Probe) + functions `sendPushOnSubmit` materialize CF DEPLOYED**. New **"สมรรถภาพ · ED Score" box** in CustomerDetailView (right col, under the 4-tab course box): latest round hero + 4 chips (ADAM/IIEF/MRS/PE) + expandable **deletable** history (followups only; intake `__intake__` protected) + "ครั้งที่ N" send button → **EDFollowupModal** (round-aware type picker + per-round link + **full-screen mobile QR**). Customer fills via existing PatientForm follow-up render (neutral `formType:'followup_assessment'`, PatientForm gates purely by `types[]`; 128-bit crypto id; `expiresAt`=+1 day OR on-complete). A **materialize CF** (`assessmentMaterialize` inside `sendPushOnSubmit`, non-fatal, canonical BASE_PATH) writes the completed answers into a durable **`be_assessments`** collection. **Round 1 = virtual record derived LIVE from intake patientData → ZERO migration** (LC-26000082, just backfilled, shows immediately). Round# is **date-rank derived (never stored) → delete renumbers**. Also: stripped ED from CDV note; TFP หมายเหตุทั่วไป shows clean ED latest-2 + dates (`generateClinicalSummary(...,includeScreening=false)` for the note builder ONLY — print + intake-view KEEP it per user Q11); DX/Tx/Plan 2× (rows 6/6/4) + health-info rows 4 → both save buttons row-align. **Verified**: full vitest **16482/0** + build clean + **Rule Q L2 12/0 on REAL prod** (`scripts/e2e-ed-assessment.mjs` — materialize durable, delete renumbers) + flow-simulate 6/0; **6-agent adversarial hunt + full-suite caught & fixed 5 real issues** (stripScreening `.startsWith`→exact `=== h`; neutral formType not `'followup_ed'`; basePath hygiene; +2 a11y P1 from `/audit`: modal role=dialog/aria-modal/aria-labelledby + icon-button aria-labels) + **3 V21 test fixups** (eod7 expiry regex, phase-26-2 note gate, v83/AV78 named-handlers). New files: `assessmentRoundsCore.js` · `edScoreDisplay.js` · `EDScoreBox.jsx` · `EDFollowupModal.jsx` · `functions/assessmentMaterialize.js`. **Honest gap (Rule Q)**: rendered-pixel L1 (the box on a real authed customer, send modal+QR, materialize round-trip, delete-renumber, theme) = USER hands-on — no browser connected this session. Spec/plan `docs/superpowers/{specs,plans}/2026-06-15-ed-score-box-followup-assessment*`. Checkpoint `.agents/sessions/2026-06-15-ed-score-feature.md`.
- **NEW (2026-06-15 EOD+1) — housekeeping only (no source, no deploy)**: master `1b5c9c13`; prod frontend `f302216c` LIVE + rules `e5418722` (both unchanged); vitest **16398/0** (reused — no source touched). (1) **ponytail statusLine**: auto-classifier HARD-blocked me writing the `-ExecutionPolicy Bypass` command into `~/.claude/settings.json` → USER added it manually (JSON valid, hooks intact; `[PONYTAIL]` badge next boot). (2) **SESSION_HANDOFF.md archived** (`1b5c9c13`): was 217.8 KiB (over 200 KB cap) — bulk had drifted to the `## Current State` one-liner index (177 KB), NOT the `### Session` blocks the cap procedure assumed; moved oldest 43 index entries (`2026-05-27 EOD+13` → `2026-05-21`) → NEW `.agents/sessions/session-handoff-archive.md`; live = **139 KiB** (<150 KB target, 61 KB headroom); all 19 detail blocks + recent index kept. (3) ภูดิท LC-26000151 / LC-26000082 = read AV194 backfill + backup-search scripts only (no env pull, no mutation), user said "ไม่ต้องทำแล้ว" → deferred.
- **NEW (2026-06-15) — appt-hub past-tab DESC DEPLOYED + ponytail GLOBAL install + 🧭 Master Flow/📊 graphify lifecycle (LoverClinic + guardrails GitHub)**: master `c48c4897` (docs/rules); prod frontend `f302216c` LIVE (appt-hub "ย้อนหลัง 30 วัน" past tab → `sortApptsByDateTimeDesc`, yesterday-at-top, +12 tests F10/F11; vercel frontend-only, no rules → no Probe-Deploy-Probe). rules `e5418722` unchanged. Full vitest **16398/0** (from the past-tab fix; all later changes docs/rules, no source). **ponytail** (DietrichGebert v4.4.0) installed every-project: 5 skills `~/.claude/skills/ponytail*` + always-on rule `~/.claude/CLAUDE.md` + hooks USER-wired into `~/.claude/settings.json` (auto-classifier HARD-blocked agent settings-hook edit by design; user did `/hooks`/Notepad). **Master Flow** (tiers T0–T3 size→gate-depth) + **graphify lifecycle** (boot reads `graphify-out/GRAPH_REPORT.md` / session-end `graphify update .`) → global core `~/.claude/CLAUDE.md` + project overlay (00-session-start + session-start/end skills) + ported to **guardrails GitHub** `2ea158a`. Caught+reverted an unexpected project `.claude/settings.json` strip. Detail → checkpoint `.agents/sessions/2026-06-15-masterflow-graphify-ponytail.md`.
- **NEW (2026-06-14) — AV193 branch-count + AV194 perf-assessment projection + Rule M backfill + AV195 chat_config cleanup ALL DEPLOYED; suite first-ever 16386/0**: master HEAD `201bd106`; **prod = frontend `201bd106` (vercel, lover-clinic-app.vercel.app HTTP 200) + firestore.rules `e5418722` (UNCHANGED — all today frontend-only → vercel-only, no Probe-Deploy-Probe)**. Four fixes (each `/systematic-debugging`, ultracode): **AV193** — StaffTab/DoctorsTab rendered raw `branchIds.length` → OoMz/Mild showed "4 สาขา" with 3 branches (orphan TEST-V81 branchId, Rule-H soft-keep) → NEW `countLiveBranchMemberships` live-resolves vs be_branches + Rule M cleanup of 2 staff docs (V47/AV25 display-orphan-FK class). **AV194** — kiosk perf/hormone assessment (symp_pe/ADAM/IIEF-5/MRS, 27 fields) blank in saved-customer intake view: `kioskPatientToCanonical`+`buildPatientDataFromForm` dropped them (proven prod: opd_sessions 116/136 carry, be_customers 0/150) — SAME class as V141/AV162 (visit_reasons), MRS doubly-latent → fix = 3-mapper triangle + NEW shared `src/lib/kioskAssessmentFields.js` (`pickKioskAssessmentFields`). **Rule M backfill** — 28 customers' perf recovered from surviving opd_sessions (STRONG match national-id OR name+phone; meaningful answers only; ambiguous LC-26000082 skipped; idempotent; ภูดิท LC-26000151 UNRECOVERABLE — session deleted + not in any of 7 backups). **AV195** — post-hoc blast-radius audit of WS1 (user: "did the security work break anything else?"): 4/5 tightenings clean; the 1 collateral = C2-bis denies client read of secret `clinic_settings/chat_config`, but 2 legacy CLIENT reads survived + failed graceful-but-noisy (fbConfig auto-seed + ChatPanel legacy fallback) → REMOVED (per-branch be_*_configs are primary; chat_config held OLD rotating secrets) + dead FbSettingsTab banner; killed console-error noise. **Origin proven**: the score bug did NOT come from the security work (git -S: perf never in projection; predates ~5 weeks). **Verification pass**: 3 long-standing suite reds proven test-infra flakes (NOT real bugs, NOT today — git show: today's 4 commits touch none of their subjects; rule correct, getAllMasterDataItems is a comment, fx-glow used 10×) → made deterministic (Node-fs/comment-aware) → **full vitest 16386/0** (first fully-green). Both prod data ops idempotent + stable (0 drift). **⚠ USER: ROTATE LINE+FB secrets** (chat_config held OLD secrets; AV195 reinforces). Checkpoint `.agents/sessions/2026-06-14-av193-194-195-perf-and-chatconfig.md`.
- **NEW (2026-06-10) — CYBERSECURITY HARDENING WS1–WS4 ALL DEPLOYED + pre-deploy audit caught a live CRITICAL**: master HEAD `c537ca47`; prod = frontend `e5965311` (vercel) + firestore.rules `e5418722` (firebase) LIVE @ lover-clinic-app.vercel.app. Whole-app security audit → 4 workstreams, each `/brainstorming`→spec→impl→Rule Q L1/L2→deploy. **WS1** anon-surface lockdown (opd_sessions get/list split killed a LIVE anon mass-PII dump; clinic_schedules/chat/form_templates; client crypto-128-bit ids + PatientDashboard→/api/patient-view + webhook chat→admin SDK). **WS1-C2-bis (found by the pre-deploy `/systematic-debugging` audit)** — `clinic_settings/chat_config` LINE/FB channel SECRETS were STILL world-readable (C2 migrated readers to admin SDK but never closed the rule; confirmed live HTTP 200). **First fix (specific staff-only match) was INEFFECTIVE — Firestore UNIONS all matching rules (`true OR isClinicStaff`=true; system_config had the same latent leak)**; real fix = wildcard `read: if settingId != 'chat_config'`. Post-probe 200→**403**; main/system_config stay public (App.jsx v86Glow anon read). **WS2** deps: html2pdf removed (CRIT XSS, dead) + jspdf 3→4.2.1 (CRIT, not-reachable; L1 PDF + node 5/5) + vite 8.0.16 + postcss → npm-audit criticals=0. **WS3** endpoint auth: send.js+saved-replies were 500-BROKEN since V50 (ghost import) + old gate checked NO claim (latent weak-auth) → NEW `verifyClinicStaffToken` (verifyIdToken + isClinicStaff/admin); no-auth → **401** (was 500); schedule token 40→128-bit. **WS4** security headers (vercel.json had NONE): CSP (inline-script SHA-256 hashes, no unsafe-inline/eval; connect *.googleapis.com) + HSTS + nosniff + X-Frame + Referrer + Permissions + X-DNS — all 7 live, L1-verified (0 CSP violations). **Verified**: Rule Q L2 `scripts/diag-ws1-anon-lockdown.mjs` **13/13** real prod + Probe-Deploy-Probe (Rule B). full vitest 16349 pass / 3 timing-flakes (pass isolated 19/19, unrelated). V23 re-break trap CLEARED (all list/query of tightened collections run in staff context). **⚠ USER ACTION (CRITICAL): ROTATE LINE channelSecret+channelAccessToken + FB appSecret+pageAccessToken** (publicly exposed for an unknown window; rule stops future reads, rotation invalidates scraped values). Checkpoint `.agents/sessions/2026-06-10-cybersecurity-hardening-ws1-4.md`.
- **NEW (2026-06-09 EOD+3 LATE) — V163: "แก้คงเหลือ" (ลด/เพิ่มคงเหลือคอร์ส) PROD CRASH `parseQtyString is not defined` FIXED + DEPLOYED LIVE**: master = `cd11542d` (docs); **prod frontend = `7c5f4e0a` LIVE** @ lover-clinic-app.vercel.app (`vercel --prod` aliased, HTTP 200; **frontend-only — firestore.rules UNCHANGED → no Probe-Deploy-Probe**). `/systematic-debugging` + ultracode. **Root**: `adjustCourseRemainingQty` (the `b8351546` unified add/reduce refactor) used `parseQtyString` but the module-top static `import {…} from './courseUtils.js'` OMITTED it (had the other 5 — that's why `formatQtyString`/`reverseQty` worked in the same fn). Undefined identifier → global lookup → `npm run build` CLEAN → only threw at runtime on the ยืนยันลด/เพิ่ม save (V6/V11/V104 build-invisible class). **Fix (1 line, class-eliminating)**: add `parseQtyString` to that static import → module-scoped for every fn. **Why 16326-green missed it**: the default-suite C1.6–C1.13 are SOURCE-GREP only (never EXECUTE the fn) → can't catch a runtime ReferenceError (V66 lesson); the lone execution test was opt-in `tests/extended/`. NEW `tests/av192-courseutil-scope-execution-2026-06-09.test.js` EXECUTES the real fn (only the Firestore tx mocked; import resolution 100% real ESM) → RED 7 w/ the exact error on the reverted import, GREEN 8 on fix; AV192.8 = classifier over every backendClient courseUtils usage. **Rule P**: 5-agent adversarial workflow swept all courseUtils usages (13 files) + the sibling stockUtils per-fn-destructure class → SOLE instance, 0 siblings. **AV192**. **Verified**: full vitest **16332/2** (the 2 = PRE-EXISTING unrelated flakes — v55-1 derivePatientTreatmentHistory fast-check random-seed + 1 env-flake; v55-1 passes 296/0 isolated) + build clean + **Rule Q L1 real browser** (local Vite dev → real prod Firestore, TEST customer, exact flow `แก้คงเหลือ→ลด→ยืนยันลด -1/หมอมายด์` → no alert, 6/12→5/12 in UI, Firestore `courses[0].qty="5 / 12 ครั้ง"`; fixture cleaned). **Honest gap (Rule Q)**: L1 on LOCAL dev = IDENTICAL committed+deployed source vs real prod Firestore; the live deployed-URL re-drive was blocked (Chrome MCP not connected, Claude Preview localhost-only) → live smoke HTTP 200. Checkpoint `.agents/sessions/2026-06-09-v163-parseqtystring-crash.md`.
- **NEW (2026-06-09 EOD+3) — DEPOSIT MONEY IN REPORTS SHIPPED + DEPLOYED LIVE**: prod = master HEAD `999fae66` LIVE @ lover-clinic-app.vercel.app (`vercel --prod` aliased; frontend-only, NO firestore.rules → no Probe-Deploy-Probe). **This deploy ALSO caught up V162 + the EOD 4-fix — the two "NOT deployed" bullets below are now SUPERSEDED (all LIVE).** `/brainstorming`×2 + `/systematic-debugging`. reports-payment: per-channel `ยอดขาย|มัดจำ|ยอดรวม|ใบเสร็จ(กดได้)|%` + footnote; กดใบเสร็จ → PaymentDocsModal (ใบขาย→SaleDetailModal / ใบมัดจำ→deep-link). reports-sale: "มัดจำคงเหลือในระบบ" chip + deposit rows **column-aligned into the 18 real columns** (teal `bg-teal-900/10` + badge "มัดจำรับเข้า", amount→ยอดที่ชำระ, full details, —=N/A), interleaved by date, **NOT summed into the footer**. deposit deep-link `?...&deposit=DEP-x` → DepositPanel DetailModal. **No double-count PROVEN on real prod** (SaleTab deducts deposit before channels → sale channels never carry มัดจำ; ฿19,000 deposit now visible, was ฿0). **TDZ crash caught by Rule Q L1** (`mergedRows` before `out` → const TDZ; build missed it) → fixed + SG3 guard. New: `loadDepositsByDateRange` · `depositReportUtils` · `paymentSummaryAggregator` rewrite · `DepositReceiptRow` · `PaymentDocsModal` · `renderDepositCell`. **AV191**. Verified: full vitest **16326/0** (exc 2 pre-existing env-flakes bsa-task7/v85-glow — pass isolated) + build clean + **Rule Q L2 real-prod diag** + **L1 real browser** (login, 18-cell teal deposit row, drill-down, chip, deep-link). Checkpoint `.agents/sessions/2026-06-09-deposit-in-reports.md`.
- **NEW (2026-06-09 EOD+1) — V162: TFP buy critical-path qty-multiply divergence + per-purchase rowId/remove collision (course+promo) — COMMITTED + PUSHED, NOT deployed**: master = `2d13c980` (ahead of prod `e56d2ac7`; **frontend-only, no firestore.rules → vercel-only when authorized, no Probe-Deploy-Probe**). `/systematic-debugging` + ultracode (user: *"จุดซื้อขายของ แม่งไม่น่าให้อภัย ... แก้ไม่ให้มีบั๊คแบบนี้หลงเหลือ"*). **Bug#1**: buy a course qty N → "ข้อมูลการใช้คอร์ส" showed 1× while the bill charged N× (`buildPurchasedCourseEntry` un-multiplied; sale + `resolvePurchasedCourseForAssign` multiplied → DISPLAY≠SALE≠PERSIST). **Bug#2**: same course bought twice → checkbox collision + delete-one-removed-both (per-purchase identity keyed off the MASTER `item.id`; `now` was only on courseId not rowId). **Fix (Rule P, course AND promo)**: per-purchase `purchaseUid` (confirmBuyModal counter ref) threaded through courseId + every rowId + grouping + `removePurchasedItem` targeting + promo consumables; PLUS `buildPurchasedCourseEntry` multiplies by `buyQty` → DISPLAY===SALE===PERSIST. SaleTab already correct. **Verified**: default vitest **16300/0** (+23 new bank incl. display===persist A6) + build clean + extended `treatmentBuyHelpers` 54/54 (+4 V21 BPCE fixups) + **Rule Q L1 real-browser of the actual Vite-served module** (display ×3 + `displayEqualsPersist:true` + distinct rowIds + checkbox-independent + targeted-remove). AV190 + V162. **Honest (Rule Q)**: assembled-UI click (login+customer) = user L1 post-deploy. **Pre-existing flagged**: `npm run test:extended` 283/4699 fail = V50-deleted AppointmentTab/MasterDataTab/CloneTab still imported by 46 stale RTL tests (opt-in; task spawned). Checkpoint `.agents/sessions/2026-06-09-tfp-buy-qty-and-rowid-collision.md`.
- **NEW (2026-06-09 EOD) — 4 user-reported fixes shipped (course แก้คงเหลือ ลด/เพิ่ม + Issue-4 wrong-course INDEX · treatment-count · stock customer-link · course-use editor) — COMMITTED + PUSHED, NOT deployed**: master = `b8351546` (1 commit ahead of prod `e56d2ac7`; **frontend-only, no firestore.rules → vercel-only when authorized, no Probe-Deploy-Probe**). `/systematic-debugging` ×4 + brainstorm (user "เสร็จแล้วอนุมัติ แก้หมดเลย"). **#1 แก้คงเหลือ**: AddQtyModal gains a เพิ่ม/ลด toggle + live preview; ROOT CAUSE of the wrong-course sale (Issue 4, LC-26000114 "Nebido" for a Shock-Wave add) = `entry.originalIndex` was a position in the FILTERED `activeCourses` consumed against the FULL `customer.courses` (a used-up 0/1 sub-item filtered out shifted every later index) — fix: carry `rawIndex` through `groupCustomerCoursesForDetailView` (also repairs exchange/share — Rule P); sale + audit derive product/name from the authoritative mutation RESULT (not a UI snapshot) + record the sub-product (Issue 2/4) + staff (Issue 3); reduceRemaining badge + reduce kind; removed dead origIdx. **#2 stale ประวัติการรักษา count**: `BackendDashboard:497` bare `viewingCustomer.proClinicId` (undefined for all LC-*) → `id || proClinicId` (V33 class, sole surviving callsite) + AV189 + tests + extended PAR1.4 (the V66-class guard that missed it); prod data SELF-HEALED (user re-created the treatment → CREATE rebuilds with the right id), heal idempotent (0 drift). **#3 stock movement → customer link**: NEW `useCustomerMap` (live-resolve id→name, defensive) + MovementLogPanel sky link (treatment/sale deductions, prod 43/43 sale + 152/152 tx carry customerId) → `window.open('?backend=1&customer=<id>','_blank')`; never red on a name (rule 04). **#4 course-use "ตัดคอร์ส โดย ..."**: now the OPD EDITOR (`editorContext.name`), not the doctor — TFP deduct (2 sites) + CourseHistoryTab live-resolve from treatment `editedByName` (V113, fixes existing). **Verified**: full vitest **16277/0** + build clean + new bank `course-adjust-and-fixes-2026-06-09` **22/0** (incl. a REAL index-fix unit test) + 7 V21 fixups + Rule-Q L2 prod. **Honest (Rule Q)**: L1 hands-on (modal/link/editor in a real browser) = user post-deploy; the adversarial-review WORKFLOW got rate-limited → self-reviewed inline. Checkpoint `.agents/sessions/2026-06-09-four-fixes-course-stock-treatment.md`.
- **NEW (2026-06-04 EOD+1) — Doctor stale-name propagation FIXED end-to-end (write chokepoint + live-resolve) + FEFO18 test-branch cleanup — DEPLOYED**: master = `e56d2ac7` = **Vercel prod `e56d2ac7`** LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; **vercel-only — NO firestore.rules change → no Probe-Deploy-Probe**). `/systematic-debugging` on user report "เปลี่ยนชื่อหมอใน tab=doctors แล้วไม่อัพเดทตามฐานข้อมูล". TWO surfaces of one class: **(1) WRITE `861711a3`** — DoctorFormModal has no `name` input; old `saveDoctor` carried `name` verbatim via `{...form}` → renames never persisted the display name the appointment dropdown reads. Fix: `composeDoctorName` (doctorValidation.js) + `saveDoctor` recomputes `safe.name` every save. **(2) READ `e56d2ac7`** — appt views rendered `appt.doctorName` RAW (frozen snapshot) at calendar/agenda/detail-body/hub-card → existing appts never tracked a rename. Fix: NEW `resolveDoctorName(appt, doctorMap)` (appointmentDisplay.js, V108/V111/V113 live-resolve class) + NEW defensive `useDoctorMap` hook wired at all 4 sites. **Rule M (LIVE on prod before deploy)**: `backfill-doctor-name.mjs --apply` (DOC-mpwmsm1i บริบูรณ์ วังแก้ว→หมอมุก, ASST-mowphsbf ""→ยาหยี; forensic; 0 appt snapshots) + `cleanup-fefo18-test-pollution.mjs --apply` (18 leaked TEST-FEFO18-* docs from `e2e-stock-fefo-expiry`: 3 branches + 3 products + 6 batches + 6 movements). **Verified**: doctor-name-compose 11/0 + appt-doctor-name-live-resolve 17/0 + build clean + **Rule Q L2 re-read REAL prod** (names correct, STALE/EMPTY 0, be_branches=3, Rule P sweep 0 test docs, both ops idempotent). Full vitest: the 15 hub-render fails my hook first caused are FIXED via the defensive `useDoctorMap` (6 hub files re-run green); the only 2 remaining reds = PRE-EXISTING env-flakes (bsa-task7 execSync git-grep POSIX-redir + v85-glow grep-PATH; zero diff overlap — no getAllMasterDataItems/fx-glow). 2 V21 source-grep fixups. **Honest gap (Rule Q)**: rendered-pixel L1 (rename a doctor WITH appointments → see it propagate to the calendar/hub) = user hands-on; logic + data + render-mount + build + deploy verified. Checkpoint `.agents/sessions/2026-06-04-doctor-name-propagation.md`.
- **Date (2026-06-04 EOD) — Appointment-system looping bug-hunt CONVERGED + DEPLOYED**: master = `0e80af8d` = **Vercel prod `0e80af8d`** (DEPLOYED this session; brought prod `bff0bde6` → `0e80af8d` = the 4 prior undeployed commits + the R5-R11 appointment loop + happy-path; aliased, live). No firestore.rules change → vercel-only, no Probe-Deploy-Probe. A **12-round adversarial loop** over the appointment Core + cross-system wiring (deposit/treatment/sale/stock/reminder/calendar/restore) **CONVERGED** (final fresh hunt + 2 independent hunts + Rule-P class-check all confirm bulletproof at the money/double-book/double-charge/corruption bar). **~12 real bugs fixed, each L2 on REAL prod + RED→GREEN**: R5 slot-HIJACK · R6 deposit reminder never fired (V67) + non-atomic deposit cancel/delete money (Rule T) + treatment-brick + cron · R7 doctor-clear orphan + cross-branch relocate + refund→slot-leak + reschedule-suppress · R8 orphan over-block (parent-status guard) + cancelled-badge · R9 restore-rebuild + AP1-msg + roomId · R10 link customer-validate + phantom-reconcile · R11 LINE-confirm resurrect. **No-regression (Rule A)**: happy-path e2e **29/0** + behavior/RTL **122/0** + full vitest **16219/0**; normal flows unchanged. Checkpoint `.agents/sessions/2026-06-04-appointment-loop-converged.md`.

- **Date (2026-06-03 EOD+5) — V161 Outstanding CLEARED + import-order creator display fix (LOCAL, NOT deployed)**: master = `491770f4`; Vercel prod still **`bff0bde6`** (4 commits ahead `a586d073..491770f4`, NOT deployed — frontend/scripts only, no rules → no Probe-Deploy-Probe). **Cleared all V161 Outstanding**: **S2 officeToPdf L2-verified on real prod** — `scripts/e2e-staff-chat-office-preview.mjs` **9/0** (S2/AV187 late-doc retry proven: doc created in Phase B after uploads → fn patched 8/8, none stuck pending; resolved the EOD+4 pending-verify by fixing the script's 3 latent never-run bugs: `dotenv` import→`loadEnvLocal`, missing PROJECT_ID fallback, TDZ guard→bottom; `a586d073`); **S1 retention pagination** real-prod dry-run clean (0 aged/0 orphans, pageToken loop + mapBounded run); SESSION_HANDOFF trim 198.5→184KB (`0c043010`); **Rule M APPLIED on prod** (`3d4ff611`) — Neuramis merge `38764←9B1DEFF7` (true same-branch dup; 20 CC batch + course mainProductId + import movement + active order all repointed; dup product deleted; forensic `_mergedFromProductId`; audit `…f5c9fd53`; idempotent; post-apply verified) + junk course "หฟแฟ" deleted (0 refs). **NEW order-creator fix (V47-class, `491770f4`)**: import-order surfaces never rendered `createdBy.userName` (29/29 branch orders populated) → added ผู้ทำรายการ to branch OrderPanel table + OrderDetailModal + central CentralStockOrderPanel table + **fixed central CentralOrderDetailModal latent bug** (read `order.user`→always '-', now `createdBy`; adjust/transfer left untouched — they correctly read `user`). **L1-verified branch on real authed app** (table "วัน" ×20 + modal "วัน"); full vitest **16142/0**; build clean; `tests/order-creator-display.test.js` 12/0. User self-checked + confirmed. Checkpoint `.agents/sessions/2026-06-03-order-creator-and-outstanding.md`.
- **Date (2026-06-03 EOD+4) — Staff-chatbox fresh-adversarial bug-hunt LOOP (V161) CONVERGED + DEPLOYED**: master = `bff0bde6`; prod = **`bff0bde6` Vercel LIVE** (deployed this turn, aliased `lover-clinic-app.vercel.app`). User "ultrathink" smell directive → `/systematic-debugging` + `Explore` adversarial sweep → fix-loop until a fresh hunt found nothing. **10 latent bugs, each failing-test-first→green+AVxx (AV180-188)**: hide-don't-unmount blast-radius (H2 draft/reply cross-branch leak AV180 · H11 inline media not paused on `display:none` AV182 · open-gate scroll/mark-read off visibility transition) · send-path orphan **D** (mint-id→upload→create-doc leaks blobs on partial-upload OR doc-create failure → shared `deleteStaffChatAttachmentFolder` at both sites, AV188) · H4 conditional auto-scroll AV181 · mention-spaces longest-match AV183 · cursor same-ms tiebreak-by-id AV184 · sticker object-URL once-per-item AV185 · **S2** officeToPdf late-doc-create race → bounded retry `patchOfficeAttachment` AV187 · **S1** retention orphan-sweep pageToken pagination + `mapBounded` (V122 class) AV186. **Verified**: staff-chat family **648/648** · full vitest **16127/16130** (3 reds = PRE-EXISTING flakes in untouched files — bsa-task7 execSync git-grep matches a comment in TFP:913 · v85-glow cmd.exe `grep` PATH · genShortId birthday-paradox — all pass isolated) · build clean · H2+client surface Rule Q L1 (Chrome MCP). **Deployed**: Vercel (frontend + S1 cron) LIVE; officeToPdf Cloud Run (S2) DEPLOYED — revision `office-to-pdf-00008-d2p` serving 100% (via `gcloud run deploy --source functions/officeToPdf` as the firebase-adminsdk service-account, exit 0). No firestore.rules change → no Probe-Deploy-Probe. **Pending verify (Rule Q-honest — S2 live-conversion L2 NOT run)**: S2 retry logic unit-verified (R1-R4) + revision healthy + Eventarc-wiring proven in V109/V110, but a real-prod conversion of the new revision is pending — BLOCKED by the canonical `scripts/e2e-staff-chat-office-preview.mjs` using a stale `import 'dotenv/config'` (dotenv uninstalled; predates the repo's inline `loadEnvLocal()`); port it to `loadEnvLocal()` (mirror `e2e-stock-realtime-lot-clear.mjs:49`) to run. S1 cron live but un-triggered. Checkpoint `.agents/sessions/2026-06-03-staffchat-bughunt-loop-v161.md`.
- **Date (2026-06-03 EOD+3) — staff-chat draft-persist-on-minimize + bubble draft-indicator + stock filter relabel SHIPPED; V160/AV179 fixed the read-cursor regression draft-persist caused — LOCAL, NOT DEPLOYED**: master = `4c46a154` (9 commits `aa47af52..4c46a154`); prod still **`62593b2c` LIVE** (all this-session work client-SDK/UI only, NO firestore.rules → vercel-only when authorized, no Probe-Deploy-Probe). `/brainstorming`→spec→`/writing-plans`→`/executing-plans` (2 feats) → `/systematic-debugging` (V160). **Feature A** hide-don't-unmount: `StaffChatPanel hidden={chat.minimized}` (display:none) keeps the Composer mounted → draft text+reply+staged files survive minimize→reopen. **A-bis** dark-zinc ✏️ bubble badge (`staff-chat-bubble-draft`) vs white/red unread; color locked via Chrome-MCP visual Q&A. **Feature B** stock ยอดคงเหลือ: `หมด (คงเหลือ 0)`→`หมด` / `ติดลบ (ต้องเติมสต็อค)`→`ติดลบ` + reorder หมด-before-ติดลบ. **V160/AV179** (systematic-debugging, real-browser-evidenced): hide-don't-unmount made "open" a visibility transition not a remount → MessageList opened at the TOP (`distanceFromBottom 5882`) + the IntersectionObserver created while `display:none` got STUCK → read cursor never advanced/persisted. Fix: MessageList `visible={!chat.minimized}` → on hidden→visible: scroll-to-bottom + mark-read direct (ref) + re-create observer. **Verified**: full vitest **16071/0** · build clean · **Rule Q L1 real browser** (draft survives + dark-zinc ✏️ badge; read-cursor: open→distanceFromBottom 0 + cursor advances + persists across reload + filter order/labels). Checkpoint `.agents/sessions/2026-06-03-staffchat-draft-persist-and-v160.md`.
- **Date (2026-06-03 EOD+1) — V159 SHIPPED (stock order line-item search + per-batch expiry edit) — LOCAL, NOT DEPLOYED**: master = `39c603b6` (8 commits `f690cfed..39c603b6`); prod still **`91bb3349` (V158) LIVE** — V159 NOT deployed (client-SDK only, NO firestore.rules change → vercel-only when authorized, no Probe-Deploy-Probe). `/brainstorming`→spec→`writing-plans`→inline TDD (subagent dispatch blocked by a 1M-context credit gate → executed inline). **F1 search**: `OrderPanel` + `CentralStockOrderPanel` now match line-item `productName` (was vendor/orderId only); matched item surfaced first via `formatOrderItemsSummary` matchQuery (backward-compat preserved). **F2 expiry**: NEW `updateStockBatchExpiry` (1 runTransaction: batch.expiresAt + forensic trail + `be_stock_adjustments` type=`expiry`/movementId null + source order-line sync by locationType; NO movement → conservation untouched; status untouched [EXPIRED derived]; Rule O live-resolve); editable `DateField` in the existing `AdjustCreateForm` (dual-path submit qty/expiry/both, covers ยอดคงเหลือ+ปรับสต็อค+central, NO new buttons); `type=expiry` rendered in adjust list + `AdjustDetailModal`. audit-stock-flow **S36** (35→36). **Verified**: full vitest **15992/0** (+22) · build clean · real-prod Rule Q L2 `scripts/e2e-v159-stock-search-and-expiry.mjs` **28/0** (batch+forensic+audit+order-sync · no movement · status unchanged · hasExpired flip · 2-concurrent last-write-wins+2 audit docs+qty intact · idempotency · no-order graceful · zero orphan). Honest scope: L1 (real browser: open ปรับ → edit date → save → see update) = user hands-on per workstyle. Checkpoint `.agents/sessions/2026-06-03-v159-stock-search-expiry.md`.
- **Date (2026-06-03 EOD) — 4-SYSTEM AUDIT LOOP (TFP/Stock/Sales/Finance) CONVERGED + V158 DEPLOYED**: master = `c31248ed` (V158 = `91bb3349`); prod = **`91bb3349` (V158) LIVE** @ lover-clinic-app.vercel.app (`vercel --prod` aliased 2026-06-03; client-SDK logic only, NO firestore.rules change → no Probe-Deploy-Probe, V153-V157 precedent). Was d780750c (V153-V157). `/systematic-debugging` 3-part loop (audit 4-system relationships → fix → repeat until a fresh round finds nothing). **R16 found V158 (M19, Rule T)** — V153's idempotency was a Σ QUERY **outside** the tx → idempotent for SEQUENTIAL repeats but **NOT a concurrency lock**: 2 concurrent cancel cascades on one sale (double-click / 2 admins) double-refunded wallet **+200 baht (re-spendable)** + over-reversed points **−50**. Fix: per-reference NET marker stored ON the doc (`saleNet` / `finance.pointsSaleNet`), maintained by deduct/earn + read by refund/reverse **IN the same tx** → OCC serializes (2nd reverse re-reads decremented marker → reverses 0); old Σ query kept as legacy seed for pre-V158 refs. Then **R17-R22 = 6 consecutive FRESH clean rounds** (concurrent sale-edit 8/0 · course lifecycle+use‖cancel race 6/0 · **4-system capstone** stock+deposit+wallet+points+course cancel-cascade + concurrent double-cancel 21/0 · report-accuracy audit [cancelled excluded from all 9 aggregators] · one-deposit/many-sales partial-cancel 12/0 · manual-adjust × sale-marker interplay 9/0) → stop-condition met (matches prior stock-loop 6-clean bar). Money domain (deposit/wallet/points/course/stock) atomic-RMW COMPLETE for **sequential AND concurrent** reverse. Full vitest **15970/0**; build clean; `tests/v158-concurrent-reverse-marker.test.js` 14/0 + audit-money-flow M19 + V158 V-entry + 2 V21 fixups. Checkpoint `.agents/sessions/2026-06-03-v158-four-system-loop.md`. **Outstanding (user, non-loop)**: dropdown หมวดหมู่ task · Neuramis merge + junk test-course "หฟแฟ"; optional future feature = cross-collection partial-failure auto-reconciliation report.
- **Date (2026-06-02 EOD+4) — STOCK ADVERSARIAL-HUNT LOOP: V147-V152 (6 concurrency/atomicity fixes) + R16-R21 (6 consecutive CLEAN verification rounds → loop CONVERGED) + ALL DEPLOYED LIVE**: master = prod = `ae51cc18` LIVE @ lover-clinic-app.vercel.app (`vercel --prod` EOD+4, **frontend-only — all V147-V152 are client-SDK runTransaction/CAS, NO firestore.rules change → no Probe-Deploy-Probe**). `/systematic-debugging` 3-part LOOP directive (find novel stock bugs → fix → repeat until a fresh round finds nothing). **6 REAL bugs found+fixed — ALL one family (non-atomic read-modify-write / stale-plan race) → NEW iron-clad Rule T**: **V147**(S32) `_deductOneItem` stale-plan FIFO race → retry loop; **V148**(AV177) `customer.courses[]` getDoc→updateDoc lost-update → `_mutateCustomerCoursesAtomic` runTransaction; **V149**(AV178) `finance.loyaltyPoints` RMW → runTransaction; **V150**(S33) `pickNegativeTargetBatch` excluded cancelled/expired carrier; **V151**(S34) reverse re-creates a V144-deleted lot (else cancel threw + stock lost); **V152**(S35) concurrent central-PO receive double-batch → CAS-claim-before-build (the documented AUDIT-V34 deferred gap). **Then 6 CLEAN verification rounds (R16-R21, real-prod Rule Q L2, 75 assertions, zero orphans)**: R16 reverse-conservation · R17 single-batch contention · R18 multi-lot FEFO+ledger-replay(S16) · R19 edit-path(reverse-old+deduct-new) · R20 cross-branch isolation · R21 transfer/withdrawal double-receive CAS. **Every R16-R21 "finding" was MY OWN test-assumption/filter error** (V144 0-lot delete, branchId-vs-locationId, V36-bis silent-skip), corrected to confirm the app is RIGHT — strong convergence signal. **Negative-stock from TFP/sale CONFIRMED INTACT** (R16.4/R17.1/R18.3/R20.3 + 18/0 comprehensive). Full vitest **15898/0**; build clean; each fix has source-grep test + audit invariant. Checkpoint: `.agents/sessions/2026-06-02-stock-concurrency-loop.md`. **Outstanding (user)**: dropdown หมวดหมู่ task (3rd original ask, not started) · V-log-archive verbose entries (V147-V152) unwritten (00-session-start one-liners suffice) · Neuramis merge + junk test-course (deferred data).
- **Date (2026-06-02 EOD+3) — B (be_products cleanup) APPLIED on prod + PRODUCT DELETE-CASCADE/GUARD (AV176) built (code LOCAL, NOT deployed) + adversarial bug-hunt (2 real bugs found)**: master = `6cff6905` (feat) + EOD docs commit. **prod bundle UNCHANGED = `85063e5d`** — the cascade code is LOCAL only; **deploy needed** to protect prod product-deletes (until then prod still uses the old bare delete = orphan risk). NO firestore.rules change → frontend-only deploy, no Probe-Deploy-Probe. **(A) B cleanup DONE on prod** (Rule M admin-SDK, two-phase): `v145-cleanup-polluted-product-junk.mjs --apply` (35 docs: junk-strip + restore cat/unit/type — 28 clean-copy + 7 manual) + `v146-cleanup-orphan-stock-and-dedup.mjs --apply` (5 orphan batches incl. screenshot's ยาทาจี้หูด/Buscopan-ฉีด + 2 FK-verified dedups). Both verified gone + idempotent; audit docs emitted. Deferred: Neuramis merge + junk test-course. **(B) `/systematic-debugging` orphan-on-delete** → bare `deleteProduct` left orphan batches+course refs. Fix = Guard+cascade, client-side (works on `npm run dev`): NEW `src/lib/productDeleteCascade.js` (pure) + `productDeleteClient.js`; ProductsTab routes through it; StockBalancePanel orphan backstop. BLOCK on stock>0 / course mainProductId / **pending inbound stock op**; CASCADE delete product + clear batches (==0 delete / <0 cancel, V144) + pull courseProducts[] + pull be_product_groups; batches cleared branch+central (location-agnostic); history kept (Rule O). **(C) Adversarial hunt (user: "ผมมีกลิ่น ... loop until dry") found 2 REAL bugs**: (1) pending-op un-receivable (139 products in active orders); (2) **op-schema heterogeneity** — my guard's `where branchId` MISSED transfers/withdrawals (no branchId, NUMERIC status) + central orders (centralWarehouseId) → fixed (load unfiltered; `isPendingOp` handles numeric+string). Disproven (code-read): purchased-course deduction (graceful skip, V36-bis), id-mismatch (0/611), marketing refs (none). **Verified**: product-delete **25/0** + **e2e 124/0** (`scripts/e2e-product-delete-cascade.mjs` — 3 branches × every scenario, real prod TEST fixtures, zero-orphan cleanup) + affected-area 230/0 + build clean. Full suite green MODULO the known `cross-branch-import-rtl` flake (15860/1 intermittent; JSON-reporter run = **0 failed**; passes isolated — NOT a regression). **AV176**. Checkpoint: `.agents/sessions/2026-06-02-product-delete-cascade.md`. **Outstanding (user)**: deploy cascade code · dropdown หมวดหมู่ task (3rd original ask, NOT started) · continue Round 3 hunt (marketing clean; quotations/DF/>450-chunk-atomicity reasoned-not-exhaustively-tested) · V-log V147 verbose entry unwritten.
- **Date (2026-06-02 EOD+2) — V145 STOCK PRODUCT-EDIT (full-doc load + real-time table) + V146 STAFF-CHAT REPLY (attachment preview + scroll-bounce) DEPLOYED + verified (L1 real app + L2 real prod)**: master = prod = `85063e5d` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, **frontend-only — NO firestore.rules change → no Probe-Deploy-Probe**, V125–V127 precedent). **V145** (AV175, `/brainstorming`→spec→plan→impl): stock-tab แก้ไข now loads the FULL `be_products` doc (was the aggregated balance row → blank/default load + `setDoc(merge:false)` overwrite-corruption risk). `normalizeProduct` WHITELISTS (drops stock-junk `batches/totalRemaining/totalCapacity/nextExpiry/expired/unit/valueCost/id`; preserves `stockConfig/createdBy/updatedBy/forensic _*`; +`__proto__` pollution guard) — whitelist enumerated from 610 real prod docs (Rule R). StockBalancePanel live-resolves unit/category/type + NEW หมวดหมู่/ประเภท columns (replaced ความจุ/มูลค่าทุน per user). **🐛 BIG bug found via L1+diag**: products-map effect had `[]` deps → STALE after branch switch → every row "-" for cat/type (BS-9/Rule L; pre-existing — canonical name + skip-filter were stale too; new columns exposed it). Fixed → deps `[selectedBranchId]`. StockTab/CentralStockTab `getProduct` fallback. **V146** (AV174): staff-chat reply quote shows image-thumb/file-icon/sticker (single `buildReplySnapshot` + shared `StaffChatReplyPreview`) + click-to-scroll + bounce highlight. **Verified**: full vitest **15836/0** + **Rule Q L2 18/0 REAL prod** (`scripts/e2e-v145-product-edit-roundtrip.mjs`: 609-doc whitelist completeness = ZERO legit-field loss + corruption round-trip + branchId preserved) + **Rule Q L1 real app** (new columns + live unit/category/type + full-doc edit modal + branch-switch re-subscribe; screenshots). **DEFERRED (user → next session)**: `be_products` data cleanup — 36 junk/corrupted docs in นครราชสีมา. Dry-run done (read-only); script `scripts/v145-cleanup-polluted-product-junk.mjs` bundles junk-strip + restore cat/unit/type (28 auto from clean cross-branch copies + 1 same-branch + 7 manual/ProClinic) + dedup 3 true same-branch dups (FK-check first). Checkpoint: `.agents/sessions/2026-06-02-v145-stock-product-edit.md`.
- **Date (2026-06-02 EOD+1) — V144 STOCK-PAGE BATCH (4 issues) DEPLOYED + L2-verified on REAL prod**: master = `2b1a8f11` (+ docs commit). prod = `2b1a8f11` LIVE @ lover-clinic-app.vercel.app (`vercel --prod` aliased + `firestore:rules`). User reported 4 issues on tab=stock → ยอดคงเหลือ (2 `/systematic-debugging` rounds): (1) no "หมด" filter; (2) 0-lots linger (want real-time delete; clarified: keep 1 if it's the LAST lot, delete all if a live lot exists); (3) ปรับ button bounces to the ปรับสต็อก tab; (4) two branch selectors (per-panel สถานที่ dropdown vs top BranchSelector). **Shipped**: (1) "หมด (คงเหลือ 0)" filter (predicate ===0). (2) NEW `_clearRedundantZeroLotsForProducts` reusing pure `planLotCleanup` (AV168), POST-COMMIT at 7 stock-mutation entry points; create*Transfer/Withdrawal AV172-exempt; deductCourseItems out-of-scope. `firestore.rules` `be_stock_batches` delete `if false`→`isClinicStaff() && resource.data.qty.remaining==0` (client can delete 0-lots real-time; live/negative undeletable; 03:45 cron stays backstop) — **Rule B Probe-Deploy-Probe 6/6 pre+post + cleanup + probe #16**. (3) user chose BOTH ปรับ+เพิ่ม: NEW `StockActionModal` (DRY) hosts EXPORTED `AdjustCreateForm`/`OrderCreateForm` in-place (no bounce; AV78 close); `StockTab` rewired (no setSubTab). (4) per-panel dropdown+auto-pick+userPickedLocation REMOVED; `locationId` DERIVED `lockLocation ? defaultLocationId : selectedBranchId` → follows top BranchSelector; central untouched; aligns with StockAdjustPanel/MovementLogPanel siblings. **AV172** (real-time lot-clear) + **AV173** (in-place modal + follow-selector). **Verified**: full vitest **15777/0** (15 V21 lock-in fixups; phase15.7-ter repurposed as removal anti-regression) + build clean + **Rule Q L1 live browser** (both modals in-place no-bounce + AV78 close · dropdown gone + dynamic switch นครราชสีมา 94 ↔ พระราม 3 5 · filter present · console clean) + **Rule Q L2 e2e 10/0 REAL prod** (`scripts/e2e-stock-realtime-lot-clear.mjs`: new-lot-in clears · FIFO-drain clears · drain-LAST keeps placeholder · negative NEVER deleted · Movement-Log intact; 0 orphans). **Rule M applied**: `stock-lot-cleanup.mjs --apply` deleted 14 lingering 0-lots (audit `v143-quater-...37330cf4`); idempotent 0 remaining + 30 placeholders kept. **DEFERRED (Rule P)**: CentralStockTab same navigate-bounce (different tab + CentralStockOrderPanel) — pinned by test CB1. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-02-stock-out-of-stock-filter-and-realtime-lot-cleanup*`. V-log V144.
- **Date (2026-06-02 EOD) — TFP MEGA-TEST complete (L1 2/0 + L2 19/0 on REAL prod) + OPD→CSV export · NO deploy (tests/scripts/docs only; prod bundle unchanged = `8e6a1d06`)**: master = `c63c3201`. User: "เทสที่เหลือให้จบ ครั้งสุดท้ายจริงๆ" (TFP = app core, longest bug history). Closed the L1 gap from the prior turn. **L2 — `scripts/e2e-tfp-mega-test.mjs` 19/0**: real CLIENT SDK (authed staff) drives the REAL backend functions TFP calls, on REAL prod (real rules/indexes/data-shapes), TEST-prefixed fixtures + cleanup. Each scenario reproduces a historical bug's exact trigger: **V104** (course 5/5→4/5) · **V36** (be_course_changes kind='use'; field is `linkedTreatmentId` NOT `treatmentId`) · over-deduct throws · **V142** carry-forward (reverse→5/5, re-deduct→4/5, no double/stuck) · **V44/V45** (assign product≠course-name) · **V108** (sale name resolved from patientData, not "-") · **Rule O/V46** (movement uses LIVE be_products name). The run caught 2 of my OWN test bugs (qty "4 / 5 ครั้ง" spaces; audit field name) before they masked a pass — Rule Q-honest working. **L1 — `tests/e2e/tfp-mega-l1.spec.js` 2/0**: real browser drives the REAL TFP UI on real prod, FRESH TEST customer+course (admin beforeAll; NO real-customer actions). Proves handleSubmit actually CALLS the chain: save → course DECREMENTS + be_treatments persisted + V36 audit emits (V104 bug = stays 5/5). **e2e test-debt found (pre-existing, NOT app bugs, documented)**: Phase 28 renamed สร้างการรักษา→`บันทึกการรักษา`; V26.1 removed the top-right `ยืนยันการรักษา` button; `tests/e2e/helpers.js` hardcodes deleted customer `2867` + stale `goToBackend` selector → old TFP specs (treatment-courses/buy-deduct/v96/v71) inherit this (FOLLOW-UP: copy tfp-mega-l1's fresh-fixture pattern). Prod left clean (0 TEST customers; verified). All 3 doctors tagged to นครราชสีมา → L1 injects `localStorage.selectedBranchId`. **CSV export** — `scripts/export-opd-customers-csv.mjs` (read-only): all 113 be_customers → 112 unique phones (customer's OWN phone, never emergencyPhone) + name/email/dob(YYYYMMDD CE)/gender(m|f) → `F:\FB\targeting\opd-customers.csv` (OUTSIDE repo, git-locked by user), deduped by last-9 digits, UTF-8 no-BOM. **Honest stance** (user asked "ไม่มีบั๊คแล้วใช่ไหม"): tested paths (Scheduled Tasks + TFP core) clean; "zero bugs whole-app" NOT claimable (tests prove presence not absence); other areas have existing 15720/0 + regression banks but weren't re-driven live. Detail → checkpoint `.agents/sessions/2026-06-02-tfp-megatest-scheduled-deploy.md`.
- **Date (2026-06-02 +1) — Scheduled Tasks 4th defense layer (resolveParam) DEPLOYED + post-deploy-verified**: prod bundle = `8e6a1d06` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; **vercel-only** — rules/storage/index/functions/vercel.json diff vs prod = empty → no Probe-Deploy-Probe). After the feature shipped, an adversarial bug-hunt (user: "ยังมีบั๊คและ edge case แน่ๆ ... ห้ามทำ bug เพิ่ม") found the 5 destructive retention/cleanup crons threaded their deletion window via `cfg.params?.X ?? DEFAULT` — `??` only falls back on null/undefined, so a corrupt `retentionHours:0` (e.g. a direct admin-SDK write bypassing the validated `saveSystemConfig`) would survive → cutoff=now → **DELETE-ALL**. Reachable paths were ALREADY triple-protected (input `min` attr + `onChange` clamp + `validateSystemConfigPatch` throw — proven by `tests/scheduled-tasks-param-safety.test.js` G1/G2/G5: every numeric param `min>=1` so 0 is structurally rejected); this adds the **unreachable-config-corruption backstop**. NEW pure `resolveParam(taskId,key,raw)` in `scheduledTasksRegistry.js`: null/undefined→default (matches `??`), valid→unchanged (**NO-OP**), 0/neg/>max→clamp to `[min,max]`, NaN/Infinity→default. Wired into all 5 crons (import + threading). **NO-OP for every valid value AND for the current prod config** (zero behavior change; only a corrupt value is made safe). **The G6 adversarial test caught a real divergence in the helper before ship** (`Number(null)===0` clamped to min instead of default → fixed: `if (rawValue == null) return default`) — the discipline working (real-adversarial, not self-serving). Also this session: hardened `readScheduledTaskConfig` fail-safe (only a literal `false` disables a cron — `"false"`/`0`/`null`/non-object entry all → enabled:true; `tests/scheduled-task-runtime.test.js` +8) + NEW `scripts/e2e-scheduled-tasks-live-guard.mjs` (LIVE adversarial probe: all 10 deployed crons reject no-CRON_SECRET → 401 = no public-trigger hole; run-now no/bad-token → 401; unknown/missing taskId → 400; **a DISABLED scheduled cron actually SKIPS on the deployed system** + force=1 overrides; reversible config toggle). **Post-deploy verify: live-guard 21/21 on fresh prod** (the 5 resolveParam crons all return 401 no-secret = modules bundle+load cleanly, no CRON_IMPORT_FAILED-style break) + full suite **15720/0** + build clean + param-safety 94/94 + scheduled group 174/174. Param-threading units all correct (hours×3600000, days×86400000, `computeCutoffISO(days)` — G4 lock). Firebase-retirement left no dangling reference in `functions/` source. **Nothing outstanding.** Commits `a0aeb0bd`(probe) → `4fe7a0da`(param-safety) → `8e6a1d06`(resolveParam).
- **Date (2026-06-02) — Scheduled Tasks tab "งานอัตโนมัติ & ตารางเวลา" DEPLOYED + verified LIVE**: prod bundle = `e32df9bc` LIVE @ lover-clinic-app.vercel.app (vercel ×3: initial → `441c0601` static-import → **`e32df9bc` FINAL internal-HTTP run-now**) + `firebase deploy --only functions` (DELETED the retired `cleanupOldStaffChatMessages` from Cloud Scheduler — the 7d/30d conflict gone). NO firestore.rules/storage change → no Probe-Deploy-Probe. Full `/brainstorming`→spec→`/writing-plans`→`/executing-plans` (12 tasks, inline TDD). User ask: "เอาระบบ Cronjob/auto-ลบ/auto-schedule มารวมเป็น tab เดียวให้ setting ได้ ... มันเยอะจนลืม". **Decisions**: Q1=A full (เปิด/ปิด + ปรับ param + last-run + run-now) / scope=A (10 scheduled tasks only; manual-cleanup endpoints keep their own UI) / safety=A (warn-confirm on disabling safety-critical) / location=A (new tab near ตั้งค่าระบบ, perm `scheduled_task_management`). **The completeness sweep (3 parallel angles) found an 11th task I'd missed — a Firebase `onSchedule` `cleanupOldStaffChatMessages` (7d) that silently OVERRODE the Vercel staff-chat-retention cron (30d)** → retired it (single deleter; the "เยอะจนลืม" payoff). **Architecture**: pure `scheduledTasksRegistry` (10 tasks, param defaults imported from each `_lib` core = single source) + `system_config.scheduledTasks` map (extend `systemConfigClient`) + `api/_lib/scheduledTaskRuntime.js` (FAIL-SAFE `readScheduledTaskConfig` — error/missing → enabled+default so a safety-critical cron never silently stops; non-fatal status write) + **all 10 crons** get a 6-line guard (read config → skip if disabled unless `force` → thread param `?? coreDefault` → write status) + `api/admin/run-scheduled-task.js` (run-now: dispatch the cron with server CRON_SECRET + force) + `ScheduledTasksTab.jsx` + `useScheduledTaskStatus` hook + extracted `SettingsPrimitives.jsx` (Rule C1) + perm/nav/tab wiring + **AV171** (every scheduled task in registry + carries the guard; exactly one scheduled deleter per collection). line-reminder hours stay per-branch (LINE settings); backup retention stays in the V122 executor → both enable-only. **Verified (Rule Q — REAL e2e per user "ผ่านทุกอย่างจริงๆ ถึง deploy ได้")**: full vitest **15617/0** + build clean (2503 modules) + unit/RTL/source-grep/Rule I flow-simulate(F1-F8)/endpoint. **L2 e2e 13/0 on REAL prod** (`scripts/e2e-scheduled-tasks-l2.mjs --apply`): write `enabled=false` to real `system_config` → `readScheduledTaskConfig` (exact cron path) returns false → **cron WOULD skip** → restore = config↔cron-read contract end-to-end on real data. **L1 Playwright e2e 1/1 on REAL browser + REAL prod** (`tests/e2e/scheduled-tasks-tab.spec.js`): render 10 tasks → toggle non-critical → toggle safety-critical (confirm) → tune param → **Save (real prod write via client SDK) → success banner → reload→PERSISTED → restore → run-now (POST fires → graceful banner)**. The L1 **caught a real UX bug** the mocked RTL missed: the post-save `system_config` onSnapshot refire reset `saved=false` → the "บันทึกเรียบร้อย" banner cleared instantly → **fixed** (drop `setSaved(false)` from the `[config.scheduledTasks]` effect; next user-edit clears it) + locked (F8). prod restored pristine (no litter). Also fixed a **pre-existing date-rollover flake** (phase15.7 FEFO N4.2 — stale `2026-06-01` expiry became past on 2026-06-02; unrelated, separate commit). **INHERENTLY post-deploy** (can't pre-deploy): the *deployed* Vercel cron actually skipping when disabled (code is L2-proven; needs the guard on Vercel) + run-now hitting the *live* serverless endpoint (vite doesn't serve `/api`; wiring is L1-proven + dispatch unit-tested) — verify immediately after deploy. **NO firestore.rules change** (the `clinic_settings/{settingId}` wildcard already covers the status doc; Firestore OR-semantics make a more-specific match inert) → deploy = **vercel + `firebase deploy --only functions`** (removes the retired fn) only, NO Probe-Deploy-Probe. **Post-deploy (the cron guards + UI + scheduled crons deployed clean first try; only the manual run-now had deploy-only bugs invisible to local vitest)**: (1) **CRON_IMPORT_FAILED** — Vercel's bundler doesn't trace a dynamic computed `import(map[taskId])` → cron module unbundled → 500 → switched to STATIC imports (`441c0601`); (2) **storageBucket conflict** — `verifyAdminOrPermissionToken` inits firebase-admin WITHOUT a bucket → a static-imported cron's own `initAdmin()` no-ops (`getApps().length>0`) → `getStorage().bucket()` throws → 500 → **internal-HTTP trigger** (run-now POSTs the task's OWN deployed cron fn `{cronPath}?force=1` + server `CRON_SECRET` → runs in its own init context; FINAL `e32df9bc`). **LIVE verification ALL GREEN**: run-now direct HTTP **200** (`ranBy:loverclinic@…`, `cronStatus:200`, status doc `lastRunAt` went FRESH → run-now → internal-HTTP → real cron exec → status write) + **L1 deployed-URL Playwright 1/1** (same spec re-run vs `https://lover-clinic-app.vercel.app` with `E2E_BASE_URL`, webServer skipped + no localhost fallback → the full UI flow incl. **run-now → live `/api/admin/run-scheduled-task` → success banner** ran on the LIVE bundle) + deployed **scheduled** cron wrote a status doc on the live system. `public/brainstorm-scheduled-tasks.html` stripped before deploy. **Nothing outstanding on this feature.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-02-scheduled-tasks-tab*`.
- **Date (2026-06-01 EOD+2) — Frontend top menu bar pinned (sticky) SHIPPED + DEPLOYED (AV170)**: master = `6aee3de3`; **prod bundle = `6aee3de3` LIVE** @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; **vercel-only** — verified 0 rules/storage/index/cron/functions in prod→HEAD diff). `/systematic-debugging`: the Frontend top menu (`<header data-testid="admin-top-menu">`) scrolled away because it was `position: relative`. The trap (proven in a real browser): a naive `sticky top-0` SILENTLY no-ops there because the parent `.admin-frontend-zone` used `overflow-x-hidden`, which CSS coerces to computed `overflow-y: auto` → the zone becomes a scroll-container that captures the sticky (header still scrolled off −568px). Fix = 3 coordinated changes: header `relative z-20`→`sticky top-0 z-20`; zone `overflow-x-hidden`→`overflow-x-clip` (clips horizontally WITHOUT becoming a scroll-container → header sticks to viewport: probe 0px); in-page QR sidebar `sticky top-8`→`top-24` (clears the ~60px sticky menu — verified top-8 overlaps, top-24 clears). Backend top bar (`BackendTopBarNew`) was ALREADY `sticky top-0` (its `overflow-x-hidden` sits on a sibling `<main>`, not an ancestor) — the working reference, unchanged (isolated Frontend miss; Rule P no expansion). **Verified (Rule Q)**: real-browser isolation probe (mechanism: hidden −568 / clip 0) + REAL authed AdminDashboard on dev server (= exact committed code + real prod Firebase): menu `sticky`/`top:0`/zone `overflow-x:clip`+`overflow-y:visible`, scroll 700px → menu stays at viewport-top **0** (`getBoundingClientRect`) + 148 targeted tests pass + build clean + **AV170** + `tests/admin-menu-sticky-source-grep.test.js` (S1-S5). **Honest gap (Rule Q-honest)**: literal LIVE-prod-URL browser nav harness-blocked (Claude Preview origin-locked to localhost; Chrome ext not connected) — deployed bundle = the verified commit, sticky CSS identical regardless of serving origin; screenshot capture stalls on the animation/listener-heavy page (no page error). **Outstanding (user)**: none for this; carryover = cron `stock-lot-cleanup` 03:45 BKK + prior-session V-log entries (sales/EOD+5/+6) unwritten.
- **Date (2026-06-01 EOD+1) — Staff-chat jump-to-latest button + cold-open scroll fix SHIPPED + DEPLOYED + L1-verified on LIVE prod**: master = `6221c5a0` (docs); prod bundle = `ede847dd` LIVE @ lover-clinic-app.vercel.app (`vercel --prod` ×2 this session, both **vercel-only** — verified 0 rules/storage/index/cron/functions in each diff). TWO staff-chat ships: **(1) jump-to-latest button** (full `/brainstorming`→spec→`/writing-plans`→`/executing-plans`; Q1=C circle + rose unread-count badge "9+" cap, Q2=A appear-when-scrolled-up; reuses the EXISTING V82 bottomSentinel IntersectionObserver to drive `isAtBottom` + `chat.unreadCount`; `StaffChatMessageList.jsx` + 1-line Widget prop; `de6f322b`/`0c26e21a`). **(2) cold-open scroll fix** (`/systematic-debugging`; `ede847dd`): on a cold tab open the chat opened scrolled-UP + stuck (image 1) — root cause (REAL-prod evidence) = the auto-scroll `endRef.scrollIntoView({behavior:'smooth'})` keyed on `[lastMessageId]` had its smooth animation INTERRUPTED by mount-time re-renders and settled ~1158px short (scrollTop 4538/5695), never self-correcting (effect re-fires only on a new last id); fix = `scrollContainerToBottom(listRef.current)` → `scrollTop = scrollHeight` (instant, immediate + rAF re-assert), deps unchanged `[lastMessageId]` (V140 no-yank-while-reading preserved); **AV169** + new regression test + V140 V21-fixup (outcome-based). **The V82 read cursor was NOT the bug** (it IS saved per-device; nothing used it to scroll). Both pre-existing (auto-scroll verbatim V140) + surfaced by the new jump button. **Verified (Rule Q)**: full vitest **15533/15534** (the 1 = known flake `phase-17-1-cross-branch-import-rtl`, confirmed PASS 7/0 isolated) + build clean + **cold-open L1 on LIVE prod on the deployed build** (fresh tab → chat lands at bottom: scrollTop **5695/5695**, distanceFromBottom **0**, jump button hidden, screenshot = image 2; before-fix 4538/5695 stuck at image 1). Subagent-driven (user-chosen) was blocked by a "1M context credits" account error → inline fallback (controller review per task). Detail: `.agents/sessions/2026-06-01-staffchat-jump-and-scroll-fix.md`. **Outstanding (user)**: none for these (deployed + L1-verified); carryover = cron `stock-lot-cleanup` 03:45 BKK + prior-session V-log entries (sales/EOD+5/+6) unwritten.
- **Date (2026-06-01 EOD) — Sales ยอดชำระจริง column + 30/page pagination + table redesign SHIPPED + DEPLOYED (29-commit batch LIVE)**: master = `5fe8edaa` (docs commit); prod bundle = `0628f91a` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `0c607f68` V142+V143). **vercel-only, no Probe-Deploy-Probe** (verified 0 rules/storage/index/cron in the 29-commit diff); dev `public/brainstorm-*.html` stripped pre-deploy. TWO features this session (each `/brainstorming`→spec→`/writing-plans`→`/executing-plans` inline TDD, cosmetic-shell, Rule Q): **(1) ยอดชำระจริง column + 30/page pagination** (tab=sales) — NEW `src/lib/financeUtils.js` `resolveSalePaidAmount/Outstanding/Tone` (paid = Σ `payment.channels` → `totalPaidAmount` fallback; **Rule R diag 35/35 real sales use channels**; **Rule Q L2 0-mismatch on 40 prod sales** via SHIPPED helper in `scripts/diag-sale-paid-shape.mjs`) + reuse canonical `usePagination(30)`+`Pagination` (Rule C1); paid number color-coded green/amber/gray (Q2=B), rename ยอดรวม→ยอดสุทธิ (Q3). **(2) sales table redesign** (Q1=A clean&compact) — clean money nowrap, source→`SaleSourceTag` tag in รายการขาย, status→`SaleStatusPill` nowrap pill, actions `p-2.5→p-1.5` compact, **responsive** `table min-w-[920px]`+truncate(ลูกค้า/รายการขาย)+overflow-x-auto (sweet 1024→2560 + Windows DPI scale); NEW `src/components/backend/SaleRowParts.jsx` (pure, RTL 6/0). Batch also carried EOD+5/EOD+6 (staffchat resizable panel, confirmed-card/course-step/confirm-btn). **Verified (Rule Q)**: full vitest **15510/0** (ALL GREEN, no flake this run) + build clean + Rule Q L2 (helper==real paid) + **Rule Q-vis** (2 themes + responsive 1280/1040/900 SEEN in Chrome MCP) + 3 V21 fixups (branch-aware C.1 header 8-col / C.4 money-clean / v33 D.1 OPD label→SaleRowParts). **Honest gap**: assembled real admin-gated `SaleTab` pixels + real Windows-scaled screen L1 = USER (Chrome harness window-resize limited; grounded mockup + fixed-width previews are the pixel evidence). Detail: `.agents/sessions/2026-06-01-sales-paid-column-and-redesign.md`. **Outstanding (user)**: USER L1 on LIVE prod; ship-artifact V-log entries (this session paid-column+redesign + carryover EOD+6 resizable-panel + EOD+5 V73-BS1 confirm-btn/course-step) NOT yet written; cron stock-lot-cleanup active 03:45 BKK.
- **Date (2026-05-31 EOD+6) — Staff-chat desktop RESIZABLE panel SHIPPED (brainstorm→spec→plan→impl) — PUSHED, NOT deployed**: master = `3678f6c5` (pushed origin/master); prod UNCHANGED = `0c607f68` LIVE. Now **17 commits ahead** of prod (EOD+5 confirmed-card/course-step 13 + EOD+6 resizable-panel 4); frontend/lib only → **no Probe-Deploy-Probe**. Full cycle `/brainstorming` (Visual Companion grounded in REAL `StaffChatPanel`/tokens per §S-design; **Q1=A** top-left-corner grip only / **Q2=A** min 360×480 .. max vw−32×vh−32 + auto-clamp) → spec → `/writing-plans` → `/executing-plans` inline TDD (4 tasks). User ask: desktop chat box ลากมุมซ้ายบน → resize กว้าง+สูงอิสระ, มุมล่างขวายึดติด, reflow ลื่นทันที, จำขนาดที่เครื่อง → ย่อ-เปิดใหม่/มีคนทักเด้ง = ขนาดเดิม ("บางทีรู้สึกเล็กไป"). **Architecture (3 additive pieces, zero change to chat flow)**: NEW `src/lib/staffChatPanelSize.js` (pure `clampSize` + per-device localStorage single key, mirrors `staffChatReadCursor.js`) + NEW `src/hooks/useStaffChatPanelResize.js` (`matchMedia(min-width:768px)` desktop-gate; drag = direct-DOM `panelRef.style` mid-move → no list re-render → 60fps, commit+`setPanelSize` on pointerup; window-resize re-clamp; double-click grip → reset DEFAULT) + `StaffChatPanel.jsx` wire (`ref` + inline `width/height` overrides `md:w-[360px]` + grip ⤡ top-left). **Reflow FREE** (existing `flex-1` list + `max-w-[80%]` bubbles); **auto-restore FREE** (minimize/popup flip `minimized` → panel remounts → mount reads `getPanelSize()`). Mobile (<768px) unchanged. **Verified (Rule Q)**: full vitest **15469/0** (+29: unit 15 + RTL 8 + Rule I flow-simulate 6; zero regression) + build clean + **L1 REAL-browser on the REAL mounted panel** (Chrome MCP, SEEN+measured: default 360×480 → drag 560×680 [bottom-right corner UNCHANGED right=1813/bottom=852 + bubbles reflowed] → drag-past-edge clamp 1797×836 onScreen → **reload restores saved size** = minimize/popup remount contract → dbl-click reset 360×480+persist). **Honest Rule Q gap**: mobile-<768 real-browser viewport-shrink harness-blocked (`resize_window` left innerWidth=1829) → mobile-unchanged = RTL-verified (R2) only; assembled auth-gated AdminDashboard widget L1 (smoothness feel + both themes + real minimize/popup w/ live messages) = USER post-deploy. Detail: `.agents/sessions/2026-05-31-staffchat-resizable-panel.md`. **Outstanding (user)**: deploy 17-commit batch + USER L1; ship-artifacts at deploy (V-log entries + delete `public/brainstorm-*.html`); carryover V142/V143 L1 + cron stock-lot-cleanup 03:45 BKK.
- **Date (2026-05-31 EOD+5) — Confirmed-card + course-step plan IMPLEMENTED + /systematic-debugging (confirm-btn-follows-status + confirmed sky→green) — PUSHED, NOT deployed**: master = `15cde92e` (pushed origin/master); prod UNCHANGED = `0c607f68` LIVE. 13 commits ahead of prod; frontend/lib only → **no Probe-Deploy-Probe**. **Plan** (`docs/superpowers/plans/2026-05-31-appt-confirmed-card-and-course-step.html`, มติ ①A/②A/③B) implemented Tasks 1-7 via TDD: **①** `sortApptsConfirmedFirst` (pure, reuse `sortApptsByDateTimeAsc`) + today-tab reorder confirmed-active-to-top + card highlight; **②A** course OPD step amber "ยังไม่ตัด"→muted "ไม่ตัดคอร์ส" (ONE SSOT `resolveCourseStepState` `'warn'`→`'not-deducted'` → drives Frontend card AND CDV history together); **③B** add 4th "course" step to CDV treatment history (keep teal/amber connectors; `courseDeducted: resolveCourseDeducted(t)` computed in the `treatmentSummary` mapper from raw `t` — V139/V104 detail-strip trap) + **7 V139 V21-fixups** + Rule R diag `scripts/diag-course-deducted-check.mjs` (SHIPPED predicate on real prod = **15 deducted / 10 not-deducted** of 25 recent; **trap-check 0** → reads `detail.*` only). Then **`/systematic-debugging`** on 2 user-reported issues: **(1)** "คอนเฟิร์มนัด" button VANISHED when pending + a treatment record exists — root cause = button dispatch led by `hasTreatmentForDay` swallowed the confirm branch (**V73-BS1 class**; `pending` was the ONLY status without a separate status-driven gate — confirmed has `showMarkCompleteBtn`, done has `showUnmarkBtn`). Fix = NEW `showConfirmBtn = rawStatus==='pending' && !isPastDate` separate gate mirroring the others → confirm follows REAL status, reappears whether edited Frontend/Backend, realtime via existing onSnapshot. `tests/appt-confirm-button-follows-status.test.jsx` 6/0. **(2)** confirmed status color **sky→green** at 3 sites (`_apptHubStyles.js` `ACCENT_BAR.confirmed` + `STATUS_CHIP_CLS.confirmed` + RowCard `surfaceCls`) — `green` distinct from done=`emerald`, both themes (token-parallel to proven emerald chip). **Verified (Rule Q)**: targeted RED→GREEN per task + **full suite 711 files / 15440 tests, 0 fail** (lightbox flake passed this run) + build clean + cosmetic-shell diff scan (plan tasks: 0 handler/state/event/testid change). **Honest Rule Q-vis gap**: assembled real-browser pixels (confirm reappears on real pending+treated admin card; green both Dark+Light; ① reorder + 2-device realtime; ②/③ course steps) = **USER L1** (admin-gated, not deployed) — high confidence (class-locked + token-parallel-AA) but SEE-it pending. **Ship artifacts at deploy**: V-entry + AVxx (V73-BS1 status-gate class + course-step-needs-`detail`) + delete dev mockups `public/brainstorm-*.html`. Detail: `.agents/sessions/2026-05-31-confirmed-card-coursestep-confirmbtn.md`. **Outstanding (user)**: deploy the 13-commit batch + USER L1; carryover V142/V143 L1 + cron stock-lot-cleanup 03:45 BKK.
- **Date (2026-05-31 EOD+4 LATE+4) — Brainstorm + spec + PLAN (confirmed-card highlight/reorder + course-step muted) + NEW rule ground-mockups-in-existing-design — NO CODE SHIPPED (implement next session)**: master HEAD = EOD docs commit (on `c5e3d47a`); prod UNCHANGED = `0c607f68` LIVE. Full `/brainstorming` (Visual Companion **grounded in REAL `TreatmentLifecycleStepper`/`AppointmentHubRowCard` source + real `index.css` tokens** — after the user scolded an un-grounded first mockup) → spec → `/writing-plans`. **Decisions locked: ①A** Frontend "วันนี้" confirmed appt card = sky-tint box (matches real sky "ยืนยันแล้ว" accent) + reorder confirmed-active to TOP of today tab (realtime via existing onSnapshot; NEW pure `sortApptsConfirmedFirst`) · **②A** OPD course step finished-but-not-deducted: amber "!ยังไม่ตัด" → muted "ไม่ตัดคอร์ส" (ONE SSOT `resolveCourseStepState` `'warn'`→`'not-deducted'` → affects BOTH Frontend card AND CDV history) · **③B** add course step to customer-detail treatment history, KEEP its teal/amber connectors. **③ root-cause VERIFIED (V139/V104 class):** `CustomerDetailView` `treatmentSummary` mapper (~L564) strips `detail` → plan computes `courseDeducted: resolveCourseDeducted(t)` in the mapper (raw `t` has `.detail`). **NEW iron-clad sub-rule** (user, angry — "ทำไมมึงไม่ดูดีไซน์เดิมกูก่อนค่อยทำตัวอย่างมา"): every design mockup MUST be grounded in the REAL existing design (provided screenshots + live app + exact source) BEFORE presenting — encoded 4 places (memory `feedback_ground_mockups_in_existing_design.md` + MEMORY.md + `.claude/rules/01-iron-clad.md` §S-design + `~/.claude/skills/brainstorming/SKILL.md`). All cosmetic-shell + 1 pure helper, frontend/lib only → no Probe-Deploy-Probe; no deploy this session. Spec/plan: `docs/superpowers/{specs,plans}/2026-05-31-appt-confirmed-card-and-course-step*`. Dev mockups `public/brainstorm-*.html` (DELETE at deploy, plan Task 7). Detail: `.agents/sessions/2026-05-31-brainstorm-confirmed-card-course-step.md`. **Outstanding (user):** implement next session; carryover L1 V142/V143 (2-device balance + NK shows 0) + cron stock-lot-cleanup (03:45 BKK).
- **Date (2026-05-31 EOD+4 LATE+3) — V143 stock cluster (show-0 + real-time + auto-clear) + held V142-quater/quinquies — SHIPPED + DEPLOYED**: master = prod = `0c607f68` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `8c3a9047`). Frontend/lib + 1 new cron → **no Probe-Deploy-Probe**. Two families deployed together: **V142** (course double-deduct: V142-quater over-credit gate + V142-quinquies finalize→doctor→finalize via persisted `_courseDeducted` flag; verified matrix 30/0 + flag-roundtrip/fuzz/stock **L2 e2e 30/0** real prod) + **V143 stock** — (a) **V143** StockBalancePanel show products drained/cleared to exactly 0 (`status∈{active,depleted}`; 7 NK products were hidden) **AV166**; (b) **V143-ter real-time** NEW `listenToStockBatchesByBranch` (Layer-1 onSnapshot BS-13 safe-by-default + Layer-2 wrapper; panel migrated to live listener) **AV167**, **L2 e2e `scripts/e2e-stock-balance-realtime.mjs` 5/0 real prod** (create/deduct/drain-0/delete from another surface push LIVE); (c) **V143-quater auto-clear-lot** pure `planLotCleanup` (keep live + ≤1 zero placeholder, DELETE-only) + daily cron `api/cron/stock-lot-cleanup.js` (03:45 BKK, vercel.json) + Rule M `scripts/stock-lot-cleanup.mjs` **AV168** (helper 14/0 + dry-run 0-redundant). **Rule M APPLIED on prod (earlier this session)**: `v143-nakhon-stock-reset.mjs --apply` (51 batches→0/0, deleted 364 movements/orders/adjustments) + `v143-collapse-nk-multilot.mjs --apply` (→ 53 products = 53 lots at 0); idempotent + audited. **3 V21 test-fixups** (v143 SG1 + v138 N10.7 + v34 INV.11.4 — all the V143-ter panel `listStockBatches`→`listenToStockBatchesByBranch` migration class; full suite confirmed no 4th). **Verified (Rule Q)**: full vitest **15418/0** (JSON-reporter run) + build clean (✓ 2.82s) + the L2 e2es above. **Honest gap**: assembled 2-device real-browser live-balance + NK-shows-0 = USER L1. Detail: `.agents/sessions/2026-05-31-v142-v143-deploy.md`. **Outstanding (user)**: L1 hands-on; cron now active (optional CRON_SECRET hit to verify like V122).
- **Date (2026-05-31 EOD+4) — V142 (course-deduct edit-resave SYMMETRY) + held V140 + V141 — SHIPPED + DEPLOYED + REAL-PROD-L2 VERIFIED**: master = prod = `8c3a9047` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `3342a9f0`). ONE combined commit (V140+V141+V142; SKILL.md AV160-AV163 spans all three; all frontend/lib). `/systematic-debugging` on a user-reported (verbatim, furious — prior tests "passed" while this slipped = the exact V104 simulate-only / Rule-Q-honest trap) course-deduction bug: "ซื้อแล้วตัดคอร์สเลย แล้วคอร์สมันไม่ตัดออกจากตัว". Frontend/lib only → **no Probe-Deploy-Probe**. **Root cause (confirmed from real prod data — diag `scripts/diag-course-not-deducted-bt.mjs` + `diag-cc-by-customer.mjs` on LC-26000115 / BT-1780203508072)**: edit-RESAVE reverse/re-deduct ASYMMETRY. `be_course_changes` recorded `use` qtyAfter "0/1" yet `customer.courses` stayed "1/1" (full). On a 2nd+ save, `handleSubmit` reverses the prior deduction (`oldPurchased` from `existingCourseItems`, refunds reliably + NO audit) then re-deducts the FRESH serialization (`backendDetail.courseItems`), EMPTY for purchased courses: (a) `purchased-…` rowIds regenerate to `be-row-N` (`mapRawCoursesToForm`) → Pass-1 miss; (b) `courseItems→treatmentItems` restore (TFP ~1158) drops productId → V101 Pass-2 can't run; (c) rem=0 → Pass-2 `rem>0` gate skips. **Refund-without-rededuct → balance reverts to full.** V12/V104 family at the EDIT-REVERSE boundary V104 never covered. **Stock parallel NOT affected** (gated by `hasStockChange` + `_resolveProductIdByName`). **Fix**: NEW pure `buildReDeductListWithCarryForward(fresh, oldReversed, selectedRowIds)` (treatmentBuyHelpers.js) → re-deduct = `fresh ∪ carry-forward(still-selected, not covered)` → reverse+re-deduct symmetric (un-check drops = correct; covered excluded = no double). TFP both sites `isEdit ? helper : freshExisting/freshPurchased` (create-mode unchanged). **AV163**. **Verified (Rule Q)**: **TRUE-L2 real-prod e2e `scripts/e2e-v142-edit-resave-course-deduct.mjs` 10/0** — SHIPPED `assign/deduct/reverse` + the SHIPPED helper on real Firestore: O.2 ★BUG REPRODUCED★ (pre-V142 → rem 1,1,1) + N.3 ★FIX VERIFIED★ (→ rem 0,0,0) + N.4 multi-edit + U.2 un-check + A.1 audit; `tests/v142-*` **20/0** (helper + Rule I simulate w/ REAL courseUtils arithmetic: C1 OLD-reverts/NEW-holds + C2 buy-no-deduct + C3 create-then-edit + C4 multi-edit + C5 un-check + C6 reordered + D rowId-regen + B source-grep); targeted course/deduct regression **543/0**; **full vitest 15336→15356/0**; build clean. **Heal dry-run** (`scripts/heal-course-reverted-by-edit-resave.mjs`): 35 scanned → **3 healable = LC-26000115** (`1/1→0/1` ×3) + 2 ambiguous = LC-26000009 (manual) + 0 false-positives. **Honest gap**: assembled real-browser multi-save on auth-gated AdminDashboard = USER L1. Detail: `.agents/sessions/2026-05-31-v142-course-deduct-edit-resave-symmetry.md`. **HEALS APPLIED**: V141 visitReasons 109 customers (audit `…eff2805c`); V142 course-heal = 0/no-op (LC-26000115 already `0/1` — a 2nd treatment `BT-1780214479261` re-deducted them in the bug window; ⚠ possible duplicate to review; LC-26000009 ambiguous=manual). **V142-bis (same session)**: user asked to test the single-save CREATE flow (ซื้อ+ตัด+คิดเงิน+เอายากลับบ้าน, กดบันทึกครั้งเดียว). Extracted the V101 two-pass courseItems IIFE VERBATIM → `buildCourseItemsForSave` (testability; behavior-identical, re-deploy optional); 2 V21 fixups (v104 SG3 + v101 A1/A2/A5 + v101 replica→real-helper). `tests/v142-bis-*` 8/0 + TRUE-L2 `scripts/e2e-v142bis-single-save-buy-deduct-charge-meds.mjs` 7/0 (real prod: course 1/1→0/1, Talafil stock 10→9, sale 2140, audit). Empirical: BT-1780203508072 CREATE save DID deduct → create path was never the bug. Full vitest **15364/0**. **V142-bis HELD uncommitted** (behavior-identical refactor). **Outstanding (user)**: commit V142-bis + L1 hands-on + review BT-1780214479261.
- **Date (2026-05-31 EOD+3) — V140 (staff-chat scroll + lightbox nav) + V141 (kiosk visitReasons preserve) — DONE + FULLY VERIFIED, UNCOMMITTED/HELD**: master HEAD = this EOD docs commit; prod UNCHANGED = `3342a9f0` (V138+V139, deployed earlier this session). **V140 + V141 SOURCE uncommitted in working tree** (6 mod + 4 new) — user ran `/session-end` without authorizing commit/deploy/heal. Both via `/systematic-debugging`. Frontend/lib only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V140 Bug1** — staff-chat กด Enter ไม่เด้งล่างสุด: `StaffChatMessageList` auto-scroll effect keyed on `[messages.length]` but the listener caps at 50 (`useStaffChat limitCount:50`) → past 50 msgs length is frozen → effect never re-fires. Fix = key on `lastMessageId` (ChatPanel `[messages]` = working ref). **AV160**. **V140 Bug2** — lightbox nav arrows `bg-white/15`+white icon invisible on white/light images → dark `bg-black/55 ring-1 ring-white/40 shadow-lg` (prev+next; ImageLightbox `bg-black/80` = ref); top-bar/PDF buttons gradient-protected (sanctioned). Rule Q-vis Chrome-MCP screenshot: visible on white/dark/blue/green/skin-tone. **AV161**. **V141** — kiosk intake→be_customers conversion folded `visitReasons`(array)→`symptoms`(string) + dropped `visitReasonOther`/`hrt*` (`kioskPatientToCanonical:153`); intake "สาเหตุที่มาพบแพทย์" (AdminDashboard:5039) + Clinical Summary read `visitReasons` → BLANK even though filled. **Rule R decisive**: opd_sessions visitReasons **100%** / be_customers **0/113** (incl. screenshot LC-26000115). Form ALREADY requires it (intake/deposit) → NOT a fill-bug, a conversion/display bug (Phase 26.2g class). Fix = 3-mapper triangle preserve 5 fields (kioskPatientToCanonical + buildPatientDataFromForm + buildFormFromCustomer; **snake_case canonical** to honor Phase 23.0 no-camelCase-on-root + camelCase patientData for readers; covers create + admin-edit-no-clobber). **AV162**. **Verified**: full vitest **15336/0** (700 files) + build clean + V140 8/0 (+lightbox/chat families 173/0) + V141 9/0 (+mapper families 157/0; updated v55-1 byte-identical snapshots + phase-23-0 camelLeak — legit contract). **V141 heal dry-run (Rule M): 109/113 recoverable** (`scripts/heal-visit-reason-from-symptoms.mjs` — split symptoms, validate vs VISIT_REASON_VALUES so admin free-text isn't corrupted; `--apply` GATED). **Honest gap**: mapper/data proven; assembled real-browser (chat-scroll on a 50+ thread; healed-customer intake bullets) = USER L1. Detail: `.agents/sessions/2026-05-31-v140-v141-chat-scroll-lightbox-visitreasons.md`. **Outstanding (user)**: commit V140+V141 → deploy → heal `--apply` → L1.
- **Date (2026-05-31 EOD+2) — V139 (OPD course-step + appt status↔tab sync) + V138 (negative-batch status invariant) SHIPPED + DEPLOYED + HEALED**: master = prod = `3342a9f0` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `409804fc`). Frontend/lib only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. ONE combined commit (V138+V139 share `backendClient.js` + audit SKILL.md). Full cycle `/brainstorming (Visual Companion; Q1=B warn "ยังไม่ตัด" / Q2=A violet / Q3=A symmetric) → spec → writing-plans → executing-plans (inline, 8 tasks)`. **V139 Feature 1** — opt-in 4th "คอร์ส" step on the นัดหมายวันนี้ card stepper (violet ✓=ตัดคอร์ส / amber "ยังไม่ตัด"=เสร็จแต่ไม่ตัด — catches staff who forget to deduct; ซื้ออย่างเดียว≠ตัด); SSOT `resolveCourseDeducted` reads `detail.courseItems`/`treatmentItems` (Rule R diag: top-level=0 on prod, 57 deducted / 24 warn-case of 88); opt-in `withCourseStep` → CDV history stays 3-step; live via existing `listenToTreatmentsByDateRange`. **V139 Feature 2** — couple `appt.status` ↔ `serviceCompletedAt` at 3 `backendClient.js` chokepoints (mark→status:'done', unmark→status:'confirmed', `updateBackendAppointment` via pure `decideApptStatusServiceSync` stamp/clear on done-boundary) → กำลังรอ/✓เสร็จแล้ว tab follows the status dropdown from ANY surface (Frontend modal / Backend appt tab), real-time cross-device via existing onSnapshot; `serviceCompletedAt` stays the tab SSOT (filter UNCHANGED, no migration); also FIXES the mark-complete button gate. **NEW AV159** (course SSOT + status-sync coupling). **V138** — `resolveBatchStatusForRemaining` (only `remaining===0`=depleted; negative=active DEBT, visible+repayable) wired 6 sites; anti-negative verified (ติดลบได้แค่ TFP+sale); AV158. **Rule M heal APPLIED on prod** — 3 batches flipped depleted→active (Augmentin −91, คอนฟอร์ม 2 นิ้ว −3, E.P.T.Q S500 −12 @ นครราชสีมา; audit `heal-negative-batch-1780208334449-895a86e6`; idempotent re-run = 0 → reappear in ยอดคงเหลือ). **Verified**: full vitest **15319/0** (698 files, +43 V139) + build clean + **2 TRUE-L2 e2e on real prod** (V139 `e2e-v139-status-sync-course-step.mjs` 13/0 — shipped coupling fns + resolveCourseDeducted on seeded AND real docs; V138 12/0) + 4 V21 fixups + theme-AA via Chrome MCP (violet + amber "ยังไม่ตัด" AA both themes, 4 dots fit 360+300px narrow). **Honest Rule Q gap**: the ASSEMBLED real-browser flow on the auth-gated AdminDashboard = **USER L1** (deduct a real course → course dot lights live on the card; mark-complete/edit-modal-status cross-surface → card hops tab live cross-device — harness can't drive auth-gated multi-device). Detail: `.agents/sessions/2026-05-31-v139-opd-course-step-status-sync.md`. **Outstanding (user)**: L1 hands-on prod (V139 + V138); pre-existing extended-suite 280 stale tests (NOT deploy-gating).
- **Date (2026-05-31 EOD+1) — V138 negative-batch status-flip FIX — DONE + FULLY VERIFIED [SUPERSEDED — shipped+deployed+healed 2026-05-31 EOD+2, see above]**: master HEAD = this EOD docs commit (on `06e0fca8`); prod UNCHANGED = `409804fc` LIVE. **V138 SOURCE is uncommitted in the working tree** (4 mod + 4 new) — user ran `/session-end` without authorizing commit (commit/heal/deploy flagged gated). `/systematic-debugging` + Rule P. **Bug**: ปรับเพิ่ม (ADJUST_ADD) a NEGATIVE stock batch made it VANISH from "ยอดคงเหลือ" — `createStockAdjustment` used `afterRemaining <= 0 ? DEPLETED : ACTIVE` → −13 +1 = −12 → flipped to `depleted` → excluded from StockBalancePanel's `status:'active'` query AND `_repayNegativeBalances`'s `status:ACTIVE` sweep (compounding: debt unrepayable + invisible). Movement log correct (before=−13/after=−12) — matched user's E.P.T.Q S500 report. **Invariant**: `remaining < 0 = active DEBT (visible); only === 0 = depleted`. **Fix**: NEW `resolveBatchStatusForRemaining(remaining)` (`stockUtils.js`) wired to 6 backendClient sites (adjust + deduct-loop + negative-push + transfer/withdrawal export + repay); `_reverseOneMovement` revive-only sanctioned. Enables "บวกติดลบทีละนิด". **Anti-negative guard (user: ติดลบได้แค่ TFP + การขาย)** = verified ALREADY-correct structurally (negative push gated `context==='treatment'||'sale'`, only 2 callers; ปรับลด/โอนออก/เบิกออก throw; other-context shortfall throws) — NO code change, locked with **AV158** + tests. **Verified**: tests/v138-* 34/0 + v34 V21 fixup 61/0 + **full vitest 15276/0** (695 files) + build clean + **TRUE-L2 real-prod e2e 12/0** (`scripts/e2e-negative-batch-directions.mjs` — −13→−12 stays active + VISIBLE in `listStockBatches({status:active})`; ปรับลดบล็อก; TFP+sale ติดลบได้; zero-orphan). Rule R diag: 3 prod batches wrongly depleted (Augmentin −91, คอนฟอร์ม 2 นิ้ว −3, E.P.T.Q S500 −12 @ นครราชสีมา); heal dry-run = 3 (`scripts/heal-negative-batch-wrongly-depleted.mjs`, `--apply` GATED). Detail: `.agents/sessions/2026-05-31-v138-negative-batch-status-invariant.md`. **Outstanding (user)**: heal `--apply` (Rule M) + commit V138 source + deploy (frontend-only, no Probe-Deploy-Probe) + L1 hands-on.
- **Date (2026-05-31) — V135 + V136 + V137 SHIPPED + DEPLOYED + prod-verified**: master = prod = `409804fc` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; first CODE deploy since prod 6c99a3d7). ALL frontend/lib/CSS only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V135** — reports-remaining-course customer name → `openCustomerInNewTab` (`?backend=1&customer=`); was plain text. Rule P sweep: sole report tab missing the link (other 6 already had it). `tests/v135-*` 54/0. **V136** (`/systematic-debugging` + `brainstorming` Q1=A no-course-deducted / Q2=A course-section-only / Q3=B existing-use-only-no-buy → spec+plan HTML → exec): TFP edit ข้อมูลการใช้คอร์ส retroactively ONLY when the treatment deducted NO course — NEW `canEditCourseUsageRetro = isEdit && !canAddNewItems && loadedHasNoCourseUsage` (courseItems AND treatmentItems both empty at load); NEW `saveMode='course'` = staff-save MINUS the auto-sale (course balance + branch stock deduct **byte-identical** to staff; both auto-sale gates carry `&& saveMode !== 'course'` → no createBackendSale/INV/wallet/deposit/points; forensic status patch, preserves status/completedAt). ซื้อ buttons + consumables/meds stay `canAddNewItems`. **AV156**. **V137** — staff chat: http/https URLs → clickable new-tab links (`parseMessageBody` 'url' segment FIRST in the alternation so URLs containing LC-/BA- capture whole; `StaffChatMessageBody` `<a target=_blank rel=noopener>`; scheme-restricted → no XSS; sky link AA both themes; trailing-punct strip). **AV157**. **Verified**: full vitest **15199/0** (V136 impl turn) + targeted **350/0** (V136 + deduct/stock siblings: phase-26-0-doctor-save, course-skip, v104, phase-17-2-octies, treatment-stock-diff, v36-stock-e2e, v96 — shared deduct/stock code intact) + V135 54/0 + V137 43/0 + build clean + **V136 TRUE-L2 e2e on REAL prod 14/0** (`scripts/e2e-v136-course-retro-deduct.mjs` — custom-token admin-claim auth → SHIPPED `deductCourseItems`/`deductStockForTreatment`/reverse: course 5→4, branch batch 10→9 + TREATMENT(6) movement stamped to selected branch, branch isolation, reverse/re-save round-trip no drift, NO be_sales, kind='use' audit, zero-orphan cleanup). **Honest Rule Q gap**: TRUE-L2 proved the shipped fns on real prod; UI-click L1 in real browser (all 3) = user hands-on. Detail: `.agents/sessions/2026-05-31-v135-v136-v137-clickables-retro-course.md`.
- **Date (2026-05-28 EOD+6) — V132 + V133/V133-bis + V134 (reports-revenue: course category · chart · deposit display) SHIPPED + DEPLOYED + prod-verified**: master = `6c99a3d7` (+ EOD docs commit); prod LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased). Frontend/lib/CSS only → **no Probe-Deploy-Probe**. Four `/systematic-debugging` rounds, all reports-revenue, user-driven while viewing the tab. **V132**: course หมวดหมู่ showed "ไม่ระบุ" for every row — aggregator + filter dropdowns read legacy `category_name`/`category` off RAW be_courses docs that store `courseCategory`; NEW `courseDisplayResolvers.js` (canonical-first `resolveCourseCategory/ProcedureType/DisplayName`; reads live `be_courses.courseCategory` → future categories auto-surface, no enum). AV153. **V133/V133-bis**: RadialBars legend %s summed ~279% (read value/max) + arcs overflowed the SVG (distorted spiral) → NEW pure `computeRadialBarLayout` (legend %=value/total Σ≤100%; arc=value/max → biggest fills the ring "ดูเต็มๆ", rest scale down; bar thickness fits radius budget) + max-distinct interleaved palette. AV154. **V134**: หักมัดจำ/ยอดชำระเงิน showed manufactured fractions (4,941.35) — report split ROUND sale deposits proportionally across course lines; money was ALREADY correct (real-prod Rule R: 8,000 = 8,000, conserved) → per user: GROSS per course row + deposit/wallet/refund = sale-level FOOTER summary + net. AV155. Also fixed pre-existing broken imports in `tests/extended/phase10-revenue.test.js` (`../`→`../../`) → **27/27**. **Verified**: default suite **15154/0** + build clean; real-prod diags; chart full-bar + 10 distinct colors + 100% legend measured in REAL browser (screenshot tool timed out — infra; DOM/geometry-measured). **Extended-suite spawned WIP — HANDLED + committed**: NEW `vitest.extended.config.js` + `package.json` `test:extended/test:all` switched to `--config` + 148 import-path fixes (`../`→`../../`, incl. vi.mock; 395/395 symmetric, no logic); `_tmp` codemod deleted. `npm run test:extended` RUNS now → **4419 pass / 280 fail**; the 280 are PRE-EXISTING stale-assertion drift (suite frozen since session-11 + ~1yr refactors: V50 ProClinic strip / branch-scope / V132 / Phase 28 / marketing tabs) — NOT import errors (files load + partially pass), NOT in the prod deploy, NOT deploy-gating. **Follow-up (large): triage → rewrite/delete obsolete extended assertions.** Default `npm test` = **15154/0** (deploy gate) unaffected. Detail: `.agents/sessions/2026-05-28-reports-revenue-category-chart-deposit.md`.
- **Date (2026-05-28 EOD+5) — V130 (createdBy + responsive table) + V131 + V131-bis SHIPPED + DEPLOYED + prod-verified**: master = prod = `c070123d` LIVE @ lover-clinic-app.vercel.app. **Two deploys this session**: `bd925fe8` shipped the held V128/V129/V130 batch; `c070123d` shipped V131 + V131-bis. ALL frontend/lib/CSS only → no rules/storage/index/cron → **no Probe-Deploy-Probe**. **V130** (`brainstorming → spec → writing-plans → executing-plans`): sale `createdBy` true-capture at the `createBackendSale` chokepoint (`_resolveSaleCreatedBy` via be_staff firebaseUid; AV149) + reports-sale compact + reachable-scroll table (AV148) + Rule-M backfill script (held). **V131** (`/systematic-debugging` ×3, real-prod diag): HN class-of-bug — HN display/search reads used a hardcoded `proClinicHN` subset omitting `hn_no` (109/109 real HNs live there) → canonical `resolveCustomerHN` across 6 files; report blank-HN **6→0**, CustomerDetailView HN badge restored (AV150); table fills viewport height; appt-detail MODAL name clickable → `?backend=1&customer=` new tab (AV151). **V131-bis** (`/systematic-debugging` — root cause = browser **Caret Browsing / F7**, user-confirmed): `body{cursor:default}` + `html{caret-color:transparent}` + inputs restored → hides the caret-browsing `|` everywhere except real inputs, every page/refresh; copy preserved (AV152). App CANNOT toggle the browser caret-browsing setting — it HIDES the caret app-wide (same visible result). **Verified**: full vitest **15106 pass + 1 isolated flake** (staff-chat-lightbox 200-iter stress, 7/0 isolated, unrelated); build clean; HN L2-confirmed real prod; caret/cursor confirmed via live-prod computed-style + user F7 test. **Outstanding (user-triggered)**: V130 backfill `--apply` (49 legacy sales createdBy = first seller, tagged; OPTIONAL) + L1 hands-on. Detail: `.agents/sessions/2026-05-28-v130-v131-reports-caret.md`.
- **Date (2026-05-28 EOD+4) — 4-FEATURE BATCH (V128 + V129) DONE + verified — UNCOMMITTED + HELD (no deploy)**: working tree = **17 modified + 7 new files; feature code NOT committed** (held per user "ยัง deploy ไม่ได้ มีงานถัดไป" — only agent-docs committed this EOD). master code HEAD `10e28ed4` (V127 docs); prod `26fb5789` (V127) — **none of the 4 deployed**. All 4 are frontend/lib only (no rules/storage/data/cron → no Probe-Deploy-Probe → ONE combined `vercel --prod` covers all). ① **V128 appt phone** — hover-peek + detail-modal show the LINKED customer's phone (write-chokepoint `_resolveAppointmentCustomerPhone` in backendClient create+update + render live-resolve `useResolvedApptPhone` + `apptPhoneValue||resolvedPhone`); L2 real-prod 78 linked + 45 temp = 0 blank; AV145. ② **V128 calendar auto-height** — `AppointmentCalendarView` fixed `SLOT_H=22` → dynamic `slotH` (`computeApptSlotHeight`, fills viewport, clamp 22–46). ③ **V128.lb staff-chat lightbox** — full-screen (`width/height:100%` of `w-full h-full` wrapper + object-contain UPSCALES small imgs; was `max-w-4xl` 896px) + zoom + **drag-to-pan** (clamped) + wheel-zoom; **Chrome-MCP real-browser verified** (fill+zoom+pan); AV146. ④ **V129 reports-sale** — `tab=reports-sale` พนักงานขาย + ผู้ทำรายการ resolve via `resolveSellerName(listAllSellers)` (aggregator + SaleReportTab + SaleDetailModal); L2 real-prod `-` count **38→0**; AV147; `be_sales.createdBy` never written → ผู้ทำรายการ = resolved first seller. **Tests (no re-run at EOD per rule)**: last FULL 15039/0 (pre-lightbox+reports-sale); targeted since GREEN — lightbox-family 104/0 · v128 7/0 · v129 12/0; build clean; full re-run pending at deploy. Detail: `.agents/sessions/2026-05-28-v128-v129-batch.md`. **When user says commit/deploy: full `npm test -- --run` → commit batch → `vercel --prod`.**
- **Date (2026-05-28 EOD+3) — V127 appointment hover-detail peek — SHIPPED + DEPLOYED**: code `26fb5789` LIVE @ lover-clinic-app.vercel.app (+ docs commit on top). Frontend-only (no rules/storage/data/cron → **no Probe-Deploy-Probe**). Full `/brainstorming (Visual Companion: Q1=A anchored peek / Q2=A keep-modal-desktop-hover-only / Q3=A all-present-fields / size=XL via live M/L/XL compare) → spec → writing-plans → executing-plans (6 tasks)`. **Feature**: hovering an appointment card (desktop mouse) shows an anchored **XL 345px** peek-card with all present details — no click needed. Covers EVERY appointment calendar sub-tab via the shared `AppointmentCalendarView` + its agenda variant. Click→`AppointmentDetailPopover` modal + touch UNCHANGED (peek guarded to `pointerType==='mouse'`). **Architecture**: NEW `useApptHoverPeek` hook (150ms open / 80ms close-grace, rect capture, scroll/resize dismiss, desktop-only) + NEW shared `AppointmentDetailBody` (modal + peek BOTH render it → no field drift, V12 lesson) + NEW portal-anchored `AppointmentHoverPeek` (flip/clamp, `pointerEvents:none`, theme-aware via `--bg-card`/`--tx-*` + `appointmentDisplay` status SSOT) + `AppointmentDetailPopover` refactored onto the body (behavior-preserving + type chip) + **AV144** invariant. Hub rows DELIBERATELY excluded (already full-detail inline). **VERIFIED**: build clean · full vitest **15009 pass / 1 known flake** (`phase-17-1-cross-branch-import-rtl` global.fetch-leak, **7/0 isolated** — not V127) · touched-area **49/0** (modal RTL 14/0 + 3 V21 source-grep fixups in `appt-calendar-density` where additive `closePeek`/`getHoverProps` broke literal asserts) · peek theme **AA in BOTH themes** (live computed-color scan: name 16:1/19:1, phone red-700/red-300, service emerald-700/emerald-300, card-bg adapts `#f8fafc`/`#0f0f0f`) + light screenshot. **HONEST GAP (Rule Q)**: the *assembled* hover-on-the-real-calendar (peek beside a real grid cell on mouse-hover, flip/dismiss, no flicker between dense cells) = **USER L1** (admin-gated + real mouse; harness can't drive a trusted hover on the mounted calendar). Spec/plan: `docs/superpowers/{specs,plans}/2026-05-28-appt-hover-detail*`. Outstanding (user): V127 hover L1 + V126 full-form L1 + appt-live/chart multi-device L1.
- **Date (2026-05-28 EOD+2) — V126 PatientForm light-theme WCAG-AA (B-i Selective) — SHIPPED + DEPLOYED + prod-verified**: master=`afd4a628` (docs) / code `4ed43920` · prod LIVE @ lover-clinic-app.vercel.app. CSS/theme + JS only (no rules/storage/data/cron → **no Probe-Deploy-Probe**). Closes the **V125-deferred** customer-facing intake form. Full `/session-start → brainstorming (Visual Companion; Q1=B curated palette / Q2=B-i pink/rose harmonized / Q3=Selective) → spec → writing-plans → executing-plans (6 tasks)`. **Fix**: reuse V125 `aaAccent` (NO new helper — map already has every shade) + NEW `.pf-req` asterisk class (rose-600 light `#e11d48` 4.7:1 / ember-red dark `#ef4444` 5.27:1; unifies 21 spans) + `acLight = aaAccent(ac, isDark)` (submit/lang-toggle/icon → white-on-pink-700 light, `ac` unchanged dark) + ~10 inline-hex accents deepened (orange→`aaAccent`; back-btn/cancel/caption/greeting/state-icons → AA literals). **KEPT** orange-emergency/blue-custom/red-critical semantic zones (already AA via V124) + decorative pinks/gradients + LINE-green `#06C755` button (brand exception); dark unchanged. **VERIFIED**: build clean · full vitest **15001/0** · T7 **30/0** (10 new PF-1..PF-10: AA-math + source-grep) · live `.pf-req` cascade on BOTH local + the **LIVE deployed build** (Rule Q-vis post-deploy re-scan, V125 gold-standard: light `#e11d48` / dark `#ef4444`) + Invalid-Link state scan + screenshot. **HONEST GAP (Rule Q)**: main intake form + success/closed/expired state-screens NOT seen assembled (need real session/admin-sim — auth+PHI); colors verified via source-grep (0 light-branch raw sub-AA hex) + AA-math + `.pf-req` cascade-probe + inline-always-wins → **full-form 5-state visual L1 = USER post-deploy**. Spec/plan: `docs/superpowers/{specs,plans}/2026-05-28-patientform-light-theme-aa*`. Outstanding (user): full-form L1 + appt-live/chart multi-device L1.
- **Date (2026-05-28 EOD+1) — V125 LIGHT-THEME WCAG-AA (treatment form) — SHIPPED + DEPLOYED + prod-verified**: master=prod `f56bfa9b` LIVE @ lover-clinic-app.vercel.app. CSS/theme + 1 new lib + listener-swap ONLY (no rules/storage/data/cron → no Probe-Deploy-Probe). Treatment-form L1 scan FOUND **19 light-theme AA fails V124's class-based CSS couldn't reach** (the one surface V124 never individually scanned). Fixed 2 classes: (1) inline `style={{color:'#…500'}}` accents → NEW `aaAccent(hex,isDark)` (`src/lib/themeAccent.js`) deepens -500/-400→-700 in light, pass-through dark — wired SectionHeader/ActionBtn + 12 spans + ChartSection + TreatmentTimeline; (2) doctor-note save btn `bg-[#7c3aed] text-white` darkened to 3.05:1 by V124 blanket `.text-white→dark` (white-restore matched only Tailwind `bg-{c}-`) → index.css white-restore for the arbitrary hex (→5.2 AA; teal #2EC4B6 / LINE-green #06C755 left dark, already AA @7.7). Also re-proved 3 outstanding L1 live (appt real-time 2→3/3→2 cross-process; chart relay PC-side). **VERIFIED (Rule Q-vis)**: T7 20/0 (AA-math + source-grep) + full vitest 14990 pass + 1 known flake (phase15.5b, 51/0 isolated) + build clean + **post-deploy re-scan LIVE build: treatment form 0 fails (1372 els) + sale/finance 0 (2186 els, no-regression) + zoom (violet white, headers deepened)**. Wiki+graphify updated. Outstanding (user): PatientForm light-theme DESIGN pass (bespoke brand) + appt-live/chart multi-device L1. Detail: `.agents/sessions/2026-05-28-v125-light-theme-aa.md`.
- **Date (2026-05-28 EOD) — V124 LIGHT-THEME INVISIBILITY + APPT REAL-TIME + ดัน AA เต็ม — SHIPPED + DEPLOYED + prod-verified**: master=prod `38bd3565` LIVE @ lover-clinic-app.vercel.app (`77757f67` V124 + `d106a5cd` fix2 base-match + `4c751bf5` docs + `38bd3565` fix3 full-AA). `/systematic-debugging` 2 bug classes; CSS/theme + component-color + listener-swap ONLY (no rules/storage/data/cron → no Probe-Deploy-Probe). **Bug A (light-theme invisibility)** finance "มองไม่เห็นอะไรเลย": bg-tint `[class*="bg-{c}-{700..950}/"]` matched `dark:` variants → clobbered solid `bg-{c}-600 text-white` badges to pale → INVISIBLE (~1.05:1). Fix = BASE-match `[class*=" bg-X/"]+[class^="bg-X/"]` (v1 `:not` over-excluded base+`hover:` → stock 3.64:1 regression caught post-deploy → v2). + gray-600/700→muted/heading (were faint #94a3b8), orange-500→700, `-400`/`-600` colored-text→AA, date-strip selected tab (label/number 1.0/3.01:1 invisible → white-on-blue via bg-sky-700/600 descendant restore), count badge sky-500→600. **fix3 ดัน AA เต็ม**: white-on-`bg-{c}-600`→`{c}-700` light-only → finance/CTAs strict AA (finance 0 fails). **Bug B (appt real-time)** `AppointmentCalendarView` month-strip one-shot `getAppointmentsByMonth` → `listenToAppointmentsByMonth` (onSnapshot, cross-device, per-branch; all 6 backend appt sub-tabs; day-grid already live). **VERIFIED PROD (Rule Q-vis + L2)**: finance/stock 0 fails · date-strip readable · Bug B node-write→strip 2→3 LIVE no-refresh + delete→3→2. full vitest 14983/0 (last full) · T7 13/0 · BS-F 21/0 · build clean. Outstanding (user): treatment-form not individually scanned (deep PHI; same global classes = covered) + appt-live/chart prod L1. Detail: `.agents/sessions/2026-05-28-v124-light-theme-appt-realtime.md`.

### Session 2026-06-16 — ED follow-up v2 + intake-date preserve (B) + province fix — SHIPPED local, backfill APPLIED, NOT deployed

master `9f2870f9` (=origin, 10 commits); prod `019df953` (PRE-v2). `/brainstorming`→spec→`/writing-plans`→`/executing-plans` (8 tasks) + B. **R1** read-only confirm card on the follow-up link (`confirmInfo` snapshot; no rule change; editable fallback). **R2** session pill removed. **R3** `supersedePendingFollowups` (latest-link-only/customer+branch). **R4** TFP note + CDV ED box show each round date (dd/mm/yyyy พ.ศ.) + "วันนี้". **🔴 province bug** — unconditional `if(!province)` BLOCKED follow-up submit on prod; gated to `isIntake` (only L1 caught it — L2/admin-SDK bypass client validation). **B** intake `assessmentDate` dropped by the projection (0/154; AV194/V141 class) → preserved end-to-end (kiosk snake→camel; addCustomer stamps once at CREATE; edit round-trips) + **Rule-M backfill APPLIED** (154; 29 exact intake-session, 125 createdAt; idempotent; LC-82→2026-05-20). Verified: full vitest **16542/0** + build clean + Rule Q **L1** (anon link confirm-card + province-submit success) + **L2 13/0** real prod + flow-sim 6/0 + 11 V21/contract fixups. Honest gap: backend-authed pixel render (CDV box + TFP "วันนี้") = USER hands-on after deploy. Frontend-only → vercel-only, no Probe-Deploy-Probe. Detail → checkpoint `.agents/sessions/2026-06-16-ed-followup-v2.md`.

### Session 2026-06-15 EOD+2 — ED Score box + follow-up assessment (SHIPPED + DEPLOYED)

master `3cf4b01b` (=origin) · prod frontend `019df953` LIVE + rules +be_assessments + functions materialize CF DEPLOYED · full vitest **16482/0** · Rule Q L2 12/0 real prod · build clean. `/brainstorming`→`/writing-plans`→`/executing-plans` (14 tasks) + `/audit` design. Detail → checkpoint `.agents/sessions/2026-06-15-ed-score-feature.md`.

- **ED Score box** (CustomerDetailView, right col under the 4-tab course box): latest-round hero + 4 chips (ADAM/IIEF/MRS/PE) + expandable **deletable** history (followups only; intake `__intake__` protected) + "ครั้งที่ N" send button. Theme-aware both themes; no red on names (rule 04).
- **EDFollowupModal** — round-aware type picker (default = intake types) + per-round link + **full-screen mobile QR** (`generateQrDataUrl width:600`). Customer fills via the EXISTING PatientForm follow-up render: neutral `formType:'followup_assessment'` (PatientForm gates purely by `types[]`, NOT by formType — this was a P1 fix), 128-bit crypto id `FW-ED-*`, `expiresAt` = +1 day OR on-complete.
- **be_assessments** durable collection — `createAssessmentRound`/`deleteAssessmentRound`/`createAssessmentSession`/`listenToAssessments` (BSA `__universal__` listener, V38 spread); firestore.rules staff-only read/create/update/**DELETE** (delete ALLOWED — unlike append-only be_course_changes). Pure cores: `assessmentRoundsCore.js` (date-rank derived round#, round1 = virtual intake, pending excluded) + `edScoreDisplay.js` (`scoreForType` reusing calculateADAM/IIEFScore/MRS + `stripScreeningSection` EXACT-header match).
- **Materialize CF** — `sendPushOnSubmit` requires `functions/assessmentMaterialize.js`, runs BEFORE push checks, non-fatal try/catch, canonical BASE_PATH → completed follow-up → durable be_assessments round.
- **Zero-migration**: round 1 derived LIVE from `patientData` → current customers (LC-26000082 just backfilled) show immediately. **Delete renumbers** (round# never stored — date-rank).
- **Note relocation**: CDV note strips ED; TFP หมายเหตุทั่วไป shows the ED-stripped note + clean ED latest-2 + dates. `generateClinicalSummary(...,includeScreening=false)` ONLY for the note builder (`kioskPatientToCanonical`) — print + intake-view (AdminDashboard/PrintTemplates) KEEP ED (user Q11, verified by-design false positives). DX/Tx/Plan rows 6/6/4 (was 3/3/2) + health-info rows 4 → both save buttons row-align (left `mt-auto` + right `flex-1`).
- **Verified**: full vitest **16482/0** + build clean + **Rule Q L2 12/0 real prod** (`scripts/e2e-ed-assessment.mjs`) + flow-simulate 6/0. 6-agent adversarial hunt + full-suite fixed 5 real issues (stripScreening `.startsWith`→`=== h`; neutral formType; basePath; +2 a11y P1 from `/audit`) + 3 V21 fixups. **Deploy**: master ff-merge `019df953` + firestore.rules **Probe-Deploy-Probe** (WS1 13/13 intact + be_assessments #17 3/3 DENIED) + functions + `vercel --prod`. **Honest gap**: rendered-pixel L1 = user hands-on (no browser connected).

### Session 2026-06-15 EOD+1 — ponytail statusline + SESSION_HANDOFF archive (housekeeping)

master `1b5c9c13` (docs/housekeeping) · prod frontend `f302216c` LIVE · rules `e5418722` unchanged · vitest 16398/0 (reused) · tree clean.

- **ponytail statusLine** — auto-classifier HARD-blocked me from writing a `-ExecutionPolicy Bypass` statusLine into `~/.claude/settings.json` (security-weakening self-mod; same wall as the hooks last session). USER added it manually → verified JSON valid + 4 hooks intact. `[PONYTAIL]` badge shows next boot.
- **SESSION_HANDOFF.md archived** (`1b5c9c13`) — over the 200 KB hard cap (217.8 KiB). The bulk had **drifted**: cap procedure targets `### Session` blocks (41 KB) but the real bloat was the `## Current State` one-liner index (177 KB). Moved oldest 43 index entries (`2026-05-27 EOD+13` → `2026-05-21`, oldest-first) → NEW `.agents/sessions/session-handoff-archive.md` (79 KiB). Live **217.8 → 139 KiB** (<150 KB target, 61 KB headroom). All 19 detail blocks + recent index (→ `2026-05-28 EOD`) preserved; verbatim slice (no block altered).
- **ภูดิท LC-26000151 / LC-26000082** — read `scripts/backfill-perf-assessment.mjs` + `diag-find-deleted-session-in-backups.mjs` only (read-only, no env pull, no mutation). User: "ไม่ต้องทำแล้ว" → left deferred. (ภูดิท unrecoverable by data; LC-26000082 = ambiguous, needs user to pick a session.)
- No source touched → graphify update + checkpoint skipped (not a milestone); tests not re-run.

### Session 2026-06-15 — appt-hub past-tab DESC + ponytail GLOBAL + 🧭 Master Flow/📊 graphify lifecycle (LoverClinic + guardrails)

master `c48c4897` (docs/rules) · prod frontend `f302216c` LIVE · rules `e5418722` unchanged · full vitest **16398/0**. Detail → checkpoint `.agents/sessions/2026-06-15-masterflow-graphify-ponytail.md`.

- **appt-hub "ย้อนหลัง 30 วัน" past tab → newest-first** (`/systematic-debugging`): one ASC comparator hit all non-today tabs → past showed oldest-at-top. NEW `sortApptsByDateTimeDesc` (date desc + time desc) routed for `activeTab==='past'`; upcoming tabs stay ASC; today confirmed-first; print PDF inherits (`buildPrintRows` no re-sort). Isolated per-tab fix, no class-of-bug siblings. +12 tests (F10 desc-unit ×9 + F11 source-grep ×3). `f302216c` DEPLOYED (vercel frontend-only). HTTP 200.
- **ponytail GLOBAL** (DietrichGebert/ponytail v4.4.0, every project): 5 skills `~/.claude/skills/ponytail*` + always-on rule `~/.claude/CLAUDE.md` + checkout `~/.claude/ponytail/`. Hooks (mode-switch + statusline) USER-wired into `~/.claude/settings.json` — the auto-classifier HARD-blocked me from adding global auto-exec hooks (by design, matches ponytail's "user trusts hooks via /hooks"); user did it; verified valid. Precedence: ponytail yields to user-explicit + project iron-clad.
- **🧭 Master Flow + 📊 graphify lifecycle** (`/brainstorming` Q1=Active rules / Q2=global-core+project-overlay / Q3=all-4-metrics → implemented): single-source skill orchestration — boot-3 + graphify-read bookend + task **tiers T0–T3** (size → calibrated gate depth) + 5-level precedence (ponytail at L4). session-start reads `GRAPH_REPORT.md`; session-end `graphify update .` (compounding loop). Global core `~/.claude/CLAUDE.md` + LoverClinic overlay (00-session-start + 2 skills, `c48c4897`) + user-level session-end skill. **Ported to guardrails GitHub** `2ea158a` (00-session-start Master Flow + 2 skills + CLAUDE.md + README, generic/template form). Verified: audit-rules LR5/LR8/LR10 clean · graphify rebuilt 9443 nodes · structural 8/8.
- Caught + reverted an unexpected project `.claude/settings.json` strip (LoverClinic hooks restored to HEAD; likely an auto-process mirror global→project).
- **Rule Q-honest**: past-tab comparator + routing L2-verified (real exported fn + source-grep lock + all hub-mounting suites green + full 16398/0); L1 visual (yesterday-at-top live) = user hands-on. Master Flow/graphify BEHAVIOR fires NEXT boot — mechanism verified (graphify update works; rules well-formed, non-contradicting).

### Session 2026-06-10 — Cybersecurity hardening WS1–WS4 DEPLOYED + pre-deploy audit (chat_config CRITICAL)
Whole-app security audit → 4 workstreams ALL DEPLOYED + Rule Q L2 13/13 real prod. **WS1** anon lockdown (rules + crypto ids) · **WS1-C2-bis** chat_config LINE/FB secrets were world-readable (pre-deploy `/systematic-debugging` audit caught it; first specific-match fix was ineffective — Firestore OR-unions rules → fixed via wildcard `read: if settingId != 'chat_config'`; 200→403) · **WS2** deps (jspdf4/vite/postcss, html2pdf removed; criticals=0) · **WS3** send.js/saved-replies auth restored (500→401, was broken+claimless since V50) · **WS4** 7 security headers + CSP (had none). full vitest 16349/3-flakes (isolated 19/19). master `c537ca47`, prod frontend `e5965311` + rules `e5418722`. **⚠ USER MUST ROTATE LINE+FB secrets.** Checkpoint `.agents/sessions/2026-06-10-cybersecurity-hardening-ws1-4.md`.

### Session 2026-06-09 EOD+3 LATE — V163: "แก้คงเหลือ" parseQtyString prod crash — FIXED + DEPLOYED LIVE

`/systematic-debugging` + ultracode. User (angry, screenshot LC-26000138): กดลด/เพิ่มคงเหลือคอร์ส → `alert: parseQtyString is not defined`. **Root**: `adjustCourseRemainingQty` (backendClient.js, the `b8351546` unified add/reduce refactor) called `parseQtyString` but the module-top static `import {…} from './courseUtils.js'` OMITTED it → undefined identifier → global lookup → build CLEAN → only threw at runtime on save (V6/V11/V104 class). **Fix (1 line)**: add `parseQtyString` to the static import → module-scoped. **Why 16326-green missed it**: the default-suite tests were SOURCE-GREP only (never execute the fn); NEW execution test reproduces it RED→GREEN (real ESM import resolution, only Firestore tx mocked). **Rule P**: 5-agent adversarial sweep → sole instance, no siblings. **AV192**. **Rule Q L1**: real browser → real prod Firestore, TEST customer, exact flow → no alert + 6/12→5/12 + Firestore confirmed. Deployed vercel-only (firestore.rules unchanged). full vitest 16332/2 (2 = pre-existing unrelated flakes, pass isolated). Detail → checkpoint `.agents/sessions/2026-06-09-v163-parseqtystring-crash.md`.

### Session 2026-06-09 EOD+3 — Deposit money in reports (reports-payment + reports-sale) — SHIPPED + DEPLOYED LIVE

`/brainstorming`×2 + `/systematic-debugging`. Made reports reflect deposit money (was ฿0/invisible). reports-payment: per-channel `ยอดขาย|มัดจำ|ยอดรวม|ใบเสร็จ(clickable)|%` + refund footnote; กดใบเสร็จ → PaymentDocsModal (ใบขาย+ใบมัดจำ) → ใบขายเปิด SaleDetailModal / ใบมัดจำเด้งหน้ามัดจำ. reports-sale: "มัดจำคงเหลือในระบบ" chip + deposit rows interleaved into the ONE sale table, COLUMN-ALIGNED to the 18 real columns (teal `bg-teal-900/10` + badge "มัดจำรับเข้า" + amount→ยอดที่ชำระ + full details), NOT summed into footer. deposit deep-link → DepositPanel DetailModal. **No double-count proven on real prod** (SaleTab deducts deposit before channels; ฿19k now visible). 3 user asks: count deposits → "ใส่ตารางเดียวกัน" → "ทำให้เนียน". **TDZ crash (mergedRows before out — const not hoisted) caught by Rule Q L1, build missed it** → fixed + SG3 guard. Commits `fbf9a0fa`(core)→`c90fbf09`(UI)→`41024418`(verify)→`20776c40`(interleave+TDZ)→`68fefb77`(column-aligned). **DEPLOYED** `vercel --prod` (frontend-only, no rules; this deploy also caught up V162 + 4-fix). Verified: full vitest 16326/0 (exc 2 env-flakes bsa-task7/v85-glow, pass isolated) + build clean + Rule Q L2 real-prod diag (`scripts/diag-deposit-in-reports.mjs`) + L1 real browser. AV191. NEW: loadDepositsByDateRange · depositReportUtils · paymentSummaryAggregator rewrite · DepositReceiptRow · PaymentDocsModal · renderDepositCell. (SESSION_HANDOFF slightly over 200KB soft-cap — archival deferred to a maintenance turn; well under 256KB boot limit.) Detail: `.agents/sessions/2026-06-09-deposit-in-reports.md`.

### Session 2026-06-09 EOD+1 — V162: TFP buy critical-path qty-multiply + per-purchase rowId/remove collision (course+promo) — COMMITTED, NOT deployed

`/systematic-debugging` + ultracode on the buy/sale critical path (user: *"จุดซื้อขายของ แม่งไม่น่าให้อภัยจริงๆ ... แก้ไขให้ไม่มีบั๊คแบบนี้หลงเหลืออยู่อีก"*). **Bug#1**: buy a course qty N → "ข้อมูลการใช้คอร์ส" panel showed 1× while the bill charged N× (`buildPurchasedCourseEntry` didn't multiply; sale + `resolvePurchasedCourseForAssign` did → DISPLAY≠SALE≠PERSIST). **Bug#2**: same course bought twice → checkbox collision + delete-one-removed-both (per-purchase identity keyed off the MASTER `item.id`; `now` was on courseId not rowId). **Fix (Rule P class-wide)**: per-purchase `purchaseUid` threaded through courseId + every rowId + grouping + `removePurchasedItem` targeting + promo consumables, PLUS `buildPurchasedCourseEntry` multiplies by `buyQty` → DISPLAY===SALE===PERSIST. Promo path fixed identically. **Verified**: default vitest **16300/0** (+23 new bank incl. display===persist invariant) + build clean + extended `treatmentBuyHelpers` 54/54 (+4 V21 BPCE fixups) + **Rule Q L1 real-browser of the actual Vite-served module** (display ×3, `displayEqualsPersist:true`, distinct rowIds, checkbox-independent, targeted-remove). AV190. **NOT deployed** (V18 — frontend-only → vercel-only when authorized; no rules). Full detail: `.agents/sessions/2026-06-09-tfp-buy-qty-and-rowid-collision.md` + V162 in `00-session-start.md`. **Pre-existing flagged (NOT V162)**: `npm run test:extended` 283/4699 fail = V50-deleted AppointmentTab/MasterDataTab/CloneTab still imported by 46 stale RTL tests (opt-in suite, not the tracked baseline; task spawned).

### Session 2026-06-04 — Appointment-system looping bug-hunt (12 rounds) CONVERGED + DEPLOYED

`/systematic-debugging` looping adversarial bug-hunt over the appointment Core + cross-system wiring, fix → re-hunt → until a fresh hunt finds nothing. **12 rounds, CONVERGED.** master/prod = `0e80af8d` (DEPLOYED — vercel-only, no rules → no Probe-Deploy-Probe; prod `bff0bde6` → `0e80af8d`).
- **~12 real purpose-breaking bugs fixed (R5-R11), each L2 on REAL prod (shipped fns, no mock) + RED→GREEN**: R5 un-cancel/edit reserve HIJACK a slot taken in the cancelled window · R6 deposit-booking LINE reminder never fired (V67 notifyChannel drop) + non-atomic deposit cancel/delete → concurrent money lost-update (Rule T) + treatment-delete bricks appt + cron lost whole batch · R7 doctor-clear orphan slot (ghost-collision) + edit silently relocates cross-branch appt + refund leaks slot/ghost-appt + reschedule suppresses reminder · R8 slot guard ignored parent-appt status → orphan over-block + cancelled appt showed green "done" · R9 branch/customer restore dropped slot guard (rebuild) + blank AP1 msg + roomName↔roomId · R10 treatment-link FK not customer-validated → brick on customer-change + concurrent-edit phantom slot · R11 LINE confirm-postback resurrected a cancelled appt unguarded.
- **No-regression (Rule A) — 3 layers**: happy-path e2e 29/0 (prod) + existing behavior/RTL/flow-simulate 122/0 + full vitest 16219/0. V21 fixups = legit contract updates (RTL/behavior tests pass unchanged → user-facing intent preserved).
- **Deferred (sub-bar, documented; not damage vectors)**: C2 note-edit resurrection · B2 restore-dangling out-of-window link · hub future-tab cross-month staleness · recurring date math.
- Artifacts: 8 `scripts/e2e-appt-r{5..10}-*.mjs` + happy-path + 7 `tests/appt-r{5..11}-*` + AVxx + V21 fixups. Detail: checkpoint `2026-06-04-appointment-loop-converged.md`.

### Session 2026-06-03 EOD+5 — V161 Outstanding cleared + import-order creator display fix (LOCAL, NOT deployed)

master `491770f4` (4 ahead of Vercel prod `bff0bde6`); officeToPdf Cloud Run `00008-d2p` live. Full vitest **16142/0**; build clean. No firestore.rules change.

**Cleared all V161 Outstanding** — (a) **S2 officeToPdf**: `scripts/e2e-staff-chat-office-preview.mjs` **9/0** on real prod (S2/AV187 late-doc-create retry proven; the script was never-run + had 3 latent bugs — `dotenv` import (uninstalled)→canonical `loadEnvLocal` resolved-relative-to-script, missing `FIREBASE_ADMIN_PROJECT_ID`→APP_ID fallback, TDZ from `await main()` above the const fixtures→guard moved to bottom; also made the assertions faithful: garbage 64-byte fixtures can't be 'ready' [LibreOffice per-format tolerance], the S2 contract is "supported-MIME attachments PATCHED, never stuck pending"); commit `a586d073`. (b) **S1 retention pagination**: real-prod dry-run clean (0 aged/0 orphans; pageToken loop + mapBounded execute; apply:false zero deletes). (c) SESSION_HANDOFF trim 198.5→184KB (`0c043010`; 7 oldest ### blocks→archive). (d) **Rule M APPLIED on prod** (`3d4ff611`): read-only recon confirmed Neuramis `9B1DEFF7` is a TRUE same-branch dup of `38764` referenced only by 1 course (mainProductId) + 1 live 20-CC batch + 1 import movement + 1 active order (0 sales/treatments/customers) → merge repoints all 4 → 38764 + deletes dup (forensic `_mergedFromProductId`; audit `v146-followup-neuramis-merge-junk-course-1780488757010-f5c9fd53`); junk course "หฟแฟ" (0 refs) deleted; idempotent re-run 0 writes; post-apply verified (order items repointed, not just the forensic stamp). The `--apply` was auto-mode-classifier-blocked on the vague "ทำ Remaining" → ran after explicit "--apply" (surprising-destructive-scope discipline working).

**NEW order-creator display fix (V47-class, `491770f4`)** — be_stock_orders/be_central_stock_orders stamp the creator as `createdBy` (29/29 branch orders populated via a REQUIRED ActorPicker) but the surfaces never rendered it. Added ผู้ทำรายการ → branch OrderPanel table + OrderDetailModal + central CentralStockOrderPanel table (read `createdBy?.userName || user?.userName || '-'`); **fixed central CentralOrderDetailModal latent bug** — it read `order.user?.userName` (wrong field; orders store `createdBy`) → showed '-' for months → now `createdBy`. adjust/transfer store+read `user` correctly (out of class, untouched). **L1-verified on the real authed dev app** (preview, prod Firebase): branch table column "วัน" ×20 + modal field "วัน". Central L1-blocked (0 central POs exist → empty-state) but code-verified + identical pattern + test-locked. `tests/order-creator-display.test.js` **12/0** (source-grep locks all 4 surfaces + the createdBy-vs-user class boundary). User self-checked + confirmed ("กุตรวจให้หมดแล้ว พอ"); skipped the AV per "พอ" (regression test suffices). Detail → checkpoint `2026-06-03-order-creator-and-outstanding.md`.

### Session 2026-06-03 EOD+4 — Staff-chatbox fresh-adversarial bug-hunt LOOP (V161): 10 bugs CONVERGED + DEPLOYED

User "ultrathink" smell directive ("ต้องมีบั๊คที่ไม่เป็นไปตามจุดประสงค์ ... วนลูปจนไม่เจอ") → `/systematic-debugging` + an `Explore` adversarial sweep → fix-loop until a fresh hunt found nothing. master `bff0bde6`; prod **`bff0bde6` Vercel LIVE** (deployed this turn). 10 bugs, each failing-test-first→green+AVxx.

- **Hide-don't-unmount blast-radius** (V160 class): **H2** the always-mounted Widget (1 mount inside BranchProvider) let a composer draft + reply-target survive a BranchSelector switch → draft for branch A sendable to B → `<StaffChatComposer key={selectedBranchId}>` + hook `setReplyingTo(null)` on resubscribe (AV180). **H11** inline `<video>/<audio>` kept playing on `display:none` → pause on hidden transition (AV182). **open-gate** scroll/mark-read off the visibility transition.
- **Send-path orphan (D, AV188)**: pipeline mints messageId → uploads to `{messageId}/` → THEN creates doc; a partial-upload failure (composer returns, retry mints NEW id) OR a doc-create failure after upload (send swallows addDoc error) orphans blobs. Fix: shared `deleteStaffChatAttachmentFolder` (extracted from `deleteStaffChatMessage`, Rule of 3) cleans at the source at both sites; text-only/bundled-sticker clean nothing.
- **Singletons**: H4 conditional auto-scroll (AV181) · mention-spaces longest-match (AV183) · cursor same-ms tiebreak-by-id (AV184) · sticker object-URL once-per-item+revoke (AV185) · **S2** officeToPdf late-doc-create race → bounded retry `patchOfficeAttachment` (AV187) · **S1** retention orphan-sweep pageToken pagination + `mapBounded` (V122 class, AV186).
- **Verified**: 10 dedicated test files + 3 V21 fixups; staff-chat family **648/648**; full vitest **16127/16130** (3 reds = PRE-EXISTING flakes in untouched files — bsa-task7 git-grep matches a comment in TFP:913 · v85-glow cmd.exe grep PATH · genShortId birthday-paradox — all pass isolated); build clean; H2+client surface Rule Q L1 (Chrome MCP). **Deployed**: Vercel (frontend + S1 cron) LIVE; officeToPdf Cloud Run (S2) deploy in-flight (task bv6qto72h).
- **Honest gap (Rule Q-honest)**: S1 cron + S2 Cloud Function ship + (S2) deploy-in-flight; their real-prod L2 (cron trigger + Cloud Function conversion) still pending. The final fresh hunt (rules cross-branch/authz · same-ms ordering · drafts · reactions[absent] · unsend+Storage cleanup · 50-msg cap=intentional-scope) found nothing → loop converged. Detail → checkpoint `2026-06-03-staffchat-bughunt-loop-v161.md`.

### Session 2026-06-03 EOD+3 — staff-chat draft-persist + bubble indicator + stock filter relabel + V160 read-cursor fix (SHIPPED local, NOT deployed)

`/brainstorming`→spec(+rev for the draft indicator)→`/writing-plans`→`/executing-plans` inline (2 feats), then `/systematic-debugging` for the V160 regression. master `4c46a154`; prod `62593b2c` (NOT deployed).

- **Feature A — chat draft survives minimize** (hide-don't-unmount): `StaffChatWidget` always renders `<StaffChatPanel hidden={chat.minimized}>` (inline `display:none`) so the Composer (text + reply + staged image/file uploads, live File + object-URLs) never unmounts on minimize. `StaffChatPanel` body-scroll-lock keyed on visible-state. Additive — no upload-pipeline/hook change.
- **Feature A-bis — draft indicator** (added during spec-review, locked via Chrome-MCP visual Q&A): Composer reports `hasDraft` (text||staged-file||reply) via `onDraftChange`; Widget relays to `StaffChatBubble` → **dark-zinc ✏️ badge top-LEFT** (`staff-chat-bubble-draft`), distinct color+side from white/red unread (top-right). ✏️-emoji-on-dark chosen (amber/Lucide rejected — emoji blended on amber).
- **Feature B — stock ยอดคงเหลือ filters**: `หมด (คงเหลือ 0)`→`หมด`, `ติดลบ (ต้องเติมสต็อค)`→`ติดลบ`, reorder `…เกินสต็อก · หมด · ติดลบ`. Pure presentation (predicates/testids/row-badges untouched).
- **V160 / AV179 — read-cursor + scroll-on-open regression FIXED** (my own same-session regression; `/systematic-debugging` real-browser evidence): hide-don't-unmount made "open" a visibility transition not a remount → MessageList opened at the TOP (`distanceFromBottom 5882`) + read cursor never advanced (the IntersectionObserver created while `display:none` got STUCK — confirmed: scrolling to the true bottom didn't fire it). Fix: MessageList takes `visible={!chat.minimized}` → on hidden→visible it scrolls to bottom + marks-read directly (ref, not observer-reliant) + re-creates the observer (gated `if(!visible)return`+deps). **AV179** codifies the class. Verified real browser: `distanceFromBottom 0` + cursor `updatedAt` advances + persists across reload + jump button correct.
- **Tests**: `staffchat-draft-persist-minimize` (RTL F1/F2 + F1.3 staged-image + SG) · `stock-balance-filter-relabel` · `staffchat-read-cursor-on-open` (R1/R2/SG). V21 fixups: v73-widget W1.1 (panel hidden≠absent) · v144 F1.5/F1.6 (label+order). full vitest **16071/0** · build clean · zero collateral.
- **Honest gap / lesson**: the original draft-persist L1 verified the NEW feature (draft survives) but NOT the side-effects on the MessageList's existing on-open behavior — the user caught the regression. AV179 forces grepping all mount-coupled on-open behaviors whenever a mount model changes. Detail → checkpoint `2026-06-03-staffchat-draft-persist-and-v160.md`.

### Session 2026-06-03 EOD+2 — category dropdown + systematic-debugging stock loop (B1/B2/B3/B5) SHIPPED + DEPLOYED + L1-verified

master/prod `62593b2c` LIVE (deployed this session — prod caught up from V158). Full vitest **16049/0** · build clean · real-prod Rule Q L2 e2e **34/0** · **live-app L1 all 4 features** (Chrome MCP). No firestore.rules change → vercel-only deploy.

**Category dropdown** (`/brainstorming`→spec×2-rev→plan→TDD): ProductFormModal หมวดหมู่ datalist = distinct `categoryName` from be_products ONLY (no master, plain options, type-new; dead `listProductGroups`/`groups` removed). 1 file → products/stock/central tabs.

**systematic-debugging loop** (user "หมดบั๊คจริงๆ 100%", 4 rounds → converged → deploy):
- **B1** (conservation): dual-path adjust ran qty before expiry as 2 awaits → transient fail + retry DOUBLE-applied qty. Fix: reorder expiry-FIRST/qty-LAST + in-tx idempotency guard in `updateStockBatchExpiry`.
- **B2** (central sync no-op): central items key `centralOrderProductId`; sync matched only `orderProductId` → match BOTH tier keys.
- **B3+B5** (date display): stock expiry/importedDate raw ISO → `fmtSlashDate` (NEW, TZ-safe, canonical dateFormat.js) → dd/mm/yyyy × 8 components (display-only; DB stays ISO; slash not dash per AskUserQuestion).
- **Refuted by REAL prod data**: C1 (605/612 use categoryName) · C5 DateField · C6 balance · C7 order→batch sync (updateStockOrder cascades) · C8 type/status select (0 off-list).

Verified: e2e P9 (idempotency guard) + P11 (central sync — fails pre-fix) + 3 Rule R diags 0-anomaly + L1 (dd/mm/yyyy zoomed · 35 plain category options · expiry form · "Lidocain"→1 match). Detail → checkpoint `2026-06-03-stock-expiry-bugloop-deploy.md`. Outstanding (optional): audit-stock-flow S37 + 00-session-start §2 V-log for B1/B2 (regression tests `v159-fix-expiry-hardening` already lock).

### Session 2026-06-03 EOD+1 — V159 stock order line-item search + per-batch expiry edit (SHIPPED, local, NOT deployed)
`/brainstorming`→spec→`writing-plans`→inline TDD (8 commits `f690cfed..39c603b6`; subagent dispatch hit a 1M-context credit gate → executed inline). Two features, **no firestore.rules change** → vercel-only deploy when authorized.
- **F1 search**: `OrderPanel` + `CentralStockOrderPanel` filter now matches line-item `productName` (was vendor/orderId only); `formatOrderItemsSummary` gained a `matchQuery` opt that surfaces the matched item first (backward-compat preserved — no matchQuery = byte-identical).
- **F2 expiry edit**: NEW `updateStockBatchExpiry` (1 runTransaction: batch.expiresAt + forensic trail [`expiresAtLegacyValue`/`EditedAt`/`EditedBy`] + `be_stock_adjustments` type=`expiry`/movementId null + source order-line sync by `locationType`; NO movement → conservation untouched; batch.status untouched [EXPIRED derived via `hasExpired`]; Rule O live-resolve productName). Editable `DateField` in the existing `AdjustCreateForm` (dual-path submit qty/expiry/both; reused by ยอดคงเหลือ + ปรับสต็อค + central — NO new buttons). `type=expiry` rendered in the adjust list + `AdjustDetailModal`.
- **Recon resolved 4 open-Qs**: `formatOrderItemsSummary` @ `src/lib/orderItemsSummary.js`; EXPIRED never persisted (→ no status normalize, the `status:'active'` dropdown already lists mistyped-past lots); central reuses `AdjustCreateForm`.
- **Verified**: full vitest **15992/0** (+22, zero V21 lock-in) · build clean · real-prod Rule Q L2 `scripts/e2e-v159-stock-search-and-expiry.mjs` **28/0** (search surfacing · batch+forensic+audit+order-sync · no movement · status unchanged · hasExpired flip · 2-concurrent last-write-wins · idempotency · no-order graceful · zero orphan). audit-stock-flow **S36** (35→36).
- **NOT deployed** (V18). Honest scope: L1 (real browser open ปรับ → edit date → save → see update) = user hands-on per workstyle.
- Detail → `.agents/sessions/2026-06-03-v159-stock-search-expiry.md`.

### Session 2026-06-03 — 4-system audit loop (TFP/Stock/Sales/Finance) CONVERGED + V158 DEPLOYED

`/systematic-debugging` (ultrathink) 3-part loop: audit the 4-system money relationships → fix every bug → stop only when a fresh round finds nothing (user: "ลุยให้จบ loop แล้ว deploy ที่เดียว ... ผมจะไปนอนรอ"). master = `c31248ed`; prod = `91bb3349` (V158) LIVE.

**R16 found V158 (M19, Rule T)** — the concurrency gap V153 EXPLICITLY documented ("Query not in-tx → idempotent for the SEQUENTIAL trigger, NOT a concurrency lock"). `refundToWallet`/`reversePointsEarned` computed "outstanding" via a Σ query OUTSIDE the tx → 2 CONCURRENT cancel cascades (double-click / 2 admins) both read the pre-state → wallet over-refunded **+200 baht (re-spendable)** + points over-reversed **−50**. Confirmed real prod `e2e-r16` (wallet 1200/pts 150 → 1000/200, 1 refund + 1 reverse). **Fix**: per-reference NET marker on the doc (`saleNet` / `finance.pointsSaleNet`) maintained by `deductWallet`/`_earnPointsInternal` + read by refund/reverse IN the same tx → Firestore OCC serializes; legacy Σ query kept as seed for pre-V158 refs. Edit-safe.

**Then 6 consecutive FRESH clean rounds** (each a different surface, all real-prod Rule Q L2): R17 concurrent sale-edit 8/0 · R18 course lifecycle + use‖cancel race 6/0 · **R19 4-system capstone** (stock+deposit+wallet+points+course cancel-cascade + concurrent double-cancel) 21/0 · R20 report-accuracy code-audit (cancelBackendSale writes status='cancelled' which ALL 9 aggregators filter; saleReport totals exclude cancelled) · R21 one-deposit/many-sales partial-cancel + concurrent multi-apply 12/0 · R22 manual-adjust × sale-marker interplay + concurrent summary RMW 9/0. Stop condition met (matches prior stock-loop 6-clean bar).

**Verify**: full vitest **15970/0** · build clean · `tests/v158-concurrent-reverse-marker.test.js` 14/0 · audit-money-flow **M19** · V158 V-entry · 2 V21 fixups (v149 atomic-RMW invariant not literal shape; v153 fnBody 3400→4400 + B2 in-tx marker). **Deploy**: `vercel --prod` aliased — V158 client-SDK only, no firestore.rules change → no Probe-Deploy-Probe (V153-V157 precedent). The deployed bundle == the R16-R22 e2e-verified source. Detail: checkpoint `.agents/sessions/2026-06-03-v158-four-system-loop.md`.

### Session 2026-06-02 EOD+4 — Stock adversarial-hunt LOOP: V147-V152 (6 concurrency/atomicity fixes) + R16-R21 (6 clean rounds → CONVERGED) — ALL DEPLOYED

master = prod = `ae51cc18` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, frontend-only — all client-SDK runTransaction/CAS, NO rules change → no Probe-Deploy-Probe). `/systematic-debugging` 3-part LOOP (find novel stock bugs → fix → repeat until a fresh round finds nothing). **6 REAL bugs, ALL one family (non-atomic read-modify-write / stale-plan race) → NEW iron-clad Rule T**: V147(S32) `_deductOneItem` stale-plan FIFO race → retry loop · V148(AV177) `customer.courses[]` lost-update → `_mutateCustomerCoursesAtomic` runTransaction · V149(AV178) `finance.loyaltyPoints` RMW → runTransaction · V150(S33) negative carrier excluded cancelled/expired · V151(S34) reverse re-creates a V144-deleted lot · V152(S35) concurrent central-PO receive double-batch → CAS-claim-before-build (the documented AUDIT-V34 deferred gap). **Then 6 CLEAN verification rounds (R16-R21, real-prod Rule Q L2, 75 assertions, zero orphans)**: R16 reverse-conservation · R17 single-batch contention · R18 multi-lot FEFO + ledger-replay(S16) · R19 edit-path (reverse-old+deduct-new) · R20 cross-branch isolation · R21 transfer/withdrawal double-receive CAS. **Every R16-R21 "finding" was MY OWN test-assumption/filter error** (V144 0-lot delete, branchId-vs-locationId, V36-bis silent-skip), corrected to confirm the app is RIGHT — strong convergence. **Negative-stock from TFP/sale CONFIRMED INTACT**. Full vitest **15898/0**; build clean. Detail → checkpoint `2026-06-02-stock-concurrency-loop.md`. Outstanding: dropdown หมวดหมู่ task (3rd ask) · V-log-archive verbose entries (V147-V152) · Neuramis merge + junk test-course.

### Session 2026-06-02 EOD+3 — B (be_products cleanup) APPLIED + product delete-cascade/guard (AV176) built (LOCAL) + adversarial hunt (2 bugs)

master `6cff6905` (feat) + docs. prod bundle UNCHANGED `85063e5d` — **cascade code LOCAL, deploy needed**. NO rules change (frontend-only). **B DONE on prod** (Rule M admin-SDK two-phase): V145 product-junk strip+restore (35) + V146 orphan batches (5, incl. ยาทาจี้หูด/Buscopan-ฉีด) + 2 dedups — verified gone + idempotent + audit docs. **Debug (`/systematic-debugging`)**: bare `deleteProduct` orphaned stock+course → Guard+cascade (NEW `productDeleteCascade.js` pure + `productDeleteClient.js` client-side; ProductsTab wired; StockBalancePanel orphan backstop). BLOCK on stock>0 / course-main / pending-op; CASCADE product + batches(branch+central; ==0 del / <0 cancel) + courseProducts[] + be_product_groups; history kept (Rule O). **Adversarial hunt (user "loop until dry") found 2 REAL bugs**: pending-op un-receivable (139 products in active orders) + **op-schema heterogeneity** (guard's `where branchId` missed transfers/withdrawals [no branchId, numeric status] + central [centralWarehouseId] → fixed). Disproven: purchased-course deduction (graceful skip V36-bis), id-mismatch (0/611), marketing (none). Verified: product-delete 25/0 + e2e 124/0 (3 branches × every scenario, real prod) + build clean; full suite green modulo known cross-branch-import-rtl flake (JSON run 0-failed). AV176. Detail → checkpoint `2026-06-02-product-delete-cascade.md`. Outstanding: deploy cascade · dropdown task · Round 3 hunt · V147 v-log entry.

### Session 2026-06-02 EOD+2 — V145 stock product-edit (full-doc + real-time) + V146 staff-chat reply preview — SHIPPED + DEPLOYED + L1/L2 verified

`/brainstorming`→spec→plan→impl (V145) · `/systematic-debugging` (V146). master = prod = `85063e5d` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, frontend-only — NO firestore.rules change → no Probe-Deploy-Probe, V125–V127 precedent).

- **V145** (AV175): tab=stock "แก้ไขสินค้า" loaded the aggregated balance ROW (blank/default fields + `setDoc(merge:false)` overwrite-corruption risk). Fix: full-doc load (`getProduct` fallback + StockBalancePanel `.full`) + `normalizeProduct` WHITELIST (drops stock-junk + `__proto__` guard; preserves stockConfig/forensic; enumerated from 610 real docs Rule R) + live unit/category/type + NEW หมวดหมู่/ประเภท columns (replaced ความจุ/มูลค่าทุน).
- **🐛 BIG bug (L1+diag)**: StockBalancePanel products-map effect `[]` deps → STALE after branch switch → all rows "-" for cat/type (BS-9; canonical name + skip-filter were stale too). Fix deps `[selectedBranchId]`.
- **🐛 Adversarial (E1)**: forensic `_`-loop copied `__proto__` → prototype pollution → guarded.
- **V146** (AV174): staff-chat reply quote now shows image-thumb/file-icon/sticker (single `buildReplySnapshot` + shared `StaffChatReplyPreview`) + click-to-scroll + bounce highlight.
- **Verified**: full vitest **15836/0** + **Rule Q L2 18/0 REAL prod** (609-doc whitelist completeness, zero loss + branchId preserved) + **Rule Q L1 REAL app** (columns + live fields + full-doc edit modal + branch-switch re-subscribe).
- **DEFERRED (user → next session)**: be_products data cleanup — 36 junk/corrupted docs in นครราชสีมา; dry-run done; `scripts/v145-cleanup-polluted-product-junk.mjs` ready (junk-strip + restore cat/unit/type + dedup 3 true same-branch dups). Run dry-run → review → `--apply` (Rule M).
- Detail: checkpoint `.agents/sessions/2026-06-02-v145-stock-product-edit.md`.

### Session 2026-05-31 EOD+4 LATE+3 — V143 stock cluster (show-0 + real-time + auto-clear) + V142 course double-deduct — SHIPPED + DEPLOYED

`/systematic-debugging` ×2. master = prod = `0c607f68` LIVE @ lover-clinic-app.vercel.app (`vercel --prod`, aliased; was `8c3a9047`). Frontend/lib + 1 new cron → no Probe-Deploy-Probe. Detail: `.agents/sessions/2026-05-31-v142-v143-deploy.md`.

- **V142** course double-deduct shipped (V142-quater over-credit gate + V142-quinquies finalize→doctor→finalize via persisted `_courseDeducted` flag). Doctor-save clarified: must NOT save course-deduction (bottom save owns it). Verified matrix 30/0 + flag-roundtrip/fuzz/stock **L2 e2e 30/0** real prod.
- **V143** StockBalancePanel shows products drained/cleared to exactly 0 (`status∈{active,depleted}`; 7 NK products were hidden). AV166.
- **V143-ter real-time** NEW `listenToStockBatchesByBranch` (Layer-1 onSnapshot BS-13 safe-by-default + Layer-2 wrapper; panel → live listener). AV167. **L2 e2e 5/0 real prod** (`e2e-stock-balance-realtime.mjs` — writes from another surface push LIVE).
- **V143-quater auto-clear-lot** pure `planLotCleanup` (keep live + ≤1 zero placeholder, DELETE-only) + daily cron `stock-lot-cleanup` (03:45 BKK, vercel.json) + Rule M script. AV168. 14/0 + dry-run 0-redundant.
- **Rule M APPLIED on prod** (earlier this session): NK reset `v143-nakhon-stock-reset.mjs --apply` (51 batches→0/0, 364 docs deleted) + `v143-collapse-nk-multilot.mjs --apply` (→ 53 products = 53 lots at 0); idempotent + audited.
- **3 V21 test-fixups** (v143 SG1 + v138 N10.7 + v34 INV.11.4 — same V143-ter panel-migration class; full suite confirmed no 4th). **Full vitest 15418/0** (JSON-reporter) + build clean.
- **Honest gap (Rule Q)**: 2-device real-browser live-balance + NK-shows-0 = USER L1. Cron now active (optional CRON_SECRET hit to verify like V122).

### Session 2026-05-28 EOD+4 — 4-feature batch V128 + V129 (UNCOMMITTED, held — no deploy)

5× `/systematic-debugging` in one session; all 4 fixes complete + per-feature-verified; **feature code UNCOMMITTED** (held per user "ยัง deploy ไม่ได้ มีงานถัดไป" — only agent-docs committed). master code HEAD `10e28ed4`; prod `26fb5789` (V127) — none deployed. All frontend/lib → no Probe-Deploy-Probe; ONE combined `vercel --prod`.

- **V128 appt phone** — hover-peek + detail-modal show the linked customer's phone. Write-chokepoint `_resolveAppointmentCustomerPhone` (backendClient create+update) + `useResolvedApptPhone` render live-resolve + `apptPhoneValue||resolvedPhone`. L2 real-prod: 78 linked + 45 temp = 0 blank. AV145.
- **V128 calendar auto-height** — `AppointmentCalendarView` fixed `SLOT_H=22` → dynamic `slotH` (computeApptSlotHeight, fills viewport, clamp 22–46).
- **V128.lb staff-chat lightbox** — full-screen `width/height:100%` of `w-full h-full` wrapper (object-contain upscales small imgs; was max-w-4xl 896px) + zoom + drag-pan (clamped) + wheel-zoom. **Chrome-MCP real-browser verified** (fill+zoom+pan). AV146.
- **V129 reports-sale** — `tab=reports-sale` พนักงานขาย + ผู้ทำรายการ resolve via `resolveSellerName(listAllSellers)` (aggregator + SaleReportTab + SaleDetailModal). L2 real-prod `-` count 38→0. AV147. `createdBy` unwritten → ผู้ทำรายการ = resolved first seller (optional follow-up: capture true createdBy at sale-write).

Tests (no EOD re-run per rule): last FULL 15039/0 (pre-lightbox+reports); targeted since GREEN (lightbox-family 104 · v128 7 · v129 12); build clean; full re-run pending at deploy. 5 V21 source-grep fixups absorbed. Diag/verify scripts kept (Rule R). Detail: `.agents/sessions/2026-05-28-v128-v129-batch.md`.

### Session 2026-05-28 EOD+1 — V125 light-theme WCAG-AA (treatment form) — SHIPPED + DEPLOYED + prod-verified

`/session-start → "Outstanding ทำเลย"`. Re-proved the 3 outstanding L1 items live (appt real-time strip 2→3 / 3→2 cross-process via node admin-SDK write/delete, per-branch; chart relay PC-side pairing modal + accurate "no tablet in branch" presence). The treatment-form L1 contrast scan (Chrome MCP, real prod, light theme) FOUND **19 light-theme AA fails V124's class-based `[data-theme=light]` CSS overrides couldn't reach** — because the treatment form sets accents via **inline `style={{color:'#…500'}}`** (no class to match) + uses an **arbitrary-hex `bg-[#7c3aed]` CTA**. This was the one surface V124 never individually scanned.

**Fix (2 classes)**: (1) NEW `src/lib/themeAccent.js` `aaAccent(hex,isDark)` deepens -500/-400 → -700 AA-dark in light, pass-through dark — wired into `SectionHeader`+`ActionBtn` (component-level) + 12 inline spans + `ChartSection` + `TreatmentTimeline` (2 hardcoded). (2) index.css white-restore for `.bg-\[\#7c3aed\].text-white` (doctor-note save btn, was 3.05:1 dark → 5.2 AA white). Teal #2EC4B6 (7.76) + LINE-green #06C755 (7.67) left dark — already AA, NOT restored.

**Verified (Rule Q / Q-vis)**: T7 **20/0** (AA-math every mapped target ≥4.5 on white + source-grep) + build clean + full vitest **14990 pass + 1 known flake** (phase15.5b global.fetch-leak, passes 51/0 isolated — not V125) + **post-deploy re-scan on the LIVE deployed build** (gold-standard, V124-fix2 pattern): treatment form 0 accent fails (1372 els) + sale/finance tab 0 (2186 els, no regression) + appt view 0 + zoom (violet white, headers deepened, teal correctly dark). Commit `f56bfa9b` → `vercel --prod` (CSS/JS only, no rules → no Probe-Deploy-Probe). Wiki updated (entity `themeAccent.js` + concept `light-theme-aa`); `python -m graphify update .` ran (graph.json refreshed). **PatientForm.jsx light-theme deferred** — bespoke `isDark?dark:light` brand colors (pink/rose), needs a DESIGN pass not a mechanical wrap. Detail: checkpoint `.agents/sessions/2026-05-28-v125-light-theme-aa.md`.

### Session 2026-05-27 EOD+13 → 2026-05-28 — Light-theme WCAG-AA audit (App UI) + brand-red red-700 — SHIPPED + DEPLOYED

- **State**: SHIPPED. master HEAD `9f6cc49d` (a4731775 light-theme + 9042934a Rule-S docs + 9f6cc49d handoff); pushed; `vercel --prod` LIVE `9042934a` (was 8f6b7ced) + prod-verified (--accent-red #b91c1c). full vitest **14976/0** (678 files) · build clean · NO rules/storage/data/cron → frontend+theme-config (no Probe-Deploy-Probe).
- **Scope (user directive)**: ไล่ดู light theme ทุกหน้า/ทุก modal แล้วปรับให้ชัด — **CSS/colour/theme-config ONLY, ห้ามแตะ wiring/flow/logic**. Brainstorm-locked: WCAG **AA 4.5:1** · Hybrid central-first · App-UI (exclude print/doc) · full live-browser pass.
- **Architecture understood**: light theme = a giant `[data-theme="light"],[data-theme="auto"]` override layer in `src/index.css` remapping hardcoded dark Tailwind classes → light. Failure = any uncovered dark class / light-pastel text on light = invisible/sub-AA.
- **Fixes (all theme-layer)**: FM-A uncovered dark surfaces → light tokens · FM-C colour-text -300/400/500 → AA-dark · **alert-box ext** (17 colours: `bg-{c}-700..950/alpha`→{c}-50 + `.text-{c}-50..300`→{c}-700; 1.0–1.3 → 4.8–6.5:1) · FM-D **14 inline `color:#hex` → `--accent-{blue,line,purple,amber,red,teal}` vars** (CustomerDetailView · LinkLineInstructionsModal · LinkRequestsTab · TreatmentTimelineModal · ClinicSettingsPanel) · teal #0d9488→#0f766e · `--tx-muted` #64748b→#5b6675 · green-500/600 → #15803d.
- **tailwind darkMode root-fix (USER-APPROVED "Global config")**: `darkMode: ['selector','[data-theme="dark"]']` — was unset (=media) so `dark:` (82 uses/15 files) coupled to OS prefers-color-scheme → light-theme on a dark-OS machine rendered dark-variant colours (invisible finance/wallet). AV136 class, fixed globally.
- **Verified (Rule Q-vis — REAL browser, measured per-page contrast scanner)**: CLEAN on 7 surfaces (sale·reports·promotions·finance[synthetic]·appointment-hub·customer-detail·frontend-appt) · darkMode fix proven BOTH directions (synthetic + frontend-appt reload) · dark theme NO regression (screenshot). The deep-scan (user push "ตรวจหมดจริงๆเหรอ" — justified) surfaced teal/muted/green/dark: that eyeballing missed. All fixed.
- **✅ Brand-red RESOLVED (user chose darken 2026-05-28)**: `.text-red-400` (261 uses) + `--accent-red` light → red-700 `#b91c1c` (was #dc2626, 4.37:1 sub-AA on tint → **6.47:1** white). Verified real browser (computed rgb(185,28,28) + screenshot on white + tinted card) + prod-verified. Other red shades already #b91c1c. Strict-AA regression guard added to T7.
- **⚠ HONEST scope gap (unchanged by ship)**: scanner ran 7 surfaces; **stock/master-data/treatment-form/chat/settings/deep-modals NOT individually scanned** (collapsed-section + drawer nav resisted programmatic clicks; login wall blocks auto-L1 — security rule). They use the SAME now-fixed global classes + brand-red darken is monotonic-safe → covered by global fixes + need user L1 spot-check on prod light theme.
- **Tier 2**: NEW `tests/light-theme-override-coverage.test.js` (T7 **5/0** — coverage walk + FM-C/A/D + strict-AA brand-red guard) + TL1.6 fixup (`#2EC4B6`→`var(--accent-teal)`).
- **Commits (2026-05-28 ship)**: `a4731775` (light-theme + brand-red, 12 files) + `9042934a` (Rule S TIMING docs) + `9f6cc49d` (handoff) — pushed + `vercel --prod` LIVE + prod-verified. spec/plan/inventory: `docs/superpowers/2026-05-27-light-theme-*`. Older 46 session blocks archived to `.agents/sessions/session-handoff-archive.md` (SH 277→142 KB, under cap).
- Detail: checkpoint `.agents/sessions/2026-05-27-light-theme-audit.md`.

