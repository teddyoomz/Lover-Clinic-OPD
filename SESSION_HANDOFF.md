# Session Handoff — LoverClinic OPD Cross-Session State

> **This file is read FIRST every new session.** Updated by `/session-end` skill.
> Link out to `.agents/sessions/*` for detail.

---

## 📏 HARD CAP: 10 sessions (2026-06-16 — supersedes the 200 KB size cap)

This file carries **at most the last 10 `### Session ...` blocks + the last 10
`## Current State` one-line bullets** — ALWAYS. `/session-end` MUST trim EVERY
turn (not "when it gets big"):

1. After inserting today's new block + bullet, count `### Session` blocks and
   `- **NEW (` / `- **Date (` Current State bullets.
2. If either is > 10, move the OLDEST overflow (sessions 11+, bullets 11+) into
   `.agents/sessions/session-handoff-archive.md` — prepend the NEW batch at the
   TOP (newest archived first; one `## Archived <date>` heading per batch).
3. Delete the moved content from this file; keep the footer pointer to the archive.
4. The canonical trimmer is `.tmp-trim.py`-style logic baked into the skill — keep
   exactly 10 + 10 (counts, not bytes). Detail per session lives in
   `.agents/sessions/*.md` checkpoints + `v-log-archive.md`, so trimming loses nothing.

**Origin**: 2026-06-16 — the size cap (180/200 KB) let the file accumulate 23
session blocks + 45 Current-State bullets (~40k tokens) while sitting *under* the
trigger, wasting boot tokens every session. User directive: *"ให้มันเหลือแค่ 10
session ที่ carry ข้อมูลไว้ในไฟล์นี้ตลอด ... จะได้ไม่ต้องมานั่งเปลือง token อ่านไฟล์ใหญ่ๆ"*.
The count cap (10+10) replaces the byte cap so the file stays ~10-12k tokens forever.

---

## 🚨🚨🚨 RULE Q — REAL-ADVERSARIAL VERIFICATION (V66, 2026-05-14) — READ EVERY TURN 🚨🚨🚨

**TRUST COLLAPSED. PHASE 29 SHIPPED WITH 5+ USER-VISIBLE BUGS WHILE 8 LAYERS OF TESTS CLAIMED PASS.**

Mock tests are NOT verification. Admin-SDK doc-level access is NOT verification.
They are **CODE-SHAPE COVERAGE ONLY**.

**Before claiming ANY of these — "verified" / "shipped" / "tests passed" / "done" / "complete" / "ready to deploy" / "PR ready" / "approved" / "working" — for ANY user-visible code (UI, API endpoint with auth, Firestore query, etc.) — you MUST satisfy ≥1 level:**

- **L1 (PREFERRED)** — Playwright/real-browser drives the REAL deployed UI with real auth + real DOM + real Firestore side-effects
- **L2 (ACCEPTABLE)** — Real client SDK (NOT admin) issuing the EXACT compound queries / listener subscriptions the UI issues
- **L3 (LAST RESORT)** — User walkthrough with written confirmation ("ลองแล้ว work" / "ลองแล้ว พัง XYZ")

**FORBIDDEN** (Rule Q violations):
- `vi.mock('firebase/firestore')` + claim "verified"
- RTL with mocked listener data only
- Admin SDK `doc.get/set/batch.commit` + claim "compound query verified"
- `firebase firestore:indexes` returns N → claim "indexes ready" (deployed ≠ built; indexes take 2-30 min)
- Post-deploy probe = anon HTTP POST to one collection (not a compound query)
- "All vitest tests pass + build clean → shipped" (INSUFFICIENT for user-visible flows)
- "I tested for 5 min and found no bugs" (<5 min + 0 bugs → retest at higher level)
- Confirmation-bias test design ("write test that assumes correctness → green")

**Self-check** (run BEFORE any "verified" claim — any "no" or "I'm not sure" → DO NOT CLAIM):
1. Did I drive REAL browser OR real client SDK?
2. Did I issue the EXACT query the UI issues?
3. Did I actively TRY to BREAK my own code?
4. If <5 min testing + 0 bugs → did I retest at higher level?
5. Can I produce output log + screenshot proving the flow?

**Full text**: `.claude/rules/01-iron-clad.md` Rule Q (top-of-file) + `~/.claude/skills/real-adversarial-verification/SKILL.md` + V66 in `.claude/rules/00-session-start.md` § 2 + verbose entry in `.claude/rules/v-log-archive.md`.

**Origin**: V66 (2026-05-14) — Phase 29 trust collapse. User curse-verified: *"กูไม่เชื่อเทสที่ไม่น่าเชื่อถือของมึงแล้ว ... ทำยังไงก็ได้ให้ต่อไปนี้การเทสของมึงจะต้องไม่เหี้ย ไม่โกหก ไม่เข้าข้างตัวเองและใช้ไม่ได้จริง"*. EVERY FUTURE "VERIFIED" CLAIM MUST PASS L1 OR L2. NO EXCEPTIONS.

---

## Current State

- **NEW (2026-06-21) — Staff-chat "ระบบ" System notification cards (AV198) SHIPPED + DEPLOYED LIVE + 8-round bug-hunt CONVERGED**: master `a62c20c4` (+EOD docs); prod = vercel `lover-clinic-cc7twr3pm` (lover-clinic-app.vercel.app) + `firebase deploy --only functions,firestore:rules` (Probe-Deploy-Probe **13/13**). Intake / follow-up form completion → `sendPushOnSubmit` ALSO writes a "ระบบ" card into the per-branch `be_staff_chat_messages` (sparkles icon, customer name + HN, clickable name → `/?backend=1&customer=<id>` new tab, sky never red). Intake cards **live-resolve** to clickable+HN the instant the walk-in is registered (`opd_session.brokerProClinicId`); counts unread + plays the chat sound (Q4, via V82 auto). `/brainstorming`(Visual Companion)→spec→`/writing-plans`(14)→inline impl. NEW `functions/staffChatNotify.js`(deterministic id)+`index.js`(write before FCM+before token-guards, non-fatal, skip edits)+`staffChatNotifyResolve.js`(live onSnapshot hook)+`StaffChatSystemCard.jsx`(theme-aware AA)+`StaffChatMessage` early-branch+`useStaffChat`/`staffChatClient`/`firestore.rules` read-only guards. **8-round adversarial bug-hunt (Workflow, Ultracode) CONVERGED** (R1 deleted-customer→R2 no-tokens CRIT→R3 dup-on-retry CRIT→R4 0/Q4-intended→R5 contrast→R6 rule-immutability→R7 hook↔picker→**R8 0 clean**). **Verified**: full vitest **16914/0** + build clean + **L2 e2e 14/0 real prod** (intake PENDING→register→FLIP, zero-orphan) + Rule R diag (branchId 35/35) + Probe-Deploy-Probe 13/13. **Honest L1 gap**: real form→card in authed chat + live flip = USER hands-on. Checkpoint `.agents/sessions/2026-06-21-staff-chat-system-notification-cards.md`.
- **NEW (2026-06-20 cont.3) — Filler STANDALONE public site SHIPPED + DEPLOYED LIVE (loverclinic.vercel.app) + OPD redeployed**: standalone prod = `loverclinic-jwk0jef67` aliased **`loverclinic.vercel.app`** (Q4 URL confirmed available, not taken); OPD prod = `lover-clinic-o5gogdbfp` aliased lover-clinic-app.vercel.app. Both frontend-only → no firestore.rules → no Probe-Deploy-Probe. `/brainstorming`(Q1 repo-single/2-builds · Q2 one deploy:filler · Q3 og meta · Q4 stop-if-URL-taken)→spec→plan(8 tasks)→inline impl (Tasks 1–5 + 7-pre + 8). **Goal**: send `?play=filler` to customers via a SEPARATE Vercel project with ZERO access to OPD (max security). **Single source, 2 build targets**: NEW `filler.html`→`src/filler-main.jsx`→`<FillerSimulator/>` (root URL + own title/favicon/og) + `vite.filler.config.js`→`dist-filler` (emits index.html). **Security = structural**: standalone bundle tree-shakes to zero firebase/OPD (proven `verify:filler` + Rule Q L1 `firebaseRequests:[]`). **🐛 Found + fixed (both configs)**: obfuscating `FillerSimulator.jsx` mangled its `import('Filler3D.jsx')` literal → the `three` 3D lazy chunk never emitted → 3D silently broken (OPD `vite.config.js` had it too, not yet deployed). Narrowed obfuscator scope to formula files only (`fillerMath.js`+`FillerGraphic2D.jsx`); OPD build now emits `Filler3D-*.js` 524.9K (R9-8 updated). **og:image** = REAL clinic logo on brand-dark (Playwright render, no AI). **Verified**: filler 102/0 + full vitest **16869/0** + build clean + `verify:filler` ✅ + **Rule Q L1 Playwright real obfuscated build** (renders + math + real logo + 3D lazy chunk loads + zero firebase network + mobile no-overflow). NEW `deploy:filler` one-command dual-deploy (frontend-only → no Probe-Deploy-Probe) + `vercel.filler.json` (tight CSP `connect-src 'self'`). **DEPLOYED + LIVE-verified (Rule Q L1 Playwright, real prod)**: loverclinic.vercel.app renders + math + real logo + 3D loads + **`offHostRequests:[]` + `firebaseRequests:[]`** (talks to nothing but its own host) + mobile no-overflow + console clean; live `/api/*` = `text/html` md5-IDENTICAL to homepage (SPA fallback, NOT an OPD function — no serverless on the public site); OPD `?play=filler` 3D loads on prod too (`filler3dChunkLoaded:true`). **Deploy mechanism gotcha**: `vercel deploy <dir>` reads vercel.json from the CWD (the OPD one with `functions`) → must `--local-config vercel.filler.json` + static config (no functions) so api/ never reaches the public project. Vercel project `loverclinic` created (team `teddyoomz-4523s-projects`); ids in `.env.filler-deploy` (gitignored). **THEN filler visual-polish — committed+pushed, NOT deployed (live sites still PRE-polish)**: `6f78c45d` red "หลังฉีด" line BOLD+glow (was barely visible; `/systematic-debugging` — root cause confirmed by peak/trough screenshots of the obfuscated build: strokeOpacity 0.6+1px + fade-to-0; fix = opacity 1, width 2.6, never-fade, always-on glow; OPD vite.config 3D-obfuscator bug found+fixed too) · `c63fd2cb` mobile controls-on-top (flip ≤820px order) + red line default OFF (`showAfter` false) · `df2f277f` condom card = HERO (top + most prominent — full-width, fire border+glow, แนะนำ badge, 30px green) + breathing FULL-disappear restored (bold→GONE→bold for before↔after contrast, keeping the bold peak). full vitest **16870/0** + build clean + Rule Q-vis screenshots. Next deploy ships all 3 via `npm run deploy:filler`. Checkpoint `.agents/sessions/2026-06-20-filler-v7-polish.md`. Spec/plan `docs/superpowers/{specs,plans}/2026-06-20-filler-standalone-public-site*` · checkpoint `.agents/sessions/2026-06-20-filler-standalone-public-site.md`.
- **NEW (2026-06-20 cont.2) — Filler v5.6 (breathe+glow + split-fix regression) + v6 (narrow cited estimate + ladder-72 + 2D dash toggles) — SHIPPED local, NOT deployed**: master `3d37eeca` (=origin); prod UNCHANGED — filler-sim still NOT deployed (pure-client → vercel-only, no Probe-Deploy-Probe). **v5.6** (`2975fce3`): red dashed "หลังฉีด" breathe+glow (Calm, fade to opacity 0, ~3.6s, reduced-motion) + faint baseline 0.5→0.25 dark / 0.42→0.21 light. **v5.6 split-fix** (`98ed2322`, Issue-1 regression, `/systematic-debugging`): the red dashed line WAS the stroke of the skin-filled body → opacity anim faded the WHOLE body; split into static skin-fill + `fill="none"` animated outline (V21: own v5.6 test had locked the broken combined element → rewritten + anti-regression). **v6** (`3d37eeca`, `/brainstorming`+`/systematic-debugging`, Ultracode): (A) range too wide → recalibrated `k 2.37/3.32 → 1.8/2.3` anchored to HA RCT [PMC7230452]; geometry confirmed correct (ISO 4074 width=½circ); 7"/30cc "XL 60–68"→"XL 60". (B) ladder→72: 66–72 real numbered sizes, "เกินมาตรฐาน 🔥" only past 72. (C) 2D dash toggles (showAfter/showBaseline) doubling as the legend + color-keys left + stronger glow (drop-shadow 4+9+15px) + 44px tap + flex-wrap auto-scale. **Verified**: filler 98/0 + full vitest **16864/0** (1 transient parallel flake, re-ran clean) + build clean (obfuscated) + **Rule Q L1** (7"/30cc→"XL 60 +4 ขนาด" girth 12.1–12.6; XXL64+50cc→"74–78 เกินมาตรฐาน 🔥"; toggles hide/show both dash sets; stronger glow live CSS; mobile 375px no-overflow + 44px). Honest gap (Rule Q-vis): `preview_screenshot` times out → rendered DOM/behavior, not JPEG. Both cited/adversarial workflows rate-limited → research done inline. Checkpoint `.agents/sessions/2026-06-20-filler-v6-narrow-estimate-toggles.md`.
- **NEW (2026-06-20 cont.) — Filler Simulator polish v5.3+v5.4 + R9 (obfuscation + watermark + contact buttons) — SHIPPED local, NOT deployed**: master `f5676bcb` (=origin); prod UNCHANGED — filler-sim still NOT deployed (pure-client → vercel-only, no Probe-Deploy-Probe). 3 user rounds. **v5.3** (`5d135a1d`): default split shaft 10·glans 0 · **split-bar legend fix** (labels moved OUT of the proportional flex segments — were hidden <14% / crammed at 1.5 — to an always-visible color-keyed legend) · bigger centered cross-section · subtitle copy · **iPad/touch** (3D `touch-action:none`, buttons `manipulation`, 44px tap targets, 30px thumb). **v5.4** (`fb58c199`): **round-DOWN results (safety)** — `condomIndexForGirth` nearest→FLOOR + `r1` Math.round→Math.floor (under-promise) · **auto-scale layout** (controls `space-between` fill-height; 2D split into flex sections side-view/cross-section/HTML-legend, `space-evenly`, no dead bands; desktop eval equal-height 679=679 + 18px bottom-gap + fills-region). **R9** (`f5676bcb`; `/brainstorming`→spec→plan→inline+3-agent research Workflow): (1) **formula obfuscation** — `vite-plugin-javascript-obfuscator` scoped to the 4 filler files, `command==='build'` only (vitest bypasses → 16846 tests unaffected); k-constants rewritten as integer fractions (237/100…, value-identical) so `numbersToExpressions` hides the floats; **dist grep: K_REALISTIC / 2.37 / 3.32 / 2.54 / condom-labels / dCgeo / condomIndexForGirth ALL absent**; **obfuscated build served :4173 → renders + computes IDENTICAL** (girth 10.4→11.5–11.9, condom Regular 52→Large 56–58). (2) **theme-aware logo watermark** — centered-faint `<image>` ×2 in 2D SVGs + DOM overlay over 3D canvas (`pointer-events:none`, ~9%; white logo dark / black light). (3) **contact buttons** — `CLINIC_CONTACT` + reusable `ContactButtons` (header compact icons + footer full), REAL brand SVG icons (official LINE/Facebook + phone), theme-aware (dark filled+white / light soft-tint+deepened+border, AA), `tel:0975251525` · `lin.ee/mFFsDkG` · `facebook.com/loverclinickorat` (LINE/FB `target=_blank rel=noopener noreferrer`). **Verified**: targeted 79/0 + full vitest **16846/0** + build clean + dist obfuscation grep + **Rule Q L1** (dev: 6 contacts correct hrefs/target/rel + real icons + 2D×2/3D watermarks + theme swap; **prod build :4173 renders + math identical, console clean**). **Honest gap (Rule Q-vis)**: `preview_screenshot` timed out repeatedly (env flake) → desktop fill proven via DOM-geometry; mobile 2D-watermark+header-icons captured. Checkpoint `.agents/sessions/2026-06-20-filler-v53-v54-r9.md`.
- **NEW (2026-06-20) — Penile Filler Size Simulator SHIPPED (v1 + v2 + debug round) — local only, NOT deployed**: master `505961af` (=origin); prod UNCHANGED `lover-clinic-228lv6o7s` (filler-sim not deployed). NEW public `?play=filler` — **pure client, NO Firestore/auth/PII, no rules** → frontend-only vercel-only, no Probe-Deploy-Probe. `/brainstorming`(Q1-Q5+rev1-4+v2 round-2)→spec×2+plan×2→inline impl→`/systematic-debugging`. **2 web-research agents** grounded the math (shaft girth+condom, glans). **v1**: shaft `C1=√(C0²+4π·shaftCc/L)×k` (k2.37/3.32, anchor 16cc→+2.0/+2.8 VERIFIED node+L1) + EXACT condom (ISO 4074 `W=รอบวง×5`, 8-rung ladder snap nearest-tie-larger) + realistic 2D SVG + WebGL 3D (Three.js **lazy**, rotatable) + เดิม→ใหม่ cards. **v2**: glans split-bar (cc รวม+glans%+stacked ลำตัว|หัว; glans NOT affecting condom, research 0.25cm Ø/cc) + mushroom 2D/3D + TH/EN i18n + light/dark (clinic palette) + removed 18+ gate + length 0.1in/max10in/default5in. **debug round** (`/systematic-debugging` 6 items): 🐛 3D glans clamped to shaft radius → followed shaft (fixed: floor-on-const + damped `est.glans.visualLow` 0.4×, research rate kept) · removed egg-ellipse/glans-card/"ทรงเห็ด" · 🐛 split-bar spill at glans=0 (`flex:0 0 pct%`+overflow hidden+conditional label). **Verified**: filler 39/0 targeted + build clean (three lazy chunk 524K isolated) + full vitest **16804/0** (v2 commit; debug-round targeted only) + **Rule Q L1 real browser** (values match fillerMath exactly · anchor +2.0/+2.8 live at cc16 · split/glans/EN/theme/3D-mushroom all work · 3D disposes clean). Honest gap (Rule Q-vis): rendered-pixel screenshot = USER eyes (preview_screenshot times out this env). Checkpoint `.agents/sessions/2026-06-20-filler-simulator.md`.
- **NEW (2026-06-19) — AV98 recurrence: ED detail/compare modal trapped in its own box — ✅ SHIPPED + DEPLOYED LIVE**: master `574141ad` (= origin pre-EOD-docs); prod = **`lover-clinic-228lv6o7s`** (vercel frontend, aliased lover-clinic-app.vercel.app HTTP 200); **firestore.rules UNCHANGED → frontend-only, no Probe-Deploy-Probe**. `/systematic-debugging` + Workflow census (rate-limited ×2 → recovered inline per V83). **Root cause**: `EDScoreBox` renders `<EDDetailModal/>` INSIDE its own `rounded-xl` card; the V86 auto-glow (index.css ~4043) makes every rounded card in `[data-backend-menu-mode=new] [data-testid=backend-content]` a containing block for `position:fixed` descendants → the 2-panel compare modal confined to the card box ("modal แค่ box ตัวเอง"). Same class as recall **V80** — missed because that regression test was recall-DIR-scoped. **Census (Rule P)**: EDDetailModal was the **LONE trapped instance** — every other overlay modal renders at a tab/panel/page ROOT as a SIBLING of (not descendant of) rounded cards (verified WalletPanel root=`space-y-4`, modals@232/245/257 siblings of the toolbar card; FinanceTab/DepositPanel/OrderPanel/report-tabs same; matches the AV98 sanctioned list). **Fix**: `createPortal(…, document.body)` on `EDDetailModal` + `EDFollowupModal` (incl. its full-screen QR sub-overlay) — AV98-canonical, byte-identical to the prod-proven recall/appt-popover portal. **Guard**: NEW `tests/av98-ed-modal-portal.test.js` (A portal lock + B EDScoreBox-nesting neutralized + C card-spawn registry); dropped a universal static "nested-in-card" walk (false-positive-prone — flagged 3 safe panels); AV98 SKILL.md updated. **Verified**: targeted 83/0 + **full vitest 16767/0** + build clean. **Honest gap (Rule Q)**: authed-CDV pixel render = USER hands-on (auth-gated; verified by code+tests+build + same fix proven LIVE for recall). Checkpoint `.agents/sessions/2026-06-19-av98-ed-modal-portal.md`.
- **NEW (2026-06-18 EOD+1) — ED Score 2-panel round COMPARE + be_assessments backup coverage (+ be_wallets phantom fix) — ✅ SHIPPED + DEPLOYED LIVE**: master `2e19ac01` (=origin); prod = **`lover-clinic-a2f1vlim5`** (vercel frontend) + **`firebase deploy --only functions`** (sendPushOnSubmit) — aliased lover-clinic-app.vercel.app HTTP 200; **firestore.rules UNCHANGED → no Probe-Deploy-Probe**. 2 features, each `/brainstorming`→spec(html)→plan(html)→inline impl→test→adversarial Workflow. **(1) ED 2-panel compare** (`303a85af`): `EDDetailModal` reworked into side-by-side compare — left/right swap (`useLayoutPreference('ed-compare')`, like TFP split-screen), per-panel round pickers, type tabs (ADAM/IIEF/MRS/PE), latest "ล่าสุด" + today "วันนี้" hero badges, changed-row highlight. **REMOVED the better/worse trend** (user). Badges no-squeeze (flex:none + centered + nowrap; picker truncates). NEW pure `src/lib/edCompare.js` (`autoPickCompareRound` nearest-prior + `markChangedRows` align-by-n). AV78 backdrop no-close; ESC/✕ close. **(2) be_assessments backup coverage** (`f9c4d36b`): the new ED `be_assessments` (2026-06-15) was missing from EVERY backup/cascade scope (V122-class drift). Added to `CUSTOMER_CASCADE_COLLECTIONS_FULL` (→17), `CUSTOMER_ONLY_UNIVERSAL`, `branchBackupCore.UNIVERSAL`. **Single-source reconcile**: `backendClient.CUSTOMER_CASCADE_COLLECTIONS` = `…_FULL` (was a drifting inline 11) → client+server delete can't diverge. NEW **drift-catcher** test (every customerId-queried be_* ⊆ FULL ∪ allowlist). Whole-system FULL already dynamic `listCollections()` (V122) → auto-covered. **(3) be_wallets phantom fix** (same commit, surfaced by the drift-catcher): cascade listed `be_wallets` (0 docs / no rule / no accessor); the real store is `be_customer_wallets` (composite id `customerId__walletTypeId`, money-adjacent) → renamed across cascade/delete/backup/scripts so customer delete no longer ORPHANS wallet balances. **Rule M (APPLIED on prod)**: Rule R diag found 5 orphans (4 be_assessments for deleted LC-26000155 + 1 be_customer_wallets for deleted 2853) → `cleanup-orphan-assessments.mjs --apply` deleted 5 + audit `cleanup-orphan-assessments-1781799289079-01e2d08b`; idempotent (0 left). **Verified**: ed-compare 11/0 · ed-detail-modal 21/0 (16 RTL + 5 source-grep) · be-assessments-backup-coverage (A–E + drift-catcher) · **Rule Q L2 e2e 9/0 REAL prod** (`scripts/e2e-be-assessments-backup-roundtrip.mjs` — seed→collect-by-customerId→restore→cleanup, TEST fixtures, zero orphans) · full vitest **16756/0** · build clean. **Honest gap (Rule Q)**: ED-compare L1 pixel render on an authed CDV w/ a real ≥2-round customer = USER hands-on (L2 + RTL + adversarial done). Checkpoint `.agents/sessions/2026-06-18-ed-compare-and-backup-coverage.md`.
- **NEW (2026-06-18) — ED Score box: round-select + per-question detail modal + viewing-state-for-past-only (/brainstorming ×2 + /systematic-debugging) + follow-up push name+HN (Cloud Function) — ✅ COMMITTED + PUSHED, NOT deployed** (user: session-end, ยังไม่ deploy): master `92c5ab0b` (=origin); prod UNCHANGED (`00da035d`). **3 ED frontend features**: (a) click a history row → chips show that round's snapshot + "← ไปที่ครั้งล่าสุด" (default/no-selection = merged latestPerType UNCHANGED); (b) click ADAM/IIEF/MRS/PE chip → **EDDetailModal** (full question text + customer's answer w/ option label, that round; AV78 explicit-close); (c) **/systematic-debugging** — selecting the LATEST (hero) = default home view (no redundant back button, no stuck state) via derived `viewingPast = selectedRound && selectedRound.id !== hero.id` (robust to delete-renumber). NEW canonical `src/lib/edQuestions.js` (ADAM/IIEF/MRS/PE questions + per-q options + `buildEdAnswerRows`) + `EDDetailModal.jsx`. **Follow-up push** (Cloud Function): pure CJS `functions/notificationContent.js` + `customerDisplay.js` (CJS mirror of the ESM canonical resolver); `functions/index.js` reads `be_customers/{linkedCustomerId}` live (canonical BASE_PATH, non-fatal) → body **"🔔 {name} · HN {hn}"** (HN = resolveCustomerHN(hn_no) || linkedCustomerId); intake+edit UNCHANGED. **Verified**: ed-score-round-select 15/0 · ed-detail-modal 10/0 · ed-questions 9/0 · notification-content 15/0 (incl ESM parity) · **Rule R real-prod diag 2/2** (real FW-ED follow-ups → correct name+HN, e.g. "นาย จักรวาล งิ้วลาย · HN LC-26000014") · full vitest **16722/0** · build clean. **Adversarial Workflows**: notification (5 lenses; cf-wiring/parity/regression CLEAN) → 5 defensive findings adjudicated+fixed (trim whitespace name/title ×2 + compose-either intake name = parity w/ canonical) + locked N11-13; ED-detail WF stalled (never returned — feature covered by RTL 10/0 + chip-click verified). **Deploy when authorized (V18): frontend 3 ED features → `vercel --prod`; notification → `firebase deploy --only functions`** (no firestore.rules change → no Probe-Deploy-Probe). **Honest gap (Rule Q)**: ED L1 pixel render + notification L1 real-push = USER hands-on after deploy. Checkpoint `.agents/sessions/2026-06-18-ed-box-detail-and-push.md`.
- **NEW (2026-06-16 EOD+2) — Mobile-load reliability (autoDetectLongPolling + useResilientLoad + LoadErrorRetry + shared reconnect) — ✅ SHIPPED + DEPLOYED + L1-verified on the LIVE build**: master `d54d58c4` (=origin); prod = frontend **`lover-clinic-p4uawr0kx`** (lover-clinic-app.vercel.app, HTTP 200) — **frontend-only, firestore.rules UNCHANGED → vercel-only, no Probe-Deploy-Probe** (V125/V145/V162 precedent). User report: mobile sometimes won't load first try (black screen / stuck spinner / empty skeleton) across the staff app AND every customer link (PatientForm/ED-Scoring/view-data/view-appointment) → needs refresh/re-scan. `/brainstorming`(Q1 no-cache/Q2 auto-retry+button/Q3 all-test-layers+L2)→spec→`/writing-plans`(14 tasks)→inline impl→**adversarial bug-hunt LOOP (Workflow, Ultracode)**. **Connection layer (big lever)**: `firebase.js` `getFirestore`→`initializeFirestore({experimentalAutoDetectLongPolling:true})` heals the half-dead WebSocket (the #1 mobile cause; auto long-polling fallback) — NO persistence (Q1 fresh-always). **Shared**: NEW `firestoreReconnect.js` (module-debounced disableNetwork→enableNetwork; V17 + hooks share ONE debounce) · `useResilientLoad.js` (loading/ready/error + 8s soft-timeout → 1 silent auto-retry → error+retry; SYNC settledRef guard; resetKey re-arm) · `LoadErrorRetry.jsx` (theme-aware card, no-red-on-name). **Wired 6 surfaces**: App.jsx (V17→shared reconnect + **resilient anon-auth gate = kills black-screen-forever**) · PatientForm/ClinicSchedule/PatientDashboard (resilient load + ลองใหม่ escape; PatientDashboard retry-budget widened) · AdminDashboard (resilient queue banner, resetKey:selectedBranchId) · useBranchAwareListener (silent auto-heal, backend tabs) · BackendDashboard (Suspense chunk-load retry). KEPT V16 render-gate + V17. **Adversarial loop CONVERGED**: R1 (6-finder/verify) → 1 race fixed (sync settledRef); R2 → resetKey gap fixed; R3 → orphaned-timer (MY R2 fix's bug, caught by re-hunt) fixed (resetKey in timer deps); **R4 → 0 findings (clean)**. **Verified**: full vitest **16673/0** (+~65; the lone utils.test.js full-suite blip = confirmed transient parallel flake — green isolated 84/0 + clean re-run) + build clean + **Rule Q L1 Playwright 3/0 on the LIVE deployed build** (iPhone-13: blocked-auth→ลองใหม่→recover; half-dead-firestore→auto-recover-without-refresh captured live t3s→t10s; normal→resolves) + **Rule Q L2 7/0 real prod** (`scripts/e2e-mobile-load-coldstart.mjs`: exact anon+staff queries resolve under BOTH autoDetect AND forced long-polling → fallback transport proven on prod). **Lessons**: (a) the REAL "stuck" is a half-dead connection (request HANGS, no error) — `route.abort()` doesn't reproduce it (SDK serves fromCache fast); only a HANGING request does; (b) the fix recovers via `disableNetwork`→fromCache snapshot at the 8s soft-timeout (no refresh needed); (c) doc-not-found MUST call markReady (a successful load), only never-fires/onError → retry; (d) the adversarial hunt caught a bug MY OWN R2 fix introduced (R3 orphaned-timer) — the loop earned its keep; agents repeatedly OVERSTATE severity (V162 pattern) → adjudicate every finding yourself. Spec/plan `docs/superpowers/{specs,plans}/2026-06-16-mobile-load-reliability*`. **Honest gap (Rule Q)**: customer-link L1 proven on the live build; AdminDashboard banner + backend auto-heal = unit + L2 proven (staff-browser hands-on optional); PatientDashboard `/api/patient-view` resilience = L2 + source (vite dev doesn't serve /api).
- **NEW (2026-06-16 EOD+1) — Dup-customer prevention (Part A, Rule T atomic identity-claim) + Recall name/delete/date fixes (Part B) — ✅ DEPLOYED + Probe-Deploy-Probe 8/8 + 3 dup pairs resolved → 0**: master `c78378a9` (=origin); **prod = frontend `lover-clinic-gpxsr048v` (lover-clinic-app.vercel.app) + firestore.rules DEPLOYED** (`be_customer_identity` get/list:false/staff-write + `be_recall_cases` delete `if false`→isClinicStaff; Probe #17 live). `/brainstorming`→spec→`/writing-plans`(14 tasks) + design-hardening workflow (4-agent census) + **adversarial-review workflow (20 agents → found+fixed 2 HIGH bugs)**. **Part A**: `addCustomer` (single chokepoint) claims `be_customer_identity/{CITIZEN:|PASSPORT:key}` + counter + customer-doc in ONE `runTransaction` (`deriveClaimKey`/`resolveClaimAction` in `src/lib/customerIdentity.js`) → throws DUPLICATE_IDENTITY → `CustomerCreatePage` warn modal (เปิดของเดิม / บันทึกซ้ำอยู่ดี→`linkedCustomerIds`+`_duplicateOfCustomerId`) + phone soft-hint; `updateCustomerFromForm` frees-old+claims-new (oldKey RE-READ in-tx → race-safe); `deleteCustomerCascade`+server free/promote claim; OPD→`addCustomerOrLinkExisting` (DUPLICATE→link existing, real HN). **Part B**: `overlayRecallNames`/`useEnrichedRecalls` live-resolve name at the recall-load chokepoint → fixes "—" in 3 lists + 5 modal headers (already clickable); `RecallRow` 📞 โทรอีกครั้ง / 📅 เลื่อนนัด date chip (dd/mm/yyyy พ.ศ.); `RecallCreateModal` resolver. **Rule-M APPLIED on prod**: backfill 128 claims + 131 denorm (audit `…a068d36f`) + nuke 7 TEST-CASE junk + **3 dup pairs resolved → 0** (`fix-dup-customer-pairs.mjs`: LC-125 recall MOVED→LC-123, LC-069 empty deleted/keep LC-074, pair3 test LC-143+155 both deleted; audit `…efab0a08`). **Verified**: full vitest **16609/0** + build clean + **L2 e2e 16/0 real prod** (`e2e-dup-customer-and-recall.mjs` — concurrency/override/edit-reclaim/cascade/enrich) + EXECUTION test (real claim throw/override/reclaim) + Probe-Deploy-Probe 8/8 (opd_sessions anon→200 intact). **LESSON**: first footprint MISSED `be_recalls` (NOT in CUSTOMER_CASCADE_COLLECTIONS) — user caught it → `feedback_full_customer_footprint_before_delete.md`. **AV196/AV197**. **Honest gap (Rule Q)**: anon-deny PROVEN (Probe #17); staff-client-SDK + backend-authed UI = USER L1. Checkpoint `.agents/sessions/2026-06-16-dup-customer-recall.md`.

### Session 2026-06-21 — Staff-chat "ระบบ" System notification cards (AV198) — SHIPPED + DEPLOYED LIVE + 8-round bug-hunt converged

master `a62c20c4` (+EOD docs). prod LIVE: vercel `lover-clinic-cc7twr3pm` (lover-clinic-app.vercel.app) + `firebase deploy --only functions,firestore:rules` (Probe-Deploy-Probe 13/13). full vitest **16914/0**; build clean.
- **Feature**: intake / follow-up completion → `sendPushOnSubmit` writes a "ระบบ" card into the per-branch staff chat (sparkles icon, customer name+HN, clickable name → `/?backend=1&customer=<id>` new tab). Intake live-resolves to clickable+HN on registration; counts unread + plays the chat sound. `/brainstorming`→spec→`/writing-plans`(14)→inline.
- NEW `functions/staffChatNotify.js` (deterministic id `CHAT-SYS-<sessionId>`) + `index.js` wire (before FCM + before token-guards, non-fatal, skip edits) + `src/lib/staffChatNotifyResolve.js` (live hook) + `StaffChatSystemCard.jsx` + `StaffChatMessage` branch + `useStaffChat`/`staffChatClient`/`firestore.rules` read-only guards. AV198.
- **8-round adversarial bug-hunt CONVERGED** (Workflow): fixed deleted-customer downgrade, no-tokens→no-card (CRIT), dup-on-retry→deterministic id (CRIT), light-mode contrast, rule-layer immutability, hook↔picker consistency. Q4-chime = INTENDED (locked). Refuted: cross-branch (by-design), session-stuck-pending (semantically correct), same-ms tie (negligible+bad fix), sky-700 light (correct AA). V162: adjudicate every finding — agents overstate.
- **Verified**: full vitest 16914/0 · build clean · functions OK · **L2 e2e 14/0 real prod** (incl. intake FLIP, zero orphans) · Rule R diag (branchId 35/35) · Probe-Deploy-Probe 13/13. **Honest gap**: real-form→card-in-authed-chat + live flip + staff-client delete-denied positive probe = USER L1 (auth/creds). Checkpoint `.agents/sessions/2026-06-21-staff-chat-system-notification-cards.md` · spec/plan `docs/superpowers/{specs,plans}/2026-06-20-staff-chat-system-notification-cards*`.

### Session 2026-06-20 (cont.3) — Filler standalone DEPLOYED LIVE (loverclinic.vercel.app) + v7/v7.1 visual polish (NOT deployed)

master `df2f277f` (=origin). prod: standalone `loverclinic.vercel.app` (`loverclinic-jwk0jef67`) + OPD `lover-clinic-app.vercel.app` (`lover-clinic-o5gogdbfp`) — both LIVE at ~`5742f73a` (PRE v7-polish). full vitest **16870/0**; build clean.
- **Standalone site** (`/brainstorming`→spec→`/writing-plans` 8 tasks→inline): SEPARATE Vercel project serving only `?play=filler` at root, prebuilt STATIC `dist-filler` → zero firebase/api/OPD. New `filler.html`/`src/filler-main.jsx`/`vite.filler.config.js`/`verify-filler-bundle.mjs`/og:image real logo (Playwright)/`vercel.filler.json` tight CSP/`deploy:filler`. DEPLOYED + Rule Q L1 (zero firebase network, 3D loads, `/api`=SPA-fallback md5=homepage). Q4 URL `loverclinic.vercel.app` available.
- 🐛 **Obfuscator-breaks-3D bug** found+fixed in BOTH vite configs (obfuscating `FillerSimulator` mangled `import('Filler3D')` → three chunk never emitted). OPD build now emits Filler3D 524.9K (R9-8 updated).
- **Deploy gotcha**: `vercel deploy <dir>` reads vercel.json from CWD (OPD's, with `functions`) → MUST `--local-config vercel.filler.json` (static, no functions) so api/ never reaches the public project.
- **v7 polish** (`/systematic-debugging`, peak/trough+mobile screenshots on obfuscated build): `6f78c45d` red line BOLD+glow (was barely visible) · `c63fd2cb` mobile controls-on-top + red default OFF · `df2f277f` condom HERO card (top+most prominent, แนะนำ badge) + breathing full-disappear (bold→GONE→bold, kept bold peak).
- **NOT deployed** — next "deploy" = `npm run deploy:filler` ships the 3 polish commits to both sites. Checkpoints `2026-06-20-filler-standalone-public-site.md` + `2026-06-20-filler-v7-polish.md`.

### Session 2026-06-20 (cont.2) — Filler v5.6 (breathe+glow + split-fix) + v6 (narrow cited estimate + ladder-72 + 2D dash toggles) — SHIPPED local, NOT deployed

master `3d37eeca` (=origin; 3 feature commits: v5.6 `2975fce3` · v5.6 split-fix `98ed2322` · v6 `3d37eeca`). prod UNCHANGED — `?play=filler` pure-client, still NOT deployed (vercel-only, no Probe-Deploy-Probe). Three rounds: a feature, a regression I caused + fixed, a credibility+UX overhaul.
- **v5.6** (`/brainstorming`): red dashed "หลังฉีด" breathe+glow — Calm, holds lit ~38%, fades to opacity 0 (~3.6s), reduced-motion guard. + faint "เดิม" baseline opacity deepened-down 0.5→0.25 (dark) / 0.42→0.21 (light) so it stops fighting the skin tone.
- **v5.6 split-fix** (`/systematic-debugging`, Issue-1 regression I introduced): the red dashed "หลังฉีด" was the STROKE of the skin-filled body (one path/circle) → the breathe class animated element opacity → faded the WHOLE body ("หายทั้งอัน"). Root-caused in the real DOM; split into a static skin-fill element + a `fill="none"` red outline carrying the animation. V21: my own v5.6 test had locked the broken combined element — rewritten + anti-regression (skin body never carries the class).
- **v6** (`/brainstorming`+`/systematic-debugging`, Ultracode): (A) estimate too wide ("XL 60–68"). Geometry CORRECT (ISO 4074 width=½circ → width=girth×5; cross-section C₁=√(C₀²+4πV/L)); k-multipliers too high+wide vs HA RCT (PMC7230452: +2.5/+1.9/+1.4cm @1/6/18mo, k≈1.7–2.5). Recalibrated `K_REALISTIC 2.37→1.8`, `K_OPTIMISTIC 3.32→2.3` (integer-fraction form so the obfuscator hides it). 7"/30cc → girth 12.1–12.6 → "XL 60". (B) ladder→72: 66–72 REAL numbered sizes; "เกินมาตรฐาน 🔥" only past 72 (REAL_MAX_W). (C) FillerGraphic2D showAfter/showBaseline state + DashToggle chip (dashed swatch + label, ≥44px, touch-action) that DOUBLES as the legend, bottom-right, color-keys left, flex-wrap auto-scale; stronger glow (drop-shadow 4+9+15px); both dashed sets render conditionally.
- **Verified**: filler 98/0 (k recalibration + ladder-72 boundaries + v6 toggle source-grep; ~12 V21 fixups) + full vitest **16864/0** (1 transient parallel flake, 0 on re-run) + build clean (obfuscated) + **Rule Q L1 real browser** (7"/30cc→"XL 60 +4 ขนาด" girth 12.1–12.6; XXL64+50cc→"74–78 เกินมาตรฐาน 🔥"; both toggles hide/show their dash set; stronger glow in live CSS; mobile 375px no-overflow + 44px chips). **Honest gap (Rule Q-vis)**: preview_screenshot times out → rendered DOM text + behavior + live CSS + mobile geometry (not JPEG). **Workflow note**: both adversarial+research workflows rate-limited (4-parallel burst) → research done inline (cited: PMC7230452 · ISO 4074 · condom width sources). Specs `docs/superpowers/specs/2026-06-20-filler-{red-line-breathe-glow,v6-narrow-estimate-and-dash-toggles}-design.html` · checkpoint `.agents/sessions/2026-06-20-filler-v6-narrow-estimate-toggles.md`.

### Session 2026-06-20 (cont.) — Filler Simulator v5.3 + v5.4 + R9 (obfuscation · watermark · contact buttons) — SHIPPED local, NOT deployed

master `f5676bcb` (=origin; 4 commits: v5.3 `5d135a1d` · v5.4 `fb58c199` · R9 spec `b366829e` · R9 impl `f5676bcb`). prod UNCHANGED — `?play=filler` is a pure-client public page, still NOT deployed (await "deploy"; vercel-only, no Probe-Deploy-Probe). Continued the filler polish across 3 user rounds.
- **v5.3** (`/systematic-debugging`): root-caused the small-glans split-bar display bug — labels were trapped INSIDE the proportional flex segments, gated `>14%` width → hidden at glans 1/0.5, crammed at 1.5 → moved OUT to an always-visible color-keyed legend. + bigger centered cross-section, subtitle copy, iPad/touch hardening.
- **v5.4** (`/systematic-debugging`): (a) **round-DOWN results for safety** — `condomIndexForGirth` nearest-snap → FLOOR (largest size that fits) + `r1` Math.round → Math.floor (under-promise the result); (b) **auto-scale layout** — both equal-height cards distribute to fill (controls `space-between`; 2D restructured into flex sections side-view/cross-section/HTML-legend with `space-evenly` → no dead bands on any device). Desktop eval: equal-height 679=679, controls bottom-gap 18px, illustration fills its 584px region.
- **R9** (`/brainstorming` HARD-GATE → spec(html) → plan(html) → inline impl + 3-agent research Workflow): (1) **formula obfuscation** scoped to the 4 filler files via `vite-plugin-javascript-obfuscator`, `command==='build'` ONLY (vitest runs serve → bypassed → 16846 tests unaffected); rewrote the k-constants as integer fractions (237/100 etc, value-identical, tests still exact) so `numbersToExpressions` hides the floats; **dist grep: K_REALISTIC / 2.37 / 3.32 / 2.54 / "Super snug 45" / "Regular 52" / dCgeo / condomIndexForGirth ALL absent**; **obfuscated build served on :4173 → renders + computes IDENTICAL** (girth 10.4→11.5–11.9, condom Regular 52→Large 56–58, console clean) — Rule Q on the real shipped artifact. (2) **theme-aware clinic-logo watermark** — centered-faint `<image>` in BOTH 2D SVGs (travels with a screenshot) + a centered DOM `<img>` overlay over the 3D canvas (`pointer-events:none`, ~9% opacity; white logo on dark / black on light). (3) **contact buttons** — `CLINIC_CONTACT` const + reusable `ContactButtons` (header compact icons + footer full — Rule of 3), REAL brand SVG icons (official LINE + Facebook `f` + phone glyph), theme-aware (dark = filled brand + white icon / light = soft brand-tint + deepened brand icon-label + border, AA-safe), `tel:0975251525` · `https://lin.ee/mFFsDkG` · `https://www.facebook.com/loverclinickorat` (LINE/FB `target=_blank rel=noopener noreferrer`).
- **Verified**: targeted filler 79/0 → full vitest **16846/0** → `npm run build` clean → dist obfuscation grep (all formula constants absent) → **Rule Q L1** (dev unobfuscated: 6 contact links correct hrefs + real icons, 2D watermark ×2 + 3D overlay, theme swap white↔black + soft-tint; **prod obfuscated build :4173 renders + math identical**). **Honest gap (Rule Q-vis)**: `preview_screenshot` timed out repeatedly (env flake) → desktop fill proven via DOM-geometry; mobile 2D-watermark + header-icons captured. Spec/plan `docs/superpowers/{specs,plans}/2026-06-20-filler-r9-protect-watermark-contact*` · checkpoint `.agents/sessions/2026-06-20-filler-v53-v54-r9.md`.

### Session 2026-06-20 — Penile Filler Size Simulator (v1 + v2 + debug round) — SHIPPED local, NOT deployed

master `505961af` (=origin); prod UNCHANGED `lover-clinic-228lv6o7s` — filler-sim is a NEW pure-client public page, NOT deployed (await "deploy"; frontend-only → vercel-only, no Probe-Deploy-Probe). `/brainstorming` → spec×2 → `/writing-plans` ×2 → inline impl → `/systematic-debugging`. Full pipeline in one session per user's go-ahead ("ทำ plan ต่อ แล้ว implementation แล้ว test").
- **NEW page `?play=filler`** — pure client, **NO Firestore / no auth / no PII / no rules**. `three` lazy-loaded (3D only; initial bundle not bloated, chunk 524K isolated). 2 background web-research agents grounded the math (shaft girth + condom sizing; glans augmentation).
- **v1 (shaft)**: `C1=√(C0²+4π·shaftCc/L)×k` k 2.37/3.32 (anchor 16cc → +2.0/+2.8 VERIFIED via node + live browser) · EXACT condom (ISO 4074 `W=รอบวง×5`, Ø=รอบวง/π, 8-rung real ladder, snap nearest-tie-larger, snap-back consistent) · realistic 2D SVG + WebGL 3D rotatable · เดิม→ใหม่ result cards (รอบวง/Ø/ถุงยาง +N sizes).
- **v2**: glans split-bar (cc รวม + glans% + stacked ลำตัว|หัว) — glans does NOT affect condom (research 0.25cm Ø/cc, separate) · mushroom 2D/3D · TH/EN i18n (`fillerStrings.js`) · light/dark (clinic palette) · removed 18+ reveal-gate · length step 0.1in / max 10in(=25.4cm) / default 5in.
- **debug round** (`/systematic-debugging`, 6 items): 🐛 **3D glans grew with shaft** — `Math.max(glansR, shaftR)` clamp tied glans to injected shaft → fixed to floor-on-const + **damped `est.glans.visualLow`** (0.4×; research dgLow/dgHigh kept for honesty). Removed egg-tint ellipse + def, glans result card, "ทรงเห็ด" label. 🐛 **split-bar spill at glans=0** → `flex:0 0 pct%` + overflow hidden + conditional label (both segments).
- **Verified**: filler **39/0** targeted + build EXIT=0 (three lazy) + full vitest **16804/0** (v2 commit 2e31a292; debug-round targeted only, not re-run at EOD) + **Rule Q L1 real browser** (Claude Preview): values match fillerMath exactly (anchor +2.0/+2.8 live at cc16), 3 cards เดิม→ใหม่, split/glans/EN/theme/3D-mushroom all functional, 3D canvas+WebGL renders + disposes clean, no console errors. Honest gap (Rule Q-vis): rendered-pixel screenshot = USER eyes (preview_screenshot times out in this env — functional L1 complete). Checkpoint `.agents/sessions/2026-06-20-filler-simulator.md`.

### Session 2026-06-19 — AV98 ED-modal portal fix (modal trapped in its own box) — SHIPPED + DEPLOYED LIVE

master `574141ad` (=origin pre-EOD-docs); prod = `lover-clinic-228lv6o7s` (vercel frontend) aliased lover-clinic-app.vercel.app HTTP 200; firestore.rules UNCHANGED → frontend-only, no Probe-Deploy-Probe. `/systematic-debugging` + Workflow census (rate-limited ×2 → recovered inline per V83) → fix → test → deploy.
- **Root cause (AV98 recurrence)**: `EDScoreBox` renders `<EDDetailModal/>` INSIDE its own `rounded-xl` card; V86 auto-glow (index.css ~4043) → every rounded card in `[data-backend-menu-mode=new] [data-testid=backend-content]` is a containing block for `position:fixed` descendants → the 2-panel compare modal confined to the card box. Same class as recall V80; missed because that regression test was recall-DIR-scoped (the ED modals shipped 2026-06-15/18, after AV98).
- **Census (Rule P, evidence-based)**: EDDetailModal = the LONE trapped instance. Every other overlay modal renders at a tab/panel/page ROOT as a SIBLING of (not descendant of) rounded cards — verified WalletPanel root `space-y-4` (modals@232/245/257 siblings of the toolbar card); FinanceTab/DepositPanel/OrderPanel/report-tabs likewise; matches the AV98 sanctioned-exceptions list. Scoped fix, not a 30-file over-reach.
- **Fix**: `createPortal(…, document.body)` on `EDDetailModal` + `EDFollowupModal` (both `fixed inset-0` returns incl. the QR full-screen). AV98-canonical, byte-identical to the prod-proven recall/appt-popover portal.
- **Guard**: NEW `tests/av98-ed-modal-portal.test.js` (A ED modals portal + B EDScoreBox-nesting neutralized + C card-spawn registry — a new card-component-that-spawns-a-modal must be registered). Dropped a universal static "nested-in-rounded-card" walk: false-positive-prone (matched `.map()`-callback rounded cards → flagged WalletPanel/PointsPanel/SaleInsuranceClaimsTab/LineReminderHistoryPanel, all verified-safe). AV98 SKILL.md updated with the recurrence + census + cross-link.
- **Verified**: targeted 83/0 + full vitest **16767/0** + build clean. DEPLOYED `574141ad` → vercel-only. Honest gap (Rule Q): authed-CDV rendered-pixel = USER hands-on (auth-gated; same fix proven live for recall). Checkpoint `.agents/sessions/2026-06-19-av98-ed-modal-portal.md`.

### Session 2026-06-18 EOD+1 — ED 2-panel round compare + be_assessments backup coverage (+ be_wallets phantom fix) — SHIPPED + DEPLOYED LIVE

master `2e19ac01` (=origin); prod = `lover-clinic-a2f1vlim5` (vercel frontend) + `firebase deploy --only functions`; aliased lover-clinic-app.vercel.app HTTP 200; firestore.rules UNCHANGED → no Probe-Deploy-Probe. 2 features, each /brainstorming → spec(html) → plan(html) → inline impl → test → adversarial Workflow → Rule R diag + Rule M cleanup + Rule Q L2 e2e → deploy.
- **ED 2-panel round compare** (`303a85af`): `EDDetailModal` → side-by-side compare. Left/right swap via `useLayoutPreference('ed-compare')` (TFP split-screen pattern); per-panel round pickers; type tabs (ADAM/IIEF/MRS/PE); latest "ล่าสุด" + today "วันนี้" hero badges so staff don't misread; changed-row highlight. **REMOVED ดีขึ้น/แย่ลง trend** (user) — keep only highlight + both scores. **Badge no-squeeze** (flex:none + centered + nowrap; picker truncates) per user. NEW pure `src/lib/edCompare.js`: `autoPickCompareRound(rounds, primary, type)` (nearest prior round measuring `type`, later fallback, excludes primary) + `markChangedRows` (align by `n`). Compare picker filtered to `r.id !== primary.id && r.types.includes(activeType)`; primary re-anchors on cross-device delete; activeType re-validated ∈ tabs. AV78 backdrop no-close; ESC/✕ close.
- **be_assessments backup coverage** (`f9c4d36b`): new ED `be_assessments` (2026-06-15) missing from EVERY backup/cascade scope → V122-class drift. Added to `CUSTOMER_CASCADE_COLLECTIONS_FULL` (→17) + `CUSTOMER_ONLY_UNIVERSAL` + `branchBackupCore.UNIVERSAL`. **Single-source reconcile**: `backendClient.CUSTOMER_CASCADE_COLLECTIONS` = `…_FULL` (was a drifting inline 11) → client-delete + server-delete can't diverge. NEW **drift-catcher** test (every customerId-queried be_* ⊆ FULL ∪ allowlist) — it immediately paid off (caught be_wallets). Whole-system FULL already dynamic `listCollections()` (V122) → auto-covered.
- **be_wallets phantom fix** (same commit): cascade listed `be_wallets` (0 docs / no rule / no accessor); the real store is `be_customer_wallets` (composite id `customerId__walletTypeId`, money-adjacent) → renamed across cascade/delete/backup/scripts so customer delete no longer ORPHANS wallet balances.
- **Rule M (APPLIED on prod)**: Rule R diag found 5 orphans (4 be_assessments for deleted LC-26000155 + 1 be_customer_wallets for deleted 2853) → `cleanup-orphan-assessments.mjs --apply` deleted 5 + audit `cleanup-orphan-assessments-1781799289079-01e2d08b`; idempotent (re-run → 0).
- **Verified**: ed-compare 11/0 · ed-detail-modal 21/0 · be-assessments-backup-coverage (A–E + drift-catcher) · **Rule Q L2 e2e 9/0 REAL prod** (`scripts/e2e-be-assessments-backup-roundtrip.mjs`) · full vitest **16756/0** (2 transient timeout flakes confirmed green on re-run) · build clean. Honest gap (Rule Q): ED-compare L1 pixel on authed CDV = USER hands-on. Checkpoint `.agents/sessions/2026-06-18-ed-compare-and-backup-coverage.md`.

### Session 2026-06-18 — ED box (round-select + detail modal + viewing-state) + follow-up push name+HN — SHIPPED local, NOT deployed

master `92c5ab0b` (=origin); prod UNCHANGED (`00da035d`) — user ended session, no deploy. 3 features, each /brainstorming or /systematic-debugging → spec → plan → inline impl → test → adversarial Workflow.
- **ED chip round-select**: click a history row → chips show that round's snapshot + header "กำลังดู: ครั้งที่ N" + "← ไปที่ครั้งล่าสุด"; default (no selection) = merged latestPerType UNCHANGED (Q2=A, additive).
- **ED chip → per-question detail modal** (EDDetailModal): click ADAM/IIEF/MRS/PE → full question text + customer's answer (option label) for that round; score banner; AV78 explicit-close (X/ESC, backdrop no-close). NEW canonical `src/lib/edQuestions.js` (questions + per-q options + `buildEdAnswerRows`).
- **viewing-state for PAST rounds only** (/systematic-debugging, ultrathink): selecting the latest (hero) = default home view (no redundant back button, no stuck-state); derived `viewingPast = selectedRound && selectedRound.id !== hero.id` gates header/chips/highlight/button; robust to delete-renumber. Rewrote R6/C1-C5 to the corrected model + new C6.
- **Follow-up push name+HN** (Cloud Function): pure CJS `notificationContent.js` + `customerDisplay.js` (CJS mirror of ESM canonical); `functions/index.js` reads `be_customers/{linkedCustomerId}` live (canonical BASE_PATH, non-fatal) → "🔔 {name} · HN {hn}"; intake+edit unchanged. HN = resolveCustomerHN(hn_no) || linkedCustomerId.
- **Verified**: ed-score-round-select 15/0 · ed-detail-modal 10/0 · ed-questions 9/0 · notification-content 15/0 (ESM parity) · **Rule R real-prod 2/2** (real follow-ups → correct name+HN) · full vitest **16722/0** · build clean.
- **Adversarial**: notification WF (5 lenses; cf-wiring/parity/regression CLEAN) → 5 defensive findings fixed (trim whitespace name/title + compose-either intake) + locked N11-13. ED-detail WF stalled (no result — RTL 10/0 + chip-click cover it). 2 stray agent-probe files (test-*.mjs) removed pre-commit (not committed).
- **Deploy when authorized (V18): frontend → `vercel --prod`; notification → `firebase deploy --only functions`** (no rules change → no Probe-Deploy-Probe). Honest gap (Rule Q): ED L1 pixel + notification L1 real-push = USER hands-on after deploy. Checkpoint `.agents/sessions/2026-06-18-ed-box-detail-and-push.md`.

### Session 2026-06-16 EOD+2 — Mobile-load reliability — SHIPPED + DEPLOYED + L1-verified LIVE

master `d54d58c4` (=origin); prod = frontend `lover-clinic-p4uawr0kx` (lover-clinic-app.vercel.app, HTTP 200) — frontend-only, firestore.rules UNCHANGED → vercel-only, no Probe-Deploy-Probe. User: mobile บางทีโหลดไม่ติดในทีเดียว (จอดำ/ค้างโหลด/skeleton ไม่มีดาต้า) ทั้ง staff + ทุกลิงก์ลูกค้า → ต้อง refresh. `/brainstorming`(Q1 no-cache / Q2 auto-retry+ปุ่ม / Q3 all-layers+L2)→spec→`/writing-plans`(14 tasks)→inline→**adversarial Workflow loop (Ultracode)**. **Fix**: `firebase.js`→`initializeFirestore({experimentalAutoDetectLongPolling:true})` (big lever — heals half-dead WebSocket; no persistence) + NEW `firestoreReconnect.js` (shared debounced toggle) + `useResilientLoad.js` (8s soft-timeout→auto-retry→ปุ่มลองใหม่; sync settledRef; resetKey) + `LoadErrorRetry.jsx`; wired App.jsx (resilient anon-auth gate kills black-screen) + PatientForm/ClinicSchedule/PatientDashboard + AdminDashboard (resetKey:branch) + useBranchAwareListener (auto-heal) + BackendDashboard (Suspense retry). **Loop CONVERGED**: R1 race→sync settledRef; R2 resetKey gap; R3 orphaned-timer (MY R2 fix's bug, re-hunt caught) → resetKey in timer deps; R4 = 0 findings. **Verified**: full vitest **16673/0** (utils.test.js 1 transient parallel flake, green isolated+re-run) + build clean + **L1 Playwright 3/0 on the LIVE deployed build** (iPhone-13) + **L2 7/0 real prod** (both transports). **Lessons**: real "stuck" = half-dead conn that HANGS (route.abort serves fromCache, doesn't repro); fix recovers via disableNetwork→fromCache at 8s no-refresh; sync-guard a setTimeout-read ref; the loop caught my own fix's bug; agents overstate severity → adjudicate. Checkpoint `.agents/sessions/2026-06-16-mobile-load-reliability.md`.

### Session 2026-06-16 EOD+1 — Dup-customer prevention (Rule T) + Recall name/delete/date fixes — DEPLOYED + 3 dup pairs resolved

master `c78378a9` (=origin); prod = frontend `lover-clinic-gpxsr048v` (lover-clinic-app.vercel.app) + firestore.rules DEPLOYED. 7-item user request → `/brainstorming`→spec→`/writing-plans`(14 tasks) + design-hardening (4-agent census) + adversarial-review (20 agents → found+fixed 2 HIGH bugs). **Part A (Rule T dup-prevention):** `addCustomer` (single chokepoint) claims `be_customer_identity/{CITIZEN:|PASSPORT:key}` + counter + customer-doc in ONE `runTransaction` (`deriveClaimKey`/`resolveClaimAction` in `src/lib/customerIdentity.js`) → throws DUPLICATE_IDENTITY → `CustomerCreatePage` warn modal (เปิดของเดิม / บันทึกซ้ำอยู่ดี→`linkedCustomerIds`+`_duplicateOfCustomerId`) + phone soft-hint; `updateCustomerFromForm` frees-old+claims-new (oldKey RE-READ in-tx → race-safe); `deleteCustomerCascade`+server free/promote claim; OPD/deposit → `addCustomerOrLinkExisting` (DUPLICATE→link existing, real HN); CDV dup badge. **Part B (recall):** `overlayRecallNames`/`useEnrichedRecalls` live-resolve name at the recall-load chokepoint → "—" fixed in 3 lists + 5 modal headers (already clickable); `RecallRow` 📞 โทรอีกครั้ง / 📅 เลื่อนนัด date chip (dd/mm/yyyy พ.ศ.); `be_recall_cases` delete narrowed `if false`→isClinicStaff; `RecallCreateModal` resolver. **Rule-M APPLIED on prod:** backfill 128 claims + 131 denorm (audit `…a068d36f`) + nuke 7 TEST-CASE junk + **3 dup pairs resolved → 0** (`fix-dup-customer-pairs.mjs`: LC-125 recall MOVED→LC-123, LC-069 empty deleted/keep LC-074, pair3 test LC-143+155 both deleted; audit `…efab0a08`). **Verified:** full vitest **16609/0** + build clean + L2 e2e 16/0 real prod + EXECUTION test + Probe-Deploy-Probe 8/8 (opd_sessions anon→200 intact). **LESSON:** first footprint MISSED `be_recalls` (NOT in CUSTOMER_CASCADE_COLLECTIONS) — user caught it → `feedback_full_customer_footprint_before_delete.md`. AV196/AV197. Honest gap (Rule Q): anon-deny PROVEN (Probe #17); staff-client-SDK + backend-authed UI = USER L1. Checkpoint `.agents/sessions/2026-06-16-dup-customer-recall.md`.

---

📂 **Older sessions (`2026-06-16` and earlier) + older Current-State index entries → `.agents/sessions/session-handoff-archive.md`** (cold storage, NOT read at boot).
