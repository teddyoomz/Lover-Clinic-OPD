# Session 2026-05-07 EOD continuation — V38 + V39 + V38-followup + e2e + V40 spec

## Summary
5-commit single-day session shipping 4 production code fixes (V38 + V39 + V38-followup + comprehensive e2e) + 1 design spec (V40). All driven by 2 user bug reports + 1 feature request: (a) "ลบ products + courses บน พระราม 3 ไม่ได้" (V38 silent-no-op via spread-order override) → multi-layer fix + 479-doc Rule M backfill; (b) "นำเข้าสินค้า/คอร์ส/โปรโมชั่น เข้าพระราม 3 ไม่ได้" (V39 migrate-button branchId silently dropped) → 4 wrappers + 4 mappers patched + V38 source-patch on cross-branch-import adapters + comprehensive 122/122 e2e on real prod Firestore; (c) brainstorming session for branch Backup/Restore/Make-Fresh feature → V40 spec approved via 6-Q&A flow + implementation plan ready.

## Current State
- master = `496a15c` · 6757/6757 tests pass · build clean · prod = `e36811f` (V38+ NOT deployed)
- 5 commits ahead of prod (V38, V39, V38-followup, e2e, V40 spec)
- Production data clean: 479 zombies stamped พระราม 3 via Rule M backfill; user deleted 5+2 V38 broken docs via post-fix delete; 29 e2e TEST docs cleaned up
- V40 implementation plan at `C:\Users\oomzp\.claude\plans\sprightly-jumping-waterfall.md` (~30 tasks, 7 phases)

## Commits (oldest → newest this session)

```
4f008a3 fix(phase-24-0-vicies-novies-novies): list spread-order V12 multi-reader fix (V38) — handleDelete silent no-op
d964b14 fix(phase-24-0-vicies-novies-decies): migrate-button branchId stamping (V39) — promotions/coupons/vouchers/df_staff_rates + cross-branch-import V38 source patches
ee40256 fix(v38-followup): mass-sweep spread-order across 17 files (AV17 complete) — 85+ callsites swapped
b33f369 test(v39-decies): comprehensive e2e — 19 migrate buttons × happy + adversarial × deep mapping (122/122 PASS on real prod Firestore)
496a15c docs(spec): branch backup/restore/make-fresh design (2026-05-07)
```

Plus this EOD doc commit.

## Files Touched (names only — diffs in commits)

**V38** (`4f008a3`): `src/lib/backendClient.js` (listProducts + listCourses spread swap) · `scripts/diag-pram3-products-courses.mjs` (NEW diag) · `scripts/phase-24-0-vicies-novies-novies-backfill-product-course-id.mjs` (NEW Rule M backfill, 7 docs applied) · `tests/phase-24-0-vicies-novies-novies-list-spread-order.test.js` (NEW) · `tests/phase-24-0-vicies-novies-novies-flow-simulate.test.js` (NEW) · `.claude/rules/00-session-start.md` (V38 entry) · `.claude/rules/v-log-archive.md` (V38 verbose) · `.agents/skills/audit-anti-vibe-code/SKILL.md` (AV17)

**V39** (`d964b14`): `src/lib/backendClient.js` (4 wrappers + 1 mapper) · `src/lib/phase9Mappers.js` (3 builders) · `api/admin/cross-branch-import.js` (generic canonicalIdField stamp) · 7 `src/lib/crossBranchImportAdapters/*.js` (canonicalIdField + strip-stray-id) · `scripts/diag-migrate-branch-stamping.mjs` (NEW) · `scripts/phase-24-0-vicies-novies-decies-backfill-zombie-branchid.mjs` (NEW + 479 docs applied) · `tests/phase-24-0-vicies-novies-decies-migrate-button-coverage.test.js` (NEW 70 tests) · `.claude/rules/00-session-start.md` (V39 entry) · `.agents/skills/audit-anti-vibe-code/SKILL.md` (AV18)

**V38-followup** (`ee40256`): 17 files via sed mass-swap — `backendClient.js`, `reportsLoaders.js`, 6 admin endpoints, 5 components, 2 pages, AV17 audit doc

**E2E** (`b33f369`): `scripts/e2e-migrate-all-buttons.mjs` (NEW comprehensive 19-button × 30-fixture × 122-assertion runner) · `src/lib/backendClient.js` (14 mapper exports for Node import)

**V40 spec** (`496a15c`): `docs/superpowers/specs/2026-05-07-branch-backup-restore-make-fresh-design.md` (NEW 374-line spec)

**V40 plan** (NOT in repo): `C:\Users\oomzp\.claude\plans\sprightly-jumping-waterfall.md`

## Decisions (one-line each)
- **V38 root cause** = spread order `{id: d.id, ...d.data()}` letting stray `data.id` (legacy ProClinic numeric) override docId → handleDelete silent no-op. Fix = swap to `{...d.data(), id: d.id}` so docId always wins.
- **V38 mass-sweep deferred originally** — done in V38-followup commit (user authorized "go all"). Net: 85+ callsites swapped, full suite 6757/6757 unchanged.
- **V39 root cause** = octies fixed 7 catalog mappers (products/courses/groups/units/instruments/holidays/df_groups) but missed 4 older Phase 9 wrappers (promotions/coupons/vouchers/df_staff_rates) that were ZERO-ARITY → handleMigrate forwarded `{branchId}` and the wrappers silently dropped it.
- **V38 actual source** = `cross-branch-import.js` endpoint (NOT `branch-merge-apply.mjs`/`customer-branch-baseline.js` as initially suspected). 7 adapters now define `canonicalIdField` + clone strips stray `id`; endpoint generically stamps `cloned.id = newId` + `cloned[canonicalIdField] = newId`.
- **E2E approach** = export 14 mappers from backendClient.js + write Node script that simulates runMasterToBeMigration with admin SDK + TEST-prefixed master_data + verify shape + cleanup. 100% pass on first try (1 fixture bug self-caught, fixed, re-ran).
- **V40 design Q&A** (6 locked): Q1 both modes (overwrite + clone), Q2 tier groups + Advanced expand, Q3 Storage + local download, Q4 T1-only clone, Q5 type-name + auto-pre-fresh backup, Q6 admin-only.

Full V-entry detail: `.claude/rules/v-log-archive.md` (V38 verbose section).

## Lessons (V38/V39 cluster)
1. V12 multi-reader-sweep applies to migrate-fn family AND list spread-order. Octies missed 4 older Phase 9 wrappers (V12 pattern at family level); listProducts+listCourses missed in V38 too (V12 at callsite level). Both fixed.
2. Octies addressed wrong root cause (visibility, not delete-id-resolution). Rule I gap: end-of-sub-phase flow-simulate MUST chain user click → handleDelete → write path.
3. "Success badge with no actual write" UI bug class — migrate fn returned `{imported: N}` truthfully while data lacked the contract field. Diag is the only catch (preview_eval against real Firestore per Rule I item b).
4. Cross-branch-import adapter `canonicalIdField` is now the contract — every new adapter MUST define it.
5. Diag-first paid off: `diag-pram3-products-courses.mjs` revealed exact shape difference between working (นครราชสีมา) and broken (พระราม 3) docs in <30 seconds. Rule M includes diagnostic scripts as first-class tool.
6. Comprehensive e2e (19 × 30 × 122) on real prod with TEST-prefixed cleanup is the right "เทส e2e จริงๆ" pattern. Self-caught a TEST FIXTURE bug mid-run + fixed → 100% pass.

## Next Todo
Idle. Awaiting user directive in new chat:
- **V40 implementation**: subagent-driven execution per plan (recommended), or inline; ~30 tasks, 7 phases
- **OR Deploy** V38+V39+V38-followup+e2e+spec to Vercel (user says "deploy" → combined `vercel --prod` + `firestore:rules` Probe-Deploy-Probe)
- **OR new directive**

## Resume Prompt
See `SESSION_HANDOFF.md` `## Resume Prompt` block.
