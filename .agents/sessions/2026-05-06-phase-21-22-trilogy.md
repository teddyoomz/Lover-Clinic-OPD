# 2026-05-06 EOD — Phase 21.0 trilogy + Phase 22.0 trilogy (10 commits)

## Summary

Continuation EOD session 3. Started at master=`79084bc` (post Phase 20.0). Shipped 10 commits across Phase 21.0 family (appointment sub-tabs incl. ทุกประเภท overview + embedded deposit subform + UI polish + position-stable refactor for empty-grid bug) + Phase 22.0 trilogy (sync-status reset migration LIVE-APPLIED on prod, kiosk modal branch correctness with deposit-pair wiring, schedule + clinic-prefs branch separation). Ended at master=`d378cf5`. ~60 commits ahead-of-prod.

## Current State

- master=`d378cf5` · prod=`024f6dd` FROZEN (V15 #22 LIVE 2026-05-05) per no-deploy directive 2026-05-06
- 5857 PASS / 5862 total · build clean · firestore.rules v26 unchanged
- Local-only workflow (no Vercel deploys; data ops via Rule M admin-SDK from local)
- Phase 22.0a migration **LIVE-APPLIED on prod**: 768 docs status-flipped, 0 deletions, audit doc `be_admin_audit/phase-22-0a-sync-status-reset-1778057983371-ceadb4fe`

## Commits (chronological — 10 ahead of session start)

```
d378cf5 feat(phase-22-0c): AdminDashboard schedule + clinic-prefs branch separation
2cec108 feat(phase-22-0b): kiosk modal branch correctness + deposit-pair wiring
e16ed7b feat(phase-22-0a): sync-status reset migration + tests + APPLIED on prod
62fdb65 docs(phase-22-0a): SAFETY revision — preserve all pc_* data, clear syncedAt only
8340e19 docs(phase-22-0a): spec — sync-status reset migration
c9794e4 feat(phase-21-0-septies): purpose row matches customer-name size for visual parity
777c51d feat(phase-21-0-quinquies+sexies): UI polish — Finance.มัดจำ purpose column + calendar grid clean-up
9590e57 feat(phase-21-0-ter+quater): embedded deposit subform in modal + position-stable sub-tab refactor (fix empty-grid bug)
4e6a9e4 feat(phase-21-0-bis): add 'นัดหมายทุกประเภท' overview sub-tab at top of section
86b1df7 fix(phase-21-0): hotfix TDZ ReferenceError in AppointmentCalendarView (blank screen on sub-tab click)
```

## Files Touched (top-level)

- src/components/backend/AppointmentCalendarView.jsx — typedDayAppts hoist (TDZ fix), 5 visual upgrades, occupied-cell border skip
- src/components/backend/AppointmentFormModal.jsx — embedded deposit subform + lockedAppointmentType prop + redirect-banner removal
- src/components/backend/DepositPanel.jsx — Finance.มัดจำ "มัดจำสำหรับ" column
- src/components/backend/nav/navConfig.js — 5 appointment sub-tabs (incl. ทุกประเภท overview)
- src/lib/tabPermissions.js — 5 sub-tab gates
- src/pages/BackendDashboard.jsx — position-stable single-element render with computed appointmentType prop
- src/pages/AdminDashboard.jsx — fetchDepositOptions branch-filter + assistants populate + confirmCreateDeposit pair-wire + clinic_schedules.branchId stamp + per-branch schedule_prefs + updateActiveSchedules per-schedule branchId query
- scripts/phase-22-0a-reset-sync-status.mjs (NEW) — Rule M migration, 768 docs flipped on prod
- 5 NEW test files (phase-21-0-quinquies-visual-polish + phase-22-0a/b/c) + 4 test patches

## Decisions (one-line each)

- **Phase 21.0 TDZ hotfix**: typedDayAppts useMemo MUST be declared BEFORE rooms useMemo (rooms references typedDayAppts in its hasOrphan check; const TDZ violation on first render → React error boundary → blank screen). Hoist + add to deps.
- **Phase 21.0-bis**: "นัดหมายทุกประเภท" overview at index 0 of section; renders AppointmentCalendarView WITHOUT appointmentType prop (typeFilter resolves null → typedDayAppts === dayAppts).
- **Phase 21.0-ter**: embedded deposit subform replaces redirect-to-Finance.มัดจำ banner per user "ทำให้สร้างได้ตั้งแต่หน้าจองมัดจำเลย ... ก็มีช่องให้กรอกรายละเอียดการมัดจำไปด้วยไง ง่ายๆ".
- **Phase 21.0-quater (position-stable)**: collapse 5 ternary branches into ONE position with computed appointmentType prop (React reuses instance across sub-tab clicks; pre-fix each sub-tab click re-mounted → dayAppts state reset → empty grid).
- **Phase 21.0-quinquies+sexies**: Finance.มัดจำ "มัดจำสำหรับ" column + calendar SLOT_H 18→22 + status-color 4px left-border + 🎯 purpose chip + per-cell borders that skip occupied cells (eliminates stripes through translucent block area per user "เอาเส้นขาวๆในพื้นที่สีส้มออกไปปป").
- **Phase 21.0-septies**: purpose row text-sm font-bold (was text-[10px] font-semibold) — same size as customer name per user "ใหญ่พอๆกะชื่อเลยก็ได้". Color (emerald) differentiates from name.
- **Phase 22.0a SAFETY revision**: Q3 reversed from B (delete pc_*) → A (clear syncedAt only) per user safety directive "อย่าลบข้อมูลลูกค้าใน frontend นะเว้ย แค่ให้หบุด sync นะเว้ยย ข้อมูลสำคัญมากนะ". ALL pc_* docs preserved; only syncedAt field flipped + forensic trail. Spec amended + committed before script written.
- **Phase 22.0b deposit-pair wiring**: kiosk จองมัดจำ now writes BOTH be_deposits + be_appointments via createDepositBookingPair (matches DepositPanel + BackendDashboard AppointmentFormModal pattern). Pre-22.0b kiosk write was opd_sessions-only → invisible to Finance.มัดจำ + BackendDashboard sub-tab. assistants dropdown was BROKEN (depositOptions.assistants never populated) — fixed by mirroring backend's "any doctor can be an assistant" pattern.
- **Phase 22.0c per-schedule branchId**: schedule docs stamp branchId at create; updateActiveSchedules keys appts by `${month}|${branchId}` per-schedule (each schedule's bookedSlots resync uses ITS OWN branchId, not the admin's current selectedBranchId). schedule_prefs__{branchId} doc-id pattern → per-branch prefs (closed days, doctor schedule, custom hours).

## V-entries

None new — all changes follow established Rule M / Rule L / V12 patterns. Phase 21.0 TDZ near-miss documented inline in AppointmentCalendarView.jsx with "🚨 ORDER MATTERS" comment block (institutional memory at the source).

## Acceptance gates

- Phase 21.0 main: per-branch × per-type matrix 8/8 PASS (16 TEST-APPT-* fixtures, 0 leakage) — verified 2026-05-06 earlier session
- Phase 21.0-quater: live preview_eval via React fiber dispatch — sub-tab clicks preserve state (3 blocks → 1 block at 100ms; switch back → 3 blocks at 100ms)
- Phase 22.0a: dry-run reported 768 to flip; --apply committed; idempotent re-run yields 0 to flip; audit doc written
- Phase 22.0b/c: source-grep contracts (23 + 16 tests) lock the shape against future drift

## Next Todo

Idle. Open NEW chat for next directive. Likely candidates:
- H-bis ProClinic full strip (delete brokerClient.js + api/proclinic + cookie-relay + MasterDataTab + clinic_settings/proclinic_session*)
- Manual-sync-to-be_* UI button (post-22.0a data prep complete)
- /audit-all pre-release pass

## Resume Prompt

See SESSION_HANDOFF.md Resume Prompt block.
