# Checkpoint — 2026-05-31 EOD+4 LATE+3 — V142 course double-deduct + V143 stock cluster SHIPPED + DEPLOYED

## Summary
Two `/systematic-debugging` families shipped + deployed together: **V142** (course
double-deduct on finalize→doctor→finalize) and the **V143 stock cluster** (show
products at 0, real-time balance, auto-clear-lot). NK stock reset to all-0 (1
lot/product) applied on prod via Rule M. master = prod = `0c607f68` LIVE.

## Current State
- master = prod = `0c607f68` @ https://lover-clinic-app.vercel.app (deployed this session; was `8c3a9047`).
- Full vitest **15418/0** (JSON-reporter run) + build clean (✓ 2.82s). Working tree clean.
- Frontend/lib + 1 NEW cron (`stock-lot-cleanup`, 03:45 BKK) → no firestore.rules/storage/index change → no Probe-Deploy-Probe.
- NK (นครราชสีมา) stock = 53 products = 53 lots, all at 0 (Rule M applied earlier this session, audited).
- No open work; awaiting next directive.

## Commits (this session, newest first)
```
0c607f68 test(stock): V21 fixup — v34 INV.11.4 balance-reader grep → V143-ter listener form
9d3646e3 test(stock): V21 fixup — v138 N10.7 balance-reader grep → V143-ter listener form
a7449029 feat(stock): V143-ter real-time balance + V143-quater auto-clear-lot system
b5e4b8eb chore(stock): V143-bis — collapse นครราชสีมา multi-lot products to 1 lot (Rule M)
32a59605 fix(stock): V143 — show products cleared/drained to exactly 0 in ยอดคงเหลือ + NK reset dry-run
f8fc0555 fix(treatment): V142-quinquies — finalize→doctor→finalize DOUBLE-DEDUCT (persisted _courseDeducted flag)
00927810 fix(treatment): V142-quater — doctor→finalize course OVER-CREDIT gate (priorSaveDeducted)
```

## Files Touched (names only)
- src/components/TreatmentFormPage.jsx (V142-quinquies `_courseDeducted` flag — state, edit-load, courseItems gate, priorSaveDeducted, persist)
- src/lib/backendClient.js (V143-ter `listenToStockBatchesByBranch` — Layer 1, onSnapshot BS-13 safe-by-default)
- src/lib/scopedDataLayer.js (V143-ter Layer 2 auto-inject wrapper)
- src/components/backend/StockBalancePanel.jsx (V143 active+depleted filter; V143-ter migrated to live listener)
- src/lib/stockLotCleanupCore.js (NEW — `planLotCleanup`, DELETE-only)
- api/cron/stock-lot-cleanup.js (NEW — daily cron, CRON_SECRET-gated)
- scripts/stock-lot-cleanup.mjs (NEW — Rule M dry-run/--apply, --branch)
- scripts/e2e-stock-balance-realtime.mjs (NEW — L2 e2e 5/0 real prod)
- scripts/v143-nakhon-stock-reset.mjs + v143-collapse-nk-multilot.mjs + diag-nakhon-stock-state.mjs
- vercel.json (cron + maxDuration for stock-lot-cleanup)
- tests: stock-lot-cleanup.test.js (NEW 14/0) + v143-stock-balance-show-depleted.test.js + v138 N10.7 + v34 INV.11.4 fixups
- .agents/skills/audit-anti-vibe-code/SKILL.md (AV165 course flag, AV166 show-0, AV167 real-time listener, AV168 lot cleanup)

## Decisions (1-line each; full reasoning → v-log-archive.md when V-entry batched)
- V142-quinquies: persist `_courseDeducted` in `detail` (round-trips create/update/getTreatment); drives `priorSaveDeducted` (replaces fragile status heuristic). Doctor/vitals saves preserve it (course-neutral).
- Doctor-save must NOT deduct courses — bottom TFP save owns course-deduction (user directive).
- V143 show-0: `status∈{active,depleted}` (exclude cancelled/expired). Depleted-at-0 = cleared/drained → still real stock the admin keyed in.
- V143-ter real-time: BS-13 safe-by-default listener (resolve branchId; empty if none + !allBranches). Layer-2 auto-inject mirrors V76 chat-history pattern.
- V143-quater lot-cleanup: per product×location keep all LIVE lots + ≤1 zero placeholder, DELETE the rest. DELETE-only (never mutate qty). Daily cron mirrors stock-movement-retention.
- 3 V21 fixups are ONE migration class (panel `listStockBatches`→`listenToStockBatchesByBranch`); full suite (JSON reporter) confirmed exactly 3 instances, no 4th.

## Rule Q verification (honest)
- L2 (real client SDK / real prod): V142 flag-roundtrip/stock e2e 30/0; V143-ter `e2e-stock-balance-realtime.mjs` 5/0 (create/deduct/drain-0/delete from another surface push live to the SHIPPED listener).
- Helper/source-grep + Rule I simulate: stock-lot-cleanup 14/0, v143-show-depleted 24/0, full vitest 15418/0, build clean.
- **Honest gap → USER L1**: assembled 2-device real-browser (open ยอดคงเหลือ on 2 devices, deduct on one, both update live) + NK products visible at 0. Code is L2-verified; the multi-device UI assembly is the user's gold-standard hands-on.

## Next Todo
- None pending. User L1 hands-on (above). Optional: hit `/api/cron/stock-lot-cleanup` once with CRON_SECRET to verify live (like V122).

## Resume Prompt
```text
Resume LoverClinic OPD — continue from 2026-05-31 EOD+4 LATE+3.

Read in order BEFORE any tool call:
1. CLAUDE.md
2. SESSION_HANDOFF.md (master=0c607f68, prod=0c607f68 LIVE)
3. .agents/active.md (15418 tests)
4. .claude/rules/00-session-start.md (iron-clad + V-summary)
5. .agents/sessions/2026-05-31-v142-v143-deploy.md

Status: master=0c607f68, 15418 tests pass, prod=0c607f68 LIVE.
Next: idle — await directive. (V142 course double-deduct + V143 stock show-0/real-time/auto-clear-lot all DEPLOYED.)
Outstanding (user-triggered): L1 hands-on (2-device live balance + NK shows 0); cron stock-lot-cleanup now active (03:45 BKK).
Rules: no deploy without "deploy" THIS turn (V18); V15 combined; Probe-Deploy-Probe on rules; Rule Q L1/L2 before "verified".
/session-start
```
