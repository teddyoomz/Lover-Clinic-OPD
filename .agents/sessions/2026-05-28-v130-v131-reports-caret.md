# 2026-05-28 EOD+5 — V130 + V131 + V131-bis SHIPPED + DEPLOYED

## Summary
One brainstorm→spec→plan→execute cycle (V130) + four `/systematic-debugging` rounds (V131 HN, table-height, appt-name, caret-browsing). Two deploys: `bd925fe8` (held V128/V129/V130 batch) then `c070123d` (V131 + V131-bis). All frontend/lib/CSS only → no Probe-Deploy-Probe. prod LIVE = `c070123d`.

## Current State
- master = prod = `c070123d` LIVE @ lover-clinic-app.vercel.app; working tree clean.
- Full vitest (this session) **15106 pass + 1 isolated flake** (staff-chat-lightbox-cached-image-race 200-iter stress; 7/0 isolated; unrelated to touched files). Build clean.
- HN L2-confirmed on real prod; caret/cursor confirmed via live-prod computed-style + user F7 test.
- firestore.rules/storage/index/cron UNCHANGED all session.
- V130 backfill `--apply` HELD (needs explicit Rule-M authorization).

## Commits
```
c070123d fix(ui,reports): V131 + V131-bis — HN canonical, fill-height, clickable name, cursor/caret
bd925fe8 feat(reports,appointments,staffchat): V128+V129+V130 batch (deployed)
```

## Files Touched (V130 + V131 + V131-bis)
- src/lib/{saleReportAggregator,customerReportAggregator,backendClient}.js
- src/components/backend/{CustomerDetailView,CustomerListTab,BulkPrintModal,AppointmentFormModal,AppointmentDetailBody,AppointmentDetailPopover}.jsx
- src/components/backend/reports/SaleReportTab.jsx · src/index.css
- scripts/{v130-backfill-sale-created-by,verify-v130-sale-created-by,diag-hn-resolution}.mjs
- tests/{v130-sale-created-by,v130-reports-sale-responsive,v131-hn-canonical-resolve,v131-appt-modal-clickable-name,v131-bis-app-cursor}.test.{js,jsx}
- .agents/skills/audit-anti-vibe-code/SKILL.md (AV148, AV149, AV150, AV151, AV152)
- docs/superpowers/{specs,plans}/2026-05-28-reports-sale-createdby-and-responsive-table*

## Decisions (1-line each)
- V130 createdBy: chokepoint resolver (V108 lineage) — caller-override → be_staff(firebaseUid) → displayName/email → none; report prefers snapshot → live-resolve id → legacy → first-seller → '-'. AV149.
- V130 table: compact + height-cap; FB2 later replaced fixed max-h-[70vh] with JS-measured fill (innerHeight - wrapperTop - 16). AV148.
- V131 HN: hardcoded `proClinicHN||hn` omits `hn_no` (109/109 real HNs there) → canonical `resolveCustomerHN` across 6 files. AV150.
- V131 appt-name: clickable only in MODAL (onOpenCustomer + appt.customerId via openCustomerInNewTab); peek/pick-later plain; cyan not red. AV151.
- V131-bis caret: root cause = browser Caret Browsing (F7), user-confirmed. App can't toggle the setting; `html{caret-color:transparent}` + inputs `auto` hides it app-wide except inputs; `body{cursor:default}` for the mouse I-beam; user-select untouched (copy). AV152.
- Q-honest lessons: (1) diagnosed wrong twice on caret (verified mouse-cursor on the wrong page; real cause was caret-browsing not reproducible in a clean browser) → fixed by user F7 confirm + computed-style. (2) earlier I claimed V130 table "shell flex blowout" — real-browser disproved (shells already min-w-0); dropped the no-op shell change.

## Next Todo
- Idle / await user.
- (user) V130 backfill `--apply` if legacy createdBy persistence wanted (optional; display unchanged).
- (user) L1 hands-on: createdBy on a new logged-in sale; reports-sale HN + fill-height on Windows-scaled screen; appt-modal clickable name; caret hidden with F7 on.

## Resume Prompt
```text
Resume LoverClinic — continue from 2026-05-28 EOD+5.
Read in order BEFORE any tool call: CLAUDE.md · SESSION_HANDOFF.md (master=c070123d, prod=c070123d) · .agents/active.md · .claude/rules/00-session-start.md · this checkpoint.
Status: master = prod = c070123d LIVE; working tree clean; full vitest 15106 pass + 1 isolated flake (staff-chat-lightbox stress, unrelated); build clean.
Next: idle.
Outstanding (user-triggered): V130 backfill --apply (49 legacy sales createdBy=first-seller, OPTIONAL) · L1 hands-on (createdBy / reports-sale HN+height / appt clickable name / caret w/ F7).
Rules: no deploy without "deploy" THIS turn (V18); V15 combined; Rule Q real-adversarial verify; Probe-Deploy-Probe for rules.
/session-start
```
