# Session 2026-05-14 EOD — Phase 27 saga (branchId + display + layout + badges)

## Summary

Phase 27.x complete. ~22 commits across 8 sub-phases: data-layer branchId attribution + doctor/assistant/branch live-resolve + TFP layout swap + unified sticky header + stacked lifecycle badges. Two prod migrations applied via Rule M (18 treatments backfilled + 4 customer summaries rebuilt). One V12 multi-reader-sweep regression caught + fixed at end (Phase 27.2-sexies = round 3 on the same file).

## Current State

- master = `9819c2e` · prod = `e8086de` (~32 commits ahead, NOT DEPLOYED)
- 9013 tests + 1 skipped + 0 fail. Build clean.
- Firestore: 18 treatments have `detail.branchId` stamped; 4 customers' `treatmentSummary` arrays have new lifecycle fields.
- Audit docs: `phase-27-0-backfill-1778702714166-e147e108` + `phase-27-2-rebuild-summaries-1778706768985-549bbd7d`.

## Commits this session (high-level)

```
9819c2e fix(Phase 27.2-sexies): CDV mapper V12 multi-reader-sweep — TRUE root cause
be6fb26 fix(Phase 27.2-quinquies): CDV dep-array fix (kept; symptom-level)
44ba976 feat(Phase 27.2-quater): Rule M migration — 4 customer summaries rebuilt
c5acfca feat(Phase 27.2-bis/ter): always-editable save buttons + tolerant badge fallback
7cd425f feat(Phase 27.2): stacked lifecycle badges with timestamps
b978011 fix(Phase 27.0-bis): optimistic branch refresh after Edit modal save
2955218 fix(Phase 27.1-sexies): center history tabs + sticky offset fix
cc4c83c feat(Phase 27.1-quinquies): merge history tabs INTO unified header
... (earlier 27.0 + 27.1 + V55 brutal pre-deploy commits)
```

## Files Touched (top-level)

**Source**:
- `src/lib/treatmentDisplayResolvers.js` (NEW — resolve doctor/assistant/branch from live maps)
- `src/lib/backendClient.js` (createBackendTreatment + updateBackendTreatment top-level extraction; rebuildTreatmentSummary new lifecycle fields)
- `src/components/TreatmentFormPage.jsx` (branchId stamp at submit; per-stage timestamps; unified header; layout swap integration)
- `src/components/backend/CustomerDetailView.jsx` (stacked badges; mapper sync; listener dep-array fix)
- `src/components/backend/TreatmentReadOnlyMirror.jsx` + `Panel.jsx` (resolver migration + ✏️ branch edit trigger)
- `src/components/backend/EditAttributionModal.jsx` (EditTreatmentBranchModal redesigned with proper modal chrome)
- `src/hooks/useLayoutPreference.js` (NEW) + `src/components/LayoutSwapButton.jsx` (NEW)

**Migration + diagnostic scripts**:
- `scripts/phase-27-0-backfill-treatment-branch-id.mjs`
- `scripts/phase-27-2-rebuild-treatment-summaries.mjs`
- `scripts/diag-phase-27-2-customer-summary.mjs`

**Tests** (new + updated):
- `tests/phase-27-0-*.test.{js,jsx}` (resolver unit + property-based + audit AV42 + flow-simulate + edit-attribution RTL + migration helper)
- `tests/phase-27-1-*.test.{js,jsx}` (hook unit + property-based + LayoutSwapButton RTL + TFP integration source-grep)
- `tests/phase-27-2-quinquies-cdv-flash-revert-fix.test.js`
- `tests/phase-27-2-sexies-cdv-mapper-multi-reader-sweep.test.js`
- 7+ V21-class test fixups across pre-existing files

**Docs**:
- `docs/superpowers/specs/2026-05-14-treatment-branch-id-and-name-resolution-design.md`
- `docs/superpowers/specs/2026-05-14-tfp-layout-swap-design.md`
- `docs/superpowers/plans/2026-05-14-phase-27-combined-branch-attribution-and-layout-swap.md`

## Key Decisions

- **Brainstorming locked 4 design Qs each for 27.0 + 27.1** — BranchSelector source, live-resolve > cache > never raw ID, customer.branchId heuristic for backfill, EditAttributionModal allows branchId change.
- **Subagent-driven execution** for 13 tasks. 2-stage review per task (spec + code quality). 7 V21-class test fixups detected by reviewers + RP1 audit.
- **Phase 27.2-sexies as TRUE root cause** — V12 multi-reader-sweep on CDV's in-component mapper. 4 fix attempts to find the real bug; per Rule P Phase 4.5 questioned architecture and ran admin-SDK firestore diagnostic to verify data state vs render state. Mirrors Phase 26.0e + 26.1 oversight pattern on the same file — class-of-bug round 3.
- **Optimistic local override pattern** for EditTreatmentBranchModal (branchIdOverride state in Mirror) — display refreshes immediately on save without waiting for parent refetch.
- **AV42 audit invariant** locks treatment display resolver discipline (Rule O class extension).

## Lessons (Rule D)

- **V12 multi-reader-sweep STRIKES THRICE on same file** — CDV's in-component mapper stripped `status` (26.0e), `editedBy*` (26.1), now lifecycle timestamps (27.2). Comment at line 497-504 even WARNED future devs about this exact pattern. Comment wasn't enough; structural fix needed (extract shared mapper between writer and reader = follow-up Phase 27.2-septies candidate).
- **Test in browser before claiming "fixed"** — user feedback "ทำไมมึงไม่เทสมาวะ" was earned. Should have used `mcp__Claude_Preview__preview_eval` + admin-SDK diag BEFORE committing each fix attempt. The admin-SDK diag revealed the data was correct in Firestore but the render mapper stripped it — would have caught it on round 1.
- **Iron-clad I (full-flow simulate) caught only helper-output**; the actual data chain `treatments[] → useMemo mapper → useMemo → paginatedTreatments → JSX` was not exercised. Future Phase 27.2-septies if user wants: write a Rule I flow-simulate that mounts CDV with realistic treatments + asserts the badge JSX renders all expected timestamps.

## Next Todo

- **(user)** explicit "deploy" for combined V15 push of ~32 commits
- **(optional follow-up)** Phase 27.2-septies — extract shared `buildTreatmentSummaryEntry(t)` helper to backendClient.js; both rebuildTreatmentSummary writer AND CDV in-component mapper consume it. Eliminates V12 multi-reader-sweep structurally.
- **(optional follow-up)** Phase 27.x e2e via mcp Preview tool — navigate backend → customer detail → verify all 3 lifecycle stages render with timestamps.

## Resume Prompt

See SESSION_HANDOFF.md "Session 2026-05-14 EOD — Phase 27 saga SHIPPED" block.
