# 2026-05-28 EOD+4 — 4-feature batch (V128 + V129), UNCOMMITTED + held

## Summary
5× `/systematic-debugging` in one session → 4 fixes, each per-feature-verified (Rule Q L1/L2). **Feature code is UNCOMMITTED** (held per user "ยัง deploy ไม่ได้ มีงานถัดไป"); only agent-docs committed this EOD. All 4 are frontend/lib only → ONE combined `vercel --prod` (no Probe-Deploy-Probe) when the user authorizes.

## Current State
- master code HEAD `10e28ed4` (V127 docs) + this EOD docs commit on top. prod `26fb5789` (V127) — **none of the 4 features deployed**.
- Working tree: 17 modified + 7 new feature files (NOT committed). `git status` tomorrow shows them.
- Tests (NO re-run at EOD per rule): last FULL `npm test` 15039/0 (pre-lightbox+reports-sale); targeted since GREEN — lightbox-family 104/0, v128 7/0, v129 12/0; build clean ×several.
- Each feature L2/L1 verified individually; FULL suite re-run is the deploy-gate (not yet run since the lightbox + reports-sale edits).
- Diag/verify scripts kept (Rule R): diag-appt-phone-display, verify-v128-appt-phone, diag-sale-report-seller-creator, verify-v129-sale-report-seller-creator.

## Commits
```
(feature code uncommitted — held)
EOD docs commit: docs(agents): EOD 2026-05-28 V128+V129 batch (uncommitted, held)
```

## Files touched (uncommitted)
- src/lib/{customerDisplayName.js (resolveCustomerPhone), backendClient.js (_resolveAppointmentCustomerPhone create+update), saleReportAggregator.js (resolveSellerName)}
- src/hooks/useResolvedApptPhone.js (NEW)
- src/components/backend/{AppointmentDetailBody.jsx, AppointmentHoverPeek.jsx, AppointmentDetailPopover.jsx, AppointmentCalendarView.jsx (slotH)}
- src/components/backend/reports/{SaleReportTab.jsx, SaleDetailModal.jsx}
- src/components/staffchat/StaffChatImageLightbox.jsx (full-screen + pan-zoom)
- tests: v128-appt-phone-and-grid-height, v128-staffchat-lightbox-size, v129-sale-report-seller-creator-resolve (NEW) + 5 V21 fixups (appt-calendar-density, phase-19-0-grid-15min-cell, phase-21-0-quinquies-visual-polish, phase15.7-septies, staff-chat-any-file, v115-mobile-lightbox)
- .agents/skills/audit-anti-vibe-code/SKILL.md (AV145, AV146, AV147)
- scripts: diag-appt-phone-display, verify-v128-appt-phone, diag-sale-report-seller-creator, verify-v129-sale-report-seller-creator (NEW)

## Decisions (1-line each)
- V128 phone: V108-class — write-chokepoint denorm (new/edited) + render live-resolve (legacy 78); customer phone at `patientData.phone`; pick-later `customerPhoneTemp` preserved.
- V128 calendar: `max-w`/`max-h` only CAP — fill needs explicit `width/height` (the lightbox lesson too); SLOT_H→dynamic slotH, clamp 22–46.
- V128.lb lightbox: full-screen via `width/height:100%` + `object-contain` UPSCALES (max-* never upscales — the 2× "verified-but-small" miss); pan = `translate(pan) scale(zoom)` + clampPan + pointer drag + wheel; verified in real browser (Chrome MCP) per V107/Q-vis.
- V129 reports-sale: V108-class — report read raw `sellers[].name` (empty 38/49) + raw `createdBy` (empty 49/49); resolve via `resolveSellerName(listAllSellers)`; staffSalesAggregator = working reference.
- createdBy never captured in be_sales → ผู้ทำรายการ = resolved first seller; true-creator capture = offered follow-up.
- All held uncommitted per user; commit only at user "commit"/"deploy" word.

## Next todo
- On user word: full `npm test -- --run` → commit the 4-feature batch → `vercel --prod` (frontend-only).
- L1 hands-on (auth-gated, user): appt hover phone · calendar fill 2K · lightbox · reports-sale columns.
- Optional: capture true `createdBy` at sale-write (V129 follow-up).

## Resume Prompt
```text
Resume LoverClinic — continue from 2026-05-28 EOD+4.
Read BEFORE any tool call: CLAUDE.md · SESSION_HANDOFF.md (code HEAD 10e28ed4, prod 26fb5789) · .agents/active.md · .claude/rules/00-session-start.md · this checkpoint.
Status: 4-feature batch (V128 appt-phone + calendar-height + V128.lb lightbox + V129 reports-sale) DONE + per-feature-verified but UNCOMMITTED + held (no deploy). Tests last FULL 15039/0; targeted since GREEN; build clean.
Next: idle — OR on user word: full npm test → commit batch → vercel --prod (frontend-only, no Probe-Deploy-Probe).
Rules: no deploy without "deploy" THIS turn (V18); V15 combined; Rule Q real-adversarial verify.
/session-start
```
