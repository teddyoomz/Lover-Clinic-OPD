# Session 2026-05-04 — Phase BS shipped + Phase BS V2 master-data branch-scoped

## Summary
Big multi-stream session. V15 #15 shipped Phase BS (top-right BranchSelector + customer baseline migration UI + 5 lister refactors + 132 new tests). Mid-session 3 production regressions surfaced (sales/treatments empty / stock raw ID / appointment branch switch ignored) — all fixed. User clarified scope: Phase BS V2 — every master-data tab must be branch-scoped except Universal list (พนักงาน / สิทธิ์ / เทมเพลต / แพทย์ / สาขา / ตั้งค่าระบบ / Sync ProClinic). 11 listers + 8 writers + 9 UI tabs + 1 admin endpoint refactored. 2833 prod docs migrated via admin SDK (LIVE).

## Current State
- master = `cf897f6` · prod = `83d8413` (V15 #15 LIVE) · **3 code commits ahead-of-prod**
- 4744/4744 tests pass · build clean · firestore.rules v24 (idempotent next deploy)
- 2833 prod docs migrated to นครราชสีมา via admin SDK (LIVE NOW, no code deploy needed for the data part)
- Phase BS V1 LIVE; Phase BS V2 master-data tabs on master awaiting V15 #16

## Commits
```
cf897f6 feat(branch-bs-v2): master-data tabs branch-scoped per user directive
aecf3a1 fix(branch): listenToAppointmentsByDate + listenToAllSales now branch-scope
da57c08 fix(stock): listStockLocations now pulls be_branches — show name not raw ID
1aeaff4 docs(agents): Phase BS shipped — V15 #15 LIVE, master=83d8413, 4744 tests
83d8413 feat(branch-selector): Phase BS — top-right tab + per-staff branchIds[] soft-gate + customer baseline migration
```

## Files Touched

**Phase BS V1 (`83d8413`)** — 14 modified + 4 new + 8 new tests:
- src/lib/{BranchContext.jsx, branchScopeUtils.js (NEW), branchSelection.js (NEW), backendClient.js, clinicReportAggregator.js, cloneOrchestrator.js, customerBranchBaselineClient.js (NEW), customerValidation.js}
- src/components/{TreatmentFormPage.jsx, backend/AppointmentFormModal.jsx, backend/AppointmentTab.jsx, backend/BranchSelector.jsx, backend/CustomerCreatePage.jsx, backend/CustomerDetailView.jsx, backend/FinanceMasterTab.jsx, backend/MasterDataTab.jsx, backend/QuotationFormModal.jsx, backend/QuotationTab.jsx, backend/SaleInsuranceClaimsTab.jsx, backend/SaleTab.jsx}
- api/admin/customer-branch-baseline.js (NEW)
- tests/branch-selector-bs-{a..h}*.test.js (8 NEW) + 6 existing tests adjusted

**Stock + listener fixes (`da57c08`, `aecf3a1`)**:
- src/lib/backendClient.js (listStockLocations + listenToAppointmentsByDate + listenToAllSales)
- src/components/backend/AppointmentTab.jsx

**Phase BS V2 (`cf897f6`)** — 14 files:
- src/lib/backendClient.js (11 listers + 8 writers via _resolveBranchIdForWrite helper)
- src/lib/linkRequestsClient.js
- src/components/backend/{ProductGroupsTab, ProductUnitsTab, MedicalInstrumentsTab, HolidaysTab, ProductsTab, CoursesTab, DfGroupsTab, DoctorSchedulesTab, EmployeeSchedulesTab, FinanceMasterTab, LinkRequestsTab}.jsx
- api/admin/link-requests.js

**Migration scripts** (kept in scripts/, not deployed):
- scripts/branch-merge-diagnostic.mjs (read-only inventory)
- scripts/branch-merge-apply.mjs (admin SDK direct writes — backfill branchId)

## Decisions
- BR-1777873556815-26df6480 IS "นครราชสีมา" (default branch). User confusion came from stock page raw-ID display bug. Source = target identity for backfill.
- Universal tabs (NOT branch-scoped per user spec): พนักงาน / สิทธิ์การใช้งาน / เทมเพลตเอกสาร / แพทย์ & ผู้ช่วย / สาขา / ตั้งค่าระบบ / Sync ProClinic.
- Migration policy: empty/missing/'main'/V35-phantom → migrate to นครราชสีมา. ADV-/TEST-/etc test prefixes preserved (separate cleanup task).
- LineSettingsTab deferred — single global config doc needs schema redesign for per-branch (branchOverrides[] map OR be_line_configs collection).
- be_link_requests reader added legacy-fallback shim (untagged requests still surface in default branch view) — webhook writer untouched, so new writes are still untagged.
- Customer doc branchId IMMUTABLE on update — both opts AND form stripped before write. Backfill happens via dedicated admin endpoint, never through edit.
- V36.G.51 satisfied via NEW pure JS module branchSelection.js — backendClient + cloneOrchestrator no longer import .jsx (no React leak into data layer).

## Next Todo
1. **V15 #16 deploy** — vercel + firebase rules (idempotent). User must type "deploy" THIS turn (V18).
2. After V15 #16 LIVE, verify in prod browser: switch BranchSelector to "พระราม 3" → all master-data tabs show empty + stock page shows "นครราชสีมา"/"พระราม 3" name not raw ID.
3. Optional: extend webhook /api/webhook/line.js to stamp branchId on new be_link_requests creates (currently relies on legacy-fallback shim in handleList).
4. Optional: design per-branch LineSettings (branchOverrides[] map OR new collection).
5. Phase BS V3 (if needed): hard-gate via Firebase custom claim.
6. Phase 17 plan TBD.

## Resume Prompt
See `SESSION_HANDOFF.md` Resume Prompt block at top. Status: master=cf897f6, prod=83d8413 LIVE (V15 #15), 4744 tests, 3 code commits ahead.
