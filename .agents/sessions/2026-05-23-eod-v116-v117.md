# EOD 2026-05-23 LATE+4 — V116 DEPLOYED + V117 LOCAL (lightbox portal)

## Summary

Two V-sagas back-to-back. V116 (link-survives-queue-delete + auto-regen + un-hide-on-re-engage) shipped + deployed via `/systematic-debugging` + Rule J brainstorming. V117 (createPortal mandate for 5 fullscreen lightboxes) shipped local after user caught V115 mobile lightbox STILL not closing despite the V115 fix — root cause was nested position:fixed bounded by StaffChatPanel ancestor on iOS Safari. V117 pushed; awaits deploy authorization.

## Current State

- master = `f43ab792` (V117 commit)
- Vercel prod = `3612d8ae` (V115 + V116 + V116-followup LIVE)
- V117 PENDING deploy
- V117 self-tests 11/11 + V83+V117 28/28 + Phase 17.1 isolated 7/7 + build clean
- Cloud Run office-to-pdf-00007-tfb (V110-bis) — unchanged

## Commits

```
f43ab792 fix(lightbox): V117 — fullscreen lightboxes MUST createPortal to body [AV117]
3612d8ae fix(opd-link): V116 — link survives queue-delete + auto-regen + un-hide on re-engage [AV116]
1b6b3330 docs(agents): EOD 2026-05-23 — V114 deployed + V115 mobile lightbox local
```

## Files Touched

V116 (already in `2026-05-23-eod-v114-v115.md` continuation):
- MOD `src/lib/appointmentDepositBatch.js` (existence-check + un-hide)
- MOD `src/pages/AdminDashboard.jsx` (deleteSession + auto-expire + 3 queue filters + walk-in gate)
- NEW `tests/v116-link-survives-queue-delete.test.js` (26 tests)
- MOD `.agents/skills/audit-anti-vibe-code/SKILL.md` (NEW AV116, 3 rules)

V117:
- MOD `src/components/staffchat/StaffChatImageLightbox.jsx` (createPortal)
- MOD `src/components/staffchat/StaffChatPdfOverlay.jsx` (createPortal)
- MOD `src/components/backend/TreatmentReadOnlyMirror.jsx` (createPortal)
- MOD `src/components/backend/TreatmentReadOnlyPanel.jsx` (createPortal)
- MOD `src/components/ChartSection.jsx` (createPortal + V21 marker reorder for V83 M2.1)
- NEW `tests/v117-lightbox-portal.test.js` (11 tests)
- MOD `.agents/skills/audit-anti-vibe-code/SKILL.md` (NEW AV117)

## Decisions (one-line each — full reasoning → v-log-archive.md at next major update)

- V116 Q1: queue-delete conditional preserve (link survives if `linkedAppointmentId || linkedDepositId`); hard-delete only when standalone "เหมือนกดผิด"
- V116 Q2: silent self-heal in provisionOpdLinkForBookingPair via existence-check before idempotent short-circuit
- V116 Q3: walk-in gate adds `createdFromBackendBooking` as 6th indicator (defense-in-depth)
- V116 Q4: architectural backstop in provision helper covers 3 broken sites + future drift
- V116-followup: un-hide on re-engage (user catch — admin needs Review surface immediately after re-clicking ดูลิ้ง, not waiting for customer fill)
- V117 Q1: full class-of-bug expansion (5 lightboxes) per Rule P — user explicit "Full class-of-bug expansion"
- V117 Q2: AV117 invariant locks closed sanctioned list of 5 + portal mandate; companion to AV114 (mobile UX gates)
- V117 root cause: iOS Safari nested position:fixed inside StaffChatPanel (`position:fixed; z-9000; overflow:hidden`) gets bounded → close button unreachable; createPortal to document.body bypasses all ancestor CSS

## Next Todo

1. **V117 deploy** when user authorizes — `vercel --prod` only (client-only, no rules/indexes/Cloud Run).
2. **Rule Q L1 hands-on iPhone** post-V117-deploy: 4 scenarios:
   - V115/V117 mobile: open staff chat image → backdrop tap closes + ✕ tappable below notch + double-tap zoom + iOS pinch native.
   - V116 new flow: send link → admin deletes from queue → re-clicks ดูลิ้ง → SAME URL + queue entry reappears.
   - V116 legacy victim: มนทวัฒน์ + สันติสุข click ดูลิ้ง → auto-regen new session + URL/QR.
   - V116 customer-fill auto-restore: hidden session + customer fills form → queue auto-reshows entry.
3. (If all clear → V115/V116/V117 saga closes + V-log archive entries can be written at next session.)

## Outstanding User-Triggered Actions

- V117 deploy authorization (when ready).
- Post-deploy iPhone L1 hands-on for V117 + V116 (4 acceptance scenarios above).

## Resume Prompt

```
Resume LoverClinic — continue from 2026-05-23 EOD+1 LATE+4.

Read in order BEFORE any tool call:
1. CLAUDE.md
2. SESSION_HANDOFF.md (master=f43ab792, prod=3612d8ae)
3. .agents/active.md (V117 LOCAL, awaiting deploy)
4. .claude/rules/00-session-start.md (iron-clad + V-summary)
5. .agents/sessions/2026-05-23-eod-v116-v117.md (this checkpoint)

Status: master=f43ab792, V117 11/11 PASS + Phase 17.1 known flake, prod=3612d8ae (V116 LIVE)
Next: V117 deploy when authorized → user L1 iPhone hands-on (4 scenarios)
Outstanding (user-triggered): V117 deploy auth · post-deploy iPhone L1 for V117 + V116
Rules: no deploy without "deploy" THIS turn (V18); V117 client-only (no rules); P-D-P NOT needed
/session-start
```
