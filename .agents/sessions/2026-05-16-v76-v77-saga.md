# 2026-05-16 EOD+1 LATE — V76 + V77 saga (chat per-branch close + 📦 backup button + V51 migration gap)

## Summary

V75 deploy earlier this session passed 8 layers of mock tests but failed Rule Q L1 — user's hands-on surfaced 5 distinct bugs across 5 rounds. Every round: I claimed-fixed, user reported still broken, I dug deeper. The session closed with V76+V77 (a/b/c/-bis/-ter) DEPLOYED + 2 Rule M backfills applied + 3,282 docs stamped. Iron-clad reminder per V66: claim ≠ verified; only L1/L2 evidence counts.

## Current State

- master = `f609f41` · prod LIVE (Vercel + Firebase rules v35 deployed 2026-05-16T12:33Z)
- ~280+ V75/V76/V77 assertions PASS across ~20 test files
- 2 Rule M backfills RAN: V76 (3,281 chat_history → นครราชสีมา), V77-bis (1 chat_conv empty-branchId → นครราชสีมา)
- AV56/57/58/59 + BS-17 + Probe #12 + V21 fixups all locked
- Rule Q L1 hands-on by user PENDING (4 scenarios in active.md Next action)

## Commits (this session — V76 → V77-ter)

```
f609f41 fix(V77-ter): isChatActive reads V51 per-branch chat hours (cs.chatHours*)
<sha>   fix(V77-bis): hardcoded fallback in webhook resolvers + backfill empty-branchId
<sha>   test(V76+V77 Rule Q L1): Playwright e2e for chat tab branch isolation
<sha>   feat(V77): 📦 สำรองลูกค้าทุกคน button — whole-fleet backup endpoint + UI
<sha>   feat(V77a): RIP frontend chat config — admin configures per-branch in Backend
<sha>   fix(V76): chat_history BSA — close V12 multi-reader-sweep at sibling reader+writer
(plus session 1 V75 commits earlier this same day)
```

## Files Touched (V76 → V77-ter — names only)

**Source (8)**:
- src/lib/backendClient.js (listenToChatHistoryByBranch Layer 1)
- src/lib/scopedDataLayer.js (Layer 2 wrapper)
- src/components/ChatPanel.jsx (history reader migration + writer stamp + ConnectionSettings rip)
- src/components/backend/WholeFleetBackupModal.jsx (NEW)
- src/components/backend/BackupManagerTab.jsx (📦 button wire)
- src/pages/AdminDashboard.jsx (V77-ter isChatActive V51 field migration)
- api/admin/whole-fleet-customer-backup-export.js (NEW)
- api/webhook/_lib/{lineChatBranchResolver,fbChatBranchResolver}.js (V77-bis hardcoded fallback)

**Scripts (3)**:
- scripts/v76-backfill-chat-history-branchid.mjs (NEW Rule M)
- scripts/v77-bis-backfill-empty-branchid-chat-conversations.mjs (NEW Rule M)
- scripts/diag-v75-chat-state.mjs + diag-v76-live-chat-doc.mjs + diag-v76-chat-hours.mjs (Rule R diag)

**Tests (4)**:
- tests/v76-chat-history-branch-scope.test.js (28)
- tests/v77-whole-fleet-backup-endpoint-and-ui.test.js (27)
- tests/v77-ter-chat-active-v51-field-migration.test.js (14)
- tests/e2e/v76-chat-branch-isolation.spec.js (Playwright, 7 scenarios)

**Config**: vercel.json (maxDuration:300 for whole-fleet endpoints)

**Audit + rules**:
- .agents/skills/audit-anti-vibe-code/SKILL.md — AV59 (chat_history BSA discipline)
- .claude/rules/00-session-start.md — V76 compact V-entry
- .claude/rules/v-log-archive.md — V75 verbose entry (V76/77 verbose deferred — class-of-bug pattern noted in V76 compact + this checkpoint)

## Decisions (one-line each)

- V76 hardcoded fallback chosen over env-only — webhook would have leaked again if env unset on next Vercel re-config (defense-in-depth)
- V77a hard rip of ConnectionSettings (vs deprecate-with-warning) — user explicit "ตัดหน้านี้ออกไป" + legacy clinic_settings/chat_config doc preserved for V75 auto-seed contract
- V77b vercel maxDuration:300 — enterprise plan supports; for >5000-customer clinics, document CLI fallback (no timeout)
- V77-bis hardcoded NAKHON_BR_ID constant in resolver code (not env-driven) — guards against forgotten Vercel env config; same pattern as V40/V74 hardcoded paths
- V77-ter V51 field migration — kept legacy cs.chatOpenTime fallback chain (backward-compat for envs that haven't merged); future refactor: drop legacy after 30-day soak

## Class-of-bug lock

V12 multi-reader-sweep at COLLECTION FAMILY level (V76 chat_conversations vs chat_history) + V51 per-branch settings migration gap (V77-ter — AV29-class). Three Rule Q L1 misses this session prove the principle: source-grep tests are necessary but not sufficient. Mock tests passed 8+ layers; user L1 hands-on found 5 bugs.

## Next Todo

1. **User Rule Q L1 hands-on** on prod (4 scenarios in active.md)
2. (next session) Full vitest run (Rule N batch-end)
3. (next session) V76 / V77 verbose V-entries in v-log-archive.md (compact landed in 00-session-start; verbose deferred — class-of-bug pattern fully captured in V76 compact + this checkpoint)
4. (V75-bis backlog) WholeFleetRestoreModal UI + Playwright L1 specs for whole-fleet roundtrip + admin-SDK e2e against real prod

## Resume Prompt

See SESSION_HANDOFF.md Resume Prompt block (next-session entry point).
