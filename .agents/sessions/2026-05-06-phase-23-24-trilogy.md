# 2026-05-06 EOD — Phase 23.0 frontend modal fixes + Phase 24.0 customer-delete suite

## Summary

Marathon EOD continuation. Fast-forward merged side-branch (Phase 21.0 + 22.0 trilogies, 15 commits), then shipped Phase 23.0 kiosk modal fixes (3 user bugs, 6 commits) + Phase 24.0 customer-delete feature end-to-end (10 sub-phases, ~25 commits) including: cascade delete 11 collections, dual perm gate, server endpoint + client-side fallback, identity-based dedup recovery, Thai gender translation, customer_type auto-set, emergencyRelation canonical field, หมายเหตุทั่วไป prominent box. Ended at master=`4240abc`. ~50 commits ahead-of-prod.

## Current State
- master=`4240abc` · prod=`024f6dd` FROZEN (V15 #22 LIVE 2026-05-05) per no-deploy directive
- 6056/6056 tests pass · build clean · firestore.rules v26 unchanged
- Local-only workflow (no Vercel deploys; data ops via Rule M admin-SDK from local)
- ~50 commits ahead-of-prod across Phase 21/22 (merged) + Phase 23 + Phase 24

## Commits (chronological — top of session)

```
4240abc feat(phase-24-0-nonies+decies): kiosk gender + customer_type + relation + note box
79d3ea1 fix(phase-24-0-septies+octies): out-of-sync recovery + identity-based dedup
e6220cc fix(phase-24-0-sexies): audit-doc write is BEST-EFFORT — don't block delete
5a39d0d fix(phase-24-0-quinquies): force-refresh ID token before audit-doc setDoc
d4eceab fix(phase-24-0-quater): graceful-skip 5 rule-locked cascade collections
f5be877 fix(phase-24-0-ter): customer-delete client wrapper goes direct-Firestore
850aba0 feat(phase-24-0-bis): collapse 3-dropdown authorizer → 1 dropdown optgroup
521fd09 fix(phase-24-0/quality-review): audit-doc snapshot size guard + onDeleted recovery
ea61291 feat(phase-24-0/issue-1): modal cascade preview row + action='preview' branch
6b3ace2 fix(phase-24-0/issue-3): server endpoint accepts customer_delete claim
... 9 prior Phase 24.0 main commits (1-7 + 8-11 + 12-16 test bank + 11-bis + Issue#1+#3)
... 4 Phase 23.0 commits (channel dropdown + branchId + sync resilience + cache version)
... 15 Phase 21/22 commits (merged via fast-forward from claude/unruffled-heyrovsky-f68428)
```

## Files Touched (top-level)

- `src/lib/kioskPatientToCanonical.js` — Thai gender mapping; customer_type='ลูกค้าทั่วไป'; contact_1_relation canonical
- `src/lib/customerValidation.js` — GENDER_OPTIONS [M,F,LGBTQ,'']; FIELD_BOUNDS contact_1_relation/contact_2_relation; gender normalize accepts Thai/EN
- `src/lib/backendClient.js` — buildPatientDataFromForm + buildFormFromCustomer mirror contact_1_relation; CUSTOMER_CASCADE_COLLECTIONS shared constant
- `src/lib/customerDeleteClient.js` (NEW Phase 24.0; rewritten Phase 24.0-ter) — direct-Firestore wrapper; graceful-skip rule-locked collections; identity-aware
- `src/components/backend/DeleteCustomerCascadeModal.jsx` (NEW) — 1-dropdown optgroup; cascade preview row; ProClinic-cloned warning
- `src/components/backend/CustomerCard.jsx` — inline ✕ icon (cloned-mode + perm-gated)
- `src/components/backend/CustomerDetailView.jsx` — prominent ลบลูกค้า button; formatGenderTh helper; หมายเหตุทั่วไป amber box
- `src/components/backend/CustomerListTab.jsx` — modal state + transient delete-success banner
- `src/components/backend/CustomerCreatePage.jsx` — LGBTQ+ option + ความสัมพันธ์ input
- `src/pages/AdminDashboard.jsx` — tryUpdateExistingCustomer helper + findExistingCustomerByIdentity helper; appointmentChannels key + schemaVersion cache; explicit branchId stamps; kioskPatientToCanonical wiring at 3 sites; identity-aware recovery branches
- `src/pages/BackendDashboard.jsx` — DeleteCustomerCascadeModal mount + onDeleteCustomer wiring
- `api/admin/delete-customer-cascade.js` (NEW) — admin-SDK server endpoint; action=preview branch; snapshot size guard
- `api/admin/_lib/adminAuth.js` — verifyAdminOrPermissionToken helper (Phase 24.0 Issue #3)
- `src/lib/permissionGroupValidation.js` — customer_delete perm key
- `src/lib/scopedDataLayer.js` — findCustomersByField universal re-export
- `firestore.rules` — be_admin_audit/customer-delete-* narrow create exception
- 5 NEW phase-24-0-* test files + 2 docs (spec + plan) + numerous test contract updates

## Decisions (one-liner each — full reasoning in v-log-archive.md)

- Phase 24.0 audit doc → server-side admin-SDK + atomic batch (atomicity > UX speed for destructive op)
- 1-dropdown authorizer (Phase 24.0-bis) — UX simplification per user; server-derived role from source map
- Direct-Firestore client wrapper (Phase 24.0-ter) — local-only directive forced fetch→Firestore SDK; server endpoint preserved for future deploy
- Graceful-skip 5 rule-locked collections (Phase 24.0-quater) — per-collection try/catch instead of Promise.all atomic-fail; audit doc records cascadeSkipped for completeness
- Audit doc best-effort (Phase 24.0-sexies) — perm gates UX action; rules-claim is defense-in-depth, not gatekeeper; PERMISSION_DENIED on audit no longer blocks cascade
- Identity-based dedup (Phase 24.0-octies) — citizen_id/passport/phone match before re-create; tie-break to highest-confidence (most-matched-fields wins); ambiguous → admin disambiguates
- customer_type='ลูกค้าทั่วไป' (Phase 24.0-nonies) — kiosk default; thai/foreigner moved to customer_type_2
- contact_1_relation canonical (Phase 24.0-nonies) — promoted from note-stuffed kludge to FIELD_BOUNDS; backward-compat via reverse-mapper fallback to legacy emergencyRelation

## V-entries

None new. All fixes follow established V12 (multi-reader sweep), V21 (source-grep can lock broken behavior), V31 (silent-swallow), V37 (no `git add -A`) patterns. Phase 24.0-ter+quater documented in commit bodies; verbatim Thai user reports preserved.

## Acceptance gates

- 6056/6056 tests pass (incl. previously-flaky phase15.5b PF.4 also recovered this run)
- Build clean (no MISSING_EXPORT, no syntax errors, no dead imports)
- Phase 24.0 5 test files: 83 tests covering helpers + RTL + flow-simulate + rules probe + perm key drift catcher
- HN-no-reuse property regression-locked via F1.2/F1.3/F5.1/F5.2 simulate
- Live verified: customer delete from UI works; modal shows cascade counts; identity-lookup resolves to existing on duplicate

## Next Todo

Idle. Open new chat for next directive. Likely candidates:
- H-bis ProClinic full strip (delete brokerClient.js + api/proclinic + cookie-relay + MasterDataTab + clinic_settings/proclinic_session*)
- BackendDashboard nav restructure (deferred from Phase 20.0)
- /audit-all pre-release pass

## Resume Prompt

See SESSION_HANDOFF.md Resume Prompt block.
