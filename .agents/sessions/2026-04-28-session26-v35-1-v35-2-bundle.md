# 2026-04-28 (session 26 EOD) — V35.1 + V35.2 + tris/quater/quinquies/sexies bundle

## Summary

Long auto-mode session triggered by 5 user reports post-V15 #4 deploy.
Shipped 4 commits across V35.1 + V35.2 sub-phases addressing dropdown UX,
phantom-product cleanup, partial-commit prevention, and a treatment-form
null-customer crash. **2783/2783 tests · 4 commits ahead of prod**.

## Current State

- master = `72bf0ca` · production = `c36888e` (V15 #4 LIVE)
- 4 commits unpushed-to-prod: 8ad853c + 513da1c + 038b3d5 + 72bf0ca
- Tests 2740 → 2783 (+43 net since prod)
- Build clean
- Awaiting V15 #5 deploy auth (per V18)

## Commits (this session)

```
72bf0ca fix(stock+treatment): V35.2-quinquies/sexies — partial-commit prevention + null-customerId guard
038b3d5 feat(stock): V35.2-quater — remove "นำเข้าจากข้อมูลพื้นฐาน" button + sort orders newest-first
513da1c feat(stock): V35.2-tris ความจุ→QtyBeforeMaxStock direct + V35.1-tris+ flip-up dropdown
8ad853c feat(stock): Phase 15.6 V35.1+V35.2 — portal dropdowns + per-lot expansion + phantom cleanup
```

## User reports (verbatim) → fixes

| # | User words (excerpt) | Commit |
|---|---|---|
| 1 | "dropdown โดน box modal limit" | `8ad853c` portal |
| 2 | "ใช้ระบบ search เลือกรายการสินค้า ในใบโอนย้าย+ใบเบิก" | `8ad853c` BatchSelectField |
| 3 | "Acetin 6 + Aloe gel 010 ยังมาปรากฎ" | `8ad853c` canonical-name + cleanup |
| 4 | "1 batch หมด ทั้งๆที่มีหลายอันเกิน 1" → clarified per-lot | `8ad853c` lot expansion |
| 5 | "scrollbar ขวาสุด ก็ยังเลื่อนลงมาไม่ได้" | `513da1c` flip-up dropdown |
| 6 | "ความจุ ให้แสดง แจ้งเกินสต็อก" | `513da1c` QtyBeforeMaxStock direct |
| 7 | "เอาปุ่ม นำเข้าจากข้อมูลพื้นฐานออกไป" | `038b3d5` button removal |
| 8 | "รายการล่าสุดต้องอยู่บนสุด" | `038b3d5` createdAt DESC primary |
| 9 | "Vendor นำเข้าคลังกลาง+สาขา ไม่นำเข้าจริง" | `72bf0ca` partial-commit prevention |
| 10 | "treatment ขึ้น be_customers/null" | `72bf0ca` customerDoc guard |

## Files Touched (NEW)

- `src/components/backend/BatchSelectField.jsx` (typeahead batch picker)
- `src/lib/batchSearchUtils.js` (compose + filter helpers)
- `tests/phase15.6f-portal-and-batch-select.test.jsx` (43 tests)

## Files Touched (MODIFIED)

- `src/components/backend/ProductSelectField.jsx` (portal + smart flip-up + scroll-into-view)
- `src/components/backend/StockBalancePanel.jsx` (canonical name, FK gate then reverted, capacity column = QtyBeforeMaxStock, per-lot expansion)
- `src/components/backend/OrderPanel.jsx` (button removed, ProductSelectField wiring)
- `src/components/backend/CentralStockOrderPanel.jsx` (ProductSelectField wiring)
- `src/components/backend/StockTransferPanel.jsx` + `StockWithdrawalPanel.jsx` (BatchSelectField wiring)
- `src/components/backend/StockAdjustPanel.jsx` (ProductSelectField wiring)
- `src/lib/backendClient.js` (`_assertProductExists` + `_assertAllProductsExist` + `customerDoc` guard + sort fix)
- `src/components/TreatmentFormPage.jsx` (null-customer guard render-time)
- `api/admin/cleanup-test-products.js` (regex extended ADVX/ADVO/ADVW)
- 3 test files updated for V35.x architecture

## Production cleanup (already deployed via direct admin SDK)

- 14 ADVX/ADVO/ADVW test products + 18 batches (audit `cleanup-test-products-v35.2-*`)
- 32 test-branch batches across ADVB-/STK-TRT-/STK-SALE-/ADVSA-/V20 BR- (audit `cleanup-test-branches-v35.2-*`)
- All audited in `be_admin_audit` collection

## Decisions (1-line each)

1. **Portal dropdown** — react-dom createPortal to document.body + position:fixed; ancestor overflow can no longer clip
2. **Smart flip-up** — flip up when `spaceBelow < 400 && spaceAbove > spaceBelow * 1.3` (catches the common case)
3. **HARD_CAP 720** — dropdown max 720px (~15 rows) instead of original 480
4. **Scroll input toward TARGET_TOP=120** when dropdown opens + `r.top > 170`; useless if page is non-scrollable but flip-up handles it anyway
5. **V35.2 read-side FK gate REVERTED** — productThresholdMap was static-loaded → hid newly-imported batches; now write-side only
6. **`_assertAllProductsExist` pre-validates BEFORE first setDoc** — atomic FK; no partial commits
7. **`customerDoc()` rejects null/undefined/'null'/'undefined'** — silent be_customers/null corruption fixed
8. **TreatmentFormPage early-return** — render error placeholder when customerId missing instead of rendering form
9. **ความจุ column = alertQtyBeforeMaxStock** (not sum of batch.qty.total) — per round-2 directive
10. **listStockOrders + listCentralStockOrders sort by createdAt DESC primary** (importedDate as secondary tiebreak)

## Next Todo

**Awaiting user "deploy" auth** for V15 #5 combined deploy:
- Pre-flight Probe-Deploy-Probe Rule B (6 positive + 5 negative)
- Vercel `--prod --yes` + Firebase `deploy --only firestore:rules` parallel
- Post-probe + cleanup + HTTP smoke
- 4 commits will ship: `8ad853c` + `513da1c` + `038b3d5` + `72bf0ca`

After deploy: live QA on (1) dropdown flip-up + size, (2) ความจุ column meaning, (3) order partial-commit prevention, (4) treatment null-customer error UX.

## Resume Prompt

```
Resume LoverClinic — continue from 2026-04-28 EOD (session 26).

Read in order BEFORE any tool call:
1. CLAUDE.md
2. SESSION_HANDOFF.md (master=72bf0ca, prod=c36888e — 4 commits unpushed)
3. .agents/active.md (2783 tests pass; V35.1+V35.2 NOT deployed)
4. .claude/rules/00-session-start.md (iron-clad + V-summary)
5. .agents/sessions/2026-04-28-session26-v35-1-v35-2-bundle.md

Status: master=72bf0ca, 2783/2783 tests pass, prod=c36888e LIVE (V15 #4).

Next: V15 #5 combined deploy when authorized (4 commits).
Live QA after deploy: dropdown flip-up + size, ความจุ=QtyBeforeMaxStock,
order partial-commit prevention, treatment null-customer guard.

Outstanding: V15 #5 deploy auth.

Rules: no deploy without "deploy" THIS turn (V18); V15 combined; Probe-Deploy-Probe Rule B.

/session-start
```
