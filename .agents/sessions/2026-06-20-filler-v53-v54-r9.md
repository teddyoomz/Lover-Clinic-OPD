# 2026-06-20 (cont.) — Filler Simulator v5.3 + v5.4 + R9

## Summary
Continued the public pure-client `?play=filler` simulator across 3 user rounds: v5.3 (split-bar legend fix + bigger cross-section + iPad/touch), v5.4 (round-DOWN results for safety + auto-scale layout), and R9 (formula obfuscation + theme-aware logo watermark on 2D+3D + real-icon contact buttons in header/footer). All SHIPPED local + pushed; NOT deployed (await "deploy"; pure-client → vercel-only, no Probe-Deploy-Probe).

## Current State
- master `f5676bcb` (= origin, pushed). prod UNCHANGED — filler-sim still NOT deployed.
- `?play=filler` = pure client, NO Firestore/auth/PII/rules. `three` lazy (3D only).
- full vitest **16846/0** · `npm run build` clean (obfuscated filler chunk 79KB) · Rule Q L1 verified (dev + prod-build :4173).
- Obfuscation is BUILD-only (`command==='build'`); dev + vitest run unobfuscated source.

## Commits (this session)
```
f5676bcb feat(filler-sim R9): scoped formula obfuscation + theme-aware logo watermark (2D+3D) + real-icon contact buttons
b366829e docs(spec): filler-sim R9
fb58c199 feat(filler-sim v5.4): round-DOWN results (condom floor + r1 floor) + auto-scale layout
5d135a1d feat(filler-sim v5.3): default glans 0 + split-bar legend fix + bigger cross-section + touch + copy
```

## Files Touched
- `vite.config.js` (obfuscator plugin, command-gated, scoped to 4 filler files) · `package.json` + `package-lock.json` (vite-plugin-javascript-obfuscator devDep)
- `src/lib/fillerMath.js` (condom FLOOR snap + k-constants as integer fractions)
- `src/pages/FillerSimulator.jsx` (r1 floor · CLINIC_CONTACT + ContactButtons header/footer · controls fill-height · pass theme to Filler3D)
- `src/components/FillerGraphic2D.jsx` (flex-section auto-scale restructure · watermark <image> ×2)
- `src/components/Filler3D.jsx` (theme prop · centered DOM watermark <img> overlay)
- `src/lib/fillerStrings.js` (subtitle copy — v5.3) · `tests/filler-simulator-flow-simulate.test.js` (v5.2→v5.5 blocks) · `tests/filler-math.test.js` (condom floor)
- `docs/superpowers/{specs,plans}/2026-06-20-filler-r9-protect-watermark-contact*`

## Decisions
- R9-Q1 = client obfuscation (not serverless) — keep page pure-client/instant/offline; deterrence not encryption (disclosed).
- R9-Q2 = centered-faint theme-aware watermark (white logo dark / black light); 2D in-SVG, 3D DOM overlay (screenshot-covered).
- R9-Q3 = contact buttons header (compact icons) + footer (full); real brand SVG icons; theme-aware (dark filled+white / light soft-tint+deepened+border, AA).
- v5.4 round-DOWN = conservative/safety (condom FLOOR + r1 floor under-promise).
- k-constants as integer fractions (237/100) = obfuscation-friendly (numbersToExpressions hides floats) + value-identical (tests exact).

## Key lessons
- `numbersToExpressions` does NOT transform float literals → write secret float constants as integer fractions so they get obfuscated (and the literal never ships).
- `command === 'build'` gate (function-form defineConfig) is the clean way to keep a build plugin OUT of vitest (which runs `serve`) → tests unaffected.
- Rule Q on the real artifact: verify the OBFUSCATED build (`vite preview` :4173) renders + computes identically — not just that constants are gone. It did.
- split-bar bug class: a label trapped inside a variable-width container + a width-gate → hidden/crammed; fix = move labels to an always-visible legend (isolated, no other instance in src/).

## Next Todo
- Idle / await. Deploy filler-sim on explicit "deploy" → `vercel --prod` (frontend-only, pure-client, no Probe-Deploy-Probe).

## Resume Prompt
Resume LoverClinic — filler-sim is SHIPPED local (master `f5676bcb`), NOT deployed. `?play=filler` pure-client. Next: idle/await; deploy on explicit "deploy" (vercel-only). full vitest 16846/0. Obfuscation is build-only; dev runs unobfuscated. Read .agents/active.md + SESSION_HANDOFF top block. /session-start
