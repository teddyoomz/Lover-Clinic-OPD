# Checkpoint — 2026-06-02 EOD+1 · V144 stock ยอดคงเหลือ 4-issue batch (DEPLOYED + L2-verified)

## Summary
User reported 4 issues on tab=stock → ยอดคงเหลือ across two `/systematic-debugging` rounds; all shipped + DEPLOYED (vercel + firestore:rules) + verified on real prod. The headline catch: a Phase-1 rule-check found a V66-class trap before ship — the real-time 0-lot delete runs via the CLIENT SDK but `be_stock_batches` had `allow delete: if false`, so it would have silently no-op'd; surfaced + narrowed the rule (user chose option A).

## Current State
- master = `0599af2e` (docs) · code `2b1a8f11`. prod = `2b1a8f11` LIVE @ lover-clinic-app.vercel.app (vercel aliased + firestore:rules).
- Full suite **15777/0** (this session) · build clean · Rule Q L1 live browser · Rule Q L2 e2e **10/0** real prod.
- firestore:rules DEPLOYED — `be_stock_batches` delete `if false` → `isClinicStaff() && resource.data.qty.remaining==0`. Rule B 6/6 pre+post + cleanup + probe #16.
- Rule M applied: 14 lingering 0-lots deleted (audit `v143-quater-…37330cf4`); idempotent 0 + 30 placeholders kept.
- Honest: L1 was on localhost-dev (same committed code, real prod Firestore); L2 on real prod. Deployed bundle = same clean-built commit.

## Commits (this session, newest first)
```
0599af2e docs(stock): V144 batch — V-log + wiki + handoff + active
2b1a8f11 feat(stock): V144 real-time 0-lot auto-clear + หมด filter + in-place modals + balance follows global selector
```

## Files Touched
- src/lib/backendClient.js (`_clearRedundantZeroLotsForProducts` + planLotCleanup import + 7 entry-point wirings + 2 AV172-exempt)
- src/components/backend/StockBalancePanel.jsx (หมด filter + derived locationId, dropdown/auto-pick removed)
- src/components/backend/StockActionModal.jsx (NEW — DRY in-place modal shell)
- src/components/backend/{StockTab,StockAdjustPanel,OrderPanel}.jsx (modal rewire + export AdjustCreateForm/OrderCreateForm)
- firestore.rules (be_stock_batches delete narrow) · .claude/rules/01-iron-clad.md (probe #16) · audit-anti-vibe-code SKILL.md (AV172+AV173)
- tests/v144-realtime-lot-clear.test.js (NEW 41) · tests/v144-stock-ux.test.js (NEW 23) · 3 V21-fixup test files (phase15.7-ter rewritten, phase15.1, phase-17-2-octies)
- scripts/e2e-stock-realtime-lot-clear.mjs (NEW) · docs/superpowers/{specs,plans}/2026-06-02-stock-out-of-stock-filter-and-realtime-lot-cleanup*
- wiki concept + log · active.md · SESSION_HANDOFF.md · 00-session-start.md (V144 row)

## Decisions (1-line each)
- Real-time trigger = post-commit hook at the 7 entry points + 03:45 cron backstop + AV172 lock (user Q1=A; inside-tx rejected = V36 risk).
- Delete mechanism = narrow the firestore rule (user choice A) over an admin endpoint (C) — `remaining==0` predicate = defense-in-depth.
- Lot rule (user clarification) = keep 1 zero-lot if it's the LAST lot; delete all if a live lot exists = exactly planLotCleanup (AV168).
- Issue 3 modal = reuse exported AdjustCreateForm/OrderCreateForm in a DRY StockActionModal (user chose BOTH ปรับ+เพิ่ม); AV78 explicit-close.
- Issue 4 = derive locationId from selectedBranchId (align with sibling panels); central (lockLocation) untouched.
- 15 V21 lock-ins (removed auto-pick/dropdown machinery) flipped; phase15.7-ter repurposed as removal anti-regression.

## Next Todo
- Idle. Optional (Rule P, flagged): convert CentralStockTab balance ปรับ/เพิ่ม to in-place modals (same class, deferred — different tab + CentralStockOrderPanel + warehouse-scoped; pinned by test CB1). Carryover: prior-session V-log entries (sales/EOD+5/+6) unwritten.

## Resume Prompt
See SESSION_HANDOFF.md Current State (top) + .agents/active.md. master=0599af2e, prod=2b1a8f11 LIVE, 15777/0. V144 stock batch deployed + L2-verified. Next: idle.
