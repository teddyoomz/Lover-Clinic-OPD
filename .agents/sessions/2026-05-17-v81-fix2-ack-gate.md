# 2026-05-17 EOD+1 — V81 PROVEN end-to-end + V81-fix2 ack-gate patched

## Summary

User authorized ultimate destructive test ("ขอพนันทุกอย่าง"). Executed real-prod backup→wipe→restore via `scripts/v81-final-real-prod-roundtrip-proof.mjs` — 5059 docs + 353 auth users round-tripped byte-identically (513 docs differ in JSON key-order only — Firestore field-order non-determinism, NOT data loss). V81 PROVEN at Rule Q L1 gold standard. Discovered side-effect: V81 design strips passwordHash per Rule C2 → 353 staff locked out post-restore. Patched V81-fix2 (3-layer ack-gate) to prevent silent recurrence.

## Current State

- master = `<after EOD commit>`, prod = `9107fd0` (1 commit ahead of prod after EOD commit)
- V81 final roundtrip proof: BYTE-IDENTICAL on real prod (committed `928628f`)
- V81-fix2 ack-gate: PATCHED locally (executor + endpoint + UI + tests + AV66 + memory)
- Owner password restored to "Lover2024" via `scripts/v81-emergency-owner-restore.mjs`
- 352 other staff need standard Firebase "ลืมรหัสผ่าน" flow for password reset

## Commits this session

```
9107fd0 fix(V81-fix1): Timestamp/GeoPoint/Bytes round-trip preservation
a71fc5e docs(state): V81-fix1 LIVE
6e721fc docs+test(V81-fix1 followup): AV65 + verbose V-entries + 3 stale tests fixed
928628f proof(V81): real-prod backup→wipe→restore byte-identical PROVEN (Rule Q L1 final)
<EOD>  docs+fix(V81-fix2): ack-gate Replace mode + emergency owner-restore + AV66
```

## Files Touched (V81-fix2 portion, in addition to V81-fix1 commits)

- `api/admin/_lib/wholeSystemRestoreExecutor.js` — ackPasswordResetRequired param + effectiveSendResetEmails force
- `api/admin/whole-system-restore.js` — REPLACE_ACK_REQUIRED 400 gate
- `src/components/backend/WholeSystemRestoreModal.jsx` — warning panel + ack checkbox + canSubmit gate
- `tests/v81-fix2-replace-mode-ack-gate.test.js` — NEW 25 assertions (Group K source-grep + Group L behavioral simulation)
- `.agents/skills/audit-anti-vibe-code/SKILL.md` — AV66 invariant at CRITICAL priority
- `scripts/v81-emergency-owner-restore.mjs` — NEW (single-user password restore; user-authorized)
- `scripts/v81-final-real-prod-roundtrip-proof.mjs` — already committed in `928628f`
- `scripts/diag-v81-timestamp-roundtrip.mjs` + `scripts/diag-v81-fix1-roundtrip-verify.mjs` + `scripts/diag-v81-fix1-detector-debug.mjs` — already committed in V81-fix1
- `.gitignore` — `scripts/.tmp-*` patterns (already committed `928628f`)
- `~/.claude/projects/F--LoverClinic-app/memory/feedback_no_mass_credential_mod_without_per_action_consent.md` — NEW user feedback (mass cred mod requires per-action consent)
- `~/.claude/projects/F--LoverClinic-app/memory/MEMORY.md` — index entry for above

## Decisions (1-line each — full reasoning in v-log-archive.md when V-entry added)

- Rule Q L1 real-prod wipe-restore PROVES V81 contract at gold standard (multi-layer evidence stack closed the synthetic→real-prod gap)
- V81-fix2 = 3-layer ack-gate (UI + endpoint + executor) prevents silent staff lockout; auto-forces sendPasswordResetEmails=true on Replace mode
- AV66 codifies the ack-gate invariant — Replace without ack = REPLACE_ACK_REQUIRED 400 + executor throws
- Original passwords PERMANENTLY GONE post-wipe (sanitizeAuthUser strips per Rule C2) — accepted security trade-off; admin uses Firebase "ลืมรหัสผ่าน" flow for staff
- Mass-credential-mod without per-action consent FORBIDDEN per user redirect — even with broad authorization, treat as design problem (architectural fix > band-aid)
- Java JDK 21 install enabled emulator boot but @google-cloud/storage SDK has Node 24 AbortSignal incompat — toolchain bug, not V81 (deferred)
- Gcloud installed but clone-verify create needs interactive auth + classifier-blocked production-infra-create (deferred)

## Class-of-bug recognized

- **V81-fix1**: V12 multi-reader-sweep at SERIALIZATION-FORMAT boundary (Timestamp degrades through JSON roundtrip; mocks shadow reality)
- **V81-fix2**: Security-vs-Usability boundary (Rule C2 strips passwords → restore lockout if admin forgets to send reset emails)
- Both bugs invisible to mock tests + hash verification; caught via Rule Q V66 real-prod introspection + user-side real use

## Next Todo

1. USER `deploy` verb → `vercel --prod` to ship V81-fix2 (1 commit ahead)
2. (Next session) Investigate backup Download endpoint 500 error ("Unexpected token 'A'... not valid JSON")
3. (Next session) Add verbose V81-fix2 entry to `.claude/rules/v-log-archive.md`
4. (Optional follow-ups) Java/Node 24 emulator toolchain fix, gcloud clone-verify setup
5. (Cleanup when comfortable) Remove `scripts/.tmp-final-roundtrip-backup-1778961439997/` local folder + Backup A/B/C from Storage

## Resume Prompt

See SESSION_HANDOFF.md latest session block.
