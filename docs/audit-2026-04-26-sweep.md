# LoverClinic Pre-Launch Audit Sweep — 2026-04-26

> Generated by `/audit-all` orchestration via 6 parallel domain-grouped Explore agents.
> 22 audit skills / ~237 invariants checked.
> Scope: master `65ba420`, 4794 tests passing, build clean.
> Read-only audit pass — fixes happen in Phase 3 of the approved plan.

## Headline Numbers (raw agent counts — pre-triage)

- CRITICAL: 12 (with 5 justified-rule findings already accounted for)
- HIGH: 9
- MEDIUM: ~10
- PASS: ~80+

---

## Findings by domain

### 1. Money + Finance + Reports (audit-money-flow / -finance-completeness / -reports-accuracy)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| M1-M8, M10-M15 | PASS | `src/lib/backendClient.js` + `financeUtils.js` | All money conservation invariants hold (deposit double-apply guard, atomic tx+log, channel sum reconciliation, roundTHB consistency) |
| M9 | WARN | `backendClient.js:2823, 2885, 2925` | Silent catch on customer doc update (loyaltyPoints summary). Mitigated by tx-log being authoritative; nightly reconciler implicit. |
| FC1-FC15 spot | PASS | Phase 12 entities | All 11 validators present, 12+ Firestore rules gated isClinicStaff, 5-seller/3-payment-method limits enforced |
| AR1-AR15 spot | PASS | `src/lib/reportsUtils.js` + aggregators | Date range inclusive, cancelled excluded, roundTHB applied, Thai locale formatting |

**Verdict**: Money flow is sound. M9 has documented mitigation. No P0 here.

---

### 2. Stock + Cascade + Referential (audit-stock-flow / -cascade-logic / -referential-integrity)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| S1-S15 | PASS | `stockUtils.js` + `backendClient.js:3813-3880` | reverse cap, FEFO determinism, transfer/withdrawal new-batch with sourceBatchId, audit user normalization |
| **C3** | **CRITICAL** | `backendClient.js:282-285` | **`deleteBackendTreatment` calls `deleteDoc` WITHOUT `reverseStockForTreatment` first → stock orphaned forever, audit chain broken** |
| C1-C2, C5, C12-C13 | PASS | `SaleTab.jsx:1074-1110, 667-726` + `backendClient.js:1188-1207, 4004-4051` | Sale cancel/delete reverse all 5 subsystems; idempotency via `includeReversed=false` |
| R1-R5, R11 | PASS | `backendClient.js:118-151, 2625+` | customerId always String-coerced; `deleteCustomerCascade` covers treatments/sales/deposits/wallets/memberships/points/appointments |

**Verdict**: 1 P0 — C3 stock leak on treatment delete.

---

### 3. Frontend Correctness (audit-frontend-forms / -links / -timezone / -treatment-form)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| **TZ1** | **CRITICAL** | `src/components/backend/SalePaymentModal.jsx:24` | **`new Date().toISOString().slice(0,10)` for `paidAt` → drifts to previous day during Bangkok 00:00-07:00. Money record date wrong** |
| TZ1 | MEDIUM | `src/components/backend/reports/StockReportTab.jsx:123` | Same pattern in CSV download filename (display safer; logic-only use ok but inconsistent) |
| TZ1 | MEDIUM | `src/lib/medicalInstrumentValidation.js:168` | Same pattern for "today" comparison (logic-only, low impact) |
| FF3 | HIGH | `src/components/TreatmentFormPage.jsx:1819` | `scrollToError` covers doctor + treatmentDate but payment/billing fields lack full `data-field` coverage |
| FF2 | MEDIUM | `SalePaymentModal.jsx` | DateField bypassed; raw new Date for paidAt (overlaps TZ1) |
| FF10 | LOW | TFP outer container | No form-level aria-label or role |
| LK1, LK2, LK4, LK9 | PASS | `AdminDashboard.jsx:1129-1174` + `ClinicSchedule.jsx:143-147` | Schedule filter persistence + safe defaults + named room/doctor pairs |
| TZ2-TZ5 | PASS | various | No naked `new Date().get*()`/raw locale display, canonical helpers used |
| TF1-TF8 | PASS | `TreatmentFormPage.jsx:655-744, 2758-3849, 2347` | Edit-restore complete, data-field coverage on critical inputs, false→true sale-creation, name+product course key, pmStatusMap correct |

**Verdict**: 1 P0 — TZ1 paidAt drift in SalePaymentModal. P1: FF3 scrollToError gap.

---

### 4. Backend Integrity (audit-backend-firestore-only / -firestore-correctness / -master-data-ownership / -firebase-admin-security)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| BF1-BF7 | PASS ALL | backend tabs | No brokerClient imports outside whitelist (MasterDataTab, CloneTab, CustomerDetailView), no /api/proclinic/* in backend UI |
| F1-F10 | PASS ALL | rules + REST | updateMask present, immutable collections enforced, runTransaction for counters, listeners cleanup, doc size manageable |
| MO1-MO10 | PASS ALL | Phase 11 | All 6 master-data CRUDs use be_*, validators pure, status enum consistent, no raw `<input type="date">` |
| FA1-FA12 | PASS ALL | `api/admin/users.js` + `adminAuth.js` | Private-key not in source, env-gated init, token verify with checkRevoked=true, custom-claim+bootstrap admin gate, self-protection on delete/revoke, sanitized errors |

**Verdict**: ALL CLEAN. 39 invariants pass. Backend integrity is the strongest area.

---

### 5. Domain UX (audit-appointment-calendar / -chat-notifications / -clone-sync / -ui-cultural-a11y)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| **AP1** | **HIGH** | `src/lib/backendClient.js:860` (createBackendAppointment) | **No pre-write collision query, no runTransaction → parallel doctor+slot writes both succeed → double-booking** |
| AP3, AP5, AP6, AP8 | PASS | `AppointmentTab.jsx:53-64`, `SalePrintView.jsx`, FormModal | dateStr Thai TZ correct, BE/CE only in print views, FCM dedup OK |
| AN1-AN6, AN8 | PASS ALL | `chatUnreadUtils.js`, `AdminDashboard.jsx:29,206,221`, `ChatPanel.jsx:275-281, 830`, `api/webhook/send.js:130` | Helpers shared per Rule of 3, mark-read effect on mount, integerValue coerce, no inline filter patterns |
| AN7 | SKIP | tests/chatUnreadUtils.test.js | Test file structure not located (likely under different name) |
| **CL1** | **HIGH** | `src/lib/cloneOrchestrator.js:93-116` | **`customerExists` only checks ProClinic ID — MISSES HN / phone / national-ID dedup → duplicate profiles on re-clone** |
| CL2 | MEDIUM | `cloneOrchestrator.js:68` | No transaction guard on cloneCustomer; re-clone race possible |
| CL3 | MEDIUM | `cloneOrchestrator.js:186-192` | Step 2 (appointments) silent partial-failure on crash → orphans |
| UC1 | MEDIUM | `AppointmentTab.jsx:284, 345-346` | `text-red-400` on weekend day labels (Thai culture: red = death; borderline — calendar weekend coloring is typical worldwide) |
| UC2-UC4, UC7-UC8 | PASS | DateField, no AM/PM, no gold/yellow abuse, aria-labels on icon buttons | |
| UC5 | SKIP | (axe-core required) | Contrast AA not grep-auditable |

**Verdict**: 2 candidate P0/P1 — AP1 collision race + CL1 dedup gap. Both are real but pre-launch volume may make them rare.

---

### 6. Cross-cutting (audit-anti-vibe-code / -api-layer / -react-patterns / -performance / -privacy-pdpa)

| ID | Sev | File:line | Finding |
|---|---|---|---|
| **AV3** | **CRITICAL** | `backendClient.js:597, 2283, 2787` | **`Math.random().toString(36)` for pickCourseId, txId (wallet), ptxId (point) — non-crypto. Date.now() prefix mitigates but for audit-chain integrity should use crypto.getRandomValues** |
| AV3 | HIGH | `backendClient.js:2999, treatmentBuyHelpers.js:242` | Math.random for shortId + form rowId (lower stakes — UI state, not persisted audit) |
| AV3 | LOW | `FancyCharts.jsx:85` | Math.random for SVG gradient ID (cosmetic, accepted) |
| AV6 | (justified) | firestore.rules:27, 41, 44, 58, 307, 319, 324 | `allow:if true` on clinic_settings/main, proclinic_session, opd_sessions, clinic_schedules, chat_conversations — ALL justified by webhook/extension/public-link needs (per Rule B comment) |
| **AV1 / RP1** | **CRITICAL** | `TreatmentFormPage.jsx:3286, 4580` | **IIFE JSX `{(() => {...})()}` pattern — Vite OXC crash risk per CLAUDE.md known bug** |
| RP5 | HIGH | `TreatmentFormPage.jsx:1083, 1156, 1192, 1259, 1316, 1435` + `brokerClient.js:54, 233, 245, 253` + `ChartTemplateSelector.jsx:37, 110, 114` | Silent catch (_) {} swallows in form flows + broker status + chart loader (could migrate to debugLog from Phase 14.7.H-J) |
| RP2 | MEDIUM | `AdminDashboard.jsx:262` | onSnapshot without explicit unsub return in one site (others at 438, 466, 701, 1274, 1283 are correct) |
| A1, A4, A7 | PASS | api/* | updateMask correct, no hardcoded credentials, no hanging promises detected |
| A8 | NOTE | api/admin/* | CORS `*` acceptable (token-gated); api/proclinic/* origin guard implicit via Vercel rewrites |
| P1-P3 | PASS | various | No N+1 queries, ~7 listeners (under 10-bound), chat history TTL + buy modal pagination |

**Verdict**: 4 real P0 candidates (3× Math.random tx IDs, 2× IIFE JSX). RP5 silent catches: opportunity to extend the new debugLog from Phase 14.7.H-J.

---

## Triage — Initial P0/P1/P2/P3 (subject to verification in Phase 2)

### P0 — Must fix before production traffic (5 findings)

1. **C3** — `deleteBackendTreatment` doesn't reverse stock first (`backendClient.js:282-285`)
2. **TZ1** — `SalePaymentModal.jsx:24` paidAt UTC→Thai drift (loses a day during 00:00-07:00 Bangkok)
3. **RP1 / AV1** — Two IIFE JSX sites in `TreatmentFormPage.jsx:3286, 4580` (Vite OXC crash class)
4. **AV3** — `txId` (wallet, line 2283) + `ptxId` (point, line 2787) using `Math.random` — audit-chain integrity for money transactions
5. **AP1** — `createBackendAppointment` no collision check / no runTransaction (`backendClient.js:860`) — race double-booking

### P1 — Should fix this session (4 findings)

6. **FF3** — scrollToError coverage gap on payment/billing fields (`TreatmentFormPage.jsx:1819`)
7. **CL1** — Clone dedup only checks ProClinic ID (misses HN/phone/national-ID)
8. **RP5** — Silent catches across TFP / brokerClient / ChartTemplateSelector → migrate to `debugLog` from 14.7.H-J
9. **CL3** — Clone Step 2 (appointments) silent partial-failure → orphans

### P2 — Nice to have (defer if time tight)

- **TZ1 mediums** — StockReportTab CSV filename + medicalInstrumentValidation today comparison
- **CL2** — Clone customer no transaction guard
- **UC1** — Weekend red labels (cultural review, may be acceptable globally)
- **M9** — Customer doc summary drift (mitigated by tx-log)
- **AV3 lower** — shortId + form rowId Math.random (UI state, not persisted)

### P3 — Deferred (out of scope for this session)

- **AV6** — Open Firestore rules (all justified by webhook/extension/public-link needs)
- **UC5** — Contrast AA scan (requires axe-core)

---

## Verification needed before fixing (Phase 2)

For each P0:
- **C3**: confirm `deleteBackendTreatment` is reachable from UI (vs only `cancelBackendTreatment`)
- **TZ1**: confirm `paidAt` is stored + used for date filtering (not just display)
- **RP1**: confirm IIFE JSX patterns at lines 3286 + 4580 — they may be inside conditionals where they don't crash, or may have been fixed since the audit playbook was written
- **AV3**: assess whether Date.now() prefix already provides sufficient uniqueness (collision probability at clinic-scale traffic)
- **AP1**: assess whether the listenToAppointmentsByDate listener (Phase 14.7.H-B) provides sufficient client-side conflict detection vs server-side runTransaction guard

Phase 3 will execute fixes after verification.
