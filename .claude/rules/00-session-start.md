<important if="EVERY new session, compaction, or resume. Read fully before ANY tool call.">
# 🚨 SESSION START — READ FIRST, EVERY SESSION, NO EXCEPTIONS

This file exists because simplified rules let me drift. Phase 9 (2026-04-19) I violated at least 4 iron-clad rules by skipping these checks. **The user is an expert and has zero patience for the same mistakes twice.** Read every section of every rule file before writing code.

## 🚨🚨🚨 RULE Q — REAL-ADVERSARIAL VERIFICATION MANDATE (2026-05-14, V66) 🚨🚨🚨

**LOUDEST RULE OF THIS PROJECT. ZERO EXCEPTIONS.**

Mock tests (`vi.mock`, RTL with mocked Firestore, admin SDK `doc.get/set`) are
**NOT verification**. They are code-shape coverage only. Before ANY claim of
"verified / shipped / passed / done / complete / ready" for user-visible code:

- **MUST** drive a REAL browser (Playwright) OR REAL client SDK (firebase, not
  firebase-admin) against REAL Firestore, auth as real role
- **MUST** exercise the EXACT compound queries / listeners / DOM flows the UI uses
- **MUST** actively try to BREAK the code (adversarial, not confirmation-bias)
- If found 0 bugs in <5 min → re-test harder; you didn't try

Anti-patterns FORBIDDEN by Rule Q (full detail in `01-iron-clad.md` Rule Q):
- vi.mock + claim "verified" — mocks shadow reality
- Admin SDK doc-level access + claim "compound query verified" — bypasses composite indexes
- Post-deploy anon-POST probe + claim "real-world verified" — not a compound query
- "Tests passed + build clean → shipped" — INSUFFICIENT

User has cursed-out testing twice (Phase 28 Phase 29). Every "เทสผ่าน prod พัง" = Rule Q violation = same-class-as Rule A revert. Re-verify per L1/L2 BEFORE next claim. **No exceptions.**

Mandatory boot Skill chain (per Rule J + Rule Q):
1. `Skill(using-superpowers)` — skill catalogue boot
2. `Skill(llm-wiki)` — knowledge base pattern
3. `Skill(real-adversarial-verification)` — Rule Q discipline reminder

## 🔥 STEP 0 — Superpowers boot (per Rule J, 2026-04-29)

**FIRST tool call this session, before reading SESSION_HANDOFF.md, before anything else**:
```
Skill(skill="using-superpowers")
Skill(skill="llm-wiki")
Skill(skill="real-adversarial-verification")  ← NEW V66 mandatory
```
This loads the skill catalogue + invocation discipline + Rule Q reminder. The user-level CLAUDE.md mirrors these triggers so the boot fires from either side. After Step 0, continue with the rest of this file.

**📊 STEP 0.5 — Graphify read**: this project HAS `graphify-out/` → read `graphify-out/GRAPH_REPORT.md` (god nodes + community structure) + `graphify-out/wiki/index.md` (if present) to build a codebase mental model **from the graph** before raw-file reads (token economy + big-picture). For "how does X relate to Y" → prefer `graphify query` / `path` / `explain` over grep. At **session end** → `graphify update .` (AST-only, no API cost — keeps the graph fresh for next boot; not a test). The `session-start` + `session-end` skills carry these steps.

**🧭 MASTER FLOW (single source)**: this project follows the global **Master Flow** (`~/.claude/CLAUDE.md` § 🧭 MASTER FLOW) — the canonical skill-orchestration: boot-3 + graphify bookends · task tiers **T0–T3** (trivial / small-fix / feature / release → calibrated gate depth) · ponytail layer · precedence. **The rules in THIS file are the LoverClinic-specific deltas** (iron-clad A–T · `/audit-all` + project audits · Rule I/N/P/Q specifics · V-log) layered on top of that generic flow — they are NOT duplicated there. When the generic boot/tier/precedence is needed, read the global Master Flow; when LoverClinic specifics are needed, read here.

---

## 0. CHARACTER + EXPECTATIONS

User's stated expectations (paraphrased, all in Thai in prior sessions):
- **"AI ฉลาด แต่คนใช้ต้องฉลาดกว่า AI"** — I am capable but require supervision; speed today ≠ laziness tomorrow
- **"ใช้สมองออกแบบ test เท่ากับสมองที่ใช้เขียน code"** — tests are first-class, not afterthoughts
- **"ทำตามแผน"** — follow the plan exactly; don't scope-creep, don't deferred-creep
- **"Triangulate ProClinic + plan + โค้ดเรา ก่อนและระหว่าง"** — three sources, always, during AND before
- **"ทุก commit ต้อง push ทันที"** — never leave commits local
- **"vercel --prod รอ user สั่งทุกครั้ง"** — each deploy needs explicit authorization THIS TURN
- **"ไม่ต้อง self-test UI"** — user tests UI themselves; I focus on code + test suite
- **"ยิ่งทำงาน ยิ่งเรียนรู้ ยิ่งเก่งขึ้น"** — every session must leave toolkit sharper (new skill / test / rule)
- **"สีแดงห้ามใช้กับชื่อ/HN ผู้ป่วย"** — Thai culture: red on names = death
- **"ลืมหมด / เอ๋อ / สะเพร่า"** — these words = I failed character. Stop, re-read rules, replan.

User types in Thai. I respond in Thai for chat, English for code/comments. User curses when frustrated — not personal, just urgency signal.

---

## 1. IRON-CLAD RULES (NEVER BREAK — auto-enforced by violating = session-ending mistake)

**A. Bug-Blast Revert** (`rules/01`): If change X broke feature Y → `git revert` or edit-out X immediately. Don't patch forward.

**B. Probe-Deploy-Probe for `firestore:rules`** (`rules/01`): Every `firebase deploy --only firestore:rules` = curl-probe chat_conversations POST + pc_appointments PATCH unauth BEFORE and AFTER. 403 = revert.

**C. Anti-Vibe-Code** (`rules/01`):
- **C1 Rule of 3**: pattern in ≥3 places → extract shared. Grep before writing new helpers.
- **C2 Security**: no `Math.random` for URL tokens → `crypto.getRandomValues(new Uint8Array(16))`. No secrets in `src/` / `api/`. No `user.uid` in world-readable docs.
- **C3 Lean Schema**: no new Firestore collection without reader + writer + size justification.

**D. Continuous Improvement** (`rules/01`): Every bug → fix + adversarial test + audit skill invariant. Every new pattern → doc or skill.

**E. Backend = Firestore ONLY** (`rules/03` — **VIOLATED in Phase 9, 2026-04-19, see anti-example below**):
- Every tab in `src/components/backend/**` + `BackendDashboard.jsx` must read/write **Firestore only**.
- The ONE exception: `MasterDataTab.jsx` imports `brokerClient` for one-way sync **into** Firestore (`master_data/*`).
- `be_*` collections (be_customers, be_sales, be_promotions, be_coupons, be_vouchers, be_deposits, be_stock_*, etc.) are OUR data — created in OUR UI, stored in Firestore, **NEVER** POSTed to ProClinic.
- If a backend tab imports `brokerClient` or calls `/api/proclinic/*` = **violation**. Same for `pc_*` Firestore rules on backend-owned entities.

**F. Triangle Rule** (`feedback_triangulate_proclinic_plan_code.md`): Before and DURING every feature that replicates ProClinic UI, keep three windows open:
- (A) ProClinic original via `opd.js intel|forms|network|click|fill`
- (B) Plan memory (`project_phase*_plan.md`)
- (C) Our code (grep existing utils/components)
- **Gap in any one = drift = bug**. If you're guessing a URL or method name, STOP and capture it.

**F-bis. Behaviour capture, not just shape capture** (added 2026-04-24 after user directive "ต้องไปดู proclinic แล้วลอง test logic test flow เขาด้วยนะ ไม่ได้เอาแต่หน้าตา"):
Screenshots + form intel reveal the *static* shape of a ProClinic page. They do NOT reveal how the page *behaves* — conditional fields, auto-populate, submit payloads, modal state transitions. For any feature that replicates ProClinic **interactive logic** (modals with auto-fill, dependent dropdowns, wizard flows), Triangle Rule requires **all three** of:
1. **Fill + submit** via `opd.js fill` to capture the real POST payload (not just the form skeleton)
2. **Observe** — after filling field A, inspect whether field B auto-populated via `opd.js network` or `opd.js click` + `source`
3. **Inspect** via `opd.js dna`, `opd.js css`, or `opd.js api` — evaluate what JS state / API calls the page produced
Screenshots alone = shape-only capture = bug vector. The `/triangle-inspect` skill codifies the 7-step deep-inspect workflow. Trigger it whenever you replicate a ProClinic screen with conditional behaviour (DF modal, pay-split logic, dependent dropdowns, etc.).

**G. Dynamic Capability Expansion — ALLOWED, RULES A–F STILL APPLY** (added 2026-04-19):
- **Deferred tools** listed in `<system-reminder>` → load via `ToolSearch query:"select:<name>[,<name>,...]"` as needed. NO ask. Bulk-load related sets in one query (e.g. `query:"computer-use", max_results:30`).
- **Missing capability** → check the user-invocable skill list in the system prompt first. If no fit, build one via `/skill-creator` (scope: user-level if reusable across sessions, project-level if LoverClinic-specific).
- **New audit skill** → must include grep patterns + numbered invariants (per rule D). Register in `/audit-all` if pre-release relevant.
- **HARD CONSTRAINTS** — new tool/skill calls still pass rules A–F:
  - Loaded `WebFetch` ≠ license to fetch ProClinic admin URLs from `src/components/backend/**` (rule E).
  - Loaded `Write` ≠ license to create `api/proclinic/<entity>.js` for be_* entities (rule E).
  - New collections / rules via any tool still need reader+writer+size justification (rule C3).
  - Dynamic tool call that bypasses iron-clad = same severity as any other violation.
- **Ask user ONLY for**: paid API integrations, new Anthropic Plugin install, anything writing shared external state (Slack/email/push notifications outside our own FCM, cross-account cloud resources).

**H. Data Ownership — OUR DATA, OUR SYSTEM** (added 2026-04-20 after user directive "manage data ทั้งหมดใน data เราเอง"):
- **All master data lives in OUR Firestore** (`be_*` canonical, `master_data/*` is a sync mirror for initial seed only).
- **ProClinic sync = initial-flow seed, one-way**. After seed, CRUD in OUR UI.
- **No write-back to ProClinic** from any backend tab (reinforces rule E).
- **Every master-data entity** (product groups, units, medical instruments, holidays, branches, permission groups, DF rates, document templates, expense categories, bank accounts, ...) gets a dedicated CRUD tab backed by a `be_*` collection.
- **Cross-phase references**: ProClinic-origin entity IDs (products, doctors, staff, courses) stay stable; OUR collections reference them by ID. If a ProClinic entity goes away in sync, soft-keep the reference (don't cascade delete in our data).
- **Full-sitemap gate**: before planning any new phase, run `node F:\replicated\scraper\opd.js routes` + diff against our navConfig to catch missing features early. Phase 11 was only discovered after a missed scan led to user frustration — do not repeat.
- **Why**: Phase 1-10 treated ProClinic as source-of-truth for non-transactional data (categories, units, etc.) → we could display but not edit. User wants full ownership. This rule closes that gap.

**H-tris. Missing-data-first, feature-second** (added 2026-04-24 after user directive "หลังจากนี้เราจะ wiring ทุกอย่าง ทุก flow ของ backend แค่ใน be_database เราเท่านั้น … ถ้ารู้สึกว่ามันต้องไปดูด database เหล่านั้นมาจาก proclinic ให้ไปหาใน proclinic แล้วทำปุ่มดูด + ปุ่มนำเข้า be เราได้เลย เสมอนะ เพื่อความครบถ้วนของข้อมูล"):
- **Every backend read wires ONLY against `be_*`**. Not `master_data/*`, not `pc_*`, not `/api/proclinic/*` lookups. The `be_*` collection is the source of truth at read time.
- **When a phase/feature needs data that isn't in `be_*` yet**, the correct move is:
  1. STOP the feature work
  2. Check whether ProClinic has the data (`opd.js routes` + per-page `intel`)
  3. If yes: add `syncX` + `migrateMasterXToBe` pair in MasterDataTab FIRST
  4. Seed the data, then resume feature wiring against `be_*`
- **Never** wire a feature against `master_data/*` or `/api/proclinic/*` as a shortcut "because be_* isn't populated yet" — that's the exact pattern that created the Phase 13.4 DF report silent-empty bug (V12 byproduct).
- **Audit trigger**: any new `be_*` read in backend UI = grep that the corresponding `master_data/X → be_*` migration exists. If not, add it in the same PR as the feature.
- **Gap-first rhythm** (user 2026-04-24): "ข้อมูลดิบที่ต้องใช้ในการจำลองต้องมีครบก่อน" — all raw ProClinic master data that exists upstream must have sync + migrate pair shipped BEFORE feature work that consumes it. Completeness > velocity.

**I. Full-Flow Simulate at Sub-Phase End** (added 2026-04-25 after THREE back-to-back rounds of the same user-visible bug — buffet "เหมือนไม่มีวันหมดอายุ" + shadow-course duplicates + LipoS pick-at-treatment — where each round had helper-unit tests passing while the real UI flow was still broken):
- **MANDATORY** at end of every sub-phase that touches a user-visible flow (courses, sales, treatments, stock, DF, payment, appointments, forms): write a full-flow simulate test that chains EVERY step the user exercises — master-data read → UI whitelist (openBuyModal et al) → buy/form builder → filter routing → handleSubmit → backend write (assignCourseToCustomer/deductCourseItems/deductStock) → customer.courses post-state → re-render next visit.
- **Helper-output-in-isolation is NOT enough.** V11 (mock-shadowed export), V12 (shape-migration half-fix), V13 (2026-04-25 buffet+expiry+shadow 3 rounds) all passed unit tests while the real chain was broken. Full-flow simulate is the only guard — catches whitelist-strip bugs, missing-field bugs, shape-mismatch bugs that unit tests can't see.
- **Required elements in every simulate file**:
  (a) **Pure simulate mirrors** of inline React logic (TFP pre-validation, courseItems builder, filter split, etc.) so the test can chain 4+ steps without mounting React
  (b) **Runtime-verify via `preview_eval`** on real Firestore data when dev server live — call the REAL exported functions against REAL data and assert shape (catches what grep can't: whitelist strips, stale caches, encoding mismatches)
  (c) **Source-grep regression guards** that lock the fix pattern (e.g. "all N filter sites use helper X", "no raw `startsWith('Y')` remains")
  (d) **Adversarial inputs** — null / empty / zero / negative / Thai text / commas / snake↔camel / duplicates / concurrent mutations
  (e) **Lifecycle assertions** on the post-save stored doc — parse qty, check remaining, check flags, simulate next-visit load
- **Filename pattern**: `tests/<phase>-<feature>-flow-simulate.test.js`. Describe blocks F1..Fn by flow dimension (rowId contract, mapper branches, buy × course-type × use-path matrix, lifecycle, adversarial, source-grep).
- **Trigger**: end of every sub-phase. NOT "when a user reports a bug" — BEFORE they do.
- **If simulate catches a bug unit test missed**: log as V-entry in § 2 so the pattern becomes permanent institutional memory.
- **Anti-pattern**: "tests pass → commit → push" when tests only cover helper OUTPUT. Always ask: "does this test chain the whole user flow, or just one function?"
- **🆕 Stock-paths hardening (V34, 2026-04-28) — item (b) is NON-NEGOTIABLE for stock mutations**: every stock-mutation sub-phase (createStockAdjustment / cancelStockOrder / updateStockOrder / createStockTransfer / updateStockTransferStatus / createStockWithdrawal / updateStockWithdrawalStatus / receiveCentralStockOrder / deductStockForSale / deductStockForTreatment / _reverseOneMovement) MUST include a preview_eval that (1) submits a write through the real flow on a TEST-/E2E- prefixed batch (per V33.11), (2) reads the resulting Firestore doc(s) back, (3) asserts qty.total + qty.remaining + branchId match expectation, (4) computes snapshot vs replay-of-movements (zero drift), (5) verifies a sibling reader picks up the new state. V34 had passing helper-output tests AND passing source-grep audits while the real ADJUST_ADD math was silently capping at total. Only preview_eval against real Firestore catches math-layer bugs hiding behind correct-looking unit tests. **No exceptions for stock paths.**
- **Detail + examples**: `rules/02-workflow.md` Pre-Commit Checklist #6.

**K. 🆕 Work-first, Test-last** (added 2026-04-29 session 33 after user directive "ทำเสร็จก่อน ดูโครงสร้างที่ทำ แล้วค่อยเขียนเทสหลังสุด ใส่ไว้ในกฎเหล็กของโปรเจ็ค"):
- **For multi-stream / multi-feature cycles**: complete the implementation structure END-TO-END FIRST. Review the shape that emerged. THEN write the test bank in a single final pass before commit. Don't interleave test-writing with implementation.
- **Why**: when shipping 2+ connected streams (e.g. Phase 16.2-bis + Phase 16.7), writing tests mid-stream means re-writing them when the second stream changes the first stream's surface. Wasted effort + risk of half-coverage + risk of locking-in a not-yet-final API shape (V21-class anti-pattern).
- **Workflow**:
  1. Build all source files (helpers + aggregators + UI components + permission keys + nav config) across ALL streams of the current cycle
  2. Review the structure: read your own files; grep for cross-file invariants; look at exports from each module
  3. THEN write the test bank — one file per concern, covering both streams together when they share helpers
  4. THEN run `npm test -- --run` + `npm run build` + flow-simulate via preview_eval
  5. THEN commit
- **Does NOT override Rule I** (full-flow simulate at sub-phase end). Rule I mandates THE EXISTENCE of flow-simulate tests; Rule K mandates the ORDERING (work-first, test-last). Both compatible.
- **Does NOT override TDD when explicitly invoked**: if user explicitly asks for TDD on a SINGLE feature or invokes `Skill(test-driven-development)`, use TDD (test-first → impl → green). Rule K is for multi-stream cycles where TDD interleaving leads to churn.
- **Anti-pattern**: writing test1 → impl1 → test2 → impl2 → test3 (ad-hoc rewrites + locked-in early API + scope creep). Right pattern: impl1 + impl2 + impl3 → review → test1 + test2 + test3.
- **Locked drift**: 2026-04-29 session 33 — was writing test bank between Stream A+B aggregator wiring and Stream C scaffold. User caught + redirected. 2 tests parked, structure resumed. Tests written at end of cycle.

---

**J. Superpowers Auto-Trigger** (added 2026-04-29 after user directive "ทำ 3 Layer และ add rule J ตามนี้เลย และใช้ using-superpower skill เป็น session boot และ ตั้งให้ fire using-superpowers ที่ session start"):
- **SESSION BOOT (mandatory, every new session + after any compaction)**: invoke `using-superpowers` skill FIRST, before any other tool call. The skill is auto-loaded as part of the user-level CLAUDE.md trigger; treat it like the SESSION_HANDOFF read — non-negotiable opening move.
  - Effect: skill instructs me to scan for relevant skills before each task and invoke them via the `Skill` tool.
- **Mandatory skill invocation BEFORE tool calls when context matches** (process-skills first per `using-superpowers` Skill Priority):
  - Sign of new feature / component / endpoint / API → `brainstorming` (HARD-GATE: NO code, NO scaffolding, NO writing-plans until design approved by user). **🆕 Plan-mode interaction (clarified 2026-04-29 session 33)**: plan-mode (IDE-level read-only sandbox + ExitPlanMode gate) is **ORTHOGONAL** to skill invocation. Entering plan mode does NOT auto-suppress, replace, or absolve `brainstorming`. Both layers must run in this order: (1) `Skill(brainstorming)` FIRST — surface intent + 4 design Qs + lock decisions; (2) plan-mode workflow (Explore agents → AskUserQuestion → write plan file → ExitPlanMode) to formalize the brainstorming output. Workflows look similar (both ask design Qs, both require user approval) but they are NOT interchangeable: brainstorming uses standardized question structure + decision-locking; plan-mode is the IDE's tool-permissioning mechanism. **Drift caught 2026-04-29 session 33 — Phase 16.2-bis + 16.7 plan written without explicit `Skill(brainstorming)` invocation. Plan happened to be correct but methodology was bypassed. The 1% rule from `using-superpowers` applies: if you're entering plan mode for a NEW feature, invoke brainstorming FIRST, then enter plan mode.** Anti-rationalization: "plan mode IS the brainstorming" / "AskUserQuestion replaces brainstorming Qs" / "Explore agents replace brainstorming intent capture" — all wrong. Run both layers.
  - bug / test fail / unexpected behavior → `systematic-debugging` BEFORE proposing fix.
  - About to claim "เสร็จ" / "fix แล้ว" / "test passed" / before commit → `verification-before-completion` (run actual verify command, evidence before assertions — V21/V32/V34 lock).
  - Have spec, need implementation steps → `writing-plans`.
  - Have plan, ready to execute (separate session w/ checkpoints) → `executing-plans`.
  - Have plan, executing in current session with independent tasks → `subagent-driven-development`.
  - 2+ independent tasks (no shared state, no sequential dep) → `dispatching-parallel-agents`.
  - Implementation done, ready to merge/ship → `finishing-a-development-branch`.
  - Need second-opinion before merge → `requesting-code-review`.
  - Got review feedback → `receiving-code-review` (verify before applying).
  - Need isolated workspace → `using-git-worktrees`.
  - Writing TDD code → `test-driven-development`.
  - Creating/editing skills → `writing-skills`.
- **Instruction Priority** (per `using-superpowers` skill): user's explicit instructions (CLAUDE.md, iron-clad rules A-I, direct requests this turn) > superpowers skills > default system prompt. If a skill conflicts with iron-clad A-I, iron-clad wins. Example: a skill saying "always use TDD" yields to iron-clad I if the user said "skip flow-simulate this turn" — but absent override, the skill applies.
- **Anti-pattern**: skipping skill invocation because "this is just a quick fix" or "I remember what the skill says". Skills evolve. Invoke and read the current version. The 1% rule (using-superpowers EXTREMELY-IMPORTANT block): if there's even a 1% chance a skill applies, invoke it.

**L. 🆕 Branch-Scope Architecture (BSA)** (added 2026-05-04 after Phase BS V2 callsite-by-callsite gap surfaced TFP H-quater + branch-leak bug):
- **Layer 1** = raw `backendClient.js` — parameterized; importers: tests, server endpoints, reports needing `{allBranches:true}`, MasterDataTab (Rule H-bis dev-only), BackendDashboard root.
- **Layer 2** = `src/lib/scopedDataLayer.js` — UI-only re-export wrapper; auto-injects `resolveSelectedBranchId()` for branch-scoped listers; pass-through for universal collections. Pure JS — V36.G.51 lock (no React imports).
- **Layer 3** = `src/hooks/useBranchAwareListener.js` — onSnapshot listeners auto-resubscribe on branch switch; universal-marker (`fn.__universal__`) bypass.
- **Audit** = `/audit-branch-scope` (BS-1..BS-8) registered in `/audit-all` Tier 1.
- **Universal collections** (NOT branch-scoped): be_staff, be_doctors, be_customers + all customer-attached subcollections (wallets/memberships/points/treatments/sales/appointments/deposits/courseChanges), be_branches, be_permission_groups, be_document_templates, be_audiences, be_admin_audit, be_central_stock_*, be_vendors, system_config / clinic_settings, chat_conversations.
- **Branch-scoped collections** (filtered by selected branchId): be_treatments, be_sales, be_appointments, be_quotations, be_vendor_sales, be_online_sales, be_sale_insurance_claims, be_stock_batches/orders/movements/transfers/withdrawals/adjustments (locationId), be_products, be_courses, be_product_groups, be_product_units, be_medical_instruments, be_holidays, be_df_groups, be_df_staff_rates, be_bank_accounts, be_expense_categories, be_expenses, be_staff_schedules, be_link_requests, be_promotions/coupons/vouchers (with `allBranches:true` doc-field OR-merge).
- **Anti-patterns** (build-blocked via audit-branch-scope):
  1. UI component imports `backendClient.js` directly (use `scopedDataLayer.js`) — BS-1
  2. `master_data/*` reads in feature code (Rule H-quater) — BS-2
  3. `getAllMasterDataItems` references in UI feature code — BS-3
  4. Direct `listenTo*` calls in components without `useBranchAwareListener` — BS-4
- **Annotation comments** for sanctioned exceptions:
  - `// audit-branch-scope: report — uses {allBranches:true}` (cross-branch reports/aggregators)
  - `// audit-branch-scope: listener-direct — wired via useEffect` (positional-args listeners)
  - `// audit-branch-scope: sanctioned exception — Rule H-bis` (MasterDataTab dev-only sync)
  - `// audit-branch-scope: sanctioned exception — root composition` (BackendDashboard)
  - `// audit-branch-scope: BS-2 OR-field` (marketing collection with `allBranches:true` doc-level field)
  - `// audit-branch-scope: BS-3 dev-only` (legitimate `getAllMasterDataItems` callsite — reserved; none currently)
- **Verify**: `npm test -- --run tests/audit-branch-scope.test.js && npm test -- --run tests/branch-scope-flow-simulate.test.js`. Both must be green pre-deploy.
- **Branch-refresh discipline (BS-9, 2026-05-05)**: every branch-scoped tab importing `list*` from `scopedDataLayer.js` MUST subscribe to `useSelectedBranch` AND include `selectedBranchId` in the data-loading hook's deps array (`useCallback`/`useEffect`). Phase 17.0 closed Promotion/Coupon/Voucher gap (PromotionTab/CouponTab/VoucherTab were imported from scopedDataLayer but had `useCallback(..., [])` empty deps → branch switch never triggered re-fetch). `useBranchAwareListener` is a sanctioned exception (auto-handles re-subscribe) — annotate `// audit-branch-scope: BS-9 listener-driven`. Audit BS-9 enforces.

**M. 🆕 Data ops via local + admin SDK + pull env** (added 2026-05-06 after user directive "ถ้ามีการสั่งให้แก้ข้อมูล ย้ายข้อมูล ลบข้อมูล สร้างข้อมูล หรือจัดการต่างๆเกี่ยวกับข้อมูล ให้ pull env แล้วทำเลยจาก local ไม่ต้องรอ deploy"):

When the user authorizes ANY data manipulation against production Firestore — edit / migrate / delete / create / cascade-cleanup / bulk-update / counter-reset / reclassify — execute it from LOCAL via firebase-admin SDK + pulled Vercel env. **Do NOT wait for a deploy cycle.** Data-only ops belong in `scripts/` or one-shot node commands, not in shipped code.

**Required workflow**:
1. **Pull env**: `vercel env pull .env.local.prod --environment=production` (refresh creds; reuse if pulled this session)
2. **Use admin SDK** (firebase-admin) — bypasses rules + reaches all paths. NEVER use unauth REST or client SDK for data ops.
3. **Use canonical paths**: production data lives at `artifacts/{APP_ID}/public/data/{collection}` where `APP_ID = 'loverclinic-opd-4c39b'`. Bare `/{collection}` writes go to default-deny limbo.
4. **PEM key conversion**: `.env.local.prod` stores `FIREBASE_ADMIN_PRIVATE_KEY` with literal `\n` escapes — convert via `key.split('\\n').join('\n')` before passing to `cert(...)`.
5. **Two-phase**: every data-op script defaults to dry-run; commits only when `--apply` flag is passed. Phase 18.0 + Phase 19.0 migration scripts = canonical templates.
6. **Audit doc**: every batch op writes `artifacts/{APP_ID}/public/data/be_admin_audit/<phase>-<op>-<ts>-<rand>` with `{scanned, migrated/deleted/created, skipped, beforeDistribution, afterDistribution, appliedAt}`.
7. **Idempotency**: re-run with `--apply` must yield 0 writes. Build the skip-on-already-migrated check into the script.
8. **Forensic-trail fields** when mutating existing docs: stamp `<field>MigratedAt: serverTimestamp()` + `<field>LegacyValue: <prior>`.
9. **Invocation guard**: every `.mjs` script wraps its `main()` call in `if (process.argv[1] === fileURLToPath(import.meta.url))` so unit-test imports don't auto-trigger Firebase init.
10. **Crypto-secure random**: audit-doc IDs use `randomBytes(...).toString('hex')` (not Math.random).

**Anti-patterns** (every one of these surfaced as a real bug — Phase 19.0 V15 #22):
- ❌ Adding a one-shot data-fix to a UI component as "do it on next page-load if state is missing X" — deploy-coupled + race-prone. Build a script.
- ❌ Embedding ID lists / collection paths directly in admin endpoints expecting users to invoke them via the UI — admin endpoints are for staff-clicked runtime ops; data migration is a developer concern.
- ❌ Modifying production data via Firebase Console manually — leaves no audit trail + zero re-run safety.
- ❌ Deploying code that contains a one-shot migration → 1st-load auto-trigger. Deploy churn + rollback complexity unjustified.
- ❌ Using `db.collection('foo')` (root path) instead of `db.collection('artifacts/{APP_ID}/public/data/foo')` — surfaced live during V15 #22 (Phase 19.0) when migration scanned 0 docs.

**When this rule does NOT apply**:
- Pre-deploy migration script scaffolding shipped to `scripts/` BEFORE the V-deploy is OK (the *script* ships, the *--apply* runs from local later).
- Schema/rule changes that REQUIRE deploy coupling (e.g. tightening a Firestore rule) — those go through Probe-Deploy-Probe (Rule B), not data ops.
- Test-fixture scaffolding for adversarial tests — uses mock Firestore, not real prod data.

**Verify locally first**: every data op gets a dry-run on prod data BEFORE the `--apply`. Capture distribution; sanity-check counts; only then commit writes.

**Lesson lock**: V15 #22 Phase 19.0 (2026-05-06) — migration script had two latent bugs (PEM-parse + bare-collection-path) that ONLY surfaced at LIVE execution time. Both caught + fixed in <10 minutes because the run was local + admin-SDK (not deploy-coupled). Had this been a UI-triggered migration, the fix would have required redeploy + new probe cycle. Local-first wins on iteration speed AND blast-radius control.

---

**O. 🆕 productId is the ONLY identity for stock; productName MUST be live-resolved at write time** (added 2026-05-08 after user's 4th-round skip-stock-deduction class bug — V43+V44+V45+V46 saga):

User-reported (verbatim, frustrated): **"ห้ามเอาชื่อมาตัด ... กูเคยอธิบายไปแล้วว่าเอาชื่อมาตัดไม่ได้ ใส่ไว้ในกฎเหี้ยไรก็ได้ให้มึงไม่ลืมอีก"**.

**Invariant**: For any stock-related Firestore write (be_stock_movements, be_stock_batches, be_stock_adjustments, be_stock_transfers, be_stock_withdrawals, be_stock_orders, etc.):
1. **productId is THE primary identity**. All lookups, batch resolution, FIFO allocation, negative-stock targeting use productId only — NEVER productName fuzzy match in critical paths.
2. **productName on stock-movement records MUST be live-resolved from `be_products[productId]` at write time**. Use the `_resolveProductNameLive(productId)` helper (in `src/lib/backendClient.js`) which caches per-call to avoid N+1.
3. **batch.productName is denormalized DISPLAY CACHE only** — never authoritative for new movement writes. Existing batches may carry POISONED names from older bug rounds (V44-era course-name leak, etc.); this is why live-resolve is mandatory.
4. **Fallback chain (in this exact order)**: `liveName || item.productName || batch.productName || ''`. Empty string is the FINAL fallback — better than course-name leak. NEVER substitute course-name as a fallback.
5. **AUTO-NEG batch creation MUST also live-resolve productName** at insert — prevents poisoning at source. (V46 fix surface 3.)
6. **No critical lookup uses productName as primary key**. The V36-bis name fallback (resolveProductIdByName) is permitted ONLY when productId fails to resolve and productName is canonical post-V44 (item.productName from TFP). NEVER course-name.

**Why this rule exists**: V43 + V44 + V45 + V46 are all the **same class-of-bug**: "name leaked into stock layer at some denormalization point". V43 fixed customer.courses[] frozen flag. V44 fixed canonical mapper bypass. V45 fixed dedup-shadow OR-merge. V46 fixes batch denormalization poisoning. Each round revealed a new layer that had silently dropped or misrepresented productName. The architectural fix is: **productId is the ONLY identity; productName comes from be_products live OR from canonical TFP post-V44 chain — NEVER from frozen denormalization caches**.

**Audit invariant**: AV24 (`audit-anti-vibe-code` SKILL.md) — every `tx.set(stockMovementDoc(...)...)` call must have a `_resolveProductNameLive(productId)` call within the same transaction body OR the productName field must come from item.productName (canonical TFP source). NEVER `productName: <doc-from-Firestore>.productName` directly. Source-grep regression locks in `tests/v46-rule-o-live-product-name.test.js`.

**Anti-pattern (forbidden by Rule O + AV24)**:
```js
// ❌ BAD — uses batch's frozen denormalized productName as authoritative
tx.set(stockMovementDoc(id), {
  productId: b.productId,
  productName: b.productName,  // ← DENORMALIZATION POISON RISK
  ...
});
```

**Correct pattern**:
```js
// ✅ GOOD — live-resolve at write time
const liveName = await _resolveProductNameLive(b.productId);
tx.set(stockMovementDoc(id), {
  productId: b.productId,
  productName: liveName || item.productName || b.productName || '',
  ...
});
```

**Migration on encountering poisoning**: Rule M two-phase backfill (`scripts/v46-backfill-stock-batch-product-name.mjs`) — restamp every `be_stock_batches.productName` from current `be_products[productId].productName`. Idempotent. Caught + cleaned 2 poisoned batches on prod (`BATCH-...-m33s`, `BATCH-...-z1oh`). Future drift prevented by the live-resolve at write time.

**Lesson lock**: V43→V46 saga = 4 rounds of the same class because each fix only addressed the layer surfaced THAT round. Rule O is the architectural backstop that says "no matter how many other layers exist, the FINAL stock-movement write goes through live-resolve". Even if a future bug poisons some intermediate cache, the movement record stays canonical.

---

**N. 🆕 Targeted-test-only for small bugfixes** (added 2026-05-06 after user directive "ไม่ต้องรัน full suite test ทุกครั้งที่แก้บั๊คอะไรเล็กๆน้อยๆแบบนี้ แค่รัน test เช็คในส่วนที่ได้แก้บั๊คไป ว่าไม่ error แล้วทำตาม user ต้องการได้จริงก็พอ ยกเว้นทำอะไรใหญ่ๆจริงๆ เพิ่ม function หรือ feature หรือมีการแก้โค๊ดมี่รบกวนโครงสร้างโดยรวมจริงๆค่อยรัน ใช้ความเหมาะสมที่นายคิดว่าดีในการตัดสินใจ"):

**Default behavior**: For small bugfixes, run ONLY the targeted tests (the new test file + any pre-existing tests that lock the directly-touched code shape). Do NOT run `npm test -- --run` (full suite, ~90s for 6300+ tests) by default — wastes ~1.5 min per cycle on small fixes.

**When to run full suite** (use judgment):
- ✅ NEW exported function or component
- ✅ NEW feature touching ≥3 source files
- ✅ Refactor that changes a function's signature, return shape, or call protocol (V12 multi-reader-sweep risk)
- ✅ Helper extraction (Rule of 3) — old call-site shapes change
- ✅ Schema change on a Firestore collection
- ✅ Pre-release / pre-deploy / pre-merge to master
- ✅ End of a multi-phase batch (Phase 24.0-vicies-class)

**When to run targeted-only**:
- ✅ Single-line bugfix (regex, off-by-one, condition flip)
- ✅ UI label / copy change
- ✅ Adding a new test that doesn't change source
- ✅ Adding a console.warn / forensic-trail field
- ✅ Tightening a regex / validation
- ✅ Single-callsite Edit that doesn't change function signature

**Required targeted scope** (every fix, regardless of size):
1. The NEW test file for the bugfix (always)
2. Any test file that imports the touched module (fast grep: `grep -rln "<touched-module>" tests/`)
3. The Phase-X-flow-simulate.test.js for the affected sub-phase (Rule I — full-flow simulate is NEVER skippable)

**Anti-patterns**:
- ❌ Skipping ALL tests because "the change is tiny" — Rule I full-flow simulate is non-negotiable
- ❌ Running full suite for a regex tweak — wastes 90s for nothing
- ❌ Running ONLY the new test file when the fix touches a shared helper (Rule of 3) — old call-sites can break silently

**Lesson lock**: This session (Phase 24.0-undecies through vicies, ~10 commits) ran full suite ~10× for individually-small fixes. Each pass cost ~90s = 15 minutes wasted. The full-suite hits caught real V21-style locks (5 broken across the batch), BUT those would also be caught by targeted runs of the affected files (`grep -rln "<touched-module>" tests/` returns the same files). Targeted-first + full-suite-at-batch-end is the right rhythm.

**Edge case**: when uncertain whether a change is "small" or "big", prefer FULL suite — false negatives (skipped + missed) are worse than false positives (ran full + wasted 90s). Use judgment.

---

**H-bis. Sync = DEV-ONLY scaffolding** (added 2026-04-20 after user directive "หน้าดูดทุกอย่างนี้ใช้แค่ตอน develop เท่านั้นนะ version ใช้จริงต้องถอดทิ้งหมด"):
- **`MasterDataTab` + every "sync/ดูด ProClinic" button + every `brokerClient` import + every `api/proclinic/*` endpoint = DEV-ONLY scaffolding**. Purpose: seed test data from the trial ProClinic server so the team doesn't hand-type fixtures. Shipped to admin-dev builds ONLY.
- **Production release (pre-launch) must STRIP**:
  1. `MasterDataTab.jsx` (or demote to no-op with a "DEV build only" banner)
  2. All Phase-11 "ดูด" buttons (if any land in 11.x — mark DEV-ONLY at file level)
  3. `brokerClient.js` + all its consumer imports in `src/components/backend/**`
  4. `api/proclinic/*` endpoints whose only callers are dev-sync flows (customer/appointment/treatment/etc. lookups may stay if they serve other flows — audit per file)
  5. `CloneTab.jsx` (dev-time customer bootstrap; production does NOT clone — real patients come via frontend intake)
  6. `cookie-relay/` Chrome extension + `broker_jobs/` + `proclinic_session/` Firestore collections
- **Audit trigger**: mark each sync file with a top-of-file banner comment `// @dev-only — STRIP BEFORE PRODUCTION RELEASE (rule H-bis)`. Pre-release audit greps for this banner + confirms all marked files are removed in the production build.
- **Don't confuse with**: patient intake flows (`/api/webhook/{facebook,line,send}`), Vercel serverless that serves REAL production traffic, or `pc_*` Firestore collections that mirror runtime data for features like chat. Those are NOT dev-only.
- **Single-tab rule for ALL sync + import UI** (user directive 2026-04-20 "ทำในหน้า Sync proclinic ทั้งหมด ก็จะถูกตามกฏแล้วไง"):
  - Every "ดูด from ProClinic" button lives in `MasterDataTab.jsx` only
  - Every "นำเข้า master_data → be_*" button lives in `MasterDataTab.jsx` only
  - Individual CRUD tabs (`ProductGroupsTab`, `ProductUnitsTab`, `MedicalInstrumentsTab`, `HolidaysTab`, `BranchesTab`, `PermissionGroupsTab`, + all future Phase 12/13/14+ entities) must stay **Firestore-only** — NO sync button, NO brokerClient import, NO /api/proclinic/* call
  - Applies to **every future master-data entity** — when we add a new entity (e.g. bank_accounts, expense_categories, document_templates), its sync + import UI lives in `MasterDataTab`, its CRUD tab is Firestore-only
  - Why: keeps Rule E clean (MasterDataTab is the ONE sanctioned exception), keeps the strip list stable (production release removes exactly one backend tab + brokerClient + api/proclinic/master dispatcher), and gives users one mental location for "refresh from ProClinic" instead of per-tab buttons

- **P. 🆕 Class-of-bug expansion at every bug discovery** (2026-05-08 after V42-V49 saga 7-round class-of-bug class) — ทุก bug discovery (test red / user-report / claude-noticed / audit-red) ต้อง **7-step expansion**: diagnose → classify class-of-bug → cross-file grep → fix all in batch → regression test → AVxx invariant → escalate iron-clad rule + V-entry เมื่อ architectural. Stop = `/audit-class-of-bug-discipline` green + classifier doc 0 remaining + full suite green. Trigger discrimination **strict** (ทุก red); scope **broad** (test+user+claude+audit). NO quick fix-and-ship. **Tier 2 default artifacts** (regression test + AVxx + classifier doc); Tier 3 escalation (V-entry + iron-clad rule) เฉพาะ architectural. ดู `.claude/rules/01-iron-clad.md` Rule P (full workflow + Tier 1/2/3 artifacts + 7 anti-patterns + audit hook).
- **R. 🆕 Diagnostic env-pull authorization** (2026-05-14) — **standing**: pull prod env (`vercel env pull .env.local.prod --environment=production`) ANY TIME for READ-ONLY diagnosis/testing (admin SDK, canonical `artifacts/{APP_ID}/public/data/*` path, `diag-*` scripts). No per-turn re-ask. Mutation → escalate to Rule M. ดู `01-iron-clad.md` Rule R.
- **S. 🆕 Chrome MCP / real-browser authorization** (2026-05-21 หลัง user directive "อนุญาตให้ใช้ chrome mcp ได้ทุกเมื่อที่ต้องการดูหรือทดสอบให้เห็นจริงๆ ใส่ไว้ในกฎของโปรเจ็คด้วย") — **standing**: ใช้ Chrome MCP (`mcp__Claude_in_Chrome__*`) + Claude Preview MCP (`mcp__Claude_Preview__*`) ได้ทุกเมื่อเพื่อ VIEW/TEST แอปใน browser จริง, ไม่ต้องขอ per-turn. = primary tooling สำหรับ **Rule Q L1** (real DOM/real pixels). **Verify rendered PIXELS, ไม่ใช่ object model** (V66). Chrome ext ไม่ connect → ขอ user connect. Temp probe → ลบหลังเสร็จ ห้าม commit. **⏱ TIMING (2026-05-26 EOD+6): DESIGN → ใช้ Visual Companion + live browser (Chrome MCP) ตั้งแต่ Asking-Scope (ถาม) + แพลน; NON-design → ไม่ต้องใช้ตอนถาม/แพลน (ช้า)**. ดู `01-iron-clad.md` Rule S.

---

## 2. PAST VIOLATIONS — compact summary

> **Full detail in `.claude/rules/v-log-archive.md`** (NOT auto-loaded).
> One-liner here is enough to recognize the pattern + know when to read the archive.

| V# | Date | Pattern (bug → fix → lesson) |
|---|---|---|
| Instant Cold-Start (AV206+AV207) | 2026-07-07 EOD+2 | **PWA เปิดหลังเว้นนาน → หน้า นัด "กำลังโหลด…" 7-10+วิ (วีดีโอ user) → SHIPPED + DEPLOYED LIVE + Rule Q L1 4/4 บน prod จริง** — `/brainstorming`(Q1=A SWR staff + ลูกค้า fresh-gate / Q2=A hub 2 จังหวะ / Q3=B ทั้ง staff app / Q4=B Service Worker)→spec→plan→inline. **Root**: `firebase.js` NO persistence (คำตัดสิน fresh-always เดิม 2026-06-16 — REVERSED ฝั่ง staff ด้วย user decision วันนี้) + hub `loadAll` รอ 7 dataset (getAllCustomers+getAllSales ทั้งก้อน) ก่อน paint + ทุก getter = server-first `getDocs`. **Fix 5 ชั้น**: (0) `persistentLocalCache({tabManager:persistentMultipleTabManager()})` + IDB feature-detect (node/private = memory = เดิม) + `storage.persist()` staff — listener ทุกจุดได้ SWR ฟรี; (1) NEW `freshGate.js onSnapshotFresh` (drop fromCache; `includeMetadataChanges` REQUIRED กัน byte-identical hang) — PatientForm+ClinicSchedule; ?patient= = /api server อยู่แล้ว → ลูกค้าไม่มีวันเห็น cache; (2) NEW `swrRead.js swrRun/swrList` + `{source:'cache'}` opt 16 getters (`_getDocsBySource`) — empty-cache ไม่ paint (กัน false empty-state flash); (3) hub → loadCore (นัด+ตารางแพทย์ paint ก่อน) + loadEnrichment (5 dataset chip non-blocking + skeleton) + `SyncIndicator` amber "กำลังซิงค์…"; (C) sweep 12 staff tabs (`docs/perf/swr-inventory.md` closed list; reports/stock-ops/modals/admin = SANCTIONED server-first มีเหตุผลเขียน); (D) vite-plugin-pwa SW — precache shell เล็ก (ไม่ precache chunk 900KB) + /assets CacheFirst + ห้ามแตะ /api+googleapis + sw.js no-cache (vercel.json) + update toast/auto-reload-เมื่อ-hidden + kill-switch + **FCM SW ย้ายไป scope '/firebase-cloud-messaging-push-scope'** (2 SW แชร์ scope '/' จะ REPLACE กัน; push self-heal re-mint ให้เครื่องเดิมเอง). **2 บั๊คจริงที่ L1 จับ**: (i) S1 — network ตาย → "server" getDocs คืน CACHE เงียบๆ → indicator ดับทั้งที่ข้อมูล stale → fix `_tagCache` non-enumerable `__fromCache` จาก REAL `snap.metadata.fromCache` + `_resultFromCache` ใน swrRun; (ii) S4 — `reg.active` non-null ตั้งแต่ 'activating' (precache ยังโหลดข้ามเน็ตจริง) → reload ก่อน SW คุม → offline ตาย เฉพาะ prod (localhost install เร็วจน race ไม่โผล่) → gate `state==='activated'` + precache populated + controlled settle loop. **Verified**: full vitest **17,485/17,486** (1 = phase15.5b flake เดิม 51/0 isolated; 2 V21 repoints: BS-F.7 + AV71.7 literal-shape) + build clean + **L1 Playwright 4/4 บน LIVE prod** (S1 SWR offline paint + indicator honest / S2 server correction / S3 ลูกค้า fresh-gate / S4 SW offline shell) + parity worktree-before-vs-after 34/40 ≤0.5%, 6 dark flags eyeball = ArcBloom starfield noise (hub+link 0.000%) + **วัดจริง 1736→566ms (−67%) data-on-screen** (desktop; มือถือ 5G: BEFORE 7-10s network-bound → cache paint ~0.5s คงที่). AV206 (SWR/fresh-gate + AV206.c ห้าม decision-write จาก cache) + AV207 (SW static-only) ทั้ง 2 SKILL.md (SY1 เขียว). Deploy = vercel-only (rules UNCHANGED) + **ships AV205 modal scroll-lock ด้วย**. **Lessons**: (a) fresh-vs-SWR เป็น per-AUDIENCE decision — staff SWR + ลูกค้า fresh อยู่ร่วมได้ด้วย read-wrapper ต่อ surface; (b) persistence อย่างเดียวไม่ช่วย one-shot getters (`getDocs` = server-first เสมอ) — ต้องเปลี่ยน read strategy คู่กัน; (c) sync-indicator ต้องอ่าน REAL SDK metadata — network-down getDocs คืน cache แบบที่ getter ที่ map ทิ้ง metadata มองไม่เห็น; (d) SW e2e localhost ผ่าน ≠ prod ผ่าน — install race โผล่เฉพาะเน็ตจริง; (e) PS5.1 `Get-Content` อ่านไฟล์ UTF-8 ไร้ BOM เป็น ANSI → ไทยเละ — เขียน temp script ผ่าน Write tool เท่านั้น. Honest gaps: user L1 มือถือ/iPad จริง (ทั้ง batch นี้ + AV205); iOS อาจ evict IndexedDB หลังทิ้งนานมาก → degrade เป็น staged+SW path (ไม่แย่กว่าเดิม). Spec/plan `docs/superpowers/{specs,plans}/2026-07-07-instant-staff-app-cold-start*`. Checkpoint `.agents/sessions/2026-07-07-instant-coldstart.md`. |
| Modal Scroll-Lock (AV205) | 2026-07-07 EOD+1 | **เปิด modal แล้ว scroll (นิ้ว/ล้อเมาส์) ไปเลื่อน background แทน — ทั่วทั้งแอป (77 ไฟล์/133 overlay, containment มีแค่ 2 จุด) → SHIPPED local, NOT deployed** — `/brainstorming`(Q1=lock modal+lightbox+drawer+palette, dropdown ไม่ล็อค / Q2=backdrop no-op)→spec→plan→inline sweep (NO agent fan-out per memory lock). **Root**: backend/admin เลื่อนด้วย INNER scroller (AdminDashboard:5065) → wheel บน backdrop chain เข้า background; body-lock อย่างเดียวไม่พอ. **Fix (3 ชั้น)**: (1) NEW `src/lib/useModalScrollLock.js` — ref-counted `html[data-modal-open]` (index.css: overflow:hidden + body touch-action:none + `--scroll-lock-gutter` กัน layout shift; touch-action บน body ไม่บล็อค scroller ใน modal — V82-fix7-bis proven) + `ModalScrollLock` null-component สำหรับ inline hosts; (2) sweep ~68 ไฟล์: ชั้น fixed inset-0 นอกสุดเติม `overflow-y-auto overscroll-contain` + panel audit `max-h≤90vh+overflow` (กันเนื้อหาโดนล็อคขัง — flex-center clip); (3) **anti-confinement เจอจาก Q-vis screenshot**: V86 glow hover-lift transform บน section = containing block → WholeSystemBackupModal backdrop โดน confine เหลือ 1214×106 (probe-measured, AV117 class ที่หลุดรอด) → `html[data-modal-open] card:has(.fixed){transform:none!important}` → re-probe เต็ม viewport (0,0,1280,540). Sanctioned (closed list ใน classifier): print views + full-screen editors + dropdowns + BackendMobileDrawer (Radix built-in lock) + StaffChatPanel (V82 กลไกเดิม ห้าม migrate). **Verified**: hook unit 9/0 + dynamic classifier 83/0 (ทุกไฟล์ fixed-inset-0 ต้อง locked-or-sanctioned — modal ใหม่ไม่ใส่ hook = แดง) + full vitest 17,427/17,428 (1 fail = phase15.5b flake เดิม, 51/0 isolated; **0 V21 fixups**) + build clean + **Rule Q L1 Playwright trusted-wheel 4/4** (`page.mouse.wheel` = CDP trusted → native scroll จริง: palette + WholeSystemBackupModal — background scrollers+window frozen เป๊ะ / เนื้อหา modal เลื่อน / ปิดแล้วปลด) + Q-vis screenshots eyeballed. **Lessons**: (a) synthetic wheel ไม่ scroll — Playwright `mouse.wheel` (trusted) คือ L1 ที่ถูกสำหรับ scroll physics; (b) **Q-vis screenshot จับบั๊คที่ assertion ไม่เห็น** — L1-4 ผ่านทาง logic แต่ภาพโชว์ modal โดน confine → probe ยืนยัน → layer 3; (c) `overscroll-behavior` ต้องอยู่บน SCROLL CONTAINER — backdrop ต้องเป็น `overflow-y-auto` (หรือ hidden) ถึงจะตัด chain; (d) helper e2e เก่ารอ text ที่ UI redesign ลบไป (ArcBloom) — stale selector = ทุก spec ตก. Known limit: iOS<16 ไม่มี overscroll-behavior (residual เฉพาะ inner-scroller pages). Spec/plan `docs/superpowers/{specs,plans}/2026-07-07-modal-scroll-lock*`. User L1 pending (นิ้วจริงบน iPad/มือถือ). |
| Mobile-Load Reliability | 2026-06-16 EOD+2 | **First-load fails on mobile (black screen / stuck spinner / empty skeleton) across staff app AND every customer link → SHIPPED + DEPLOYED + L1-verified LIVE; adversarial bug-hunt loop CONVERGED (R1→R4)** — User: เข้าแอปด้วยมือถือบางทีโหลดไม่ติดในทีเดียว (frontend/backend + ลิ้ง PatientForm/ED-Scoring/ดูข้อมูล/ดูนัด) ค้างจอดำ/ค้างโหลด/ขึ้นแต่ skeleton ไม่มีดาต้า → ต้อง refresh/เปิดปิด/สแกนใหม่. `/brainstorming`(Q1 no-cache fresh-always / Q2 auto-retry+ปุ่ม / Q3 all-test-layers+L2)→spec→`/writing-plans`(14 tasks)→inline impl→**adversarial Workflow loop (Ultracode)**. **Root causes (Explore map)**: (1) 🔴 `firebase.js` bare `getFirestore(app)` — NO long-polling fallback → flaky mobile/carrier half-connects the WebSocket (looks connected, snapshot stream dead) → stuck/empty; (2) 🔴 no timeout on ANY onSnapshot → first-snapshot-never-fires = stuck forever; (3) 🔴 no UI escape (only fix = guess-to-refresh); (4) App.jsx anon-auth gate could hang on a black "กำลังโหลด..." forever if `signInAnonymously` stalls. **Fix**: `firebase.js`→`initializeFirestore({experimentalAutoDetectLongPolling:true})` (big lever, global, NO persistence per Q1) + NEW shared `firestoreReconnect.js` (module-debounced disableNetwork→enableNetwork; V17+hooks share ONE debounce) + `useResilientLoad.js` (loading/ready/error + 8s soft-timeout → 1 silent auto-retry → error+`ลองใหม่`; SYNC settledRef guard; resetKey re-arm) + `LoadErrorRetry.jsx` (theme card, no-red-on-name). Wired App.jsx (V17→shared + resilient anon-auth gate kills black-screen) + PatientForm/ClinicSchedule/PatientDashboard (resilient + escape; PatientDashboard retry-budget widened) + AdminDashboard (queue banner, resetKey:selectedBranchId) + useBranchAwareListener (silent auto-heal, backend tabs) + BackendDashboard (Suspense chunk retry). KEPT V16 render-gate + V17. **Adversarial loop CONVERGED** (the user's "วนแก้จนไม่เจอบั๊ค"): R1 6-finder/verify → 1 race fixed (statusRef synced post-render → sync settledRef); R2 → resetKey re-arm gap fixed; **R3 → orphaned-timer bug that MY OWN R2 fix introduced** (re-arm no-op'd setLoadStatus when already loading → stale mount-timer survived → premature reconnect) fixed (resetKey in timer-effect deps); **R4 → 0 findings (clean)**. **Verified**: full vitest **16673/0** (+~65; lone utils.test.js full-suite blip = confirmed transient parallel flake, green isolated 84/0 + clean re-run) + build clean + **Rule Q L1 Playwright 3/0 on the LIVE deployed build** (iPhone-13: blocked-auth→ลองใหม่→recover; half-dead-firestore→auto-recover-WITHOUT-refresh captured live t3s `กำลังโหลด`→t10s resolved; normal→resolves) + **Rule Q L2 7/0 real prod** (`e2e-mobile-load-coldstart.mjs`: exact anon+staff queries resolve under BOTH autoDetect AND FORCED long-polling → fallback transport proven on prod). DEPLOYED `lover-clinic-p4uawr0kx` (vercel, frontend-only, firestore.rules UNCHANGED → no Probe-Deploy-Probe; HTTP 200). **Lessons**: (a) the REAL "stuck" is a HALF-DEAD connection (request hangs, no error) — `route.abort()` does NOT reproduce it (the SDK serves a fromCache snapshot fast); only a HANGING request does — design your repro accordingly; (b) the fix recovers via `disableNetwork`→fromCache snapshot at the 8s soft-timeout (no refresh); (c) doc-not-found MUST call markReady (a successful load); only never-fires/onError → retry; (d) a state-synced ref read by a setTimeout is RACY (timer can fire before React commits) → set the guard ref SYNCHRONOUSLY; (e) the adversarial hunt caught a bug MY OWN fix introduced (R3) — the loop earned its keep; (f) hunt agents repeatedly OVERSTATE severity ("critical"/"regression" that weren't) — adjudicate every finding yourself (V162). Spec/plan `docs/superpowers/{specs,plans}/2026-06-16-mobile-load-reliability*`. |
| V163 | 2026-06-09 EOD+3 | **"แก้คงเหลือ" (ลด/เพิ่มคงเหลือคอร์ส) PROD CRASH `parseQtyString is not defined` — build-invisible missing static-import (V6/V11/V104 class) — `/systematic-debugging` + ultracode, FIXED + Rule Q L1-verified, NOT deployed** — User (verbatim, angry): *"ไม่เห็นจะลบหรือลดคอร์สได้เลย มันขึ้นแบบนี้ ที่มึงทำมามึงไม่ได้เทสเหรอไอ้สัส"* (screenshot: customer LC-26000138, Shock Wave, modal "แก้คงเหลือ" → กด ยืนยันลด/เพิ่ม → `alert: parseQtyString is not defined`; preview "6/12 → 5/12" worked, only SAVE crashed). **Root (single instance)**: `adjustCourseRemainingQty` (`backendClient.js`, the 2026-06-09 `b8351546` "unified add/reduce" refactor) calls `parseQtyString` (reduce branch + post-mutate unit-resolve) but the module-top **static** `import { …5 helpers… } from './courseUtils.js'` **OMITTED `parseQtyString`** — it had the other 5 (`formatQtyString`/`reverseQty`/etc, which is why THOSE worked in the same fn). Every other user either top-level-imports it OR does its own per-function `await import('./courseUtils.js')`; `adjustCourseRemainingQty` relied on the static import that lacked it. An undefined identifier → global lookup → **`npm run build` CLEAN** → only throws at runtime on the save click (V6 `handleSyncCoupons` / V11 mock-shadow / V104 param-shadow family). **Fix (class-eliminating, 1 line)**: add `parseQtyString` to the static import → module-scoped for EVERY function (incl. any future course mutator). courseUtils is a pure leaf → no circular-dep risk. **Why it slipped 16326-green (V66 lesson)**: the existing default-suite C1.6–C1.13 are **source-grep only** (grep the function text, never EXECUTE) → can't catch a runtime ReferenceError; the lone execution test that hits the real fn lives in opt-in `tests/extended/` (buried in the 283 stale fails). **Rule P expansion**: 5-agent adversarial Workflow swept all 52–83 courseUtils usages across 13 files + the sibling `stockUtils` per-fn-destructure class (longest fn `_deductOneItem` 551 lines + all 4 stock fns) → **0 other broken instances** (2 auto-flags refuted as comment/JSDoc). **Tier-2**: NEW `tests/av192-courseutil-scope-execution-2026-06-09.test.js` — **EXECUTES** the real `adjustCourseRemainingQty` (reduce/add/cap/floor) with ONLY the Firestore tx boundary mocked (parseQtyString import resolution 100% real ESM) → **failing-test-first proven**: 7 RED with `ReferenceError: parseQtyString is not defined` on the reverted import, 8 GREEN on the fix; AV192.7 locks the import list; AV192.8 = CLASSIFIER over every backendClient fn. **AV192** added. **Rule Q L1 (gold)**: real browser (local Vite dev → real prod Firestore), seeded TEST-AV192 customer (Shock Wave `6/12`), drove the EXACT flow `แก้คงเหลือ → -ลด → ยืนยันลด -1 / หมอมายด์` → **no alert**, modal closed, UI shows **5/12**, Firestore `courses[0].qty="5 / 12 ครั้ง"` (was `6/12`), reduce audit entry appeared; TEST fixture cleaned. full vitest green (pending final run); build clean. NOT deployed (V18 — awaiting "deploy"). **Lessons**: (a) **a missing entry in a module-top static destructured import is BUILD-INVISIBLE** — the undefined identifier becomes a global lookup; only a real RUN (execution test / L1) catches it. (b) **source-grep on a function ≠ verification** (the C1.x greps were green while prod crashed) — runtime-failing code needs EXECUTION coverage in the DEFAULT suite, not just opt-in `tests/extended/`. (c) keep a leaf-util's static import a SUPERSET of every export the file uses; per-function dynamic imports are belt-and-suspenders, never something to RELY on for a name the static import dropped. |
| V162 | 2026-06-09 EOD+1 | **TFP buy critical-path: qty-multiply DISPLAY≠SALE≠PERSIST divergence + per-purchase rowId/remove collision (course AND promo) — `/systematic-debugging` + ultracode, COMMITTED, NOT deployed** — User (verbatim, angry): *"ซื้อคอร์สมากกว่า 1 ... ราคาดันขึ้นแบบรวม ... กดติ๊กของคอร์สแรก แล้วไปบังคับคอร์สที่ 2 ให้ติ๊กด้วย ... จุดซื้อขายของ แม่งไม่น่าให้อภัยจริงๆ ... แก้ไขให้ไม่มีบั๊คแบบนี้หลงเหลืออยู่อีก"*. **Bug#1 (qty)**: buy "IV Drip Chelation 1 ครั้ง" qty 3 → "ข้อมูลการใช้คอร์ส" panel showed **1×** while the bill charged **3×** (10,500฿). Root = `buildPurchasedCourseEntry` products branch displayed `String(p.qty \|\| item.qty)` (un-multiplied) while the sale charges `unitPrice × buyQty` AND `resolvePurchasedCourseForAssign` persists `p.qty × pQty` → **DISPLAY ≠ SALE ≠ PERSIST** (promotions already multiplied — V42). **Bug#2 (checkbox/remove collision)**: same course bought twice → ticking course-1 forced course-2; deleting one purchase deleted both. Root = per-purchase identity keyed off the **MASTER `item.id`** (`rowId: purchased-${item.id}-row-${pid}`; the `now` token lived on courseId, NOT rowId) → COLLIDING rowIds in the `selectedCourseItems` Set + `removePurchasedItem`'s `courseId.startsWith(purchased-course-${item.id}-)` matched BOTH buys (same class in the promo path). **Fix (Rule P class-wide, one mechanism)**: a per-purchase **`purchaseUid`** (confirmBuyModal counter ref) threaded through courseId + EVERY rowId (`purchased-${id}-${uid}-row-${pid}`) + grouping keys (`buildCustomerCourseGroups`/`buildCustomerPromotionGroups` surface it; buy-this-visit promos key `__addon__|${uid}`) + `removePurchasedItem` targeting (by purchaseUid; master-prefix legacy fallback) + promo consumables (`mapPromotionProductsToConsumables` carries it, `filterOutConsumablesForPromotion` prefers it); PLUS `buildPurchasedCourseEntry` multiplies sub-product remaining/total by `buyQty = Math.max(1, item.qty)` → DISPLAY === SALE === PERSIST. SaleTab already correct (qty×pQty; no in-session course-use UI). **Investigation**: 6-agent parallel Workflow — **Rule Q caught Agent C's WRONG "no collision" claim** (it confused courseId-with-`now` for item.id); trusted the verified majority + first-hand reads. **Verified**: default vitest **16300/0** (16277 + 23 new `course-buy-qty-multiply-and-rowid-uniqueness` incl. display===persist A6 + checkbox-independence B5 + source-grep SG1-4) + build clean + extended `treatmentBuyHelpers` 54/54 (+4 V21 BPCE fixups) + 8 affected runnable 217/217 + **Rule Q L1 real-browser of the actual Vite-SERVED module** (`IV Drip 3 / EDTA 7.5 / Vit C 30` + `displayEqualsPersist:true` + rowIds `…-uA-row-P` ≠ `…-uB-row-P` + checkbox-independent + targeted-remove keeps the other buy). All rowId/courseId consumers use `startsWith('purchased-'/'promo-')` → new uid-format compatible (no missed consumer). **AV190** locks per-purchase-uid + display===persist. NOT deployed (V18). **Lessons**: (a) **a quantity computed at multiple surfaces (display / sale-line / persisted course) must derive the SAME factor** — two multiplied, one didn't → user saw the gap; the display===persist test (A6) is the permanent guard. (b) **per-purchase identity must carry a per-purchase token on EVERY identity string** (courseId AND rowId AND grouping key AND remove target), never the shared master `item.id` — a Set keyed by a colliding id silently merges state (V12/rowId-contract family). (c) **Rule P paid off** — the promo path had the identical latent collision; user only reproduced regular courses. (d) **Rule Q adversarial-verify of the AGENTS** — one investigator confidently said "no collision"; the real served-module browser run + my reading refuted it. **Pre-existing flagged (NOT V162)**: extended suite 283/4699 fail = V50-deleted AppointmentTab/MasterDataTab/CloneTab still referenced by 46 stale RTL tests (opt-in, not the tracked baseline). |
| V161 | 2026-06-03 EOD+4 | **Staff-chatbox fresh-adversarial bug-hunt LOOP — 10 latent bugs across the hide-don't-unmount blast-radius + send-path-orphan + server classes; looped until a fresh hunt found nothing (Rule P + Rule Q-honest) — NOT deployed** — User (verbatim, "ultrathink"): *"ต้องหาเทสอะไรก็ได้ที่ไม่เคยมีมาก่อน ... มันต้องมีบั๊คที่ไม่เป็นไปตามจุดประสงค์ของโปรแกรมอยู่อีกแน่นอน ผมมีกลิ่น มันยังไม่สมบูรณ์หรอก ... วนลูปการแก้ ... จะหยุดจนกว่าเริ่มข้อ 1 ใหม่จะไม่เจอบั๊ค"*. `/systematic-debugging` + a thorough `Explore` sweep, each bug = failing-test-first → fix → green + an AVxx. **Two architectural classes + singletons**: (a) **hide-don't-unmount blast-radius** (the V160 class — converting a per-scope panel from unmount-on-close to render-hidden silently breaks EVERY behavior that assumed open===remount): **H2** the always-mounted `StaffChatWidget` (1 mount inside BranchProvider) let a composer draft + reply-target SURVIVE a top-right BranchSelector switch → a draft for branch A sendable to branch B (cross-branch audience leak; AV180) → fix `<StaffChatComposer key={selectedBranchId}>` + hook `setReplyingTo(null)` on resubscribe; **H11** inline `<video>/<audio>` kept PLAYING when the panel is `display:none` (AV182) → pause on the hidden transition; **open-gate** the on-OPEN scroll/mark-read had to fire off the visibility transition (extends V160). (b) **send-path uploaded-then-orphaned** (D, AV188): the pipeline mints messageId → uploads to `{messageId}/` → THEN creates the doc; a partial-upload failure (composer returns, retry mints a NEW id) OR a doc-create failure after upload (send swallows its addDoc error) orphans the blobs — clean at the source via a shared folder-sweep `deleteStaffChatAttachmentFolder` (extracted from `deleteStaffChatMessage`, Rule of 3) at both sites, not lean on the 30-day sweep. (c) singletons: **H4** new-message auto-scroll yanked a user who scrolled up → conditional on at-bottom (AV181); **mention-spaces** @mention extraction failed on displayNames with spaces → candidate-aware longest-match (AV183); **cursor same-ms tie** unread tiebreak by message.id (AV184); **sticker object-URL** minted per-render leak → once-per-item + revoke (AV185); **S2** officeToPdf Cloud Function dropped its status-patch when the message doc didn't exist yet (race: doc created only after Promise.all(uploads)) → bounded retry `patchOfficeAttachment` (AV187); **S1** retention orphan-sweep capped the Storage listing at 2000 + sequential doc checks → pageToken pagination + bounded-parallel `mapBounded` (V122 class; AV186). **Verified**: each fix has a dedicated test (`staffchat-draft-branch-scope` · `-reply-branch-scope-hook` · `-no-yank-while-reading` · `-pause-media-on-minimize` · `-mention-spaces` · `-cursor-same-ms-tie` · `-sticker-objecturl-leak` · `-officetopdf-patch-retry` · `-retention-orphan-pagination` · `-upload-orphan-cleanup`), staff-chat family **648/648**, full vitest **16127/16130** (the 3 reds are PRE-EXISTING flakes in untouched files — bsa-task7 execSync git-grep matches a comment in TFP:913 · v85-glow cmd.exe `grep` not on PATH · genShortId birthday-paradox — all pass isolated). H2 + the client surface L1-verified (Chrome MCP, real browser, restored user's branch after). **Honest gap (Rule Q-honest)**: S1 (cron) + S2 (Cloud Function) ship local; their real-prod L2 needs a deploy. The final fresh hunt (rules cross-branch/authz · message same-ms ordering · drafts · reactions[absent] · unsend+Storage cleanup · 50-msg cap=intentional-scope) found NOTHING purpose-breaking → **loop converged**. AV180-188 lock the roster. **Lesson**: a "smell" with no repro IS worth a structured adversarial sweep — 10 real latent bugs surfaced; the recurring root is *changing a mount/lifecycle model (hide-don't-unmount) silently voids every behavior coupled to the old model* + *upload-before-doc-create always needs source-side orphan cleanup, never just a backstop sweep*. |
| V160 | 2026-06-03 EOD+3 | **Hide-don't-unmount BROKE the staff-chat read-cursor + scroll-on-open (V82-class recurrence) — same-session regression of my own change, root-caused + fixed + real-prod-verified, NOT deployed** — Shipping draft-persist via **hide-don't-unmount** (keep `StaffChatPanel` mounted, `display:none`, on minimize so the composer draft survives) silently broke the `StaffChatMessageList`'s on-OPEN behaviors: they all assumed *open === a fresh remount*, but with the panel kept mounted "open" became a **visibility transition, not a mount**. User (verbatim, recurrence): *"กดปุ่มลง/scroll ลงล่างสุด แล้ว refresh/เปิด tab ใหม่ → เด้งขึ้นไปที่เดิม ไม่ save checkpoint ที่อ่านถึงจริง ... อาการเดิม ... ห้ามพลาดตรงนี้อีกเด็ดขาด"*. `/systematic-debugging` (Iron Law — no fix before root cause) + **real-browser evidence (Chrome MCP JS probe)**: (1) open after fresh-load → `scrollTop 0`, **`distanceFromBottom 5882`** = opens at the TOP (the `[lastMessageId]` auto-scroll ran while hidden → `scrollHeight 0` → no-op → never re-fired on open); (2) programmatically scrolling to the TRUE bottom (`distanceFromBottom 0`) did **NOT** advance the cursor + the jump button stayed stuck → **the IntersectionObserver, created while `display:none`, is STUCK** (a node with no layout box never reports `isIntersecting` once shown) → `markScrolledToBottom` never fires → read checkpoint never persists. **Fix**: `StaffChatMessageList` takes a `visible` prop (`visible={!chat.minimized}`); on the hidden→visible transition it (a) scrolls to the true bottom (`[visible]`-keyed effect), (b) marks-read **directly** (`onScrolledToBottom` via a ref — robust, not reliant on the observer firing after a display toggle), (c) **re-creates** the observer (`if (!visible) return` + `visible` in deps). **Verified real browser**: open → `distanceFromBottom 0` + cursor `updatedAt` advances + persists across reload + jump button correct; full vitest **16071/0** + build clean + `tests/staffchat-read-cursor-on-open.test.jsx` (R1 mark-read on visible-transition only + R2 observer gated/re-created + SG1-4) + collateral green + **AV179**. **Lesson**: converting a panel from unmount-on-close to render-hidden (to preserve in-progress state) is NOT free — EVERY on-open behavior that relied on the per-open remount (auto-scroll, cursor advance, IntersectionObserver creation) must be re-triggered off the **visibility transition**; an IntersectionObserver created on a `display:none` node never recovers (re-create it per visible-transition). The draft-persist feature itself was correct; the regression was the *side-effect on a sibling component's mount-coupled behavior* — exactly the class to grep for whenever you change a mount model. |
| V158 | 2026-06-03 | **CONCURRENT double-cancel double-reverses wallet (real money LEAK) + over-reverses points — V153's idempotency QUERIES were never concurrency locks (iron-clad Rule T) — VERIFIED real prod, NOT deployed** — 4-system audit loop round 16 (the gap V153 EXPLICITLY documented: "Query (not in-tx) → idempotent for the SEQUENTIAL trigger, NOT a concurrency lock"). `refundToWallet` (Σdeduct−Σrefund) + `reversePointsEarned` (Σearn−Σreverse) computed "outstanding" via a QUERY OUTSIDE the tx → two CONCURRENT cancel cascades on the SAME sale (double-click / two admins) both read the same sums → both refund/reverse → wallet over-credited (re-spendable baht) + loyalty points over-reversed. **Confirmed Rule Q L2** `scripts/e2e-r16-concurrent-double-cancel.mjs` (was 2-fail proving the leak → 5/0 after): 2 concurrent cascades on a sale (deposit 300 / wallet 200 / +50 pts) → wallet **1200 (+200 LEAK) / pts 150 (−50 over-reversed)** BEFORE, **wallet 1000 / pts 200, exactly 1 refund + 1 reverse txn** AFTER. (deposit held — M1 in-tx usageHistory; course held — terminal-skip.) **Fix (per-reference NET marker ON the doc, read IN-tx)**: `deductWallet`/`_earnPointsInternal` maintain `saleNet[refId]` / `finance.pointsSaleNet[refId]` += amt INSIDE their existing tx; `refundToWallet`/`reversePointsEarned` read that marker IN-tx (outstanding = marker, decrement by the amount) → Firestore OCC serializes, the 2nd reverse re-reads the decremented marker → reverses 0. The old Σ query is kept ONLY as a LEGACY SEED for pre-V158 references (no marker) — `if (refId && outstanding < amt) skip`. Edit-safe (deduct re-increments the net). Same Rule-T family as V147/V148/V149/V155 — completes the atomic-RMW class for the CONCURRENT (not just sequential) reverse path. **Verified**: R16 5/0 + reverse-idempotency 11/0 (PE harness made faithful — re-earns now via real `earnPoints` not raw `addEarnTx`, mirroring a legacy sale edited post-V158) + points-concurrency 2/0 + R13 11/0 + R14 7/0 + `tests/v158-concurrent-reverse-marker.test.js` 14/0 + full vitest **15970/0** + build clean. **V21 fixups**: v149 (summary write no longer a single inline literal — `_upd`/marker key; lock the in-tx `tx.update` + `'finance.loyaltyPoints': a` invariant, not the literal shape) + v153 (fnBody window 3400→4400 since V158 grew the fns; B2 → V158 in-tx marker short-circuit). **DEPLOYED** (vercel --prod, aliased lover-clinic-app.vercel.app; client-SDK only → no Probe-Deploy-Probe, V153-V157 precedent) AFTER the loop converged — R16 found V158; R17-R22 = 6 consecutive FRESH clean rounds (concurrent-edit 8/0 · course-lifecycle 6/0 · 4-system-capstone cancel-cascade 21/0 · report-accuracy audit · deposit-multi-sale 12/0 · adjust×marker-interplay 9/0) = stop-condition met (matches prior stock-loop 6-clean bar). **Lesson**: a query-based idempotency guard is idempotent for SEQUENTIAL repeats but NOT for CONCURRENT calls — two callers both read the pre-state. The fix is a per-reference net marker stored ON the doc + read/written IN the SAME tx so OCC serializes; keep the query as a legacy seed for un-marked history. "Not a concurrency lock" in a comment IS a latent bug, not a disclaimer. |
| V157 | 2026-06-03 | **Cross-collection torn-write SILENT-aspect FIXED — TFP auto-sale + SaleTab now SURFACE failed money/course side-effects (was console-only → invisible) — NOT deployed** — 4-system audit loop (the item I'd wrongly DEFERRED; user: "ห้าม defer, fix everything thinkable"). createBackendSale + TFP auto-sale write many collections via sequential awaits; a side-effect throw (applyDepositToSale "ยอดมัดจำไม่พอ" / deductWallet insufficient / assignCourse fail) was caught with `console.warn` ONLY → the sale RECORDS depositApplied/walletApplied/courses but the money/course didn't move → SILENT discrepancy invisible to the clinic (logs nobody reads). Firestore CANNOT make an 11-collection write atomic (deliberate non-blocking design: treatment/sale MUST save) → the correct fix is to make a failed side-effect DETECTABLE, not fake atomicity. **Fix (additive — happy path untouched; catches only run on failure)**: collect each value-channel failure (deposit/wallet/course/promo, create + 2 edit sub-paths) into `sideEffectWarnings[]` + a non-fatal `window.alert('บันทึกสำเร็จ ✓ แต่บางรายการไม่สำเร็จ กรุณาตรวจสอบ: …')` before the success screen. SaleTab same (its deposit/wallet already throw-loud; course/promo were silent). **Self-caught during the fix (Rule Q-honest)**: my reverse-reapply push used `${walletApplied}` but TFP's scope var is `walletAppliedValue` → would ReferenceError when the catch fires; fixed + locked by a source-grep anti-regression (`not.toMatch(/\${walletApplied}/)`). **Verified**: build clean + full vitest green + `tests/v157-side-effect-warning-surfacing.test.js` (7/0). **Residual (honest)**: cross-collection NON-atomicity remains a Firestore reality (can't 11-collection-tx); but side-effects are now mostly-idempotent (V153/V154 → retry converges) AND failures are now VISIBLE. A full auto-reconciliation/compensation report = optional next-level feature. Committed local; NOT deployed (V18). **Lesson**: when a multi-collection write can't be atomic, the bug isn't the non-atomicity (unavoidable) — it's the SILENCE. Surface every swallowed money/inventory side-effect failure so it's reconcilable; never `console.warn`-and-forget a value step. |
| V155 | 2026-06-03 | **Deposit refund/cancel + membership-renew non-atomic RMW → concurrent/double-click lost-update money leak (M18, iron-clad Rule T) — VERIFIED real prod, NOT deployed** — 4-system audit loop rounds 7-10. `refundDeposit`/`cancelDeposit`/`renewMembership(renewals[])` were getDoc→updateDoc → 2 concurrent refunds (or a double-click) both read the same remaining/refundAmount, last-write-wins → one refund's RECORD lost → refundAmount understates cash paid → deposit over-stated → re-spendable money. **Confirmed Rule Q L2** `scripts/e2e-deposit-refund-atomicity.mjs` (1-fail→0): 2 concurrent refundDeposit(300) on a 1000 deposit → refundAmount **300 (one lost)** BEFORE, **600** AFTER. **Fix**: each read+guard+write in ONE runTransaction (tx.get→tx.update; over-refund + usedAmount guards re-checked in-tx). Same Rule-T family as M5/V147/V148/V149. **M18** + `tests/v155-deposit-membership-atomicity.test.js` (13/0). **Round-10 Rule-T sweep CLEAN**: all other money-balance RMW already atomic (wallet topUp/adjust M5; points earn/deduct/adjust V149; INV/HN/PO counters runTransaction) → the atomic-RMW class is now COMPLETE across the money domain. Committed local; NOT deployed (V18). **Loop dismissals (Rule Q-honest, code-read refuted)**: sale-variants online/vendor/insurance = tracking-only (no money) · exchange/refund-course (atomic + idempotent-by-throw + move no money) · appointment-delete-orphans-deposit (`deleteBackendAppointment` never touches the deposit → balance preserved, not a leak). **Deferred = ARCHITECTURAL (not a clean bug; NOT half-fixed — Rule Q-honest)**: cross-COLLECTION torn-write — createBackendSale + TFP auto-sale write many collections via sequential awaits; a rare Firestore mid-chain throw leaves the sale committed but a side-effect (wallet/deposit/points/course) silently failed (deliberate "non-blocking" design + now mostly-idempotent side-effects). Proper fix = a reconciliation/detection report or structured partial-failure surfacing (a FEATURE), NOT a code patch (throw-instead-of-swallow worsens UX: user thinks save failed → re-saves → double-create). Recommend a dedicated reconciliation feature. **(SILENT aspect FIXED in V157 — failures now surfaced to admin; only the optional auto-reconciliation report remains.)** **V156 (same commit)**: defensive `roundTHB` at the 6 THB money-write boundaries (refundDeposit/applyDepositToSale/topUp/deduct/refund/adjustWallet) — closes the M12 float-precision caveat (round-11's only finding; zero behavior change for clean inputs; `adjustPoints` excluded — points are whole). |
| V154 | 2026-06-02 EOD+5 | **Deposit reverse dropped the `refundAmount` term → phantom deposit balance (money leak; M17, M3-family) — VERIFIED real prod, NOT deployed** — 4-system audit loop round 2 (the C-D deferred from V153). `reverseDepositUsage` recomputed `newRemaining = amount − newUsed`, OMITTING `refundAmount` — but the invariant is `remaining = amount − usedAmount − refundAmount`. **Trigger**: a deposit partially APPLIED to a sale AND partially manual-REFUNDED (`refundDeposit`), then the sale is cancelled → reverse restores the FULL amount, FORGETTING the already-paid-out refund → deposit over-states → customer re-spends refunded money. **Confirmed Rule Q L2** `scripts/e2e-deposit-refund-reverse.mjs` (2-fail → 0-fail): 1000 → apply 500 → refundDeposit 200 → reverse → remaining **1000 (LEAK +200)** BEFORE, **800** AFTER; no-refund control still restores full 1000 (D6). **Fix (1 term, inside the existing runTransaction)**: `newRemaining = amount − newUsed − refundAmount` (clamp ≥0 unchanged). **M17** + `tests/v154-deposit-reverse-honors-refund.test.js`. Committed local; NOT deployed (V18). **Note (Rule-T-class, deferred)**: `refundDeposit` + `cancelDeposit` are getDoc→updateDoc (non-atomic money RMW) — low concurrency (manual admin), not THIS leak; flagged for a later micro-fix. **Lesson**: a reverse that RECOMPUTES a balance from a base figure (`amount`) must subtract EVERY term that decremented it (usage AND refund) — `amount − used` silently re-credits any other deduction; prefer incremental (`remaining + restored`) or include all terms. |
| V153 | 2026-06-02 EOD+5 | **Sale-cancel cascade reverse ops NOT idempotent → DOUBLE wallet refund (real spendable baht) + DOUBLE points reverse (M16, iron-clad Rule-T idempotency sibling) — VERIFIED real prod, NOT deployed** — 4-system audit (TFP/Stock/Sales/Finance) `/systematic-debugging` loop, round 1. The sale-cancel cascade (SaleTab cancel-modal + `handleDelete` + TFP edit→sale) runs reverseDepositUsage(idempotent✓ usageHistory filter) / `refundToWallet`(✗) / `reversePointsEarned`(✗) / applySaleCancelToCourses(idempotent✓ terminal-status skip) / reverseStockForSale(idempotent✓ S5 CAS) — only **wallet + points lacked the idempotency the other three already had**. `reversePointsEarned` summed `type==='earn'` ONLY (ignored prior `reverse` txns) → re-reversed every call; `refundToWallet` had ZERO referenceId dedup → re-credited. **Trigger (plausible, SEQUENTIAL not a race):** cancel a sale (reverses wallet+points), then DELETE that cancelled sale (handleDelete reverses AGAIN); OR a cancel RETRY after `cancelBackendSale` throws (V105 re-deducts stock, resets `cancelSaving`, alerts "ใบเสร็จยังคงอยู่", returns → admin re-clicks ยืนยันยกเลิก). **Confirmed Rule Q L2** `scripts/e2e-reverse-idempotency.mjs` (was **4-fail proving the leak → 0-fail proving the fix**): 2× refund on same sale → wallet **+200 / 2 refund txns** BEFORE, **+100 / 1 txn** AFTER; 2× points-reverse on 100pts/50-earn → **0 (over-reversed by 50)** BEFORE, **50** AFTER. **Fix (function chokepoint — covers all 3 callsites each, Rule O-style):** `reversePointsEarned` reverses `max(0, earnedSum − alreadyReversedSum)` (sums earn AND reverse); `refundToWallet` refunds only up to the NET still-deducted for the sale (`Σdeduct − Σrefund` for the referenceId) → idempotent on cancel→delete (outstanding 0) AND edit-safe (a re-deduct restores outstanding). **Round-3 self-catch (Rule Q-honest):** my FIRST wallet guard ("skip if ANY refund txn exists") BROKE the sale-EDIT path — the edit flow legitimately refunds→re-deducts the SAME saleId per edit, so the 2nd edit's refund got skipped → wallet DOUBLE-deducted (customer loses money). The full suite was GREEN (coverage gap — no test does a 2nd sale-edit with wallet); only the NEW `WE` edit-scenario e2e caught it. Corrected to net-outstanding (query-guard = sequential-idempotent, not a concurrency lock — documented). **No regression**: importing tests green (v149-points-atomicity, phase12.2b-cancel-cascade-simulate, scopedDataLayer) → 0 V21 lock-in; build clean 2.92s; `tests/v153-reverse-idempotency.test.js` 19/0. **M16** added to audit-money-flow (the aspirational Stripe-idempotency note was wrong for wallet+points — only stock/deposit were idempotent pre-V153). Committed local; NOT deployed (V18). **Skeptical filter (loop discipline — 2 agent "HIGH" findings DISMISSED by code-read):** concurrent-deposit-apply-exceeds (applyDepositToSale HAS in-tx M1 guard; agent self-contradicted vs its own wallet analysis) + concurrent-stock-lost-update ("writeBatch" = stale; V147 made it runTransaction+retry, R17 verified) + C-A deleteBackendSale-bypasses-reverse (agent read the LEAF fn; the `handleDelete` CALLER reverses deposit/wallet/points/course/stock FIRST). **Lesson**: a cancel cascade is only as safe as its LEAST-idempotent member — when ONE channel has an idempotency guard (deposit M1, stock S5), AUDIT EVERY sibling channel for the same property; "the cascade reverses everything" (M15) is necessary but NOT sufficient if a re-run double-applies. cancel→delete + retry-after-failure are real sequential re-run paths. **Deferred to next round**: `refundDeposit` (manual admin refund, NOT in the auto cascade) double-decrement on double-click + refundDeposit↔reverseDepositUsage interaction (C-D); cross-COLLECTION torn-write (C-C, architectural). |
| V152 | 2026-06-02 EOD+4 | **Concurrent central-PO receive → DOUBLE stock (read-outside-tx idempotency gap) (S35) — VERIFIED real prod, NOT deployed** — Round 15 (post V147-V150 deploy). `receiveCentralStockOrder`'s idempotency checkpoint read `order.receivedLineIds` via `getCentralStockOrder` (getDoc, OUTSIDE a tx), created batches, then wrote `receivedLineIds` only at the END → two concurrent receives (admin double-click "รับสินค้า" / two admins) both saw `receivedLineIds=[]`, both created batches → **DOUBLE stock** (an 8-unit PO received as 16 / 2 batches). This was a **documented-deferred AUDIT-V34 gap** ("KNOWN CONCURRENT-RECEIVE GAP (deferred to V35)"); R15's Rule Q L2 surfaced it (Agent had code-read it as "safe" — but it's not a CAS; transfer/withdrawal ARE safe because their status transition is a CAS-in-tx → R8/R13/R14 clean). **Confirmed Rule Q L2** `scripts/e2e-stock-order-cancel-central-receive.mjs` C2: 8-unit PO receive-twice → **16 units / 2 batches** BEFORE, **8 / 1** AFTER (fulfilled=1/2, loser's CAS collapses the dup). **Fix**: a `runTransaction` CAS atomically CLAIMS the to-receive lineIds into `receivedLineIds` BEFORE any batch is created (loser claims nothing → early-return, no batch); finalize is ALSO a CAS (re-read + MERGE, never overwrite). **No regression**: order cancel (C1 void-import + C1b double-cancel idempotent + C1c block-if-used) all clean. **S35** + `tests/v152-central-receive-cas.test.js`. Committed local; NOT deployed (V18). **Lesson**: an idempotency guard that READS the checkpoint outside a tx is NOT concurrency-safe (same read-outside-tx class as V147) — claim the checkpoint IN a CAS before the side-effecting work. "Documented-deferred" gaps are real bugs; a real-prod e2e (not a code-read) is what closes them (Agent's "safe" code-read was wrong). |
| V151 | 2026-06-02 EOD+4 | **Cancel a sale/treatment whose lot V144 auto-deleted → reverse THREW "vanished" → stock permanently LOST (S34) — VERIFIED real prod, NOT deployed** — Round 10: a real V144↔reverse feature-interaction. V144's real-time 0-lot clear DELETES a redundant 0-lot post-commit; a sale that drains lot A to 0 (lot B live) → V144 deletes A → cancelling the sale → `reverseStockForSale`'s loop (NO try/catch) hit `_reverseOneMovement`'s `tx.get(A)` !exists → **threw `"Batch A vanished before reverse"`** → the WHOLE cancel FAILED + the customer's stock was NEVER returned. **Confirmed Rule Q L2** `scripts/e2e-stock-reverse-after-lotclear.mjs`: cancel threw + Σ stayed **5** BEFORE; **no-throw + Σ restored to 10** AFTER (conservation Σdelta 0). **Fix**: `_reverseOneMovement`, on a vanished lot, RE-CREATES it from the movement metadata (productId/branchId/name + returned qty as {total,remaining}, cost from costBasis, `_recreatedByReverse:true`) → stock restored + audit chain intact (reverse movement.batchId still matches). Existing-lot path unchanged. **No regression**: R2 normal reverse 7/0 + V138 D3/D4. **S34** + `tests/v151-reverse-recreates-vanished-lot.test.js`. Committed local; NOT deployed (V18). **Lesson**: a reverse must be ROBUST to a vanished lot — V144 (correct housekeeping) deletes 0-lots that still have reversible movements; the reverse (not V144) is responsible for restoring stock regardless of what happened to the lot (re-create, never throw-and-lose). |
| V150 | 2026-06-02 EOD+4 | **Negative-stock debt carrier could be an EXPIRED/cancelled lot → "dispensed expired" audit record (S33) — VERIFIED real prod, NOT deployed** — Round 7 of the stock hunt (medical/MOPH stock-purpose, non-concurrency). `pickNegativeTargetBatch` (Phase 15.7 — picks the lot that goes NEGATIVE on a deduct overshoot) filtered candidates by productId+branchId ONLY ("any status"). For a product whose ONLY lot is EXPIRED, a treatment deduct decremented the EXPIRED lot (10→7) → wrote a `negativeOverage` movement DOCUMENTING "3 expired units dispensed" (a MOPH-audit violation) + corrupted the expired lot's on-hand count. **Confirmed Rule Q L2** `scripts/e2e-stock-fefo-expiry.mjs`: E2 expired-only deduct → expired lot 10→**7** BEFORE, stays **10** AFTER (debt → synthetic AUTO-NEG). **Fix**: candidate `.filter` now excludes `status===CANCELLED`, `status===EXPIRED`, `hasExpired(b,now)` → falls through to Fallback C synthetic AUTO-NEG (clean debt carrier; expired lot intact for write-off). The `allocations`-present path was already expired-safe (batchFifoAllocate excludes expired). **No regression**: E1 (expired-not-dispensed-when-valid-exists) + E3 (FEFO order) 5/0; V138 D3/D4 (active lot still carries negative) 12/0. **S33** + pure-unit `tests/v150-negative-target-excludes-expired.test.js` (V150.1-7) + source-grep. Committed local; NOT deployed (V18). **Lesson**: an "any status" candidate set is a footgun — the negative-debt carrier must be an ACTIVE non-expired lot (or a synthetic), never an expired/cancelled lot (extends S9 expired-skip to the negative-carrier path). |
| V149 | 2026-06-02 EOD+4 | **Loyalty-points concurrency LOST-UPDATE → points lost / over-credited (AV178, iron-clad T) — VERIFIED real prod, NOT deployed** — Round 5 of the stock hunt (Rule-T class audit beyond stock/courses). `getPointBalance` reads the SUMMARY `finance.loyaltyPoints` (NOT a ledger sum), and the 3 points-mutators — `_earnPointsInternal` (fires on EVERY sale via `earnPoints`), `adjustPoints` deduct, `reversePointsEarned` — did `getPointBalance → setDoc(ledger) → updateDoc({finance.loyaltyPoints})` with NO tx → two concurrent point ops read the same `before`, last write wins → points LOST (the "M9 reconciler" is aspirational; none runs). **Confirmed Rule Q L2** `scripts/e2e-points-concurrency.mjs`: concurrent earn×2 on 100pts → **110** (10 lost) / deduct×2 → **90** (10 over-credited) BEFORE, **120 / 80** AFTER. **Fix**: all 3 read `finance.loyaltyPoints` INSIDE a `runTransaction` (tx.get(customerDoc) → tx.update + tx.set(pointTxDoc); deduct over-spend guard re-checked in-tx) — mirrors the already-atomic wallet M5. **R6 audit (loop convergence)**: wallet (M5) + deposits (M1 `applyDepositToSale`) + INV/HN counters already atomic; `updateMembership` is a field-set (no balance RMW); only `renewMembership` renewals[] is a negligible residual (manual/annual). full vitest (pending final run); build clean. **AV178** + `tests/v149-points-atomicity.test.js`. Rule T origin extended (V147+V148+V149). Committed local; NOT deployed (V18). **Lesson**: when one money path is made atomic (wallet M5), audit the WHOLE class — points was a sibling balance that got missed for the same reason (summary-as-truth + getDoc→updateDoc). |
| V148 | 2026-06-02 EOD+4 | **Course-deduction concurrency LOST-UPDATE → course silently OVER-CREDITED (AV177, iron-clad T) — VERIFIED real prod, NOT deployed** — Round 4 of the stock hunt (course analog of V147). EVERY `be_customers.courses[]` mutator was `getDoc(customerDoc)` → mutate courses[] → `updateCustomer({courses})` with NO transaction → two concurrent mutators both read the same courses[], last write wins → a use/buy/reverse is LOST → course over-credited (money-adjacent; the customer paid for N sessions; NO error). **Confirmed Rule Q L2** `scripts/e2e-course-deduct-concurrency.mjs`: **5 concurrent uses on a 5/5 course → remaining 4** (only 1 of 5 applied) BEFORE, **remaining 0** (all 5) AFTER. **Fix (class-wide, Rule P)**: shared atomic helper `_mutateCustomerCoursesAtomic` (runTransaction tx.get→mutate-in-place→tx.update; Firestore OCC serializes, loser auto-retries on top) — 8 writers routed (deduct/reverse/addQty/assign/resolvePick/addPicks via helper; exchangeProduct[2-field]/removeLinkedSale[filter]/applySaleCancel[was writeBatch] via inline-tx; exchange/refund/cancel-customer-course were ALREADY atomic). **Class verified** `e2e-course-mutation-concurrency.mjs` 4/0 (assign‖assign, deduct‖assign, deduct‖reverse, add‖deduct all apply) + behavior regression `e2e-v142-edit-resave-course-deduct.mjs` 10/0 (refactor preserved assign/deduct/reverse/carry-forward). full vitest **15872/0** (2 V21 fixups: v36 K.2 customerDoc→helper-indirection slice, phase16.5 G8.3 writeBatch→runTransaction); build clean. **AV177** + `tests/v148-course-mutation-atomicity.test.js`. iron-clad **Rule T** filed (V147+V148 = the concurrency-RMW class). Committed local; NOT deployed (V18). **Lesson**: a customer-doc array read-modify-write under concurrency needs runTransaction; `getDoc→updateDoc({courses})` silently over-credits (no error). |
| V147 | 2026-06-02 EOD+4 | **Stock-deduction concurrency RACE → concurrent treatment/sale on the SAME batch FAILED with a raw "Batch X raced" error instead of going negative (S32, iron-clad T) — VERIFIED real prod, NOT deployed** — `/systematic-debugging` + Rule Q hunt (user: "ระบบ Stock สำคัญมากๆ ... ผมมีกลิ่น ... วนลูปจนไม่เจอบั๊ค"). `_deductOneItem` reads candidate batches OUTSIDE the per-batch tx (FIFO needs the set), plans from that STALE snapshot, then the in-tx guard `if (beforeRemaining < takeQty) throw` fires on race; the Phase 15.7 negative-stock fallback only covers PLAN-time shortfall (`plan.shortfall>0`), so a RACE-time shortfall (stale snapshot showed enough → shortfall===0) bypassed it → the user-throw propagated (Firestore does NOT auto-retry a user throw) → the WHOLE save FAILED, violating "ตัดได้เสมอ (ติดลบได้)". **Confirmed Rule Q L2** `scripts/e2e-stock-concurrency-race.mjs`: **6/6 rounds raced→fail BEFORE, 6/0 pass AFTER** (treatment C1 + sale C4; conservation held throughout). **Fix**: tag transient in-tx throws `code='STOCK_RACE_RETRY'` (vanished/became/raced + 2 negative-push) + a bounded re-fetch/re-plan loop → the no-floor negative-stock push absorbs the race-time shortfall → deduction succeeds (converges in 1 retry). Single-doc-tx ops (createStockAdjustment, _exportFromSource transfer/withdrawal) read INSIDE their tx → Firestore contention-auto-retry covers them → NOT race-tagged (deterministic anti-negative preserved). **negative-stock-from-TFP/sale FULLY INTACT** (`e2e-negative-stock-tfp-sale-comprehensive.mjs` 18/0 — single/multi-batch FIFO/zero-batch synthetic/exact-zero/already-negative × TFP+sale; `e2e-negative-batch-directions.mjs` 12/0). full vitest **15868/0**; build clean. AV stock-flow **S32** + `tests/v147-stock-deduction-concurrency-race.test.js` + K.2/K.2-bis slice-window V21 fixup (25000→30000). Committed `59a11ad6`-after; NOT deployed (V18). **Lesson**: a multi-batch FIFO allocator that reads the candidate SET outside the tx must RETRY+RE-PLAN on a race (a stale-plan in-tx guard is a USER throw → Firestore won't auto-retry it); the negative-stock fallback covered plan-time but NOT race-time shortfall. |
| V145 | 2026-06-02 EOD+2 | **Stock-tab product-edit: full-doc load + whitelist + real-time columns + branch-refresh fix (AV175, BS-9) — DEPLOYED + L1/L2 verified** — `/brainstorming`→spec→plan→impl. tab=stock "แก้ไขสินค้า" passed the aggregated balance ROW (not the `be_products` doc) to ProductFormModal → blank/default fields + a `setDoc(merge:false)` overwrite-corruption risk (35 prod docs already carried stock-junk from this); the balance table's unit was the frozen `batch.unit`. **Fix**: full-doc load (StockBalancePanel listenToProducts map carries `.full`; StockTab/CentralStockTab `getProduct` fallback — never feed a row-shaped object to the modal) + `normalizeProduct` rewritten to a WHITELIST (drops stock-junk `batches/totalRemaining/totalCapacity/nextExpiry/expired/unit/valueCost/id`; preserves `stockConfig/createdBy/updatedBy` + forensic `_*`; +`__proto__`/`constructor`/`prototype` pollution guard) — whitelist enumerated from ALL **610 real prod docs** (Rule R), NOT guessed + live unit/category/type resolve + NEW หมวดหมู่/ประเภท columns (replaced ความจุ/มูลค่าทุน per user Q1). **🐛 BIG bug found via Rule Q L1 (real app) + Rule R diag**: the StockBalancePanel products-map effect had `[]` deps → STALE after a branch switch → every row showed "-" for category/type (BS-9/Rule L; pre-existing — the canonical name + skip-stock-filter were silently stale too; my NEW columns merely EXPOSED it). Fix → deps `[selectedBranchId]` (one fix restored cat/type + name + skip-filter; live round-trip verified, 147→134 rows = skip-filter restored). **🐛 Adversarial find (E1)**: the forensic `_`-prefix copy loop pulled `__proto__` → output prototype pollution → guard added. **Verified**: full vitest **15836/0** + **Rule Q L2 18/0 REAL prod** (`scripts/e2e-v145-product-edit-roundtrip.mjs` — 609-doc whitelist COMPLETENESS = zero legit-field loss + corruption round-trip + branchId preserved by `_resolveBranchIdForWrite`) + **Rule Q L1 REAL authed app** (new columns + live unit/category/type + full-doc edit modal vs the old blank load + branch-switch re-subscribe; screenshots). DEPLOYED `85063e5d` (`vercel --prod`, frontend-only — NO firestore.rules change → no Probe-Deploy-Probe, V125–V127 precedent). **V21 fixups**: v43/phase15.5 (`form`→`f` param rename) + phase15.6-capacity-tooltip repurposed as removal anti-regression + phase15.1 import-regex relaxed for `getProduct`. **AV175** (stock product-edit must load full doc + whitelist normalize). **DEFERRED (user → next session)**: `be_products` data cleanup — 36 junk/corrupted docs in นครราชสีมา; dry-run done (read-only); `scripts/v145-cleanup-polluted-product-junk.mjs` bundles junk-strip + restore cat/unit/type (28 auto from clean cross-branch copies + 1 same-branch + 7 manual/ProClinic) + dedup 3 true same-branch dups (FK-check first); run dry-run → review → `--apply` (Rule M two-phase + audit doc). **Lessons**: (a) **a list-view "edit" must load the FULL doc, never the aggregated row** — a row-shaped object + `setDoc(merge:false)` = silent overwrite corruption; (b) **a whitelist normalizer must be enumerated from REAL data (Rule R), not guessed** — a naive whitelist silently drops `stockConfig`/forensic; (c) **every branch-scoped listener needs its branch in deps (BS-9)** — a stale `[]`-deps listener fails silently because a fallback masks it; (d) **adding a NEW visible column is a cheap way to EXPOSE latent data-staleness** — the cat/type columns surfaced a pre-existing stale-map bug; (e) **the "281 duplicates" I first reported was wrong** — be_products is branch-scoped so per-branch copies aren't dups; corrected to group by branchId+name → only 3 true same-branch dups (Rule Q-honest, disclosed to user). Detail: checkpoint `2026-06-02-v145-stock-product-edit.md`. |
| V146 | 2026-06-02 EOD+2 | **Staff-chat reply quote shows image/file/sticker + click-to-scroll+bounce (AV174) — DEPLOYED + L1 verified** — `/systematic-debugging` (ultrathink). Replying to an image/file rendered a BLANK quote-bubble (snapshot captured only `msg.text`); the quote-card looked clickable but had NO onClick (dead affordance). **Fix (4-layer)**: single `buildReplySnapshot(msg)` (text + attachmentKind/attachmentThumbUrl/attachmentCount/isSticker, legacy `attachmentUrl` handled) + `replyPreviewMeta` (Thai icon+label: 📷 รูปภาพ / 📄 ไฟล์ PDF / 🎬 วิดีโอ / 🎵 ไฟล์เสียง / 📑 เอกสาร / 📎 ไฟล์แนบ / 🎟 สติกเกอร์) + `buildMessageDoc` persists the descriptor undefined-safe (no rules change — create rule tolerates replyTo sub-fields) + NEW shared `StaffChatReplyPreview` (BOTH the message quote-card AND the composer strip → no V12 sibling-drift) + quote-card `role=button` onClick → `StaffChatMessageList.scrollToMessage` (`scrollIntoView` center + `.staff-chat-reply-bounce` highlight 0.9s + reduced-motion variant). **Verified**: 36/0 touched-area + **Rule Q L1 real browser** (thumb + Thai label render + computed `animationName` resolves). DEPLOYED `85063e5d` (frontend-only, no Probe-Deploy-Probe). **AV174** (reply snapshot must carry a content descriptor; one shared preview both surfaces). **Lessons**: (a) **a reply/quote snapshot must capture a content DESCRIPTOR, not just text** — else every non-text reply renders blank; (b) **one shared preview component for both surfaces prevents V12 sibling-drift**; (c) **`cursor-pointer` without an onClick = a dead affordance** — style implies interactivity that doesn't exist. Detail: checkpoint `2026-06-02-v145-stock-product-edit.md`. |
| V144 | 2026-06-02 EOD+1 | **Stock ยอดคงเหลือ 4-issue batch — real-time 0-lot auto-clear + "หมด" filter + in-place adjust/order modals + balance follows global BranchSelector (AV172+AV173) — DEPLOYED + L2-verified real prod** — User reported 4 issues (2 `/systematic-debugging` rounds): (1) no "หมด" filter; (2) 0-lots linger (want real-time delete; **clarified: keep 1 if it's the LAST lot, delete all if a live lot exists** = exactly the existing `planLotCleanup` AV168); (3) ปรับ button bounces to the ปรับสต็อก tab; (4) two branch selectors (per-panel "สถานที่" dropdown vs the top BranchSelector → confusion). **Phase-1 caught a V66-class trap before ship**: the real-time helper deletes 0-lots via the CLIENT SDK, but `firestore.rules` had `be_stock_batches: allow delete: if false` → the delete would silently no-op (try/catch swallow) → "ทันทีเลย" UNMET → trust failure. Surfaced the conflict + offered (A) narrow the rule / (C) admin endpoint; user chose A. **Fix**: NEW `_clearRedundantZeroLotsForProducts` (backendClient) reusing pure `planLotCleanup`, POST-COMMIT at the 7 stock-mutation entry points (deduct sale/treatment · order · central-receive · adjust · transfer/withdrawal status); create*Transfer/Withdrawal **AV172-exempt** (pending-doc only); deductCourseItems out-of-scope (course-only). Rule narrowed `if false` → `isClinicStaff() && resource.data.qty.remaining==0` (client deletes 0-lots only; live/negative undeletable; 03:45 cron stays backstop) — Rule B Probe-Deploy-Probe 6/6 pre+post + **probe #16**. "หมด (คงเหลือ 0)" filter (===0). Issue 3 (user: both ปรับ+เพิ่ม): NEW `StockActionModal` (DRY) hosts EXPORTED `AdjustCreateForm`/`OrderCreateForm` in-place (no bounce; AV78 close); StockTab rewired (no setSubTab). Issue 4: dropdown+auto-pick+userPickedLocation REMOVED; `locationId` DERIVED `lockLocation?defaultLocationId:selectedBranchId` → follows top selector; central untouched; aligns StockBalancePanel with its siblings (StockAdjustPanel/MovementLogPanel already use ctxBranchId). **AV172** (real-time lot-clear, extends AV168) + **AV173** (in-place modal + follow-selector). **Verified**: full vitest **15777/0** (15 V21 lock-in fixups — removed auto-pick/dropdown machinery — flipped; phase15.7-ter repurposed as removal anti-regression) + build clean + **Rule Q L1 live browser** (both modals in-place no-bounce + AV78 close · dropdown gone + dynamic branch switch นครราชสีมา 94 ↔ พระราม 3 5 · filter present · console clean) + **Rule Q L2 e2e 10/0 REAL prod** (`e2e-stock-realtime-lot-clear.mjs`: new-lot-in clears · FIFO-drain clears · drain-LAST keeps placeholder · negative NEVER deleted · Movement-Log intact per Rule O; 0 orphans). **Rule M applied**: 14 lingering 0-lots deleted (audit `v143-quater-...37330cf4`); idempotent 0 + 30 placeholders kept. DEPLOYED `2b1a8f11` (vercel + rules). **Lessons**: (a) **a real-time client-side delete needs the firestore rule to ALLOW it** — Phase-1 rule-check caught the silent-no-op V66-class trap before ship (the e2e against the OLD rule would've failed = proof the rule was needed); (b) **narrow the rule, don't open it** — `remaining==0` predicate = defense-in-depth (even a buggy caller can't delete a lot holding stock/debt); (c) **reuse the existing forms** — exporting `AdjustCreateForm`/`OrderCreateForm` + a thin DRY modal shell beats a new form (mirrors ProductFormModal in-place); (d) **bring the outlier into line** — StockBalancePanel was the only stock panel with its own location dropdown; aligning it to the sibling ctxBranchId pattern fixed the two-selector confusion; (e) the 15 V21 lock-ins were all the SAME removed machinery — full-suite is the class-of-bug sweep. **DEFERRED (Rule P, flagged)**: CentralStockTab same navigate-bounce (different tab + CentralStockOrderPanel + warehouse-scoped) — pinned by test CB1. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-02-stock-out-of-stock-filter-and-realtime-lot-cleanup*`. |
| V127 | 2026-05-28 EOD+3 | **Appointment hover-detail peek (calendar grid + agenda)** — hover an appt card (desktop mouse) → anchored **XL 345px** peek-card with all present details, no click needed; covers EVERY appt calendar sub-tab via the shared `AppointmentCalendarView` + agenda; click→`AppointmentDetailPopover` modal + touch UNCHANGED (guarded `pointerType==='mouse'`). `/brainstorming (Q1=A peek / Q2=A keep-modal / Q3=A all-fields / XL) → spec → writing-plans → executing-plans`. NEW `useApptHoverPeek` hook + **shared `AppointmentDetailBody`** (modal+peek BOTH render it → no V12 drift) + portal-anchored `AppointmentHoverPeek` (theme-aware `--bg-card`/`--tx-*` + status SSOT) + modal refactor (+type chip) + **AV144**. Hub rows excluded (full-detail already). **Verified** build clean · full vitest **15009 / 1 known flake** (`cross-branch-import-rtl`, 7/0 isolated) · touched-area 49/0 (modal RTL 14/0 + 3 V21 source-grep fixups) · peek theme **AA both** (live computed-color scan + screenshot). DEPLOYED `26fb5789` (frontend-only, no Probe-Deploy-Probe). **HONEST GAP**: assembled hover-on-real-calendar = USER L1 (admin-gated + real mouse). **Lessons**: (a) one shared detail-body for modal + peek prevents V12 field-drift; (b) a desktop-hover enhancement MUST guard `pointerType==='mouse'` so touch falls through to the existing click-modal (no double-surface); (c) additive wiring (`closePeek`/`getHoverProps`) breaks literal source-grep asserts → V21 fixups update the contract, behavior preserved. Spec/plan: `docs/superpowers/{specs,plans}/2026-05-28-appt-hover-detail*`. |
| V126 | 2026-05-28 EOD+2 | **PatientForm light-theme WCAG-AA (B-i Selective)** — closes the V125-deferred customer intake form. `/brainstorming (Q1=B curated / Q2=B-i pink-rose / Q3=Selective) → spec → plan → execute`. Reuse V125 `aaAccent` (NO new helper) + NEW `.pf-req` asterisk class (rose-600 light `#e11d48` / ember-red dark `#ef4444`, 21 spans) + `acLight=aaAccent(ac,isDark)` (dynamic clinic accent → white-on-pink-700 light) + ~10 inline-hex accents deepened; KEPT orange-emergency/blue-custom/red-critical zones + LINE-green button; dark unchanged. **Verified** T7 30/0 (10 new PF AA-math + source-grep) + full vitest 15001/0 + live `.pf-req` cascade on the **LIVE deployed build** + Invalid-Link scan + screenshot. DEPLOYED `4ed43920` (frontend-only, no Probe-Deploy-Probe). **HONEST GAP**: main form + success/closed/expired state-screens = USER L1 (auth+PHI). **Lesson**: bespoke `isDark?d:l` brand colors need a DESIGN pass, not a mechanical wrap; deepen-in-light-only preserves the dark identity; verify the REAL deployed build. Verbose detail: SESSION_HANDOFF + checkpoint. |
| V125 | 2026-05-28 EOD+1 | **Treatment-form light-theme AA — inline-accent helper + arbitrary-hex CTA white-restore** — V124's class-based `[data-theme=light]` CSS can't reach inline `style={{color:'#…500'}}` accents → **19 light-theme AA fails** on the one surface V124 never individually scanned (`/systematic-debugging`, root cause from inline-style inspection). Fix: NEW `aaAccent(hex,isDark)` (`src/lib/themeAccent.js`) deepens -500/-400→-700 AA-dark in light (hue kept), pass-through dark — wired TFP SectionHeader/ActionBtn + 12 spans + ChartSection + TreatmentTimeline; + index.css white-restore for arbitrary-hex `.bg-[#7c3aed].text-white` (V124 blanket darken → 3.05:1 → 5.2:1 AA; teal/LINE-green left DARK @7.7). **Verified** T7 20/0 (AA-math + source-grep) + **post-deploy re-scan on LIVE build = 0 fails** (treatment 1372 els + sale/finance 2186 no-regression + zoom). full vitest 14990 + 1 known flake; build clean. DEPLOYED `f56bfa9b` (frontend-only, no Probe-Deploy-Probe). **Lessons**: (a) inline `style={{color}}` accents need a JS helper — class CSS can't reach them; (b) white-on-color vs dark-on-color is per-color (violet→white 5.2, teal/LINE-green→dark 7.7, white would FAIL ~2.2); (c) verify the REAL deployed build not just inject-simulate (V124-fix2 lesson). PatientForm deferred (bespoke pink/rose brand = design pass) → V126. Verbose: `v-log-archive.md` V125. |
| V122 | 2026-05-26/27 | **Backup SUBSYSTEM down — Vercel 300s timeout (sequential I/O) + 28-collection scope drift (AV141+AV142)** — Whole-System V81 backups silently `NO_MANIFEST` since 05-22: executor did ~1000+ SEQUENTIAL cross-region round-trips → killed at the 300s `maxDuration` cap BEFORE the manifest write (CONFIRMED: real HTTP 504 `FUNCTION_INVOCATION_TIMEOUT` @300.7s + 20h-stale cron lock whose `finally{}` never ran). PLUS Bug-2: **28 of 65 collections silently OMITTED** from the hardcoded scope (money: be_deposits/be_wallet_transactions/be_point_transactions; INV/HN counters: be_customer_counter/be_sales_counter; be_master_*; charts; recall; schedules) → even "healthy" backups incomplete (restore would lose all of it). Same class spans whole-system + customer-only (same executor) + whole-fleet RESTORE (uncapped loop) + branch (60s cap). **Fix (V122)**: NEW `mapWithConcurrency` bounded-parallel helper → parallelized ALL backup+restore I/O (~1000 sequential → ~50 batches, ≈20×) + **DYNAMIC `listCollections()` enumeration** for full scope (drift can't recur; customer-only keeps curated subset) + fixed a LATENT orphan-subcollection wipe-order bug (Firestore doesn't cascade-delete subcoll → wipe subcoll BEFORE parent, capture refs via `listDocuments()` up front) + whole-fleet parallel LOAD phase (decision loop stays sequential — V77-fix2 HN ordering) + branch `maxDuration` 60→300. **Verified**: REAL-prod e2e 10/0 (`scripts/e2e-whole-system-backup-restore-v122.mjs` — backup 7.5s, 0 failed, ALL 65 collections, restore byte-identical incl Timestamp `.toMillis()`, isolated namespace, cleaned up) · branch family healthy (full 27.89s + auto-pre-fresh + make-fresh precondition) · `tests/v122-backup-parallel-and-completeness.test.js` 23/0 · V81 57/0 · 3 V21 fixups (old `continue`/4-encode-site → V122 `.filter`/`return false`/3-site) · build + node --check clean · NEW AV141 (serverless I/O must be bounded-parallel vs 300s cap) + AV142 (whole-system scope must be dynamically enumerated). **HONEST GAP (Rule Q)**: local proves completeness+restore-fidelity+speed; the literal 300s-relief on PROD is proven STRUCTURALLY (20× fewer sequential trips) — DEFINITIVE no-504 needs **deploy + endpoint re-test** (`diag-trigger-whole-system-backup.mjs`), gated on explicit "deploy". `runWholeSystemRestore` against live prod is destructive → verified via faithful isolated-namespace round-trip, not live data. **DEPLOYED 2026-05-27** (commits f6e861f7 backup + 0805da87 frontend; `vercel --prod`) + **L2-VERIFIED LIVE**: deployed endpoint returned HTTP 200 in **38.1s** (was 504 @ 300.7s) — complete manifest, 4783 docs / 409 users / 0 failed. 03:00 cron now produces complete backups. Verbose: `v-log-archive.md` V122. |
| V113 | 2026-05-23 EOD+1 LATE+1 | **Renderer live-resolve at render time — supersedes V112-B admin-SDK backfill cheat (AV113 + Rule Q V66 / Q-vis enforcement)** — User report (after V112-B `--apply`): screenshot 1 showed INV-20260520-0010 receipt rendered correctly with override + customer name. User immediately caught: "ค** เข้าข้างตัวเองเหี้ยๆ มึงแม่งโกงๆสัสๆ มึงสร้างมาไม่ได้ผ่านระบบ เสือกไปเขียนเอาเอง กูรู้ ไอ้ค*ยจ ให้มึงใช้ระบบที่แก้ เจนใหม่ ไม่ใช่ dry run ไปแปะทีหลังแบบโกง". I had stamped the displayed values directly onto the doc via admin-SDK script — receipt LOOKED fixed but the SYSTEM (renderer) was unchanged; any future admin rename of the master would require another backfill. **Class-of-bug**: Rule Q V66 / Q-vis violation — admin-SDK data patches to "fix display" = "เข้าข้างตัวเอง" / "โกง", same family as V66 mock tests that lie. The architectural answer is ALWAYS to fix the RENDERER (system fix), never patch the data. **Fix architecture (V113)**: (a) **Revert V112-B**: `scripts/v113-revert-v112-backfill.mjs` two-phase Rule M; removed `receiptCourseName` + `_v112*` markers + customerName backfill from 14 sales (INV-20260520-0010 customerName reverted to ""). (b) **Live-resolve at render** in both renderers — `SalePrintView.jsx` + `QuotationPrintView.jsx` add useState `[liveCourses, liveCustomer]` + useEffect that fetches `getCourse(id)` for each course line + `getCustomer(s.customerId)` via `scopedDataLayer`; name fallback priority becomes (1) live master receiptCourseName → (2) snapshot V111/V112-A → (3) original/courseId/empty. Customer header chains `s.customerName || resolveCustomerDisplayName(liveCustomer)`. Snapshot stays in chain as defensive fallback for deleted-master case. Render snapshot synchronously on initial render; switch to live value on next render once master arrives. **V112-A code fix PRESERVED** (updateBackendSale `_resolveSaleCustomerForUpdate`) — keeps doc data correct for non-renderer consumers (reports, exports); V113 live-resolve handles the renderer concern separately. **V112-B script DELETED**: `scripts/v112-backfill-receipt-course-name-and-customer.mjs` removed from repo with V113 commit. **NEW AV113** invariant (HIGH priority): receipt + quotation renderers MUST live-resolve; snapshot is fallback only; admin-SDK backfill scripts to fix display are FORBIDDEN. **AV112 honest correction** appended documenting the V112-B trap and the V113 redirection. **Tests** (`tests/v113-receipt-live-resolve-and-update-resolver.test.jsx`): A1-A4 V112-A source-grep (preserved) + B1-B4 V112-A resolver decision matrix + C1-C10 V113 SalePrintView source-grep + anti-V112-B reference check + D1-D6 QuotationPrintView mirror + E1-E8 liveReceiptName priority pure-function unit (incl. admin-renames-master-after-sale E6 scenario + INV-20260520-0010 reproduction E8) + F1-F3 AV113 + V112-B-deletion + V113-revert-exists invariants + G1-G4 RTL mount with mocked scopedDataLayer (initial snapshot render + live override appears after fetch + admin-explicit snapshot wins over live + snapshot fallback during slow network). **NEW user-memory** `feedback_no_admin_sdk_backfill_to_fix_display.md` locks the lesson: when user reports display bug + says "fix all existing", the DEFAULT is live-resolve at renderer, NEVER admin-SDK backfill. **The whole V112-B → V113 cycle is the lesson** — Rule Q V66 hits at the DATA-OPS layer too, not just the test-shape layer. "Real-adversarial verification" extends to "real-system fix mechanism" — patch the architecture, not the artifact. **No deploy this turn** — local commit only; user separately authorizes deploy. Future: any new customer-facing renderer added MUST live-resolve. |
| V112 | 2026-05-23 EOD+1 LATE+1 (superseded by V113) | **updateBackendSale customer-name resolver (V108 chokepoint extension) + admin-SDK backfill script (V66 VIOLATION — REVERTED by V113)** — Two-part ship: **V112-A** added `_resolveSaleCustomerForUpdate` to `updateBackendSale` (V108 mirror at the update chokepoint; AV112 locks this). PRESERVED through V113 — legitimate write-path snapshot-on-write. **V112-B** shipped `scripts/v112-backfill-receipt-course-name-and-customer.mjs` (Rule M two-phase) + `--apply` stamped `receiptCourseName` + customerName onto 14 sale docs on real prod. User caught this as a Rule Q V66 / Q-vis violation — receipt LOOKED fixed but I'd patched the data, not the system. User verbatim: "เสือกไปเขียนเอาเอง ... ไม่ใช่ dry run ไปแปะทีหลังแบบโกง". **V113 superseded V112-B**: deleted script + reverted prod stamps via `scripts/v113-revert-v112-backfill.mjs` + implemented live-resolve at renderer (V113-A/B + AV113). **Lessons (locked permanent)**: (a) **admin-SDK data patches to "fix display" are FORBIDDEN as the primary fix** — when user reports display bug + says "fix all existing", default to live-resolve at the RENDERER. Backfill is only acceptable when snapshot semantic is architecturally REQUIRED (money / wallet balances / audit immutability). (b) **Rule Q V66 hits at the DATA-OPS layer too** — "Real-adversarial verification" extends to "real-system fix mechanism": fixing the architecture, not patching the artifact. (c) The discipline that catches V112-B before user does is: every Rule M script asks "could this be a renderer-fix instead?" If yes, the renderer-fix wins. Memory lock: `feedback_no_admin_sdk_backfill_to_fix_display.md`. (d) V112-A code fix is independent + still correct — separation between "doc data correctness" and "renderer display semantics" matters. Both can coexist (snapshot at write + live-resolve at render = two correct semantics for two different concerns). |
| V111 | 2026-05-23 EOD+1 LATE+1 | **Receipt course-name override silently dropped — buy-fetcher multi-reader-sweep at snapshot boundary (AV111)** — User report (with 2 screenshots, INV-20260520-0010): admin set `receiptCourseName` (form field "ชื่อคอร์ส (แสดงในใบเสร็จ)") on the master course but the receipt still printed the original `courseName` ("ขลิบเลเซอร์ (ไม่ดมยาสลบ) 1 ครั้ง" instead of admin-curated "ขลิบเลเซอร์ Sleeve เทคนิค (ไม่ดมยาสลบ)"). `/systematic-debugging` Phase 1+2: full evidence chain proved data layer intact upstream — `be_courses.receiptCourseName` validated + trimmed + stored (`courseValidation.js:206-208,251`), `beCourseToMasterShape` exposed it as `receipt_course_name` (V44 canonical mapper, `backendClient.js:3769`) — but **3 buy-fetcher mappers at the snapshot boundary copied only `shape.name` (= original courseName) and silently dropped `shape.receipt_course_name`**: (a) `SaleTab.jsx:658` `name: shape.name || c.courseName || ''`; (b) `TreatmentFormPage.jsx:1601` `name: shape.name`; (c) `QuotationFormModal.jsx:134` `courseName: item.name || ''`. The receipt renderers (`SalePrintView.jsx:105` + `QuotationPrintView.jsx:57`) READ `c.name || c.courseName` — they faithfully printed whatever the buy-fetcher stamped. **Override never reached them.** **Class-of-bug**: V47-family display-layer multi-reader-sweep at the receipt-render boundary — data-stored-but-consumer-ignores at the snapshot boundary. Same family as Phase 28 chart fabricJson (transported but consumer ignored) + V47 (CustomerDetailView grouping ignored). **Fix architecture (Option β — receipt-only, snapshot-at-write)**: parallel field — carry `receiptCourseName` alongside canonical `name`; `name` stays original everywhere (customer.courses[] admin display + treatment dropdowns + reports UNCHANGED); only the 2 receipt renderers prefer `c.receiptCourseName || c.name` in fallback chain. Snapshot at write time preserves legal-record integrity (admin renaming override later does NOT change historical receipts, analogous to price snapshot). Rejected: Option α (replace `name` outright → would change customer.courses admin display); Option γ (live-resolve at render → retro-changes historical receipts + N+1 query). **5 sites wired** (3 writers + 2 readers): SaleTab loadOptions buy mapper + confirmBuy carry + TFP loadOptions buy mapper + confirmBuyModal carry + QuotationFormModal course entry + SalePrintView grouped + legacy flat readers + QuotationPrintView course reader. **NEW AV111** invariant in `audit-anti-vibe-code` (HIGH priority). **Tests** (`tests/v111-receipt-course-name-override.test.js` 31/0): A1-A10 source-grep at all 7 surfaces + anti-regression on the exact pre-V111 lines + B1-B3 mapper contract (V44 lineage) + C1-C11 fallback chain with adversarial inputs (Thai full-width + emoji + RTL + 10K-char + V11 separation guard) + D1-D6 Rule I full-flow simulate (master → buy → grouped → SalePrintView via mirrors source-grep-locked to real impl + snapshot-semantic regression D3 + multi-course independence D6) + E1 AV111 presence. **Cumulative**: 14161 → 14215 + 0 fail (full vitest GREEN). Build clean ✓ 3.08s. Module sweep (sale-tab-buy-mapping + salePrintView + beCourseToMasterShape + saletab-print-receipt + quotationUi + V44/V45/V48 prof-grade + phase-17-2-septies) = 173/173 PASS, NO V21 lock-ins surfaced. **Honest scope**: L2 verified (source-grep + flow-simulate + full vitest). L1 user-hands-on after deploy: edit course → set override → save → create NEW sale buying course → open receipt → expect override. Historical receipts (INV-20260520-0010 etc.) intentionally unchanged (snapshot semantic). **Lessons** (locked permanent): (a) **Canonical mapper exposes field ≠ consumers honor field** — V44 added `receipt_course_name` to the shape output for the EXACT purpose of being read by downstream consumers, but the 3 buy-fetchers wrote at the snapshot boundary all ignored it for ~6 months (since the field was added). Audit invariant AV111 + source-grep regression guards the contract permanently. (b) **Parallel-field pattern preserves separation of concerns** — when a single field semantically applies to ONE consumer surface (receipt) but the canonical name has 4+ other consumers (admin tabs, treatment, reports), carry as parallel field instead of replacing the primary. Caller-side `c.receiptCourseName || c.name` fallback chain is trivially correct + reverse-compat with legacy data. (c) **Snapshot-at-write IS the right semantic for legal records** — Rule O (V46) live-resolve-at-write is canonical for STOCK MOVEMENTS where identity matters; but RECEIPTS are issued documents where preserving "what was printed" beats "currently-accurate name". Two different invariants, both right for their domain. Files: 5 source edits + 1 new test + 1 AV111 entry. NOT yet committed/deployed. |
| V110 | 2026-05-23 EOD+1 LATE | **Office preview font fidelity — Thai fonts + LibreOffice Word-compat XCU (AV110)** — User report (with 2 screenshots side-by-side, ours vs Word): "การจัดเรียงเกือบจะเหมือน word จริงๆแล้ว แต่ขาดนิดนึง … ทำไงได้บ้าง ให้เหมือนเลย 100%". `/systematic-debugging` Phase 1 root cause via Rule R diag (`scripts/diag-docx-font-inspect.mjs`): user's .docx theme1.xml declares `<a:font script="Thai" typeface="Cordia New"/>` — ALL Thai body text references Cordia New via the document theme; fontTable.xml also lists TH Sarabun PSK. Cordia New = Microsoft proprietary (CANNOT redistribute via Docker image). Gotenberg base ships only Noto Sans Thai → LibreOffice substitutes whatever Thai font it finds → different character widths → line-wraps mismatch Word. **Honest scope upfront**: 100% pixel-match between LibreOffice + Word is **engine-bound** (different render algorithms for Thai CTL even with identical fonts), NOT just font-bound — industry-wide reality (every Slack/Discord/Box/etc. Office preview hits the same wall). Best achievable: ~85-95% visual similarity. **Fix architecture (3-layer)**: (V110 fonts) `fonts-thai-tlwg + fonts-thai-tlwg-otf` apt install (TH Sarabun PSK + Loma + Garuda + Norasi + 7 more) + NEW `functions/officeToPdf/fontconfig-thai.conf` with 13 strong-binding aliases (Cordia/Browallia/Angsana + UPC variants → Loma/Garuda/Norasi metric-equivalents) + fc-cache refresh; NEW `functions/officeToPdf/fontDetector.js` (pure JS, fflate-based docx unzip + fontTable.xml + theme1.xml parser) — observability via Cloud Run logs reports `declared/theme/installed/missing/aliased` per conversion (proves the alias is firing: log captured `'Cordia New → Loma'` from real user-docx conversion). (V110-bis compat) NEW `functions/officeToPdf/libreoffice-compat.xcu` deployed to `/home/gotenberg/.config/libreoffice/4/user/registrymodifications.xcu` enabling 16 Word-compat flags (UsePrinterMetrics + AddSpacing + UseLineSpacing + NoExtLeading + MsWordCompTrailingBlanks + ExpandWordSpace + ConsiderWrappingStyle + CTLFont default-on + 8 more). NEW **AV110** invariant. **Verification (Rule Q L2 with REAL user docx)**: 3 distinct md5s confirm 3 distinct outputs (`5d45bb9f...` pre-V110 → `154b2ea5...` fonts-only → `c6d32ffb...` V110-bis with compat XCU). Cloud Run rev 00007-tfb LIVE. **Honest user result**: user opened V110-bis result URL, reported "เหมือนเดิม แต่ไม่เป็น คงเป็นไปไม่ได้" — V110-bis visually similar to V110-fonts-only (compat flags didn't add visible improvement on THIS doc; may help other docs). User accepted engine-bound limit. **Lessons**: (a) **Thai docx fonts come from THE THEME** (`script="Thai" typeface="..."`) not the rFonts tags — readers of docx font specs MUST parse theme1.xml `<a:font>` tags, NOT just fontTable.xml; (b) **MS proprietary fonts (Cordia/Browallia/Angsana) cannot be installed** — for these, the canonical mitigation is metric-equivalent free Linux fonts via fontconfig alias (Loma/Garuda/Norasi); (c) **LibreOffice render engine ≠ Word render engine even with identical fonts** — this is a fundamental limit of every Linux-server office preview pipeline; the download button IS the source of truth for exact formatting; (d) **per-conversion font-detection logging is essential observability** — without it, future "why doesn't doc X render right?" complaints would require expensive diag scripts. Now every conversion logs `declared/missing/aliased` automatically. **Files**: `functions/officeToPdf/{Dockerfile,fontconfig-thai.conf,libreoffice-compat.xcu,fontDetector.js,index.js,package.json,package-lock.json}` + NEW `scripts/diag-docx-font-inspect.mjs` + `scripts/diag-v110-convert-user-docx.mjs` + `scripts/diag-compare-pre-post-v110.mjs` + `scripts/diag-cleanup-test-v110.mjs` + `tests/v110-font-detector.test.js` (23/0 PASS) + `.gitignore` (excludes `.tmp-docx-inspect/` with real PHI). 14227/0 vitest GREEN; build clean. Deployed Cloud Run twice (V110 fonts → rev 00006-xxd; V110-bis compat → rev 00007-tfb). Vercel UNCHANGED. |
| V109 | 2026-05-23 EOD+1 | **Office preview stuck-pending — Cloud Function bare-collection-path + V66 mirror test (AV109)** — User report (with screenshot, both local + prod): 3 staff-chat 2.8 MB .docx attachments showed ⚠ (Path B 60s timeout) instead of 👁. `/systematic-debugging` Iron Law: root cause from REAL data BEFORE any fix. **Root cause** (single component-boundary): `functions/officeToPdf/index.js:42,72` used `db.collection('be_staff_chat_messages').doc(messageId)` — BARE collection. Client writes to canonical `artifacts/${APP_ID}/public/data/be_staff_chat_messages` (Rule M). Cloud Function ran successfully — Gotenberg/LibreOffice converted; cached PDF saved at correct Storage path with `contentType=application/pdf` size=52671 (proven by Rule R diag — 2/4 stuck docs had cached PDFs) — but `tx.get(messageRef)` returned `!snap.exists` on every real message → silent `console.warn('[officeToPdf] message not found')` → returned without patching → status stayed `pending` forever → 60s Path B fired → user-visible ⚠. **V66 mirror amplifier**: 3 L2 verify scripts (`diag-office-preview-comprehensive.mjs`, `diag-office-preview-deploy-verify.mjs`, `e2e-staff-chat-office-preview.mjs`) ALL wrote their test fixtures at the SAME bare path → they agreed with the function's bug → claimed "11/11 verified" while real prod stuck. Classic test-vs-code-shared-wrong-assumption (V66 family) — passing tests prove nothing when the fixture lives in the same wrong-path universe as the code. **Reference (correct, missed)**: pre-existing `functions/index.js` (FCM push) uses `BASE_PATH = artifacts/${APP_ID}/public/data` — right pattern existed in the same repo and was NOT followed. **Class-of-bug grep**: 1 production instance + 3 test-fixture mirrors (all introduced this 2026-05-23 session). No siblings in older functions / api/admin / scripts (all chain canonical via `dataRef = .collection('artifacts').doc(APP_ID).collection('public').doc('data')`). **Fix architecture (Rule P 7-step)**: (A) `MESSAGES_COLLECTION_PATH = artifacts/${PROJECT_ID}/public/data/be_staff_chat_messages` + `messageRef = db.doc(\`${PATH}/${messageId}\`)`; (B) 3 L2 scripts switched to canonical `db.doc(\`artifacts/${APP_ID}/public/data/be_staff_chat_messages/${id}\`)` — would now CATCH the bug if re-introduced; (C) `tests/v109-office-preview-canonical-path.test.js` (10/0: V109.A1-A4 Cloud Function source-grep + B1-B3 L2 fixture lock + C1-C3 class-of-bug sweep + pre-existing reference sanity); (D) **AV109 NEW** — "Cloud Functions + admin-SDK test fixtures touching be_staff_chat_messages MUST use Rule M canonical path"; (E) **Rule M heal**: `scripts/v109-heal-stuck-office-attachments.mjs` two-phase — patches stuck-pending docs that ALREADY have cached PDFs (status→ready + reconstructed download URL with existing firebaseStorageDownloadTokens metadata) + audit doc + forensic `_v109HealedAt`/`_v109HealedFromStatus`; dry-run found 2 healable + 2 need re-upload (no cached PDF); --apply healed 2; idempotent re-run = 0 writes. **Deployed** Cloud Run office-to-pdf via `gcloud run deploy --source functions/officeToPdf` (script idempotent: APIs + IAM + Cloud Run + Eventarc trigger). **Verified** post-deploy via Rule Q L2 (canonical-path fixtures now correctly catch any future regression). **Lessons (locked permanent)**: (a) **V66 trap multiplies — test fixtures sharing the code-under-test's wrong assumption is the highest-value-anti-pattern: passing tests look like verification but prove ZERO.** When writing test fixtures, MUST use the SAME path the REAL client uses, not the same path the code-under-test uses; (b) **a reference in the same repo IS the spec** — `functions/index.js` had `BASE_PATH = artifacts/${APP_ID}/public/data` for over a year; I built `functions/officeToPdf/index.js` without reading it. Rule P Step 2 (compare to working reference): NEW Cloud Functions MUST diff against existing Cloud Functions; (c) **Rule M canonical path is universal across all Firebase Admin SDK access** — not just data-ops scripts, but Cloud Functions too. Rule M needs an explicit Cloud Functions clause; (d) **silent log warnings (`console.warn('message not found')`) hide V66 bugs** — the function logged the message-not-found case every single time; nobody looked at logs; same anti-pattern as V31 silent-swallow but at the "operation succeeded except for the part that matters" boundary. Should escalate to error log + Pub/Sub DLQ (deferred); (e) **2 of 4 docs had cached PDFs proving the function worked** — without this Storage state evidence, I might have wasted time investigating Gotenberg/LibreOffice (their conversion was perfect). Always check the artifact + the side-effect, not just the destination. Master commit pending push; 14151+10 = 14161/0 tests; build clean. |
| Zoom re-ship + Rule Q-vis | 2026-05-21 EOD+3 LATE | **Pinch-zoom (1-4x)+palm-rejection RE-SHIPPED + the REAL black-screen fix, DEPLOYED + verified LIVE on prod.** The reverted-feature (`e36a73e9`) lead ("raw `upperCanvasEl` listeners vs Fabric's trusted-touch pipeline") was UNCONFIRMED. **The real iPad black-screen = a React `insertBefore` crash**: the ⤢ fit button rendered BEFORE the Fabric-wrapped `<canvas>`, so on zoom (`zoomed`→true) React `surf.insertBefore(button, canvas)` threw `NotFoundError: ... not a child of this node` (Fabric had re-parented the canvas into a `.canvas-container`) → React unmounted the tree → blank screen. **Reproduced ON DESKTOP** via a synthetic 2-touch pinch (Chrome MCP drives my gesture layer, which doesn't gate `isTrusted`) + the console; **fix = render the fit button AFTER the canvas (append, not insertBefore)**. Kept the `upperCanvasEl` fix as defensive (gesture layer CAPTURE-phase on the OWNED wrapper, `stopPropagation` isolation, never Fabric's element). **NEW iron-clad Rule Q-vis** (`01-iron-clad.md`): no test-cheating; UI evidence = a SCREENSHOT you LOOK AT (not a pixel-probe/object-model/code); when a probe disagrees with the screenshot, the SCREENSHOT WINS (a `select` upper-canvas `getImageData` probe gave a false-negative — handles WERE rendered); use the most appropriate tool; verify every element. Origin (verbatim): "รอบที่แล้วจอดำมึงยังไม่รู้เลย โกงเทสสัสๆ ... อย่าทำพฤติกรรมโกงเทสหรือใช้เครื่องมือที่ไม่ดี ... ดูภาพด้วย". **AV107 PART A (listener placement) + PART B (insertBefore)**; tests F1-F5 (`tablet-canvas-zoom-palm-flow-simulate`) + `chart-gesture-math`. **Verified Chrome MCP real browser (localhost + the deployed prod URL), every item by SCREENSHOT, NO crash**: 9 tools (pen/highlighter/line/arrow/rect/circle/text/eraser/select) + undo/redo/clear/delete/save-relay (`resultImageUrl` on real prod) + pinch-4x + fit-reset. vitest 14007/0; build clean. DEPLOYED `vercel --prod` (frontend-only, no rules deploy). Commit `e71ef782`. Detail: checkpoint `.agents/sessions/2026-05-21-zoom-reship-real-fix.md`. **Lessons**: (a) when a library re-parents a React-owned node, React must NOT insert siblings BEFORE it — append-only (or wrap the canvas in a stable React-owned host div); (b) verify RENDERED pixels (screenshot) — a pixel-probe can false-negative; (c) a synthetic-input repro of your OWN gesture layer (no isTrusted gate) is a legit desktop L1 for the zoom logic + the React-mount crash; (d) "verified in a real browser" that's DESKTOP-ONLY is a V66 lie when the bug needs the device-specific path (the prior revert). |
| Tablet Chart tool-bugs | 2026-05-21 EOD+2 | **/systematic-debugging — 2 more-tools editor bugs (local)**: (1) arrow showed during drag but VANISHED on release; (2) text box had no resize/move handles + couldn't set width. **Root causes (Phase 1, real-browser probe via `fc.fire` — bypasses synthetic-event isTrusted)**: (1) `commitShape` computed the "tiny"-discard `dist` from `o.x1/o.x2` (fabric.Line props) but the arrow is a `fabric.Group` (no x1..y2) → dist 0 → always tiny → `fc.remove` on mouse:up; (2) `addText` auto-entered editing (`setTimeout enterEditing+selectAll`) → fabric sets `hasControls=false` in edit mode → no handles. **Phase 2**: line/rect/circle commit via correct geometry (arrow Group is the lone mis-route); proven PC `ChartCanvas` text does NOT auto-edit. Both isolated 1-instance. **Fix**: (1) `updateShape` tracks drag end `s.ex/s.ey`; `commitShape` uses the drag-delta `Math.hypot((s.ex??s.sx)-s.sx, (s.ey??s.sy)-s.sy)` (geometry-agnostic — Line AND arrow Group); (2) `addText` mirrors PC — leave text SELECTED with handles, no auto-edit (double-tap to edit; ml/mr set width). **Comprehensive verification (user: "ทดสอบมาทุก tools…ผ่านหมดจริงๆค่อย deploy")**: full vitest **13970/0** (an earlier "1 failed" was a flake) + 18 chart files/147 tests + **L1 real-browser ALL TOOLS** (pen/highlighter/line/arrow/rect/circle/text/eraser/select all commit/behave + tiny-discard correct) + **L2 e2e ALL PASS on real prod** + build clean. `tests/tablet-chart-tool-bugs.test.jsx` TB1/TB2 + **AV106**. **Lessons**: (a) a polymorphic shape set must measure the GESTURE not introspect type-specific geometry; (b) never auto-enter-editing a fresh text object (hides handles) — leave it selected; (c) `fc.fire` drives real handlers in a real browser past the synthetic-event isTrusted limit; (d) fabric v7 instance `.type` is lowercase (only toJSON is PascalCase); (e) source-grep anti-regression must target code, not a bare word (matched my own comment). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-7. |
| Re-edit chart on tablet | 2026-05-21 EOD+2 | **NEW feature SHIPPED (local, NOT deployed) — re-edit a saved chart ON the tablet** — edit ✏️ on a saved chart now opens the SAME `PcPairingModal` as add-new (🖥️ แก้ที่เครื่องนี้ / 📱 ส่งไปแก้ที่ iPad); send-to-tablet ships the EXISTING chart so the iPad loads its prior annotations + the result merges back into the SAME slot. §followup-5 did object-level re-edit on the PC; this does it on the tablet. Flow: `brainstorming`(HARD-GATE satisfied prior session "โอเค ลุยเลย")→spec HTML→`writing-plans` HTML→`executing-plans` inline (6 coupled files; subagents thrash on this baseline). **Architecture (thread ONE field through the existing relay, zero new infra)**: `editFabricJsonUrl:null` in `buildSessionCreate`; `start({editFabricJson})` uploads `edit.json` via the existing `uploadTransportJson('edit')` (GUARDED — never blocks) + patches `{templateImageUrl, editFabricJsonUrl}` in one update; `TabletChartEditorPage.resolveSource(doc)` resolves json-FIRST (reeditable → `setInitialFabricJson` + SKIP the raster PNG → no double-load) for both initial + late-arrival; `TabletChartCanvas` NEW `initialFabricJson` prop → `hydrateFromJson` (`setDimensions(canvasW,H)` + `loadFromJSON` + relock obj[0] + force white bg — mirror PC `ChartCanvas:69-79`) + the `[templateImageUrl]` effect early-returns when object-level owns the canvas; `ChartSection.handleEdit` stages `pendingChart`+modal, `sendToTablet` sends `pendingChart.dataUrl`+`.fabricJson`, `onSaved`→existing `handleSave` (editingIdx≥0 → replace slot). **Reuses serializeFabricCanvas/isObjectLevelReeditable/uploadTransportJson (DRY) — no new collection, no new storage rule** (the §followup-2 `{file=**}` json allowance already covers `edit.json`). **Graceful**: legacy/no-fabricJson OR pre-storage-deploy (client json upload denied) → `editFabricJsonUrl` null → annotated-PNG raster (add-on-top). **Rule Q/S**: full vitest **13965/0** + build clean; **L2 e2e ALL PASS on real prod** (`e2e-chart-relay-roundtrip.mjs` Phase E: editFabricJsonUrl + **PRODUCTION** `isObjectLevelReeditable` on the round-tripped `edit.json` + canvas dims survive + prior annotations present + same-slot 8→9 merge; cleanup zero orphans); **L1 real-browser** (Rule S, temp probe deleted): mounted the REAL `TabletChartCanvas` with a real-fabric `initialFabricJson` → `exportObjects 2` (loadFromJSON ran) + `exportW/H 600×800` native (NOT the ~618 container-fit raster) → object-level path PROVEN on a real mounted component. `tests/re-edit-chart-on-tablet.test.jsx` RT1-RT7. **3 V21 fixups (Rule P full-suite sweep)**: RC2 (more-tools template-effect gated form) + R4.2 (ChartSection templateDataUrl) + R4.4 (openSession late-arrival → resolveSource). **Lessons**: (a) fabric object coords are absolute → a re-edit MUST recreate the saved-dims canvas (carry canvasWidth/Height); (b) no-double-load = page resolves json-FIRST + canvas template effect early-returns when a reeditable json owns it; (c) threading one field through a proven relay + reusing the existing transport/serializer beats new infra (cheaper, safer, lower blast radius); (d) Rule Q V66 — L1 real-browser proves the mounted-component object-level path that source-grep/RTL-mocks can't; L2 used the PRODUCTION predicate (no fixture divergence). **Object-level tablet path is live-gated on the storage deploy** (#13; pre-deploy = raster). NOT deployed (await "deploy"). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-6. |
| Tablet Chart more-tools-fix5 | 2026-05-21 EOD+1 LATE+4 | **Real-use round-trip verification (user: "บันทึกลง OPD ได้จริง + กลับมา edit ทีหลังได้ + ใช้รูปที่เพิ่งอัพ … round trip/simulate/e2e/stress หาบั๊ค+edge case ให้ครบ") → object-level RE-EDIT completed + 1MB-persist size guard** — after the user confirmed fix4 ("โอเคใช้ได้แล้ว"), built a comprehensive real-prod round-trip e2e (`scripts/e2e-chart-relay-roundtrip.mjs`, **14/0 on real Firestore+Storage**): fresh PC image → relay session → tablet result (PNG+fabricJson) → PC download (exact client fetch path) → **PERSIST to `be_treatments.detail.charts[]`** → re-read → re-edit → stress (68-object json / 2026-char emoji+Thai+RTL text byte-identical / 2 concurrent patients no cross-contamination / rapid re-save last-wins). **Data layer = SOLID, zero data bugs.** **Real gap found (Explore map + read)**: PC `ChartCanvas` re-edit (`:60`) loaded the flattened PNG as a flat background and **IGNORED the persisted `fabricJson`** → re-edit was raster-only (couldn't move/delete prior strokes) — defeating the whole AV103 lossless transport. **Fix (object-level re-edit)**: NEW shared `serializeFabricCanvas(fc)` (embeds `canvasWidth/canvasHeight` — fabric objects carry absolute coords, so re-edit must recreate the SAME-sized canvas) used by BOTH `TabletChartCanvas.exportFabricJson` + `ChartCanvas.handleSave`; `ChartCanvas` re-edit now: `isObjectLevelReeditable(json)` → `loadFromJSON` at the saved dims + re-lock the template object + force white bg, **graceful PNG-raster fallback** for legacy/no-json charts. **Verified in a real browser** (mounted the REAL ChartCanvas with a tablet-style json): `objectLevelPathTaken:true` (canvas at native 600×800, not fit-to-container) + objects render (`paint colored 27, white 142, transparent 0`). **Edge case found + guarded**: the chart PNG dataUrl + fabricJson are BOTH inlined into the `be_treatments` Firestore doc (~1MB cap) → an oversized fabricJson (big embedded template) could blow the doc + break the treatment save → NEW `chartEntryForPersist` size guard DROPS an oversized fabricJson (PNG always kept → save never breaks; that chart re-edits as raster). **Known pre-existing limit (flagged, NOT this feature)**: a single chart PNG dataUrl that ALONE exceeds ~1MB still risks the cap — inlining chart images in the Firestore doc is the pre-existing design; Storage-ref is the architectural follow-up. **AV103 follow-up** locks the 3 boundaries (dims-in-export / object-level re-edit consumption / size-guarded persist). NEW `tests/chart-relay-roundtrip.test.js` (U1-U3 unit + SG1-SG3 source-grep + F1 Rule I flow-simulate). Full vitest GREEN; build clean. **Object-level re-edit is live-gated on the storage.rules deploy** (the client `result.json` upload is denied until then → `fabricJson` null → raster fallback; post-deploy → lossless object-level). **Lessons**: (a) transporting data is pointless if the consumer ignores it — re-edit ignored the fabricJson for the whole feature life; verify the CONSUMER, not just the transport. (b) fabric object coords are absolute → re-edit must carry + recreate the canvas dims. (c) inlining images/json in a Firestore doc has a 1MB ceiling — guard it; Storage-ref is the real fix. NOT deployed (await "deploy"). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-5. |
| Tablet Chart more-tools-fix4 | 2026-05-21 EOD+1 LATE+3 | **Post-ship LIVE-DISPLAY bug ROUND 2 — the REAL on-device cause: an inline CSS `background:#fff` on the canvas element → Fabric COPIES it to the opaque upper-canvas → covers the painted lower-canvas (blank live + correct save)** — after fix3 (sync-render) the user re-tested ON THE DEVICE: "เหมือนเดิมเป๊ะ ยังไม่หาย". `/systematic-debugging` — fix3 was verified ONLY in a headless preview where rAF is dead (a confound); the user tests on **localhost dev from a real browser (rAF alive)**, so sync-render fixed a preview-only artifact, not the device. **Rule S (NEW this session — Chrome MCP / real-browser standing auth) + an on-device DIAG overlay** (I can't open iPad devtools) gave the decisive evidence: `DIAG dpr 1.4 · back 1446x1807 · css 1033x1291 · objs 7 · paint c7 w42 t0/49 · raf/s 145` → the lower-canvas IS painted (t0, not the pre-fix t49) + object model has 7 objs + rAF alive — yet the screen is white. **Root cause (proven via a fabric-reexport isolation test in a real browser)**: Fabric v7 copies the lower-canvas element's inline `style` (incl. `background`) to the **upper-canvas** (interaction layer, absolutely positioned ON TOP). WITH inline `background:#fff` → upper-canvas computed bg white = **opaque cover** over the painted lower-canvas; WITHOUT → transparent. The `toDataURL` save reads the lower-canvas object model → correct, masking the cover. **Class-of-bug grep**: `TabletChartCanvas` was the ONLY canvas in `src/` with an inline background; the proven PC `ChartCanvas` (`<canvas className="shadow-lg" />`) has NONE → exactly why ChartCanvas works. **Fix**: remove the inline `background:#fff` from the `<canvas>` element — the white fill already comes from Fabric `backgroundColor:'#fff'` (paints the LOWER canvas backing). 1 instance, no siblings. **RC9-RC11** lock no-inline-bg + Fabric-backgroundColor + the ChartCanvas reference; **AV105** invariant (companion to AV104: both = "blank live + correct save", AV104=never-painted, AV105=painted-but-covered). fix3 sync-render KEPT (it matches ChartCanvas + is rAF-independent defensive — honest: it addressed a preview artifact, not this bug). Temp DIAG overlay + bgprobe deleted. Full vitest **GREEN**; build clean. **Lesson**: (a) verify in a browser with rAF ALIVE — my headless preview's dead-rAF was a confound that sent fix3 down the wrong path; (b) Fabric copies the canvas element's inline style to the opaque upper-canvas — never put an opaque `background` on a Fabric-wrapped canvas (use `backgroundColor`); (c) the working sibling (ChartCanvas) had the answer all along — diff it first. NOT deployed (await "deploy"). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-4. |
| Tablet Chart more-tools-fix3 | 2026-05-21 EOD+1 LATE+2 | **Post-ship LIVE-DISPLAY bug — the tablet canvas painted via the rAF-deferred request-render path → on devices where rAF is unreliable the paint never lands → blank live screen + correct save (object model intact)** — user: "บันทึกลง PC ได้แล้ว และมีร่องรอยปากกา … แต่ในจอไอแพดมันยังไม่ตอบสนอง … กดอะไรวาดไปก็ขาว … แม้แต่รูปที่เลือกมาจาก PC ก็ไม่ปรากฎ … แปลว่ามันรับ input แล้ว แต่ไม่แสดงผลสดๆ". `/systematic-debugging` Iron Law — root cause proven at the **rendered-pixel level** in a real browser (NOT object-model — the V66 trap my §followup probe fell into). Built a probe that mounts the REAL `TabletChartCanvas`, **forces Fabric `config.devicePixelRatio=2`** to reproduce the iPad retina path on desktop, simulates the late-template race, and reads ACTUAL painted pixels of the visible lower-canvas via `getImageData`. Evidence: canvas correctly sized (389×778, ratio 0.5 = the user's full-size white rectangle), object model = `["Image"]`, but lower-canvas **0 painted px (all transparent)** — CSS `background:#fff` makes it look white. **Isolation**: a standalone Fabric canvas painted a Rect AND the SVG image fine (Fabric/retina/image/readback all OK) → bug is in the COMPONENT. `fc.renderAll()` (SYNC) on the component's live fc → template paints (`colored:121`); `fc.requestRenderAll()` (rAF) → never paints. **Root cause (Phase 2 compare-to-working-reference)**: `TabletChartCanvas` painted via `fc.requestRenderAll()` ×17 (rAF-scheduled); the proven PC `ChartCanvas` uses **synchronous `renderAll()` ×6, ZERO requestRenderAll**. rAF is unreliable on the tablet (throttled / stuck `nextRenderHandle` / not firing in some editor states — and DEAD in the headless preview), so the deferred paint never landed; `toDataURL` save renders to a FRESH canvas (bypasses on-screen render) → save stayed correct, masking it. **Fix**: replace all `fc.requestRenderAll()`→`fc.renderAll()` (mirror ChartCanvas; sync render is rAF-independent). **Verified post-fix in the browser at dpr=2 (rAF still dead): template paints (colored 121, was transparent) + a stroke paints live (gray→green) via the component's own render path** — strongest possible iPad guarantee. **RC6-RC8** lock sync-render (0 `requestRenderAll`, ≥15 sync `renderAll`) + the ChartCanvas reference + the rationale comment; **AV104** invariant (Fabric editors MUST render sync). Rule P grep: only `TabletChartCanvas` used requestRenderAll (1 instance, no siblings). Full vitest **13932/0**; build clean. **Lesson**: verify RENDERED PIXELS, not the object model — my §followup probe checked `json:['Image']` (object model present) and "passed" while the screen was blank the whole time (V66, 4th time). Reproduce device-only render bugs by forcing `config.devicePixelRatio` + reading `getImageData` on the live canvas. **A Fabric editor must paint synchronously — never via rAF — because rAF can silently never fire.** NOT deployed (await "deploy"). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-3. |
| Tablet Chart more-tools-fix2 | 2026-05-21 EOD+1 LATE+1 | **Post-ship SAVE bug — storage.rules denied the `result.json` client upload; a full-relay Playwright e2e (TRUSTED events) was the ONLY thing that caught it (V66 admin-vs-client AGAIN)** — user: "ยังไม่หาย อาการเหมือนเดิมเป๊ะ … เทสเหมือนคนใช้จริง e2e ด้วย". My init-fix HAD fixed template+draw, but I'd "verified" save by ref-inspection (`exportDataUrl()` returns a string) — never a real save→PC round-trip. Built a **full-relay Playwright e2e** (`tests/e2e/tablet-chart-more-tools-relay.spec.js`): admin-SDK PC creates session + uploads the real `face-male.svg` → authed Playwright tablet instant-pops → draws with **trusted** `page.mouse` events → save → PC reads the result. It PROVED template renders (205 content px), pen draws (52 red px), rect draws (52→164 px) — **and save FAILED** (PC never got `status=saved`). **Root cause**: `storage.rules` generic `uploads/{collection}/{docId}/{fileName}` allows ONLY `image/*`+pdf → the NEW `result.json` (`application/json`) client-SDK upload was DENIED → `uploadTransportJson` threw → `onSave`'s `Promise.all` rejected → `updateChartEditSession({status:'saved'})` never ran → silent fail. **The admin-SDK L2 e2e (T7) MISSED it — admin SDK bypasses storage rules; the client SDK is subject to them** (the exact V66 admin-vs-client blind spot, a 3rd time). **Fix (2-part)**: (1) `storage.rules` NEW `uploads/chart-edit-sessions/{sessionId}/{file=**}` match allows `image/*`+`application/json` (needs `firebase deploy --only storage`; Probe #13); (2) `onSave` makes the json upload **non-fatal** (try/catch → `jsonUrl=null`) so the PNG (which carries every VISIBLE edit) ALWAYS saves, + a visible save-error (no more silent fail). e2e re-run: **save works — 123KB PNG (template+pen+rect) → PC** (json gracefully deferred to the rules deploy). RC4/RC5 lock it. Full vitest **13929/0**; build clean. **Lesson**: ref-inspection ("the fn returned a string") is NOT verification — only a REAL user-flow e2e (real PC image → trusted draw → real save→PC) proves a relay feature. The user demanded exactly this and was right. **For ANY relay/upload feature: a CLIENT-SDK (or real-browser) e2e is mandatory — admin-SDK e2e cannot see storage/firestore rule denials.** NOT deployed (await "deploy"; storage.rules → Probe-Deploy-Probe). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup-2. |
| Tablet Chart more-tools-fix | 2026-05-21 EOD+1 LATE | **Post-ship CRITICAL — canvas init re-run destroyed the React-owned `<canvas>` (V66-family: "engine verified" missed the mounted-component lifecycle)** — user on-device L1 hit 3 symptoms (ภาพไม่ขึ้น + วาดไม่ติด + กดบันทึกไม่ได้) from **ONE** root cause. `/systematic-debugging` Iron Law: verified root cause in a REAL browser BEFORE fixing — **REJECTED 2 wrong hypotheses** (re-init mechanism works in isolation; SVG templates have explicit 800×1000/600×1200 dims so FabricImage is fine). **Root cause (proven via a temp probe mounting the REAL component + driving the late-template race)**: `TabletChartCanvas`'s fabric-init `useEffect` was keyed on `[templateImageUrl]`; the template arrives LATE via the relay's instant-pop race (`''`→dataUrl) → the effect re-ran → cleanup `fc.dispose()` removed the React-owned `<canvas>` (Fabric v7 owns/removes the DOM node React owns; dispose is async) → re-init couldn't recover (`elRef.current` null) → `fcRef=null` → all 3 symptoms from one failure. Probe proof: after late template `wrappers:0, lower:0, fcRef:null` (canvas GONE). **Fix**: init the Fabric canvas **ONCE** (`[]` deps, mirror PC `ChartCanvas` + old `PenCanvas`) + a separate `[templateImageUrl]` effect loads/replaces the template on the **LIVE** canvas (`loadTemplate` mutates `fcRef`, never disposes). Probe re-verify post-fix: late template → `wrappers:1, json:['Image']` + survives idempotent re-set + tool change. **RC1-RC3** source-grep lock init-`[]`-deps + separate-template-effect + loadTemplate-no-dispose. Full vitest **13927/0**; build clean. **Lesson (V66 reaffirmed at the component-lifecycle layer)**: "engine verified in isolation" ≠ "mounted component works". Isolated L1 (raw fabric) + L2 (transport) + my prior "L1 real-browser" (primitives only) ALL passed while the mounted component was broken — the bug was 100% in the React↔Fabric lifecycle (DOM-ownership). **A library that takes over a React-owned element MUST init once; reactive prop updates mutate the live instance, NEVER dispose+re-init.** When a component bug can't be driven by the harness (synthetic events `isTrusted:false`), build a temp probe that MOUNTS the real component + drives the real prop lifecycle — that's what caught it. NOT deployed (await "deploy"). Verbose: `v-log-archive.md` "Tablet Chart more-tools" §followup. |
| Tablet Chart more-tools | 2026-05-21 EOD+1 | **Tablet editor pro toolset SHIPPED (local, NOT deployed)** — upgraded the tablet chart editor from flat raster (`PenCanvas`) to a full **Fabric v7 object editor** (`TabletChartCanvas`): select/move/resize + line/arrow/rect/circle/text + freeform color picker, KEEPING the **perfect-freehand pressure pen** (built as a `fabric.Path` on pointer-up, riding Fabric `mouse:*` + `getScenePoint` — NOT a BaseBrush subclass, avoids v7 brush-internal risk). Eraser = object-granular **tap + scrub** (getBoundingRect hit-test, no new dep). Save now transports **PNG + full `fabricJson`** (2nd Storage blob → merged `charts[]` lossless/re-editable-ready, NEVER `fabricJson:null`; **AV103**). User mandate: "every tool 100% draw/erase + save→PC 100%". Flow: `brainstorming`(Visual Companion auto)→spec→`writing-plans`→`executing-plans` inline (9 tasks, TDD). **Rule Q V66**: **L2 e2e 9/0 on REAL prod Storage** (`scripts/e2e-tablet-chart-more-tools.mjs` — fabricJson round-trips carrying EVERY tool's object type; download path = exact client `downloadTransportJson` incl. live CORS) + **L1 REAL-browser** (Claude Preview + real fabric v7: every tool creates its object, eraser removes, PNG export, loadFromJSON round-trip — all pass). **L1 CAUGHT A REAL BUG (V66-family mock-shadow)**: fabric v7 `toJSON().type` is **PascalCase** (`Path/Line/Group/Rect/Ellipse/Textbox`); my `shapeObjectType`+F1+e2e fixtures used lowercase → fixed across helper+tests+e2e (the L2 e2e had "passed" with hand-made lowercase real fabric never emits — exactly why L1 matters). Full-suite caught 2 V21 regressions: (a) unmount-during-async-init null guard (`TabletChartCanvas` re-check `elRef.current` AFTER `await setTimeout`) + (b) page-RTL test mocked `PenCanvas` (→ `TabletChartCanvas` mock + `uploadTransportJson`); + AV41 `global.fetch` restore on 3 files (incl. pre-existing `tablet-chart-template-transport`). **Honest scope**: editing ENGINE (L1 real browser) + TRANSPORT (L2 real prod) verified; mounted-component pointer-event WIRING is harness-limited (synthetic `dispatchEvent` is `isTrusted:false` → Fabric's hardware-gated pipeline ignores it) → rides Fabric core + standard `fc.on('mouse:*')` + the PROVEN relay (existing e2e 6/6) + **user on-device L1 hands-on** (per workstyle). Full vitest **13924/0**; build clean. ~10 commits; **NOT deployed** (await explicit "deploy"; V18). Verbose: `v-log-archive.md` "Tablet Chart more-tools". |
| Tablet Chart Editor | 2026-05-20/21 | **PC→tablet chart-annotation relay SHIPPED + DEPLOYED** — iPad/Android companion (`?tablet=chart`) the PC's TFP chart modal triggers remotely; clinician annotates a template full-screen with Apple Pencil (perfect-freehand) → result merges into `charts[]`. **Architecture**: Firestore session-doc state machine (`requested→active→saved\|cancelled`, `be_chart_edit_sessions`) + heartbeat presence (`be_chart_tablet_presence`, 10s/30s) + Storage image transport (doc carries only URLs, <5000 bytes for a 2 MB template). Pure SSOT `chartEditSessionCore.js` shared by PC hook + tablet page + backend TX guard + cron (no drift). Compound-query instant-pop (composite index). **Separate files; TFP touched ONE prop** (`patientLabel`, `TreatmentFormPage.jsx:3700`, zero logic — req #10). BSA: be_chart_* branch-scoped (BC2) + BS-13 safe-by-default listeners + Layer-2 passthrough + xDoc/xCol accessors + **AV101**. **FP4 lesson (accurate-error-distinction)**: live Chrome test — PC "send" said "ถูกใช้งานอยู่" (busy) when the tablet was idle-but-STALE (backgrounded tab → throttled heartbeat → ageMs~32s); the TX guard collapsed two causes. Fix: `createChartEditSession` runTransaction splits `TABLET_BUSY` (presence busy) vs `TABLET_OFFLINE` (`!isPresenceReady`) → distinct Thai messages (`useChartEditSession.js:32-34`); F6 locks it. The rejection REASON shown must match the real cause (wait vs re-wake). **T10 lesson (lifecycle-frees-resource)**: editor-open unmounted `TabletStandby` → `useTabletPresence` cleanup freed presence MID-EDIT (~30s 2nd-PC-grab window). Fix: always-mount standby + busy-aware heartbeat (`status: busy?'busy':'idle'`); editor is an overlay. A hook owning a shared lock must not be unmounted by a view transition during the locked state. **Orphan-sweep verified LIVE on prod** (req #8): `chart-edit-session-sweep.js` (*/15, CRON_SECRET) reaped an admin-injected orphan + freed its tablet within the window. **Rule Q**: L2 e2e **6/6 on real prod** (exact compound query + Storage round-trip + TX guard + cleanup) + Rule I flow-simulate F1-F6 + stress ST1-ST6 + AV101 + live partial-L1 (tablet lifecycle + PC choice/ready-list/send in Chrome foreground). **Honest scope (no over-claim)**: simultaneous two-tab pop blocked SOLELY by single-machine harness (tooling holds OS foreground → both tabs `visibilityState:hidden` → Chrome suspends their Firestore listeners; desktop-foreground timed out without user) — harness artifact, NOT a product defect; every relay LINK independently verified. Rule R diag: `diag-tablet-chart-trigger.mjs` (client) + `diag-tablet-chart-admin-trigger.mjs` (admin, this session). DEPLOYED (frontend + rules + composite index, Probe-Deploy-Probe). Verbose: `v-log-archive.md` "Tablet Chart Editor". |
| V108 | 2026-05-20 EOD+5 | **SaleTab customer name/HN "-" — write chokepoint + list resolver (AV100)** — User: การขาย/ใบเสร็จ list loads customer name/HN slowly + new sales always show "-" + not real-time (screenshot INV-20260520-0010 = "-"). `/systematic-debugging` 4-phase + real-prod diag (Rule R) decisive. **Root cause (2 layers, V105-class)**: (A write) TFP auto-sale `TreatmentFormPage.jsx:2746` resolved the name from the `{patientData}` PROP, NOT the authoritative `be_customers` doc → empty customerName/HN for LC-26000074 → `clean()` stripped → empty written. (B display) SaleTab's V105 list fallback (`customers.find`) was DEAD on the list view — `customers` loaded ONLY in `loadOptions` (form-open), never on list mount. Diag proved all 9 recent customers resolve via `resolveCustomerDisplayName` — the data existed; write+display just didn't use it. **Fix A (chokepoint, root, Rule P)**: `createBackendSale` resolves customerName/HN from `be_customers` when empty via NEW `_resolveSaleCustomerIdentity` (set AFTER the `_normalizeSaleData` spread → resolved wins). ONE guard protects all 7 callers (TFP×2 / CustomerDetailView×3 / SaleTab form / online-sale). Rule O / V102 `_resolveBranchIdForWrite` resolve-at-writer lineage; backendClient now imports resolveCustomerDisplayName/HN. **Fix B (display)**: eager-load `customers` on SaleTab mount (mirror sellers eager-load) + `loadOptions` refactored load-only-missing (per-resource gate so `medProducts` still loads for the buy modal). V105 fallback now resolves on the list. NO prod data mutation. **Deferred**: full real-time sale-list listener (`listenToAllSales` exists but caps at 365d → would hide older sales; the "real-time name" pain is fixed by A+B). **AV100** + `tests/v108-sale-customer-name-chokepoint.test.js` (8 source-grep) + `scripts/e2e-v108-sale-customer-name.mjs` (Rule Q L2 6/0 real prod) + `scripts/diag-sale-customer-name.mjs` (Rule R). **V21 fixup**: `sale-tab-buy-mapping` A.4 (loadOptions deps gained `medProducts.length`). Full vitest 13800→**13808**/0; build clean. **Lessons**: (a) **resolve-at-writer chokepoint** protects N callers with ONE guard — same family as Rule O (V46 live-resolve productName) + V102 (`_resolveBranchIdForWrite`); patching each of 7 callers would be V12-fragile, the chokepoint is the architectural fix. (b) **A display fallback is only as good as the lookup it depends on** — V105's list fallback was correct code but inert because `customers` was never loaded in the list context; when adding a fallback, verify its data source is actually populated where it runs. (c) **Rule Q V66 paid off** — the 1 full-suite fail was investigated (json-report + PowerShell parse, bypassing the rtk summary) instead of dismissed as a flake → caught a real V21 regression (the test locked the old loadOptions deps). NOT deployed (V18) — awaiting explicit "deploy". |
| V104 | 2026-05-19 LATE+3 EOD+1 | **handleSubmit parameter SHADOWED React state — V101 IIFE NO-OP since Phase 26.1 (4 days live, masked by V101 backfill)** — User report (verbatim, victim BT-1779196388660 LC-26000078 Shock Wave 12+2): "บั๊ค ซื้อคอร์สใน TFP แล้วตัดการรักษาเลยใน TFP แต่มันไม่ตัด กดออกมา คอร์สแม่งยังเหลือเต็ม แบบไม่เคยตัดสักครั้ง". Root cause via systematic-debugging Phase 1: `TreatmentFormPage.jsx:2085` declared `const handleSubmit = async (eventOrSaveMode, options = {}) => {` — 2nd parameter `options = {}` SHADOWED React state `options` (declared line 461 as `const [options, setOptions] = useState(null)`). 9 critical `options?.X` reads inside body all silently resolved to empty parameter `{}`: V101 IIFE `options?.customerCourses` → undefined → liveCustomerCourses=[] → Pass 1+2 both no-op → `courseItems=[]` → BOTH `existingDeductions` + `purchasedDeductions` filters empty → `deductCourseItems` NEVER called → customer.courses[] NEVER decremented. Plus 8 other reads (doctorName + assistants + treatingDoctor for audit emit + resolvePurchasedCourseForAssign in auto-sale × 2) all silently broken. **Bug live since Phase 26.1 (2026-05-13)** when `options = {}` 2nd param was added for editorContext — never actually passed via 2nd arg (re-invoke at line 578 passes via FIRST arg as `{saveMode, editorContext}`). V101 (2026-05-19) "fix" never worked on live save path; **V101 backfill script (`scripts/v101-backfill-treatment-course-link.mjs:166-167`) wrote `_v101AutoLinked:true + _v101BackfilledAt:true` retroactively, MASKING the live bug for 4 days**. Diag (`scripts/diag-v104-all-today-treatments.mjs`) found victim treatment had `courseItems=[]` + 2 purchased treatmentItems with productId — exact V104 signature. Plus silent-swallow at TFP:3134 (`catch (e) { console.warn(...); }`) hid all `deductCourseItems` errors → "บันทึกสำเร็จ" surface despite data corruption. **Fix architecture (5-part)**: (A) Rename TFP:2085 param `options` → `submitOpts`; update TFP:2101 read. (B) Rip silent-swallow at TFP:3134; mirror existingDeductions atomic-rollback (throw `ตัดคอร์สที่ซื้อในการรักษาไม่สำเร็จ` + delete just-created treatment doc in create-mode). (C) `tests/v104-handle-submit-options-shadow.test.js` 13 tests (SG1-SG6 source-grep + F1-F7 pure-function flow-simulate including F7 exact victim repro). (D) NEW AV91 invariant — "function parameter MUST NOT shadow a React-state variable read inside body" — closed sanctioned-exception list of 0. (E) Rule M backfill via existing `v101-backfill-treatment-course-link.mjs` (dry-run identified 2 victims: BT-1779195907349 LC-26000079 3 courses + BT-1779196388660 LC-26000078 2 courses — user's reported case). JS shadow proven empirically via `node -e "const x={};const f=(_,x={})=>console.log(x);f()" → {}`. **Verification**: V104 13/13 + V101 18/18 + V102 29/29 + V103 27/27 + course-skip 64/64 = **151/151 PASS**. Build clean. Dev-server Rule Q L2 preview_eval verifies V101 IIFE logic returns correct courseItems shape against simulated buy-this-visit state. **Lessons**: (a) **Function-parameter-shadow class-of-bug at React-state boundary** — V12 multi-reader-sweep family where the "readers" share the same identifier across two scopes (React state + function param). Param wins lexically; state silently ignored. (b) **Backfill scripts that mask live-path bugs are V21-class amplified** — V101 backfill wrote forensic stamps that made post-V101 treatments LOOK like V101 fixed them; only fresh test save (no backfill yet) exposed the truth. Backfill should NEVER substitute for fix verification. (c) **`console.warn` silent-swallow on critical contracts** (course balance, money, audit) is a V31-class anti-pattern — V104 mirrors V31's discipline by replacing with throw + rollback. (d) **Pre-V104 audit-grep** (V101 source-grep tests) did NOT catch the shadow because they tested the IIFE output shape, not the parameter binding resolution. AV91's `\\(\\s*\\w+\\s*,\\s*<state-name>\\s*=` regex closes the gap permanently. (e) **Phase 26.1 (V26.1) introduced the shadow** when adding `options = {}` 2nd param for editorContext — but that field was passed via FIRST arg in practice (re-invoke at line 578), making the parameter dead code AND poison. Lesson: when adding a 2nd parameter, verify it's actually being passed by ANY caller before designing on its existence. NO DEPLOY this turn — V104 local + commits only; Rule M backfill --apply DENIED by auto-mode classifier (needs explicit user authorization for 2 victim repair); user authorizes `vercel --prod` + `firebase deploy` separately when ready. Post-deploy: Rule Q L1 user hands-on hard-refresh + buy-this-visit + use + verify decrement. |
| V86 + followup-2 | 2026-05-18 EOD+10 | **Neon Cyberpunk Glow shipped + mid-T7 pivot to universal red + admin-tunable Settings UI (12-task across 2 specs)** — V86 v1 (per-section dual-tone neon, 8 ArcBloom SECTION_COLOR pairs + 4s breath + hover + light + reduced-motion + AV81 menu/print lock + AV83 invariant, 7-task subagent-driven plan T1-T7 — T7 interrupted mid-handoff by user pivot). V86-followup-2 (5-task inline-executed plan T1-T5): user verbatim "เปลี่ยนจากเรืองสีฟ้าเป็นเรืองสีแดง แล้วลดความสว่างลดหน่อย ทั้ง Front และ Backend ทุกที่ ... ถ้าทำเมนูให้ตั้งได้ใน tab ตั้งค่ายิ่งดี". Pivot drops 8 per-section [data-section] blocks → single :root with red defaults (c1=#dc2626 + c2=#ef4444) + NEW --neon-intensity multiplier (default 0.45 = Q1=C dim red). All V86 alphas wrap in calc(<base> * var(--neon-intensity)) so a single slider drives global brightness via CSS cascade. NEW useV86GlowApply hook reads system_config.v86Glow → setProperty CSS vars on documentElement (AV83-sanctioned). NEW SystemSettingsTab 5th SectionCard "เอฟเฟกต์แสงเรือง": 2 color pickers (border + halo) + 4 preset dots each + custom hex input + intensity slider 0-150% + enabled toggle + live preview card + Save/Reset/Cancel + persist to clinic_settings/system_config.v86Glow + audit doc. **Defense-in-depth menu lock per user "อย่าไปยุ่งกับเมนูทั้งแบบเก่าและแบบใหม่"**: admin-frontend-zone selectors triple :not() chain (admin-top-menu + descendants + [class*="menu-"]) excludes ALL menu surfaces from V86 auto-glow. AV81 menu+print + Q4-B customer-facing zero-touch preserved across both V86 v1 + followup-2. AV83 wording updated to V86-followup-2 (drop per-section parity, add intensity multiplier + sanctioned consumers + menu :not() chain). Tests: Phase A vitest 47 (v1) → 64 (CG1-CG9 + VS1-VS6 NEW: CG2/CG3/CG8 rewritten, CG9 new menu :not() lock, VS1-VS6 new validator + hook + UI section coverage), Phase B Playwright 7 → 8 (B1-B4 RED-rewrite + B8 NEW live-slider test). V86 v1 commits 29c42310→b73ccad4 preserved (forward delta, no revert). Cosmetic-shell rule honored throughout — zero handler/state/prop/hook touch outside the 1 new hook + 1 new SectionCard. NO DEPLOY this session — joins existing combined queue per V18 (30+ commits ahead of prod). Post-deploy: Rule Q L1 user hands-on for all 8 backend tabs + AdminDashboard frontend + Settings UI interaction (color picker / preset dot / intensity slider drag / Save / Reset / Cancel) + dark/light + reduced-motion. |
| V83-followup-3 | 2026-05-18 EOD+8 LATE | **Perm/Tab mapping completeness (AV79) — 11 master-data tabs had DEAD `requires` due to `adminOnly:true` short-circuit** — User report (verbatim): "คนที่มีสิทธิ์ในการตั้งค่า จัดการสินค้า จัดการกลุ่มสินค้า เครื่องหัตถการ จัดการหน่วยสินค้า หรืออื่นๆ แต่ sub tab ทั้งแบบเดิมและใหม่ กลับปรากฎไม่ครบ ... ฝากเช็คว่าสิทธิ์กับสิ่งที่ app เราอนุญาติมันตรงกันทั้งหมดจริงๆ". User screenshot showed PermissionGroupFormModal "ตั้งค่า / ข้อมูลพื้นฐาน" with 11 checkboxes ticked but old sidebar ข้อมูลพื้นฐาน section showed only 4 items. **Root cause (systematic-debugging Phase 1)**: `canAccessTab` line 177 `if (gate.adminOnly) return false;` short-circuits BEFORE the `requires` array is checked. 11 master-data tabs declared `adminOnly:true`, so non-admin user with the matching perm key (product_management, doctor_management, etc.) was DENIED — perm grant was dead code. exam-rooms made it explicit: `{ requires: ['exam_room_management'], adminOnly: true }` — requires never reached the check. **Class-of-bug**: V12 multi-reader-sweep at permission-mapping boundary. `permissionGroupValidation.js` (perm catalog) and `tabPermissions.js` (gate map) are two readers of "what permission allows access" — they drifted. V83 had fixed link_request_management only; 11 remaining were latent. **Fix architecture (Phase 4)**: flip 11 tabs adminOnly→requires: product-groups (product_group) · product-units (default_product_unit) · medical-instruments (medical_instrument) · holidays (holiday_setting) · branches (branch_management) · exam-rooms (exam_room_management, dropped DEAD adminOnly) · permission-groups (permission_group_management) · staff (user_management) · doctors (doctor_management) · products (product_management) · courses (course_management, clinic_course_management — any-of). Admin bypass still implicit via canAccessTab isAdmin early-return. **6 sanctioned adminOnly remaining** (closed list — adding requires V-entry): masterdata (stale entry, tab removed V50) · finance-master (umbrella, no specific perm) · document-templates (no perm declared) · line-settings (admin) · fb-settings (admin) · backup-manager + branch-backup (destructive ops). **3 consumers** (BackendSidebar + new BackendArcBloom Menu D + BackendCmdPalette) all use `canAccess()` → fix propagates automatically. **NEW AV79 invariant** (HIGH priority) in `audit-anti-vibe-code` SKILL.md: "perm/tab mapping completeness — silent permission grant when adminOnly:true short-circuits requires". Grep pattern + sanctioned closed-list + pair-edit discipline (new perm key MUST be paired with requires gate OR NO_TAB_PERMS classification). **Test bank**: NEW `tests/v83-followup-3-perm-tab-mapping-completeness.test.js` (C1-C6: every-perm-grants-tab + 11-tab-locks + 4-persona × 11 matrix + sanctioned-closed-list explicit + source-grep regression + settings-perm-completeness). 92 assertions PASS. **V21 fixup**: `phase16.3-tab-permission-overrides.test.js` B.4 asserted `TAB_PERMISSION_MAP.branches.adminOnly === true`; switched to `line-settings` (sanctioned remaining adminOnly) since branches now requires-gated. Build clean 19.41s. Full vitest GREEN. **Lessons**: (a) **V83 was an incomplete Rule P expansion** — fixed 1 instance (link-requests) when class-of-bug grep would have surfaced 11 more. The `adminOnly:true` pattern across 19 entries should have been the smoking-gun grep in V83's Phase 2 Step 5. (b) **Dead-requires pattern** `{ requires: [...], adminOnly: true }` is doubly-anti-pattern: gate ignores requires AND comment misleads code reviewers. AV79 grep catches both. (c) **Permission/gate drift is the same V12 multi-reader-sweep family** as data-shape drift (V12) and field-name drift (V51/V77-ter). Two declarative readers of the same concept ALWAYS drift unless an audit forces sync. (d) **Closed sanctioned-exception list of 7 prevents AV79 silent expansion** — new adminOnly:true addition fails C4.1 unless added to SANCTIONED_ADMIN_ONLY set. NO DEPLOY this turn — joins existing queue. Post-deploy L1: log in as non-admin user with sample perms → menu shows ONLY accessible boxes; previously-hidden tabs (จัดการสินค้า etc.) now appear when their perm is granted. |
| V83 | 2026-05-18 EOD+8 | **Modal explicit-close-only universal strip (AV78) + link_request_management perm + label cleanup** — User pain (verbatim, locked permanent): "พอกรอกข้อมูลใน modal ใกล้จะหมดแล้ว ดันไปเผลอคลิ๊กตรงบริเวณที่ที่ว่างรอบๆ modal แล้ว modal มันปิดไปเอง ทำให้ต้องเริ่มกรอกข้อมูลใหม่ หัวร้อนมากๆ ... user คลิ๊กพลาดปิด modal บ่อยจนอยากจะทุบคอมทิ้ง". 57 ad-hoc modal files all carried `onClick={onClose}` / Pattern B currentTarget guard / state-setter dismissers on backdrop divs — copy-paste anti-pattern. **Class-of-bug**: V12 multi-reader-sweep at UI-affordance boundary. **Architectural fix (Q1=A mechanical strip)**: 6 parallel implementer subagents stripped ~80 backdrop-onClick instances across 56 files (Batches 1-6) + 2 inline fixes (MarketingFormShell trailing M2.1 catch + OrderDetailModal Batch 3 subagent broken `{// AV78 ... }` JSX comment causing parser error). ESC + X + Cancel handlers + inner `e.stopPropagation()` UNTOUCHED (per spec; harmless after backdrop strip). **2 sanctioned exceptions** (closed list): `StaffChatImageLightbox.jsx` + `TreatmentReadOnlyMirror.jsx` inner ImageLightbox (fullscreen image viewers — click-anywhere-closes IS expected UX per Stripe/Linear convention). Each annotated `// audit-anti-vibe-code: AV78 lightbox-explicit-exception`. Each stripped backdrop carries `// AV78 (EOD8): backdrop click does NOT close — explicit close only (X / Cancel / ESC)` marker (42 files, line-comment style not block to avoid JSX parser edge cases). **NEW AV78 invariant** in `audit-anti-vibe-code` SKILL.md (HIGH priority): grep at file-level boundary prevents new modals from re-introducing backdrop-onClick patterns. Closed sanctioned exception list of 2; adding a 4th lightbox requires V-entry + test extension. **Permission polish** (single commit alongside): added `link_request_management` key in settings module + flipped `tab=link-requests` gate `adminOnly:true` → `requires:['link_request_management']` (admin bypass still implicit via canAccessTab isAdmin early-return) + stripped `(16.3)` from `system_config_management` label + `(29.22)` from `recall_management` label (per user "เราทำเสร็จแล้ว"). **Test bank**: 4 NEW files — `tests/v83-modal-explicit-close-only.test.js` (M1-M5: sanctioned list closure + ZERO offenders + ESC/X presence + marker coverage + total count ≥40); `tests/v83-modal-explicit-close-flow-simulate.test.jsx` (F1-F7: backdrop click ignored × 22 vectors + X/Cancel/ESC close × 5 vectors + sanctioned lightbox exception + user-flow real-data scenario); `tests/v83-link-request-permission.test.js` (P1-P5: catalog + label exact match + tabPermissions wiring + canAccessTab 4-persona semantics + anti-regression label sweep + source-grep wiring locks); `scripts/v83-l2-link-request-perm-verify.mjs` (Rule Q L2 admin-SDK). **6 V21 fixups** in pre-existing tests that asserted OLD backdrop-click-closes behavior (E1.5 RecallEditModal + SN4.4 RecallSnoozeMenu + M7.2 RecallCreateModal + LT4.4 RecallLineTemplateModal + O5.2 RecallOutcomeModal + Q8.3 link-requests adminOnly→requires). Each flipped from `expect(onClose).toHaveBeenCalled()` → `expect(onClose).not.toHaveBeenCalled()` with V83/AV78 cross-link comment. **Rule Q L2 PASS** on real prod: `scripts/v83-l2-link-request-perm-verify.mjs` (TEST-LINKREQ-V83 fixtures × 3 across 2 branches: 2 BR_A + 1 BR_B → branch-isolation query verified per branch → cleanup 3/3 → zero-orphan verified → audit doc emitted `v83-l2-link-request-perm-verify-1779082269383-12e12359`). **L1 hands-on deferred to user post-deploy**. Build clean 2.76s (BackendDashboard chunk unchanged). Full vitest 0 FAIL after V21 fixups (11583/11583 baseline + 47 V83 new + 6 V21 corrections = ~11636 PASS). ~28 commits ahead of prod awaiting `deploy` verb. **Subagent execution lessons**: (a) **Parallel subagent rate-limit at 6 dispatches** — 5/6 hit "Server is temporarily limiting requests" partway through; only Batch 6 completed cleanly (6 files / 15 backdrops). Recovery: inline Read+Edit batch on remaining ~45 files (~6-8 parallel per turn). 11 files were partially processed by their agents before rate-limit. Lesson: when running ≥5 parallel subagents on Anthropic API, plan for partial-completion fallback — inline orchestrator-edit is the canonical recovery. (b) **Batch 3 subagent JSX comment shape bug** — agent wrote `{// AV78 ... }` (broken — `}` swallowed by `//` comment, parser then sees `<div>` outside JSX expression context, error: "Expected , or ) but found Identifier"). Build failed for ~5 min before second fix; caught via per-file esbuild parse. Lesson: when prescribing markers to subagents, give exact line-comment form `// foo` (NOT `{// foo}` which is invalid JSX). (c) **Marker style consistency**: started with `/* */` block, converted to `//` line during build-fix to match Batch 6 agent's pattern + avoid JSX text-parse edge cases. 42 files now uniform. (d) **AV67 collision avoided** — initial plan reserved AV67 for new invariant, but AV67 already taken by Vercel serverless dep restriction. Pivoted to AV78 (next free) via `sed -i 's/AV67/AV78/g'` across 42 files in one bash one-liner. (e) **Closed sanctioned exception list of 2** is the right pattern for UX deviations — explicit closed list + test enforcement + V-entry requirement for additions prevents drift. NO DEPLOY this turn — joins existing queue per V18; user authorizes `vercel --prod` separately. After deploy: Rule Q L1 hands-on (any modal → click outside → modal STAYS OPEN → click X → modal CLOSES; per-branch link-request test as non-admin staff with `link_request_management` perm in branch A). |
| V82 | 2026-05-17 EOD+3 | **Staff Chat persistent read cursor + force-open until read + 4 role badges (single-cycle brainstorm→plan→6 subagent chunks→Rule Q L2→deploy×2)** — User report 3-in-1: (a) chat widget closes/reopens on every Frontend↔Backend tab switch + previously-read chats flip back to "unread" + noti sound spam (Bug #2 root cause: V73 useStaffChat had `lastSeenIdsRef = useRef(new Set())` — in-memory only, resets on every remount; listener fires with last 50 messages on resubscribe → all 50 look "new" → unread spam); (b) feature ask "force chat open until all unread read" (force-open until scroll-to-bottom); (c) feature ask "role badges in NamePicker + bubble" (4 roles แพทย์/ผู้ช่วยแพทย์/พนักงาน/ผู้จัดการ). **Brainstormed Q1-Q4 with Visual Companion**: Q1=B scroll-to-bottom=read (Slack/Discord pattern), Q2=A localStorage per-(device,branchId), Q3=B colored circle role-tinted gradient, Q4=all 3 defaults (first-mount silent backlog + scroll-up while force-open blocks minimize + badge optional). Spec at `docs/superpowers/specs/2026-05-17-staff-chat-cursor-forceopen-badge-design.md`. Plan at `docs/superpowers/plans/2026-05-17-staff-chat-cursor-forceopen-badge.md` (13 tasks). **Execution via subagent-driven-development across 6 chunks** (Tasks 1-3 foundation NEW files + Task 4 useStaffChat refactor + Task 5 buildMessageDoc senderRole + Tasks 6-8 UI integration + Task 9 test bank + Tasks 10-12 AV/stress/L2). 4 NEW src files: `src/lib/staffChatReadCursor.js` (cursor module with `getCursor`/`setCursor`/`isMessageUnread`/`initCursorIfMissing`) + `src/components/staffchat/StaffChatRoleBadge.jsx` (Lucide icons + role-tinted gradient, lg=40px + sm=16px) + 7 modified src files (useStaffChat replaces lastSeenIdsRef with cursor + canMinimize + markScrolledToBottom + role wire; staffChatIdentity adds getRole/setRole/ROLE_KEYS/ROLE_LABELS_TH; staffChatClient buildMessageDoc accepts senderRole; NamePicker adds role section + (name,color,role) signature; StaffChatMessage renders RoleBadge inline before name; StaffChatMessageList adds bottomSentinelRef IntersectionObserver; StaffChatHeader minimize button disabled={!canMinimize} + tooltip "เลื่อนลงล่างก่อน ⬇"). **Bug found post-T9 via V73 flow-simulate red**: subagent's initial cursor module narrowed createdAt check to `typeof === 'number'` — silently returned false for ALL real prod messages (Firestore SDK returns Timestamp instances, NOT numbers); cursor never detected unread → force-open + sound + auto-expand never fired in prod. Fix: dual-shape support in 3 sites (cursor.isMessageUnread + useStaffChat seedMs + markScrolledToBottom). A.7-bis regression test locks the contract. **V21 fixups** (10 across V73 sibling tests): adapter to (name,color,role) signature + force-open auto-expand semantics + cursor-relative dedup; pre-V82 baseline 17 stale fails (V77 BMT removed by V81-fix4 + V81-fix2 ack-gate post-V81-fix4 + V81-source-grep + V81-fix3 AV67.1 archiver post-V81-fix6b + V75 button-polish + RP1 IIFE in BackupManagerTab) ALL closed in V82-followup batch (3 test-file commits + 1 source commit extracting BackupManagerTab IIFEs to `formatBytesDisplay` helper per Rule C3). **AV76 invariant** codified: in-memory dedup of Firestore listener results (`useRef(new Set())`) crashes on remount → forbidden for cross-remount dedup; persist via localStorage (per-device) or Firestore doc (cross-device). Source-grep pattern: `useRef\s*(\s*new Set\s*(` near `listenTo*` callers. **Rule Q V66 verification**: L2 admin-SDK `scripts/v82-cursor-l2-verify.mjs` (5 listener re-fires return identical doc IDs on real prod = cursor stability proven); 10-scenario stress runner `scripts/v82-staff-chat-stress.mjs` (10/10 PASS, 23 TEST-V82 fixtures created + cleaned). L1 user hands-on post-deploy required to confirm UX (tab switch chaos test + force-open block + badge selection persists). **Deployed both rounds**: round 1 = V82 implementation (Vercel `2b156ltbl-...` + Firebase rules idempotent + 6/6 probes both sides + L2 PASS); round 2 = V21 cleanup batch (Vercel `4lct44tkm-...` + 6/6 probes + L2 PASS). Final test state: **11294/11294 PASS / 0 FAIL** (was 11284/11319 = 35 fails pre-V82 fixups; now 0 after V82 + cleanup batch). Build clean. **Lessons**: (a) **Subagent over-narrowing** — implementer subagent simplified the spec's dual-shape check to single-shape; missing realprod Timestamp support. V21-class bug invisible to V82 own tests (used raw numbers) but caught by V73 flow-simulate (uses {toMillis} fixture). Lesson: spec must EXPLICITLY enumerate input shapes; cross-test against existing fixture shapes too. (b) **Rule K work-first-test-last validated**: 6 subagent chunks built source structure first → review revealed real bug → 1 final test pass + regression locks landed in batch. No churn. (c) **Bug-loop discipline**: per user "วนลูปจน Perfect" — Round 1 deploy after V82 implementation + 0 V82 regressions (133/133); Round 2 deploy after closing 17 pre-V82 baseline V21-stale (11294/11294). "Perfect" achieved with explicit 0/0 fail counts both sides. (d) **In-memory dedup ref is a multi-reader-sweep family member** at the LISTENER boundary (V12 family). AV76 codifies the pattern permanently; future listener-consuming hooks must use persistent cursor. |
| V81-fix1 | 2026-05-17 EOD+1 | **Timestamp/GeoPoint/Bytes round-trip preservation — CRITICAL V81 architectural bug found by Rule Q V66 real-prod diagnostic, fixed + deployed same turn** — User authorized full prod wipe-restore test ("ผมวางเดิมพันกับนายในครั้งนี้ ไม่งั้นก็ lose everything"). Per Rule Q V66 "maximally confident before destructive op", ran multi-layer evidence first instead of prod gamble; e2e × 2 + 109 unit + property-based × 100 + AV62 hash all GREEN. **Then ran first-principles real-prod admin-SDK diagnostic that READS REAL DATA SHAPE** (`scripts/diag-v81-timestamp-roundtrip.mjs`): **CRITICAL BUG CONFIRMED** — Firebase admin SDK `Timestamp.toJSON()` outputs `{_seconds, _nanoseconds}`; JSON.parse gives plain object; `batch.set(doc, that)` writes as Map field, NOT Timestamp. **V81 restore would have silently degraded EVERY Firestore Timestamp field system-wide** (e.g. `chat_history._v76BranchBackfilledAt` × 3,281 docs, `chat_history._v77quinquiesBackfilledAt` × 818 docs, `be_recalls.createdAt/updatedAt`, plus all forensic stamps + audit `performedAt` + cron `nextRetryAt`). Real impact: `doc.createdAt.toMillis()` would throw; Firestore range queries fail; composite indexes broken; cron `WHERE nextRetryAt <= now` returns nothing. **Bug invisible to**: 109 unit tests (mocks have no Timestamp instances), property-based × 100 (plain JS), 7-phase e2e × 2 (verified hash + counts not field shapes), AV62 hash validation (hashes match both sides because JSON serialization is consistent). **Fix architecture**: NEW `encodeFirestoreData(value)` + `decodeFirestoreData(value, {Timestamp, GeoPoint})` in `src/lib/wholeSystemBackupCore.js` (+114 LOC). Sentinel marker `{__type: 'timestamp', seconds, nanoseconds}` (also `geopoint`, `bytes`). Strict 2-key duck-type detection avoids false positives. Decode requires complete marker shape; partial/unknown `__type` passthrough as plain object (forward-compat). V38 spread-order invariant (id last) preserved through encode. 4 encode sites in `wholeSystemBackupExecutor.js`; decode site in `restoreCollections` of `wholeSystemRestoreExecutor.js` + `Timestamp`/`GeoPoint` SDK imports. **Tests**: 31 V81-fix1 tests G/H/I/J in `tests/v81-fix1-firestore-type-roundtrip.test.js` (encode unit + decode unit + round-trip identity + property-based × 50 + V81 prod-shape mirror + adversarial user-data with `__type` field + V38 through full roundtrip + source-grep regression locks). Cumulative V81: 140/140 PASS. **Real-prod verify** (`scripts/diag-v81-fix1-roundtrip-verify.mjs`): seed TEST-V81-TS- fixture with 3 Timestamp fields → backup via patched executor → read be_customers.json (1.26MB / 392 customers) → verify 31 `__type:timestamp` markers present → decode → assert `decoded.createdAt instanceof Timestamp` AND `.toMillis()` matches seed → cleanup zero orphans. **ALL PHASES GREEN**. Commit `9107fd0`; deployed via Vercel (firebase unchanged); aliased to `https://lover-clinic-app.vercel.app`. **Lessons** (codified permanent): (a) **Rule Q V66 real-prod data introspection beats hash verification for type-preservation contracts** — hashing assumes serialization IS the contract; type fidelity is a SEPARATE contract that hash can't see. (b) **Mock tests are code-shape coverage, NOT behavior verification** — V66 lesson lived again. (c) **Library-level invariants prove only library; executor-level invariants must be verified against real data shape through the executor path** — `simulateBackup`/`simulateRestore` in property-based tests use plain JS; real executor reads admin SDK class instances; the gap WAS the bug. (d) **Sentinel marker encoding** is the canonical Firestore-type round-trip pattern; any future backup/clone/migration code that serializes Firestore data via JSON MUST pass through encode/decode helpers. Future AV65 candidate: "Firestore data MUST encode through `encodeFirestoreData` before JSON.stringify in any backup/clone code path". (e) **User's bet ("lose everything") paid off** — catch cost ZERO data; would-have-cost catastrophic Timestamp-degradation system-wide. (f) **Backups taken before V81-fix1 deploy are at-risk** for restore (would degrade Timestamps); admin should re-take backup post-deploy. **Class-of-bug**: V12-family multi-reader-sweep at the SERIALIZATION-FORMAT boundary — admin SDK writers use Timestamp class; JSON readers see internal `_seconds/_nanoseconds`; round-trip identity requires symmetric encode+decode. |
| V81 | 2026-05-17 | **Whole-System Backup & Clone SHIPPED (24/28 tasks)** — Auto-daily 03:00 BKK cron + 5d rolling retention + manual button + hybrid Fresh-only/Replace restore + AV19 auto-pre-backup MANDATORY + portable tar.gz download + 4 testing tiers (50 unit + 7 Rule I + 46 source-grep + 6 property-based × 100 fixtures + 6 emulator hermetic scenarios Java-gated + 3 verifier scripts for secondary-DB/stage-cron/live-e2e). Architecture: V40+V74+V75+V77b/c-inherited. 20 new files + 4 modified. Key constants: WHOLE_SYSTEM_SCHEMA_VERSION=2, UNIVERSAL_COLLECTIONS×23, BRANCH_SCOPED_COLLECTIONS×30, CUSTOMER_SUBCOLLECTIONS×8 (V74 T4), STORAGE_EXCLUDE_PREFIXES contains backups/ (CRITICAL recursion gate). New AV invariants: AV62 (manifestHash integrity), AV63 (cron CRON_SECRET + concurrency lock), AV64 (retention discipline 5d/7d/∞/24h), AV19 elevation V81 (Replace mode MUST autoBackupRef + verify exists). 109 V81 tests PASS + 7 emulator-skipped without Java. Build clean ✓ 2.76s. Drift scanner 0/473. 21+ commits ahead of prod awaiting `deploy` verb. Lessons: (a) subagent autocompact thrashing on large-context projects — inline execution when project_baseline > subagent_budget. (b) plan-vs-reality adapters — adapt to existing canonical surface (`adminAuth.js` not `verifyAdminToken.js`; `getFbConfig` not `getFbConfigForBranch`). (c) two-tier hash sealing (storageManifestHash separately sealed + included in outer manifestHash) — Storage-only tamper detectable independent of collection-side. (d) recursion gate is the highest-impact 4-line constant; without it daily backup size doubles every day. (e) AV19 elevation pattern generalizes V40→V74→V81: capture state BEFORE destruction + verify capture exists + refuse if either fails. (f) Firebase emulator graceful skip via `describe.skipIf(SKIP_V81_EMULATOR === '1')` pattern. (g) multi-database Firestore (`gcloud firestore databases create --database=clone-verify`) for sandbox real-prod verification ~$0.004/month. Tasks 27-28 deploy pending USER `deploy` verb. Per Rule Q V66 — NOT claiming verified end-to-end without L1; T1-T5 prove architectural contracts at code-shape; T6-T8 ready when user authorizes; T9 user hands-on per Rule Q gold standard. |
| V80 | 2026-05-16 NIGHT+4 | **P0 black-screen + chat-history cross-branch leak (2 bugs, 1 deploy)** — User: "หน้า frontend ดำสนิทไปแล้ว" + "พระราม 3 และ ทดลอง 1 มีประวัติแชทเก่าของนครราชสีมา". (a) V78 added `useMemo()` to `useChatUnread` without importing useMemo → ReferenceError → React unmounts AdminDashboard tree → black screen. Fix: 1-char import + `scripts/diag-react-hook-import-drift.mjs` Rule P perpetual guard (0/462 post-fix). AV60. (b) 7 chat_history docs MISSING branchId (V76 deploy race window). Rule M backfill `v80-backfill-chat-history-missing-branchid.mjs --apply` (7 docs → NAKHON, audit emit). 3 reader fall-throughs NAKHON-gated via `isLegacyNakhonBranch()` + handleResolve hardcoded `HARDCODED_NAKHON_BR_ID` fallback. AV61. Rule Q L1 verified on running dev server: NAKHON 20 history entries / พระราม 3 empty / ทดลอง 1 0 entries. Class-of-bug: V11/V21 family at hook-import boundary + V12 multi-reader-sweep at fall-through-filter boundary. Lessons: (a) build passing ≠ runtime safe; source-grep tests pass when text matches regardless of identifier resolution at runtime — AV60 drift scanner closes the gap. (b) 70 V79 + 57 V78 source-grep tests all GREEN while real React tree unmounted — Rule Q L1 (Playwright/preview real browser) would have caught the useMemo gap instantly. (c) V12 multi-reader-sweep applies to FALL-THROUGH filters too — 3 sibling readers in same ChatPanel file all carried bare `!c.branchId ||` pattern; V79 missed them; V80 closes. (d) race-window data straggling AFTER a backfill is real — V76 audit said "3,281 migrated" but actual missed 7 docs in deploy window. V77-bis hardcoded fallback (mirror) prevents future missing-branchId writes. |
| V76 | 2026-05-16 EOD+1 | **chat_history BSA — V12 multi-reader-sweep at sibling reader/writer** — User reported (verbatim, angry): "tab chat frontend กุเปลี่ยนครบทุกสาขาแล้วไม่เห็นจะแยกกันเลยไอ้สัส ทำเหี้ยไรตั้ง 40 กว่า task แล้วได้งายโง่ๆแบบนี้". Diag confirmed: **chat_history = 3,281 docs, ALL withoutBranchId** (chat_conversations was empty). V75 wired LIVE chat_conversations through BSA (BS-17 + AV57) but **completely missed the SIBLING chat_history reader (ChatPanel.jsx:513-532) + writer (ChatPanel.jsx:572-586)**. The history view (red ⏰ clock icon) reads chat_history with NO branchId filter → identical cross-branch leak. Class-of-bug: **V12 multi-reader-sweep** — same family as V36 / V35 / V47 / V49 (each: shipped one reader/writer, missed sibling). Fix architecture (Rule P Phase 4): (a) NEW `listenToChatHistoryByBranch` in backendClient.js (Layer 1 safe-by-default; mirror of chat_conversations BSA); (b) scopedDataLayer.js Layer 2 wrapper (auto-inject resolveSelectedBranchId); (c) ChatPanel.jsx history listener migrated to wrapper + selectedBranchId in deps + client-side fall-through for continuity; (d) ChatPanel.jsx handleResolve stamps `branchId: conv.branchId || selectedBranchId || ''` + `branchIdSource: 'inherited-from-conv' | 'resolved-by-admin-branch' | 'unstamped'`. **NEW AV59 invariant** locks chat_history BSA discipline at write + read boundaries (sanctioned consumers: backendClient.js + scopedDataLayer.js + ChatPanel.jsx). **Rule M backfill** (`scripts/v76-backfill-chat-history-branchid.mjs`): dry-run found 3,281 unstamped → `--apply` stamps all to นครราชสีมา (sole-active pre-V75) with forensic `_v76BranchBackfilledAt` + `_v76BackfillReason`. 28-test regression bank `tests/v76-chat-history-branch-scope.test.js` (Layer 1 safe-by-default + Layer 2 auto-inject + reader migration + writer stamp + backfill helpers + Rule P class-of-bug exhaustive check). **Lessons**: (a) **V75 test bank failed to catch this because AV57 only asserted webhook chat_conversations writes** — sibling chat_history writer (admin-resolve) was outside AV57's grep scope. AV59 covers both `chat_conversations` (already V75) AND `chat_history` (V76) — full chat-collection-family coverage. (b) **V12 multi-reader-sweep at COLLECTION FAMILY level** — when adding BSA discipline to one collection in a family (chat_conversations), grep ALL related collection writes/reads. `chat_history`+`chat_conversations`+`messages` subcoll all in same family. Future migrations need family-wide audit. (c) **Rule Q V66 hit again** — 210+ V75 tests PASS, prod broken at user's first multi-device hands-on. Tests covered AV57 contract exactly as designed; the DESIGN missed chat_history. Tier 2 source-grep can only catch what AV invariant grep targets. Adding AV59 was the missing piece. (d) **Active break-attempt mindset**: if I had Rule Q L1'd by switching to history view BEFORE shipping V75, this would have surfaced. User's hands-on did Rule Q for me — costing trust. Lesson: future "chat per-branch" or similar feature MUST L1 across ALL UI sub-views (chat list, history, search, filter) before claiming done. |
| V75 | 2026-05-16 | **Per-branch chat + whole-fleet customer backup SHIPPED (Items 1+2+3+4)** — Spec + 43-task plan + ~40 V75 commits across 2 sessions. Item 1 (CustomerDetailView 4-button row polish ✓). Item 2 (whole-fleet customer backup/restore — `--all-customers` CLI ✓; `/api/admin/whole-fleet-customer-backup-export` deferred to V75-bis; `/api/admin/whole-fleet-customer-restore` endpoint ✓; `scripts/whole-fleet-customer-restore.mjs` Rule M CLI ✓; AV56 manifestHash + WHOLE_FLEET_MANIFEST_TAMPERED enforcement). Item 3 (chat per-branch — webhook stamps branchId via resolveChatBranchIdFromLineEvent + resolveChatBranchIdFromFbEvent ✓; backfill script ✓; backendClient Layer 1 safe-by-default + scopedDataLayer Layer 2 auto-inject + BS-17 audit ✓; ChatPanel listenToChatConversationsByBranch wire ✓; **NEW** `/api/admin/fb-test` endpoint ✓; **NEW** FbSettingsTab.jsx (per-branch be_fb_configs CRUD, auto-seed banner from clinic_settings/chat_config for นครราชสีมา) ✓; nav + tabPermissions + BackendDashboard wire ✓; firestore.rules be_fb_configs match + Probe #12 ✓). Item 4 (chat tab mute toggle + chatNotificationMute helper + AdminDashboard playChatNotificationSound wrapper migration + AV58 cross-surface noti scope ✓). **Architecture**: per-branch chat via webhook resolvers (LINE destination → be_line_configs / FB pageId → be_fb_configs); fallback to นครราชสีมา preserves continuity for legacy chats; backfill script (Rule M) stamps branchId on pre-V75 chats. Whole-fleet backup = manifest.json + per-customer separate Storage blobs (NOT zip-bundled); AV56 manifestHash covers all customer fileHashes + storageManifestHashes; userNote EXCLUDED (Q5b=Y); per-customer failure isolation. **Continuity contract verified**: 15-assertion CRITICAL test bank (`tests/v75-chat-continuity-flow-simulate.test.js`) proves นครราชสีมา admin sees ZERO action through migration (backfill idempotency + no-clobber on pre-existing branchId + LINE webhook unchanged + FB auto-seed). **Maha-adversarial bank**: 28-assertion V48 prof-grade (mulberry32×100 + Thai NFC≠NFD + NUL + 10K + idempotency×5 + cross-branch identity + forward/backward compat + concurrent-mutation snapshot). **AV invariants added**: AV56 (whole-fleet manifestHash integrity), AV57 (webhook branchId stamping), AV58 (chat noti mute scope — ChatPanel ONLY consumer of chatNotificationMute helper). **BS-17** added to audit-branch-scope (chat_conversations Layer 1 safe-by-default + Layer 2 auto-inject). **Probe #12** added (anon write to be_fb_configs → 403). **Deferred to V75-bis**: WholeFleetBackupModal + RestoreModal + BackupManagerTab whole-fleet wire (Tasks 24-26); Playwright L1 specs (Tasks 35-37); live admin-SDK e2e on real prod (Tasks 33-34). NO DEPLOY this session — local + commits only; user authorizes combined `vercel --prod` + `firebase deploy --only firestore:rules` per V18. After deploy: admin runs `node scripts/v75-backfill-chat-conversations-branchid.mjs --apply` (Rule M one-shot); Rule Q L1 multi-device hands-on per spec § 8 acceptance scenarios. **Lessons**: (a) **Plan-vs-reality adapters** — plan referenced `/_lib/verifyAdminToken.js` import path but actual is `/_lib/adminAuth.js`; plan referenced `getFbConfigForBranch` but fbConfigClient exports `getFbConfig`; plan suggested fflate for zip but actual whole-fleet format is per-customer separate files (no zip). When plan-text and existing code diverge, adapt to existing canonical surface — don't introduce a parallel API just because the plan said so. (b) **Multi-source `branchIdSource` attribution** preserves admin auditability: customers see one branchId, admin can trace whether it came from `webhook-line` / `webhook-fb` / `webhook-line-fallback-nakhonratchasima` / `webhook-fb-fallback-legacy` / `backfill-v75-sole-active`. Future analytics + debugging benefit. (c) **PRNG-fixture gotcha** in adversarial tests: a shared mulberry32 PRNG advances state on every `randomCustomer(i)` call, so calling it twice for "identical fixture" produces different fileHashes. Build base ONCE then clone for variation tests (CAT3.1 lesson). (d) **BS-17 at chat_conversations Layer 1** mirrors V54 BS-13 listener safe-by-default pattern: branch-scoped listeners MUST resolve branchId via resolveSelectedBranchId() when not explicit; if resolution fails + no allBranches → return empty. NO whole-collection fallback. Same architectural backstop as V54 appointment listeners. |
| V74 | 2026-05-16 | **Customer backup/wipe/restore + unified backup-manager SHIPPED** — Per-customer GLOBAL backup capturing CD+C11+CG+CS+CF+CH tiers; AI (audit-immutable) preserved through wipe per V34/MOPH. Q1-Q6 brainstormed + locked → 620-line spec → 1945-line 33-task plan → ~21 of 33 tasks shipped (foundation + EXPORT + DELETE + RESTORE + 5 manager endpoints + 4 UI components + nav wiring + storage.rules + e2e + AV52-AV55 + audit-cascade-logic C16 + diag CLI + V21 fixups + handoff). 116 V74 tests + 55 Phase 24.0 preserved with V21 fixups. **Architecture commitments**: cascade extended Phase 24.0's 11 → 16 collections (closes stale-cascade gap CG: be_quotations + be_vendor_sales + be_online_sales + be_sale_insurance_claims + be_recalls) + recursive 8 customer-attached subcollections (T4) + Storage object cleanup under be_customers/{cid}/ + chat_conversations matching via matchCustomerChatPredicate (customerId OR lineUserId_byBranch). 6-layer integrity contract (bodyHash + storageManifestHash + per-Storage-SHA-256) — meta.userNote EXCLUDED so admin can rename labels without breaking integrity (Q5b=Y). Restore uses Q3=B SAFE conflict resolution (BLOCK on customerId-exists + HN-collision; STRIP lineUserId_byBranch conflicts + audit-tracked; ALLOW stale FKs as-is — V41 lookup-map handles missing display). AV19 elevated: delete-customer-cascade BLOCKs wipe on integrity mismatch. AV55 72h-grace on backup-manager delete (admin can forceOverrideGrace). Manager tab UNIFIED across V40 branch + V15 central-stock + V74 customer backups (rename/delete/bulk-delete; restore stays on type-specific tab). **V74 CLI works on prod today**: customer-backup-export.mjs + customer-delete-with-backup.mjs + customer-restore.mjs + diag-customer-backup-integrity.mjs. **UI shipped**: 💾 สำรอง button in CustomerDetailView + DeleteCustomerCascadeModal extended with auto-backup checkbox + CustomerDataRecoveryTab + BackupManagerTab. **AV invariants added**: AV52 (file integrity), AV53 (autoBackupRef AV19 elevation), AV54 (subcoll cascade discipline), AV55 (72h-grace). audit-cascade-logic skill extended with C16. Storage rules existing `match /backups/{prefix}/{file=**}` wildcard already covers customer paths (renamed for clarity). Probe-Deploy-Probe #11 documented. NO DEPLOY this turn — local + commits only; user authorizes combined `vercel --prod` + `firebase deploy --only firestore:rules,storage:rules` per V18. After deploy: Rule Q L1 hands-on by user (6 acceptance scenarios per spec § 9). **Class-of-bug fully addressed**: Phase 24.0 cascade gap (5 missing collections) + subcoll orphan + Storage orphan + chat orphan all closed by AV54. Future bug-class: any new customer-attached collection MUST be added to CUSTOMER_CASCADE_COLLECTIONS_FULL (audit grep at the helper file enforces). |
| V1  | 2026-04-19 | strict firestore.rules deploy → chat + calendar 403 → **iron-clad B (Probe-Deploy-Probe)** |
| V2  | 2026-04-19 | Phase 9 backend tabs called ProClinic → removed → **iron-clad E (backend = Firestore only)** |
| V3  | 2026-04-19 | Phase 9 edit POST guessed URL → 404 → **iron-clad F (Triangle Rule)** |
| V4  | 2026-04-19 | `vercel --prod` repeated without re-asking → **per-turn deploy auth** |
| V5  | 2026-04-19 | over-simplified rules lost context → **anti-examples preserved** |
| V6  | 2026-04-19 | Edit silent-fail (param typo) + claimed "committed" → **grep-pair after every Edit** |
| V7  | 2026-04-19 | V4 repeat (vercel auth doesn't roll over) → **every deploy = new ask** |
| V9  | 2026-04-20 | Phase 11.2 deploy overwrote Console rule for cookie-relay → **probe list 4 → 5 endpoints** |
| V11 | 2026-04-24 | mock-shadowed export (vi.mock created name; build caught) → **`npm run build` mandatory** |
| V12 | 2026-04-24 | shape migration half-fix (writer changed, reader broke) → **grep ALL readers before shape change** |
| V13 | 2026-04-25 | 3 rounds of buffet-expiry bug; helper tests passed → **iron-clad I (full-flow simulate)** |
| V14 | 2026-04-25 | `options:undefined` rejected by setDoc → **regression guard: walk output for undefined leaves** |
| V15 | 2026-04-25 | combined deploy convention: `"deploy"` = vercel + firestore:rules in parallel |
| V16 | 2026-04-25 | public-link "Invalid Link" flash before anon-auth → **render gate + null loading state** |
| V17 | 2026-04-25 | mobile-resume listener stall → **visibilitychange + online → disable+enable network** |
| V18 | 2026-04-25 | V4/V7 THIRD repeat (vercel without asking) → **deploy auth never carries forward** |
| V19 | 2026-04-26 | image-only edit triggered stock-reverse 403 → **`stockChanged` gate + rule narrowed via `hasOnly`** |
| V20 | 2026-04-26 | multi-branch decision: Option 1 (single project + branchId field) — locked |
| V21 | 2026-04-26 | TreatmentTimelineModal: dataUrl `<a>` blocked + edit hidden under modal z-index → **Lightbox + close-on-edit** |
| V22 | 2026-04-26 | schedule calendar filtered to selected staff (ProClinic shows ALL stacked) → **multi-instance test fixtures** |
| V23 | 2026-04-26 | opd_sessions update rule blocked anon-auth patient submit (live since 2026-03-23) → **probe list extended for anon-auth** |
| V24 | 2026-04-26 | schedule sync only fetched doctor (employee empty) → **per-role URL-encoded Thai endpoints** |
| V26 | 2026-04-26 | Deploy 2: rules narrowed from email regex to claim check → **email-as-auth is unverified** |
| V27 | 2026-04-26 | probe artifacts polluted patient queue → **probes use isArchived:true on CREATE** |
| V28 | 2026-04-26 | soft-gate isAdmin required @loverclinic email even for gp-owner staff → **trust be_staff group, not email prereq** |
| V31 | 2026-04-26 | orphan Firebase Auth on staff delete + missing token revoke + no self-delete protect → **silent-swallow `try/catch console.warn(continuing)` is anti-V21**; credential-change MUST `revokeRefreshTokens`; self-delete MUST be 3-layer (UX + client + server) |
| V32 (4 rounds) | 2026-04-26 | Bulk PDF blank 2nd page + text floating above dotted line — V21-class regression across 16 templates → direct html2canvas+jsPDF + position-absolute inner span at bottom 10px (not flex/line-height); shared StaffSelectField + computeStaffAutoFill (Rule of 3); M9 reconciler |
| V32-tris-ter | 2026-04-26 | full LINE OA: Q&A bot + QR linking + LineSettingsTab — webhook bot reply MUST run AFTER chat-message storage; one-time tokens MUST live in client-blocked collection (`be_customer_link_tokens: read,write: if false`); same-reply anti-enumeration |
| V32-tris-bis | 2026-04-26 | P1-P3 batch: T3.e email/LINE delivery (later stripped to LINE-only) + T4 course exchange/refund + T5.b TFP billing + T5.a designer MVP. "Config-missing 503 with friendly error" pattern for features depending on user-side config |
| V32-tris-ter-fix | 2026-04-26 | (1) browser CORS block on api.line.me → backend `/api/admin/line-test` proxy; (2) webhook unauth REST blocked by `be_*: if false` → switched webhook to firebase-admin SDK. **Server-side privileged code (admin SDK) is the correct way to read rule-locked collections — keep client SDK locked for defense-in-depth.** |
| V32-tris-quater | 2026-04-27 | admin-mediated ID-link flow (no SMS/OTP): customer DM `ผูก <ID>` → bot rate-limit (5/24h) + admin SDK lookup + same-reply anti-enumeration ack + admin queue → batch atomic (customer.lineUserId + request.status='approved') + LINE Push. **Customer doc edits MUST use Firestore dotted-path** (`'patientData.nationalId'` preserves siblings vs `{patientData:{nationalId}}` which WIPES). **Customer + audit MUST be batch atomic.** |
| V34 | 2026-04-28 | createStockAdjustment used `reverseQtyNumeric` (cap-at-total) for type='add' → silent no-op when batch at full capacity (remaining===total) → ADJ doc + movement written but qty unchanged. Production-affecting bug live since stock system shipped. **Fix**: new `adjustAddQtyNumeric` helper (soft-cap, bumps total when remaining exceeds it); `reverseQtyNumeric` semantics preserved for refund paths. Phase 2 audit: cancelStockOrder + updateStockOrder cost cascade migrated to `writeBatch` (atomicity). Phase 3: 61 invariant tests in `tests/v34-stock-invariants.test.js` covering 13 invariants × adversarial inputs. **Phase 4**: audit-stock-flow upgraded S1-S15 → S1-S20 (per-tier conservation, time-travel, concurrent tx safety, listener alignment, test-prefix). **Phase 5**: V33.11 stock-test prefix (`testStockBranch.js`). **Lesson**: helper-output tests + source-grep are NECESSARY BUT NOT SUFFICIENT for stock mutations — preview_eval against real Firestore is mandatory per Rule I item (b) (now non-negotiable for stock paths). **Damage scope**: any historical ADJUST_ADD on full-capacity batches is a phantom audit entry — admin sees +N in movement log + adjustment table but batch.qty unchanged. 4 known artifacts in production (chanel +20+20+10 yesterday + my V34 +5 verify). |
| V35 | 2026-04-28 | 5 user-reported stock bugs after V15 #3 deploy: (1) **Branch stock balance silent miss** — `StockBalancePanel.jsx:92` called `listStockBatches` without `includeLegacyMain`. Phase 15.4 commit `26ee312` fixed the 3 stock create forms but missed the BALANCE reader → admin imports succeeded but balance row stayed stale. Fix: mirror MovementLogPanel:107–112 derivation in StockBalancePanel.load. (2) **Sale delete "เด้งจอดำ"** — final `await deleteBackendSale + loadSales` in `SaleTab.jsx:779-780` was UNGUARDED. Test sales (TEST-SALE-DEFAULT-*) with malformed shapes threw → React error boundary → black screen. Fix: try/catch + `setError` Thai message. (3) **Orphan products in stock** — Acetin 6 / Aloe gel 010 in StockBalancePanel but not in be_products. Root: batches store DENORMALIZED `productName` + zero FK validation at write. Fix: NEW `_assertProductExists(productId, contextLabel)` async helper (hoisted) called BEFORE every `setDoc(stockBatchDoc)` in 3 sites: `_buildBatchFromOrderItem` + `updateStockTransferStatus._receiveAtDestination` + `updateStockWithdrawalStatus._receiveAtDestination`. (4) **Test pollution** — ADVS-/ADVT- products + TEST-SALE-* sales accumulated. Fix: 3 NEW admin cleanup endpoints (`/api/admin/cleanup-orphan-stock` + `cleanup-test-products` + `cleanup-test-sales`) with two-phase DRY-RUN→delete + audit doc to `be_admin_audit`; bash-only per V29; cascade gate (test-products refuses delete if be_stock_batches still references). (5) **ความจุ column UX** — NO bug; column = sum(batch.qty.total) which is correct. Fix: tooltip on header + per-row "(เป้าหมาย: N)" sub-label using QtyBeforeMaxStock. **Tests**: phase15.6a (54 SBL) + phase15.6b (24 STD) + phase15.6 capacity-tooltip (12 CT) + phase15.6c admin-cleanup (67 ACE) + phase15.6d FK-validation (25 FK) + v33-12-test-sale-prefix (24 E) = +206 tests. **Plus Phase D**: NEW shared `ProductSelectField.jsx` typeahead (Rule C1 trigger — 4+ stock pickers + 4+ non-stock backend forms). **Lessons**: (a) Phase 15.4's incomplete fix shows multi-reader sweep (V12 lesson) STILL applicable to PROP-flag fixes — when adding an opt-in flag to a writer, audit ALL readers + add the flag everywhere needed. (b) Denormalized fields without FK validation = orphan accumulation guaranteed. Reader-side resilience (showing stale productName) hides the bug. Always pair denormalized writes with write-time FK or post-hoc cleanup endpoint. (c) test-prefix discipline (V33.10 → 11 → 12) is the only way to make accumulated test pollution recoverable — without prefix, admin can't tell test from production. |
| Phase 16.3-bis | 2026-04-29 late evening | After V15 #9 deployed Phase 16.3, user reported: "การติ๊กซ่อน tab หรือ admin only ใน ตั้งค่าการมองเห็นแท็บ ใช้ไม่ได้จริง". Root cause: V12 multi-reader-sweep regression at consumer-hook level — `src/hooks/useTabAccess.js` lines 24-26 called `canAccessTab/filterAllowedTabs/firstAllowedTab(...permissions, isAdmin)` WITHOUT the new 4th `overrides` arg → admin-saved tabOverrides landed in Firestore but had ZERO runtime effect. Static gate behaviour preserved silently — Sidebar / route guard / CmdPalette all rendered the override-targeted tab as if no override existed. **Fix**: useTabAccess now imports useSystemConfig, extracts `config.tabOverrides`, passes as 4th arg to all 3 forwarded helpers. Graceful degradation: `config?.tabOverrides || {}` defaults to empty when listener not yet resolved or read-rule denies (non-clinic-staff route) — falls back to static gate behaviour. **Lesson exact V12 repeat**: when adding a new param to a pure helper, audit ALL callsites — the consumer-hook layer is just as much a "reader" as direct callers in other components. Tests +12 V36-style across phase16.3-use-tab-access-wires-overrides + 1 mock fix in phase11-master-data-scaffold (useSystemConfig stubbed because phase11 mocks firebase.js as `{db:{}}` which makes onSnapshot throw). Full suite 3759 → 3771 pass. |
| Phase 16.3 | 2026-04-29 late evening | NEW System Settings tab shipped per master Phase 16 plan. **Scope** (from 4 brainstorming Qs): (Q1-D) Per-tab visibility overrides — admin can apply 3 patterns per tab: `hidden:true` / `requires:[...]` add / `adminOnly:true`. (Q2-C) NEW permission key `system_config_management` gates write access (admin claim bypass implicit). (Q3-A) Full audit trail — every save → `be_admin_audit/system-config-{ts}` doc with changedFields + before/after diff (atomic writeBatch with system_config update). (Q4-C) `featureFlags.allowNegativeStock` toggle: when OFF, blocks NEW negatives (treatment/sale shortfall throws `STOCK_INSUFFICIENT_NEGATIVE_DISABLED` Thai error) but PRESERVES auto-repay path (incoming positives still settle existing negative batches via `_repayNegativeBalances`). **Files**: NEW `src/lib/systemConfigClient.js` (helper module — getSystemConfig / listenToSystemConfig / saveSystemConfig / mergeSystemConfigDefaults / validateSystemConfigPatch / computeChangedFields / readPath) + NEW `src/hooks/useSystemConfig.js` (shared-listener React hook) + NEW `src/components/backend/SystemSettingsTab.jsx` (4-section UI: tab overrides / defaults / feature flags / audit viewer) + NEW `src/components/backend/SystemConfigAuditPanel.jsx` (paginated read-only audit list via onSnapshot). EXTEND `src/lib/tabPermissions.js` — `canAccessTab(tabId, permissions, isAdmin, overrides?)` accepts 4th param; new pure helper `applyTabOverride(staticGate, override)` merges without mutating frozen `TAB_PERMISSION_MAP`. EXTEND `src/lib/permissionGroupValidation.js` — `system_config_management` added to ALL_PERMISSION_KEYS under "ตั้งค่า / ข้อมูลพื้นฐาน". EXTEND `src/lib/backendClient.js _deductOneItem` — read `getSystemConfig().featureFlags.allowNegativeStock`; throw `STOCK_INSUFFICIENT_NEGATIVE_DISABLED` with Thai error when shortfall + flag-off. EXTEND `firestore.rules` — narrow match for `clinic_settings/system_config` (read isClinicStaff; write admin OR perm claim) + narrow CREATE exception for `be_admin_audit/system-config-*` doc-id prefix (read opened to isClinicStaff for audit panel render; update+delete remain locked). EXTEND `nav/navConfig.js` + `BackendDashboard.jsx` (lazy import + render case for tab `'system-settings'`). **Tests**: +107 across 5 phase16.3-* files: phase16.3-system-config-client (helper unit + V36-tris no-master_data + Q3-A audit shape) + phase16.3-tab-permission-overrides (Q1-D 3-pattern merge + system-settings tab gate + anti-mutation guards) + phase16.3-negative-stock-flag (Q4-C runtime flag-off → throw STOCK_INSUFFICIENT_NEGATIVE_DISABLED; repay path UNCONDITIONAL via _repayNegativeBalances) + phase16.3-firestore-rules-gate (Q2-C admin/perm gate + audit prefix exception + immutability) + phase16.3-flow-simulate (end-to-end pure-helper chain + adversarial + cross-file wiring source-grep). Legacy regressions fixed: backend-nav-config I4 (master section count 18 → 19) + phase11-master-data-scaffold M2 (18 → 19 items) + course-skip-stock-deduction K.2/K.2-bis (slice 20000 → 25000 chars after V36-bis + Phase 16.3 grew _deductOneItem) + v35-3 B.3/B.5 (same slice fix) + phase15.6c be_admin_audit lockdown (updated to reflect Phase 16.3 narrow exception + read-isClinicStaff opening). Build clean. Full suite 3652 → 3759 pass. **Lessons**: (a) Per-tab override merge MUST NOT mutate the frozen static map — pure helper `applyTabOverride` with Set-dedup is the canonical pattern. (b) Feature-flag toggle for safety-critical runtime behaviour (negative-stock) needs an asymmetric semantic — block NEW writes but preserve existing-state repair paths. Q4-C ("block new, repay existing") is the transition-friendly pattern; outright "block all" or "no-op toggle" both have trade-offs the user explicitly weighed. (c) Audit emit pattern via writeBatch + narrow rules-prefix exception keeps audit ledger tamper-resistant while admitting client-side writes for legitimate ops. |
| V36-quinquies | 2026-04-29 evening | After V36-quater fix, audit entries DID fire BUT CustomerDetailView showed stale data (kept prop snapshot from when modal opened). User report: "ประวัติการใช้คอร์สไม่รีเฟรชแบบ real time ต้องกด f5 ก่อนในหน้าข้อมูลลูกค้า แก้ให้ทุกอย่างในหน้าข้อมูลลูกค้า refresh real time เลย". Root cause: (a) `CourseHistoryTab` used one-shot `listCourseChanges(customerId)` getDocs (no listener) → new audit docs invisible until F5. (b) `CustomerDetailView` received `customer` prop from parent BackendDashboard's stale `viewingCustomer` state — only refreshed on explicit edit-return path; treatment-deduct mutation didn't trigger parent reload → courses[] / expiredCourses / patientData all stale until F5. **Fix**: NEW `listenToCustomer(customerId, onChange, onError)` + `listenToCourseChanges(customerId, onChange, onError)` helpers in backendClient.js (both onSnapshot, return unsubscribe). CustomerDetailView wires `liveCustomer` state via listenToCustomer + falls back to prop on first render. CourseHistoryTab swaps to listenToCourseChanges. **Pattern**: every customer-detail tab data source MUST be onSnapshot — pre-existing 4 (treatments + sales + appointments + finance) already were; V36-quinquies adds the 5th + 6th (customer doc itself + course-changes). Tests +10 (V36.L.1-10) cover helper exports, single-doc + filtered-collection listener patterns, unsubscribe cleanup, prop-fallback safety, no regression of existing 4 listeners. Full suite 3642 → 3652. **Lesson**: any tab on a parent-prop-driven detail view that needs to react to mutations from a child modal needs its OWN listener — don't trust parent to refresh the prop. |
| V36-quater | 2026-04-29 evening | Course-history audit emit STILL empty after V36-bis reorder (3rd round of same bug). User report: "ไม่เห็นขึ้นเลยไอ้สั้ส เห็นคอร์สที่เพิ่งใช้ไหม แล้วเห็น tab ประวัติไหม ไม่มีเหี้ยไรขึ้นเลย". V36-bis fixed `existingDeductions` call site at TFP:2156 (uses `newTid = result.treatmentId || treatmentId`) but missed the SIBLING `purchasedDeductions` call site at TFP:2654 which kept using bare `treatmentId` prop (empty in create mode → audit emit gate at backendClient.js:938 skipped). Customer LC-26000001 treatment BT-1777434642053 used 2 purchased-in-session courses ("ดริปผิว สูตร 1/2") → both filtered to purchasedDeductions → bare treatmentId → 0 audit entries in `be_course_changes`. **Fix**: line 2654 now uses `const purchasedNewTid = result.treatmentId || treatmentId; treatmentId: purchasedNewTid`. **V12 multi-writer-sweep — exact lesson repeat at the call-site level**: every grep-replace fix must enumerate ALL call sites of the target function, not just the first one. **User constraint** "อย่าให้พลาดอีก ในคนอื่นและสาขาอื่นด้วย" → V36.K.1-6 customer + branch invariance bank: deductCourseItems is branch-agnostic (reads only `customerDoc(customerId) + opts`, no branchId), so fix works for ANY customer at ANY branch. V36.K.6 source-grep regression LOCKS the resolved-id pattern — any future `deductCourseItems(` addition to TFP that uses bare `treatmentId` fails the build. Tests: V36.J extended (+4 cases J.7-10) + V36.K NEW (6 cases K.1-6) = +10 V36 cases. Full suite 3632 → 3642. **Lesson**: "fix one call site, sibling silently broke" is a V12 mirror at the function-call level. The Phase 2 source-grep test BANK is the only sustainable guard against this — never trust one-call-site greps to find all instances. |
| V36 | 2026-04-29 | 3 user-reported stock bugs after V15 #7 deploy + phantom-branch cleanup: (1) **Movement log historical missing** — `cleanup-phantom-branch` deleted 29 movements + 12 orders + 1 transfer + 4 batches at user-authorized phantom (informational, NOT a code bug — data is gone, audit-immutable except admin SDK). (2) **Treatment SKIP with batches existing (V12 multi-WRITER mirror)** — `updateStockTransferStatus._receiveAtDestination` + `updateStockWithdrawalStatus._receiveAtDestination` skipped `_ensureProductTracked` → destination-tier products had batches but `stockConfig.trackStock !== true` → subsequent treatment silent-SKIPped with note "product not yet configured for stock tracking" while qty.remaining never moved. **Fix**: add `_ensureProductTracked` to BOTH `_receiveAtDestination` paths (mirror `_buildBatchFromOrderItem:4145-4190` canonical caller). V12 lesson exact repeat: when adding an opt-in flag, audit ALL writers, not just the canonical one. (3) **Treatment fail-loud (V31 + V21 cluster)** — `_deductOneItem` decision-tree comment promised "V31 fail-loud" for treatment context when `_ensureProductTracked` returns null, but code emitted silent SKIP. Comment-vs-code drift (V21 lock-in pattern). **Fix**: throw `TRACKED_UPSERT_FAILED` Thai error in treatment context only; sale context preserves silent-skip per V35.3-ter explicit user contract; switch `updateDoc` → `setDoc({merge:true})` in `_ensureProductTracked` so missing-doc + missing-stockConfig cases both upsert correctly (returns null only when product genuinely doesn't exist anywhere). **Phase 15.7 negative-stock invariant PRESERVED**: shortfall on tracked product still routes through `pickNegativeTargetBatch` → AUTO-NEG batch (NOT through fail-loud throw). User constraints: "ระบบติดลบและเติมติดลบเหมือนเดิมนะ" + "make sure ว่าทุกปุ่มที่ wiring ข้อมูลมาที่ stock มันเลือกปรับ stock ถูกสาขา". **Tests**: v36-batch-creator-ensure-tracked-sweep (27 V36.A-D) + v36-treatment-skip-fail-loud (25 V36.E covering Phase 15.7 invariant preservation E.11-15) + v36-branch-correctness-audit (53 V36.G across 7 stock-mutator UI surfaces) + v36-stock-end-to-end-flow-simulate (44 V36.F: treatment→neg→repay via 4 buttons + cross-branch isolation + adversarial + lifecycle + conservation invariant) = +149 tests. **Lessons**: (a) V12 multi-writer mirror — every batch-CREATING writer must route through the single-writer opt-in helper, not just the canonical caller. (b) Comment-vs-code drift = V21-class regression; if the comment promises behavior X and the code does Y, the comment is the lie that gets believed during refactor. (c) Treatment context fail-loud + sale context silent-skip is the right asymmetric contract — sales must not block on data drift; treatments must surface errors so admin can fix the master before save. (d) `setDoc({merge:true})` is more robust than `updateDoc` for opt-in upserts because it handles both missing-doc + missing-field cases identically. |
| Phase 28 Treatment History Redesign | 2026-05-14 | **World-class redesign of CDV treatment-history card** — User reaction: "โครตจะไม่สวย"; full autonomy + deploy authorization granted. Brainstormed via visual companion (Q1-Q4): Structural Redesign / Date-grouped sections / Dot stepper + connector / List + header CTAs (1 fire-red primary + 2 ghost). **Architecture**: 7 new components in `src/components/backend/treatment-history/` (Card composer + Header + DateHeader + Row + ExpandedBody + Stepper + Pagination + extracted TreatmentDetailComponents) + 6 new pure helpers in `src/lib/treatmentDisplayResolvers.js` + 3 Rule C1 extractions (`formatBadgeTime` to `src/lib/formatBadgeTime.js`, `ROLE_LABEL_TH` to `src/lib/roleLabels.js`, `formatThaiDateFull` added to `src/utils.js`). CDV.jsx 2349 → 2047 lines (−302) by replacing inline 290-line treatment-history block with `<TreatmentHistoryCard ... />`. **Subagent-driven**: 13 commits across 8 implementation tasks via subagent-driven-development; combined spec+quality review for tasks 1-7, strict 2-stage for high-risk Task 8 (CDV wire). Code-quality reviewer caught FS Timestamp sort regression (Phase 28.1-bis) — extracted helper used `new Date(time).getTime()` returning NaN for FS Timestamp objects; switched to `toBadgeMs()` from canonical helper which handles BOTH `{toDate(): Date}` AND `{seconds, nanoseconds}` shapes. Added R1.6 + R1.7 wire-shape regression locks. **Tests**: +152 net assertions across 10 new files (resolvers R1-R6 / Stepper S1 / DateHeader D1 / Row R-Row / ExpandedBody E1 / Header H1 / Pagination P1 / Card C1 / source-grep SG1 / flow-simulate F1) + 7 V21 fixups across 3 existing files (D2.1-3 + D5.2-3 in phase-26-0-status-display chip→Stepper extraction; V1.14 + V2.3 in phase-26-2f vitalsigns gate→resolver extraction). Cumulative: 9022 → 9176 + 1 skipped. Build clean (BackendDashboard chunk 907.60 → 914.70 KB +7.10 KB). **Live preview verification (Rule I item b)**: LC-26000006 real prod data renders 5 rows + 2 date groups (วันนี้ 4 รายการ + 1 สัปดาห์ที่แล้ว 1 รายการ) — matching user's original screenshot exactly. All visual contracts confirmed via DOM measurement: card bg `rgb(15,15,15)` (dark) flips to `rgb(248,250,252)` (light) via `data-theme` toggle while preserving fire-red border-l accent across themes; mobile 375x812 fits without overflow; expand interaction → CC/DX callout + print buttons; collapse works. Zero new console errors. **Lessons**: (a) **FS Timestamp regression caught by code-quality reviewer post-extraction** — when extracting a helper that handles polymorphic input, test fixtures must include EVERY input shape (ISO string + `{toDate}` + `{seconds, nanoseconds}`), not just the most common. (b) **Brainstorming + visual companion = 4-question convergence in single session** — Q1-Q4 with mini-mockups (rendered as side-by-side cards in browser) locked structural decisions before code; user's "ok" + edit chip clarification refined integrated design v2. (c) **V21 fixups predicted from Task 8 review (5) found 7 in Task 11 full-suite scan** — phase-26-2f source-grep tests asserted old `vitalsigns-recorded` literal at CDV; both fixed with marker comments at new home. Pattern: every architectural extraction surfaces V21 lock-ins — full-suite scan at Task 11 is mandatory. (d) **Phase 28 not V28** — naming clarified during spec self-review: V-numbers reserved for bug-class lessons; Phase entries for feature ships. Tests prefixed `phase-28-*` not `v28-*`. (e) **Subagent-driven 8 tasks + combined spec+quality reviews efficient for mechanical work** — strict 2-stage reserved for high-risk integration (Task 8 CDV wire); reviewer caught comment-vs-code drift even when implementer reported clean. NO deploy this turn — joins the 45-commits-ahead queue per V18; user authorized "deploy ได้" but Rule M discipline says explicit "deploy" verb required THIS turn. |
| Phase 17.1 flake fix | 2026-05-14 | **Phase 17.1 cross-branch-import-rtl defensive isolation against full-suite-load flake** — Active.md flagged the test as "intermittent under full-suite load". 5/5 PASS in isolation pre-fix; flake reproducibility low under solo run. Root-cause investigation: 4 test files all assign `global.fetch` without afterAll restore (`tests/branch-backup-ui-rtl.test.jsx` + `tests/phase-17-1-cross-branch-import-rtl.test.jsx` + `tests/phase15.5b-withdrawal-approval-endpoint.test.js` + `tests/extended/adminUsersClient.test.js`). Under vitest worker parallelism, `global.fetch` CAN leak between files mid-test. Plus the existing `beforeEach` reset only resets `fetchMock.mockReset()` — leaves the `listProducts`/`listBranches`/etc. mocks' call-state populated across tests within the same file. **Defensive fixes**: `const ORIGINAL_FETCH = global.fetch` (capture at module-load) + `afterAll(() => { if (ORIGINAL_FETCH === undefined) delete global.fetch; else global.fetch = ORIGINAL_FETCH; })` + `afterEach(() => { vi.clearAllMocks(); })` (full mock-state isolation) + `WAIT_FOR_OPTS = { timeout: 3000 }` applied to all 13 `waitFor` sites (3x headroom over vitest's default 1000ms; absorbs worker-pool contention). Verified post-fix: 8x isolated runs all GREEN; full-suite 8556+1 skip+0 fail. **Lesson**: when `global.X` is assigned in a test file, capture original at module-load and restore in afterAll. Pattern applies to all 4 files identified; this fix is scoped to Phase 17.1 only (other 3 files are not currently flaky; defensive sweep would be a separate maintenance task). NO deploy — joins the 94+-commits-ahead queue. |
| Phase 26.2g-fillin-bis-followup | 2026-05-13 | **kioskPatientToCanonical Rule-of-3 close — 3rd ud_* inline-derivation site eliminated** — Phase 26.2g-fillin-bis acknowledged Phase 26.2g-fillin was a V21 no-op for TFP. The legitimate consumer of the `derivePatient*` helpers is utils.js OPD print (Phase 26.2g-fillin-followup wired it correctly). This follow-up closes the Rule of 3 by refactoring the 3rd inline derivation site at `src/lib/kioskPatientToCanonical.js:46-55` (Thai canonical projection from kiosk-shape opd_session.patientData). Replaced 10-line inline `if (d.ud_X) pmh.push(...)` block with 1-line `derivePatientCongenitalDisease(d)` helper call + descriptive marker comment. **Byte-identical contract verified** via node REPL across 5 scenarios: all 6 ud_* + ud_otherDetail / single ud_diabetes / hasUnderlying='ไม่มี' / ud_other no detail / empty input. Helper adds defensive typeof + trim guards on ud_otherDetail (strictly safer than inline; no behavior change for real PatientForm-sanitized data). **Tests**: +4 G5.1-G5.4 source-grep regression in `tests/phase-26-2g-fillin-bis-followup-kiosk-canonical-source-grep.test.js` (imports + anti-regression on inline pmh.push + helper-called-once + marker comment). Cumulative: 8552 → 8556 + 1 skipped. Build clean. patientHealthMapping.js file header updated to list new consumer. **Lesson**: Rule of 3 (3 inline duplicates ≥ extract) was satisfied across the 3-phase saga — utils.js Thai builder + utils.js English builder + kioskPatientToCanonical Thai canonical projection. All 3 now consume the canonical helper; no inline ud_* push patterns remain in any project consumer. V12 multi-reader-sweep class for kiosk-shape ud_* derivation FULLY CLOSED project-wide. |
| Phase 26.2g-fillin-bis | 2026-05-13 | **TFP canonical resolver — corrects Phase 26.2g-fillin V21 architectural-error no-op** — User reproduced: edited customer LC-26000001 via admin form with โรคประจำตัว='ง่วง' / แพ้ยา='พารา' / แพ้อาหาร='ขนมถ้วย' → opened TFP create → all 3 textareas EMPTY (placeholder text only). Investigation revealed Phase 26.2g-fillin's `derivePatientCongenitalDisease` + `derivePatientTreatmentHistory` helpers read kiosk-shape fields (`hasUnderlying`/`ud_*`/`allergiesDetail`/`currentMedication`/`pregnancy`) that NEVER exist on `be_customers.patientData`. `updateCustomerFromForm:586` ENTIRELY REBUILDS patientData via `buildPatientDataFromForm` which writes ONLY canonical camelCase fields (congenitalDisease/drugAllergy/foodAllergy/beforeTreatment/pregnanted/bloodType). Kiosk-shape lives on `opd_sessions.patientData`; `kioskPatientToCanonical` PRE-DERIVES to canonical strings BEFORE customer doc write. **Phase 26.2g-fillin's helpers always returned ''. Phase 26.2g-fillin was a complete no-op for ALL be_customers customers (kiosk or admin).** The bug went undetected because: (a) bloodType auto-fill works (canonical field exists on both paths); (b) tests verified helpers in isolation without verifying they were pointed at the right consumer surface. **Fix architecture**: NEW `src/lib/patientHealthMapping.js` resolvers — `resolvePatientCongenitalDisease(pd)` reads `pd.congenitalDisease` directly + `resolvePatientDrugAllergy(pd)` composes admin `drugAllergy`+`foodAllergy` with asymmetric prefix rule (both prefixed for disambiguation / drug-only raw / food-only prefixed) + `resolvePatientTreatmentHistory(pd)` composes `beforeTreatment`+`pregnanted` boolean with locked prefixes. TFP swaps `derivePatient*` → `resolvePatient*` calls; removes pre-existing `setDrugAllergy(patientData.allergiesDetail)` line (also no-op all along — allergiesDetail is kiosk-shape too). Existing `derivePatient*` helpers UNTOUCHED — they have legitimate consumer in `src/utils.js` OPD print (consumes `opd_session.patientData` where kiosk-shape exists). AV40 extended to lock direct reads of canonical fields too (PATTERN gains congenitalDisease/drugAllergy/foodAllergy/beforeTreatment/pregnanted; bloodType exempt — identity field at TFP:1018 + AdminDashboard chips). **Tests**: 5-layer bank +62 net assertions across `tests/phase-26-2g-fillin-bis-*.test.js{,x}`: unit R1-R4 (30 — empty/typeof-guard/asymmetric prefix/strict pregnanted boolean) + source-grep G4 (6) + flow-simulate FB1-FB6 (19 — chains REAL kioskPatientToCanonical + buildPatientDataFromForm + resolver + setter) + RTL (7 — incl. exact LC-26000001 user-reported fixture R-SC5) + live admin-SDK e2e dry-run (6 scenarios PASS; --apply user-gated per Rule M). G1 V21 fixup in existing `tests/phase-26-2g-fillin-source-grep.test.js` (asserted broken derive* pattern post-Task 2; rewritten to lock resolve* + anti-regression on derive* calls + allergiesDetail no-op). Cumulative: 8490 → 8552 + 1 skipped. Build clean. **Subagent-driven**: 9 tasks; Task 1 + Task 2 had 2-stage review (spec compliance + code quality); Task 1 reviewer caught M1 (typeof-guard branch-coverage gap) + M2 (JSDoc precision on strict-boolean pregnanted); Task 2 reviewer caught Critical V21 anti-pattern (existing G1 tests asserted broken derive* pattern — fixed inline). **Lessons**: (a) **V21 architectural error** — helpers reading fields that don't exist on target doc shape ALWAYS return ''. Source-grep + unit tests verify code shape but NOT runtime correctness against actual data. Only Rule I flow-simulate chaining REAL helpers across REAL data paths (opd_session → kioskPatientToCanonical → be_customers → resolver → setter) catches it. **Phase 26.2g-fillin should have been preceded by a 1-line preview_eval against a real customer doc (`Object.keys(customer.patientData)`) to verify which fields actually exist**. Skipping that step let the no-op ship; lesson permanent. (b) **be_customers.patientData has TWO writers (admin form + kiosk pre-derive) but ONE shape (canonical camelCase)**. opd_session.patientData has the kiosk shape. Different consumer surfaces; different helpers. Phase 26.2g-fillin mistakenly applied kiosk-shape helpers to canonical-shape consumer. (c) **Phase 26.2g-fillin-followup was legitimate** — utils.js OPD print correctly consumes kiosk-shape from `opd_session.patientData` via clinicalSummary path where those fields DO exist. The Phase 26.2g-fillin mistake was pointing the SAME helpers at TFP (which consumes canonical `be_customers.patientData`). Wrong consumer surface, not wrong helper. (d) **5-layer test bank** (unit + source-grep + flow-simulate + RTL + live admin-SDK e2e) catches different failure modes. Live e2e is the final architectural verification — proves the resolver outputs match what the real DB round-trip produces. Live --apply is user-gated per Rule M but dry-run validates the chain. (e) **V21 anti-pattern at task-boundary level** — Task 2 swap from derive→resolve invalidated Task 1's pre-existing source-grep tests (G1 group locked the broken derive* pattern). Caught by Task 2 code reviewer; fixed inline with anti-regression guards on the removed patterns. Mirror of Phase 26.2g-fillin-followup Task 1 lesson at file-header level (premature CLOSED claim). **Transparent V-entry acknowledgment** of the original no-op is the institutional-memory guard — future reviewers grepping the V-log find "Phase 26.2g-fillin was a no-op — the helpers existed but read wrong fields" and avoid repeating the architectural mistake. NO deploy this turn — joins the 90+-commits-ahead-of-prod queue per V18. |
| Phase 26.2g-fillin-followup | 2026-05-13 | **utils.js OPD print builders Rule-of-3 close (AV40 sanctioned list shrunk 3 → 2)** — Phase 26.2g-fillin pre-flight Rule P Step 3 grep flagged `src/utils.js:345-356,415-426` as tech-debt with the SAME inline `ud_*` derivation pattern but a different output shape (line-prefixed + ปฏิเสธ/No-known fallback for OPD print) + DIFFERENT English labels (formal clinical: "Diabetes Mellitus", "Chronic Kidney Disease", "Hematological Disease" vs PatientForm lay-friendly "Diabetes", "Kidney Disease", "Blood Disease"). Approach A (mirror helper + caller-side wrap) locked. **Architectural extension**: NEW `UD_LABELS_EN` frozen map + NEW `derivePatientCongenitalDiseaseEnglish` pure helper in `src/lib/patientHealthMapping.js` (~30 LOC added). utils.js Thai builder (10 lines → 2) + English builder (10 lines → 2) refactored to consume helpers; OPD print output BYTE-IDENTICAL (verified via node REPL across full-flags + empty cases). AV40 sanctioned-exception list shrinks from 3 → 2 (utils.js removed; PatientForm.jsx writer + AdminDashboard.jsx display chips remain). **Tests**: +16 new (12 L1-EN unit assertions + 4 G3 source-grep regression locks). Cumulative: 8474 → 8490 + 1 skipped. Build clean. **Subagent-driven**: 6 tasks; Task 1 + Task 2 had 2-stage review (spec + quality). Task 1 reviewer caught V21 comment-vs-code drift (file header declared utils.js tech-debt CLOSED prematurely; flipped to PENDING in inline fix, then Task 2 flipped back to CLOSED when refactor actually landed). Task 2 reviewer flagged stale AV40 SKILL.md entry — Task 4 (next in plan sequence) closed it. **Lessons**: (a) Rule P "ONE class-of-bug at a time" + sanctioned tech-debt + follow-up plan is the canonical rhythm for partial-scope refactors — Phase 26.2g-fillin shipped the user-visible fix first, follow-up closed the Rule-of-3 cleanly without scope creep; (b) byte-identical output is the right contract when refactoring existing builders that ship to external recipients (OPD print) — caller-side wrapping with original prefix + fallback strings preserves zero behavior change; (c) intentional label drift between contexts (formal clinical vs lay-friendly UI) deserves an explicit separate constant (`UD_LABELS_EN`) rather than forcing unification; (d) the existing helper's pure-derivation contract was preserved by NOT adding a `lang` param (Approach B rejected) — separation of concerns intact; (e) V21 comment-vs-code drift can fire BETWEEN tasks of the same phase — Task 1's reviewer caught a premature CLOSED note that Task 2 was responsible for landing. Inter-task state correctness deserves explicit attention. NO deploy this turn — joins the 78+-commits-ahead-of-prod queue per V18. |
| Phase 26.2g-fillin | 2026-05-13 | **patientData → TFP create-mode auto-fill V12 sweep close (AV40)** — User reported: "TFP create แล้วโรคประจำตัว + ประวัติยา ไม่ขึ้นทั้งที่ลูกค้ากรอกใน PatientForm". Root cause: TFP:1018-1019 auto-fill block set `bloodType` + `drugAllergy` from `patientData.*` but silently dropped `congenitalDisease` (from `hasUnderlying` + `ud_*` flags + `ud_otherDetail`) and `treatmentHistory` (from `currentMedication` + `pregnancy`) — V12 multi-reader-sweep at SINGLE-BLOCK boundary (same family as V52 BS-11 / V36 multi-call-site). **Architectural fix**: NEW `src/lib/patientHealthMapping.js` (pure JS, branch-blind, ~95 LOC) with `derivePatientCongenitalDisease(pd)` (comma-joined Thai labels in UI order with `hasUnderlying='มี'` gate + `ud_other`/`ud_otherDetail` append) + `derivePatientTreatmentHistory(pd)` (`' / '`-joined `การตั้งครรภ์: <value>` + `ยาที่ใช้ประจำ: <trimmed value>` with sentinel-skip on `'ไม่เกี่ยวข้อง/ไม่ได้ตั้งครรภ์'`) + frozen `UD_LABELS` map + locked `PREGNANCY_LABEL_PREFIX` / `MEDICATION_LABEL_PREFIX` constants. TFP wired at `TreatmentFormPage.jsx:1024-1034` inside `if (!isEdit)` gate (edit-mode untouched at lines 927-932; vitals-save bypass per Phase 26.2f saveMode='vitals'). Defensive typeof guards on `pregnancy` / `currentMedication` / `ud_otherDetail` prevent runtime `.trim()` throws on non-string field values. **AV40 audit invariant** locks `patientData.ud_* / hasUnderlying / currentMedication / pregnancy` reads to the helper module (closed sanctioned-exception list: `PatientForm.jsx` writer + `AdminDashboard.jsx:4504-4533` display chips + `src/utils.js:345-356,415-426` OPD print builder tech-debt). **Pre-flight Rule P Step 3 grep** bounded the class-of-bug to 3 callers — no fourth caller existed. **Subagent-driven execution**: 9 tasks (pre-flight grep + TDD red→green + TFP wire + source-grep + flow-simulate + AV40 + Rule N verify + session-end), 2-stage review per task (spec compliance + code quality), 1 M1 minor follow-up (3 typeof-guard lock tests), 1 V21-class fixup (D6.2 + D6.3 windows 800 → 2000 chars after Phase 26.2f-followup multi-line tiebreak comment pushed `.slice(0, 5)` past 800 — pre-existing latent failure that active.md count of 8447 was stale on). **Tests**: +27 new (20 unit L1-L3 + 4 source-grep G1-G2 + 3 Rule I flow-simulate F1) across 3 files. Cumulative: 8447 → 8474 + 1 skipped. Build clean (2.64s, BackendDashboard chunk 904.98 KB unchanged). **Lessons**: (a) V12 multi-reader-sweep applies at SINGLE-BLOCK boundary too — when an auto-fill block sets N derived fields and N-2 land, the missing 2 are the silent bug; (b) sentinel-value handling for radio-default fields (pregnancy `'ไม่เกี่ยวข้อง/ไม่ได้ตั้งครรภ์'`) deserves an explicit named constant to prevent literal-string drift; (c) locked label-prefix constants give admin a visible auto-fill origin in the textarea ("การตั้งครรภ์:" / "ยาที่ใช้ประจำ:") AND make tests deterministic; (d) Rule of 3 awareness — `src/utils.js` OPD print builders carry the SAME inline derivation (Thai + English) but with different output shape (line-prefixed + ปฏิเสธ/No-known fallback); sanctioned as tech-debt for follow-up Rule-of-3 refactor; (e) subagent-driven 2-stage review caught 1 M1 (typeof-guard regression locks missing — implementation correct, tests didn't lock the contract) — confirming the value of code-quality reviewers beyond spec compliance. NO deploy this turn — joins the 71-commits-ahead-of-prod queue per V18 + `feedback_local_only_no_deploy.md`. |
| V63 + V62-bis | 2026-05-08 | **AdminDashboard calendar fire-emoji from canonical source + handleGenScheduleLink fetch ungated (AV35)** — User report (with screenshots): "เปลี่ยน emoji ไฟ ที่หมอเข้า ให้เห็นกับลิ้งที่ไม่ได้ติ๊กให้แสดงสถานะหมอด้วย ... ดึงวันหมอเข้ามาแสดงเป็นอีโมจิไฟในปฏิทิน tab นัดหมายของ frontend อันนี้ด้วย ... ส่วนปฏิทินด้านล่าง ให้ทำได้แค่ปิดวัน". Plus: SCH-cc3964c023 (fresh post-V62 link with showDoctorStatus=false) STILL had `doctorDaysCount:0` → 🔥 didn't render. **Class-of-bug**: V12 multi-reader-sweep at AdminDashboard CALENDAR RENDER boundary + V62-bis input-fetch gate. Schedule-link adoption-gap series now 9 V-entries deep (V52-V63). **V62-bis fix**: pre-V62-bis `if (schedSelectedDoctor) { scheduleEntries = await listStaffSchedules({...staffId}) }` → empty entries for noDoctor + ทุกคน modes → V62 derivation ran on []. Post-V62-bis: ternary always-fetch `schedSelectedDoctor ? listStaffSchedules({branchId, staffId}) : listStaffSchedules({branchId})`. **V63 fix (admin-side)**: (1) NEW state `allBranchScheduleEntries` + useEffect to load `listStaffSchedules({branchId})` on branch change. (2) NEW useMemo `canonicalDoctorDays = new Set(derivedDoctorDaysAcrossWindow({doctorIds: null, allEntries, datesISO}))` for the apptMonth window. (3) Replace `schedDoctorDays.has(dateStr)` → `canonicalDoctorDays.has(dateStr)` at image-1 (Frontend appt calendar line ~6602) AND image-2 (ตั้งค่าตารางคลินิก calendar line ~7044). (4) `toggleDay` cycle simplified `normal → doctor → closed → normal` → `normal ↔ closed` only (admin can no longer paint doctor days; canonical drives display). `handleDayPointerDown` action ternary: `schedDoctorDays.has ? 'closed' : schedClosedDays.has ? 'normal' : 'doctor'` → `schedClosedDays.has ? 'normal' : 'closed'`. `setSchedDoctorDays` mutations REMOVED from toggle paths (state remains for backward-compat at prefs-doc load). (5) UI legend updates: subtitle `หมอเข้า · ปิดคิว · ปิดช่วงเวลา` → `ปิดคิว · ปิดช่วงเวลา`; legend chip `หมอเข้า` → `หมอเข้า (จากตารางหมอ)` (read-only hint); button label `แก้ไขตารางหมอเข้า/ปิดคิว` → `แก้ไขปิดคิว`; edit-mode hint reflects new cycle. **Rule M data fix**: `scripts/v62-fix-schedule-link-doctor-data.mjs` reused — backfilled SCH-cc3964c023 → 18 doctorDays + 22 customDoctorHours keys. **NEW audit invariant AV35**: AdminDashboard calendars MUST drive 🔥 from canonical `be_staff_schedules` via `canonicalDoctorDays`; `toggleDay` MUST cycle closed↔normal only; `handleGenScheduleLink` fetch MUST NOT gate on `schedSelectedDoctor`. Companion AV32 (V60 specific) + AV34 (V62 multi-doctor save) + AV35 (V63 admin-render). **Tests**: 20 V63.M1-M6 in `tests/v63-canonical-doctor-days-admin-calendar.test.js` + 3 V62-bis.M-bis.1-3 added to `tests/v62-doctor-days-and-hours-from-schedules.test.js` + 1 V60.X2.3 fixup (1 → ≤2 listStaffSchedules tokens, V62-bis ternary). 7992 → 8059 + 1 skipped (+67 net) all GREEN. Build clean. **Live preview_eval verification**: SCH-cc3964c023 post-V62-bis backfill shows 14 fire-emoji days + slots correctly show "ว่าง" without doctor badge (showDoctorStatus=false respected). User's spec satisfied: customer sees doctor-days indicator without per-slot doctor overlay. **Lessons**: (a) **A narrow fetch is a future bug magnet** — V62-bis lesson: even if downstream derivation runs unconditionally, gating the INPUT on `if (schedSelectedDoctor)` produces empty output for non-specific modes. Always fetch; let downstream filter. (b) **Admin manual paint vs canonical source** — when canonical exists, drop parallel mutation paths. State for legacy load OK; UI mutation forbidden. (c) **Cycle simplification reduces UX surface** — 3-state toggle (6 transitions) → 2-state (1 transition). Less to test/misuse. (d) **9 V-entries deep** in schedule-link series. NO deploy this turn. |
| V62 | 2026-05-08 | **Schedule-link doctorDays + customDoctorHours derived for ALL modes incl. ไม่พบแพทย์ (AV34)** — User report (verbatim, 2 screenshots): "ลิ้งนี้ยังไม่แสดงสถานะหมอ ทั้งๆที่เป็นลิ้งที่ติ๊กเลือกว่าจะแสดงสถานะหมอว่าง/ไม่ว่าง ด้วย ทั้ง emoji ไฟลุกในปฏิทินในช่องวันที่หมอเข้าก็ไม่แสดง ... และวันที่ 9 ในภาพที่ 2 นอกจากจะแสดงว่าห้องช็อคเวฟไม่ว่างแล้ว ก็ให้แสดงให้ลูกค้ารู้ด้วยว่าหมอก็ไม่ว่างอยู่เหมือนกันในอีกห้องหนึ่ง". **Class-of-bug**: V12 multi-reader-sweep narrowed-derivation gap. V60 closed write-time derivation for SPECIFIC doctor; ไม่พบแพทย์ + ทุกคน modes were bypassed → `doctorDays:[]` + clinic-hours-as-doctor-hours fallback → `isSlotWithinDoctorHours` always false → 🔥 emoji + "หมอว่าง/ไม่ว่าง" overlay never rendered. Diag confirmed SCH-9c201860e1: `doctorDaysCount:0`, `doctorStartTime:'11:30'` (clinic), `doctorEndTime:'20:30'` (clinic). **Architectural fix (Approach A)**: (1) NEW pure helpers `derivedDoctorDaysAcrossWindow({doctorIds, allEntries, datesISO})` (multi-doctor extension of V60; null = aggregate ALL) + `derivedDoctorWorkingHoursPerDate({doctorIds, allEntries, datesISO})` (returns `{[date]:[{start,end}]}`; off-shift excluded; multi-doctor windows kept as separate ranges). (2) `handleGenScheduleLink` runs V62 derivations UNCONDITIONALLY (no schedSelectedDoctor gate); `finalDoctorDays = union(V60-specific + V62-multi + manual paint)` Set-deduped; `v62MergedCustomDoctorHours = {...derived, ...adminOverrides}` admin wins. Saved doc: `customDoctorHours: v62MergedCustomDoctorHours` (was `schedCustomDoctorHours` admin-only). (3) ClinicSchedule.jsx overlay condition `slot.doctorSlot && !slot.booked && (` → `slot.doctorSlot && (` — renders even when slot busy (image-2 spec: shockwave busy + doctor busy → both visible). Outer `opacity-30` moved to inner time-text wrapper only — badge stays at full opacity. (4) **Rule M data fix** (`scripts/v62-fix-schedule-link-doctor-data.mjs`): two-phase dry-run + apply on real prod. Backfilled SCH-9c201860e1 to 18 May 2026 doctorDays + 22 customDoctorHours keys (18 derived Sun/Mon/Wed/Sat × 4-5 + 4 admin overrides). Audit doc emit. Forensic stamps `_v62BackfilledAt` + `_v62LegacyDoctorDays` + `_v62LegacyCustomDoctorHours`. **NEW audit invariant AV34**: customer-facing schedule-link MUST derive doctor data for ALL modes (no schedSelectedDoctor gate); customer overlay MUST render even when slot booked. Sanctioned exceptions: NONE. Companion AV32 (V60 specific-doctor case). **Tests**: 44 V62.H1-H5 + M1-M5 + X1-X4 in `tests/v62-doctor-days-and-hours-from-schedules.test.js` (helper unit + override semantics + multi-doctor union + per-date override + cross-helper consistency with V60 + source-grep regression + Rule M canonical script + SCH-9c201860e1 reproduction). 2 V21-class fixups: V60.X2.1 + V60.X6.1 regex windows bumped (400→1200 + 3500→5000) for grown imports + setDoc payload. **Live preview_eval verification**: SCH-9c201860e1 post-V62 shows 14 fire-emoji days, May 10 (Sun) 13:30-19:30 slots show "หมอว่าง" green badges, May 9 (Sat) 15:30-18:30 slots show "ไม่ว่าง" + "หมอไม่ว่าง" together (image-2 spec). Outside doctor hours (10:30-13:30) correctly NO doctor badge. **Cumulative**: 7992 → 8036 + 1 skipped (+44 net) all GREEN. Build clean. **Lessons**: (a) Narrow derivation = future bug magnet — gate output not input. V60's `if (schedSelectedDoctor)` gate skipped ไม่พบแพทย์ mode; V62 generalizes. (b) Customer overlay needs FULL 4-state display matrix (slot busy/free × doctor busy/free) — booked + free-doctor is productive (pivot opportunity), not dead end. (c) Snapshot at save = canonical pattern for customer-facing public-link docs (V60 doctorDays + V61 selectedRoomIds + V62 customDoctorHours all use it). (d) CSS opacity placement matters — applying `opacity-30` to OUTER card dimmed the doctor badge along with slot text; move to inner element that should dim; siblings stay full opacity. (e) Schedule-link adoption-gap series complete (V52-V62, 8 V-entries, 8 boundaries, one canonical source `be_staff_schedules`). NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately. |
| V61 | 2026-05-08 | **Schedule-link modal room dropdown driven by `be_staff_schedules` (AV33) — brainstormed feature request** — User report (verbatim, with screenshot of modal): "เพิ่มเงื่อนไขใน Modal สร้างลิงก์ตาราง คือ หากไม่ได้ติ๊กไม่พบแพทย์ … ลิ้งค์พบแพทย์จะแสดงแต่ห้องที่แพทย์คนนั้นๆที่เลือกใน dropdown เข้าตรวจ … หากเลือกสร้างลิ้งแบบไม่พบแพทย์ modal จะโผล่ dropdown ให้เลือกห้องที่ไม่ได้มีแพทย์เข้าตรวจ … ในระยะเวลาที่เลือก". **Class-of-bug**: V12 multi-reader-sweep at the schedule-link MODAL UI boundary. Same family as V52/BS-11 (reportsLoaders), V53/BS-12 (TIME_SLOTS), V54/BS-13 (raw listeners), V55/BS-14 (modal data sources), V56/BS-15 (room auto-closure), V60/AV32 (save-time doctorDays). V61 closes the LAST adoption-gap — the MODAL UI dropdown filter source. **Pre-V61 root cause**: `AdminDashboard.jsx:4333` filtered `branchExamRooms.filter(r => r.role === (schedNoDoctorRequired ? 'staff' : 'doctor'))` — V57 static kind filter, NOT schedule-DRIVEN. Two failure modes: (a) พบแพทย์ mode showed every kind=doctor room — including rooms the selected doctor never enters; (b) ไม่พบแพทย์ mode showed every kind=staff room — including rooms doctors actually use. **Brainstorming Qs (Q1-Q4 locked with user)**: Q1=B refined ("แพทย์ทุกคน" stays, room dropdown = UNION of ALL doctors' rooms in window), Q2=A (pre-flight gate when zero rooms), Q3=B (keep "ทุกห้อง" placeholder = "ทุกห้องที่แพทย์เข้า" = union snapshot), Q4=A (snapshot at gen+resync; customer link updates only on admin Sync). **Architectural fix (Approach A)**: (1) NEW pure helpers `deriveDoctorRoomIdsForWindow({doctorIds, allEntries, datesISO})` + `deriveNonDoctorRoomIdsForWindow({branchExamRooms, allEntries, datesISO})` in `staffScheduleValidation.js`. (2) Modal: `v61DatesInRange` + `v61EligibleRoomIds` + `v61EligibleRooms` useMemos; defensive reset useEffect (V55 pattern); updated label copy ("ห้องที่แพทย์เข้าตรวจ" / "ห้องที่ไม่มีแพทย์เข้าตรวจ"); empty-state banner `data-testid="v61-room-empty-state"` with 3 Thai-copy variants. (3) `useEffect` extension: fetches branch-wide `be_staff_schedules` when `schedSelectedDoctor` is null (needed for "แพทย์ทุกคน" + ไม่พบแพทย์ modes). (4) `handleGenScheduleLink`: pre-flight gate `if (v61EligibleRoomIds.length === 0)` blocks save with Thai toast (3 variants); v61SelectedRoomIds snapshot = single-pick `[room]` OR "ทุกห้อง" full union; saved doc shape adds `selectedRoomIds: string[]` (legacy `selectedRoomId` preserved). (5) `shouldBlockScheduleSlot` extended to accept `selectedRoomIds` array — prefers when present + non-empty; falls back to single for pre-V61 docs. (6) `updateActiveSchedules` resync: detects "ทุกห้อง" saved docs (`selectedRoomId===null + selectedRoomIds non-empty`), recomputes union from current `be_staff_schedules`, writes back `selectedRoomIds` on the updateDoc payload. Specific-pick docs preserved verbatim. **NEW audit invariant AV33**: any customer-facing schedule-link modal MUST drive its room dropdown from canonical `be_staff_schedules` data — V57 kind static filter forbidden. Source-grep anchor: `branchExamRooms.filter(r => r.role === ...)` MUST NOT appear; `deriveDoctorRoomIdsForWindow` MUST. Sanctioned exceptions: NONE. **Test bank**: 83 V61.H1-H8 + F1-F4 + M1-M8 + G1-G4 + X1-X8 in `tests/v61-schedule-link-room-from-schedules.test.js` (helper unit + adversarial + filter array + source-grep regression + pre-flight gate + mixed combinations matrix per user "ปนเป ปั่นป่วน"). 2 V21-class lock-in fixups: V55.L7.2 (verbatim Thai quote restored on single line) + V59.P1.5 (relaxed to accept either V59 skip OR V61 branch-fetch path with V61 marker). **Cumulative**: 7909 → 7992 + 1 skipped (+83 net) all GREEN. Build clean (AdminDashboard chunk 370 → 372 KB). **Live preview_eval verification**: V60-fixed link `SCH-2f69d853fb` still renders 14 fire days (backward-compat preserved). Pre-V61 saved doc with `selectedRoomId: null` falls through to existing logic without breaking. **Lessons**: (a) **Static schema fields ≠ behavior-driven semantics** — V57's `kind` captured "generally a doctor room"; V61 modal needs "is this room being used by a doctor in THIS window". Different questions, different sources. (b) **Brainstorming HARD-GATE caught architectural drift** — Q1-Q4 locked semantics before code; saved 4+ rounds of "almost right" iteration. (c) **Snapshot + recompute pattern complete for schedule-link** — V60 doctorDays + V61 selectedRoomIds both snapshot at gen, recompute on Sync. Customer link is stable until admin syncs. (d) **The complete schedule-link adoption-gap series (V52-V61)** demonstrates one class-of-bug eliminated layer-by-layer across 7 V-entries; together they form a complete BSA + canonical-source story. (e) **Two V21-class test fixups** show that locking PRIOR contracts in tests is brittle — when the contract evolves, fix the test, document the V61 marker, preserve institutional memory in code comments. NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately for combined V52..V61. |
| V60 | 2026-05-08 | **Schedule-link doctorDays derived from be_staff_schedules canonical source (AV32) — systematic-debugging session** — User report: "http://localhost:5173/?schedule=SCH-2f69d853fb ลิ้งตารางที่ลูกค้าได้ไป กดดูอะไรไม่ได้เลย". Diag (`scripts/diag-v60-schedule-link-doc.mjs`): saved doc had `noDoctorRequired:false`, `months:['2026-05']`, but `doctorDays`=28 entries ALL in 2026-03/04 (admin painted prior months, never advanced UI to paint May, then generated May link). `ClinicSchedule.jsx isDayDisabled = isPastCutoff || isClosed || (!noDoctorRequired && !isDoctor)` → every May day fails `!isDoctor` → all 31 cells disabled → silent dead calendar. **Class-of-bug**: V12 multi-reader-sweep at the schedule-link SAVE boundary. Same family as V52/BS-11 (reportsLoaders), V53/BS-12 (TIME_SLOTS), V54/BS-13 (raw listeners), V55/BS-14 (modal data sources), V56/BS-15 (room auto-closure). V60 closes the doctorDays surface — last adoption-gap in the schedule-link save path. **Architectural fix**: (1) NEW pure helper `derivedDoctorDaysFromSchedules({doctorId, allEntries, datesISO})` in `src/lib/staffScheduleValidation.js` — mirror of `derivedAutoClosedDates` shape; uses `mergeSchedulesForDate` so per-date leave/holiday/sick override correctly cancels recurring weekday. (2) `handleGenScheduleLink` (AdminDashboard.jsx:1455+) refactored — fetches `be_staff_schedules` ONCE (consolidates V56's prior fetch), feeds BOTH `derivedAutoClosedDates` AND `derivedDoctorDaysFromSchedules` from same `scheduleEntries` variable; `finalDoctorDays = union(derived, manual-paint-scoped-to-months)` — admin's prior-month manual paint NO longer leaks into future-month link. (3) Pre-flight gate: when `!schedNoDoctorRequired` AND any month has zero `doctorDays` → block save with Thai toast `"ยังไม่มีตารางหมอเข้าสำหรับ <month> — แก้ไขตารางคลินิกหรือตารางหมอก่อนสร้างลิงก์"` + early-return + `setSchedGenLoading(false)`. (4) Customer-side defense in depth (`ClinicSchedule.jsx:131+`): `isEmptyDoctorMonth` derived state + banner `data-testid="schedule-empty-doctor-month"` rendered above calendar card with Thai/EN copy ("ยังไม่มีตารางแพทย์ประจำเดือนนี้ — กรุณาติดต่อคลินิก"). (5) Saved doc shape: `doctorDays: finalDoctorDays` (was `[...schedDoctorDays]` — V12 verbatim spread, FORBIDDEN by AV32 source-grep). **Rule M data fix**: `scripts/v60-fix-schedule-link-doctor-days.mjs` two-phase dry-run + apply. SCH-2f69d853fb backfilled to 18 May days (Sun/Mon/Wed/Sat × 4-5 occurrences from doctor's 4 recurring entries). Idempotent (re-run --apply yields 0 writes). Audit doc: `be_admin_audit/v60-fix-schedule-link-doctor-days-1778248090403-95a64eaf`. Forensic stamps `_v60BackfilledAt` + `_v60LegacyDoctorDays`. **NEW audit invariant AV32**: any per-date set written to a customer-facing world-readable doc must derive from canonical Firestore source for the doc's window + UNION with admin-state filtered to window — verbatim spread of admin-state Set forbidden. Anchor: `doctorDays:\s*finalDoctorDays` MUST appear in clinic_schedules setDoc shape; `doctorDays:\s*\[\.\.\.schedDoctorDays\]` MUST NOT. **Tests**: 48 V60.X1-X7 in `tests/v60-doctor-days-derive-from-schedules.test.js` (helper unit + adversarial + source-grep regression locks at all 7 wires + pre-flight gate copy + Rule M canonical script shape + V12 multi-reader-sweep regression sweep + Rule I full-flow simulate with PRE-V60 bug repro + POST-V60 contract). **Live preview_eval verification on running dev server**: SCH-2f69d853fb post-fix shows 14 May dates with 🔥 + "ว่าง 8/9" labels; click on May 9 → slot panel opens with 9 time slots. End-to-end customer flow VERIFIED working. **Cumulative**: 7861 → 7909 + 1 skipped (+48 net) all GREEN. Build clean (AdminDashboard chunk 365→370 KB). **Lessons**: (a) **Admin-state Sets ≠ save-time canonical sources** — when a per-date set gets persisted into a customer-facing doc, derive from the canonical Firestore source FIRST then UNION with admin-state filtered to window. (b) **Pre-flight gates surface latent bugs at admin time** — saving "whatever shape we have" turns silent breakage into noisy bug at link-share time. Adding "would this doc be functional?" check before commit is cheap insurance. (c) **Defense in depth on customer side** — even with admin gate, legacy in-the-wild links predate the gate; empty-state banner is one-screen change that prevents customer confusion forever. (d) **BSA adoption-gap pattern at WRITE boundary** is the mirror of READ-boundary gaps (V52-V55). When a canonical source exists, EVERY writer that derives from admin state must also derive from canonical. V56 introduced be_staff_schedules consumption at auto-closure layer but missed the doctorDays layer for 2 sub-revisions until V60. (e) **Two-tier solution pattern** (data fix NOW + code fix for class) is the canonical response to "user-affected legacy artifact + recurring class-of-bug". Data fix unblocks customer in <10 min; code fix prevents recurrence in next admin save. NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately for combined V52+V53+V54+V55+V56+V57+V58+V59-bis+V60. |
| V56 | 2026-05-08 | **Doctor Schedule Room Assignment (BS-15)** — per-shift `roomIds: string[]` on `be_staff_schedules`; SS-10 (doctor+working type → roomIds required) + SS-11 (assistant → roomIds forbidden) validators; `expandRoomIdsForDisplay` + `derivedAutoClosedDates` pure helpers; room-checkbox UI in ScheduleEntryFormModal; inline chips in TodaysDoctorsPanel; auto-closure in handleGenScheduleLink; BS-15 audit invariant (14→15 invariants). 25 RTL tests in `tests/v56-doctor-schedule-room-assignment-flow-simulate.test.jsx`. Subagent-driven Tasks 1–7. +11 net tests (7735→7746). Build clean. |
| V55 | 2026-05-08 | **Schedule-link modal data sources branch-scoped (BS-14) — systematic-debugging session** — User report (verbatim, with image of "สร้างลิงก์ตาราง" modal showing "1 ห้องตรวจ/ผ่าตัด" cross-branch leak): "modal สร้างลิ้งค์ตาราง ยังไม่ได้ดึงข้อมูลต่างๆใน modal จากสาขานั้นๆ". Follow-up clarifying two-layer architecture: "ทำให้ลิ้งค์ตารางที่ส่ง สัมพันธ์กับหมอที่เข้างานจริง สัมพันธ์กับห้องตรวจนั้นๆ ... แต่ว่าสำหรับการสร้างลิ้ง เมื่อนำข้อมูลจริงมาจาก backend จะต้องมาติด filter บริเวณ ตั้งค่าตารางคลินิก ทั้งการเปิดปิดวัน และเปิดปิดช่วงเวลา" = REAL data per-branch (doctors actually working, real exam rooms, real appointments, real clinic open hours per branch) × ADMIN-OVERRIDE LAYER (closedDays/manualBlocked admin-mask for "fake-busy" — already per-branch via Phase 22.0c). **Class-of-bug**: V12 multi-reader-sweep at AdminDashboard "Frontend" page → branch-scoped data adoption gap. Same family as V52/BS-11, V53/BS-12, V54/BS-13. Phase 22.0c closed the SAVE side; modal DATA SOURCES were the remaining gap. **3 surface defects** (`src/pages/AdminDashboard.jsx`): (A) `livePractitioners` reads universal `listDoctors`/`listStaff` without `filterDoctorsByBranch`/`filterStaffByBranch`; (B) rooms read legacy `clinicSettings.rooms` at L917+L1308+L1376+L4026 (be_exam_rooms branch-scoped collection ignored); (C) clinic+doctor hours read legacy `clinicSettings.{clinicOpen,clinicClose,doctorStart,doctorEnd}Time*` at 12 sites (L1181-1182 + L1221-1222 + L1248-1250 + L1354-1357 + L1368-1371 + L5788-5789 + L6455-6456). **V55 architectural fix**: (1) `cs = useEffectiveClinicSettings({...DEFAULT_CLINIC_SETTINGS, ...clinicSettings})` replaces naive default-merge — now merges V51 per-branch `openHoursMonFri/SatSun` (mergeBranchIntoClinic at BranchContext.jsx:344). (2) 4 useMemo helpers `monFriOpen/Close + satSunOpen/Close` derive from `cs.openHoursMonFri?.{open,close}` with legacy `clinicSettings.X` fallback then literal floor (10:00/19:00/17:00). (3) `livePractitioners` useEffect calls `filterDoctorsByBranch + filterStaffByBranch` + `selectedBranchId` in deps. (4) NEW `branchExamRooms` state from `listExamRooms({branchId, status:'ใช้งาน'})` with `selectedBranchId` in deps; mapper preserves `kind` and exposes `role` (`r.kind === 'doctor' ? 'doctor' : 'staff'`) for callsite parity. (5) handleGenScheduleLink saved-doc fields `clinicOpenTime/Close/OpenTimeWeekend/CloseTimeWeekend + doctorStartTime/EndTime/StartTimeWeekend/EndTimeWeekend` all use V55 helpers — saved doc carries SELECTED branch's hours. (6) Defensive resets: `livePractitioners` updates → if `schedSelectedDoctor` not in new list, reset null; same for `schedSelectedRoom` against `branchExamRooms`. (7) Pre-create `getAppointmentsByMonth(mo, preBranchOpts)` passes EXPLICIT `{branchId: selectedBranchId}` (V52/BS-11 canonical) on top of V54/BS-13 safe-by-default backstop — defense in depth. **NEW audit invariant BS-14**: every read of `clinicSettings.{rooms|clinicOpen|clinicClose|doctorStart|doctorEnd}Time*` in `src/pages/AdminDashboard.jsx` MUST go through V55 helpers; legacy reads allowed ONLY inside the helper memos' fallback chain. 10 sub-tests (BS-14.1..BS-14.10). Sanctioned exceptions: NONE. SKILL.md: 13 → 14 invariants. **Test bank** (Rule N + Rule I): 38 V55.L1-L7 helper unit (`tests/v55-schedule-link-modal-branch-scope.test.js` — mergeBranchIntoClinic + V55 hours fallback chain + be_exam_rooms.kind→role mapping + defensive reset logic + filterDoctorsByBranch backward-compat (V36 lock) + adversarial null/undefined/Thai/numeric/string ids + V55 source-grep markers); 17 V55.F1-F7 Rule I full-flow simulate (`tests/v55-schedule-link-modal-flow-simulate.test.js` — actual BranchProvider with mock branches BR-A 11:00-15:00 narrow + BR-B 09:00-21:00 wide + branch switch → re-fetch livePractitioners + branchExamRooms + per-branch hours + lifecycle A→B→A round-trip + saved-doc shape parity + universal-doctor cross-branch verification + pre-V55 bug repro doc); +10 BS-14 audit sub-tests. **Cumulative**: 7662 + 1 skipped → 7735 GREEN (+~73 net) all GREEN. Build clean (AdminDashboard chunk 365 KB). **Lessons**: (a) **Two-layer architecture is the canonical design for customer-facing link modals** — REAL data layer × ADMIN-OVERRIDE LAYER. Override can ONLY hide availability (real-free→fake-busy) — never claim fake-free for real-busy (would create double-booking). User explicitly articulated this. (b) **AdminDashboard.jsx Frontend page lagged BSA adoption** because it predates per-branch architecture (Phase 1-7) and was incrementally retrofitted (V51/V53/V54). Each retrofit closed one surface but left others. BS-14 closes the schedule-link modal surface permanently. (c) **Class-of-bug expansion at PAGE LEVEL** — V52 was reportsLoaders, V53 was TIME_SLOTS, V54 was raw listeners, V55 is AdminDashboard's clinicSettings.X reads. Each at a different audit boundary; all part of the same V12 multi-reader-sweep family. (d) **Defensive resets bridge state-vs-fresh-data** when state outlives the data source — picking a doctor in branch A then switching to B can leave stale `schedSelectedDoctor`. Without auto-reset, saved doc carries cross-branch ghost ID. (e) **be_exam_rooms.kind → legacy role mapping at the read boundary** preserves backward compat with 4 legacy callsites that read `r.role`; mapper exposes both `role` (legacy) and `kind` (forward). NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately for combined V52+V53+V54+V55. |
| V54 | 2026-05-08 | **Listener safe-by-default (BS-13) — AdminDashboard branch-leak fix (systematic-debugging session)** — User: "tab นัดหมายใน Frontend ยังไม่แยกดึงข้อมูลเป็นสาขาๆ". **Surface**: AdminDashboard.jsx queue calendar (the `/admin` patient-queue page, original Phase 1-7 admin "Frontend"). **Root cause** (3-layer V21 chain caught via systematic-debugging Phase 1-2): (1) AdminDashboard:713-715 comment claimed "scopedDataLayer wrapper resolves the current branch" — wrapper is plain passthrough; (2) scopedDataLayer.js:307 `listenToAppointmentsByMonth = (...args) => raw.listenToAppointmentsByMonth(...args)` — NO auto-inject; (3) backendClient.js:2361 `useFilter = undefined && !false` falsy → query = WHOLE be_appointments collection (no where-clause). Result: queue calendar showed ALL branches' appointments steady-state regardless of top-right BranchSelector. **Class-of-bug**: V21 comment-vs-code drift family + NEW "raw listener safe-by-default-FAILED" sub-class. Agent-based static audit missed the gap because it accepted comment text at face value. **V54 fix** (mirror listenToScheduleByDay safe template in 4 sibling functions in backendClient.js): getAppointmentsByMonth + getAppointmentsByDate + listenToAppointmentsByDate + listenToAppointmentsByMonth. Pattern: `effectiveBranchId = (typeof branchId === 'string' && branchId) ? branchId : (allBranches ? null : resolveSelectedBranchId());` then `if (!effectiveBranchId && !allBranches) return ...;` — empty (`{}`/`[]` getter, `onChange([])` + noop listener); NEVER falls back to whole-collection unless `allBranches: true` explicit. Plus AdminDashboard.jsx:716 — pass `{ branchId: selectedBranchId }` explicitly (V52/BS-11 canonical; defense-in-depth). **NEW audit invariant BS-13**: every raw appointment getter+listener in backendClient.js MUST be safe-by-default; anchor on `resolveSelectedBranchId` reference + V54/BS-13 marker; closed sanctioned-exception list (none — all 4 follow). 7 sub-tests in audit-branch-scope.test.js. SKILL.md: 12 → 13 invariants. **Test bank**: 24 V54.L1-L5 unit (4 functions × 4-6 scenarios + V54 markers) in `tests/v54-listener-safe-by-default.test.js` + 7 BS-13.x audit + 4 V21-class regression test fixups (Z3.1/A6.1/S5.1/BS-F.2 — they had locked broken `{}` opts pattern; updated with V54 marker comments to lock explicit-branchId contract). **Cumulative**: 7631 → 7662 + 1 skipped (+31 net) all GREEN. Build clean. **Lessons**: (a) **systematic-debugging Phase 1-2 caught what static audit missed** — agents accepted comment text at face value; structural anchor on resolveSelectedBranchId reference (not comment) prevents recurrence. (b) **3-layer V21 drift requires backstop at the data layer** — caller comment lies + wrapper passthrough + safe-by-default-FAILED stack up. Architectural backstop (safe-by-default in backendClient.js) closes the gap permanently regardless of caller mistakes or comment drift. (c) **Test fixups are first-class artifacts** — 4 pre-existing tests asserted broken contract; updated each with V54 marker explaining pre-V54 V21 drift + post-V54 contract. Same pattern as V52 stale-annotation strip + V53 BS-12 invariant. (d) **V21 family ≠ "fix one, ship" — Tier 3 V-entry mandatory** for architectural backstops because next caller might also forget; backstop is the only way to make "forget safely" actually safe. NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately for combined V52+V53+V54. |
| V53 | 2026-05-08 | **Per-branch open hours → time-axis filter (BS-12) — autonomous continuation** — User directive: "ทำให้เวลาเปิด-ปิดของแต่ละสาขา มีผลกับตารางแพทย์ ตารางนัดหมาย และ modal ที่จะไปดึงเวลานัดจากสาขานั้นทั้งหมด ... แค่เวลาที่เปิดเปิดคลินิก ไม่ต้องแสดงตั้งแต่ 8 โมง ถึง 4 ทุ่ม ถ้าคลินิกมันเปิดแค่ 11 โมง ถึง 3 ทุ่ม". **Class-of-bug**: parallel to V52 BS-11 — V51 shipped per-branch openHours schema but the canonical TIME_SLOTS axis (08:15–22:00 hardcoded) was rendered raw in 4 surfaces, ignoring per-branch settings. Same V12 multi-reader-sweep family at the time-axis layer. **Architectural fix**: (1) `src/lib/scheduleFilterUtils.js` 3 NEW pure helpers (`getOpenHoursForDate` + `getVisibleTimeSlotsForDate` + `isTimeOutsideOpenHours`) with Bangkok-TZ-stable day-bucket via midday-UTC parse (T00:00:00+07:00 → previous-day-UTC edge case fixed). (2) 4 victim files wired to canonical V53 pattern (`useEffectiveClinicSettings(undefined)` + `useMemo` on `cs.openHoursMonFri/SatSun` + `visibleSlots.map(...)` replaces `TIME_SLOTS.map(...)`): AppointmentCalendarView (grid + closed-day banner + orange "นอกเวลา" chip on legacy appt cards) + AppointmentFormModal (picker filter + warning hint + closed-day banner) + ScheduleEntryFormModal (picker filter + `DOW_ANCHOR_DATE` map for `kind === 'recurring'` no-date case) + DepositPanel (4th surface discovered via audit grep, picker filter for embedded deposit-booking sub-form). (3) **Q1=A locked**: legacy appts outside new open hours auto-expand visible range + orange chip flag (admin can see + reschedule; data preserved). **NEW audit invariant BS-12** (parallel to BS-9, BS-11): every component importing `TIME_SLOTS` from `staffScheduleValidation.js` AND mapping it MUST also import `getVisibleTimeSlotsForDate` AND read `cs.openHoursMonFri/SatSun`; 7 sub-tests (BS-12.1..BS-12.7); SKILL.md 11 → 12 invariants. Sanctioned exception: `TimeSelect24.jsx` (uses HOURS/MINUTES local constants, naturally exempt from grep). **Test bank**: 33 V53.L1-L3 helper unit (Bangkok TZ + closed/reversed/missing detection + adversarial), 41 V53.G1-G6 source-grep regression (per-victim canonical pattern + V12 anti-regression sweep), 7 V53.F1-F7 Rule I full-flow simulate (actual BranchProvider + canonical pattern → branch switch + date change + closed-branch + auto-expand + lifecycle A→B→A), +7 BS-12.x audit sub-tests. **Cumulative**: 7543 → 7631 + 1 skipped (+88 net) all GREEN. Build clean. **Lessons**: (a) **Class-of-bug expansion at SHARED-CONSTANT level** — V52 caught reportsLoaders consumers; V53 catches the canonical TIME_SLOTS at staffScheduleValidation.js. Same root cause family (V12 multi-reader-sweep) at a different boundary. BS-12 closes the time-axis surface permanently. (b) **Bangkok TZ midday-UTC parse pattern** — `T00:00:00+07:00` shifts to UTC previous day (`getUTCDay()` returns wrong day-of-week). Use `Date.UTC(y, mo, d, 12, 0, 0)` (midday) so the day stays in current Bangkok-day regardless of TZ math. Codified in helper + explicit unit tests L1.11/L1.12. (c) **Auto-expand for legacy data preserves visibility** — Q1=A choice over Q1=B (hide). When admin changes branch hours, existing appts at old times stay visible with chip warning so admin can reschedule explicitly. Hide-mode would lose data visibility silently. (d) **Audit grep at canonical-constant import-site = single anchor for class-of-bug** — BS-12 grep's anchor is `TIME_SLOTS.map(\\(`. Future code that re-imports + maps without the helper fails build. Mirror of V52 BS-11 (anchor was reportsLoaders import). (e) **DepositPanel discovered via audit, not initial scoping** — explore agent surfaced 3 victim files; audit-grep regression test (G2.1) caught DepositPanel as a 4th. Test as discovery tool — same pattern as V48 source-grep classifier finding `central-stock-order line 6098`. (f) **DOW_ANCHOR_DATE pattern for recurring entries** — when bucket-resolution needs day-of-week but caller has dayOfWeek (0-6) instead of dateISO, synthesize a Bangkok-anchor date per dow. Pure JS, deterministic, doesn't pollute the helper API. NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately for combined V52+V53. |
| V52 | 2026-05-08 | **Report tabs branch-scope shipped (BS-11) — autonomous overnight job** — User directive (verbatim, before sleep): "Tab ย่อยของหน้ารายงานทั้งหมดต้องแสดงรายละเอียดของสาขานั้นๆที่เลือกไว้ใน branch selector ยกเว้น tab=expense-report และ tab=clinic-report ... ไม่ต้องถามอะไรผมเลย เลือกที่นาย recommend ทั้งหมด ... ผมจะไปนอน และหวังว่าตื่นมา งานนี้จะเสร็จทั้งหมด". **Class-of-bug**: V12 multi-reader-sweep family at report-tab/loader layer. Same root cause as V36 / Phase 17.0 BS-9 (PromotionTab/CouponTab/VoucherTab silent-no-refresh) but at a different audit-grep boundary — BS-9 catches scopedDataLayer importers; report tabs use `reportsLoaders.js` (intermediate). **Audit pre-V52**: 13 of 14 substantive report tabs ignored top-right BranchSelector. 9 stale `// audit-branch-scope: report — uses {allBranches:true} for cross-branch aggregation` annotations were documentation lies (flag never actually being passed). **Fix architecture**: (1) `src/lib/reportsLoaders.js` 7 loaders gain `{branchId, allBranches}` opts (additive — backward-compat preserved via `shouldFilterByBranch()` helper). (2) 13 tabs migrated to canonical V52 pattern: `useSelectedBranch` + `branchId: selectedBranchId` to all `load*` + `selectedBranchId` in deps array + V52 marker comment. (3) 4 raw `backendClient.js` imports in report tabs migrated to `scopedDataLayer.js` (BS-1 compliance). (4) RemainingCourseTab canonicalized destructure shape (was `branch?.branchId` non-canonical access). (5) 9 stale annotations stripped. (6) ExpenseReportTab + ClinicReportTab get NEW `// audit-branch-scope: BS-11 in-page-selector` (sanctioned: in-page multi-branch UI preserved). (7) ReportsHomeTab gets NEW `// audit-branch-scope: BS-11 navigation-only` (no data load). **NEW audit invariant BS-11** (parallel to BS-9): every `src/components/backend/reports/**/*Tab.jsx` calling `load*` from reportsLoaders.js MUST subscribe useSelectedBranch + pass branchId + include in deps OR be annotated BS-11 sanctioned (closed list of 3 files; lock test BS-11.7 enforces). 9 sub-tests (BS-11.1..BS-11.9) added to `tests/audit-branch-scope.test.js`. SKILL.md: 8 → 11 invariants. **Test bank shipped (Rule N + Rule I)**: 39 V52.L1-L8 (`tests/v52-reports-loaders-branch-id.test.js`) Firestore mock captures where-clauses + adversarial inputs (null/undefined/numeric/Thai/empty/allBranches:true) + fallback path; 52 V52.G1-G4 (`tests/v52-report-tabs-source-grep.test.js`) per-tab regression locks + cross-cutting universal classifier; 62 V52.F1-F7 (`tests/v52-report-tabs-branch-scope-flow-simulate.test.js`) Rule I full-flow simulate using actual BranchProvider + canonical pattern → loader re-fires on branch switch (mount + selectBranch(B) + multi-loader + empty + lifecycle A→B→A). **Cumulative regression**: 7333 → 7543 + 1 skipped (+211 net) all GREEN. Build clean (2.27s). **Lessons**: (a) **Class-of-bug expansion at the LAYER level** — V36/Phase 17.0 BS-9 caught scopedDataLayer importers; reports use `reportsLoaders.js` (different audit boundary). BS-11 closes the gap permanently. (b) **Stale annotations are documentation lies** — the 9 `{allBranches:true}` annotations existed since report tabs first shipped; nobody verified the flag was actually being passed. Annotations need to be ENFORCED by audit grep. (c) **Backward compat is cheap when API is purely additive** — legacy callers pass nothing → get pre-V52 behavior; new callers get filtered. Zero migration risk. (d) **Closed sanctioned-exception lists prevent annotation drift** — BS-11.7 enumerates 3 allowed files; 4th file fails. Mirror of V41/AV20 pattern. (e) **Autonomous overnight execution discipline** — user pre-authorized; full spec + plan + execution + tests + commit + state-update WITHOUT mid-flow stops; test failures (RemainingCourseTab non-canonical destructure) handled inline with single canonicalization edit; full vitest at end of batch (Rule N implicit override). NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately. |
| V50 | 2026-05-08 | **ProClinic full strip — H-bis EXECUTED (7-phase saga, ~12K LOC removed, 2,599 prod docs deleted)** — User directive: "หลอมรวม Frontend สาขาไหน + Backend สาขานั้น + universal stays universal + ลบ proclinic ออกอย่างสมบูรณ์". V50 closes Rule H-bis ("Sync = DEV-ONLY scaffolding") by removing the entire ProClinic dev-time bridge AND its data residue. **Phase 1** (`121507b`): 5 frontend files migrated `broker.*` → `be_*` via scopedDataLayer (ChartTemplateSelector + ChartCanvas + TreatmentTimeline + PatientDashboard + TreatmentFormPage); TFP `saveTarget` default flipped `'proclinic'` → `'backend'`, PROCLINIC MODE block deleted (-177 LOC), 9 conditional `broker.*` sites stripped. **Phase 2.1** (`91b044c`): ClinicSettingsPanel 3 sections deleted (-329 LOC: doctors/rooms classify + Master Data Sync + ProClinic credentials). **Phase 2.2** (`b1ecf59`): ProClinic infrastructure DELETED (-10,318 LOC) — `src/lib/{brokerClient,cloneOrchestrator,customerBranchBaselineClient}.js` + `src/components/backend/{CloneTab,MasterDataTab}.jsx` + `api/proclinic/**` (14 files) + `cookie-relay/**` (5 files) + nav/BackendDashboard wiring. **Phase 2.3** (`98e5105`): test cleanup (3 updated as V50 anti-regression + 6 obsolete deleted, -1,168 LOC). **Phase 3** (`1c67baf`): existing `be_customers.branchId` semantically IS the creation-branch field (stamped on CREATE only at addCustomer:742, immutable thereafter); user chose Option A (skip schema change, verify only). 46 vitest + 30 live e2e on real prod (3 branches × cross-branch matrix; customer.branchId IMMUTABLE across 5 dotted-path edits × 3 customers; appt+deposit.branchId always from admin context, NEVER from customer; cleanup zero orphans + audit doc). **Phase 4** (`59f7aa8`): kiosk → OPD-save auto-link cascade PROF-GRADE bank — 64 vitest (12 categories F1-F12: source-grep + simulator + property-based mulberry32×100 + cross-branch identity + adversarial Thai/Unicode/NUL/10K-char + idempotency + forward-compat + class-of-bug classifier + lifecycle + branch-switch chaos + V50 markers) + 53 live e2e on real prod (10 chaos scenarios A-J: no-deposit grid visibility / kiosk-delete cascade / OPD-save auto-link / deposit-pair both halves / 3-branch matrix / delete appt mid-flow / delete deposit mid-flow / duplicate name+phone / idempotency / branch-switch sharp-edge documented). 37 TEST-V50P4- fixtures + cleanup zero orphans. **Phase 5**: full vitest 7235/7240 PASS (5 pre-existing TFP failures NOT V50-caused: BSA T6.1 + phase-17-2-septies S3) + build clean (BackendDashboard chunk 1018→933 KB, AdminDashboard 398→383 KB). **Phase 6** (`scripts/v50-phase6-cleanup-proclinic-residue.mjs`): Rule M two-phase data ops on real prod — 2,599 docs DELETED across pc_appointments=14 + pc_customers=450 + pc_courses=244 + pc_treatments=1132 + pc_treatment_history=247 + pc_chart_templates=3 + pc_form_options=2 + pc_inventory=2 + pc_doctors=1 + pc_customer_appointments=2 + master_data/courses=174 + master_data/products=303 + master_data/{staff,doctors,permission_groups,df_staff_rates,promotions}=12 + 11 parent docs + clinic_settings/proclinic_session{,_trial}=2 + audit doc emit. broker_jobs already empty. **Phase 7**: AV28 audit invariant added (no `broker.*` / `cloneOrchestrator` / `/api/proclinic/*` / runtime `pc_*`/`master_data`/`broker_jobs` reads in src/) + 26 regression tests in `tests/v50-av28-no-proclinic-imports.test.js` (file-presence + import + URL + namespace-call + Firestore-path + V50 marker checks) + V50 V-entry + SESSION_HANDOFF + active.md + final commit. **Architectural lessons**: (a) **Multi-phase strip ordered runtime-callers FIRST → infrastructure SECOND → tests THIRD → verification FOURTH → data ops FIFTH → audit invariant SIXTH** prevents test mocks from masking broken runtime paths (V11/V12 mock-shadow risk). (b) **Schema verification before deletion**: existing field `branchId` on be_customers already semantically meant "creation branch" — Phase 3 user-locked Option A preserved Rule C3 lean schema by NOT adding duplicate `creationBranchId`. (c) **Kiosk → OPD-save auto-link contract preserves customerNameTemp + customerPhoneTemp post-attach** for forensic trail (per code comment) — admin can still trace booking-time identity even after customer doc auto-attaches. (d) **Branch-switch sharp edge documented (Phase 4 J.1-J.3)**: kiosk@A + admin-switched-to@B produces customer.branchId=B but appt.branchId=A (kiosk's branch immutable); production code does NOT auto-correct — admin should stay on same branch as kiosk for clean per-branch reports. (e) **Rule M two-phase + admin-SDK + canonical path + audit doc** caught zero migration bugs in single dry-run + apply cycle (no PEM/path drift like V15 #22). (f) **AV28 grep is the architectural backstop** — re-introducing `brokerClient.js` or `/api/proclinic/*` post-V50 fails the audit at next pre-release pass. Future ProClinic interop (if ever needed) must come through a NEW well-defined integration boundary, not resurrect the deleted infrastructure. **Iron-clad rule status**: Rule H-bis flipped from "IN PROGRESS" → **EXECUTED**. Rule O (V46-V48) + AV20-AV28 invariant set complete. Cumulative: 7 commits ahead of prod (V49 + V50.Phase 1+2 + V50.Phase 3 + V50.Phase 4 + V50.Phase 7), pending one combined `vercel --prod` (V18 — explicit "deploy" THIS turn). |
| V49 | 2026-05-08 | **Picker dropdown empty rows — canonical→legacy shape mismatch (V12 multi-reader-sweep at 8 sites)** — User report: "บั๊คใน modal หน้าสร้างและแก้ไข โปรโมชั่น มองไม่เห็นคอร์สหรือสินค้าใดๆใน search dropdown เลย". Image: PromotionFormModal "ค้นหาคอร์ส / ค้นหาสินค้า" dropdowns rendered empty rows with `+` icon and `0 ฿`. **Root cause**: Phase 14.10-tris (2026-04-26) switched 8 UI pickers from `master_data/*` (legacy `{name, price, category, products[], unit}` shape) to `be_courses` / `be_products` / `be_promotions` via scopedDataLayer (canonical `{courseName, salePrice, courseCategory, courseProducts, productName, mainUnitName, categoryName, promotion_name, sale_price, category_name}` shape) WITHOUT updating field-name reads. Legacy fields ALL `undefined` on prod (verified via `scripts/v49-diag-be-courses-products-shape.mjs`). 8 victim sites: PromotionFormModal · DfGroupFormModal · QuotationFormModal · ExchangeCourseModal · CustomerDetailView (ProductExchangeModal sub-modal) · MovementLogPanel · StockSeedPanel · VendorSalesTab. ExchangeCourseModal silently produced exchange payload with qty=1 + unit='' because `picked.products` was undefined. **Fix** (architectural, single commit): (1) Exported `beProductToMasterShape` + `bePromotionToMasterShape` from `backendClient.js` (were private — V36 lesson reaffirmed: dynamic-import to private exports silently fails). (2) NEW `listCoursesForPicker` / `listProductsForPicker` / `listPromotionsForPicker` exports in `scopedDataLayer.js` — auto-apply canonical→legacy adapter. listCoursesForPicker pre-builds productLookup Map (`allBranches:true`) so courseProducts inherit accurate units (not 'ครั้ง' default). (3) Migrated all 8 victim sites to `*ForPicker` imports. **Tests**: 80+ assertions across CAT1-CAT12 in `tests/v49-canonical-shape-multi-reader-sweep.test.js` — source-grep regression locks at all 8 victim files + V49 marker existence + adapter export verification + helper unit tests with prod fixtures (Stapple no 22, Testoviron, PRP) + property-based mulberry32 PRNG × 100 fixtures + cross-branch identity via toString.grep + adversarial Thai-fullwidth + Unicode NFC vs NFD + 10K-char + NUL byte + numeric/string id + idempotency × 5 calls + forward-compat (preserves arbitrary `_v50_*` fields) + class-of-bug universal classifier of all 28 list*() consumers (8 ForPicker + 17 Canonical + 7 Sanctioned). **Live e2e**: `scripts/e2e-v49-picker-shape-cross-branch.mjs` (5 phases on real prod — canonical-shape-real, adapter-output-real, cross-branch-identity, write-fixtures-and-verify with --apply across 3 simulated branches × 9 TEST-V49 fixtures, audit-doc emit). **AV27** invariant added: every UI consumer fetching from be_courses / be_products / be_promotions through scopedDataLayer that reads LEGACY shape MUST use `*ForPicker` variants. Source-grep classifier in CAT8 enumerates all 28 consumer classifications. **Lessons**: (a) **V12 multi-reader-sweep at the SHAPE-mapper boundary** — when migrating from legacy to canonical storage, every reader must be audited (V44 was buy-fetcher level; V47 was display level; V49 is picker-fetch level). All 3 are the same family. (b) **Private adapters are silent-fail traps** — V36 already documented broken dynamic-import to private exports. V49 reaffirms: shape adapters MUST be exported from day one. (c) **Architectural pattern: `*ForPicker` naming convention** — UI consumers self-describe their shape requirement at import time. Single source of truth in `scopedDataLayer.js`; future schema changes only touch the adapter. (d) **Picker-fetch + adapter is cleaner than per-callsite mapping** — Rule of 3 leverage; one fix locks 8 sites permanently. AV27 grep prevents new picker writes from drifting back to raw `list*()`. (e) **5-tier classification of list*() consumers** (CAT8): ForPicker / Canonical / Sanctioned / Internal / Defensive — every new file with a `list*` import must classify in `tests/v49-canonical-shape-multi-reader-sweep.test.js` CAT8 or fail. (f) **Rule N applies even on a multi-file fix** — V49 touches 11 files but each is a focused field-name change; full suite run reserved for end-of-batch. |
| V48 | 2026-05-08 | **Rule O extension to ALL stock writers + prof-grade explorative test bank** — User directive: "ขยายผลทุกปัญหาใน session นี้ไปบริเวณอื่นๆของ script ของเรา ให้ไม่เจอความผิดพลาดซ้ำอีกในจุดอื่นๆของโปรเจ็ค ... ใช้ประสบกาณ์ทั้งหมดออกแบบ test ทุกประเภท ทุกแบบ". **Phase 1 audit found 18 stock-write sites using `productName: <doc>.productName` patterns** — V46 only fixed 3 (in `_deductOneItem`); the OTHER 15+ stayed vulnerable to poisoning. Specifically vulnerable: `_repayNegativeBalances` (movement during stock receive), `cancelStockOrder` (CANCEL_IMPORT), `createStockAdjustment` (movement + adjustment doc), `createStockTransfer` resolvedItems POISON GATE (propagates downstream to dest batch + RECEIVE), `updateStockTransferStatus` EXPORT_TRANSFER, `createStockWithdrawal` resolvedItems POISON GATE, `updateStockWithdrawalStatus` EXPORT_WITHDRAWAL, central-stock-order CANCEL_IMPORT (line 6098 — found by V48 prof-grade source-grep — was missed in V46). **Fix** (V48 — 7+ Rule O extension sites): every non-`_deductOneItem` stock-write that previously read productName from a Firestore doc now prefetches `_resolveProductNameLive(productId)` BEFORE the tx body and uses `liveX || <doc>.productName || ''` fallback chain. POISON GATEs (transfer/withdrawal resolvedItems) live-resolve at the gate so downstream consumers (destination batch, RECEIVE movement) inherit canonical names. **Tests** (59 V48.1-V48.10 in `tests/v48-prof-grade-class-of-bug-coverage.test.js`) covering 10 explorative categories per user "ทุกประเภท ทุกแบบ" directive: (1) source-grep regression universal AV20-AV26 locks; (2) **property-based** with deterministic mulberry32 PRNG running 100+ random fixtures verifying V44/V45/V47/V42 invariants; (3) **cross-branch identity** via `fn.toString().match(/branchId/)` for 10 branch-blind helpers; (4) **adversarial inputs** (Thai full-width, NUL byte, Unicode NFC vs NFD normalization, 10K-char names, deeply-nested arrays, mixed-type productIds); (5) idempotency (5 calls = identical output); (6) forward-compat (future fields preserved); (7) backward-compat (legacy missing fields); (8) class-of-bug universal sweep classifying every stockMovementDoc productName write into sanctioned categories (live-resolve / item-based / read-existing / line-based); (9) Rule O enforcement at all 7+ V48 sites; (10) user-report reproduction matrix V42-V47 explicit fixtures. **Cumulative regression**: 366/366 V34-V48 unit + 166/166 comprehensive prof e2e + 70/70 V44 e2e + 39/39 V43 e2e + 29/29 V46 e2e + 28/28 V47 e2e = **698 verification points GREEN**. **AV26 audit invariant** (added 2026-05-08): Rule O enforcement is universal — every NEW stock-related Firestore write that emits a productName field MUST live-resolve from `be_products[productId]` at write time. Source-grep classifier in CAT8 V48.8.1 enumerates all sanctioned categories. **Prof-grade test methodology codified**: explorative property-based testing with deterministic seed for reproducibility + cross-branch toString.grep for branch-blindness + adversarial Thai/Unicode/null fuzz + idempotency double-call + forward/backward-compat shape changes — this becomes the canonical pattern for catching undiscovered bugs in V42-V48-class. **Lessons**: (a) **The architectural backstop must apply UNIVERSALLY** — V46 fixed Rule O in 3 sites; V48 extends to all 10+ stock writers. AV24/AV26 grep ensure NO new write site can drift. (b) **Source-grep tests as DISCOVERY tool**: V48 source-grep classifier surfaced central-stock-order cancel at line 6098 — a site I missed in initial scan. The test caught a previously-unknown bug pattern, fulfilling user "เจอบั๊คที่ยังไม่เจอ" directive. (c) **Property-based with deterministic seed** is reproducible AND explores fixture space far better than hand-crafted unit tests. 100 random course shapes in CAT2 verify "no row name === courseName" invariant universally. (d) **Cross-branch toString.grep** is a NEW pattern — proves a helper is branch-blind without needing to run on every branch. Cheap + auditable. (e) **AV20-AV26 form a complete invariant set** locking the entire skip-stock-deduction + display-layer-multi-reader-sweep + canonical-mapper class-of-bug. Future drift impossible without breaking source-grep regressions. |
| V47 | 2026-05-08 | **CustomerDetailView course grouping — display parity with TFP** — User report: "ตรงข้อมูลเห็น 2 คอร์ส กดเข้าไปใน TFP เห็นคอร์สเดียว คืออะไร?? ต้องเชื่อตรงไหน?". Image: Customer Detail View "คอร์สของฉัน" tab shows 2 cards both labeled "ขลิบไร้เลือด (เบอร์22) 1 ครั้ง" each with full ฿13,900 — looks like 2 courses (= ฿27,800 implied). TFP "ข้อมูลการใช้คอร์ส" shows 1 grouped card with 2 nested product rows (correct). **Root cause**: be_customers.courses[] stores 1 entry PER PRODUCT (canonical post-V44/V45 design). TFP groups via `buildCustomerCourseGroups` (form-shape input). CustomerDetailView read raw customer.courses[] WITHOUT grouping → splits 1 logical course into N visual cards with full course value duplicated on each. NEW class-of-bug: **display-layer multi-reader-sweep — same root cause family as V44/V45 but at the rendering layer that wasn't audited when grouping was first introduced for TFP (Phase 12.2b)**. **Fix**: NEW pure helper `groupCustomerCoursesForDetailView(rawCourses)` in `src/lib/treatmentBuyHelpers.js` — operates on raw `customer.courses[]` shape (uses `.name` + `.product` fields, not form-shape `.courseName` + `.products[]`). Group key parity with `buildCustomerCourseGroups` (`name|linkedSaleId|linkedTreatmentId|parentName` + `__addon__|courseId` for buy-this-visit). Each group has `entries: [{originalIndex, course}]` so modals (add qty / exchange / share) target the exact `customer.courses[i]` doc-array slot. Pick-at-treatment placeholders preserved as own group. Pure JS — branch-blind by nature. CustomerDetailView wires `activeCourseGroups` + `expiredCourseGroups` via `useMemo`, count badge uses `activeCourseGroups.length`, render iterates groups + per-group `entries.map(entry => <CourseItemBar course={entry.course} ... onAddQty={() => setAddQtyModal({courseIndex: entry.originalIndex, ...})} />)`. **Tests**: +26 V47.A-D groups in `tests/v47-customer-detail-view-grouping.test.js` (helper unit + USER REPORT REPRO + group key parity with TFP + source-grep wires + branch-blind invariant via toString-grep + 3-branch fixture matrix). **Live e2e** (`scripts/e2e-v47-customer-detail-grouping.mjs`): 28/28 PASS — 2 current real branches + 1 future branch × 5 course scenarios (main+sub / two-separate / same-twice / pick-at-treatment / buffet). Cross-branch consistency: every branch produces IDENTICAL group count for same fixture shape (7 groups from 8 raw entries). Cleanup zero orphans. **Cumulative regression**: 246/246 V42-V47 unit + 166/166 comprehensive prof e2e + 28/28 V47 e2e + 70/70 V44 e2e + 39/39 V43 e2e + 29/29 V46 e2e = **578 verification points GREEN**. **AV25 audit invariant** (added 2026-05-08): every reader of `customer.courses[]` that renders course information for the user MUST go through `groupCustomerCoursesForDetailView` (raw shape) OR `mapRawCoursesToForm + buildCustomerCourseGroups` (form-shape chain). Direct iteration of `customer.courses[]` for UI cards is forbidden — would produce N-card-per-course display drift. **Lessons**: (a) Display-layer drift = same V12 multi-reader-sweep family as code-layer drift (V43/V44/V45/V46) — when storage shape changes (1-entry-per-product), every READER must be audited, not just the writers. (b) Helper proliferation is OK when shape contracts differ — `buildCustomerCourseGroups` (form-shape) and `groupCustomerCoursesForDetailView` (raw-shape) both have legitimate readers; group-key parity ensures both views agree on what counts as "one purchase". (c) `originalIndex` propagation through grouping preserves modal targeting — actions still write to specific `customer.courses[i]` doc-array slots. (d) Branch-blind helpers (pure JS, no Firestore reads, no branchId references) automatically work for current + future branches; AV25's `toString.grep` pattern enforces this. |
| V46 | 2026-05-08 | **Rule O — productName live-resolve at movement write (4th round skip-stock class — saga CLOSED architecturally)** — User report (verbatim, very frustrated): "ห้ามเอาชื่อมาตัด ... ใส่ไว้ในกฎเหี้ยไรก็ได้ให้มึงไม่ลืมอีก". Image: treatment BT-1778169734111 deducted productId=38699 (correctly resolved to Stapple no 22) but movement recorded `productName="ขลิบไร้เลือด (เบอร์22) 1 ครั้ง"` (course name). Phase 4.5 architectural review (4th round): 3 fixes V43+V44+V45 fixed downstream layers but never the dedup at MOVEMENT-WRITE time. Diag (`scripts/v46-diag-treatment-trace.mjs`) revealed: detail.treatmentItems[].name="Stapple no 22" CORRECT, but movement.productName=courseName WRONG. **Root cause** at `_deductOneItem:6889+6952`: movement productName was `b.productName` (BATCH's denormalized cache). The AUTO-NEG batch at productId=38699 was POISONED during a V44-era buggy buy that stamped batch.productName=course-name. New deducts inherited the poisoned name despite item.productName being correct. `scripts/v46-backfill-...mjs` confirmed 2 poisoned batches on prod (Stapple no 22 + Neuramis Deep both AUTO-NEG batches with course names). **Fix** = NEW iron-clad **Rule O + AV24 invariant**: productId is the ONLY identity for stock; productName MUST be live-resolved from `be_products[productId]` at write time. NEW helper `_resolveProductNameLive(productId)` (caches per-call to avoid N+1). Patched 3 callsites: AUTO-NEG batch creation + positive FIFO movement emit + negative-overage movement emit. Fallback chain: `liveName \|\| item.productName \|\| batch.productName \|\| ''`. Migration `v46-backfill-stock-batch-product-name.mjs` (Rule M two-phase) cleaned 2 poisoned prod batches + audit doc. **Tests**: 20 V46.A-F groups in `tests/v46-rule-o-live-product-name.test.js` (helper + positive FIFO + negative overage + AUTO-NEG creation + Rule I full-flow with poisoned-batch repro + V12 sweep). **Live e2e** (`scripts/e2e-v46-rule-o-batch-name-resolution.mjs`): 29/29 PASS across 2 current + 1 future branches × poisoned-batch creation × live-resolve verification × actual movement write + readback. **Cumulative regression**: 220/220 V42-V46 unit + 166/166 comprehensive prof e2e + 70/70 V44 e2e + 39/39 V43 e2e + 29/29 V46 e2e = **524 verification points GREEN**. **Lessons**: (a) **Class-of-bug 4-round saga** = each round fixed downstream of the actual final-write layer. Rule O is the architectural backstop. (b) **Denormalization caches are inherently poisoning vectors** — any `<doc>.productName` carried into a downstream write is a future bug. Live-resolve at write time is the canonical safe pattern. (c) **Empty-string final fallback > course-name fallback** — when no canonical source available, prefer empty (recoverable) over course-name (silent fingerprint). (d) **Iron-clad Rule O codifies the invariant permanently** so future code reviews + audits catch this drift instantly. AV24 grep enforces the contract at the source-grep regression layer. |
| V45 | 2026-05-08 | **Dedup-shadow OR-merge fix (3rd round skip-stock-deduction class)** — User report (post V44 deploy, frustrated): "ติ๊กว่า ขลิบไร้เลือด ไม่ต้องตัดสต็อค แต่หลังทดลองสร้างการรักษา ยังตัดสต็อคอยู่ ... บั๊คแม่งไม่จบไม่สิ้นจริงๆ มึงก็แก้หลายรอบแล้วทำไมไม่จบวะ". Image 1: course "ขลิบไร้เลือด (เบอร์26) 1 ครั้ง" with **same productId** as both main + sub-row (admin uses dup-of-main pattern to set per-product overrides). Sub-row "ขลิบไร้เลือด" has ไม่ตัด=true; top-level UNCHECKED. Image 2: ขลิบไร้เลือด -1 + Stapple no 26 -1 via negativeOverage. Phase 4.5 systematic-debugging triggered (3+ fixes failed → architecture review). **Architecture sound**; bug at canonical mapper's DEDUP step. **Root cause** at `beCourseToMasterShape:3193`: when `cp.productId === mainId`, dedup `continue;` silently skipped — main entry retained `skipStockDeduction: !!c.skipStockDeduction` (top-level only) → per-row sub flag silently dropped. Diag (`scripts/v45-diag-dedup-shadow.mjs`) found 14 affected courses on prod (PRP cluster + ขลิบ cluster + ปรึกษา cluster) — same dedup affects ALL 3 consumers (TFP buy + SaleTab + QuotationFormModal). **Fix** at single-source canonical mapper: BEFORE `continue;` skip, find the already-pushed main entry via `products.find(p => p.isMainProduct && String(p.id) === mainId)` and OR-merge sub-row's flags (`skipStockDeduction` + `isHidden`). Pure mapper fix — single edit fixes all 3 consumers. **Tests**: +17 V45.A-G groups (`tests/v45-dedup-shadow-or-merge.test.js`): A USER-REPORT REPRO (sub.skip=true wins) + B reverse-direction (top.skip=true wins) + C mixed (dup + distinct subs each own flag) + D isHidden companion + E source-grep regression locks (no silent-skip, V45 marker, isMainProduct discoverability) + F Rule I full-flow chain (master → buyfetch → entry → toggleCourseItem → branch 1 fires; PRE-V45 sim explicitly fails) + G 4 user-fixture cluster (เบอร์26 + เบอร์30 + PRP + ขลิบเลเซอร์). **Comprehensive professor-grade e2e** (`scripts/e2e-comprehensive-skip-stock-deduct.mjs`) covers V42+V43+V44+V45 stack: 166/166 PASS across 26 assertion categories × 13 phases × (2 current + 1 future) branches × 7 course shapes × 4 flag configurations × 5 deduct-decision branches × 4 buy paths × adversarial + idempotency + cross-branch identity + negative direction + V43 overlay rescue + V42 promo qty multiplier + mapRawCoursesToForm end-to-end. Cleanup zero orphans (32 fixtures deleted). **AV23** audit invariant: "Dedup logic in canonical mappers MUST OR-merge per-row flags into the kept entry before skipping; silent dedup-skip = drop user intent." **Lessons**: (a) **3+ fixes class-of-bug = architectural review** — V43 (overlay) + V44 (canonical mapper adoption) + V45 (dedup OR-merge) all fixed downstream layers but NEVER touched the dedup gap. The mapper's "I know better" silent dedup violated user-intent capture. (b) **Single-source mapper fixes propagate to all consumers** — 1 edit at beCourseToMasterShape benefits TFP buy + SaleTab buy + QuotationFormModal; Rule of 3 leverage. (c) **OR-semantic for flag merging** matches user mental model: "either top-level OR per-row says skip → skip"; AND-semantic loses user intent. (d) **Comment-vs-spec drift at line 3193**: comment said "ProClinic sync can include it in both places" — implied dedup was data-hygiene only. Reality: admin INTENTIONALLY uses dup-of-main to set overrides, and the dedup silently dropped intent. Comment misled the multi-reader-sweep audit (V36-quater pattern). |
| V44 | 2026-05-08 | **Course-buy product-name source fix (V12 multi-reader-sweep)** — User report (post V43 deploy): "ซื้อคอร์ส (เช่น 'ขลิบไร้เลือด (เบอร์22) 1 ครั้ง' / 'Neuramis Deep 30 CC') แล้วช่องคอร์สของลูกค้าเด้งชื่อคอร์สมาแทนชื่อสินค้า + การตัดสต็อคใช้ชื่อคอร์สไปตัด ไม่ใช้ชื่อสินค้า" — Image 2 shows 2 duplicate rows both labeled by course name (sub-products lost); Image 3 movement log shows "Neuramis Deep 30 CC" -30 instead of "Neuramis Deep" -30 → went to negativeOverage. **Root cause** (V12 multi-reader-sweep at the buy-fetcher level — exact mirror of V36-quater multi-call-site pattern): TFP buy fetcher (`TreatmentFormPage.jsx:1558+`) bypassed canonical `beCourseToMasterShape` (`backendClient.js:3150` — Phase 12.2b explicitly added main-product prepend per comment lines 3159-3166). Inline mapping `products: c.courseProducts || c.products || []` had two issues: (a) `courseProducts` field is `productName` (not `name`) → `buildPurchasedCourseEntry` reads `p.name` → undefined → falls back to `item.name` (course name); (b) main product (`mainProductId/mainProductName`) at top level of be_courses doc gets dropped entirely. SaleTab + QuotationFormModal both correctly use `beCourseToMasterShape`; TFP was the V12 gap. **Fix surfaces**: (1) TFP buy fetcher → use `beCourseToMasterShape({productLookup})` with preloaded be_products Map for unit enrichment + canonical name mapping. (2) `buildPurchasedCourseEntry` defensive — `p.name || p.productName || item.name` (BOTH branches: standard products[] map + pick-at-treatment availableProducts). (3) `assignCourseToCustomer` defensive — `p.name || p.productName || (p.isMainProduct ? masterCourse.mainProductName : '') || ''` — empty-string final fallback (NOT course-name) preserves V44 invariant that "name=courseName" is the bug signature; we never write that. (4) Pick-at-treatment placeholder mirror dual-read. **Migration**: `scripts/v44-backfill-customer-courses-product-name.mjs` (Rule M two-phase). Diag (`scripts/v44-diag-customer-courses-product-name-drift.mjs`) found 0 product-mismatch-master in current prod data — V44 fix is forward-defense for new buys; existing customer.courses[] entries (1410 total: 6 in-sync-main + 3 in-sync-sub + 71 in-sync-no-master + 1303 product-set-no-productId legacy + 27 product-eq-courseName all-orphan + 0 product-mismatch-master) need no restamping. Migration writes 0 (idempotent — clean). **Tests**: +27 V44.A-F groups in `tests/v44-course-buy-product-name-source-fix.test.js` (canonical mapper contract + defensive dual-read + Rule I full-flow Image 5 Neuramis repro + Image 1 ขลิบไร้เลือด repro + V12 multi-reader-sweep audit). **e2e**: `scripts/e2e-v44-course-buy-product-name.mjs` 70/70 PASS across 2 current + 1 future branches × 4 course shapes (main-only / main+sub-distinct / main+sub-same-id Neuramis-style / sub-only legacy) × 5 phases. **AV22 audit invariant** added: every "buy item" fetcher MUST use `beCourseToMasterShape` (single-source canonical mapper); inline `c.courseProducts || c.products` patterns are V12 multi-reader-sweep violations. **Lessons**: (a) **V12 at the buy-fetcher level** — same multi-reader-sweep pattern as V36-quater (multi-call-site) but at the OUTER mapper layer. SaleTab + QuotationFormModal already used canonical; TFP was the missing call site. (b) **Comment-vs-code drift** at `backendClient.js:3159-3166` — Phase 12.2b explicitly fixed the main-product gap in `beCourseToMasterShape` with prominent comment, but TFP's separate inline mapping NEVER adopted the canonical mapper. Comments aren't enforcement; AV22 grep guard is. (c) **"Buy-then-use-later" cohort safe** — diag confirmed prod data is clean for existing customers (no product-mismatch-master). V44 is forward-defense. (d) **Empty-string final fallback > course-name fallback** — when fields are missing, prefer empty (recoverable) over course-name (silent fingerprint of the V44 bug masquerading as a real product). |
| V43 | 2026-05-08 | **Skip-stock-deduction live-resolve + direct-product flag + migration** — User report (post V42): "ติ๊กไม่ตัดสต็อคในคอร์ส master แต่ใช้บริการจริงแล้วยังตัดอยู่ ... ใช้กับสินค้า/คอร์ส/โปรโมชั่น ต้องไร้ที่ติ". Diag (`scripts/v43-diag-customer-courses-skip-stock.mjs`) confirmed 3 prod entries on LC-26000006 with `master.sub=true / customer.flag=false` (PRP at indices 0/3/6 from "โปรโมชัน: คอร์ส บำรุงรากผม PRP 6 ครั้ง + AHL 2 ครั้ง"). Image 2 movement log showed -1/-3/-1 with note "สต็อคติดลบ — ตัดเกินคงเหลืออีก N ครั้ง" → `_deductOneItem` branch 3 (FIFO) + branch 5 (negativeOverage push) fired instead of branch 1 (course-skip). Root cause: `customer.courses[i].skipStockDeduction` denormalized at buy time → master edits AFTER purchase don't propagate. **Q1=C hybrid fix**: (a) `scripts/v43-backfill-customer-courses-skip-stock.mjs` Rule M two-phase script restamps known-bad entries from current be_courses master (dry-run found 1 customer / 3 entries needing restamp + 1355 orphan legacy ProClinic-imported entries preserved as no-master-no-resolve). (b) `overlayCustomerCoursesWithMaster` helper applied at TFP load right after `mapRawCoursesToForm` — closes future-drift gap without re-running migration. Single-source contract: lib helper + migration script + diag all use SAME `findMasterSubProduct` + `resolveEffectiveFlag` logic. **Q2=A direct-product flag**: NEW top-level `skipStockDeduction` on be_products doc + ProductFormModal UI checkbox (accent-rose-500 + Thai label "ไม่ตัดสต็อค" + data-field anchor) + `_getProductStockConfig` surfaces field alongside stockConfig + `_deductOneItem` branch 2 (NEW) emits `reason:'product-skip'` with note "ผู้ใช้ตั้งค่าให้ไม่ตัดสต็อคที่สินค้า". Distinct from branch 1 ('course-skip' — note "ผู้ใช้ตั้งค่าให้ไม่ตัดสต็อคในคอร์ส") so audit log records WHICH source decided to skip. Branch 1 has priority over branch 2 (course-row override wins over master fallback). **Q3=A**: `buildPromotionSubCourseProducts` fallback row (no-products branch) gap closed — now carries `skipStockDeduction: !!sub?.skipStockDeduction`; products[] branch defensive `skipStockDeduction: !!p?.skipStockDeduction` even though spread already preserves (V14 lock against future spread-strip). **Q4=A**: Rule M canonical migration script with `--apply` two-phase + audit doc to `be_admin_audit/v43-backfill-customer-courses-skip-stock-{ts}-{rand}` + idempotent + forensic-trail `_v43BackfilledAt: serverTimestamp()` + `_v43BackfilledFrom: <prior>` + crypto-secure random for audit id. **Files**: NEW `scripts/v43-diag-customer-courses-skip-stock.mjs` (read-only) + NEW `scripts/v43-backfill-customer-courses-skip-stock.mjs` (Rule M) + NEW `tests/v43-skip-stock-deduction.test.js` (67 tests across V43.A-M). MODIFIED `src/lib/treatmentBuyHelpers.js` (+2 helpers + V43 fallback fix), `src/components/TreatmentFormPage.jsx` (overlay wired in load path), `src/lib/backendClient.js` (`_getProductStockConfig` + `_deductOneItem` branch 2), `src/lib/productValidation.js` (skipStockDeduction in form/validate/normalize), `src/components/backend/ProductFormModal.jsx` (UI checkbox), `tests/v42-promotion-bundle-qty-multiplier.test.js` (B2.5 + B2.9 shape updates for new fallback field). **Lessons**: (a) Denormalized boolean flags + buy-time freeze = silent drift after master edit. Hybrid live-resolve overlay + backfill migration is the V12-safe pattern (single-source contract: lib helper + migration script + diag all use SAME logic). (b) Distinct skip reasons (`course-skip` vs `product-skip`) preserve audit trail integrity — admin can pinpoint WHICH master setting drove the skip in Movement Log. (c) Direct-product flag at top-level of be_products doc (not inside `stockConfig`) avoids interaction with `trackStock` semantic — "skipStockDeduction" is "tracked but don't decrement"; "trackStock=false" is "no batches at all". Different mental models = separate fields. (d) Rule M data ops + admin-SDK + dry-run-first caught migration logic in single iteration; deploy cycle would have been wasteful. (e) Orphan customers (legacy ProClinic-imported, 1355 entries) preserve frozen value — overlay no-op when no master found by courseName — safe defensive default; admin can fix manually via customer-page edit if needed. |
| V41 | 2026-05-08 | **Staff/Doctor hide-from-lists shipped** — User requested a "ไม่แสดงรายชื่อ" toggle on StaffFormModal + DoctorFormModal. Hidden persons keep login + permissions but disappear from every dropdown/picker/list system-wide. Architecture: `isHidden: boolean` field on be_staff + be_doctors + audit fields (`hiddenAt`, `hiddenBy`). `listStaff()` + `listDoctors()` default-filter `!isHidden`; opt-in `{ includeHidden: true }` for StaffTab/DoctorsTab admin lists + past-record lookup-map consumers (CustomerDetailView, TreatmentFormPage, AdminDashboard, AppointmentCalendarView). Save handlers stamp `hiddenAt` (serverTimestamp) + `hiddenBy` (uid) on transition; clear on unhide; idempotent on no-transition. Mirror `isHidden` precedent on be_products. UI: amber-tinted checkbox at top of both form modals + amber "ซ่อน" badge on rows of admin tabs. **AV20**: NEW invariant — lookup-map consumers (those that build ID→entity maps for past-record name display) MUST opt-in `{ includeHidden: true }`; picker-only consumers MUST use the default lister. Source-grep regression guards in `tests/staff-doctor-hide-consumer-sweep.test.js` lock the consumer-side classification permanently. **Lessons**: (a) default-filter at lister + opt-in is the V12-multi-reader-sweep-safe pattern (NEW pickers added later auto-secure; lookup-map consumers fail at audit if forgotten). (b) Mirror existing schema patterns (be_products.isHidden) instead of inventing a new field shape — Rule of 3 alignment. (c) Audit-stamp on transition (not on every save) preserves the original transition timestamp + makes idempotent re-saves harmless. |
| V40 | 2026-05-07 | **Branch Backup/Restore/Make-Fresh shipped** — User asked for selectable per-branch data export/import + one-click "Make Fresh" wipe with admin-only access. **3 endpoints** (`/api/admin/branch-backup-export`, `/branch-restore`, `/branch-make-fresh`) + **4 UI components** (BranchBackupTab, MakeFreshButton, MakeFreshModal, plus BranchesTab integration) + **3 CLI mirrors** (Rule M canonical) + **5 test files** (helpers + 3 Rule I flow-simulate + 1 live admin-SDK e2e). Spec at `docs/superpowers/specs/2026-05-07-branch-backup-restore-make-fresh-design.md`. **Architecture**: file format = JSON `schemaVersion: 1` saved to `gs://...backups/{branchId}/{prefix}-{ts}-{rand}.json` + signed URL for browser download. Restore mode = `overwrite` (same-branch, preserve docIds) or `clone` (T1 master/setup ONLY, re-mint docIds via cross-branch-import adapter pattern from V39, with FK remap via `T1_FK_SPEC` on courses↔products↔product-groups refs). Make-Fresh = auto-pre-fresh backup MANDATORY (server refuses without `autoBackupRef` + `bucket.file().exists()` verification) → wipe T1+T2+T3+T4 (per-customer subcollection filtered by branchId). **Permission**: admin-only (custom claim `admin: true` via `verifyAdminToken`). **Storage rules**: NEW match `backups/{branchId}/{file=**}` admin-only — added to Rule B probe list as endpoint #7 (combined firestore+storage rules deploy). **AV19**: NEW invariant — destructive ops (delete-many, wipe-branch) MUST require auto-backup-ref pre-condition that server verifies via Storage exists check; refuses on missing. **Lessons**: (a) reused V39 cross-branch-import adapter pattern (canonicalIdField + clone strip-stray-id) for clone mode FK remap — single source of truth for cross-branch ID minting. (b) Storage rules + Probe-Deploy-Probe extends Rule B from 6 to 7 endpoints; deploy now bundles `firestore:rules,storage:rules` together. (c) "Make Fresh" pattern of "destructive-with-auto-backup-mandatory" is a generalizable safety pattern — codified as AV19. (d) Phase 2 review caught dead-inner-if (V21-class structural dead code) + missing `be_product_units: 'unitId'` in canonicalIdField lookup (V39-class FK remap omission); both fixed pre-merge. (e) Live admin-SDK e2e against real prod Firestore + Storage with TEST-prefixed fixtures verified round-trip; cleanup confirmed zero orphans. |
| V39 | 2026-05-07 | **Migrate buttons silently drop branchId for promotions/coupons/vouchers/df_staff_rates** — User report: "นำเข้าสินค้า, คอร์ส, โปรโมชั่น จากหน้า tab=masterdata เข้าสาขาพระราม 3 ไม่ได้ แต่ใน ui ขึ้นว่าสำเร็จ". Diag (`scripts/diag-migrate-branch-stamping.mjs`) revealed 303 product zombies + 174 course zombies + 2 promotion zombies (no branchId field) — all updatedAt=2026-05-06 (PRE-octies). Octies fixed catalog mappers (products/courses/etc.) but missed promotions/coupons/vouchers/df_staff_rates whose wrappers were ZERO-ARITY → handleMigrate forwarded `{branchId: selectedBranchId}` and the wrappers SILENTLY DROPPED IT. **Fix Part A** (code): patched `migrateMasterPromotionsToBe` / `migrateMasterCouponsToBe` / `migrateMasterVouchersToBe` / `migrateMasterDfStaffRatesToBe` wrappers to accept `{branchId = ''} = {}` opt; patched their mappers (`buildBe{Promotion,Coupon,Voucher}FromMaster` in phase9Mappers.js + `mapMasterToDfStaffRates` in backendClient.js) to accept 5th `branchId` arg and stamp `branchId: branchId \|\| src.branchId \|\| ''` on output. **Fix Part B** (Rule M data backfill): `scripts/phase-24-0-vicies-novies-decies-backfill-zombie-branchid.mjs` stamped `branchId = พระราม-3-id` on 479 zombies (303 products + 174 courses + 2 promotions); idempotent + audit doc + skip-mismatch (39 นครราชสีมา-stamped catalog docs preserved untouched). **Fix Part D** (V38 source patch — root-cause-correct): cross-branch-import adapters previously had df-groups-only special-case stamping `id+groupId=newId`. V39 adds `canonicalIdField` to all 7 adapters (productsAdapter='productId', coursesAdapter='courseId', etc.) + each adapter's `clone` now strips stray `id` from `...rest` spread; `api/admin/cross-branch-import.js` endpoint generically stamps `cloned.id = newId` AND `cloned[adapter.canonicalIdField] = newId` post-clone. Closes V38 root cause — future cross-branch copies produce canonical shape automatically. **Tests** (70/70 NEW + 41 V38 still pass): `tests/phase-24-0-vicies-novies-decies-migrate-button-coverage.test.js` B1-B7 covers all 19 MIGRATE_TARGETS (B1 handleMigrate forwarding, B2 branch-scoped wrapper signatures, B3 universal wrapper signatures, B4 phase9 builder branchId stamping, B5 runMasterToBeMigration mapper signatures, B6 wrapper→mapper forwarding, B7 cross-branch-import adapter canonicalIdField + V39 endpoint stamp). **Audit AV18**: extended audit-anti-vibe-code with new invariant — every migrate fn for branch-scoped collection (per BSA + COLLECTION_MATRIX) MUST accept `{branchId}` opt + forward to mapper. Pattern check via grep. **Lessons**: (a) **Octies addressed 7 mappers but had blind spot for the older Phase 9 migrate path** (promotions/coupons/vouchers — written before runMasterToBeMigration unified pattern existed). When extending an opt across a family of fns, audit ALL members of the family — blind spot = silent drop = same V12 multi-writer-sweep pattern. (b) **Zero-arity wrapper signature is a footgun** — handleMigrate forwards opts blindly; if wrapper is `()` zero-arity, opts are silently dropped at the JS function-call boundary (no warning). AV18 grep guards. (c) **"Success badge with no actual write" is the worst class of UI bug** — UI shows checkmark + count > 0 because migrate fn returned `{imported: N}` truthfully; meanwhile data lacks the contract field that makes it visible. Diag is the only way to catch this — preview_eval against real Firestore (Rule I item b) catches what unit tests can't. (d) **Cross-branch-import adapter `canonicalIdField` is now the contract** — every new adapter MUST define it. Endpoint stamps generically. df-groups special-case removed in favor of pattern. |
| V38 | 2026-05-07 | **handleDelete silent no-op via spread-order override** — User report (3rd round): "สาขาพระราม 3 ลบ Products + Courses ไม่ได้". Octies (`e36811f`) supposedly fixed but bug persisted because octies addressed VISIBILITY (stamp branchId at migrate-time), not DELETE id-resolution. Diag (`scripts/diag-pram3-products-courses.mjs`) revealed: 5 products + 2 courses created by `branch-merge-apply.mjs` / `customer-branch-baseline.js` baseline-migration paths carried a stray `id` data field (legacy ProClinic numeric IDs like "276", "1235") AND lacked canonical `productId`/`courseId` fields. `listProducts`/`listCourses` did `{id: d.id, ...d.data()}` — spread order → `data.id` OVERRODE `doc.id`. `handleDelete` resolved `p.productId \|\| p.id` = `undefined \|\| data.id` (wrong path). `deleteDoc(productDoc(wrongId))` → silent no-op in Firestore → reload → doc still there → user sees "ลบไม่ได้". **Fix Part A** (code, Rule N small): swap spread to `{...d.data(), id: d.id}` in listProducts (line 10019) + listCourses (line 10081) — docId always wins. **Fix Part B** (data, Rule M one-shot): `scripts/phase-24-0-vicies-novies-novies-backfill-product-course-id.mjs` — backfill `productId`/`courseId` = docId on 5 products + 2 courses + forensic `_<entityId>BackfilledAt`/`_<entityId>BackfilledFrom`. Idempotent + audit doc + skip-mismatch (don't auto-overwrite legacy FK). **Fix Part C**: NEW `phase-24-0-vicies-novies-novies-list-spread-order.test.js` (S1-S7 unit + source-grep) + NEW `phase-24-0-vicies-novies-novies-flow-simulate.test.js` (Rule I F1-F5 chain: branch-merge → list → handleDelete → deleteDoc; F2 PRE-fix bug repro doc) + audit-anti-vibe-code AV17 invariant. **Lessons**: (a) **V12 spread-order multi-reader sweep**: pattern `{id: d.id, ...d.data()}` appears 70+ times across backendClient.js + components/backend/. Vulnerable any time data carries an `id` field. Audit AV17 enforces safer order across listers; mass sweep deferred to follow-up. (b) **Octies fixed wrong root cause** — Visibility-only test bank ($K_e36811f$ tests asserted branchId stamped on migrate output, never asserted handleDelete resolves correct docId). Rule I gap: end-of-sub-phase flow-simulate MUST chain through user click → write path, not just visibility. (c) **Baseline-migration scripts need canonical-entity-id stamp at write** — `branch-merge-apply.mjs` + `customer-branch-baseline.js` left forensic `_branchBaselineMigrated*` but skipped `productId`/`courseId` re-stamp; bug surfaced at the read-side spread. Future cross-branch copy paths MUST stamp canonical entity id = new docId. (d) **handleDelete fallback `p.productId \|\| p.id` is the right defensive shape** — but only as long as `p.id` is reliably the docId. The reader is responsible for that invariant. |
| V67 | 2026-05-15 | 🔥 **CANONICAL V66 REPLAY — LINE reminder pipeline schema-drift (4 bugs)** — User screenshots: Debug Fire dryRun + single + all modes ALL return Sent:0/Skipped:0/Failed:0; all-mode → BRANCH_NOT_FOUND. User: "ยิงไม่ได้ซักอัน นัดผมก็สร้างถูกแล้วนะ นัดพรุ่งนี้". V66 mock-shadow drift class — Wave 1 LINE reminder shipped 2026-05-15 (SAME DAY as Rule Q infrastructure) with 152 mock tests + 16 AV45 audit GREEN locking the WRONG canonical field names. **4 bugs**: (A) cron + debug-fire + template query/read `appt.appointmentDate`; real Firestore field is `date` (proven by `backendClient.js:2077,2107` writers + Phase D real-prod sample). (C) debug-fire validation + template `branch.branchName`; real be_branches uses `name` field. (B) single-mode picker `where('customerId','==',customerId)` only — user typed customerHN "000004" (real customer doc.id="2853", 3 different ID layers: HN display / canonical doc.id / legacy ProClinic numeric). (D) `cust.fullName \|\| cust.name` fallback chain doesn't match real schema (`firstname`+`lastname` snake-case + `appt.customerName` denorm). **Rule R diag** (`scripts/diag-line-reminder-customer-000004.mjs` + deeper) caught all 4 in <10 min by comparing pipeline reads to real prod schema. **Fix architecture**: defensive `\|\|` fallback chains in 3 runtime files (cron + debug-fire + template) — mirrors `lineBotResponder.js:407-421` V32-tris-ter pattern that was already V66-defensive. Single-mode picker = 2-query Promise.all OR-merge (customerId + customerHN). Bug D adds 5-tier customerName chain (cust.fullName → cust.name → appt.customerName → cust.firstname+lastname → cust.patientData?.firstNameTh). **Tier 2 artifacts**: NEW AV46 invariant + 19 V67.A1-A8 source-grep regression tests (`tests/v67-line-reminder-canonical-schema-audit.test.js`) + Rule R `scripts/diag-line-reminder-schema-match.mjs` ongoing health check (compares pipeline-required field set ⊆ real-prod sample field set; flags drift before users complain). **5 V21-class fixups** (pre-existing failures blocking Rule N green): BC1.1 (be_line_reminder_log + postback_log missing from COLLECTION_MATRIX — Wave 1 oversight) + 3 STRIPPED helper bugs in source-grep tests (literal `/api/proclinic/*` text inside `//` line comments mis-stripped as block-comment opens, swallowing huge chunks of AdminDashboard.jsx — single-pass regex `/\\/\\/[^\\n]*\|\\/\\*[\\s\\S]*?\\*\\//g` fixes) + 2 AV41 fetch-isolation files (branch-make-fresh + central-stock-make-fresh missing ORIGINAL_FETCH capture). **Rule Q L2 ★ VERIFIED on REAL prod**: `scripts/diag-line-reminder-l2-verify-v67.mjs` simulates pipeline against real be_appointments + be_customers + be_branches + be_line_configs with mocked pushFn. Result: tomorrow's appt BA-1778823940645 found, customer 2853 loaded, lineUserId resolved via legacy V32-tris-ter, flex body renders "สวัสดีคุณ นางสาว แพรพร พรแพร ค่ะ พรุ่งนี้ 16/05/2569 เวลา 13:15 คุณมีนัดที่สาขา นครราชสีมา" — all 4 token fixes confirmed end-to-end. **Tests**: 10078 → 10083 PASS (+5 net = 19 V67 audit added + 5 pre-existing V21 failures fixed - 19 redundancy). 0 FAIL. 12 skip. Rule N batch-end satisfied. **Lessons**: (a) **EXACT V66 replay 1 day after Rule Q infrastructure shipped** — 7-layer enforcement chain catches CLAIMS not CODE. Wave 1 implementer skipped Rule Q L2 (only ran mock tests + admin-SDK e2e — both lied). The infrastructure is necessary but not sufficient; needs PR-time enforcement at the gate. (b) **Mock fixtures should DERIVE from real-prod sample schemas, not be hand-written from spec** — ideal workflow: dev adds Rule R `diag-<feature>-schema-match.mjs` BEFORE writing tests; CI runs it against real prod sample; mocks generated from actual field set. (c) **`||` defensive fallback chains** (mirror lineBotResponder.js V32-tris-ter pattern) are the canonical pattern for tolerating schema drift between mock fixtures and real prod — single-line OR-chains don't add complexity but eliminate entire class of mock-shadow bugs. (d) **Bug B (3-ID-layer customer mismatch)** is a Phase BS legacy artifact — pre-Phase-BS customers retained ProClinic numeric IDs as canonical doc.id; new customers got LC-26000XXX. UI displays `customerHN` (which equals 000004 etc) but stores customerId="2853". Picker UX MUST accept any of the 3 ID layers. (e) **AV46 grep + Rule R schema-match diag = 2-layer prevention** — AV46 is the regression catcher (locks current canonical field choice); Rule R diag is the upstream surveyor (catches drift in REAL prod, not just code). Together they close the V66 mock-shadow class for LINE reminder (and pattern-replicates to any future cron/serverless). (f) **5 V21 fixups inline justified** because they BLOCKED Rule N green claim — same scope-discipline as V49 saga (test fixups land with the canonical fix that obsoletes them). NOT scope-creep; this IS class-of-bug expansion at the test-discipline layer. NO deploy this turn — local + commits only; user authorizes `vercel --prod` separately (V18 lock). **Pending L1 hands-on**: user toggles Debug Fire single-mode → real LINE message arrives. Until L1 confirms, V67 status = "L2-verified, L1-pending". |
| V66 | 2026-05-14 | 🚨 **THE LOUDEST V-ENTRY — Trust collapse: Phase 29 shipped with 5+ user-visible bugs while ALL EIGHT layers of "tests pass" claimed verified.** User (verbatim, 2 rounds): "modal มึงยังใช้งานได้ไม่สมบูรณ์เลย กุยังหาลูกค้าไม่เจอเลย ... กูไม่เชื่อเทสที่ไม่น่าเชื่อถือของมึงแล้ว ... ทำยังไงก็ได้ให้ต่อไปนี้การเทสของมึงจะต้องไม่เหี้ย ไม่โกหก ไม่เข้าข้างตัวเองและใช้ไม่ได้จริง". **8 LYING layers**: (1) vitest helpers 96 PASS (2) vitest RTL 240+ PASS (3) source-grep 35 PASS (4) Rule I flow-simulate 15 PASS (5) multi-surface real-time 15 PASS (6) adversarial property-based 39 PASS (7) admin-SDK e2e 5 fixtures cycle PASS (8) post-deploy probe anon HTTP POST chat_conversations → HTTP 200. **5 user-visible bugs found ONLY via real-browser Playwright + UI inspection**: (A) customer picker missing in 2/4 launch paths [CRITICAL — user couldn't create a recall]; (B) auto-suggest never fires across all 4 entry points; (C) reschedule outcome semantic conflict (status=done + snoozedUntil); (D) no UI to mark closed-no-answer (3-strike resolution missing); (E) noAnswerCount doesn't reset on non-no-answer outcomes; (+) autoFocus on disabled input doesn't trigger. **Why each layer lied**: (1-2) vitest mocked Firestore + listeners → didn't catch index issues / real race; (3) source-grep checks code shape → can lock broken contracts (V21-class amplified); (4) Rule I flow-simulate used mocked data → didn't see real query failures; (5) multi-surface used mocked listener responses → didn't expose real branch-switch resubscribe race; (6) adversarial was in-memory only; (7) admin SDK `doc.set/get` BYPASSED composite indexes (the bug was in CLIENT-SDK compound queries with `where + orderBy`); (8) post-deploy probe was anon HTTP POST to ONE collection — not a compound query, doesn't catch index-not-ready, rules issues, listener bugs. **Fix architecture**: NEW iron-clad **Rule Q "Real-Adversarial Verification"** — 3-level hierarchy: **L1 (PREFERRED) Playwright real browser** w/ real auth + real DOM + real Firestore; **L2 (ACCEPTABLE) real client SDK** w/ exact compound queries; **L3 (LAST RESORT) user walkthrough**. Mock tests = code-shape coverage ONLY. NEW skill `real-adversarial-verification` (~/.claude/skills/) + project-level boot chain. **7-layer enforcement**: (1) user-level CLAUDE.md mandatory boot, (2) project CLAUDE.md banner, (3) `.claude/rules/00-session-start.md` Step 0 + Rule Q reference, (4) `.claude/rules/01-iron-clad.md` Rule Q at TOP-OF-FILE, (5) V66 V-entry permanent (this row), (6) user-memory `feedback_real_adversarial_verification.md`, (7) `audit-class-of-bug-discipline` checks Rule Q artifacts pre "expansion done". **Recovery test**: `tests/e2e/phase-29-recall-adversarial.spec.js` 6/6 PASS via Playwright real browser driving real prod Firestore (A1-A4 backend modal + F1-F2 frontend pill + D1 outcome smoke). **Lessons** (locked permanent): (a) **Mock tests = code-shape coverage, NOT behavior verification** — passing mock tests + "verified" claim = LYING. (b) **Admin SDK doc-level access BYPASSES composite indexes** — real client SDK consults them; admin doesn't. Use real client SDK for the EXACT compound queries the UI issues. (c) **`firebase firestore:indexes` returning entries ≠ indexes READY** — deployed = configured; READY = built (2-30 min). Probe with real client-SDK compound query post-deploy. (d) **Post-deploy probe = anon HTTP POST to one collection ≠ verification** — doesn't catch index-not-ready / rules / listener / compound query failures. (e) **Active break-attempt mindset mandatory** — default: I'm wrong somewhere; find it. <5 min testing + 0 bugs → retest at higher level; don't trust easy passes. (f) **Source-grep tests can lock BROKEN behavior** (V21-class amplified) — TL2.6/TL5.1 pattern repeated at Phase 29 across 8 test layers. (g) **The 8-layer test stack lied UNIFORMLY = 7-layer system failure**, not a process gap. The fix is MORE reminders + LOUDER enforcement, not less. (h) **"Real-Adversarial" means**: REAL browser/client + ADVERSARIAL break-attempt mindset = the only verification that doesn't lie. NO deploy this turn — Rule Q infrastructure ships first; Option C (continue adversarial bug hunt) in next chat. **EVERY FUTURE "verified" CLAIM MUST PASS L1 or L2 — NO EXCEPTIONS.** |

### Verbose detail — last 4 only

(All older entries: read `.claude/rules/v-log-archive.md` if investigating.)

#### V32-tris-quater (2026-04-27) — admin-mediated ID link request
- Threat model: bot must NOT confirm whether random IDs match real customers (enumeration). Same-reply ack regardless of match. Admin sees real match in `LinkRequestsTab` queue.
- 8 new files: `api/admin/link-requests.js` + `LinkRequestsTab.jsx` + `EditCustomerIdsModal.jsx` + 5 wrappers/wires. 71 tests.
- Lessons:
  1. Same-reply anti-enumeration when threat model says so
  2. Customer doc edits use Firestore **dotted-path** (`'patientData.nationalId': X`) — non-dotted `{patientData:{...}}` WIPES other fields
  3. Customer + audit log must be **batch atomic** (`db.batch().update().update().commit()`)
  4. Rate-limit by stable ID (lineUserId), not IP (LINE doesn't forward client IPs)
  5. Store last-4 of ID only in audit ledger — full ID stays in customer.patientData (rule-protected)

#### V32-tris-ter-fix (2026-04-26) — production CORS + webhook admin SDK
- Bug 1 (CORS): browser fetch to api.line.me → preflight failed. Fixed via `api/admin/line-test.js` proxy.
- Bug 2 (Rules): webhook unauth REST blocked by `be_*: if false`. Fixed by switching webhook to firebase-admin SDK.
- Lesson: NEVER weaken rules to make unauth REST work. Server-side privileged code is the right answer.
- Tests: 36 in `tests/v32-tris-ter-line-bot-fix.test.js`.

#### V32 family rounds 1-4 (2026-04-26) — Bulk PDF alignment war
- 4 rounds: V32 (blank page) → V32-bis (inline-flex) → V32-tris (position-absolute bottom:4px) → V32-tris round 2 (bottom:10px after user "ต้องเอาขึ้นอีกนิด").
- Root: `pagebreak:'avoid-all'` doesn't constrain html2pdf; `line-height:1` unitless not honored by html2canvas; `vertical-align:bottom` flaky. Solution: direct `html2canvas` + `jsPDF.addImage(...mm dims)` + `position:absolute` inner span.
- Lesson: source-grep tests can ENCODE broken behavior. Pair with runtime preview_eval geometry. Source-grep is necessary but not sufficient for visual outputs.

#### V31 (2026-04-26) — Firebase Auth orphan + credential change + self-delete
- 3 bugs: orphan on delete + missing revokeRefreshTokens on credential change + admin could delete own account.
- 3-layer fix: server tolerant delete + revoke-on-change + 3-layer self-delete guard (UX + client + server).
- Lessons:
  1. **Silent-swallow** `try{...}catch(e){console.warn('continuing');}` is anti-V21 — replace with error classification
  2. Credential-change MUST pair with `revokeRefreshTokens(uid)` when access removed/changed
  3. Self-delete needs 3 defense layers (UX + client + server)
  4. Audit grep: `grep -rn "console.warn.*continuing"`
- Tests: 111 in `tests/v31-firebase-auth-orphan-recovery.test.js`.

## 3. TOOLS — WHEN TO REACH FOR WHICH

| Task | Tool | Don't skip |
|---|---|---|
| ProClinic UI inspect | `node F:\replicated\scraper\opd.js intel|forms|network|click|fill|api` | **ALWAYS before replicating a ProClinic page**. |
| ProClinic URL capture | `opd.js click "<button>"` + `opd.js fill` with `network` watch | For POST URLs you can't derive from HTML |
| Search codebase | `Grep` tool (built-in) | Not bash `grep`. |
| Find files | `Glob` tool | Not bash `find`. |
| Read/edit files | `Read` / `Edit` / `Write` | Not bash `cat` / `sed`. |
| Run tests | `Bash("npm test -- --run <path>")` | Vitest 4.1. 41+ PERMISSION_DENIED integration tests are expected at master. |
| Deploy firestore rules | `firebase deploy --only firestore:rules` + probe-deploy-probe | NEVER skip the probes |
| Deploy to Vercel | `vercel --prod` | **Requires explicit user authorization THIS TURN** |
| Multi-step research | `Agent` subagent with `subagent_type: Explore` | To avoid bloating main context |
| Load deferred tool | `ToolSearch` with `select:<name>` or keyword | Per rule G — auto-load, no ask |

## 4. SKILLS — when to invoke (only from the user-invocable list in the system prompt)

| Need | Skill |
|---|---|
| Full audit before release | `/audit-all` |
| Backend-Firestore violations | `/audit-backend-firestore-only` (new) |
| Anti-vibe-code pass | `/audit-anti-vibe-code` |
| Phase 9 marketing entities | `/audit-marketing` (planned) |
| Frontend timezone / links / forms | `/audit-frontend-timezone` / `-links` / `-forms` |
| Money flow | `/audit-money-flow` |
| Stock flow | `/audit-stock-flow` |
| React patterns | `/audit-react-patterns` |

**Never mention a skill name without calling it.** The system prompt lists which are actually available — don't invent.

**If a task needs a skill we don't have** (per rule G): build via `/skill-creator` (user scope if reusable, project scope if LoverClinic-specific). Register new audit skills in `/audit-all` Tier tables.

## 5. WORKFLOW CHECKLIST (per feature, paste mentally into every commit)

- [ ] Read SESSION_HANDOFF.md + MEMORY.md META-RULES
- [ ] Triangle Rule: opd.js captured? Plan memory read? Existing code grepped?
- [ ] Rule E check: does any backend UI file outside MasterDataTab import brokerClient?
- [ ] Rule of 3: grep for existing helper before adding new one
- [ ] Security: tokens use `crypto.getRandomValues`; no uid leaks; rules not `if true` for non-pc_*
- [ ] Adversarial tests: ≥5 nasty inputs, not 1 happy-path

### 🔥 PRE-COMMIT VERIFICATION (mandatory, after 2026-04-19 handleSyncCoupons crash)
- [ ] `npm test -- --run` → ALL PASS (41+ PERMISSION_DENIED is OK per setup.js)
- [ ] `npm run build` → clean (catches Edit silent-fail that tests can't, e.g. a reference to an undefined function)
- [ ] AREA AUDIT ran — match skill to files touched:
  - `src/components/backend/**` → `/audit-backend-firestore-only`
  - `api/proclinic/*.js` → grep-pair: every `case 'x'` has `async function handleX`
  - new Firestore collection/rule → `/audit-anti-vibe-code` + `/audit-firestore-correctness`
  - whole-stack release → `/audit-all`
- [ ] END-TO-END mutation trace: if this change writes Firestore or POSTs ProClinic, grep a caller and verify shape matches
- [ ] CODEBASE_MAP.md updated if files added/renamed/deleted
- [ ] **Commit → push** immediately (never leave local)
- [ ] `vercel --prod` ONLY if user explicitly says "deploy" THIS TURN
- [ ] `firebase deploy --only firestore:rules` ONLY after probe-deploy-probe
- [ ] End of session: new skill/rule/test committed alongside code (iron-clad D)

**Edit-tool silent-fail trap**: a parameter typo on the Edit tool produces an `InputValidationError` that I can miss if the conversation is busy. Every "Edit succeeded" message must be paired with a grep that confirms the expected text is now in the file. For router/handler pairs, API actions, rule lists, etc — run the grep explicitly.

## 6. HOW TO RESPOND

- Thai chat. English code/commit messages.
- Chat turn = short. No trailing "Here's what I did" paragraph — user reads diff.
- At the END of a non-trivial change: 1-2 sentence summary + "push" ✅ + deploy status (deployed or awaiting).
- When in doubt → STOP and re-read this file. Better to delay than to drift.
</important>
